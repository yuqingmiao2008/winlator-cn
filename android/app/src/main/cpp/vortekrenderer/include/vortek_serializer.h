#ifndef VORTEK_SERIALIZER_H
#define VORTEK_SERIALIZER_H

#include "vortek.h"

#ifdef VT_SERVER
extern bool vortekSerializerCastVkObject;
#endif

static inline int vt_sizeof_VkInstance(VkInstance val) {
    return 8;
}

static inline void vt_serialize_VkInstance(VkInstance val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkInstance(VkInstance val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkPhysicalDevice(VkPhysicalDevice val) {
    return 8;
}

static inline void vt_serialize_VkPhysicalDevice(VkPhysicalDevice val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkPhysicalDevice(VkPhysicalDevice val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkDevice(VkDevice val) {
    return 8;
}

static inline void vt_serialize_VkDevice(VkDevice val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkDevice(VkDevice val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkQueue(VkQueue val) {
    return 8;
}

static inline void vt_serialize_VkQueue(VkQueue val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkQueue(VkQueue val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkCommandBuffer(VkCommandBuffer val) {
    return 8;
}

static inline void vt_serialize_VkCommandBuffer(VkCommandBuffer val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkCommandBuffer(VkCommandBuffer val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkDeviceMemory(VkDeviceMemory val) {
    return 8;
}

static inline void vt_serialize_VkDeviceMemory(VkDeviceMemory val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkDeviceMemory(VkDeviceMemory val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkCommandPool(VkCommandPool val) {
    return 8;
}

static inline void vt_serialize_VkCommandPool(VkCommandPool val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkCommandPool(VkCommandPool val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkBuffer(VkBuffer val) {
    return 8;
}

static inline void vt_serialize_VkBuffer(VkBuffer val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkBuffer(VkBuffer val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkBufferView(VkBufferView val) {
    return 8;
}

static inline void vt_serialize_VkBufferView(VkBufferView val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkBufferView(VkBufferView val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkImage(VkImage val) {
    return 8;
}

static inline void vt_serialize_VkImage(VkImage val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkImage(VkImage val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkImageView(VkImageView val) {
    return 8;
}

static inline void vt_serialize_VkImageView(VkImageView val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkImageView(VkImageView val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkShaderModule(VkShaderModule val) {
    return 8;
}

static inline void vt_serialize_VkShaderModule(VkShaderModule val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkShaderModule(VkShaderModule val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkPipeline(VkPipeline val) {
    return 8;
}

static inline void vt_serialize_VkPipeline(VkPipeline val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkPipeline(VkPipeline val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkPipelineLayout(VkPipelineLayout val) {
    return 8;
}

static inline void vt_serialize_VkPipelineLayout(VkPipelineLayout val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkPipelineLayout(VkPipelineLayout val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkSampler(VkSampler val) {
    return 8;
}

static inline void vt_serialize_VkSampler(VkSampler val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkSampler(VkSampler val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkDescriptorSet(VkDescriptorSet val) {
    return 8;
}

static inline void vt_serialize_VkDescriptorSet(VkDescriptorSet val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkDescriptorSet(VkDescriptorSet val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkDescriptorSetLayout(VkDescriptorSetLayout val) {
    return 8;
}

static inline void vt_serialize_VkDescriptorSetLayout(VkDescriptorSetLayout val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkDescriptorSetLayout(VkDescriptorSetLayout val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkDescriptorPool(VkDescriptorPool val) {
    return 8;
}

static inline void vt_serialize_VkDescriptorPool(VkDescriptorPool val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkDescriptorPool(VkDescriptorPool val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkFence(VkFence val) {
    return 8;
}

static inline void vt_serialize_VkFence(VkFence val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkFence(VkFence val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkSemaphore(VkSemaphore val) {
    return 8;
}

static inline void vt_serialize_VkSemaphore(VkSemaphore val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkSemaphore(VkSemaphore val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkEvent(VkEvent val) {
    return 8;
}

static inline void vt_serialize_VkEvent(VkEvent val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkEvent(VkEvent val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkQueryPool(VkQueryPool val) {
    return 8;
}

static inline void vt_serialize_VkQueryPool(VkQueryPool val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkQueryPool(VkQueryPool val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkFramebuffer(VkFramebuffer val) {
    return 8;
}

static inline void vt_serialize_VkFramebuffer(VkFramebuffer val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkFramebuffer(VkFramebuffer val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkRenderPass(VkRenderPass val) {
    return 8;
}

static inline void vt_serialize_VkRenderPass(VkRenderPass val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkRenderPass(VkRenderPass val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkPipelineCache(VkPipelineCache val) {
    return 8;
}

static inline void vt_serialize_VkPipelineCache(VkPipelineCache val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkPipelineCache(VkPipelineCache val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkDescriptorUpdateTemplateKHR(VkDescriptorUpdateTemplateKHR val) {
    return 8;
}

static inline void vt_serialize_VkDescriptorUpdateTemplateKHR(VkDescriptorUpdateTemplateKHR val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkDescriptorUpdateTemplateKHR(VkDescriptorUpdateTemplateKHR val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkSamplerYcbcrConversion(VkSamplerYcbcrConversion val) {
    return 8;
}

static inline void vt_serialize_VkSamplerYcbcrConversion(VkSamplerYcbcrConversion val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkSamplerYcbcrConversion(VkSamplerYcbcrConversion val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkSamplerYcbcrConversionKHR(VkSamplerYcbcrConversionKHR val) {
    return 8;
}

static inline void vt_serialize_VkSamplerYcbcrConversionKHR(VkSamplerYcbcrConversionKHR val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkSamplerYcbcrConversionKHR(VkSamplerYcbcrConversionKHR val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkSurfaceKHR(VkSurfaceKHR val) {
    return 8;
}

static inline void vt_serialize_VkSurfaceKHR(VkSurfaceKHR val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkSurfaceKHR(VkSurfaceKHR val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkSwapchainKHR(VkSwapchainKHR val) {
    return 8;
}

static inline void vt_serialize_VkSwapchainKHR(VkSwapchainKHR val, char* outputBuffer) {
#ifdef VT_SERVER
    *(uint64_t*)(outputBuffer) = (uint64_t)val;
#else
    *(uint64_t*)(outputBuffer) = *(uint64_t*)(val);
#endif
}

static inline void vt_unserialize_VkSwapchainKHR(VkSwapchainKHR val, char* inputBuffer, MemoryPool* memoryPool) {
    *(uint64_t*)(val) = *(uint64_t*)(inputBuffer);
}

static inline int vt_sizeof_VkExtent3D(VkExtent3D* val) {
    int bufferSize = 0;
    bufferSize += 4; // width
    bufferSize += 4; // height
    bufferSize += 4; // depth
    return bufferSize;
}

static inline void vt_serialize_VkExtent3D(VkExtent3D* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->width;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->height;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->depth;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkExtent3D(VkExtent3D* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->width = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->height = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depth = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkOffset3D(VkOffset3D* val) {
    int bufferSize = 0;
    bufferSize += 4; // x
    bufferSize += 4; // y
    bufferSize += 4; // z
    return bufferSize;
}

static inline void vt_serialize_VkOffset3D(VkOffset3D* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(int32_t*)(outputBuffer + bufferOffset) = val->x;
    bufferOffset += 4;
    *(int32_t*)(outputBuffer + bufferOffset) = val->y;
    bufferOffset += 4;
    *(int32_t*)(outputBuffer + bufferOffset) = val->z;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkOffset3D(VkOffset3D* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->x = *(int32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->y = *(int32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->z = *(int32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkImageSubresourceLayers(VkImageSubresourceLayers* val) {
    int bufferSize = 0;
    bufferSize += 4; // aspectMask
    bufferSize += 4; // mipLevel
    bufferSize += 4; // baseArrayLayer
    bufferSize += 4; // layerCount
    return bufferSize;
}

static inline void vt_serialize_VkImageSubresourceLayers(VkImageSubresourceLayers* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkImageAspectFlags*)(outputBuffer + bufferOffset) = val->aspectMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->mipLevel;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->baseArrayLayer;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->layerCount;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkImageSubresourceLayers(VkImageSubresourceLayers* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->aspectMask = *(VkImageAspectFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->mipLevel = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->baseArrayLayer = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->layerCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkImageResolve2(VkImageResolve2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    bufferSize += vt_sizeof_VkOffset3D(&val->srcOffset);
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    bufferSize += vt_sizeof_VkOffset3D(&val->dstOffset);
    bufferSize += vt_sizeof_VkExtent3D(&val->extent);
    return bufferSize;
}

static inline void vt_serialize_VkImageResolve2(VkImageResolve2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    vt_serialize_VkImageSubresourceLayers(&val->srcSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    vt_serialize_VkOffset3D(&val->srcOffset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset3D(&val->srcOffset);
    vt_serialize_VkImageSubresourceLayers(&val->dstSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    vt_serialize_VkOffset3D(&val->dstOffset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset3D(&val->dstOffset);
    vt_serialize_VkExtent3D(&val->extent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);
}

static inline void vt_unserialize_VkImageResolve2(VkImageResolve2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    vt_unserialize_VkImageSubresourceLayers(&val->srcSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    vt_unserialize_VkOffset3D(&val->srcOffset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset3D(&val->srcOffset);
    vt_unserialize_VkImageSubresourceLayers(&val->dstSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    vt_unserialize_VkOffset3D(&val->dstOffset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset3D(&val->dstOffset);
    vt_unserialize_VkExtent3D(&val->extent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);
}

static inline int vt_sizeof_VkBufferImageCopy2(VkBufferImageCopy2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // bufferOffset
    bufferSize += 4; // bufferRowLength
    bufferSize += 4; // bufferImageHeight
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->imageSubresource);
    bufferSize += vt_sizeof_VkOffset3D(&val->imageOffset);
    bufferSize += vt_sizeof_VkExtent3D(&val->imageExtent);
    return bufferSize;
}

static inline void vt_serialize_VkBufferImageCopy2(VkBufferImageCopy2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->bufferOffset;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->bufferRowLength;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->bufferImageHeight;
    bufferOffset += 4;
    vt_serialize_VkImageSubresourceLayers(&val->imageSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->imageSubresource);
    vt_serialize_VkOffset3D(&val->imageOffset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset3D(&val->imageOffset);
    vt_serialize_VkExtent3D(&val->imageExtent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent3D(&val->imageExtent);
}

static inline void vt_unserialize_VkBufferImageCopy2(VkBufferImageCopy2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->bufferOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->bufferRowLength = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->bufferImageHeight = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkImageSubresourceLayers(&val->imageSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->imageSubresource);
    vt_unserialize_VkOffset3D(&val->imageOffset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset3D(&val->imageOffset);
    vt_unserialize_VkExtent3D(&val->imageExtent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent3D(&val->imageExtent);
}

static inline int vt_sizeof_VkImageBlit2(VkImageBlit2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    bufferSize += 2 * vt_sizeof_VkOffset3D(&val->srcOffsets[0]);
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    bufferSize += 2 * vt_sizeof_VkOffset3D(&val->dstOffsets[0]);
    return bufferSize;
}

static inline void vt_serialize_VkImageBlit2(VkImageBlit2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    vt_serialize_VkImageSubresourceLayers(&val->srcSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    memcpy(outputBuffer + bufferOffset, val->srcOffsets, 2 * vt_sizeof_VkOffset3D(&val->srcOffsets[0]));
    bufferOffset += 2 * vt_sizeof_VkOffset3D(&val->srcOffsets[0]);
    vt_serialize_VkImageSubresourceLayers(&val->dstSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    memcpy(outputBuffer + bufferOffset, val->dstOffsets, 2 * vt_sizeof_VkOffset3D(&val->dstOffsets[0]));
    bufferOffset += 2 * vt_sizeof_VkOffset3D(&val->dstOffsets[0]);
}

static inline void vt_unserialize_VkImageBlit2(VkImageBlit2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    vt_unserialize_VkImageSubresourceLayers(&val->srcSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    memcpy(val->srcOffsets, inputBuffer + bufferOffset, 2 * vt_sizeof_VkOffset3D(&val->srcOffsets[0]));
    bufferOffset += 2 * vt_sizeof_VkOffset3D(&val->srcOffsets[0]);
    vt_unserialize_VkImageSubresourceLayers(&val->dstSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    memcpy(val->dstOffsets, inputBuffer + bufferOffset, 2 * vt_sizeof_VkOffset3D(&val->dstOffsets[0]));
    bufferOffset += 2 * vt_sizeof_VkOffset3D(&val->dstOffsets[0]);
}

static inline int vt_sizeof_VkImageCopy2(VkImageCopy2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    bufferSize += vt_sizeof_VkOffset3D(&val->srcOffset);
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    bufferSize += vt_sizeof_VkOffset3D(&val->dstOffset);
    bufferSize += vt_sizeof_VkExtent3D(&val->extent);
    return bufferSize;
}

static inline void vt_serialize_VkImageCopy2(VkImageCopy2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    vt_serialize_VkImageSubresourceLayers(&val->srcSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    vt_serialize_VkOffset3D(&val->srcOffset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset3D(&val->srcOffset);
    vt_serialize_VkImageSubresourceLayers(&val->dstSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    vt_serialize_VkOffset3D(&val->dstOffset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset3D(&val->dstOffset);
    vt_serialize_VkExtent3D(&val->extent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);
}

static inline void vt_unserialize_VkImageCopy2(VkImageCopy2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    vt_unserialize_VkImageSubresourceLayers(&val->srcSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    vt_unserialize_VkOffset3D(&val->srcOffset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset3D(&val->srcOffset);
    vt_unserialize_VkImageSubresourceLayers(&val->dstSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    vt_unserialize_VkOffset3D(&val->dstOffset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset3D(&val->dstOffset);
    vt_unserialize_VkExtent3D(&val->extent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);
}

static inline int vt_sizeof_VkBufferCopy2(VkBufferCopy2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // srcOffset
    bufferSize += 8; // dstOffset
    bufferSize += 8; // size
    return bufferSize;
}

static inline void vt_serialize_VkBufferCopy2(VkBufferCopy2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->srcOffset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->dstOffset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkBufferCopy2(VkBufferCopy2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->srcOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->dstOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkCommandBufferInheritanceRenderingInfo(VkCommandBufferInheritanceRenderingInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // viewMask
    bufferSize += 4; // colorAttachmentCount
    bufferSize += 4;
    bufferSize += val->pColorAttachmentFormats ? val->colorAttachmentCount * 4 : 0;
    bufferSize += 4; // depthAttachmentFormat
    bufferSize += 4; // stencilAttachmentFormat
    bufferSize += 4; // rasterizationSamples
    return bufferSize;
}

static inline void vt_serialize_VkCommandBufferInheritanceRenderingInfo(VkCommandBufferInheritanceRenderingInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkRenderingFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->viewMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->colorAttachmentCount;
    bufferOffset += 4;

    if (val->pColorAttachmentFormats) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pColorAttachmentFormats, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkFormat*)(outputBuffer + bufferOffset) = val->depthAttachmentFormat;
    bufferOffset += 4;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->stencilAttachmentFormat;
    bufferOffset += 4;
    *(VkSampleCountFlagBits*)(outputBuffer + bufferOffset) = val->rasterizationSamples;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkCommandBufferInheritanceRenderingInfo(VkCommandBufferInheritanceRenderingInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkRenderingFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->viewMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->colorAttachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkFormat* arrValues = vt_alloc(memoryPool, dataSize6 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize6 * 4);
        bufferOffset += dataSize6 * 4;
        val->pColorAttachmentFormats = arrValues;
    }
    else val->pColorAttachmentFormats = NULL;
    val->depthAttachmentFormat = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stencilAttachmentFormat = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->rasterizationSamples = *(VkSampleCountFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkCommandBufferInheritanceConditionalRenderingInfoEXT(VkCommandBufferInheritanceConditionalRenderingInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // conditionalRenderingEnable
    return bufferSize;
}

static inline void vt_serialize_VkCommandBufferInheritanceConditionalRenderingInfoEXT(VkCommandBufferInheritanceConditionalRenderingInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->conditionalRenderingEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkCommandBufferInheritanceConditionalRenderingInfoEXT(VkCommandBufferInheritanceConditionalRenderingInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->conditionalRenderingEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkCommandBufferInheritanceInfo(VkCommandBufferInheritanceInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_COMMAND_BUFFER_INHERITANCE_CONDITIONAL_RENDERING_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkCommandBufferInheritanceConditionalRenderingInfoEXT((VkCommandBufferInheritanceConditionalRenderingInfoEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_COMMAND_BUFFER_INHERITANCE_RENDERING_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkCommandBufferInheritanceRenderingInfo((VkCommandBufferInheritanceRenderingInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkRenderPass(val->renderPass);
    bufferSize += 4; // subpass
    bufferSize += vt_sizeof_VkFramebuffer(val->framebuffer);
    bufferSize += 4; // occlusionQueryEnable
    bufferSize += 4; // queryFlags
    bufferSize += 4; // pipelineStatistics
    return bufferSize;
}

static inline void vt_serialize_VkCommandBufferInheritanceInfo(VkCommandBufferInheritanceInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_COMMAND_BUFFER_INHERITANCE_CONDITIONAL_RENDERING_INFO_EXT: {
                    int itemSize = vt_sizeof_VkCommandBufferInheritanceConditionalRenderingInfoEXT((VkCommandBufferInheritanceConditionalRenderingInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkCommandBufferInheritanceConditionalRenderingInfoEXT((VkCommandBufferInheritanceConditionalRenderingInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_COMMAND_BUFFER_INHERITANCE_RENDERING_INFO: {
                    int itemSize = vt_sizeof_VkCommandBufferInheritanceRenderingInfo((VkCommandBufferInheritanceRenderingInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkCommandBufferInheritanceRenderingInfo((VkCommandBufferInheritanceRenderingInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkRenderPass(val->renderPass, outputBuffer + bufferOffset);
#else
    VkObject* renderPassObject = VkObject_fromHandle(val->renderPass);
    vt_serialize_VkRenderPass((VkRenderPass)&renderPassObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkRenderPass(val->renderPass);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subpass;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkFramebuffer(val->framebuffer, outputBuffer + bufferOffset);
#else
    VkObject* framebufferObject = VkObject_fromHandle(val->framebuffer);
    vt_serialize_VkFramebuffer((VkFramebuffer)&framebufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkFramebuffer(val->framebuffer);
    *(VkBool32*)(outputBuffer + bufferOffset) = val->occlusionQueryEnable;
    bufferOffset += 4;
    *(VkQueryControlFlags*)(outputBuffer + bufferOffset) = val->queryFlags;
    bufferOffset += 4;
    *(VkQueryPipelineStatisticFlags*)(outputBuffer + bufferOffset) = val->pipelineStatistics;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkCommandBufferInheritanceInfo(VkCommandBufferInheritanceInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_COMMAND_BUFFER_INHERITANCE_CONDITIONAL_RENDERING_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkCommandBufferInheritanceConditionalRenderingInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_COMMAND_BUFFER_INHERITANCE_CONDITIONAL_RENDERING_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkCommandBufferInheritanceConditionalRenderingInfoEXT));
                    vt_unserialize_VkCommandBufferInheritanceConditionalRenderingInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_COMMAND_BUFFER_INHERITANCE_RENDERING_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkCommandBufferInheritanceRenderingInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_COMMAND_BUFFER_INHERITANCE_RENDERING_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkCommandBufferInheritanceRenderingInfo));
                    vt_unserialize_VkCommandBufferInheritanceRenderingInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);

    uint64_t renderPassId;
    vt_unserialize_VkRenderPass((VkRenderPass)&renderPassId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->renderPass = VkObject_fromId(renderPassId);
#else
    VkObject* renderPassObject = VkObject_create(VK_OBJECT_TYPE_RENDER_PASS, renderPassId);
    val->renderPass = VkObject_toHandle(renderPassObject);
#endif
    bufferOffset += vt_sizeof_VkRenderPass(val->renderPass);
    val->subpass = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t framebufferId;
    vt_unserialize_VkFramebuffer((VkFramebuffer)&framebufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->framebuffer = VkObject_fromId(framebufferId);
#else
    VkObject* framebufferObject = VkObject_create(VK_OBJECT_TYPE_FRAMEBUFFER, framebufferId);
    val->framebuffer = VkObject_toHandle(framebufferObject);
#endif
    bufferOffset += vt_sizeof_VkFramebuffer(val->framebuffer);
    val->occlusionQueryEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queryFlags = *(VkQueryControlFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->pipelineStatistics = *(VkQueryPipelineStatisticFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkAttachmentReference(VkAttachmentReference* val) {
    int bufferSize = 0;
    bufferSize += 4; // attachment
    bufferSize += 4; // layout
    return bufferSize;
}

static inline void vt_serialize_VkAttachmentReference(VkAttachmentReference* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->attachment;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->layout;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkAttachmentReference(VkAttachmentReference* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->attachment = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->layout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSubpassDependency(VkSubpassDependency* val) {
    int bufferSize = 0;
    bufferSize += 4; // srcSubpass
    bufferSize += 4; // dstSubpass
    bufferSize += 4; // srcStageMask
    bufferSize += 4; // dstStageMask
    bufferSize += 4; // srcAccessMask
    bufferSize += 4; // dstAccessMask
    bufferSize += 4; // dependencyFlags
    return bufferSize;
}

static inline void vt_serialize_VkSubpassDependency(VkSubpassDependency* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->srcSubpass;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstSubpass;
    bufferOffset += 4;
    *(VkPipelineStageFlags*)(outputBuffer + bufferOffset) = val->srcStageMask;
    bufferOffset += 4;
    *(VkPipelineStageFlags*)(outputBuffer + bufferOffset) = val->dstStageMask;
    bufferOffset += 4;
    *(VkAccessFlags*)(outputBuffer + bufferOffset) = val->srcAccessMask;
    bufferOffset += 4;
    *(VkAccessFlags*)(outputBuffer + bufferOffset) = val->dstAccessMask;
    bufferOffset += 4;
    *(VkDependencyFlags*)(outputBuffer + bufferOffset) = val->dependencyFlags;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSubpassDependency(VkSubpassDependency* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->srcSubpass = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstSubpass = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcStageMask = *(VkPipelineStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstStageMask = *(VkPipelineStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcAccessMask = *(VkAccessFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstAccessMask = *(VkAccessFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dependencyFlags = *(VkDependencyFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSubpassDescription(VkSubpassDescription* val) {
    int bufferSize = 0;
    bufferSize += 4; // flags
    bufferSize += 4; // pipelineBindPoint
    bufferSize += 4; // inputAttachmentCount
    bufferSize += 4;
    if (val->pInputAttachments) {
        int arrSize = val->inputAttachmentCount ? val->inputAttachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkAttachmentReference(&val->pInputAttachments[i]);
        }
    }
    bufferSize += 4; // colorAttachmentCount
    bufferSize += 4;
    if (val->pColorAttachments) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkAttachmentReference(&val->pColorAttachments[i]);
        }
    }
    bufferSize += 4;
    if (val->pResolveAttachments) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkAttachmentReference(&val->pResolveAttachments[i]);
        }
    }
    bufferSize += 4;
    bufferSize += val->pDepthStencilAttachment ? vt_sizeof_VkAttachmentReference(val->pDepthStencilAttachment) : 0;
    bufferSize += 4; // preserveAttachmentCount
    bufferSize += 4;
    bufferSize += val->pPreserveAttachments ? val->preserveAttachmentCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkSubpassDescription(VkSubpassDescription* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkSubpassDescriptionFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkPipelineBindPoint*)(outputBuffer + bufferOffset) = val->pipelineBindPoint;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->inputAttachmentCount;
    bufferOffset += 4;

    if (val->pInputAttachments) {
        int arrSize = val->inputAttachmentCount ? val->inputAttachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkAttachmentReference(&val->pInputAttachments[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkAttachmentReference(&val->pInputAttachments[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->colorAttachmentCount;
    bufferOffset += 4;

    if (val->pColorAttachments) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkAttachmentReference(&val->pColorAttachments[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkAttachmentReference(&val->pColorAttachments[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pResolveAttachments) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkAttachmentReference(&val->pResolveAttachments[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkAttachmentReference(&val->pResolveAttachments[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pDepthStencilAttachment) {
        int itemSize = vt_sizeof_VkAttachmentReference(val->pDepthStencilAttachment);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkAttachmentReference(val->pDepthStencilAttachment, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->preserveAttachmentCount;
    bufferOffset += 4;

    if (val->pPreserveAttachments) {
        int arrSize = val->preserveAttachmentCount ? val->preserveAttachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pPreserveAttachments, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkSubpassDescription(VkSubpassDescription* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->flags = *(VkSubpassDescriptionFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->pipelineBindPoint = *(VkPipelineBindPoint*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->inputAttachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkAttachmentReference* arrValues = vt_alloc(memoryPool, dataSize4 * sizeof(VkAttachmentReference));
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkAttachmentReference(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pInputAttachments = arrValues;
    }
    else val->pInputAttachments = NULL;
    val->colorAttachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkAttachmentReference* arrValues = vt_alloc(memoryPool, dataSize6 * sizeof(VkAttachmentReference));
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkAttachmentReference(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pColorAttachments = arrValues;
    }
    else val->pColorAttachments = NULL;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkAttachmentReference* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkAttachmentReference));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkAttachmentReference(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pResolveAttachments = arrValues;
    }
    else val->pResolveAttachments = NULL;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        val->pDepthStencilAttachment = vt_alloc(memoryPool, sizeof(VkAttachmentReference));
        vt_unserialize_VkAttachmentReference(val->pDepthStencilAttachment, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize8;
    }
    else val->pDepthStencilAttachment = NULL;
    val->preserveAttachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize10 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize10 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize10 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize10 * 4);
        bufferOffset += dataSize10 * 4;
        val->pPreserveAttachments = arrValues;
    }
    else val->pPreserveAttachments = NULL;
}

static inline int vt_sizeof_VkAttachmentDescription(VkAttachmentDescription* val) {
    int bufferSize = 0;
    bufferSize += 4; // flags
    bufferSize += 4; // format
    bufferSize += 4; // samples
    bufferSize += 4; // loadOp
    bufferSize += 4; // storeOp
    bufferSize += 4; // stencilLoadOp
    bufferSize += 4; // stencilStoreOp
    bufferSize += 4; // initialLayout
    bufferSize += 4; // finalLayout
    return bufferSize;
}

static inline void vt_serialize_VkAttachmentDescription(VkAttachmentDescription* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkAttachmentDescriptionFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->format;
    bufferOffset += 4;
    *(VkSampleCountFlagBits*)(outputBuffer + bufferOffset) = val->samples;
    bufferOffset += 4;
    *(VkAttachmentLoadOp*)(outputBuffer + bufferOffset) = val->loadOp;
    bufferOffset += 4;
    *(VkAttachmentStoreOp*)(outputBuffer + bufferOffset) = val->storeOp;
    bufferOffset += 4;
    *(VkAttachmentLoadOp*)(outputBuffer + bufferOffset) = val->stencilLoadOp;
    bufferOffset += 4;
    *(VkAttachmentStoreOp*)(outputBuffer + bufferOffset) = val->stencilStoreOp;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->initialLayout;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->finalLayout;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkAttachmentDescription(VkAttachmentDescription* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->flags = *(VkAttachmentDescriptionFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->samples = *(VkSampleCountFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->loadOp = *(VkAttachmentLoadOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storeOp = *(VkAttachmentStoreOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stencilLoadOp = *(VkAttachmentLoadOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stencilStoreOp = *(VkAttachmentStoreOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->initialLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->finalLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDescriptorBufferInfo(VkDescriptorBufferInfo* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkBuffer(val->buffer);
    bufferSize += 8; // offset
    bufferSize += 8; // range
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorBufferInfo(VkDescriptorBufferInfo* val, char* outputBuffer) {
    int bufferOffset = 0;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->buffer, outputBuffer + bufferOffset);
#else
    VkObject* bufferObject = VkObject_fromHandle(val->buffer);
    vt_serialize_VkBuffer((VkBuffer)&bufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->offset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->range;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkDescriptorBufferInfo(VkDescriptorBufferInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    uint64_t bufferId;
    vt_unserialize_VkBuffer((VkBuffer)&bufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->buffer = VkObject_fromId(bufferId);
#else
    VkObject* bufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, bufferId);
    val->buffer = VkObject_toHandle(bufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    val->offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->range = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkDescriptorImageInfo(VkDescriptorImageInfo* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkSampler(val->sampler);
    bufferSize += vt_sizeof_VkImageView(val->imageView);
    bufferSize += 4; // imageLayout
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorImageInfo(VkDescriptorImageInfo* val, char* outputBuffer) {
    int bufferOffset = 0;

#ifdef VT_SERVER
    vt_serialize_VkSampler(val->sampler, outputBuffer + bufferOffset);
#else
    VkObject* samplerObject = VkObject_fromHandle(val->sampler);
    vt_serialize_VkSampler((VkSampler)&samplerObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkSampler(val->sampler);

#ifdef VT_SERVER
    vt_serialize_VkImageView(val->imageView, outputBuffer + bufferOffset);
#else
    VkObject* imageViewObject = VkObject_fromHandle(val->imageView);
    vt_serialize_VkImageView((VkImageView)&imageViewObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImageView(val->imageView);
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->imageLayout;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDescriptorImageInfo(VkDescriptorImageInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    uint64_t samplerId;
    vt_unserialize_VkSampler((VkSampler)&samplerId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->sampler = VkObject_fromId(samplerId);
#else
    VkObject* samplerObject = VkObject_create(VK_OBJECT_TYPE_SAMPLER, samplerId);
    val->sampler = VkObject_toHandle(samplerObject);
#endif
    bufferOffset += vt_sizeof_VkSampler(val->sampler);

    uint64_t imageViewId;
    vt_unserialize_VkImageView((VkImageView)&imageViewId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->imageView = VkObject_fromId(imageViewId);
#else
    VkObject* imageViewObject = VkObject_create(VK_OBJECT_TYPE_IMAGE_VIEW, imageViewId);
    val->imageView = VkObject_toHandle(imageViewObject);
#endif
    bufferOffset += vt_sizeof_VkImageView(val->imageView);
    val->imageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDescriptorPoolSize(VkDescriptorPoolSize* val) {
    int bufferSize = 0;
    bufferSize += 4; // type
    bufferSize += 4; // descriptorCount
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorPoolSize(VkDescriptorPoolSize* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkDescriptorType*)(outputBuffer + bufferOffset) = val->type;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->descriptorCount;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDescriptorPoolSize(VkDescriptorPoolSize* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->type = *(VkDescriptorType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDescriptorSetLayoutBinding(VkDescriptorSetLayoutBinding* val) {
    int bufferSize = 0;
    bufferSize += 4; // binding
    bufferSize += 4; // descriptorType
    bufferSize += 4; // descriptorCount
    bufferSize += 4; // stageFlags
    bufferSize += 4;
    if (val->pImmutableSamplers) {
        for (int i = 0; i < val->descriptorCount; i++) {
            bufferSize += vt_sizeof_VkSampler(val->pImmutableSamplers[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorSetLayoutBinding(VkDescriptorSetLayoutBinding* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->binding;
    bufferOffset += 4;
    *(VkDescriptorType*)(outputBuffer + bufferOffset) = val->descriptorType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->descriptorCount;
    bufferOffset += 4;
    *(VkShaderStageFlags*)(outputBuffer + bufferOffset) = val->stageFlags;
    bufferOffset += 4;

    if (val->pImmutableSamplers) {
        *(int*)(outputBuffer + bufferOffset) = val->descriptorCount;
        bufferOffset += 4;
        for (int i = 0; i < val->descriptorCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkSampler(val->pImmutableSamplers[i], outputBuffer + bufferOffset);
#else
            VkObject* immutableSamplersObject = VkObject_fromHandle(val->pImmutableSamplers[i]);
            vt_serialize_VkSampler((VkSampler)&immutableSamplersObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkSampler(val->pImmutableSamplers[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDescriptorSetLayoutBinding(VkDescriptorSetLayoutBinding* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->binding = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorType = *(VkDescriptorType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stageFlags = *(VkShaderStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkSampler* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkSampler));
        for (int i = 0; i < dataSize5; i++) {
            uint64_t immutableSamplersId;
            vt_unserialize_VkSampler((VkSampler)&immutableSamplersId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(immutableSamplersId);
#else
                VkObject* immutableSamplersObject = VkObject_create(VK_OBJECT_TYPE_SAMPLER, immutableSamplersId);
                arrValues[i] = VkObject_toHandle(immutableSamplersObject);
#endif
                bufferOffset += vt_sizeof_VkSampler(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkSampler(VK_NULL_HANDLE);
        }
        val->pImmutableSamplers = arrValues;
    }
    else val->pImmutableSamplers = NULL;
}

static inline int vt_sizeof_VkSpecializationMapEntry(VkSpecializationMapEntry* val) {
    int bufferSize = 0;
    bufferSize += 4; // constantID
    bufferSize += 4; // offset
    bufferSize += 8; // size
    return bufferSize;
}

static inline void vt_serialize_VkSpecializationMapEntry(VkSpecializationMapEntry* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->constantID;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->offset;
    bufferOffset += 4;
    *(size_t*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkSpecializationMapEntry(VkSpecializationMapEntry* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->constantID = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->offset = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->size = *(size_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkImageSubresource(VkImageSubresource* val) {
    int bufferSize = 0;
    bufferSize += 4; // aspectMask
    bufferSize += 4; // mipLevel
    bufferSize += 4; // arrayLayer
    return bufferSize;
}

static inline void vt_serialize_VkImageSubresource(VkImageSubresource* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkImageAspectFlags*)(outputBuffer + bufferOffset) = val->aspectMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->mipLevel;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->arrayLayer;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkImageSubresource(VkImageSubresource* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->aspectMask = *(VkImageAspectFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->mipLevel = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->arrayLayer = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSpecializationInfo(VkSpecializationInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // mapEntryCount
    bufferSize += 4;
    if (val->pMapEntries) {
        int arrSize = val->mapEntryCount ? val->mapEntryCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSpecializationMapEntry(&val->pMapEntries[i]);
        }
    }
    bufferSize += 8; // dataSize
    bufferSize += 4;
    bufferSize += val->pData ? val->dataSize : 0;
    return bufferSize;
}

static inline void vt_serialize_VkSpecializationInfo(VkSpecializationInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->mapEntryCount;
    bufferOffset += 4;

    if (val->pMapEntries) {
        int arrSize = val->mapEntryCount ? val->mapEntryCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSpecializationMapEntry(&val->pMapEntries[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSpecializationMapEntry(&val->pMapEntries[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(size_t*)(outputBuffer + bufferOffset) = val->dataSize;
    bufferOffset += 8;

    if (val->pData) {
        *(int*)(outputBuffer + bufferOffset) = val->dataSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pData, val->dataSize);
        bufferOffset += val->dataSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkSpecializationInfo(VkSpecializationInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->mapEntryCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        VkSpecializationMapEntry* arrValues = vt_alloc(memoryPool, dataSize2 * sizeof(VkSpecializationMapEntry));
        for (int i = 0; i < dataSize2; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSpecializationMapEntry(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pMapEntries = arrValues;
    }
    else val->pMapEntries = NULL;
    val->dataSize = *(size_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        val->pData = vt_alloc(memoryPool, dataSize4);
        memcpy(val->pData, inputBuffer + bufferOffset, dataSize4);
        bufferOffset += dataSize4;
    }
    else val->pData = NULL;
}

static inline int vt_sizeof_VkPipelineShaderStageModuleIdentifierCreateInfoEXT(VkPipelineShaderStageModuleIdentifierCreateInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // identifierSize
    bufferSize += 4;
    bufferSize += val->pIdentifier ? val->identifierSize * 1 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkPipelineShaderStageModuleIdentifierCreateInfoEXT(VkPipelineShaderStageModuleIdentifierCreateInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->identifierSize;
    bufferOffset += 4;

    if (val->pIdentifier) {
        int arrSize = val->identifierSize ? val->identifierSize : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pIdentifier, arrSize * 1);
        bufferOffset += arrSize * 1;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkPipelineShaderStageModuleIdentifierCreateInfoEXT(VkPipelineShaderStageModuleIdentifierCreateInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->identifierSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        uint8_t* arrValues = vt_alloc(memoryPool, dataSize4 * 1);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 1);
        bufferOffset += dataSize4 * 1;
        val->pIdentifier = arrValues;
    }
    else val->pIdentifier = NULL;
}

static inline int vt_sizeof_VkPipelineShaderStageRequiredSubgroupSizeCreateInfo(VkPipelineShaderStageRequiredSubgroupSizeCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // requiredSubgroupSize
    return bufferSize;
}

static inline void vt_serialize_VkPipelineShaderStageRequiredSubgroupSizeCreateInfo(VkPipelineShaderStageRequiredSubgroupSizeCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->requiredSubgroupSize;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineShaderStageRequiredSubgroupSizeCreateInfo(VkPipelineShaderStageRequiredSubgroupSizeCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->requiredSubgroupSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkShaderModuleCreateInfo(VkShaderModuleCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 8; // codeSize
    bufferSize += 4;
    bufferSize += val->codeSize;
    return bufferSize;
}

static inline void vt_serialize_VkShaderModuleCreateInfo(VkShaderModuleCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkShaderModuleCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(size_t*)(outputBuffer + bufferOffset) = val->codeSize;
    bufferOffset += 8;
    *(int*)(outputBuffer + bufferOffset) = val->codeSize;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, (char*)val->pCode, val->codeSize);
    bufferOffset += val->codeSize;
}

static inline void vt_unserialize_VkShaderModuleCreateInfo(VkShaderModuleCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkShaderModuleCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->codeSize = *(size_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    char* arrValues = vt_alloc(memoryPool, dataSize5);
    memcpy(arrValues, inputBuffer + bufferOffset, dataSize5);
    val->pCode = (uint32_t*)arrValues;
    bufferOffset += dataSize5;
}

static inline int vt_sizeof_VkVertexInputBindingDivisorDescriptionKHR(VkVertexInputBindingDivisorDescriptionKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // binding
    bufferSize += 4; // divisor
    return bufferSize;
}

static inline void vt_serialize_VkVertexInputBindingDivisorDescriptionKHR(VkVertexInputBindingDivisorDescriptionKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->binding;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->divisor;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkVertexInputBindingDivisorDescriptionKHR(VkVertexInputBindingDivisorDescriptionKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->binding = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->divisor = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkVertexInputAttributeDescription(VkVertexInputAttributeDescription* val) {
    int bufferSize = 0;
    bufferSize += 4; // location
    bufferSize += 4; // binding
    bufferSize += 4; // format
    bufferSize += 4; // offset
    return bufferSize;
}

static inline void vt_serialize_VkVertexInputAttributeDescription(VkVertexInputAttributeDescription* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->location;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->binding;
    bufferOffset += 4;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->format;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->offset;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkVertexInputAttributeDescription(VkVertexInputAttributeDescription* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->location = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->binding = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->offset = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkVertexInputBindingDescription(VkVertexInputBindingDescription* val) {
    int bufferSize = 0;
    bufferSize += 4; // binding
    bufferSize += 4; // stride
    bufferSize += 4; // inputRate
    return bufferSize;
}

static inline void vt_serialize_VkVertexInputBindingDescription(VkVertexInputBindingDescription* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->binding;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->stride;
    bufferOffset += 4;
    *(VkVertexInputRate*)(outputBuffer + bufferOffset) = val->inputRate;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkVertexInputBindingDescription(VkVertexInputBindingDescription* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->binding = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stride = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->inputRate = *(VkVertexInputRate*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineVertexInputDivisorStateCreateInfoKHR(VkPipelineVertexInputDivisorStateCreateInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // vertexBindingDivisorCount
    bufferSize += 4;
    if (val->pVertexBindingDivisors) {
        int arrSize = val->vertexBindingDivisorCount ? val->vertexBindingDivisorCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkVertexInputBindingDivisorDescriptionKHR(&val->pVertexBindingDivisors[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkPipelineVertexInputDivisorStateCreateInfoKHR(VkPipelineVertexInputDivisorStateCreateInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->vertexBindingDivisorCount;
    bufferOffset += 4;

    if (val->pVertexBindingDivisors) {
        int arrSize = val->vertexBindingDivisorCount ? val->vertexBindingDivisorCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkVertexInputBindingDivisorDescriptionKHR(&val->pVertexBindingDivisors[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkVertexInputBindingDivisorDescriptionKHR(&val->pVertexBindingDivisors[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkPipelineVertexInputDivisorStateCreateInfoKHR(VkPipelineVertexInputDivisorStateCreateInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->vertexBindingDivisorCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkVertexInputBindingDivisorDescriptionKHR* arrValues = vt_alloc(memoryPool, dataSize4 * sizeof(VkVertexInputBindingDivisorDescriptionKHR));
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkVertexInputBindingDivisorDescriptionKHR(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pVertexBindingDivisors = arrValues;
    }
    else val->pVertexBindingDivisors = NULL;
}

static inline int vt_sizeof_VkExtent2D(VkExtent2D* val) {
    int bufferSize = 0;
    bufferSize += 4; // width
    bufferSize += 4; // height
    return bufferSize;
}

static inline void vt_serialize_VkExtent2D(VkExtent2D* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->width;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->height;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkExtent2D(VkExtent2D* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->width = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->height = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkOffset2D(VkOffset2D* val) {
    int bufferSize = 0;
    bufferSize += 4; // x
    bufferSize += 4; // y
    return bufferSize;
}

static inline void vt_serialize_VkOffset2D(VkOffset2D* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(int32_t*)(outputBuffer + bufferOffset) = val->x;
    bufferOffset += 4;
    *(int32_t*)(outputBuffer + bufferOffset) = val->y;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkOffset2D(VkOffset2D* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->x = *(int32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->y = *(int32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineTessellationDomainOriginStateCreateInfo(VkPipelineTessellationDomainOriginStateCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // domainOrigin
    return bufferSize;
}

static inline void vt_serialize_VkPipelineTessellationDomainOriginStateCreateInfo(VkPipelineTessellationDomainOriginStateCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkTessellationDomainOrigin*)(outputBuffer + bufferOffset) = val->domainOrigin;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineTessellationDomainOriginStateCreateInfo(VkPipelineTessellationDomainOriginStateCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->domainOrigin = *(VkTessellationDomainOrigin*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkRect2D(VkRect2D* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkOffset2D(&val->offset);
    bufferSize += vt_sizeof_VkExtent2D(&val->extent);
    return bufferSize;
}

static inline void vt_serialize_VkRect2D(VkRect2D* val, char* outputBuffer) {
    int bufferOffset = 0;
    vt_serialize_VkOffset2D(&val->offset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset2D(&val->offset);
    vt_serialize_VkExtent2D(&val->extent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent2D(&val->extent);
}

static inline void vt_unserialize_VkRect2D(VkRect2D* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    vt_unserialize_VkOffset2D(&val->offset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset2D(&val->offset);
    vt_unserialize_VkExtent2D(&val->extent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent2D(&val->extent);
}

static inline int vt_sizeof_VkViewport(VkViewport* val) {
    int bufferSize = 0;
    bufferSize += 4; // x
    bufferSize += 4; // y
    bufferSize += 4; // width
    bufferSize += 4; // height
    bufferSize += 4; // minDepth
    bufferSize += 4; // maxDepth
    return bufferSize;
}

static inline void vt_serialize_VkViewport(VkViewport* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(float*)(outputBuffer + bufferOffset) = val->x;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->y;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->width;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->height;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->minDepth;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->maxDepth;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkViewport(VkViewport* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->x = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->y = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->width = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->height = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->minDepth = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDepth = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineViewportDepthClipControlCreateInfoEXT(VkPipelineViewportDepthClipControlCreateInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // negativeOneToOne
    return bufferSize;
}

static inline void vt_serialize_VkPipelineViewportDepthClipControlCreateInfoEXT(VkPipelineViewportDepthClipControlCreateInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->negativeOneToOne;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineViewportDepthClipControlCreateInfoEXT(VkPipelineViewportDepthClipControlCreateInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->negativeOneToOne = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineRasterizationProvokingVertexStateCreateInfoEXT(VkPipelineRasterizationProvokingVertexStateCreateInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // provokingVertexMode
    return bufferSize;
}

static inline void vt_serialize_VkPipelineRasterizationProvokingVertexStateCreateInfoEXT(VkPipelineRasterizationProvokingVertexStateCreateInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkProvokingVertexModeEXT*)(outputBuffer + bufferOffset) = val->provokingVertexMode;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineRasterizationProvokingVertexStateCreateInfoEXT(VkPipelineRasterizationProvokingVertexStateCreateInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->provokingVertexMode = *(VkProvokingVertexModeEXT*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineRasterizationDepthClipStateCreateInfoEXT(VkPipelineRasterizationDepthClipStateCreateInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // depthClipEnable
    return bufferSize;
}

static inline void vt_serialize_VkPipelineRasterizationDepthClipStateCreateInfoEXT(VkPipelineRasterizationDepthClipStateCreateInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkPipelineRasterizationDepthClipStateCreateFlagsEXT*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->depthClipEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineRasterizationDepthClipStateCreateInfoEXT(VkPipelineRasterizationDepthClipStateCreateInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkPipelineRasterizationDepthClipStateCreateFlagsEXT*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthClipEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSampleLocationEXT(VkSampleLocationEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // x
    bufferSize += 4; // y
    return bufferSize;
}

static inline void vt_serialize_VkSampleLocationEXT(VkSampleLocationEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(float*)(outputBuffer + bufferOffset) = val->x;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->y;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSampleLocationEXT(VkSampleLocationEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->x = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->y = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineRasterizationStateStreamCreateInfoEXT(VkPipelineRasterizationStateStreamCreateInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // rasterizationStream
    return bufferSize;
}

static inline void vt_serialize_VkPipelineRasterizationStateStreamCreateInfoEXT(VkPipelineRasterizationStateStreamCreateInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkPipelineRasterizationStateStreamCreateFlagsEXT*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->rasterizationStream;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineRasterizationStateStreamCreateInfoEXT(VkPipelineRasterizationStateStreamCreateInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkPipelineRasterizationStateStreamCreateFlagsEXT*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->rasterizationStream = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSampleLocationsInfoEXT(VkSampleLocationsInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // sampleLocationsPerPixel
    bufferSize += vt_sizeof_VkExtent2D(&val->sampleLocationGridSize);
    bufferSize += 4; // sampleLocationsCount
    bufferSize += 4;
    if (val->pSampleLocations) {
        int arrSize = val->sampleLocationsCount ? val->sampleLocationsCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSampleLocationEXT(&val->pSampleLocations[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkSampleLocationsInfoEXT(VkSampleLocationsInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkSampleCountFlagBits*)(outputBuffer + bufferOffset) = val->sampleLocationsPerPixel;
    bufferOffset += 4;
    vt_serialize_VkExtent2D(&val->sampleLocationGridSize, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent2D(&val->sampleLocationGridSize);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->sampleLocationsCount;
    bufferOffset += 4;

    if (val->pSampleLocations) {
        int arrSize = val->sampleLocationsCount ? val->sampleLocationsCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSampleLocationEXT(&val->pSampleLocations[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSampleLocationEXT(&val->pSampleLocations[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkSampleLocationsInfoEXT(VkSampleLocationsInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->sampleLocationsPerPixel = *(VkSampleCountFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkExtent2D(&val->sampleLocationGridSize, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent2D(&val->sampleLocationGridSize);
    val->sampleLocationsCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkSampleLocationEXT* arrValues = vt_alloc(memoryPool, dataSize6 * sizeof(VkSampleLocationEXT));
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSampleLocationEXT(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pSampleLocations = arrValues;
    }
    else val->pSampleLocations = NULL;
}

static inline int vt_sizeof_VkPipelineRasterizationConservativeStateCreateInfoEXT(VkPipelineRasterizationConservativeStateCreateInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // conservativeRasterizationMode
    bufferSize += 4; // extraPrimitiveOverestimationSize
    return bufferSize;
}

static inline void vt_serialize_VkPipelineRasterizationConservativeStateCreateInfoEXT(VkPipelineRasterizationConservativeStateCreateInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkPipelineRasterizationConservativeStateCreateFlagsEXT*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkConservativeRasterizationModeEXT*)(outputBuffer + bufferOffset) = val->conservativeRasterizationMode;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->extraPrimitiveOverestimationSize;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineRasterizationConservativeStateCreateInfoEXT(VkPipelineRasterizationConservativeStateCreateInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkPipelineRasterizationConservativeStateCreateFlagsEXT*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->conservativeRasterizationMode = *(VkConservativeRasterizationModeEXT*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extraPrimitiveOverestimationSize = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineSampleLocationsStateCreateInfoEXT(VkPipelineSampleLocationsStateCreateInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // sampleLocationsEnable
    bufferSize += vt_sizeof_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo);
    return bufferSize;
}

static inline void vt_serialize_VkPipelineSampleLocationsStateCreateInfoEXT(VkPipelineSampleLocationsStateCreateInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sampleLocationsEnable;
    bufferOffset += 4;
    vt_serialize_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo);
}

static inline void vt_unserialize_VkPipelineSampleLocationsStateCreateInfoEXT(VkPipelineSampleLocationsStateCreateInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->sampleLocationsEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo);
}

static inline int vt_sizeof_VkStencilOpState(VkStencilOpState* val) {
    int bufferSize = 0;
    bufferSize += 4; // failOp
    bufferSize += 4; // passOp
    bufferSize += 4; // depthFailOp
    bufferSize += 4; // compareOp
    bufferSize += 4; // compareMask
    bufferSize += 4; // writeMask
    bufferSize += 4; // reference
    return bufferSize;
}

static inline void vt_serialize_VkStencilOpState(VkStencilOpState* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStencilOp*)(outputBuffer + bufferOffset) = val->failOp;
    bufferOffset += 4;
    *(VkStencilOp*)(outputBuffer + bufferOffset) = val->passOp;
    bufferOffset += 4;
    *(VkStencilOp*)(outputBuffer + bufferOffset) = val->depthFailOp;
    bufferOffset += 4;
    *(VkCompareOp*)(outputBuffer + bufferOffset) = val->compareOp;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->compareMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->writeMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->reference;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkStencilOpState(VkStencilOpState* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->failOp = *(VkStencilOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->passOp = *(VkStencilOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthFailOp = *(VkStencilOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->compareOp = *(VkCompareOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->compareMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->writeMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->reference = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineColorBlendAttachmentState(VkPipelineColorBlendAttachmentState* val) {
    int bufferSize = 0;
    bufferSize += 4; // blendEnable
    bufferSize += 4; // srcColorBlendFactor
    bufferSize += 4; // dstColorBlendFactor
    bufferSize += 4; // colorBlendOp
    bufferSize += 4; // srcAlphaBlendFactor
    bufferSize += 4; // dstAlphaBlendFactor
    bufferSize += 4; // alphaBlendOp
    bufferSize += 4; // colorWriteMask
    return bufferSize;
}

static inline void vt_serialize_VkPipelineColorBlendAttachmentState(VkPipelineColorBlendAttachmentState* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->blendEnable;
    bufferOffset += 4;
    *(VkBlendFactor*)(outputBuffer + bufferOffset) = val->srcColorBlendFactor;
    bufferOffset += 4;
    *(VkBlendFactor*)(outputBuffer + bufferOffset) = val->dstColorBlendFactor;
    bufferOffset += 4;
    *(VkBlendOp*)(outputBuffer + bufferOffset) = val->colorBlendOp;
    bufferOffset += 4;
    *(VkBlendFactor*)(outputBuffer + bufferOffset) = val->srcAlphaBlendFactor;
    bufferOffset += 4;
    *(VkBlendFactor*)(outputBuffer + bufferOffset) = val->dstAlphaBlendFactor;
    bufferOffset += 4;
    *(VkBlendOp*)(outputBuffer + bufferOffset) = val->alphaBlendOp;
    bufferOffset += 4;
    *(VkColorComponentFlags*)(outputBuffer + bufferOffset) = val->colorWriteMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineColorBlendAttachmentState(VkPipelineColorBlendAttachmentState* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->blendEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcColorBlendFactor = *(VkBlendFactor*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstColorBlendFactor = *(VkBlendFactor*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->colorBlendOp = *(VkBlendOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcAlphaBlendFactor = *(VkBlendFactor*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstAlphaBlendFactor = *(VkBlendFactor*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->alphaBlendOp = *(VkBlendOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->colorWriteMask = *(VkColorComponentFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineColorWriteCreateInfoEXT(VkPipelineColorWriteCreateInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    bufferSize += val->pColorWriteEnables ? val->attachmentCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkPipelineColorWriteCreateInfoEXT(VkPipelineColorWriteCreateInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->attachmentCount;
    bufferOffset += 4;

    if (val->pColorWriteEnables) {
        int arrSize = val->attachmentCount ? val->attachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pColorWriteEnables, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkPipelineColorWriteCreateInfoEXT(VkPipelineColorWriteCreateInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkBool32* arrValues = vt_alloc(memoryPool, dataSize4 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
        val->pColorWriteEnables = arrValues;
    }
    else val->pColorWriteEnables = NULL;
}

static inline int vt_sizeof_VkPipelineDynamicStateCreateInfo(VkPipelineDynamicStateCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // dynamicStateCount
    bufferSize += 4;
    bufferSize += val->pDynamicStates ? val->dynamicStateCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkPipelineDynamicStateCreateInfo(VkPipelineDynamicStateCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkPipelineDynamicStateCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dynamicStateCount;
    bufferOffset += 4;

    if (val->pDynamicStates) {
        int arrSize = val->dynamicStateCount ? val->dynamicStateCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pDynamicStates, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkPipelineDynamicStateCreateInfo(VkPipelineDynamicStateCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkPipelineDynamicStateCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dynamicStateCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkDynamicState* arrValues = vt_alloc(memoryPool, dataSize5 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize5 * 4);
        bufferOffset += dataSize5 * 4;
        val->pDynamicStates = arrValues;
    }
    else val->pDynamicStates = NULL;
}

static inline int vt_sizeof_VkPipelineColorBlendStateCreateInfo(VkPipelineColorBlendStateCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PIPELINE_COLOR_WRITE_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineColorWriteCreateInfoEXT((VkPipelineColorWriteCreateInfoEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // logicOpEnable
    bufferSize += 4; // logicOp
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    if (val->pAttachments) {
        int arrSize = val->attachmentCount ? val->attachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkPipelineColorBlendAttachmentState(&val->pAttachments[i]);
        }
    }
    bufferSize += 4 * 4;
    return bufferSize;
}

static inline void vt_serialize_VkPipelineColorBlendStateCreateInfo(VkPipelineColorBlendStateCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PIPELINE_COLOR_WRITE_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkPipelineColorWriteCreateInfoEXT((VkPipelineColorWriteCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineColorWriteCreateInfoEXT((VkPipelineColorWriteCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkPipelineColorBlendStateCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->logicOpEnable;
    bufferOffset += 4;
    *(VkLogicOp*)(outputBuffer + bufferOffset) = val->logicOp;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->attachmentCount;
    bufferOffset += 4;

    if (val->pAttachments) {
        int arrSize = val->attachmentCount ? val->attachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkPipelineColorBlendAttachmentState(&val->pAttachments[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkPipelineColorBlendAttachmentState(&val->pAttachments[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    memcpy(outputBuffer + bufferOffset, val->blendConstants, 4 * 4);
    bufferOffset += 4 * 4;
}

static inline void vt_unserialize_VkPipelineColorBlendStateCreateInfo(VkPipelineColorBlendStateCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PIPELINE_COLOR_WRITE_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineColorWriteCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_COLOR_WRITE_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineColorWriteCreateInfoEXT));
                    vt_unserialize_VkPipelineColorWriteCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkPipelineColorBlendStateCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->logicOpEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->logicOp = *(VkLogicOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkPipelineColorBlendAttachmentState* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkPipelineColorBlendAttachmentState));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkPipelineColorBlendAttachmentState(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pAttachments = arrValues;
    }
    else val->pAttachments = NULL;
    memcpy(val->blendConstants, inputBuffer + bufferOffset, 4 * 4);
    bufferOffset += 4 * 4;
}

static inline int vt_sizeof_VkPipelineDepthStencilStateCreateInfo(VkPipelineDepthStencilStateCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // depthTestEnable
    bufferSize += 4; // depthWriteEnable
    bufferSize += 4; // depthCompareOp
    bufferSize += 4; // depthBoundsTestEnable
    bufferSize += 4; // stencilTestEnable
    bufferSize += vt_sizeof_VkStencilOpState(&val->front);
    bufferSize += vt_sizeof_VkStencilOpState(&val->back);
    bufferSize += 4; // minDepthBounds
    bufferSize += 4; // maxDepthBounds
    return bufferSize;
}

static inline void vt_serialize_VkPipelineDepthStencilStateCreateInfo(VkPipelineDepthStencilStateCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkPipelineDepthStencilStateCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->depthTestEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->depthWriteEnable;
    bufferOffset += 4;
    *(VkCompareOp*)(outputBuffer + bufferOffset) = val->depthCompareOp;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->depthBoundsTestEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->stencilTestEnable;
    bufferOffset += 4;
    vt_serialize_VkStencilOpState(&val->front, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkStencilOpState(&val->front);
    vt_serialize_VkStencilOpState(&val->back, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkStencilOpState(&val->back);
    *(float*)(outputBuffer + bufferOffset) = val->minDepthBounds;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->maxDepthBounds;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineDepthStencilStateCreateInfo(VkPipelineDepthStencilStateCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkPipelineDepthStencilStateCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthTestEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthWriteEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthCompareOp = *(VkCompareOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthBoundsTestEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stencilTestEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkStencilOpState(&val->front, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkStencilOpState(&val->front);
    vt_unserialize_VkStencilOpState(&val->back, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkStencilOpState(&val->back);
    val->minDepthBounds = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDepthBounds = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineMultisampleStateCreateInfo(VkPipelineMultisampleStateCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PIPELINE_SAMPLE_LOCATIONS_STATE_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineSampleLocationsStateCreateInfoEXT((VkPipelineSampleLocationsStateCreateInfoEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // rasterizationSamples
    bufferSize += 4; // sampleShadingEnable
    bufferSize += 4; // minSampleShading
    bufferSize += 4; // alphaToCoverageEnable
    bufferSize += 4; // alphaToOneEnable
    return bufferSize;
}

static inline void vt_serialize_VkPipelineMultisampleStateCreateInfo(VkPipelineMultisampleStateCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PIPELINE_SAMPLE_LOCATIONS_STATE_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkPipelineSampleLocationsStateCreateInfoEXT((VkPipelineSampleLocationsStateCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineSampleLocationsStateCreateInfoEXT((VkPipelineSampleLocationsStateCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkPipelineMultisampleStateCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkSampleCountFlagBits*)(outputBuffer + bufferOffset) = val->rasterizationSamples;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sampleShadingEnable;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->minSampleShading;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->alphaToCoverageEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->alphaToOneEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineMultisampleStateCreateInfo(VkPipelineMultisampleStateCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PIPELINE_SAMPLE_LOCATIONS_STATE_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineSampleLocationsStateCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_SAMPLE_LOCATIONS_STATE_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineSampleLocationsStateCreateInfoEXT));
                    vt_unserialize_VkPipelineSampleLocationsStateCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkPipelineMultisampleStateCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->rasterizationSamples = *(VkSampleCountFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sampleShadingEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->minSampleShading = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->pSampleMask = NULL;
    val->alphaToCoverageEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->alphaToOneEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineRasterizationStateCreateInfo(VkPipelineRasterizationStateCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_CONSERVATIVE_STATE_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineRasterizationConservativeStateCreateInfoEXT((VkPipelineRasterizationConservativeStateCreateInfoEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_STATE_STREAM_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineRasterizationStateStreamCreateInfoEXT((VkPipelineRasterizationStateStreamCreateInfoEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_DEPTH_CLIP_STATE_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineRasterizationDepthClipStateCreateInfoEXT((VkPipelineRasterizationDepthClipStateCreateInfoEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_PROVOKING_VERTEX_STATE_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineRasterizationProvokingVertexStateCreateInfoEXT((VkPipelineRasterizationProvokingVertexStateCreateInfoEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // depthClampEnable
    bufferSize += 4; // rasterizerDiscardEnable
    bufferSize += 4; // polygonMode
    bufferSize += 4; // cullMode
    bufferSize += 4; // frontFace
    bufferSize += 4; // depthBiasEnable
    bufferSize += 4; // depthBiasConstantFactor
    bufferSize += 4; // depthBiasClamp
    bufferSize += 4; // depthBiasSlopeFactor
    bufferSize += 4; // lineWidth
    return bufferSize;
}

static inline void vt_serialize_VkPipelineRasterizationStateCreateInfo(VkPipelineRasterizationStateCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_CONSERVATIVE_STATE_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkPipelineRasterizationConservativeStateCreateInfoEXT((VkPipelineRasterizationConservativeStateCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineRasterizationConservativeStateCreateInfoEXT((VkPipelineRasterizationConservativeStateCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_STATE_STREAM_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkPipelineRasterizationStateStreamCreateInfoEXT((VkPipelineRasterizationStateStreamCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineRasterizationStateStreamCreateInfoEXT((VkPipelineRasterizationStateStreamCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_DEPTH_CLIP_STATE_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkPipelineRasterizationDepthClipStateCreateInfoEXT((VkPipelineRasterizationDepthClipStateCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineRasterizationDepthClipStateCreateInfoEXT((VkPipelineRasterizationDepthClipStateCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_PROVOKING_VERTEX_STATE_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkPipelineRasterizationProvokingVertexStateCreateInfoEXT((VkPipelineRasterizationProvokingVertexStateCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineRasterizationProvokingVertexStateCreateInfoEXT((VkPipelineRasterizationProvokingVertexStateCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkPipelineRasterizationStateCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->depthClampEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->rasterizerDiscardEnable;
    bufferOffset += 4;
    *(VkPolygonMode*)(outputBuffer + bufferOffset) = val->polygonMode;
    bufferOffset += 4;
    *(VkCullModeFlags*)(outputBuffer + bufferOffset) = val->cullMode;
    bufferOffset += 4;
    *(VkFrontFace*)(outputBuffer + bufferOffset) = val->frontFace;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->depthBiasEnable;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->depthBiasConstantFactor;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->depthBiasClamp;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->depthBiasSlopeFactor;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->lineWidth;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineRasterizationStateCreateInfo(VkPipelineRasterizationStateCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_CONSERVATIVE_STATE_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineRasterizationConservativeStateCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_CONSERVATIVE_STATE_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineRasterizationConservativeStateCreateInfoEXT));
                    vt_unserialize_VkPipelineRasterizationConservativeStateCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_STATE_STREAM_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineRasterizationStateStreamCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_STATE_STREAM_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineRasterizationStateStreamCreateInfoEXT));
                    vt_unserialize_VkPipelineRasterizationStateStreamCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_DEPTH_CLIP_STATE_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineRasterizationDepthClipStateCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_DEPTH_CLIP_STATE_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineRasterizationDepthClipStateCreateInfoEXT));
                    vt_unserialize_VkPipelineRasterizationDepthClipStateCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_PROVOKING_VERTEX_STATE_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineRasterizationProvokingVertexStateCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_RASTERIZATION_PROVOKING_VERTEX_STATE_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineRasterizationProvokingVertexStateCreateInfoEXT));
                    vt_unserialize_VkPipelineRasterizationProvokingVertexStateCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkPipelineRasterizationStateCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthClampEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->rasterizerDiscardEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->polygonMode = *(VkPolygonMode*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->cullMode = *(VkCullModeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->frontFace = *(VkFrontFace*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthBiasEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthBiasConstantFactor = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthBiasClamp = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthBiasSlopeFactor = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->lineWidth = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineViewportStateCreateInfo(VkPipelineViewportStateCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PIPELINE_VIEWPORT_DEPTH_CLIP_CONTROL_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineViewportDepthClipControlCreateInfoEXT((VkPipelineViewportDepthClipControlCreateInfoEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // viewportCount
    bufferSize += 4;
    if (val->pViewports) {
        int arrSize = val->viewportCount ? val->viewportCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkViewport(&val->pViewports[i]);
        }
    }
    bufferSize += 4; // scissorCount
    bufferSize += 4;
    if (val->pScissors) {
        int arrSize = val->scissorCount ? val->scissorCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkRect2D(&val->pScissors[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkPipelineViewportStateCreateInfo(VkPipelineViewportStateCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PIPELINE_VIEWPORT_DEPTH_CLIP_CONTROL_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkPipelineViewportDepthClipControlCreateInfoEXT((VkPipelineViewportDepthClipControlCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineViewportDepthClipControlCreateInfoEXT((VkPipelineViewportDepthClipControlCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkPipelineViewportStateCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->viewportCount;
    bufferOffset += 4;

    if (val->pViewports) {
        int arrSize = val->viewportCount ? val->viewportCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkViewport(&val->pViewports[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkViewport(&val->pViewports[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->scissorCount;
    bufferOffset += 4;

    if (val->pScissors) {
        int arrSize = val->scissorCount ? val->scissorCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkRect2D(&val->pScissors[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkRect2D(&val->pScissors[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkPipelineViewportStateCreateInfo(VkPipelineViewportStateCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PIPELINE_VIEWPORT_DEPTH_CLIP_CONTROL_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineViewportDepthClipControlCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_VIEWPORT_DEPTH_CLIP_CONTROL_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineViewportDepthClipControlCreateInfoEXT));
                    vt_unserialize_VkPipelineViewportDepthClipControlCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkPipelineViewportStateCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->viewportCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkViewport* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkViewport));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkViewport(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pViewports = arrValues;
    }
    else val->pViewports = NULL;
    val->scissorCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkRect2D* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkRect2D));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkRect2D(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pScissors = arrValues;
    }
    else val->pScissors = NULL;
}

static inline int vt_sizeof_VkPipelineTessellationStateCreateInfo(VkPipelineTessellationStateCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PIPELINE_TESSELLATION_DOMAIN_ORIGIN_STATE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineTessellationDomainOriginStateCreateInfo((VkPipelineTessellationDomainOriginStateCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // patchControlPoints
    return bufferSize;
}

static inline void vt_serialize_VkPipelineTessellationStateCreateInfo(VkPipelineTessellationStateCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PIPELINE_TESSELLATION_DOMAIN_ORIGIN_STATE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkPipelineTessellationDomainOriginStateCreateInfo((VkPipelineTessellationDomainOriginStateCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineTessellationDomainOriginStateCreateInfo((VkPipelineTessellationDomainOriginStateCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkPipelineTessellationStateCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->patchControlPoints;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineTessellationStateCreateInfo(VkPipelineTessellationStateCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PIPELINE_TESSELLATION_DOMAIN_ORIGIN_STATE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineTessellationDomainOriginStateCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_TESSELLATION_DOMAIN_ORIGIN_STATE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineTessellationDomainOriginStateCreateInfo));
                    vt_unserialize_VkPipelineTessellationDomainOriginStateCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkPipelineTessellationStateCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->patchControlPoints = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineInputAssemblyStateCreateInfo(VkPipelineInputAssemblyStateCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // topology
    bufferSize += 4; // primitiveRestartEnable
    return bufferSize;
}

static inline void vt_serialize_VkPipelineInputAssemblyStateCreateInfo(VkPipelineInputAssemblyStateCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkPipelineInputAssemblyStateCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkPrimitiveTopology*)(outputBuffer + bufferOffset) = val->topology;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->primitiveRestartEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineInputAssemblyStateCreateInfo(VkPipelineInputAssemblyStateCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkPipelineInputAssemblyStateCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->topology = *(VkPrimitiveTopology*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->primitiveRestartEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineVertexInputStateCreateInfo(VkPipelineVertexInputStateCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PIPELINE_VERTEX_INPUT_DIVISOR_STATE_CREATE_INFO_KHR: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineVertexInputDivisorStateCreateInfoKHR((VkPipelineVertexInputDivisorStateCreateInfoKHR*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // vertexBindingDescriptionCount
    bufferSize += 4;
    if (val->pVertexBindingDescriptions) {
        int arrSize = val->vertexBindingDescriptionCount ? val->vertexBindingDescriptionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkVertexInputBindingDescription(&val->pVertexBindingDescriptions[i]);
        }
    }
    bufferSize += 4; // vertexAttributeDescriptionCount
    bufferSize += 4;
    if (val->pVertexAttributeDescriptions) {
        int arrSize = val->vertexAttributeDescriptionCount ? val->vertexAttributeDescriptionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkVertexInputAttributeDescription(&val->pVertexAttributeDescriptions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkPipelineVertexInputStateCreateInfo(VkPipelineVertexInputStateCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PIPELINE_VERTEX_INPUT_DIVISOR_STATE_CREATE_INFO_KHR: {
                    int itemSize = vt_sizeof_VkPipelineVertexInputDivisorStateCreateInfoKHR((VkPipelineVertexInputDivisorStateCreateInfoKHR*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineVertexInputDivisorStateCreateInfoKHR((VkPipelineVertexInputDivisorStateCreateInfoKHR*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkPipelineVertexInputStateCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->vertexBindingDescriptionCount;
    bufferOffset += 4;

    if (val->pVertexBindingDescriptions) {
        int arrSize = val->vertexBindingDescriptionCount ? val->vertexBindingDescriptionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkVertexInputBindingDescription(&val->pVertexBindingDescriptions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkVertexInputBindingDescription(&val->pVertexBindingDescriptions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->vertexAttributeDescriptionCount;
    bufferOffset += 4;

    if (val->pVertexAttributeDescriptions) {
        int arrSize = val->vertexAttributeDescriptionCount ? val->vertexAttributeDescriptionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkVertexInputAttributeDescription(&val->pVertexAttributeDescriptions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkVertexInputAttributeDescription(&val->pVertexAttributeDescriptions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkPipelineVertexInputStateCreateInfo(VkPipelineVertexInputStateCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PIPELINE_VERTEX_INPUT_DIVISOR_STATE_CREATE_INFO_KHR: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineVertexInputDivisorStateCreateInfoKHR* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_VERTEX_INPUT_DIVISOR_STATE_CREATE_INFO_KHR) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineVertexInputDivisorStateCreateInfoKHR));
                    vt_unserialize_VkPipelineVertexInputDivisorStateCreateInfoKHR(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkPipelineVertexInputStateCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->vertexBindingDescriptionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkVertexInputBindingDescription* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkVertexInputBindingDescription));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkVertexInputBindingDescription(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pVertexBindingDescriptions = arrValues;
    }
    else val->pVertexBindingDescriptions = NULL;
    val->vertexAttributeDescriptionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkVertexInputAttributeDescription* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkVertexInputAttributeDescription));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkVertexInputAttributeDescription(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pVertexAttributeDescriptions = arrValues;
    }
    else val->pVertexAttributeDescriptions = NULL;
}

static inline int vt_sizeof_VkPipelineShaderStageCreateInfo(VkPipelineShaderStageCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_SHADER_MODULE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkShaderModuleCreateInfo((VkShaderModuleCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_SHADER_STAGE_REQUIRED_SUBGROUP_SIZE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineShaderStageRequiredSubgroupSizeCreateInfo((VkPipelineShaderStageRequiredSubgroupSizeCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_SHADER_STAGE_MODULE_IDENTIFIER_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineShaderStageModuleIdentifierCreateInfoEXT((VkPipelineShaderStageModuleIdentifierCreateInfoEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // stage
    bufferSize += vt_sizeof_VkShaderModule(val->module);
    bufferSize += 4;
    bufferSize += val->pName ? strlen(val->pName) + 1 : 0;
    bufferSize += 4;
    bufferSize += val->pSpecializationInfo ? vt_sizeof_VkSpecializationInfo(val->pSpecializationInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_VkPipelineShaderStageCreateInfo(VkPipelineShaderStageCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_SHADER_MODULE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkShaderModuleCreateInfo((VkShaderModuleCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkShaderModuleCreateInfo((VkShaderModuleCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PIPELINE_SHADER_STAGE_REQUIRED_SUBGROUP_SIZE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkPipelineShaderStageRequiredSubgroupSizeCreateInfo((VkPipelineShaderStageRequiredSubgroupSizeCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineShaderStageRequiredSubgroupSizeCreateInfo((VkPipelineShaderStageRequiredSubgroupSizeCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PIPELINE_SHADER_STAGE_MODULE_IDENTIFIER_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkPipelineShaderStageModuleIdentifierCreateInfoEXT((VkPipelineShaderStageModuleIdentifierCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineShaderStageModuleIdentifierCreateInfoEXT((VkPipelineShaderStageModuleIdentifierCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkPipelineShaderStageCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkShaderStageFlagBits*)(outputBuffer + bufferOffset) = val->stage;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkShaderModule(val->module, outputBuffer + bufferOffset);
#else
    VkObject* moduleObject = VkObject_fromHandle(val->module);
    vt_serialize_VkShaderModule((VkShaderModule)&moduleObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkShaderModule(val->module);

    if (val->pName) {
        int strSize = strlen(val->pName) + 1;
        *(int*)(outputBuffer + bufferOffset) = strSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pName, strSize);
        bufferOffset += strSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pSpecializationInfo) {
        int itemSize = vt_sizeof_VkSpecializationInfo(val->pSpecializationInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSpecializationInfo(val->pSpecializationInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkPipelineShaderStageCreateInfo(VkPipelineShaderStageCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_SHADER_MODULE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkShaderModuleCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SHADER_MODULE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkShaderModuleCreateInfo));
                    vt_unserialize_VkShaderModuleCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_SHADER_STAGE_REQUIRED_SUBGROUP_SIZE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineShaderStageRequiredSubgroupSizeCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_SHADER_STAGE_REQUIRED_SUBGROUP_SIZE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineShaderStageRequiredSubgroupSizeCreateInfo));
                    vt_unserialize_VkPipelineShaderStageRequiredSubgroupSizeCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_SHADER_STAGE_MODULE_IDENTIFIER_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineShaderStageModuleIdentifierCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_SHADER_STAGE_MODULE_IDENTIFIER_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineShaderStageModuleIdentifierCreateInfoEXT));
                    vt_unserialize_VkPipelineShaderStageModuleIdentifierCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkPipelineShaderStageCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stage = *(VkShaderStageFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t moduleId;
    vt_unserialize_VkShaderModule((VkShaderModule)&moduleId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->module = VkObject_fromId(moduleId);
#else
    VkObject* moduleObject = VkObject_create(VK_OBJECT_TYPE_SHADER_MODULE, moduleId);
    val->module = VkObject_toHandle(moduleObject);
#endif
    bufferOffset += vt_sizeof_VkShaderModule(val->module);

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        char* arrValues = vt_alloc(memoryPool, dataSize6);
        memcpy(arrValues, inputBuffer + bufferOffset, dataSize6);
        val->pName = arrValues;
        bufferOffset += dataSize6;
    }

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        val->pSpecializationInfo = vt_alloc(memoryPool, sizeof(VkSpecializationInfo));
        vt_unserialize_VkSpecializationInfo(val->pSpecializationInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize7;
    }
    else val->pSpecializationInfo = NULL;
}

static inline int vt_sizeof_VkSparseImageMemoryBind(VkSparseImageMemoryBind* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkImageSubresource(&val->subresource);
    bufferSize += vt_sizeof_VkOffset3D(&val->offset);
    bufferSize += vt_sizeof_VkExtent3D(&val->extent);
    bufferSize += vt_sizeof_VkDeviceMemory(val->memory);
    bufferSize += 8; // memoryOffset
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_VkSparseImageMemoryBind(VkSparseImageMemoryBind* val, char* outputBuffer) {
    int bufferOffset = 0;
    vt_serialize_VkImageSubresource(&val->subresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresource(&val->subresource);
    vt_serialize_VkOffset3D(&val->offset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset3D(&val->offset);
    vt_serialize_VkExtent3D(&val->extent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);

#ifdef VT_SERVER
    vt_serialize_VkDeviceMemory(val->memory, outputBuffer + bufferOffset);
#else
    VkObject* memoryObject = VkObject_fromHandle(val->memory);
    vt_serialize_VkDeviceMemory((VkDeviceMemory)&memoryObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->memoryOffset;
    bufferOffset += 8;
    *(VkSparseMemoryBindFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSparseImageMemoryBind(VkSparseImageMemoryBind* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    vt_unserialize_VkImageSubresource(&val->subresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresource(&val->subresource);
    vt_unserialize_VkOffset3D(&val->offset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset3D(&val->offset);
    vt_unserialize_VkExtent3D(&val->extent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);

    uint64_t memoryId;
    vt_unserialize_VkDeviceMemory((VkDeviceMemory)&memoryId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->memory = vortekSerializerCastVkObject ? ((ResourceMemory*)VkObject_fromId(memoryId))->memory : VkObject_fromId(memoryId);
#else
    VkObject* memoryObject = VkObject_create(VK_OBJECT_TYPE_DEVICE_MEMORY, memoryId);
    val->memory = VkObject_toHandle(memoryObject);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    val->memoryOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->flags = *(VkSparseMemoryBindFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSparseMemoryBind(VkSparseMemoryBind* val) {
    int bufferSize = 0;
    bufferSize += 8; // resourceOffset
    bufferSize += 8; // size
    bufferSize += vt_sizeof_VkDeviceMemory(val->memory);
    bufferSize += 8; // memoryOffset
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_VkSparseMemoryBind(VkSparseMemoryBind* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->resourceOffset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 8;

#ifdef VT_SERVER
    vt_serialize_VkDeviceMemory(val->memory, outputBuffer + bufferOffset);
#else
    VkObject* memoryObject = VkObject_fromHandle(val->memory);
    vt_serialize_VkDeviceMemory((VkDeviceMemory)&memoryObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->memoryOffset;
    bufferOffset += 8;
    *(VkSparseMemoryBindFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSparseMemoryBind(VkSparseMemoryBind* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->resourceOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;

    uint64_t memoryId;
    vt_unserialize_VkDeviceMemory((VkDeviceMemory)&memoryId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->memory = vortekSerializerCastVkObject ? ((ResourceMemory*)VkObject_fromId(memoryId))->memory : VkObject_fromId(memoryId);
#else
    VkObject* memoryObject = VkObject_create(VK_OBJECT_TYPE_DEVICE_MEMORY, memoryId);
    val->memory = VkObject_toHandle(memoryObject);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    val->memoryOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->flags = *(VkSparseMemoryBindFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSparseImageMemoryBindInfo(VkSparseImageMemoryBindInfo* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkImage(val->image);
    bufferSize += 4; // bindCount
    bufferSize += 4;
    if (val->pBinds) {
        int arrSize = val->bindCount ? val->bindCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSparseImageMemoryBind(&val->pBinds[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkSparseImageMemoryBindInfo(VkSparseImageMemoryBindInfo* val, char* outputBuffer) {
    int bufferOffset = 0;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->image, outputBuffer + bufferOffset);
#else
    VkObject* imageObject = VkObject_fromHandle(val->image);
    vt_serialize_VkImage((VkImage)&imageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->bindCount;
    bufferOffset += 4;

    if (val->pBinds) {
        int arrSize = val->bindCount ? val->bindCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSparseImageMemoryBind(&val->pBinds[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSparseImageMemoryBind(&val->pBinds[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkSparseImageMemoryBindInfo(VkSparseImageMemoryBindInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    uint64_t imageId;
    vt_unserialize_VkImage((VkImage)&imageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->image = VkObject_fromId(imageId);
#else
    VkObject* imageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, imageId);
    val->image = VkObject_toHandle(imageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
    val->bindCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkSparseImageMemoryBind* arrValues = vt_alloc(memoryPool, dataSize3 * sizeof(VkSparseImageMemoryBind));
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSparseImageMemoryBind(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pBinds = arrValues;
    }
    else val->pBinds = NULL;
}

static inline int vt_sizeof_VkSparseImageOpaqueMemoryBindInfo(VkSparseImageOpaqueMemoryBindInfo* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkImage(val->image);
    bufferSize += 4; // bindCount
    bufferSize += 4;
    if (val->pBinds) {
        int arrSize = val->bindCount ? val->bindCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSparseMemoryBind(&val->pBinds[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkSparseImageOpaqueMemoryBindInfo(VkSparseImageOpaqueMemoryBindInfo* val, char* outputBuffer) {
    int bufferOffset = 0;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->image, outputBuffer + bufferOffset);
#else
    VkObject* imageObject = VkObject_fromHandle(val->image);
    vt_serialize_VkImage((VkImage)&imageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->bindCount;
    bufferOffset += 4;

    if (val->pBinds) {
        int arrSize = val->bindCount ? val->bindCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSparseMemoryBind(&val->pBinds[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSparseMemoryBind(&val->pBinds[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkSparseImageOpaqueMemoryBindInfo(VkSparseImageOpaqueMemoryBindInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    uint64_t imageId;
    vt_unserialize_VkImage((VkImage)&imageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->image = VkObject_fromId(imageId);
#else
    VkObject* imageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, imageId);
    val->image = VkObject_toHandle(imageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
    val->bindCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkSparseMemoryBind* arrValues = vt_alloc(memoryPool, dataSize3 * sizeof(VkSparseMemoryBind));
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSparseMemoryBind(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pBinds = arrValues;
    }
    else val->pBinds = NULL;
}

static inline int vt_sizeof_VkSparseBufferMemoryBindInfo(VkSparseBufferMemoryBindInfo* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkBuffer(val->buffer);
    bufferSize += 4; // bindCount
    bufferSize += 4;
    if (val->pBinds) {
        int arrSize = val->bindCount ? val->bindCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSparseMemoryBind(&val->pBinds[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkSparseBufferMemoryBindInfo(VkSparseBufferMemoryBindInfo* val, char* outputBuffer) {
    int bufferOffset = 0;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->buffer, outputBuffer + bufferOffset);
#else
    VkObject* bufferObject = VkObject_fromHandle(val->buffer);
    vt_serialize_VkBuffer((VkBuffer)&bufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->bindCount;
    bufferOffset += 4;

    if (val->pBinds) {
        int arrSize = val->bindCount ? val->bindCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSparseMemoryBind(&val->pBinds[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSparseMemoryBind(&val->pBinds[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkSparseBufferMemoryBindInfo(VkSparseBufferMemoryBindInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    uint64_t bufferId;
    vt_unserialize_VkBuffer((VkBuffer)&bufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->buffer = VkObject_fromId(bufferId);
#else
    VkObject* bufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, bufferId);
    val->buffer = VkObject_toHandle(bufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    val->bindCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkSparseMemoryBind* arrValues = vt_alloc(memoryPool, dataSize3 * sizeof(VkSparseMemoryBind));
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSparseMemoryBind(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pBinds = arrValues;
    }
    else val->pBinds = NULL;
}

static inline int vt_sizeof_VkPhysicalDeviceFeatures(VkPhysicalDeviceFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // robustBufferAccess
    bufferSize += 4; // fullDrawIndexUint32
    bufferSize += 4; // imageCubeArray
    bufferSize += 4; // independentBlend
    bufferSize += 4; // geometryShader
    bufferSize += 4; // tessellationShader
    bufferSize += 4; // sampleRateShading
    bufferSize += 4; // dualSrcBlend
    bufferSize += 4; // logicOp
    bufferSize += 4; // multiDrawIndirect
    bufferSize += 4; // drawIndirectFirstInstance
    bufferSize += 4; // depthClamp
    bufferSize += 4; // depthBiasClamp
    bufferSize += 4; // fillModeNonSolid
    bufferSize += 4; // depthBounds
    bufferSize += 4; // wideLines
    bufferSize += 4; // largePoints
    bufferSize += 4; // alphaToOne
    bufferSize += 4; // multiViewport
    bufferSize += 4; // samplerAnisotropy
    bufferSize += 4; // textureCompressionETC2
    bufferSize += 4; // textureCompressionASTC_LDR
    bufferSize += 4; // textureCompressionBC
    bufferSize += 4; // occlusionQueryPrecise
    bufferSize += 4; // pipelineStatisticsQuery
    bufferSize += 4; // vertexPipelineStoresAndAtomics
    bufferSize += 4; // fragmentStoresAndAtomics
    bufferSize += 4; // shaderTessellationAndGeometryPointSize
    bufferSize += 4; // shaderImageGatherExtended
    bufferSize += 4; // shaderStorageImageExtendedFormats
    bufferSize += 4; // shaderStorageImageMultisample
    bufferSize += 4; // shaderStorageImageReadWithoutFormat
    bufferSize += 4; // shaderStorageImageWriteWithoutFormat
    bufferSize += 4; // shaderUniformBufferArrayDynamicIndexing
    bufferSize += 4; // shaderSampledImageArrayDynamicIndexing
    bufferSize += 4; // shaderStorageBufferArrayDynamicIndexing
    bufferSize += 4; // shaderStorageImageArrayDynamicIndexing
    bufferSize += 4; // shaderClipDistance
    bufferSize += 4; // shaderCullDistance
    bufferSize += 4; // shaderFloat64
    bufferSize += 4; // shaderInt64
    bufferSize += 4; // shaderInt16
    bufferSize += 4; // shaderResourceResidency
    bufferSize += 4; // shaderResourceMinLod
    bufferSize += 4; // sparseBinding
    bufferSize += 4; // sparseResidencyBuffer
    bufferSize += 4; // sparseResidencyImage2D
    bufferSize += 4; // sparseResidencyImage3D
    bufferSize += 4; // sparseResidency2Samples
    bufferSize += 4; // sparseResidency4Samples
    bufferSize += 4; // sparseResidency8Samples
    bufferSize += 4; // sparseResidency16Samples
    bufferSize += 4; // sparseResidencyAliased
    bufferSize += 4; // variableMultisampleRate
    bufferSize += 4; // inheritedQueries
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceFeatures(VkPhysicalDeviceFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->robustBufferAccess;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->fullDrawIndexUint32;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->imageCubeArray;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->independentBlend;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->geometryShader;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->tessellationShader;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sampleRateShading;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->dualSrcBlend;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->logicOp;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->multiDrawIndirect;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->drawIndirectFirstInstance;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->depthClamp;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->depthBiasClamp;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->fillModeNonSolid;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->depthBounds;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->wideLines;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->largePoints;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->alphaToOne;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->multiViewport;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->samplerAnisotropy;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->textureCompressionETC2;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->textureCompressionASTC_LDR;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->textureCompressionBC;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->occlusionQueryPrecise;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->pipelineStatisticsQuery;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->vertexPipelineStoresAndAtomics;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->fragmentStoresAndAtomics;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderTessellationAndGeometryPointSize;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderImageGatherExtended;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageImageExtendedFormats;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageImageMultisample;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageImageReadWithoutFormat;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageImageWriteWithoutFormat;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderUniformBufferArrayDynamicIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSampledImageArrayDynamicIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageBufferArrayDynamicIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageImageArrayDynamicIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderClipDistance;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderCullDistance;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderFloat64;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderInt64;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderInt16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderResourceResidency;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderResourceMinLod;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sparseBinding;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sparseResidencyBuffer;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sparseResidencyImage2D;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sparseResidencyImage3D;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sparseResidency2Samples;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sparseResidency4Samples;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sparseResidency8Samples;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sparseResidency16Samples;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->sparseResidencyAliased;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->variableMultisampleRate;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->inheritedQueries;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceFeatures(VkPhysicalDeviceFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->robustBufferAccess = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->fullDrawIndexUint32 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->imageCubeArray = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->independentBlend = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->geometryShader = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->tessellationShader = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sampleRateShading = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dualSrcBlend = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->logicOp = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->multiDrawIndirect = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->drawIndirectFirstInstance = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthClamp = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthBiasClamp = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->fillModeNonSolid = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->depthBounds = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->wideLines = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->largePoints = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->alphaToOne = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->multiViewport = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->samplerAnisotropy = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->textureCompressionETC2 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->textureCompressionASTC_LDR = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->textureCompressionBC = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->occlusionQueryPrecise = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->pipelineStatisticsQuery = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->vertexPipelineStoresAndAtomics = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->fragmentStoresAndAtomics = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderTessellationAndGeometryPointSize = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderImageGatherExtended = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageImageExtendedFormats = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageImageMultisample = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageImageReadWithoutFormat = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageImageWriteWithoutFormat = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderUniformBufferArrayDynamicIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSampledImageArrayDynamicIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageBufferArrayDynamicIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageImageArrayDynamicIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderClipDistance = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderCullDistance = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderFloat64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderInt64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderInt16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderResourceResidency = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderResourceMinLod = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sparseBinding = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sparseResidencyBuffer = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sparseResidencyImage2D = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sparseResidencyImage3D = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sparseResidency2Samples = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sparseResidency4Samples = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sparseResidency8Samples = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sparseResidency16Samples = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sparseResidencyAliased = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->variableMultisampleRate = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->inheritedQueries = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDeviceQueueCreateInfo(VkDeviceQueueCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // queueFamilyIndex
    bufferSize += 4; // queueCount
    bufferSize += 4;
    bufferSize += val->pQueuePriorities ? val->queueCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkDeviceQueueCreateInfo(VkDeviceQueueCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDeviceQueueCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->queueFamilyIndex;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->queueCount;
    bufferOffset += 4;

    if (val->pQueuePriorities) {
        int arrSize = val->queueCount ? val->queueCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pQueuePriorities, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDeviceQueueCreateInfo(VkDeviceQueueCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkDeviceQueueCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queueCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        float* arrValues = vt_alloc(memoryPool, dataSize6 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize6 * 4);
        bufferOffset += dataSize6 * 4;
        val->pQueuePriorities = arrValues;
    }
    else val->pQueuePriorities = NULL;
}

static inline int vt_sizeof_VkApplicationInfo(VkApplicationInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4;
    bufferSize += val->pApplicationName ? strlen(val->pApplicationName) + 1 : 0;
    bufferSize += 4; // applicationVersion
    bufferSize += 4;
    bufferSize += val->pEngineName ? strlen(val->pEngineName) + 1 : 0;
    bufferSize += 4; // engineVersion
    bufferSize += 4; // apiVersion
    return bufferSize;
}

static inline void vt_serialize_VkApplicationInfo(VkApplicationInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

    if (val->pApplicationName) {
        int strSize = strlen(val->pApplicationName) + 1;
        *(int*)(outputBuffer + bufferOffset) = strSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pApplicationName, strSize);
        bufferOffset += strSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->applicationVersion;
    bufferOffset += 4;

    if (val->pEngineName) {
        int strSize = strlen(val->pEngineName) + 1;
        *(int*)(outputBuffer + bufferOffset) = strSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pEngineName, strSize);
        bufferOffset += strSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->engineVersion;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->apiVersion;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkApplicationInfo(VkApplicationInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        char* arrValues = vt_alloc(memoryPool, dataSize3);
        memcpy(arrValues, inputBuffer + bufferOffset, dataSize3);
        val->pApplicationName = arrValues;
        bufferOffset += dataSize3;
    }
    val->applicationVersion = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        char* arrValues = vt_alloc(memoryPool, dataSize5);
        memcpy(arrValues, inputBuffer + bufferOffset, dataSize5);
        val->pEngineName = arrValues;
        bufferOffset += dataSize5;
    }
    val->engineVersion = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->apiVersion = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPipelineCreationFeedback(VkPipelineCreationFeedback* val) {
    int bufferSize = 0;
    bufferSize += 4; // flags
    bufferSize += 8; // duration
    return bufferSize;
}

static inline void vt_serialize_VkPipelineCreationFeedback(VkPipelineCreationFeedback* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkPipelineCreationFeedbackFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint64_t*)(outputBuffer + bufferOffset) = val->duration;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkPipelineCreationFeedback(VkPipelineCreationFeedback* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->flags = *(VkPipelineCreationFeedbackFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->duration = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkPushConstantRange(VkPushConstantRange* val) {
    int bufferSize = 0;
    bufferSize += 4; // stageFlags
    bufferSize += 4; // offset
    bufferSize += 4; // size
    return bufferSize;
}

static inline void vt_serialize_VkPushConstantRange(VkPushConstantRange* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkShaderStageFlags*)(outputBuffer + bufferOffset) = val->stageFlags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->offset;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPushConstantRange(VkPushConstantRange* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->stageFlags = *(VkShaderStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->offset = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->size = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSemaphoreSubmitInfo(VkSemaphoreSubmitInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkSemaphore(val->semaphore);
    bufferSize += 8; // value
    bufferSize += 8; // stageMask
    bufferSize += 4; // deviceIndex
    return bufferSize;
}

static inline void vt_serialize_VkSemaphoreSubmitInfo(VkSemaphoreSubmitInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkSemaphore(val->semaphore, outputBuffer + bufferOffset);
#else
    VkObject* semaphoreObject = VkObject_fromHandle(val->semaphore);
    vt_serialize_VkSemaphore((VkSemaphore)&semaphoreObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkSemaphore(val->semaphore);
    *(uint64_t*)(outputBuffer + bufferOffset) = val->value;
    bufferOffset += 8;
    *(VkPipelineStageFlags2*)(outputBuffer + bufferOffset) = val->stageMask;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceIndex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSemaphoreSubmitInfo(VkSemaphoreSubmitInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t semaphoreId;
    vt_unserialize_VkSemaphore((VkSemaphore)&semaphoreId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->semaphore = VkObject_fromId(semaphoreId);
#else
    VkObject* semaphoreObject = VkObject_create(VK_OBJECT_TYPE_SEMAPHORE, semaphoreId);
    val->semaphore = VkObject_toHandle(semaphoreObject);
#endif
    bufferOffset += vt_sizeof_VkSemaphore(val->semaphore);
    val->value = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->stageMask = *(VkPipelineStageFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->deviceIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkCommandBufferSubmitInfo(VkCommandBufferSubmitInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkCommandBuffer(val->commandBuffer);
    bufferSize += 4; // deviceMask
    return bufferSize;
}

static inline void vt_serialize_VkCommandBufferSubmitInfo(VkCommandBufferSubmitInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkCommandBuffer(val->commandBuffer, outputBuffer + bufferOffset);
#else
    VkObject* commandBufferObject = VkObject_fromHandle(val->commandBuffer);
    vt_serialize_VkCommandBuffer((VkCommandBuffer)&commandBufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkCommandBuffer(val->commandBuffer);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkCommandBufferSubmitInfo(VkCommandBufferSubmitInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t commandBufferId;
    vt_unserialize_VkCommandBuffer((VkCommandBuffer)&commandBufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->commandBuffer = VkObject_fromId(commandBufferId);
#else
    VkObject* commandBufferObject = VkObject_create(VK_OBJECT_TYPE_COMMAND_BUFFER, commandBufferId);
    val->commandBuffer = VkObject_toHandle(commandBufferObject);
#endif
    bufferOffset += vt_sizeof_VkCommandBuffer(val->commandBuffer);
    val->deviceMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkImageSubresourceRange(VkImageSubresourceRange* val) {
    int bufferSize = 0;
    bufferSize += 4; // aspectMask
    bufferSize += 4; // baseMipLevel
    bufferSize += 4; // levelCount
    bufferSize += 4; // baseArrayLayer
    bufferSize += 4; // layerCount
    return bufferSize;
}

static inline void vt_serialize_VkImageSubresourceRange(VkImageSubresourceRange* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkImageAspectFlags*)(outputBuffer + bufferOffset) = val->aspectMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->baseMipLevel;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->levelCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->baseArrayLayer;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->layerCount;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkImageSubresourceRange(VkImageSubresourceRange* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->aspectMask = *(VkImageAspectFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->baseMipLevel = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->levelCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->baseArrayLayer = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->layerCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSparseImageFormatProperties(VkSparseImageFormatProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // aspectMask
    bufferSize += vt_sizeof_VkExtent3D(&val->imageGranularity);
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_VkSparseImageFormatProperties(VkSparseImageFormatProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkImageAspectFlags*)(outputBuffer + bufferOffset) = val->aspectMask;
    bufferOffset += 4;
    vt_serialize_VkExtent3D(&val->imageGranularity, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent3D(&val->imageGranularity);
    *(VkSparseImageFormatFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSparseImageFormatProperties(VkSparseImageFormatProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->aspectMask = *(VkImageAspectFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkExtent3D(&val->imageGranularity, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent3D(&val->imageGranularity);
    val->flags = *(VkSparseImageFormatFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkComponentMapping(VkComponentMapping* val) {
    int bufferSize = 0;
    bufferSize += 4; // r
    bufferSize += 4; // g
    bufferSize += 4; // b
    bufferSize += 4; // a
    return bufferSize;
}

static inline void vt_serialize_VkComponentMapping(VkComponentMapping* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkComponentSwizzle*)(outputBuffer + bufferOffset) = val->r;
    bufferOffset += 4;
    *(VkComponentSwizzle*)(outputBuffer + bufferOffset) = val->g;
    bufferOffset += 4;
    *(VkComponentSwizzle*)(outputBuffer + bufferOffset) = val->b;
    bufferOffset += 4;
    *(VkComponentSwizzle*)(outputBuffer + bufferOffset) = val->a;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkComponentMapping(VkComponentMapping* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->r = *(VkComponentSwizzle*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->g = *(VkComponentSwizzle*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->b = *(VkComponentSwizzle*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->a = *(VkComponentSwizzle*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSparseImageMemoryRequirements(VkSparseImageMemoryRequirements* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkSparseImageFormatProperties(&val->formatProperties);
    bufferSize += 4; // imageMipTailFirstLod
    bufferSize += 8; // imageMipTailSize
    bufferSize += 8; // imageMipTailOffset
    bufferSize += 8; // imageMipTailStride
    return bufferSize;
}

static inline void vt_serialize_VkSparseImageMemoryRequirements(VkSparseImageMemoryRequirements* val, char* outputBuffer) {
    int bufferOffset = 0;
    vt_serialize_VkSparseImageFormatProperties(&val->formatProperties, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkSparseImageFormatProperties(&val->formatProperties);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->imageMipTailFirstLod;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->imageMipTailSize;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->imageMipTailOffset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->imageMipTailStride;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkSparseImageMemoryRequirements(VkSparseImageMemoryRequirements* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    vt_unserialize_VkSparseImageFormatProperties(&val->formatProperties, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkSparseImageFormatProperties(&val->formatProperties);
    val->imageMipTailFirstLod = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->imageMipTailSize = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->imageMipTailOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->imageMipTailStride = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkMemoryRequirements(VkMemoryRequirements* val) {
    int bufferSize = 0;
    bufferSize += 8; // size
    bufferSize += 8; // alignment
    bufferSize += 4; // memoryTypeBits
    return bufferSize;
}

static inline void vt_serialize_VkMemoryRequirements(VkMemoryRequirements* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->alignment;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->memoryTypeBits;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkMemoryRequirements(VkMemoryRequirements* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->alignment = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->memoryTypeBits = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkImagePlaneMemoryRequirementsInfo(VkImagePlaneMemoryRequirementsInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // planeAspect
    return bufferSize;
}

static inline void vt_serialize_VkImagePlaneMemoryRequirementsInfo(VkImagePlaneMemoryRequirementsInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkImageAspectFlagBits*)(outputBuffer + bufferOffset) = val->planeAspect;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkImagePlaneMemoryRequirementsInfo(VkImagePlaneMemoryRequirementsInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->planeAspect = *(VkImageAspectFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkInputAttachmentAspectReference(VkInputAttachmentAspectReference* val) {
    int bufferSize = 0;
    bufferSize += 4; // subpass
    bufferSize += 4; // inputAttachmentIndex
    bufferSize += 4; // aspectMask
    return bufferSize;
}

static inline void vt_serialize_VkInputAttachmentAspectReference(VkInputAttachmentAspectReference* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subpass;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->inputAttachmentIndex;
    bufferOffset += 4;
    *(VkImageAspectFlags*)(outputBuffer + bufferOffset) = val->aspectMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkInputAttachmentAspectReference(VkInputAttachmentAspectReference* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->subpass = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->inputAttachmentIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->aspectMask = *(VkImageAspectFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkAttachmentDescriptionStencilLayout(VkAttachmentDescriptionStencilLayout* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // stencilInitialLayout
    bufferSize += 4; // stencilFinalLayout
    return bufferSize;
}

static inline void vt_serialize_VkAttachmentDescriptionStencilLayout(VkAttachmentDescriptionStencilLayout* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->stencilInitialLayout;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->stencilFinalLayout;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkAttachmentDescriptionStencilLayout(VkAttachmentDescriptionStencilLayout* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->stencilInitialLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stencilFinalLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkAttachmentReferenceStencilLayout(VkAttachmentReferenceStencilLayout* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // stencilLayout
    return bufferSize;
}

static inline void vt_serialize_VkAttachmentReferenceStencilLayout(VkAttachmentReferenceStencilLayout* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->stencilLayout;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkAttachmentReferenceStencilLayout(VkAttachmentReferenceStencilLayout* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->stencilLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkMemoryBarrier2(VkMemoryBarrier2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // srcStageMask
    bufferSize += 8; // srcAccessMask
    bufferSize += 8; // dstStageMask
    bufferSize += 8; // dstAccessMask
    return bufferSize;
}

static inline void vt_serialize_VkMemoryBarrier2(VkMemoryBarrier2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkPipelineStageFlags2*)(outputBuffer + bufferOffset) = val->srcStageMask;
    bufferOffset += 8;
    *(VkAccessFlags2*)(outputBuffer + bufferOffset) = val->srcAccessMask;
    bufferOffset += 8;
    *(VkPipelineStageFlags2*)(outputBuffer + bufferOffset) = val->dstStageMask;
    bufferOffset += 8;
    *(VkAccessFlags2*)(outputBuffer + bufferOffset) = val->dstAccessMask;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkMemoryBarrier2(VkMemoryBarrier2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->srcStageMask = *(VkPipelineStageFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->srcAccessMask = *(VkAccessFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->dstStageMask = *(VkPipelineStageFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->dstAccessMask = *(VkAccessFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkAttachmentReference2(VkAttachmentReference2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_ATTACHMENT_REFERENCE_STENCIL_LAYOUT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkAttachmentReferenceStencilLayout((VkAttachmentReferenceStencilLayout*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // attachment
    bufferSize += 4; // layout
    bufferSize += 4; // aspectMask
    return bufferSize;
}

static inline void vt_serialize_VkAttachmentReference2(VkAttachmentReference2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_ATTACHMENT_REFERENCE_STENCIL_LAYOUT: {
                    int itemSize = vt_sizeof_VkAttachmentReferenceStencilLayout((VkAttachmentReferenceStencilLayout*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkAttachmentReferenceStencilLayout((VkAttachmentReferenceStencilLayout*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->attachment;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->layout;
    bufferOffset += 4;
    *(VkImageAspectFlags*)(outputBuffer + bufferOffset) = val->aspectMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkAttachmentReference2(VkAttachmentReference2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_ATTACHMENT_REFERENCE_STENCIL_LAYOUT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkAttachmentReferenceStencilLayout* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_ATTACHMENT_REFERENCE_STENCIL_LAYOUT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkAttachmentReferenceStencilLayout));
                    vt_unserialize_VkAttachmentReferenceStencilLayout(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->attachment = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->layout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->aspectMask = *(VkImageAspectFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSubpassDescriptionDepthStencilResolve(VkSubpassDescriptionDepthStencilResolve* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // depthResolveMode
    bufferSize += 4; // stencilResolveMode
    bufferSize += 4;
    bufferSize += val->pDepthStencilResolveAttachment ? vt_sizeof_VkAttachmentReference2(val->pDepthStencilResolveAttachment) : 0;
    return bufferSize;
}

static inline void vt_serialize_VkSubpassDescriptionDepthStencilResolve(VkSubpassDescriptionDepthStencilResolve* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkResolveModeFlagBits*)(outputBuffer + bufferOffset) = val->depthResolveMode;
    bufferOffset += 4;
    *(VkResolveModeFlagBits*)(outputBuffer + bufferOffset) = val->stencilResolveMode;
    bufferOffset += 4;

    if (val->pDepthStencilResolveAttachment) {
        int itemSize = vt_sizeof_VkAttachmentReference2(val->pDepthStencilResolveAttachment);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkAttachmentReference2(val->pDepthStencilResolveAttachment, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkSubpassDescriptionDepthStencilResolve(VkSubpassDescriptionDepthStencilResolve* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->depthResolveMode = *(VkResolveModeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stencilResolveMode = *(VkResolveModeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        val->pDepthStencilResolveAttachment = vt_alloc(memoryPool, sizeof(VkAttachmentReference2));
        vt_unserialize_VkAttachmentReference2(val->pDepthStencilResolveAttachment, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize5;
    }
    else val->pDepthStencilResolveAttachment = NULL;
}

static inline int vt_sizeof_VkSubpassDependency2(VkSubpassDependency2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_MEMORY_BARRIER_2: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkMemoryBarrier2((VkMemoryBarrier2*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // srcSubpass
    bufferSize += 4; // dstSubpass
    bufferSize += 4; // srcStageMask
    bufferSize += 4; // dstStageMask
    bufferSize += 4; // srcAccessMask
    bufferSize += 4; // dstAccessMask
    bufferSize += 4; // dependencyFlags
    bufferSize += 4; // viewOffset
    return bufferSize;
}

static inline void vt_serialize_VkSubpassDependency2(VkSubpassDependency2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_MEMORY_BARRIER_2: {
                    int itemSize = vt_sizeof_VkMemoryBarrier2((VkMemoryBarrier2*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkMemoryBarrier2((VkMemoryBarrier2*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->srcSubpass;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstSubpass;
    bufferOffset += 4;
    *(VkPipelineStageFlags*)(outputBuffer + bufferOffset) = val->srcStageMask;
    bufferOffset += 4;
    *(VkPipelineStageFlags*)(outputBuffer + bufferOffset) = val->dstStageMask;
    bufferOffset += 4;
    *(VkAccessFlags*)(outputBuffer + bufferOffset) = val->srcAccessMask;
    bufferOffset += 4;
    *(VkAccessFlags*)(outputBuffer + bufferOffset) = val->dstAccessMask;
    bufferOffset += 4;
    *(VkDependencyFlags*)(outputBuffer + bufferOffset) = val->dependencyFlags;
    bufferOffset += 4;
    *(int32_t*)(outputBuffer + bufferOffset) = val->viewOffset;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSubpassDependency2(VkSubpassDependency2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_MEMORY_BARRIER_2: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkMemoryBarrier2* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_MEMORY_BARRIER_2) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkMemoryBarrier2));
                    vt_unserialize_VkMemoryBarrier2(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->srcSubpass = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstSubpass = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcStageMask = *(VkPipelineStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstStageMask = *(VkPipelineStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcAccessMask = *(VkAccessFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstAccessMask = *(VkAccessFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dependencyFlags = *(VkDependencyFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->viewOffset = *(int32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSubpassDescription2(VkSubpassDescription2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_SUBPASS_DESCRIPTION_DEPTH_STENCIL_RESOLVE: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkSubpassDescriptionDepthStencilResolve((VkSubpassDescriptionDepthStencilResolve*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // pipelineBindPoint
    bufferSize += 4; // viewMask
    bufferSize += 4; // inputAttachmentCount
    bufferSize += 4;
    if (val->pInputAttachments) {
        int arrSize = val->inputAttachmentCount ? val->inputAttachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkAttachmentReference2(&val->pInputAttachments[i]);
        }
    }
    bufferSize += 4; // colorAttachmentCount
    bufferSize += 4;
    if (val->pColorAttachments) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkAttachmentReference2(&val->pColorAttachments[i]);
        }
    }
    bufferSize += 4;
    if (val->pResolveAttachments) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkAttachmentReference2(&val->pResolveAttachments[i]);
        }
    }
    bufferSize += 4;
    bufferSize += val->pDepthStencilAttachment ? vt_sizeof_VkAttachmentReference2(val->pDepthStencilAttachment) : 0;
    bufferSize += 4; // preserveAttachmentCount
    bufferSize += 4;
    bufferSize += val->pPreserveAttachments ? val->preserveAttachmentCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkSubpassDescription2(VkSubpassDescription2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_SUBPASS_DESCRIPTION_DEPTH_STENCIL_RESOLVE: {
                    int itemSize = vt_sizeof_VkSubpassDescriptionDepthStencilResolve((VkSubpassDescriptionDepthStencilResolve*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkSubpassDescriptionDepthStencilResolve((VkSubpassDescriptionDepthStencilResolve*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkSubpassDescriptionFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkPipelineBindPoint*)(outputBuffer + bufferOffset) = val->pipelineBindPoint;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->viewMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->inputAttachmentCount;
    bufferOffset += 4;

    if (val->pInputAttachments) {
        int arrSize = val->inputAttachmentCount ? val->inputAttachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkAttachmentReference2(&val->pInputAttachments[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkAttachmentReference2(&val->pInputAttachments[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->colorAttachmentCount;
    bufferOffset += 4;

    if (val->pColorAttachments) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkAttachmentReference2(&val->pColorAttachments[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkAttachmentReference2(&val->pColorAttachments[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pResolveAttachments) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkAttachmentReference2(&val->pResolveAttachments[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkAttachmentReference2(&val->pResolveAttachments[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pDepthStencilAttachment) {
        int itemSize = vt_sizeof_VkAttachmentReference2(val->pDepthStencilAttachment);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkAttachmentReference2(val->pDepthStencilAttachment, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->preserveAttachmentCount;
    bufferOffset += 4;

    if (val->pPreserveAttachments) {
        int arrSize = val->preserveAttachmentCount ? val->preserveAttachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pPreserveAttachments, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkSubpassDescription2(VkSubpassDescription2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_SUBPASS_DESCRIPTION_DEPTH_STENCIL_RESOLVE: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkSubpassDescriptionDepthStencilResolve* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SUBPASS_DESCRIPTION_DEPTH_STENCIL_RESOLVE) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkSubpassDescriptionDepthStencilResolve));
                    vt_unserialize_VkSubpassDescriptionDepthStencilResolve(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkSubpassDescriptionFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->pipelineBindPoint = *(VkPipelineBindPoint*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->viewMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->inputAttachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkAttachmentReference2* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkAttachmentReference2));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkAttachmentReference2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pInputAttachments = arrValues;
    }
    else val->pInputAttachments = NULL;
    val->colorAttachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        VkAttachmentReference2* arrValues = vt_alloc(memoryPool, dataSize9 * sizeof(VkAttachmentReference2));
        for (int i = 0; i < dataSize9; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkAttachmentReference2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pColorAttachments = arrValues;
    }
    else val->pColorAttachments = NULL;

    int dataSize10 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize10 > 0) {
        VkAttachmentReference2* arrValues = vt_alloc(memoryPool, dataSize10 * sizeof(VkAttachmentReference2));
        for (int i = 0; i < dataSize10; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkAttachmentReference2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pResolveAttachments = arrValues;
    }
    else val->pResolveAttachments = NULL;

    int dataSize11 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize11 > 0) {
        val->pDepthStencilAttachment = vt_alloc(memoryPool, sizeof(VkAttachmentReference2));
        vt_unserialize_VkAttachmentReference2(val->pDepthStencilAttachment, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize11;
    }
    else val->pDepthStencilAttachment = NULL;
    val->preserveAttachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize13 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize13 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize13 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize13 * 4);
        bufferOffset += dataSize13 * 4;
        val->pPreserveAttachments = arrValues;
    }
    else val->pPreserveAttachments = NULL;
}

static inline int vt_sizeof_VkAttachmentDescription2(VkAttachmentDescription2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_ATTACHMENT_DESCRIPTION_STENCIL_LAYOUT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkAttachmentDescriptionStencilLayout((VkAttachmentDescriptionStencilLayout*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // format
    bufferSize += 4; // samples
    bufferSize += 4; // loadOp
    bufferSize += 4; // storeOp
    bufferSize += 4; // stencilLoadOp
    bufferSize += 4; // stencilStoreOp
    bufferSize += 4; // initialLayout
    bufferSize += 4; // finalLayout
    return bufferSize;
}

static inline void vt_serialize_VkAttachmentDescription2(VkAttachmentDescription2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_ATTACHMENT_DESCRIPTION_STENCIL_LAYOUT: {
                    int itemSize = vt_sizeof_VkAttachmentDescriptionStencilLayout((VkAttachmentDescriptionStencilLayout*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkAttachmentDescriptionStencilLayout((VkAttachmentDescriptionStencilLayout*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkAttachmentDescriptionFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->format;
    bufferOffset += 4;
    *(VkSampleCountFlagBits*)(outputBuffer + bufferOffset) = val->samples;
    bufferOffset += 4;
    *(VkAttachmentLoadOp*)(outputBuffer + bufferOffset) = val->loadOp;
    bufferOffset += 4;
    *(VkAttachmentStoreOp*)(outputBuffer + bufferOffset) = val->storeOp;
    bufferOffset += 4;
    *(VkAttachmentLoadOp*)(outputBuffer + bufferOffset) = val->stencilLoadOp;
    bufferOffset += 4;
    *(VkAttachmentStoreOp*)(outputBuffer + bufferOffset) = val->stencilStoreOp;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->initialLayout;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->finalLayout;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkAttachmentDescription2(VkAttachmentDescription2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_ATTACHMENT_DESCRIPTION_STENCIL_LAYOUT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkAttachmentDescriptionStencilLayout* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_ATTACHMENT_DESCRIPTION_STENCIL_LAYOUT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkAttachmentDescriptionStencilLayout));
                    vt_unserialize_VkAttachmentDescriptionStencilLayout(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkAttachmentDescriptionFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->samples = *(VkSampleCountFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->loadOp = *(VkAttachmentLoadOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storeOp = *(VkAttachmentStoreOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stencilLoadOp = *(VkAttachmentLoadOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stencilStoreOp = *(VkAttachmentStoreOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->initialLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->finalLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkFramebufferAttachmentImageInfo(VkFramebufferAttachmentImageInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // usage
    bufferSize += 4; // width
    bufferSize += 4; // height
    bufferSize += 4; // layerCount
    bufferSize += 4; // viewFormatCount
    bufferSize += 4;
    bufferSize += val->pViewFormats ? val->viewFormatCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkFramebufferAttachmentImageInfo(VkFramebufferAttachmentImageInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkImageCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkImageUsageFlags*)(outputBuffer + bufferOffset) = val->usage;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->width;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->height;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->layerCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->viewFormatCount;
    bufferOffset += 4;

    if (val->pViewFormats) {
        int arrSize = val->viewFormatCount ? val->viewFormatCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pViewFormats, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkFramebufferAttachmentImageInfo(VkFramebufferAttachmentImageInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkImageCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->usage = *(VkImageUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->width = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->height = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->layerCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->viewFormatCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        VkFormat* arrValues = vt_alloc(memoryPool, dataSize9 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize9 * 4);
        bufferOffset += dataSize9 * 4;
        val->pViewFormats = arrValues;
    }
    else val->pViewFormats = NULL;
}

static inline int vt_sizeof_VkMemoryHeap(VkMemoryHeap* val) {
    int bufferSize = 0;
    bufferSize += 8; // size
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_VkMemoryHeap(VkMemoryHeap* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 8;
    *(VkMemoryHeapFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkMemoryHeap(VkMemoryHeap* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->flags = *(VkMemoryHeapFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkMemoryType(VkMemoryType* val) {
    int bufferSize = 0;
    bufferSize += 4; // propertyFlags
    bufferSize += 4; // heapIndex
    return bufferSize;
}

static inline void vt_serialize_VkMemoryType(VkMemoryType* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkMemoryPropertyFlags*)(outputBuffer + bufferOffset) = val->propertyFlags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->heapIndex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkMemoryType(VkMemoryType* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->propertyFlags = *(VkMemoryPropertyFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->heapIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceMemoryProperties(VkPhysicalDeviceMemoryProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // memoryTypeCount
    bufferSize += val->memoryTypeCount * vt_sizeof_VkMemoryType(&val->memoryTypes[0]);
    bufferSize += 4; // memoryHeapCount
    bufferSize += val->memoryHeapCount * vt_sizeof_VkMemoryHeap(&val->memoryHeaps[0]);
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceMemoryProperties(VkPhysicalDeviceMemoryProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->memoryTypeCount;
    bufferOffset += 4;
    if (val->memoryTypeCount <= VK_MAX_MEMORY_TYPES) memcpy(outputBuffer + bufferOffset, val->memoryTypes, val->memoryTypeCount * vt_sizeof_VkMemoryType(&val->memoryTypes[0]));
    bufferOffset += val->memoryTypeCount * vt_sizeof_VkMemoryType(&val->memoryTypes[0]);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->memoryHeapCount;
    bufferOffset += 4;
    if (val->memoryHeapCount <= VK_MAX_MEMORY_HEAPS) memcpy(outputBuffer + bufferOffset, val->memoryHeaps, val->memoryHeapCount * vt_sizeof_VkMemoryHeap(&val->memoryHeaps[0]));
    bufferOffset += val->memoryHeapCount * vt_sizeof_VkMemoryHeap(&val->memoryHeaps[0]);
}

static inline void vt_unserialize_VkPhysicalDeviceMemoryProperties(VkPhysicalDeviceMemoryProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->memoryTypeCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (val->memoryTypeCount <= VK_MAX_MEMORY_TYPES) memcpy(val->memoryTypes, inputBuffer + bufferOffset, val->memoryTypeCount * vt_sizeof_VkMemoryType(&val->memoryTypes[0]));
    bufferOffset += val->memoryTypeCount * vt_sizeof_VkMemoryType(&val->memoryTypes[0]);
    val->memoryHeapCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (val->memoryHeapCount <= VK_MAX_MEMORY_HEAPS) memcpy(val->memoryHeaps, inputBuffer + bufferOffset, val->memoryHeapCount * vt_sizeof_VkMemoryHeap(&val->memoryHeaps[0]));
    bufferOffset += val->memoryHeapCount * vt_sizeof_VkMemoryHeap(&val->memoryHeaps[0]);
}

static inline int vt_sizeof_VkPhysicalDeviceMemoryBudgetPropertiesEXT(VkPhysicalDeviceMemoryBudgetPropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += VK_MAX_MEMORY_HEAPS;
    bufferSize += VK_MAX_MEMORY_HEAPS;
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceMemoryBudgetPropertiesEXT(VkPhysicalDeviceMemoryBudgetPropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->heapBudget, VK_MAX_MEMORY_HEAPS);
    bufferOffset += VK_MAX_MEMORY_HEAPS;
    memcpy(outputBuffer + bufferOffset, val->heapUsage, VK_MAX_MEMORY_HEAPS);
    bufferOffset += VK_MAX_MEMORY_HEAPS;
}

static inline void vt_unserialize_VkPhysicalDeviceMemoryBudgetPropertiesEXT(VkPhysicalDeviceMemoryBudgetPropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    memcpy(val->heapBudget, inputBuffer + bufferOffset, VK_MAX_MEMORY_HEAPS);
    bufferOffset += VK_MAX_MEMORY_HEAPS;
    memcpy(val->heapUsage, inputBuffer + bufferOffset, VK_MAX_MEMORY_HEAPS);
    bufferOffset += VK_MAX_MEMORY_HEAPS;
}

static inline int vt_sizeof_VkQueueFamilyProperties(VkQueueFamilyProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // queueFlags
    bufferSize += 4; // queueCount
    bufferSize += 4; // timestampValidBits
    bufferSize += vt_sizeof_VkExtent3D(&val->minImageTransferGranularity);
    return bufferSize;
}

static inline void vt_serialize_VkQueueFamilyProperties(VkQueueFamilyProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkQueueFlags*)(outputBuffer + bufferOffset) = val->queueFlags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->queueCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->timestampValidBits;
    bufferOffset += 4;
    vt_serialize_VkExtent3D(&val->minImageTransferGranularity, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent3D(&val->minImageTransferGranularity);
}

static inline void vt_unserialize_VkQueueFamilyProperties(VkQueueFamilyProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->queueFlags = *(VkQueueFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queueCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->timestampValidBits = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkExtent3D(&val->minImageTransferGranularity, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent3D(&val->minImageTransferGranularity);
}

static inline int vt_sizeof_VkImageStencilUsageCreateInfo(VkImageStencilUsageCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // stencilUsage
    return bufferSize;
}

static inline void vt_serialize_VkImageStencilUsageCreateInfo(VkImageStencilUsageCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkImageUsageFlags*)(outputBuffer + bufferOffset) = val->stencilUsage;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkImageStencilUsageCreateInfo(VkImageStencilUsageCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->stencilUsage = *(VkImageUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceExternalImageFormatInfo(VkPhysicalDeviceExternalImageFormatInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // handleType
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceExternalImageFormatInfo(VkPhysicalDeviceExternalImageFormatInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkExternalMemoryHandleTypeFlagBits*)(outputBuffer + bufferOffset) = val->handleType;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceExternalImageFormatInfo(VkPhysicalDeviceExternalImageFormatInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->handleType = *(VkExternalMemoryHandleTypeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkImageFormatProperties(VkImageFormatProperties* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkExtent3D(&val->maxExtent);
    bufferSize += 4; // maxMipLevels
    bufferSize += 4; // maxArrayLayers
    bufferSize += 4; // sampleCounts
    bufferSize += 8; // maxResourceSize
    return bufferSize;
}

static inline void vt_serialize_VkImageFormatProperties(VkImageFormatProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    vt_serialize_VkExtent3D(&val->maxExtent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent3D(&val->maxExtent);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxMipLevels;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxArrayLayers;
    bufferOffset += 4;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->sampleCounts;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->maxResourceSize;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkImageFormatProperties(VkImageFormatProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    vt_unserialize_VkExtent3D(&val->maxExtent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent3D(&val->maxExtent);
    val->maxMipLevels = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxArrayLayers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxResourceSize = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkSamplerYcbcrConversionImageFormatProperties(VkSamplerYcbcrConversionImageFormatProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // combinedImageSamplerDescriptorCount
    return bufferSize;
}

static inline void vt_serialize_VkSamplerYcbcrConversionImageFormatProperties(VkSamplerYcbcrConversionImageFormatProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->combinedImageSamplerDescriptorCount;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSamplerYcbcrConversionImageFormatProperties(VkSamplerYcbcrConversionImageFormatProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->combinedImageSamplerDescriptorCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkExternalMemoryProperties(VkExternalMemoryProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // externalMemoryFeatures
    bufferSize += 4; // exportFromImportedHandleTypes
    bufferSize += 4; // compatibleHandleTypes
    return bufferSize;
}

static inline void vt_serialize_VkExternalMemoryProperties(VkExternalMemoryProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkExternalMemoryFeatureFlags*)(outputBuffer + bufferOffset) = val->externalMemoryFeatures;
    bufferOffset += 4;
    *(VkExternalMemoryHandleTypeFlags*)(outputBuffer + bufferOffset) = val->exportFromImportedHandleTypes;
    bufferOffset += 4;
    *(VkExternalMemoryHandleTypeFlags*)(outputBuffer + bufferOffset) = val->compatibleHandleTypes;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkExternalMemoryProperties(VkExternalMemoryProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->externalMemoryFeatures = *(VkExternalMemoryFeatureFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->exportFromImportedHandleTypes = *(VkExternalMemoryHandleTypeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->compatibleHandleTypes = *(VkExternalMemoryHandleTypeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkExternalImageFormatProperties(VkExternalImageFormatProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkExternalMemoryProperties(&val->externalMemoryProperties);
    return bufferSize;
}

static inline void vt_serialize_VkExternalImageFormatProperties(VkExternalImageFormatProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    vt_serialize_VkExternalMemoryProperties(&val->externalMemoryProperties, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExternalMemoryProperties(&val->externalMemoryProperties);
}

static inline void vt_unserialize_VkExternalImageFormatProperties(VkExternalImageFormatProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    vt_unserialize_VkExternalMemoryProperties(&val->externalMemoryProperties, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExternalMemoryProperties(&val->externalMemoryProperties);
}

static inline int vt_sizeof_VkPhysicalDeviceSparseProperties(VkPhysicalDeviceSparseProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // residencyStandard2DBlockShape
    bufferSize += 4; // residencyStandard2DMultisampleBlockShape
    bufferSize += 4; // residencyStandard3DBlockShape
    bufferSize += 4; // residencyAlignedMipSize
    bufferSize += 4; // residencyNonResidentStrict
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceSparseProperties(VkPhysicalDeviceSparseProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->residencyStandard2DBlockShape;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->residencyStandard2DMultisampleBlockShape;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->residencyStandard3DBlockShape;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->residencyAlignedMipSize;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->residencyNonResidentStrict;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceSparseProperties(VkPhysicalDeviceSparseProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->residencyStandard2DBlockShape = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->residencyStandard2DMultisampleBlockShape = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->residencyStandard3DBlockShape = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->residencyAlignedMipSize = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->residencyNonResidentStrict = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceLimits(VkPhysicalDeviceLimits* val) {
    int bufferSize = 0;
    bufferSize += 4; // maxImageDimension1D
    bufferSize += 4; // maxImageDimension2D
    bufferSize += 4; // maxImageDimension3D
    bufferSize += 4; // maxImageDimensionCube
    bufferSize += 4; // maxImageArrayLayers
    bufferSize += 4; // maxTexelBufferElements
    bufferSize += 4; // maxUniformBufferRange
    bufferSize += 4; // maxStorageBufferRange
    bufferSize += 4; // maxPushConstantsSize
    bufferSize += 4; // maxMemoryAllocationCount
    bufferSize += 4; // maxSamplerAllocationCount
    bufferSize += 8; // bufferImageGranularity
    bufferSize += 8; // sparseAddressSpaceSize
    bufferSize += 4; // maxBoundDescriptorSets
    bufferSize += 4; // maxPerStageDescriptorSamplers
    bufferSize += 4; // maxPerStageDescriptorUniformBuffers
    bufferSize += 4; // maxPerStageDescriptorStorageBuffers
    bufferSize += 4; // maxPerStageDescriptorSampledImages
    bufferSize += 4; // maxPerStageDescriptorStorageImages
    bufferSize += 4; // maxPerStageDescriptorInputAttachments
    bufferSize += 4; // maxPerStageResources
    bufferSize += 4; // maxDescriptorSetSamplers
    bufferSize += 4; // maxDescriptorSetUniformBuffers
    bufferSize += 4; // maxDescriptorSetUniformBuffersDynamic
    bufferSize += 4; // maxDescriptorSetStorageBuffers
    bufferSize += 4; // maxDescriptorSetStorageBuffersDynamic
    bufferSize += 4; // maxDescriptorSetSampledImages
    bufferSize += 4; // maxDescriptorSetStorageImages
    bufferSize += 4; // maxDescriptorSetInputAttachments
    bufferSize += 4; // maxVertexInputAttributes
    bufferSize += 4; // maxVertexInputBindings
    bufferSize += 4; // maxVertexInputAttributeOffset
    bufferSize += 4; // maxVertexInputBindingStride
    bufferSize += 4; // maxVertexOutputComponents
    bufferSize += 4; // maxTessellationGenerationLevel
    bufferSize += 4; // maxTessellationPatchSize
    bufferSize += 4; // maxTessellationControlPerVertexInputComponents
    bufferSize += 4; // maxTessellationControlPerVertexOutputComponents
    bufferSize += 4; // maxTessellationControlPerPatchOutputComponents
    bufferSize += 4; // maxTessellationControlTotalOutputComponents
    bufferSize += 4; // maxTessellationEvaluationInputComponents
    bufferSize += 4; // maxTessellationEvaluationOutputComponents
    bufferSize += 4; // maxGeometryShaderInvocations
    bufferSize += 4; // maxGeometryInputComponents
    bufferSize += 4; // maxGeometryOutputComponents
    bufferSize += 4; // maxGeometryOutputVertices
    bufferSize += 4; // maxGeometryTotalOutputComponents
    bufferSize += 4; // maxFragmentInputComponents
    bufferSize += 4; // maxFragmentOutputAttachments
    bufferSize += 4; // maxFragmentDualSrcAttachments
    bufferSize += 4; // maxFragmentCombinedOutputResources
    bufferSize += 4; // maxComputeSharedMemorySize
    bufferSize += 3 * 4;
    bufferSize += 4; // maxComputeWorkGroupInvocations
    bufferSize += 3 * 4;
    bufferSize += 4; // subPixelPrecisionBits
    bufferSize += 4; // subTexelPrecisionBits
    bufferSize += 4; // mipmapPrecisionBits
    bufferSize += 4; // maxDrawIndexedIndexValue
    bufferSize += 4; // maxDrawIndirectCount
    bufferSize += 4; // maxSamplerLodBias
    bufferSize += 4; // maxSamplerAnisotropy
    bufferSize += 4; // maxViewports
    bufferSize += 2 * 4;
    bufferSize += 2 * 4;
    bufferSize += 4; // viewportSubPixelBits
    bufferSize += 8; // minMemoryMapAlignment
    bufferSize += 8; // minTexelBufferOffsetAlignment
    bufferSize += 8; // minUniformBufferOffsetAlignment
    bufferSize += 8; // minStorageBufferOffsetAlignment
    bufferSize += 4; // minTexelOffset
    bufferSize += 4; // maxTexelOffset
    bufferSize += 4; // minTexelGatherOffset
    bufferSize += 4; // maxTexelGatherOffset
    bufferSize += 4; // minInterpolationOffset
    bufferSize += 4; // maxInterpolationOffset
    bufferSize += 4; // subPixelInterpolationOffsetBits
    bufferSize += 4; // maxFramebufferWidth
    bufferSize += 4; // maxFramebufferHeight
    bufferSize += 4; // maxFramebufferLayers
    bufferSize += 4; // framebufferColorSampleCounts
    bufferSize += 4; // framebufferDepthSampleCounts
    bufferSize += 4; // framebufferStencilSampleCounts
    bufferSize += 4; // framebufferNoAttachmentsSampleCounts
    bufferSize += 4; // maxColorAttachments
    bufferSize += 4; // sampledImageColorSampleCounts
    bufferSize += 4; // sampledImageIntegerSampleCounts
    bufferSize += 4; // sampledImageDepthSampleCounts
    bufferSize += 4; // sampledImageStencilSampleCounts
    bufferSize += 4; // storageImageSampleCounts
    bufferSize += 4; // maxSampleMaskWords
    bufferSize += 4; // timestampComputeAndGraphics
    bufferSize += 4; // timestampPeriod
    bufferSize += 4; // maxClipDistances
    bufferSize += 4; // maxCullDistances
    bufferSize += 4; // maxCombinedClipAndCullDistances
    bufferSize += 4; // discreteQueuePriorities
    bufferSize += 2 * 4;
    bufferSize += 2 * 4;
    bufferSize += 4; // pointSizeGranularity
    bufferSize += 4; // lineWidthGranularity
    bufferSize += 4; // strictLines
    bufferSize += 4; // standardSampleLocations
    bufferSize += 8; // optimalBufferCopyOffsetAlignment
    bufferSize += 8; // optimalBufferCopyRowPitchAlignment
    bufferSize += 8; // nonCoherentAtomSize
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceLimits(VkPhysicalDeviceLimits* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxImageDimension1D;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxImageDimension2D;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxImageDimension3D;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxImageDimensionCube;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxImageArrayLayers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTexelBufferElements;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxUniformBufferRange;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxStorageBufferRange;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPushConstantsSize;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxMemoryAllocationCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxSamplerAllocationCount;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->bufferImageGranularity;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->sparseAddressSpaceSize;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxBoundDescriptorSets;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorSamplers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUniformBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorStorageBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorSampledImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorStorageImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorInputAttachments;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageResources;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetSamplers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUniformBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUniformBuffersDynamic;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetStorageBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetStorageBuffersDynamic;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetSampledImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetStorageImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetInputAttachments;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxVertexInputAttributes;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxVertexInputBindings;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxVertexInputAttributeOffset;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxVertexInputBindingStride;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxVertexOutputComponents;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTessellationGenerationLevel;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTessellationPatchSize;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTessellationControlPerVertexInputComponents;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTessellationControlPerVertexOutputComponents;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTessellationControlPerPatchOutputComponents;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTessellationControlTotalOutputComponents;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTessellationEvaluationInputComponents;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTessellationEvaluationOutputComponents;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxGeometryShaderInvocations;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxGeometryInputComponents;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxGeometryOutputComponents;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxGeometryOutputVertices;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxGeometryTotalOutputComponents;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxFragmentInputComponents;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxFragmentOutputAttachments;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxFragmentDualSrcAttachments;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxFragmentCombinedOutputResources;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxComputeSharedMemorySize;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->maxComputeWorkGroupCount, 3 * 4);
    bufferOffset += 3 * 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxComputeWorkGroupInvocations;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->maxComputeWorkGroupSize, 3 * 4);
    bufferOffset += 3 * 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subPixelPrecisionBits;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subTexelPrecisionBits;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->mipmapPrecisionBits;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDrawIndexedIndexValue;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDrawIndirectCount;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->maxSamplerLodBias;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->maxSamplerAnisotropy;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxViewports;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->maxViewportDimensions, 2 * 4);
    bufferOffset += 2 * 4;
    memcpy(outputBuffer + bufferOffset, val->viewportBoundsRange, 2 * 4);
    bufferOffset += 2 * 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->viewportSubPixelBits;
    bufferOffset += 4;
    *(size_t*)(outputBuffer + bufferOffset) = val->minMemoryMapAlignment;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->minTexelBufferOffsetAlignment;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->minUniformBufferOffsetAlignment;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->minStorageBufferOffsetAlignment;
    bufferOffset += 8;
    *(int32_t*)(outputBuffer + bufferOffset) = val->minTexelOffset;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTexelOffset;
    bufferOffset += 4;
    *(int32_t*)(outputBuffer + bufferOffset) = val->minTexelGatherOffset;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTexelGatherOffset;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->minInterpolationOffset;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->maxInterpolationOffset;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subPixelInterpolationOffsetBits;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxFramebufferWidth;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxFramebufferHeight;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxFramebufferLayers;
    bufferOffset += 4;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->framebufferColorSampleCounts;
    bufferOffset += 4;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->framebufferDepthSampleCounts;
    bufferOffset += 4;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->framebufferStencilSampleCounts;
    bufferOffset += 4;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->framebufferNoAttachmentsSampleCounts;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxColorAttachments;
    bufferOffset += 4;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->sampledImageColorSampleCounts;
    bufferOffset += 4;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->sampledImageIntegerSampleCounts;
    bufferOffset += 4;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->sampledImageDepthSampleCounts;
    bufferOffset += 4;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->sampledImageStencilSampleCounts;
    bufferOffset += 4;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->storageImageSampleCounts;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxSampleMaskWords;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->timestampComputeAndGraphics;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->timestampPeriod;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxClipDistances;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxCullDistances;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxCombinedClipAndCullDistances;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->discreteQueuePriorities;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->pointSizeRange, 2 * 4);
    bufferOffset += 2 * 4;
    memcpy(outputBuffer + bufferOffset, val->lineWidthRange, 2 * 4);
    bufferOffset += 2 * 4;
    *(float*)(outputBuffer + bufferOffset) = val->pointSizeGranularity;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->lineWidthGranularity;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->strictLines;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->standardSampleLocations;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->optimalBufferCopyOffsetAlignment;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->optimalBufferCopyRowPitchAlignment;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->nonCoherentAtomSize;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkPhysicalDeviceLimits(VkPhysicalDeviceLimits* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->maxImageDimension1D = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxImageDimension2D = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxImageDimension3D = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxImageDimensionCube = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxImageArrayLayers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTexelBufferElements = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxUniformBufferRange = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxStorageBufferRange = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPushConstantsSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxMemoryAllocationCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxSamplerAllocationCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->bufferImageGranularity = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->sparseAddressSpaceSize = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->maxBoundDescriptorSets = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorSamplers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUniformBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorStorageBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorSampledImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorStorageImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorInputAttachments = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageResources = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetSamplers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUniformBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUniformBuffersDynamic = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetStorageBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetStorageBuffersDynamic = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetSampledImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetStorageImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetInputAttachments = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxVertexInputAttributes = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxVertexInputBindings = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxVertexInputAttributeOffset = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxVertexInputBindingStride = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxVertexOutputComponents = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTessellationGenerationLevel = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTessellationPatchSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTessellationControlPerVertexInputComponents = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTessellationControlPerVertexOutputComponents = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTessellationControlPerPatchOutputComponents = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTessellationControlTotalOutputComponents = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTessellationEvaluationInputComponents = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTessellationEvaluationOutputComponents = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxGeometryShaderInvocations = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxGeometryInputComponents = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxGeometryOutputComponents = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxGeometryOutputVertices = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxGeometryTotalOutputComponents = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxFragmentInputComponents = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxFragmentOutputAttachments = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxFragmentDualSrcAttachments = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxFragmentCombinedOutputResources = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxComputeSharedMemorySize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    memcpy(val->maxComputeWorkGroupCount, inputBuffer + bufferOffset, 3 * 4);
    bufferOffset += 3 * 4;
    val->maxComputeWorkGroupInvocations = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    memcpy(val->maxComputeWorkGroupSize, inputBuffer + bufferOffset, 3 * 4);
    bufferOffset += 3 * 4;
    val->subPixelPrecisionBits = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->subTexelPrecisionBits = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->mipmapPrecisionBits = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDrawIndexedIndexValue = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDrawIndirectCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxSamplerLodBias = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxSamplerAnisotropy = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxViewports = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    memcpy(val->maxViewportDimensions, inputBuffer + bufferOffset, 2 * 4);
    bufferOffset += 2 * 4;
    memcpy(val->viewportBoundsRange, inputBuffer + bufferOffset, 2 * 4);
    bufferOffset += 2 * 4;
    val->viewportSubPixelBits = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->minMemoryMapAlignment = *(size_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->minTexelBufferOffsetAlignment = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->minUniformBufferOffsetAlignment = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->minStorageBufferOffsetAlignment = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->minTexelOffset = *(int32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTexelOffset = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->minTexelGatherOffset = *(int32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTexelGatherOffset = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->minInterpolationOffset = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxInterpolationOffset = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->subPixelInterpolationOffsetBits = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxFramebufferWidth = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxFramebufferHeight = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxFramebufferLayers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->framebufferColorSampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->framebufferDepthSampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->framebufferStencilSampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->framebufferNoAttachmentsSampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxColorAttachments = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sampledImageColorSampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sampledImageIntegerSampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sampledImageDepthSampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sampledImageStencilSampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storageImageSampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxSampleMaskWords = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->timestampComputeAndGraphics = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->timestampPeriod = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxClipDistances = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxCullDistances = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxCombinedClipAndCullDistances = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->discreteQueuePriorities = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    memcpy(val->pointSizeRange, inputBuffer + bufferOffset, 2 * 4);
    bufferOffset += 2 * 4;
    memcpy(val->lineWidthRange, inputBuffer + bufferOffset, 2 * 4);
    bufferOffset += 2 * 4;
    val->pointSizeGranularity = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->lineWidthGranularity = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->strictLines = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->standardSampleLocations = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->optimalBufferCopyOffsetAlignment = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->optimalBufferCopyRowPitchAlignment = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->nonCoherentAtomSize = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkFormatProperties(VkFormatProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // linearTilingFeatures
    bufferSize += 4; // optimalTilingFeatures
    bufferSize += 4; // bufferFeatures
    return bufferSize;
}

static inline void vt_serialize_VkFormatProperties(VkFormatProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkFormatFeatureFlags*)(outputBuffer + bufferOffset) = val->linearTilingFeatures;
    bufferOffset += 4;
    *(VkFormatFeatureFlags*)(outputBuffer + bufferOffset) = val->optimalTilingFeatures;
    bufferOffset += 4;
    *(VkFormatFeatureFlags*)(outputBuffer + bufferOffset) = val->bufferFeatures;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkFormatProperties(VkFormatProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->linearTilingFeatures = *(VkFormatFeatureFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->optimalTilingFeatures = *(VkFormatFeatureFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->bufferFeatures = *(VkFormatFeatureFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkFormatProperties3(VkFormatProperties3* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // linearTilingFeatures
    bufferSize += 8; // optimalTilingFeatures
    bufferSize += 8; // bufferFeatures
    return bufferSize;
}

static inline void vt_serialize_VkFormatProperties3(VkFormatProperties3* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkFormatFeatureFlags2*)(outputBuffer + bufferOffset) = val->linearTilingFeatures;
    bufferOffset += 8;
    *(VkFormatFeatureFlags2*)(outputBuffer + bufferOffset) = val->optimalTilingFeatures;
    bufferOffset += 8;
    *(VkFormatFeatureFlags2*)(outputBuffer + bufferOffset) = val->bufferFeatures;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkFormatProperties3(VkFormatProperties3* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->linearTilingFeatures = *(VkFormatFeatureFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->optimalTilingFeatures = *(VkFormatFeatureFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->bufferFeatures = *(VkFormatFeatureFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkPhysicalDeviceProperties(VkPhysicalDeviceProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // apiVersion
    bufferSize += 4; // driverVersion
    bufferSize += 4; // vendorID
    bufferSize += 4; // deviceID
    bufferSize += 4; // deviceType
    bufferSize += strlen(val->deviceName) + 1;
    bufferSize += VK_UUID_SIZE;
    bufferSize += vt_sizeof_VkPhysicalDeviceLimits(&val->limits);
    bufferSize += vt_sizeof_VkPhysicalDeviceSparseProperties(&val->sparseProperties);
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceProperties(VkPhysicalDeviceProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->apiVersion;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->driverVersion;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->vendorID;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceID;
    bufferOffset += 4;
    *(VkPhysicalDeviceType*)(outputBuffer + bufferOffset) = val->deviceType;
    bufferOffset += 4;
    int dataSize6 = strlen(val->deviceName) + 1;
    *(int*)(outputBuffer + bufferOffset) = dataSize6;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->deviceName, dataSize6);
    bufferOffset += dataSize6;
    memcpy(outputBuffer + bufferOffset, val->pipelineCacheUUID, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
    vt_serialize_VkPhysicalDeviceLimits(&val->limits, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkPhysicalDeviceLimits(&val->limits);
    vt_serialize_VkPhysicalDeviceSparseProperties(&val->sparseProperties, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkPhysicalDeviceSparseProperties(&val->sparseProperties);
}

static inline void vt_unserialize_VkPhysicalDeviceProperties(VkPhysicalDeviceProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->apiVersion = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->driverVersion = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->vendorID = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->deviceID = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->deviceType = *(VkPhysicalDeviceType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        memcpy(val->deviceName, inputBuffer + bufferOffset, dataSize6);
        bufferOffset += dataSize6;
    }
    memcpy(val->pipelineCacheUUID, inputBuffer + bufferOffset, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
    vt_unserialize_VkPhysicalDeviceLimits(&val->limits, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkPhysicalDeviceLimits(&val->limits);
    vt_unserialize_VkPhysicalDeviceSparseProperties(&val->sparseProperties, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkPhysicalDeviceSparseProperties(&val->sparseProperties);
}

static inline int vt_sizeof_VkPhysicalDeviceMapMemoryPlacedPropertiesEXT(VkPhysicalDeviceMapMemoryPlacedPropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // minPlacedMemoryMapAlignment
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceMapMemoryPlacedPropertiesEXT(VkPhysicalDeviceMapMemoryPlacedPropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->minPlacedMemoryMapAlignment;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkPhysicalDeviceMapMemoryPlacedPropertiesEXT(VkPhysicalDeviceMapMemoryPlacedPropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->minPlacedMemoryMapAlignment = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT(VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += VK_UUID_SIZE;
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT(VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->shaderModuleIdentifierAlgorithmUUID, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
}

static inline void vt_unserialize_VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT(VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    memcpy(val->shaderModuleIdentifierAlgorithmUUID, inputBuffer + bufferOffset, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
}

static inline int vt_sizeof_VkPhysicalDeviceShaderIntegerDotProductProperties(VkPhysicalDeviceShaderIntegerDotProductProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // integerDotProduct8BitUnsignedAccelerated
    bufferSize += 4; // integerDotProduct8BitSignedAccelerated
    bufferSize += 4; // integerDotProduct8BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProduct4x8BitPackedUnsignedAccelerated
    bufferSize += 4; // integerDotProduct4x8BitPackedSignedAccelerated
    bufferSize += 4; // integerDotProduct4x8BitPackedMixedSignednessAccelerated
    bufferSize += 4; // integerDotProduct16BitUnsignedAccelerated
    bufferSize += 4; // integerDotProduct16BitSignedAccelerated
    bufferSize += 4; // integerDotProduct16BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProduct32BitUnsignedAccelerated
    bufferSize += 4; // integerDotProduct32BitSignedAccelerated
    bufferSize += 4; // integerDotProduct32BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProduct64BitUnsignedAccelerated
    bufferSize += 4; // integerDotProduct64BitSignedAccelerated
    bufferSize += 4; // integerDotProduct64BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating8BitUnsignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating8BitSignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating8BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating4x8BitPackedUnsignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating4x8BitPackedSignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating4x8BitPackedMixedSignednessAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating16BitUnsignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating16BitSignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating16BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating32BitUnsignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating32BitSignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating32BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating64BitUnsignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating64BitSignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating64BitMixedSignednessAccelerated
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceShaderIntegerDotProductProperties(VkPhysicalDeviceShaderIntegerDotProductProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct8BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct8BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct8BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct4x8BitPackedUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct4x8BitPackedSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct4x8BitPackedMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct16BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct16BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct16BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct32BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct32BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct32BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct64BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct64BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct64BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating8BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating8BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating8BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating4x8BitPackedUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating4x8BitPackedSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating4x8BitPackedMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating16BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating16BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating16BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating32BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating32BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating32BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating64BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating64BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating64BitMixedSignednessAccelerated;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceShaderIntegerDotProductProperties(VkPhysicalDeviceShaderIntegerDotProductProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->integerDotProduct8BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct8BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct8BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct4x8BitPackedUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct4x8BitPackedSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct4x8BitPackedMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct16BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct16BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct16BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct32BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct32BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct32BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct64BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct64BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct64BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating8BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating8BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating8BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating4x8BitPackedUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating4x8BitPackedSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating4x8BitPackedMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating16BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating16BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating16BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating32BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating32BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating32BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating64BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating64BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating64BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceProvokingVertexPropertiesEXT(VkPhysicalDeviceProvokingVertexPropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // provokingVertexModePerPipeline
    bufferSize += 4; // transformFeedbackPreservesTriangleFanProvokingVertex
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceProvokingVertexPropertiesEXT(VkPhysicalDeviceProvokingVertexPropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->provokingVertexModePerPipeline;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->transformFeedbackPreservesTriangleFanProvokingVertex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceProvokingVertexPropertiesEXT(VkPhysicalDeviceProvokingVertexPropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->provokingVertexModePerPipeline = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->transformFeedbackPreservesTriangleFanProvokingVertex = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceRobustness2PropertiesEXT(VkPhysicalDeviceRobustness2PropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // robustStorageBufferAccessSizeAlignment
    bufferSize += 8; // robustUniformBufferAccessSizeAlignment
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceRobustness2PropertiesEXT(VkPhysicalDeviceRobustness2PropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->robustStorageBufferAccessSizeAlignment;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->robustUniformBufferAccessSizeAlignment;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkPhysicalDeviceRobustness2PropertiesEXT(VkPhysicalDeviceRobustness2PropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->robustStorageBufferAccessSizeAlignment = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->robustUniformBufferAccessSizeAlignment = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkPhysicalDeviceExtendedDynamicState3PropertiesEXT(VkPhysicalDeviceExtendedDynamicState3PropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // dynamicPrimitiveTopologyUnrestricted
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceExtendedDynamicState3PropertiesEXT(VkPhysicalDeviceExtendedDynamicState3PropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->dynamicPrimitiveTopologyUnrestricted;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceExtendedDynamicState3PropertiesEXT(VkPhysicalDeviceExtendedDynamicState3PropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->dynamicPrimitiveTopologyUnrestricted = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceCustomBorderColorPropertiesEXT(VkPhysicalDeviceCustomBorderColorPropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maxCustomBorderColorSamplers
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceCustomBorderColorPropertiesEXT(VkPhysicalDeviceCustomBorderColorPropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxCustomBorderColorSamplers;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceCustomBorderColorPropertiesEXT(VkPhysicalDeviceCustomBorderColorPropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxCustomBorderColorSamplers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceVulkan13Properties(VkPhysicalDeviceVulkan13Properties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // minSubgroupSize
    bufferSize += 4; // maxSubgroupSize
    bufferSize += 4; // maxComputeWorkgroupSubgroups
    bufferSize += 4; // requiredSubgroupSizeStages
    bufferSize += 4; // maxInlineUniformBlockSize
    bufferSize += 4; // maxPerStageDescriptorInlineUniformBlocks
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindInlineUniformBlocks
    bufferSize += 4; // maxDescriptorSetInlineUniformBlocks
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindInlineUniformBlocks
    bufferSize += 4; // maxInlineUniformTotalSize
    bufferSize += 4; // integerDotProduct8BitUnsignedAccelerated
    bufferSize += 4; // integerDotProduct8BitSignedAccelerated
    bufferSize += 4; // integerDotProduct8BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProduct4x8BitPackedUnsignedAccelerated
    bufferSize += 4; // integerDotProduct4x8BitPackedSignedAccelerated
    bufferSize += 4; // integerDotProduct4x8BitPackedMixedSignednessAccelerated
    bufferSize += 4; // integerDotProduct16BitUnsignedAccelerated
    bufferSize += 4; // integerDotProduct16BitSignedAccelerated
    bufferSize += 4; // integerDotProduct16BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProduct32BitUnsignedAccelerated
    bufferSize += 4; // integerDotProduct32BitSignedAccelerated
    bufferSize += 4; // integerDotProduct32BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProduct64BitUnsignedAccelerated
    bufferSize += 4; // integerDotProduct64BitSignedAccelerated
    bufferSize += 4; // integerDotProduct64BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating8BitUnsignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating8BitSignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating8BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating4x8BitPackedUnsignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating4x8BitPackedSignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating4x8BitPackedMixedSignednessAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating16BitUnsignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating16BitSignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating16BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating32BitUnsignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating32BitSignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating32BitMixedSignednessAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating64BitUnsignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating64BitSignedAccelerated
    bufferSize += 4; // integerDotProductAccumulatingSaturating64BitMixedSignednessAccelerated
    bufferSize += 8; // storageTexelBufferOffsetAlignmentBytes
    bufferSize += 4; // storageTexelBufferOffsetSingleTexelAlignment
    bufferSize += 8; // uniformTexelBufferOffsetAlignmentBytes
    bufferSize += 4; // uniformTexelBufferOffsetSingleTexelAlignment
    bufferSize += 8; // maxBufferSize
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceVulkan13Properties(VkPhysicalDeviceVulkan13Properties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->minSubgroupSize;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxSubgroupSize;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxComputeWorkgroupSubgroups;
    bufferOffset += 4;
    *(VkShaderStageFlags*)(outputBuffer + bufferOffset) = val->requiredSubgroupSizeStages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxInlineUniformBlockSize;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorInlineUniformBlocks;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindInlineUniformBlocks;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetInlineUniformBlocks;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindInlineUniformBlocks;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxInlineUniformTotalSize;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct8BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct8BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct8BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct4x8BitPackedUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct4x8BitPackedSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct4x8BitPackedMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct16BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct16BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct16BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct32BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct32BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct32BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct64BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct64BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProduct64BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating8BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating8BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating8BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating4x8BitPackedUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating4x8BitPackedSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating4x8BitPackedMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating16BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating16BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating16BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating32BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating32BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating32BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating64BitUnsignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating64BitSignedAccelerated;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->integerDotProductAccumulatingSaturating64BitMixedSignednessAccelerated;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->storageTexelBufferOffsetAlignmentBytes;
    bufferOffset += 8;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storageTexelBufferOffsetSingleTexelAlignment;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->uniformTexelBufferOffsetAlignmentBytes;
    bufferOffset += 8;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->uniformTexelBufferOffsetSingleTexelAlignment;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->maxBufferSize;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkPhysicalDeviceVulkan13Properties(VkPhysicalDeviceVulkan13Properties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->minSubgroupSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxSubgroupSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxComputeWorkgroupSubgroups = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->requiredSubgroupSizeStages = *(VkShaderStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxInlineUniformBlockSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorInlineUniformBlocks = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindInlineUniformBlocks = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetInlineUniformBlocks = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindInlineUniformBlocks = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxInlineUniformTotalSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct8BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct8BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct8BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct4x8BitPackedUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct4x8BitPackedSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct4x8BitPackedMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct16BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct16BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct16BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct32BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct32BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct32BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct64BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct64BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProduct64BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating8BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating8BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating8BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating4x8BitPackedUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating4x8BitPackedSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating4x8BitPackedMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating16BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating16BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating16BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating32BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating32BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating32BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating64BitUnsignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating64BitSignedAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->integerDotProductAccumulatingSaturating64BitMixedSignednessAccelerated = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storageTexelBufferOffsetAlignmentBytes = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->storageTexelBufferOffsetSingleTexelAlignment = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->uniformTexelBufferOffsetAlignmentBytes = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->uniformTexelBufferOffsetSingleTexelAlignment = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxBufferSize = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkConformanceVersion(VkConformanceVersion* val) {
    int bufferSize = 0;
    bufferSize += 1; // major
    bufferSize += 1; // minor
    bufferSize += 1; // subminor
    bufferSize += 1; // patch
    return bufferSize;
}

static inline void vt_serialize_VkConformanceVersion(VkConformanceVersion* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint8_t*)(outputBuffer + bufferOffset) = val->major;
    bufferOffset += 1;
    *(uint8_t*)(outputBuffer + bufferOffset) = val->minor;
    bufferOffset += 1;
    *(uint8_t*)(outputBuffer + bufferOffset) = val->subminor;
    bufferOffset += 1;
    *(uint8_t*)(outputBuffer + bufferOffset) = val->patch;
    bufferOffset += 1;
}

static inline void vt_unserialize_VkConformanceVersion(VkConformanceVersion* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->major = *(uint8_t*)(inputBuffer + bufferOffset);
    bufferOffset += 1;
    val->minor = *(uint8_t*)(inputBuffer + bufferOffset);
    bufferOffset += 1;
    val->subminor = *(uint8_t*)(inputBuffer + bufferOffset);
    bufferOffset += 1;
    val->patch = *(uint8_t*)(inputBuffer + bufferOffset);
    bufferOffset += 1;
}

static inline int vt_sizeof_VkPhysicalDeviceVulkan12Properties(VkPhysicalDeviceVulkan12Properties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // driverID
    bufferSize += strlen(val->driverName) + 1;
    bufferSize += strlen(val->driverInfo) + 1;
    bufferSize += vt_sizeof_VkConformanceVersion(&val->conformanceVersion);
    bufferSize += 4; // denormBehaviorIndependence
    bufferSize += 4; // roundingModeIndependence
    bufferSize += 4; // shaderSignedZeroInfNanPreserveFloat16
    bufferSize += 4; // shaderSignedZeroInfNanPreserveFloat32
    bufferSize += 4; // shaderSignedZeroInfNanPreserveFloat64
    bufferSize += 4; // shaderDenormPreserveFloat16
    bufferSize += 4; // shaderDenormPreserveFloat32
    bufferSize += 4; // shaderDenormPreserveFloat64
    bufferSize += 4; // shaderDenormFlushToZeroFloat16
    bufferSize += 4; // shaderDenormFlushToZeroFloat32
    bufferSize += 4; // shaderDenormFlushToZeroFloat64
    bufferSize += 4; // shaderRoundingModeRTEFloat16
    bufferSize += 4; // shaderRoundingModeRTEFloat32
    bufferSize += 4; // shaderRoundingModeRTEFloat64
    bufferSize += 4; // shaderRoundingModeRTZFloat16
    bufferSize += 4; // shaderRoundingModeRTZFloat32
    bufferSize += 4; // shaderRoundingModeRTZFloat64
    bufferSize += 4; // maxUpdateAfterBindDescriptorsInAllPools
    bufferSize += 4; // shaderUniformBufferArrayNonUniformIndexingNative
    bufferSize += 4; // shaderSampledImageArrayNonUniformIndexingNative
    bufferSize += 4; // shaderStorageBufferArrayNonUniformIndexingNative
    bufferSize += 4; // shaderStorageImageArrayNonUniformIndexingNative
    bufferSize += 4; // shaderInputAttachmentArrayNonUniformIndexingNative
    bufferSize += 4; // robustBufferAccessUpdateAfterBind
    bufferSize += 4; // quadDivergentImplicitLod
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindSamplers
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindUniformBuffers
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindStorageBuffers
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindSampledImages
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindStorageImages
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindInputAttachments
    bufferSize += 4; // maxPerStageUpdateAfterBindResources
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindSamplers
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindUniformBuffers
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindUniformBuffersDynamic
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindStorageBuffers
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindStorageBuffersDynamic
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindSampledImages
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindStorageImages
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindInputAttachments
    bufferSize += 4; // supportedDepthResolveModes
    bufferSize += 4; // supportedStencilResolveModes
    bufferSize += 4; // independentResolveNone
    bufferSize += 4; // independentResolve
    bufferSize += 4; // filterMinmaxSingleComponentFormats
    bufferSize += 4; // filterMinmaxImageComponentMapping
    bufferSize += 8; // maxTimelineSemaphoreValueDifference
    bufferSize += 4; // framebufferIntegerColorSampleCounts
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceVulkan12Properties(VkPhysicalDeviceVulkan12Properties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDriverId*)(outputBuffer + bufferOffset) = val->driverID;
    bufferOffset += 4;
    int dataSize4 = strlen(val->driverName) + 1;
    *(int*)(outputBuffer + bufferOffset) = dataSize4;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->driverName, dataSize4);
    bufferOffset += dataSize4;
    int dataSize5 = strlen(val->driverInfo) + 1;
    *(int*)(outputBuffer + bufferOffset) = dataSize5;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->driverInfo, dataSize5);
    bufferOffset += dataSize5;
    vt_serialize_VkConformanceVersion(&val->conformanceVersion, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkConformanceVersion(&val->conformanceVersion);
    *(VkShaderFloatControlsIndependence*)(outputBuffer + bufferOffset) = val->denormBehaviorIndependence;
    bufferOffset += 4;
    *(VkShaderFloatControlsIndependence*)(outputBuffer + bufferOffset) = val->roundingModeIndependence;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSignedZeroInfNanPreserveFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSignedZeroInfNanPreserveFloat32;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSignedZeroInfNanPreserveFloat64;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormPreserveFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormPreserveFloat32;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormPreserveFloat64;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormFlushToZeroFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormFlushToZeroFloat32;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormFlushToZeroFloat64;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTEFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTEFloat32;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTEFloat64;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTZFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTZFloat32;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTZFloat64;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxUpdateAfterBindDescriptorsInAllPools;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderUniformBufferArrayNonUniformIndexingNative;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSampledImageArrayNonUniformIndexingNative;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageBufferArrayNonUniformIndexingNative;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageImageArrayNonUniformIndexingNative;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderInputAttachmentArrayNonUniformIndexingNative;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->robustBufferAccessUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->quadDivergentImplicitLod;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindSamplers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindUniformBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindStorageBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindSampledImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindStorageImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindInputAttachments;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageUpdateAfterBindResources;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindSamplers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindUniformBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindUniformBuffersDynamic;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindStorageBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindStorageBuffersDynamic;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindSampledImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindStorageImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindInputAttachments;
    bufferOffset += 4;
    *(VkResolveModeFlags*)(outputBuffer + bufferOffset) = val->supportedDepthResolveModes;
    bufferOffset += 4;
    *(VkResolveModeFlags*)(outputBuffer + bufferOffset) = val->supportedStencilResolveModes;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->independentResolveNone;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->independentResolve;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->filterMinmaxSingleComponentFormats;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->filterMinmaxImageComponentMapping;
    bufferOffset += 4;
    *(uint64_t*)(outputBuffer + bufferOffset) = val->maxTimelineSemaphoreValueDifference;
    bufferOffset += 8;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->framebufferIntegerColorSampleCounts;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceVulkan12Properties(VkPhysicalDeviceVulkan12Properties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->driverID = *(VkDriverId*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        memcpy(val->driverName, inputBuffer + bufferOffset, dataSize4);
        bufferOffset += dataSize4;
    }

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        memcpy(val->driverInfo, inputBuffer + bufferOffset, dataSize5);
        bufferOffset += dataSize5;
    }
    vt_unserialize_VkConformanceVersion(&val->conformanceVersion, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkConformanceVersion(&val->conformanceVersion);
    val->denormBehaviorIndependence = *(VkShaderFloatControlsIndependence*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->roundingModeIndependence = *(VkShaderFloatControlsIndependence*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSignedZeroInfNanPreserveFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSignedZeroInfNanPreserveFloat32 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSignedZeroInfNanPreserveFloat64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormPreserveFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormPreserveFloat32 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormPreserveFloat64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormFlushToZeroFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormFlushToZeroFloat32 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormFlushToZeroFloat64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTEFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTEFloat32 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTEFloat64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTZFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTZFloat32 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTZFloat64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxUpdateAfterBindDescriptorsInAllPools = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderUniformBufferArrayNonUniformIndexingNative = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSampledImageArrayNonUniformIndexingNative = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageBufferArrayNonUniformIndexingNative = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageImageArrayNonUniformIndexingNative = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderInputAttachmentArrayNonUniformIndexingNative = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->robustBufferAccessUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->quadDivergentImplicitLod = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindSamplers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindUniformBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindStorageBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindSampledImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindStorageImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindInputAttachments = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageUpdateAfterBindResources = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindSamplers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindUniformBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindUniformBuffersDynamic = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindStorageBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindStorageBuffersDynamic = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindSampledImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindStorageImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindInputAttachments = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->supportedDepthResolveModes = *(VkResolveModeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->supportedStencilResolveModes = *(VkResolveModeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->independentResolveNone = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->independentResolve = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->filterMinmaxSingleComponentFormats = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->filterMinmaxImageComponentMapping = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTimelineSemaphoreValueDifference = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->framebufferIntegerColorSampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceVulkan11Properties(VkPhysicalDeviceVulkan11Properties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += VK_UUID_SIZE;
    bufferSize += VK_UUID_SIZE;
    bufferSize += VK_LUID_SIZE;
    bufferSize += 4; // deviceNodeMask
    bufferSize += 4; // deviceLUIDValid
    bufferSize += 4; // subgroupSize
    bufferSize += 4; // subgroupSupportedStages
    bufferSize += 4; // subgroupSupportedOperations
    bufferSize += 4; // subgroupQuadOperationsInAllStages
    bufferSize += 4; // pointClippingBehavior
    bufferSize += 4; // maxMultiviewViewCount
    bufferSize += 4; // maxMultiviewInstanceIndex
    bufferSize += 4; // protectedNoFault
    bufferSize += 4; // maxPerSetDescriptors
    bufferSize += 8; // maxMemoryAllocationSize
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceVulkan11Properties(VkPhysicalDeviceVulkan11Properties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->deviceUUID, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
    memcpy(outputBuffer + bufferOffset, val->driverUUID, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
    memcpy(outputBuffer + bufferOffset, val->deviceLUID, VK_LUID_SIZE);
    bufferOffset += VK_LUID_SIZE;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceNodeMask;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->deviceLUIDValid;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subgroupSize;
    bufferOffset += 4;
    *(VkShaderStageFlags*)(outputBuffer + bufferOffset) = val->subgroupSupportedStages;
    bufferOffset += 4;
    *(VkSubgroupFeatureFlags*)(outputBuffer + bufferOffset) = val->subgroupSupportedOperations;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->subgroupQuadOperationsInAllStages;
    bufferOffset += 4;
    *(VkPointClippingBehavior*)(outputBuffer + bufferOffset) = val->pointClippingBehavior;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxMultiviewViewCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxMultiviewInstanceIndex;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->protectedNoFault;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerSetDescriptors;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->maxMemoryAllocationSize;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkPhysicalDeviceVulkan11Properties(VkPhysicalDeviceVulkan11Properties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    memcpy(val->deviceUUID, inputBuffer + bufferOffset, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
    memcpy(val->driverUUID, inputBuffer + bufferOffset, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
    memcpy(val->deviceLUID, inputBuffer + bufferOffset, VK_LUID_SIZE);
    bufferOffset += VK_LUID_SIZE;
    val->deviceNodeMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->deviceLUIDValid = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->subgroupSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->subgroupSupportedStages = *(VkShaderStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->subgroupSupportedOperations = *(VkSubgroupFeatureFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->subgroupQuadOperationsInAllStages = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->pointClippingBehavior = *(VkPointClippingBehavior*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxMultiviewViewCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxMultiviewInstanceIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->protectedNoFault = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerSetDescriptors = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxMemoryAllocationSize = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkPhysicalDeviceSubgroupSizeControlProperties(VkPhysicalDeviceSubgroupSizeControlProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // minSubgroupSize
    bufferSize += 4; // maxSubgroupSize
    bufferSize += 4; // maxComputeWorkgroupSubgroups
    bufferSize += 4; // requiredSubgroupSizeStages
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceSubgroupSizeControlProperties(VkPhysicalDeviceSubgroupSizeControlProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->minSubgroupSize;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxSubgroupSize;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxComputeWorkgroupSubgroups;
    bufferOffset += 4;
    *(VkShaderStageFlags*)(outputBuffer + bufferOffset) = val->requiredSubgroupSizeStages;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceSubgroupSizeControlProperties(VkPhysicalDeviceSubgroupSizeControlProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->minSubgroupSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxSubgroupSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxComputeWorkgroupSubgroups = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->requiredSubgroupSizeStages = *(VkShaderStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceTexelBufferAlignmentProperties(VkPhysicalDeviceTexelBufferAlignmentProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // storageTexelBufferOffsetAlignmentBytes
    bufferSize += 4; // storageTexelBufferOffsetSingleTexelAlignment
    bufferSize += 8; // uniformTexelBufferOffsetAlignmentBytes
    bufferSize += 4; // uniformTexelBufferOffsetSingleTexelAlignment
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceTexelBufferAlignmentProperties(VkPhysicalDeviceTexelBufferAlignmentProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->storageTexelBufferOffsetAlignmentBytes;
    bufferOffset += 8;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storageTexelBufferOffsetSingleTexelAlignment;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->uniformTexelBufferOffsetAlignmentBytes;
    bufferOffset += 8;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->uniformTexelBufferOffsetSingleTexelAlignment;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceTexelBufferAlignmentProperties(VkPhysicalDeviceTexelBufferAlignmentProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->storageTexelBufferOffsetAlignmentBytes = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->storageTexelBufferOffsetSingleTexelAlignment = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->uniformTexelBufferOffsetAlignmentBytes = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->uniformTexelBufferOffsetSingleTexelAlignment = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceDepthStencilResolveProperties(VkPhysicalDeviceDepthStencilResolveProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // supportedDepthResolveModes
    bufferSize += 4; // supportedStencilResolveModes
    bufferSize += 4; // independentResolveNone
    bufferSize += 4; // independentResolve
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceDepthStencilResolveProperties(VkPhysicalDeviceDepthStencilResolveProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkResolveModeFlags*)(outputBuffer + bufferOffset) = val->supportedDepthResolveModes;
    bufferOffset += 4;
    *(VkResolveModeFlags*)(outputBuffer + bufferOffset) = val->supportedStencilResolveModes;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->independentResolveNone;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->independentResolve;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceDepthStencilResolveProperties(VkPhysicalDeviceDepthStencilResolveProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->supportedDepthResolveModes = *(VkResolveModeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->supportedStencilResolveModes = *(VkResolveModeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->independentResolveNone = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->independentResolve = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR(VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maxVertexAttribDivisor
    bufferSize += 4; // supportsNonZeroFirstInstance
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR(VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxVertexAttribDivisor;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->supportsNonZeroFirstInstance;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR(VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxVertexAttribDivisor = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->supportsNonZeroFirstInstance = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT(VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maxVertexAttribDivisor
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT(VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxVertexAttribDivisor;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT(VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxVertexAttribDivisor = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceTimelineSemaphoreProperties(VkPhysicalDeviceTimelineSemaphoreProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // maxTimelineSemaphoreValueDifference
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceTimelineSemaphoreProperties(VkPhysicalDeviceTimelineSemaphoreProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint64_t*)(outputBuffer + bufferOffset) = val->maxTimelineSemaphoreValueDifference;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkPhysicalDeviceTimelineSemaphoreProperties(VkPhysicalDeviceTimelineSemaphoreProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxTimelineSemaphoreValueDifference = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkPhysicalDeviceDescriptorIndexingProperties(VkPhysicalDeviceDescriptorIndexingProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maxUpdateAfterBindDescriptorsInAllPools
    bufferSize += 4; // shaderUniformBufferArrayNonUniformIndexingNative
    bufferSize += 4; // shaderSampledImageArrayNonUniformIndexingNative
    bufferSize += 4; // shaderStorageBufferArrayNonUniformIndexingNative
    bufferSize += 4; // shaderStorageImageArrayNonUniformIndexingNative
    bufferSize += 4; // shaderInputAttachmentArrayNonUniformIndexingNative
    bufferSize += 4; // robustBufferAccessUpdateAfterBind
    bufferSize += 4; // quadDivergentImplicitLod
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindSamplers
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindUniformBuffers
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindStorageBuffers
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindSampledImages
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindStorageImages
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindInputAttachments
    bufferSize += 4; // maxPerStageUpdateAfterBindResources
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindSamplers
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindUniformBuffers
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindUniformBuffersDynamic
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindStorageBuffers
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindStorageBuffersDynamic
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindSampledImages
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindStorageImages
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindInputAttachments
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceDescriptorIndexingProperties(VkPhysicalDeviceDescriptorIndexingProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxUpdateAfterBindDescriptorsInAllPools;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderUniformBufferArrayNonUniformIndexingNative;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSampledImageArrayNonUniformIndexingNative;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageBufferArrayNonUniformIndexingNative;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageImageArrayNonUniformIndexingNative;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderInputAttachmentArrayNonUniformIndexingNative;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->robustBufferAccessUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->quadDivergentImplicitLod;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindSamplers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindUniformBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindStorageBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindSampledImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindStorageImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindInputAttachments;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageUpdateAfterBindResources;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindSamplers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindUniformBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindUniformBuffersDynamic;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindStorageBuffers;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindStorageBuffersDynamic;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindSampledImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindStorageImages;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindInputAttachments;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceDescriptorIndexingProperties(VkPhysicalDeviceDescriptorIndexingProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxUpdateAfterBindDescriptorsInAllPools = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderUniformBufferArrayNonUniformIndexingNative = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSampledImageArrayNonUniformIndexingNative = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageBufferArrayNonUniformIndexingNative = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageImageArrayNonUniformIndexingNative = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderInputAttachmentArrayNonUniformIndexingNative = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->robustBufferAccessUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->quadDivergentImplicitLod = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindSamplers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindUniformBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindStorageBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindSampledImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindStorageImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindInputAttachments = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageUpdateAfterBindResources = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindSamplers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindUniformBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindUniformBuffersDynamic = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindStorageBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindStorageBuffersDynamic = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindSampledImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindStorageImages = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindInputAttachments = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceConservativeRasterizationPropertiesEXT(VkPhysicalDeviceConservativeRasterizationPropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // primitiveOverestimationSize
    bufferSize += 4; // maxExtraPrimitiveOverestimationSize
    bufferSize += 4; // extraPrimitiveOverestimationSizeGranularity
    bufferSize += 4; // primitiveUnderestimation
    bufferSize += 4; // conservativePointAndLineRasterization
    bufferSize += 4; // degenerateTrianglesRasterized
    bufferSize += 4; // degenerateLinesRasterized
    bufferSize += 4; // fullyCoveredFragmentShaderInputVariable
    bufferSize += 4; // conservativeRasterizationPostDepthCoverage
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceConservativeRasterizationPropertiesEXT(VkPhysicalDeviceConservativeRasterizationPropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->primitiveOverestimationSize;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->maxExtraPrimitiveOverestimationSize;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->extraPrimitiveOverestimationSizeGranularity;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->primitiveUnderestimation;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->conservativePointAndLineRasterization;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->degenerateTrianglesRasterized;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->degenerateLinesRasterized;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->fullyCoveredFragmentShaderInputVariable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->conservativeRasterizationPostDepthCoverage;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceConservativeRasterizationPropertiesEXT(VkPhysicalDeviceConservativeRasterizationPropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->primitiveOverestimationSize = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxExtraPrimitiveOverestimationSize = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extraPrimitiveOverestimationSizeGranularity = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->primitiveUnderestimation = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->conservativePointAndLineRasterization = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->degenerateTrianglesRasterized = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->degenerateLinesRasterized = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->fullyCoveredFragmentShaderInputVariable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->conservativeRasterizationPostDepthCoverage = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceFloatControlsProperties(VkPhysicalDeviceFloatControlsProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // denormBehaviorIndependence
    bufferSize += 4; // roundingModeIndependence
    bufferSize += 4; // shaderSignedZeroInfNanPreserveFloat16
    bufferSize += 4; // shaderSignedZeroInfNanPreserveFloat32
    bufferSize += 4; // shaderSignedZeroInfNanPreserveFloat64
    bufferSize += 4; // shaderDenormPreserveFloat16
    bufferSize += 4; // shaderDenormPreserveFloat32
    bufferSize += 4; // shaderDenormPreserveFloat64
    bufferSize += 4; // shaderDenormFlushToZeroFloat16
    bufferSize += 4; // shaderDenormFlushToZeroFloat32
    bufferSize += 4; // shaderDenormFlushToZeroFloat64
    bufferSize += 4; // shaderRoundingModeRTEFloat16
    bufferSize += 4; // shaderRoundingModeRTEFloat32
    bufferSize += 4; // shaderRoundingModeRTEFloat64
    bufferSize += 4; // shaderRoundingModeRTZFloat16
    bufferSize += 4; // shaderRoundingModeRTZFloat32
    bufferSize += 4; // shaderRoundingModeRTZFloat64
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceFloatControlsProperties(VkPhysicalDeviceFloatControlsProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkShaderFloatControlsIndependence*)(outputBuffer + bufferOffset) = val->denormBehaviorIndependence;
    bufferOffset += 4;
    *(VkShaderFloatControlsIndependence*)(outputBuffer + bufferOffset) = val->roundingModeIndependence;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSignedZeroInfNanPreserveFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSignedZeroInfNanPreserveFloat32;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSignedZeroInfNanPreserveFloat64;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormPreserveFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormPreserveFloat32;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormPreserveFloat64;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormFlushToZeroFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormFlushToZeroFloat32;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDenormFlushToZeroFloat64;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTEFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTEFloat32;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTEFloat64;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTZFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTZFloat32;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderRoundingModeRTZFloat64;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceFloatControlsProperties(VkPhysicalDeviceFloatControlsProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->denormBehaviorIndependence = *(VkShaderFloatControlsIndependence*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->roundingModeIndependence = *(VkShaderFloatControlsIndependence*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSignedZeroInfNanPreserveFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSignedZeroInfNanPreserveFloat32 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSignedZeroInfNanPreserveFloat64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormPreserveFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormPreserveFloat32 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormPreserveFloat64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormFlushToZeroFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormFlushToZeroFloat32 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDenormFlushToZeroFloat64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTEFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTEFloat32 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTEFloat64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTZFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTZFloat32 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderRoundingModeRTZFloat64 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceMaintenance4Properties(VkPhysicalDeviceMaintenance4Properties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // maxBufferSize
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceMaintenance4Properties(VkPhysicalDeviceMaintenance4Properties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->maxBufferSize;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkPhysicalDeviceMaintenance4Properties(VkPhysicalDeviceMaintenance4Properties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxBufferSize = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkPhysicalDeviceMaintenance3Properties(VkPhysicalDeviceMaintenance3Properties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maxPerSetDescriptors
    bufferSize += 8; // maxMemoryAllocationSize
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceMaintenance3Properties(VkPhysicalDeviceMaintenance3Properties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerSetDescriptors;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->maxMemoryAllocationSize;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkPhysicalDeviceMaintenance3Properties(VkPhysicalDeviceMaintenance3Properties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxPerSetDescriptors = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxMemoryAllocationSize = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkPhysicalDeviceInlineUniformBlockProperties(VkPhysicalDeviceInlineUniformBlockProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maxInlineUniformBlockSize
    bufferSize += 4; // maxPerStageDescriptorInlineUniformBlocks
    bufferSize += 4; // maxPerStageDescriptorUpdateAfterBindInlineUniformBlocks
    bufferSize += 4; // maxDescriptorSetInlineUniformBlocks
    bufferSize += 4; // maxDescriptorSetUpdateAfterBindInlineUniformBlocks
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceInlineUniformBlockProperties(VkPhysicalDeviceInlineUniformBlockProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxInlineUniformBlockSize;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorInlineUniformBlocks;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPerStageDescriptorUpdateAfterBindInlineUniformBlocks;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetInlineUniformBlocks;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxDescriptorSetUpdateAfterBindInlineUniformBlocks;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceInlineUniformBlockProperties(VkPhysicalDeviceInlineUniformBlockProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxInlineUniformBlockSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorInlineUniformBlocks = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxPerStageDescriptorUpdateAfterBindInlineUniformBlocks = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetInlineUniformBlocks = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxDescriptorSetUpdateAfterBindInlineUniformBlocks = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceSampleLocationsPropertiesEXT(VkPhysicalDeviceSampleLocationsPropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // sampleLocationSampleCounts
    bufferSize += vt_sizeof_VkExtent2D(&val->maxSampleLocationGridSize);
    bufferSize += 2 * 4;
    bufferSize += 4; // sampleLocationSubPixelBits
    bufferSize += 4; // variableSampleLocations
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceSampleLocationsPropertiesEXT(VkPhysicalDeviceSampleLocationsPropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkSampleCountFlags*)(outputBuffer + bufferOffset) = val->sampleLocationSampleCounts;
    bufferOffset += 4;
    vt_serialize_VkExtent2D(&val->maxSampleLocationGridSize, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent2D(&val->maxSampleLocationGridSize);
    memcpy(outputBuffer + bufferOffset, val->sampleLocationCoordinateRange, 2 * 4);
    bufferOffset += 2 * 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->sampleLocationSubPixelBits;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->variableSampleLocations;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceSampleLocationsPropertiesEXT(VkPhysicalDeviceSampleLocationsPropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->sampleLocationSampleCounts = *(VkSampleCountFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkExtent2D(&val->maxSampleLocationGridSize, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent2D(&val->maxSampleLocationGridSize);
    memcpy(val->sampleLocationCoordinateRange, inputBuffer + bufferOffset, 2 * 4);
    bufferOffset += 2 * 4;
    val->sampleLocationSubPixelBits = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->variableSampleLocations = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceSamplerFilterMinmaxProperties(VkPhysicalDeviceSamplerFilterMinmaxProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // filterMinmaxSingleComponentFormats
    bufferSize += 4; // filterMinmaxImageComponentMapping
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceSamplerFilterMinmaxProperties(VkPhysicalDeviceSamplerFilterMinmaxProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->filterMinmaxSingleComponentFormats;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->filterMinmaxImageComponentMapping;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceSamplerFilterMinmaxProperties(VkPhysicalDeviceSamplerFilterMinmaxProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->filterMinmaxSingleComponentFormats = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->filterMinmaxImageComponentMapping = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceProtectedMemoryProperties(VkPhysicalDeviceProtectedMemoryProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // protectedNoFault
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceProtectedMemoryProperties(VkPhysicalDeviceProtectedMemoryProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->protectedNoFault;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceProtectedMemoryProperties(VkPhysicalDeviceProtectedMemoryProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->protectedNoFault = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDevicePointClippingProperties(VkPhysicalDevicePointClippingProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // pointClippingBehavior
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDevicePointClippingProperties(VkPhysicalDevicePointClippingProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkPointClippingBehavior*)(outputBuffer + bufferOffset) = val->pointClippingBehavior;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDevicePointClippingProperties(VkPhysicalDevicePointClippingProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->pointClippingBehavior = *(VkPointClippingBehavior*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceSubgroupProperties(VkPhysicalDeviceSubgroupProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // subgroupSize
    bufferSize += 4; // supportedStages
    bufferSize += 4; // supportedOperations
    bufferSize += 4; // quadOperationsInAllStages
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceSubgroupProperties(VkPhysicalDeviceSubgroupProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subgroupSize;
    bufferOffset += 4;
    *(VkShaderStageFlags*)(outputBuffer + bufferOffset) = val->supportedStages;
    bufferOffset += 4;
    *(VkSubgroupFeatureFlags*)(outputBuffer + bufferOffset) = val->supportedOperations;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->quadOperationsInAllStages;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceSubgroupProperties(VkPhysicalDeviceSubgroupProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->subgroupSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->supportedStages = *(VkShaderStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->supportedOperations = *(VkSubgroupFeatureFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->quadOperationsInAllStages = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceMultiviewProperties(VkPhysicalDeviceMultiviewProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maxMultiviewViewCount
    bufferSize += 4; // maxMultiviewInstanceIndex
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceMultiviewProperties(VkPhysicalDeviceMultiviewProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxMultiviewViewCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxMultiviewInstanceIndex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceMultiviewProperties(VkPhysicalDeviceMultiviewProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxMultiviewViewCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxMultiviewInstanceIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceIDProperties(VkPhysicalDeviceIDProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += VK_UUID_SIZE;
    bufferSize += VK_UUID_SIZE;
    bufferSize += VK_LUID_SIZE;
    bufferSize += 4; // deviceNodeMask
    bufferSize += 4; // deviceLUIDValid
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceIDProperties(VkPhysicalDeviceIDProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->deviceUUID, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
    memcpy(outputBuffer + bufferOffset, val->driverUUID, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
    memcpy(outputBuffer + bufferOffset, val->deviceLUID, VK_LUID_SIZE);
    bufferOffset += VK_LUID_SIZE;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceNodeMask;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->deviceLUIDValid;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceIDProperties(VkPhysicalDeviceIDProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    memcpy(val->deviceUUID, inputBuffer + bufferOffset, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
    memcpy(val->driverUUID, inputBuffer + bufferOffset, VK_UUID_SIZE);
    bufferOffset += VK_UUID_SIZE;
    memcpy(val->deviceLUID, inputBuffer + bufferOffset, VK_LUID_SIZE);
    bufferOffset += VK_LUID_SIZE;
    val->deviceNodeMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->deviceLUIDValid = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceDriverProperties(VkPhysicalDeviceDriverProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // driverID
    bufferSize += strlen(val->driverName) + 1;
    bufferSize += strlen(val->driverInfo) + 1;
    bufferSize += vt_sizeof_VkConformanceVersion(&val->conformanceVersion);
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceDriverProperties(VkPhysicalDeviceDriverProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDriverId*)(outputBuffer + bufferOffset) = val->driverID;
    bufferOffset += 4;
    int dataSize4 = strlen(val->driverName) + 1;
    *(int*)(outputBuffer + bufferOffset) = dataSize4;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->driverName, dataSize4);
    bufferOffset += dataSize4;
    int dataSize5 = strlen(val->driverInfo) + 1;
    *(int*)(outputBuffer + bufferOffset) = dataSize5;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->driverInfo, dataSize5);
    bufferOffset += dataSize5;
    vt_serialize_VkConformanceVersion(&val->conformanceVersion, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkConformanceVersion(&val->conformanceVersion);
}

static inline void vt_unserialize_VkPhysicalDeviceDriverProperties(VkPhysicalDeviceDriverProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->driverID = *(VkDriverId*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        memcpy(val->driverName, inputBuffer + bufferOffset, dataSize4);
        bufferOffset += dataSize4;
    }

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        memcpy(val->driverInfo, inputBuffer + bufferOffset, dataSize5);
        bufferOffset += dataSize5;
    }
    vt_unserialize_VkConformanceVersion(&val->conformanceVersion, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkConformanceVersion(&val->conformanceVersion);
}

static inline int vt_sizeof_VkPhysicalDevicePushDescriptorPropertiesKHR(VkPhysicalDevicePushDescriptorPropertiesKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maxPushDescriptors
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDevicePushDescriptorPropertiesKHR(VkPhysicalDevicePushDescriptorPropertiesKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxPushDescriptors;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDevicePushDescriptorPropertiesKHR(VkPhysicalDevicePushDescriptorPropertiesKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxPushDescriptors = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceMapMemoryPlacedFeaturesEXT(VkPhysicalDeviceMapMemoryPlacedFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // memoryMapPlaced
    bufferSize += 4; // memoryMapRangePlaced
    bufferSize += 4; // memoryUnmapReserve
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceMapMemoryPlacedFeaturesEXT(VkPhysicalDeviceMapMemoryPlacedFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->memoryMapPlaced;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->memoryMapRangePlaced;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->memoryUnmapReserve;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceMapMemoryPlacedFeaturesEXT(VkPhysicalDeviceMapMemoryPlacedFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->memoryMapPlaced = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->memoryMapRangePlaced = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->memoryUnmapReserve = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT(VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // shaderModuleIdentifier
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT(VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderModuleIdentifier;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT(VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->shaderModuleIdentifier = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceShaderIntegerDotProductFeatures(VkPhysicalDeviceShaderIntegerDotProductFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // shaderIntegerDotProduct
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceShaderIntegerDotProductFeatures(VkPhysicalDeviceShaderIntegerDotProductFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderIntegerDotProduct;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceShaderIntegerDotProductFeatures(VkPhysicalDeviceShaderIntegerDotProductFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->shaderIntegerDotProduct = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceProvokingVertexFeaturesEXT(VkPhysicalDeviceProvokingVertexFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // provokingVertexLast
    bufferSize += 4; // transformFeedbackPreservesProvokingVertex
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceProvokingVertexFeaturesEXT(VkPhysicalDeviceProvokingVertexFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->provokingVertexLast;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->transformFeedbackPreservesProvokingVertex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceProvokingVertexFeaturesEXT(VkPhysicalDeviceProvokingVertexFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->provokingVertexLast = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->transformFeedbackPreservesProvokingVertex = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceSynchronization2Features(VkPhysicalDeviceSynchronization2Features* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // synchronization2
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceSynchronization2Features(VkPhysicalDeviceSynchronization2Features* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->synchronization2;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceSynchronization2Features(VkPhysicalDeviceSynchronization2Features* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->synchronization2 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceColorWriteEnableFeaturesEXT(VkPhysicalDeviceColorWriteEnableFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // colorWriteEnable
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceColorWriteEnableFeaturesEXT(VkPhysicalDeviceColorWriteEnableFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->colorWriteEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceColorWriteEnableFeaturesEXT(VkPhysicalDeviceColorWriteEnableFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->colorWriteEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceDepthClipControlFeaturesEXT(VkPhysicalDeviceDepthClipControlFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // depthClipControl
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceDepthClipControlFeaturesEXT(VkPhysicalDeviceDepthClipControlFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->depthClipControl;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceDepthClipControlFeaturesEXT(VkPhysicalDeviceDepthClipControlFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->depthClipControl = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT(VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // mutableDescriptorType
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT(VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->mutableDescriptorType;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT(VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->mutableDescriptorType = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceShaderTerminateInvocationFeatures(VkPhysicalDeviceShaderTerminateInvocationFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // shaderTerminateInvocation
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceShaderTerminateInvocationFeatures(VkPhysicalDeviceShaderTerminateInvocationFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderTerminateInvocation;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceShaderTerminateInvocationFeatures(VkPhysicalDeviceShaderTerminateInvocationFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->shaderTerminateInvocation = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDevice4444FormatsFeaturesEXT(VkPhysicalDevice4444FormatsFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // formatA4R4G4B4
    bufferSize += 4; // formatA4B4G4R4
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDevice4444FormatsFeaturesEXT(VkPhysicalDevice4444FormatsFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->formatA4R4G4B4;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->formatA4B4G4R4;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDevice4444FormatsFeaturesEXT(VkPhysicalDevice4444FormatsFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->formatA4R4G4B4 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->formatA4B4G4R4 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceImageRobustnessFeatures(VkPhysicalDeviceImageRobustnessFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // robustImageAccess
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceImageRobustnessFeatures(VkPhysicalDeviceImageRobustnessFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->robustImageAccess;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceImageRobustnessFeatures(VkPhysicalDeviceImageRobustnessFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->robustImageAccess = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceRobustness2FeaturesEXT(VkPhysicalDeviceRobustness2FeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // robustBufferAccess2
    bufferSize += 4; // robustImageAccess2
    bufferSize += 4; // nullDescriptor
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceRobustness2FeaturesEXT(VkPhysicalDeviceRobustness2FeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->robustBufferAccess2;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->robustImageAccess2;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->nullDescriptor;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceRobustness2FeaturesEXT(VkPhysicalDeviceRobustness2FeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->robustBufferAccess2 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->robustImageAccess2 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->nullDescriptor = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures(VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // shaderZeroInitializeWorkgroupMemory
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures(VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderZeroInitializeWorkgroupMemory;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures(VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->shaderZeroInitializeWorkgroupMemory = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceExtendedDynamicState3FeaturesEXT(VkPhysicalDeviceExtendedDynamicState3FeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // extendedDynamicState3TessellationDomainOrigin
    bufferSize += 4; // extendedDynamicState3DepthClampEnable
    bufferSize += 4; // extendedDynamicState3PolygonMode
    bufferSize += 4; // extendedDynamicState3RasterizationSamples
    bufferSize += 4; // extendedDynamicState3SampleMask
    bufferSize += 4; // extendedDynamicState3AlphaToCoverageEnable
    bufferSize += 4; // extendedDynamicState3AlphaToOneEnable
    bufferSize += 4; // extendedDynamicState3LogicOpEnable
    bufferSize += 4; // extendedDynamicState3ColorBlendEnable
    bufferSize += 4; // extendedDynamicState3ColorBlendEquation
    bufferSize += 4; // extendedDynamicState3ColorWriteMask
    bufferSize += 4; // extendedDynamicState3RasterizationStream
    bufferSize += 4; // extendedDynamicState3ConservativeRasterizationMode
    bufferSize += 4; // extendedDynamicState3ExtraPrimitiveOverestimationSize
    bufferSize += 4; // extendedDynamicState3DepthClipEnable
    bufferSize += 4; // extendedDynamicState3SampleLocationsEnable
    bufferSize += 4; // extendedDynamicState3ColorBlendAdvanced
    bufferSize += 4; // extendedDynamicState3ProvokingVertexMode
    bufferSize += 4; // extendedDynamicState3LineRasterizationMode
    bufferSize += 4; // extendedDynamicState3LineStippleEnable
    bufferSize += 4; // extendedDynamicState3DepthClipNegativeOneToOne
    bufferSize += 4; // extendedDynamicState3ViewportWScalingEnable
    bufferSize += 4; // extendedDynamicState3ViewportSwizzle
    bufferSize += 4; // extendedDynamicState3CoverageToColorEnable
    bufferSize += 4; // extendedDynamicState3CoverageToColorLocation
    bufferSize += 4; // extendedDynamicState3CoverageModulationMode
    bufferSize += 4; // extendedDynamicState3CoverageModulationTableEnable
    bufferSize += 4; // extendedDynamicState3CoverageModulationTable
    bufferSize += 4; // extendedDynamicState3CoverageReductionMode
    bufferSize += 4; // extendedDynamicState3RepresentativeFragmentTestEnable
    bufferSize += 4; // extendedDynamicState3ShadingRateImageEnable
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceExtendedDynamicState3FeaturesEXT(VkPhysicalDeviceExtendedDynamicState3FeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3TessellationDomainOrigin;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3DepthClampEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3PolygonMode;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3RasterizationSamples;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3SampleMask;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3AlphaToCoverageEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3AlphaToOneEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3LogicOpEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3ColorBlendEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3ColorBlendEquation;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3ColorWriteMask;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3RasterizationStream;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3ConservativeRasterizationMode;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3ExtraPrimitiveOverestimationSize;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3DepthClipEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3SampleLocationsEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3ColorBlendAdvanced;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3ProvokingVertexMode;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3LineRasterizationMode;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3LineStippleEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3DepthClipNegativeOneToOne;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3ViewportWScalingEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3ViewportSwizzle;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3CoverageToColorEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3CoverageToColorLocation;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3CoverageModulationMode;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3CoverageModulationTableEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3CoverageModulationTable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3CoverageReductionMode;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3RepresentativeFragmentTestEnable;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState3ShadingRateImageEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceExtendedDynamicState3FeaturesEXT(VkPhysicalDeviceExtendedDynamicState3FeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->extendedDynamicState3TessellationDomainOrigin = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3DepthClampEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3PolygonMode = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3RasterizationSamples = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3SampleMask = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3AlphaToCoverageEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3AlphaToOneEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3LogicOpEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3ColorBlendEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3ColorBlendEquation = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3ColorWriteMask = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3RasterizationStream = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3ConservativeRasterizationMode = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3ExtraPrimitiveOverestimationSize = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3DepthClipEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3SampleLocationsEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3ColorBlendAdvanced = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3ProvokingVertexMode = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3LineRasterizationMode = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3LineStippleEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3DepthClipNegativeOneToOne = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3ViewportWScalingEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3ViewportSwizzle = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3CoverageToColorEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3CoverageToColorLocation = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3CoverageModulationMode = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3CoverageModulationTableEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3CoverageModulationTable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3CoverageReductionMode = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3RepresentativeFragmentTestEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->extendedDynamicState3ShadingRateImageEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceExtendedDynamicStateFeaturesEXT(VkPhysicalDeviceExtendedDynamicStateFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // extendedDynamicState
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceExtendedDynamicStateFeaturesEXT(VkPhysicalDeviceExtendedDynamicStateFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->extendedDynamicState;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceExtendedDynamicStateFeaturesEXT(VkPhysicalDeviceExtendedDynamicStateFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->extendedDynamicState = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceBorderColorSwizzleFeaturesEXT(VkPhysicalDeviceBorderColorSwizzleFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // borderColorSwizzle
    bufferSize += 4; // borderColorSwizzleFromImage
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceBorderColorSwizzleFeaturesEXT(VkPhysicalDeviceBorderColorSwizzleFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->borderColorSwizzle;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->borderColorSwizzleFromImage;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceBorderColorSwizzleFeaturesEXT(VkPhysicalDeviceBorderColorSwizzleFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->borderColorSwizzle = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->borderColorSwizzleFromImage = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceCustomBorderColorFeaturesEXT(VkPhysicalDeviceCustomBorderColorFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // customBorderColors
    bufferSize += 4; // customBorderColorWithoutFormat
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceCustomBorderColorFeaturesEXT(VkPhysicalDeviceCustomBorderColorFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->customBorderColors;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->customBorderColorWithoutFormat;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceCustomBorderColorFeaturesEXT(VkPhysicalDeviceCustomBorderColorFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->customBorderColors = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->customBorderColorWithoutFormat = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceVulkan13Features(VkPhysicalDeviceVulkan13Features* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // robustImageAccess
    bufferSize += 4; // inlineUniformBlock
    bufferSize += 4; // descriptorBindingInlineUniformBlockUpdateAfterBind
    bufferSize += 4; // pipelineCreationCacheControl
    bufferSize += 4; // privateData
    bufferSize += 4; // shaderDemoteToHelperInvocation
    bufferSize += 4; // shaderTerminateInvocation
    bufferSize += 4; // subgroupSizeControl
    bufferSize += 4; // computeFullSubgroups
    bufferSize += 4; // synchronization2
    bufferSize += 4; // textureCompressionASTC_HDR
    bufferSize += 4; // shaderZeroInitializeWorkgroupMemory
    bufferSize += 4; // dynamicRendering
    bufferSize += 4; // shaderIntegerDotProduct
    bufferSize += 4; // maintenance4
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceVulkan13Features(VkPhysicalDeviceVulkan13Features* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->robustImageAccess;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->inlineUniformBlock;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingInlineUniformBlockUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->pipelineCreationCacheControl;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->privateData;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDemoteToHelperInvocation;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderTerminateInvocation;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->subgroupSizeControl;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->computeFullSubgroups;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->synchronization2;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->textureCompressionASTC_HDR;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderZeroInitializeWorkgroupMemory;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->dynamicRendering;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderIntegerDotProduct;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->maintenance4;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceVulkan13Features(VkPhysicalDeviceVulkan13Features* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->robustImageAccess = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->inlineUniformBlock = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingInlineUniformBlockUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->pipelineCreationCacheControl = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->privateData = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDemoteToHelperInvocation = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderTerminateInvocation = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->subgroupSizeControl = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->computeFullSubgroups = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->synchronization2 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->textureCompressionASTC_HDR = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderZeroInitializeWorkgroupMemory = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dynamicRendering = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderIntegerDotProduct = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maintenance4 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceVulkan12Features(VkPhysicalDeviceVulkan12Features* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // samplerMirrorClampToEdge
    bufferSize += 4; // drawIndirectCount
    bufferSize += 4; // storageBuffer8BitAccess
    bufferSize += 4; // uniformAndStorageBuffer8BitAccess
    bufferSize += 4; // storagePushConstant8
    bufferSize += 4; // shaderBufferInt64Atomics
    bufferSize += 4; // shaderSharedInt64Atomics
    bufferSize += 4; // shaderFloat16
    bufferSize += 4; // shaderInt8
    bufferSize += 4; // descriptorIndexing
    bufferSize += 4; // shaderInputAttachmentArrayDynamicIndexing
    bufferSize += 4; // shaderUniformTexelBufferArrayDynamicIndexing
    bufferSize += 4; // shaderStorageTexelBufferArrayDynamicIndexing
    bufferSize += 4; // shaderUniformBufferArrayNonUniformIndexing
    bufferSize += 4; // shaderSampledImageArrayNonUniformIndexing
    bufferSize += 4; // shaderStorageBufferArrayNonUniformIndexing
    bufferSize += 4; // shaderStorageImageArrayNonUniformIndexing
    bufferSize += 4; // shaderInputAttachmentArrayNonUniformIndexing
    bufferSize += 4; // shaderUniformTexelBufferArrayNonUniformIndexing
    bufferSize += 4; // shaderStorageTexelBufferArrayNonUniformIndexing
    bufferSize += 4; // descriptorBindingUniformBufferUpdateAfterBind
    bufferSize += 4; // descriptorBindingSampledImageUpdateAfterBind
    bufferSize += 4; // descriptorBindingStorageImageUpdateAfterBind
    bufferSize += 4; // descriptorBindingStorageBufferUpdateAfterBind
    bufferSize += 4; // descriptorBindingUniformTexelBufferUpdateAfterBind
    bufferSize += 4; // descriptorBindingStorageTexelBufferUpdateAfterBind
    bufferSize += 4; // descriptorBindingUpdateUnusedWhilePending
    bufferSize += 4; // descriptorBindingPartiallyBound
    bufferSize += 4; // descriptorBindingVariableDescriptorCount
    bufferSize += 4; // runtimeDescriptorArray
    bufferSize += 4; // samplerFilterMinmax
    bufferSize += 4; // scalarBlockLayout
    bufferSize += 4; // imagelessFramebuffer
    bufferSize += 4; // uniformBufferStandardLayout
    bufferSize += 4; // shaderSubgroupExtendedTypes
    bufferSize += 4; // separateDepthStencilLayouts
    bufferSize += 4; // hostQueryReset
    bufferSize += 4; // timelineSemaphore
    bufferSize += 4; // bufferDeviceAddress
    bufferSize += 4; // bufferDeviceAddressCaptureReplay
    bufferSize += 4; // bufferDeviceAddressMultiDevice
    bufferSize += 4; // vulkanMemoryModel
    bufferSize += 4; // vulkanMemoryModelDeviceScope
    bufferSize += 4; // vulkanMemoryModelAvailabilityVisibilityChains
    bufferSize += 4; // shaderOutputViewportIndex
    bufferSize += 4; // shaderOutputLayer
    bufferSize += 4; // subgroupBroadcastDynamicId
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceVulkan12Features(VkPhysicalDeviceVulkan12Features* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->samplerMirrorClampToEdge;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->drawIndirectCount;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storageBuffer8BitAccess;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->uniformAndStorageBuffer8BitAccess;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storagePushConstant8;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderBufferInt64Atomics;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSharedInt64Atomics;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderInt8;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderInputAttachmentArrayDynamicIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderUniformTexelBufferArrayDynamicIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageTexelBufferArrayDynamicIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderUniformBufferArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSampledImageArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageBufferArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageImageArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderInputAttachmentArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderUniformTexelBufferArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageTexelBufferArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingUniformBufferUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingSampledImageUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingStorageImageUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingStorageBufferUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingUniformTexelBufferUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingStorageTexelBufferUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingUpdateUnusedWhilePending;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingPartiallyBound;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingVariableDescriptorCount;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->runtimeDescriptorArray;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->samplerFilterMinmax;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->scalarBlockLayout;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->imagelessFramebuffer;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->uniformBufferStandardLayout;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSubgroupExtendedTypes;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->separateDepthStencilLayouts;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->hostQueryReset;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->timelineSemaphore;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->bufferDeviceAddress;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->bufferDeviceAddressCaptureReplay;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->bufferDeviceAddressMultiDevice;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->vulkanMemoryModel;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->vulkanMemoryModelDeviceScope;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->vulkanMemoryModelAvailabilityVisibilityChains;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderOutputViewportIndex;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderOutputLayer;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->subgroupBroadcastDynamicId;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceVulkan12Features(VkPhysicalDeviceVulkan12Features* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->samplerMirrorClampToEdge = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->drawIndirectCount = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storageBuffer8BitAccess = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->uniformAndStorageBuffer8BitAccess = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storagePushConstant8 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderBufferInt64Atomics = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSharedInt64Atomics = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderInt8 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderInputAttachmentArrayDynamicIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderUniformTexelBufferArrayDynamicIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageTexelBufferArrayDynamicIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderUniformBufferArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSampledImageArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageBufferArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageImageArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderInputAttachmentArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderUniformTexelBufferArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageTexelBufferArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingUniformBufferUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingSampledImageUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingStorageImageUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingStorageBufferUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingUniformTexelBufferUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingStorageTexelBufferUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingUpdateUnusedWhilePending = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingPartiallyBound = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingVariableDescriptorCount = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->runtimeDescriptorArray = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->samplerFilterMinmax = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->scalarBlockLayout = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->imagelessFramebuffer = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->uniformBufferStandardLayout = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSubgroupExtendedTypes = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->separateDepthStencilLayouts = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->hostQueryReset = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->timelineSemaphore = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->bufferDeviceAddress = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->bufferDeviceAddressCaptureReplay = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->bufferDeviceAddressMultiDevice = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->vulkanMemoryModel = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->vulkanMemoryModelDeviceScope = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->vulkanMemoryModelAvailabilityVisibilityChains = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderOutputViewportIndex = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderOutputLayer = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->subgroupBroadcastDynamicId = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceVulkan11Features(VkPhysicalDeviceVulkan11Features* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // storageBuffer16BitAccess
    bufferSize += 4; // uniformAndStorageBuffer16BitAccess
    bufferSize += 4; // storagePushConstant16
    bufferSize += 4; // storageInputOutput16
    bufferSize += 4; // multiview
    bufferSize += 4; // multiviewGeometryShader
    bufferSize += 4; // multiviewTessellationShader
    bufferSize += 4; // variablePointersStorageBuffer
    bufferSize += 4; // variablePointers
    bufferSize += 4; // protectedMemory
    bufferSize += 4; // samplerYcbcrConversion
    bufferSize += 4; // shaderDrawParameters
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceVulkan11Features(VkPhysicalDeviceVulkan11Features* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storageBuffer16BitAccess;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->uniformAndStorageBuffer16BitAccess;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storagePushConstant16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storageInputOutput16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->multiview;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->multiviewGeometryShader;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->multiviewTessellationShader;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->variablePointersStorageBuffer;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->variablePointers;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->protectedMemory;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->samplerYcbcrConversion;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDrawParameters;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceVulkan11Features(VkPhysicalDeviceVulkan11Features* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->storageBuffer16BitAccess = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->uniformAndStorageBuffer16BitAccess = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storagePushConstant16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storageInputOutput16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->multiview = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->multiviewGeometryShader = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->multiviewTessellationShader = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->variablePointersStorageBuffer = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->variablePointers = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->protectedMemory = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->samplerYcbcrConversion = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderDrawParameters = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDevicePipelineCreationCacheControlFeatures(VkPhysicalDevicePipelineCreationCacheControlFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // pipelineCreationCacheControl
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDevicePipelineCreationCacheControlFeatures(VkPhysicalDevicePipelineCreationCacheControlFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->pipelineCreationCacheControl;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDevicePipelineCreationCacheControlFeatures(VkPhysicalDevicePipelineCreationCacheControlFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->pipelineCreationCacheControl = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceSubgroupSizeControlFeatures(VkPhysicalDeviceSubgroupSizeControlFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // subgroupSizeControl
    bufferSize += 4; // computeFullSubgroups
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceSubgroupSizeControlFeatures(VkPhysicalDeviceSubgroupSizeControlFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->subgroupSizeControl;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->computeFullSubgroups;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceSubgroupSizeControlFeatures(VkPhysicalDeviceSubgroupSizeControlFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->subgroupSizeControl = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->computeFullSubgroups = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT(VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // texelBufferAlignment
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT(VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->texelBufferAlignment;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT(VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->texelBufferAlignment = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures(VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // shaderDemoteToHelperInvocation
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures(VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDemoteToHelperInvocation;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures(VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->shaderDemoteToHelperInvocation = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures(VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // separateDepthStencilLayouts
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures(VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->separateDepthStencilLayouts;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures(VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->separateDepthStencilLayouts = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT(VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // fragmentShaderSampleInterlock
    bufferSize += 4; // fragmentShaderPixelInterlock
    bufferSize += 4; // fragmentShaderShadingRateInterlock
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT(VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->fragmentShaderSampleInterlock;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->fragmentShaderPixelInterlock;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->fragmentShaderShadingRateInterlock;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT(VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->fragmentShaderSampleInterlock = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->fragmentShaderPixelInterlock = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->fragmentShaderShadingRateInterlock = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceTextureCompressionASTCHDRFeatures(VkPhysicalDeviceTextureCompressionASTCHDRFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // textureCompressionASTC_HDR
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceTextureCompressionASTCHDRFeatures(VkPhysicalDeviceTextureCompressionASTCHDRFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->textureCompressionASTC_HDR;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceTextureCompressionASTCHDRFeatures(VkPhysicalDeviceTextureCompressionASTCHDRFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->textureCompressionASTC_HDR = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceImagelessFramebufferFeatures(VkPhysicalDeviceImagelessFramebufferFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // imagelessFramebuffer
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceImagelessFramebufferFeatures(VkPhysicalDeviceImagelessFramebufferFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->imagelessFramebuffer;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceImagelessFramebufferFeatures(VkPhysicalDeviceImagelessFramebufferFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->imagelessFramebuffer = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceBufferDeviceAddressFeatures(VkPhysicalDeviceBufferDeviceAddressFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // bufferDeviceAddress
    bufferSize += 4; // bufferDeviceAddressCaptureReplay
    bufferSize += 4; // bufferDeviceAddressMultiDevice
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceBufferDeviceAddressFeatures(VkPhysicalDeviceBufferDeviceAddressFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->bufferDeviceAddress;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->bufferDeviceAddressCaptureReplay;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->bufferDeviceAddressMultiDevice;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceBufferDeviceAddressFeatures(VkPhysicalDeviceBufferDeviceAddressFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->bufferDeviceAddress = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->bufferDeviceAddressCaptureReplay = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->bufferDeviceAddressMultiDevice = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceDepthClipEnableFeaturesEXT(VkPhysicalDeviceDepthClipEnableFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // depthClipEnable
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceDepthClipEnableFeaturesEXT(VkPhysicalDeviceDepthClipEnableFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->depthClipEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceDepthClipEnableFeaturesEXT(VkPhysicalDeviceDepthClipEnableFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->depthClipEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceUniformBufferStandardLayoutFeatures(VkPhysicalDeviceUniformBufferStandardLayoutFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // uniformBufferStandardLayout
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceUniformBufferStandardLayoutFeatures(VkPhysicalDeviceUniformBufferStandardLayoutFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->uniformBufferStandardLayout;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceUniformBufferStandardLayoutFeatures(VkPhysicalDeviceUniformBufferStandardLayoutFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->uniformBufferStandardLayout = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceScalarBlockLayoutFeatures(VkPhysicalDeviceScalarBlockLayoutFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // scalarBlockLayout
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceScalarBlockLayoutFeatures(VkPhysicalDeviceScalarBlockLayoutFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->scalarBlockLayout;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceScalarBlockLayoutFeatures(VkPhysicalDeviceScalarBlockLayoutFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->scalarBlockLayout = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR(VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // vertexAttributeInstanceRateDivisor
    bufferSize += 4; // vertexAttributeInstanceRateZeroDivisor
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR(VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->vertexAttributeInstanceRateDivisor;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->vertexAttributeInstanceRateZeroDivisor;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR(VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->vertexAttributeInstanceRateDivisor = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->vertexAttributeInstanceRateZeroDivisor = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceShaderAtomicInt64Features(VkPhysicalDeviceShaderAtomicInt64Features* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // shaderBufferInt64Atomics
    bufferSize += 4; // shaderSharedInt64Atomics
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceShaderAtomicInt64Features(VkPhysicalDeviceShaderAtomicInt64Features* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderBufferInt64Atomics;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSharedInt64Atomics;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceShaderAtomicInt64Features(VkPhysicalDeviceShaderAtomicInt64Features* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->shaderBufferInt64Atomics = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSharedInt64Atomics = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceVulkanMemoryModelFeatures(VkPhysicalDeviceVulkanMemoryModelFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // vulkanMemoryModel
    bufferSize += 4; // vulkanMemoryModelDeviceScope
    bufferSize += 4; // vulkanMemoryModelAvailabilityVisibilityChains
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceVulkanMemoryModelFeatures(VkPhysicalDeviceVulkanMemoryModelFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->vulkanMemoryModel;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->vulkanMemoryModelDeviceScope;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->vulkanMemoryModelAvailabilityVisibilityChains;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceVulkanMemoryModelFeatures(VkPhysicalDeviceVulkanMemoryModelFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->vulkanMemoryModel = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->vulkanMemoryModelDeviceScope = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->vulkanMemoryModelAvailabilityVisibilityChains = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceConditionalRenderingFeaturesEXT(VkPhysicalDeviceConditionalRenderingFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // conditionalRendering
    bufferSize += 4; // inheritedConditionalRendering
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceConditionalRenderingFeaturesEXT(VkPhysicalDeviceConditionalRenderingFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->conditionalRendering;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->inheritedConditionalRendering;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceConditionalRenderingFeaturesEXT(VkPhysicalDeviceConditionalRenderingFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->conditionalRendering = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->inheritedConditionalRendering = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDevice8BitStorageFeatures(VkPhysicalDevice8BitStorageFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // storageBuffer8BitAccess
    bufferSize += 4; // uniformAndStorageBuffer8BitAccess
    bufferSize += 4; // storagePushConstant8
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDevice8BitStorageFeatures(VkPhysicalDevice8BitStorageFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storageBuffer8BitAccess;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->uniformAndStorageBuffer8BitAccess;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storagePushConstant8;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDevice8BitStorageFeatures(VkPhysicalDevice8BitStorageFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->storageBuffer8BitAccess = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->uniformAndStorageBuffer8BitAccess = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storagePushConstant8 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceTimelineSemaphoreFeatures(VkPhysicalDeviceTimelineSemaphoreFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // timelineSemaphore
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceTimelineSemaphoreFeatures(VkPhysicalDeviceTimelineSemaphoreFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->timelineSemaphore;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceTimelineSemaphoreFeatures(VkPhysicalDeviceTimelineSemaphoreFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->timelineSemaphore = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceDescriptorIndexingFeatures(VkPhysicalDeviceDescriptorIndexingFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // shaderInputAttachmentArrayDynamicIndexing
    bufferSize += 4; // shaderUniformTexelBufferArrayDynamicIndexing
    bufferSize += 4; // shaderStorageTexelBufferArrayDynamicIndexing
    bufferSize += 4; // shaderUniformBufferArrayNonUniformIndexing
    bufferSize += 4; // shaderSampledImageArrayNonUniformIndexing
    bufferSize += 4; // shaderStorageBufferArrayNonUniformIndexing
    bufferSize += 4; // shaderStorageImageArrayNonUniformIndexing
    bufferSize += 4; // shaderInputAttachmentArrayNonUniformIndexing
    bufferSize += 4; // shaderUniformTexelBufferArrayNonUniformIndexing
    bufferSize += 4; // shaderStorageTexelBufferArrayNonUniformIndexing
    bufferSize += 4; // descriptorBindingUniformBufferUpdateAfterBind
    bufferSize += 4; // descriptorBindingSampledImageUpdateAfterBind
    bufferSize += 4; // descriptorBindingStorageImageUpdateAfterBind
    bufferSize += 4; // descriptorBindingStorageBufferUpdateAfterBind
    bufferSize += 4; // descriptorBindingUniformTexelBufferUpdateAfterBind
    bufferSize += 4; // descriptorBindingStorageTexelBufferUpdateAfterBind
    bufferSize += 4; // descriptorBindingUpdateUnusedWhilePending
    bufferSize += 4; // descriptorBindingPartiallyBound
    bufferSize += 4; // descriptorBindingVariableDescriptorCount
    bufferSize += 4; // runtimeDescriptorArray
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceDescriptorIndexingFeatures(VkPhysicalDeviceDescriptorIndexingFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderInputAttachmentArrayDynamicIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderUniformTexelBufferArrayDynamicIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageTexelBufferArrayDynamicIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderUniformBufferArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSampledImageArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageBufferArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageImageArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderInputAttachmentArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderUniformTexelBufferArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderStorageTexelBufferArrayNonUniformIndexing;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingUniformBufferUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingSampledImageUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingStorageImageUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingStorageBufferUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingUniformTexelBufferUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingStorageTexelBufferUpdateAfterBind;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingUpdateUnusedWhilePending;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingPartiallyBound;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingVariableDescriptorCount;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->runtimeDescriptorArray;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceDescriptorIndexingFeatures(VkPhysicalDeviceDescriptorIndexingFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->shaderInputAttachmentArrayDynamicIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderUniformTexelBufferArrayDynamicIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageTexelBufferArrayDynamicIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderUniformBufferArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderSampledImageArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageBufferArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageImageArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderInputAttachmentArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderUniformTexelBufferArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderStorageTexelBufferArrayNonUniformIndexing = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingUniformBufferUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingSampledImageUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingStorageImageUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingStorageBufferUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingUniformTexelBufferUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingStorageTexelBufferUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingUpdateUnusedWhilePending = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingPartiallyBound = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingVariableDescriptorCount = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->runtimeDescriptorArray = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceHostQueryResetFeatures(VkPhysicalDeviceHostQueryResetFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // hostQueryReset
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceHostQueryResetFeatures(VkPhysicalDeviceHostQueryResetFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->hostQueryReset;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceHostQueryResetFeatures(VkPhysicalDeviceHostQueryResetFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->hostQueryReset = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceShaderFloat16Int8Features(VkPhysicalDeviceShaderFloat16Int8Features* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // shaderFloat16
    bufferSize += 4; // shaderInt8
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceShaderFloat16Int8Features(VkPhysicalDeviceShaderFloat16Int8Features* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderFloat16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderInt8;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceShaderFloat16Int8Features(VkPhysicalDeviceShaderFloat16Int8Features* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->shaderFloat16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->shaderInt8 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceShaderDrawParametersFeatures(VkPhysicalDeviceShaderDrawParametersFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // shaderDrawParameters
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceShaderDrawParametersFeatures(VkPhysicalDeviceShaderDrawParametersFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderDrawParameters;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceShaderDrawParametersFeatures(VkPhysicalDeviceShaderDrawParametersFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->shaderDrawParameters = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceMaintenance4Features(VkPhysicalDeviceMaintenance4Features* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maintenance4
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceMaintenance4Features(VkPhysicalDeviceMaintenance4Features* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->maintenance4;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceMaintenance4Features(VkPhysicalDeviceMaintenance4Features* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maintenance4 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceInlineUniformBlockFeatures(VkPhysicalDeviceInlineUniformBlockFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // inlineUniformBlock
    bufferSize += 4; // descriptorBindingInlineUniformBlockUpdateAfterBind
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceInlineUniformBlockFeatures(VkPhysicalDeviceInlineUniformBlockFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->inlineUniformBlock;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->descriptorBindingInlineUniformBlockUpdateAfterBind;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceInlineUniformBlockFeatures(VkPhysicalDeviceInlineUniformBlockFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->inlineUniformBlock = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorBindingInlineUniformBlockUpdateAfterBind = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceProtectedMemoryFeatures(VkPhysicalDeviceProtectedMemoryFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // protectedMemory
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceProtectedMemoryFeatures(VkPhysicalDeviceProtectedMemoryFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->protectedMemory;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceProtectedMemoryFeatures(VkPhysicalDeviceProtectedMemoryFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->protectedMemory = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceSamplerYcbcrConversionFeatures(VkPhysicalDeviceSamplerYcbcrConversionFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // samplerYcbcrConversion
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceSamplerYcbcrConversionFeatures(VkPhysicalDeviceSamplerYcbcrConversionFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->samplerYcbcrConversion;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceSamplerYcbcrConversionFeatures(VkPhysicalDeviceSamplerYcbcrConversionFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->samplerYcbcrConversion = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures(VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // shaderSubgroupExtendedTypes
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures(VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->shaderSubgroupExtendedTypes;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures(VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->shaderSubgroupExtendedTypes = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDevice16BitStorageFeatures(VkPhysicalDevice16BitStorageFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // storageBuffer16BitAccess
    bufferSize += 4; // uniformAndStorageBuffer16BitAccess
    bufferSize += 4; // storagePushConstant16
    bufferSize += 4; // storageInputOutput16
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDevice16BitStorageFeatures(VkPhysicalDevice16BitStorageFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storageBuffer16BitAccess;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->uniformAndStorageBuffer16BitAccess;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storagePushConstant16;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->storageInputOutput16;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDevice16BitStorageFeatures(VkPhysicalDevice16BitStorageFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->storageBuffer16BitAccess = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->uniformAndStorageBuffer16BitAccess = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storagePushConstant16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storageInputOutput16 = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceMultiviewFeatures(VkPhysicalDeviceMultiviewFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // multiview
    bufferSize += 4; // multiviewGeometryShader
    bufferSize += 4; // multiviewTessellationShader
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceMultiviewFeatures(VkPhysicalDeviceMultiviewFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->multiview;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->multiviewGeometryShader;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->multiviewTessellationShader;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceMultiviewFeatures(VkPhysicalDeviceMultiviewFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->multiview = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->multiviewGeometryShader = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->multiviewTessellationShader = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceVariablePointersFeatures(VkPhysicalDeviceVariablePointersFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // variablePointersStorageBuffer
    bufferSize += 4; // variablePointers
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceVariablePointersFeatures(VkPhysicalDeviceVariablePointersFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->variablePointersStorageBuffer;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->variablePointers;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceVariablePointersFeatures(VkPhysicalDeviceVariablePointersFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->variablePointersStorageBuffer = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->variablePointers = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkClearDepthStencilValue(VkClearDepthStencilValue* val) {
    int bufferSize = 0;
    bufferSize += 4; // depth
    bufferSize += 4; // stencil
    return bufferSize;
}

static inline void vt_serialize_VkClearDepthStencilValue(VkClearDepthStencilValue* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(float*)(outputBuffer + bufferOffset) = val->depth;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->stencil;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkClearDepthStencilValue(VkClearDepthStencilValue* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->depth = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stencil = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkClearColorValue(VkClearColorValue* val) {
    return sizeof(VkClearColorValue);
}

static inline void vt_serialize_VkClearColorValue(VkClearColorValue* val, char* outputBuffer) {
    memcpy(outputBuffer, val, sizeof(VkClearColorValue));
}

static inline void vt_unserialize_VkClearColorValue(VkClearColorValue* val, char* inputBuffer, MemoryPool* memoryPool) {
    memcpy(val, inputBuffer, sizeof(VkClearColorValue));
}

static inline int vt_sizeof_VkPhysicalDevicePrivateDataFeatures(VkPhysicalDevicePrivateDataFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // privateData
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDevicePrivateDataFeatures(VkPhysicalDevicePrivateDataFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->privateData;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDevicePrivateDataFeatures(VkPhysicalDevicePrivateDataFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->privateData = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkClearValue(VkClearValue* val) {
    return sizeof(VkClearValue);
}

static inline void vt_serialize_VkClearValue(VkClearValue* val, char* outputBuffer) {
    memcpy(outputBuffer, val, sizeof(VkClearValue));
}

static inline void vt_unserialize_VkClearValue(VkClearValue* val, char* inputBuffer, MemoryPool* memoryPool) {
    memcpy(val, inputBuffer, sizeof(VkClearValue));
}

static inline int vt_sizeof_VkRenderingAttachmentInfo(VkRenderingAttachmentInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkImageView(val->imageView);
    bufferSize += 4; // imageLayout
    bufferSize += 4; // resolveMode
    bufferSize += vt_sizeof_VkImageView(val->resolveImageView);
    bufferSize += 4; // resolveImageLayout
    bufferSize += 4; // loadOp
    bufferSize += 4; // storeOp
    bufferSize += vt_sizeof_VkClearValue(&val->clearValue);
    return bufferSize;
}

static inline void vt_serialize_VkRenderingAttachmentInfo(VkRenderingAttachmentInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImageView(val->imageView, outputBuffer + bufferOffset);
#else
    VkObject* imageViewObject = VkObject_fromHandle(val->imageView);
    vt_serialize_VkImageView((VkImageView)&imageViewObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImageView(val->imageView);
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->imageLayout;
    bufferOffset += 4;
    *(VkResolveModeFlagBits*)(outputBuffer + bufferOffset) = val->resolveMode;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImageView(val->resolveImageView, outputBuffer + bufferOffset);
#else
    VkObject* resolveImageViewObject = VkObject_fromHandle(val->resolveImageView);
    vt_serialize_VkImageView((VkImageView)&resolveImageViewObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImageView(val->resolveImageView);
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->resolveImageLayout;
    bufferOffset += 4;
    *(VkAttachmentLoadOp*)(outputBuffer + bufferOffset) = val->loadOp;
    bufferOffset += 4;
    *(VkAttachmentStoreOp*)(outputBuffer + bufferOffset) = val->storeOp;
    bufferOffset += 4;
    vt_serialize_VkClearValue(&val->clearValue, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkClearValue(&val->clearValue);
}

static inline void vt_unserialize_VkRenderingAttachmentInfo(VkRenderingAttachmentInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t imageViewId;
    vt_unserialize_VkImageView((VkImageView)&imageViewId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->imageView = VkObject_fromId(imageViewId);
#else
    VkObject* imageViewObject = VkObject_create(VK_OBJECT_TYPE_IMAGE_VIEW, imageViewId);
    val->imageView = VkObject_toHandle(imageViewObject);
#endif
    bufferOffset += vt_sizeof_VkImageView(val->imageView);
    val->imageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->resolveMode = *(VkResolveModeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t resolveImageViewId;
    vt_unserialize_VkImageView((VkImageView)&resolveImageViewId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->resolveImageView = VkObject_fromId(resolveImageViewId);
#else
    VkObject* resolveImageViewObject = VkObject_create(VK_OBJECT_TYPE_IMAGE_VIEW, resolveImageViewId);
    val->resolveImageView = VkObject_toHandle(resolveImageViewObject);
#endif
    bufferOffset += vt_sizeof_VkImageView(val->resolveImageView);
    val->resolveImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->loadOp = *(VkAttachmentLoadOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->storeOp = *(VkAttachmentStoreOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkClearValue(&val->clearValue, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkClearValue(&val->clearValue);
}

static inline int vt_sizeof_VkDeviceGroupRenderPassBeginInfo(VkDeviceGroupRenderPassBeginInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // deviceMask
    bufferSize += 4; // deviceRenderAreaCount
    bufferSize += 4;
    if (val->pDeviceRenderAreas) {
        int arrSize = val->deviceRenderAreaCount ? val->deviceRenderAreaCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkRect2D(&val->pDeviceRenderAreas[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkDeviceGroupRenderPassBeginInfo(VkDeviceGroupRenderPassBeginInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceRenderAreaCount;
    bufferOffset += 4;

    if (val->pDeviceRenderAreas) {
        int arrSize = val->deviceRenderAreaCount ? val->deviceRenderAreaCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkRect2D(&val->pDeviceRenderAreas[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkRect2D(&val->pDeviceRenderAreas[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDeviceGroupRenderPassBeginInfo(VkDeviceGroupRenderPassBeginInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->deviceMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->deviceRenderAreaCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkRect2D* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkRect2D));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkRect2D(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pDeviceRenderAreas = arrValues;
    }
    else val->pDeviceRenderAreas = NULL;
}

static inline int vt_sizeof_VkDeviceGroupPresentInfoKHR(VkDeviceGroupPresentInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // swapchainCount
    bufferSize += 4;
    bufferSize += val->pDeviceMasks ? val->swapchainCount * 4 : 0;
    bufferSize += 4; // mode
    return bufferSize;
}

static inline void vt_serialize_VkDeviceGroupPresentInfoKHR(VkDeviceGroupPresentInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->swapchainCount;
    bufferOffset += 4;

    if (val->pDeviceMasks) {
        int arrSize = val->swapchainCount ? val->swapchainCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pDeviceMasks, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkDeviceGroupPresentModeFlagBitsKHR*)(outputBuffer + bufferOffset) = val->mode;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDeviceGroupPresentInfoKHR(VkDeviceGroupPresentInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->swapchainCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize4 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
        val->pDeviceMasks = arrValues;
    }
    else val->pDeviceMasks = NULL;
    val->mode = *(VkDeviceGroupPresentModeFlagBitsKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkImageFormatListCreateInfo(VkImageFormatListCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // viewFormatCount
    bufferSize += 4;
    bufferSize += val->pViewFormats ? val->viewFormatCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkImageFormatListCreateInfo(VkImageFormatListCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->viewFormatCount;
    bufferOffset += 4;

    if (val->pViewFormats) {
        int arrSize = val->viewFormatCount ? val->viewFormatCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pViewFormats, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkImageFormatListCreateInfo(VkImageFormatListCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->viewFormatCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkFormat* arrValues = vt_alloc(memoryPool, dataSize4 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
        val->pViewFormats = arrValues;
    }
    else val->pViewFormats = NULL;
}

static inline int vt_sizeof_VkDeviceGroupSwapchainCreateInfoKHR(VkDeviceGroupSwapchainCreateInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // modes
    return bufferSize;
}

static inline void vt_serialize_VkDeviceGroupSwapchainCreateInfoKHR(VkDeviceGroupSwapchainCreateInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDeviceGroupPresentModeFlagsKHR*)(outputBuffer + bufferOffset) = val->modes;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDeviceGroupSwapchainCreateInfoKHR(VkDeviceGroupSwapchainCreateInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->modes = *(VkDeviceGroupPresentModeFlagsKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSurfaceCapabilitiesKHR(VkSurfaceCapabilitiesKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // minImageCount
    bufferSize += 4; // maxImageCount
    bufferSize += vt_sizeof_VkExtent2D(&val->currentExtent);
    bufferSize += vt_sizeof_VkExtent2D(&val->minImageExtent);
    bufferSize += vt_sizeof_VkExtent2D(&val->maxImageExtent);
    bufferSize += 4; // maxImageArrayLayers
    bufferSize += 4; // supportedTransforms
    bufferSize += 4; // currentTransform
    bufferSize += 4; // supportedCompositeAlpha
    bufferSize += 4; // supportedUsageFlags
    return bufferSize;
}

static inline void vt_serialize_VkSurfaceCapabilitiesKHR(VkSurfaceCapabilitiesKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->minImageCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxImageCount;
    bufferOffset += 4;
    vt_serialize_VkExtent2D(&val->currentExtent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent2D(&val->currentExtent);
    vt_serialize_VkExtent2D(&val->minImageExtent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent2D(&val->minImageExtent);
    vt_serialize_VkExtent2D(&val->maxImageExtent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent2D(&val->maxImageExtent);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxImageArrayLayers;
    bufferOffset += 4;
    *(VkSurfaceTransformFlagsKHR*)(outputBuffer + bufferOffset) = val->supportedTransforms;
    bufferOffset += 4;
    *(VkSurfaceTransformFlagBitsKHR*)(outputBuffer + bufferOffset) = val->currentTransform;
    bufferOffset += 4;
    *(VkCompositeAlphaFlagsKHR*)(outputBuffer + bufferOffset) = val->supportedCompositeAlpha;
    bufferOffset += 4;
    *(VkImageUsageFlags*)(outputBuffer + bufferOffset) = val->supportedUsageFlags;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSurfaceCapabilitiesKHR(VkSurfaceCapabilitiesKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->minImageCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxImageCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkExtent2D(&val->currentExtent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent2D(&val->currentExtent);
    vt_unserialize_VkExtent2D(&val->minImageExtent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent2D(&val->minImageExtent);
    vt_unserialize_VkExtent2D(&val->maxImageExtent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent2D(&val->maxImageExtent);
    val->maxImageArrayLayers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->supportedTransforms = *(VkSurfaceTransformFlagsKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->currentTransform = *(VkSurfaceTransformFlagBitsKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->supportedCompositeAlpha = *(VkCompositeAlphaFlagsKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->supportedUsageFlags = *(VkImageUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSurfaceFormatKHR(VkSurfaceFormatKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // format
    bufferSize += 4; // colorSpace
    return bufferSize;
}

static inline void vt_serialize_VkSurfaceFormatKHR(VkSurfaceFormatKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->format;
    bufferOffset += 4;
    *(VkColorSpaceKHR*)(outputBuffer + bufferOffset) = val->colorSpace;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSurfaceFormatKHR(VkSurfaceFormatKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->colorSpace = *(VkColorSpaceKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSwapchainCreateInfoKHR(VkSwapchainCreateInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_SWAPCHAIN_CREATE_INFO_KHR: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDeviceGroupSwapchainCreateInfoKHR((VkDeviceGroupSwapchainCreateInfoKHR*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkImageFormatListCreateInfo((VkImageFormatListCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += vt_sizeof_VkSurfaceKHR(val->surface);
    bufferSize += 4; // minImageCount
    bufferSize += 4; // imageFormat
    bufferSize += 4; // imageColorSpace
    bufferSize += vt_sizeof_VkExtent2D(&val->imageExtent);
    bufferSize += 4; // imageArrayLayers
    bufferSize += 4; // imageUsage
    bufferSize += 4; // imageSharingMode
    bufferSize += 4; // queueFamilyIndexCount
    bufferSize += 4;
    bufferSize += val->pQueueFamilyIndices ? val->queueFamilyIndexCount * 4 : 0;
    bufferSize += 4; // preTransform
    bufferSize += 4; // compositeAlpha
    bufferSize += 4; // presentMode
    bufferSize += 4; // clipped
    bufferSize += vt_sizeof_VkSwapchainKHR(val->oldSwapchain);
    return bufferSize;
}

static inline void vt_serialize_VkSwapchainCreateInfoKHR(VkSwapchainCreateInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DEVICE_GROUP_SWAPCHAIN_CREATE_INFO_KHR: {
                    int itemSize = vt_sizeof_VkDeviceGroupSwapchainCreateInfoKHR((VkDeviceGroupSwapchainCreateInfoKHR*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDeviceGroupSwapchainCreateInfoKHR((VkDeviceGroupSwapchainCreateInfoKHR*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkImageFormatListCreateInfo((VkImageFormatListCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkImageFormatListCreateInfo((VkImageFormatListCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkSwapchainCreateFlagsKHR*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;

    vt_serialize_VkSurfaceKHR(val->surface, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkSurfaceKHR(val->surface);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->minImageCount;
    bufferOffset += 4;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->imageFormat;
    bufferOffset += 4;
    *(VkColorSpaceKHR*)(outputBuffer + bufferOffset) = val->imageColorSpace;
    bufferOffset += 4;
    vt_serialize_VkExtent2D(&val->imageExtent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent2D(&val->imageExtent);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->imageArrayLayers;
    bufferOffset += 4;
    *(VkImageUsageFlags*)(outputBuffer + bufferOffset) = val->imageUsage;
    bufferOffset += 4;
    *(VkSharingMode*)(outputBuffer + bufferOffset) = val->imageSharingMode;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->queueFamilyIndexCount;
    bufferOffset += 4;

    if (val->pQueueFamilyIndices) {
        int arrSize = val->queueFamilyIndexCount ? val->queueFamilyIndexCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pQueueFamilyIndices, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkSurfaceTransformFlagBitsKHR*)(outputBuffer + bufferOffset) = val->preTransform;
    bufferOffset += 4;
    *(VkCompositeAlphaFlagBitsKHR*)(outputBuffer + bufferOffset) = val->compositeAlpha;
    bufferOffset += 4;
    *(VkPresentModeKHR*)(outputBuffer + bufferOffset) = val->presentMode;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->clipped;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkSwapchainKHR(val->oldSwapchain, outputBuffer + bufferOffset);
#else
    VkObject* oldSwapchainObject = VkObject_fromHandle(val->oldSwapchain);
    vt_serialize_VkSwapchainKHR((VkSwapchainKHR)&oldSwapchainObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkSwapchainKHR(val->oldSwapchain);
}

static inline void vt_unserialize_VkSwapchainCreateInfoKHR(VkSwapchainCreateInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_SWAPCHAIN_CREATE_INFO_KHR: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDeviceGroupSwapchainCreateInfoKHR* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DEVICE_GROUP_SWAPCHAIN_CREATE_INFO_KHR) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDeviceGroupSwapchainCreateInfoKHR));
                    vt_unserialize_VkDeviceGroupSwapchainCreateInfoKHR(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkImageFormatListCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkImageFormatListCreateInfo));
                    vt_unserialize_VkImageFormatListCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkSwapchainCreateFlagsKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    vt_unserialize_VkSurfaceKHR(val->surface, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkSurfaceKHR(val->surface);
    val->minImageCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->imageFormat = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->imageColorSpace = *(VkColorSpaceKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkExtent2D(&val->imageExtent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent2D(&val->imageExtent);
    val->imageArrayLayers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->imageUsage = *(VkImageUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->imageSharingMode = *(VkSharingMode*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queueFamilyIndexCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize13 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize13 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize13 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize13 * 4);
        bufferOffset += dataSize13 * 4;
        val->pQueueFamilyIndices = arrValues;
    }
    else val->pQueueFamilyIndices = NULL;
    val->preTransform = *(VkSurfaceTransformFlagBitsKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->compositeAlpha = *(VkCompositeAlphaFlagBitsKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->presentMode = *(VkPresentModeKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->clipped = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t oldSwapchainId;
    vt_unserialize_VkSwapchainKHR((VkSwapchainKHR)&oldSwapchainId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->oldSwapchain = VkObject_fromId(oldSwapchainId);
#else
    VkObject* oldSwapchainObject = VkObject_create(VK_OBJECT_TYPE_SWAPCHAIN_KHR, oldSwapchainId);
    val->oldSwapchain = VkObject_toHandle(oldSwapchainObject);
#endif
    bufferOffset += vt_sizeof_VkSwapchainKHR(val->oldSwapchain);
}

static inline int vt_sizeof_VkPresentInfoKHR(VkPresentInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_PRESENT_INFO_KHR: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDeviceGroupPresentInfoKHR((VkDeviceGroupPresentInfoKHR*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // waitSemaphoreCount
    bufferSize += 4;
    if (val->pWaitSemaphores) {
        for (int i = 0; i < val->waitSemaphoreCount; i++) {
            bufferSize += vt_sizeof_VkSemaphore(val->pWaitSemaphores[i]);
        }
    }
    bufferSize += 4; // swapchainCount
    bufferSize += 4;
    if (val->pSwapchains) {
        for (int i = 0; i < val->swapchainCount; i++) {
            bufferSize += vt_sizeof_VkSwapchainKHR(val->pSwapchains[i]);
        }
    }
    bufferSize += 4;
    bufferSize += val->pImageIndices ? val->swapchainCount * 4 : 0;
    bufferSize += 4;
    bufferSize += val->pResults ? val->swapchainCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkPresentInfoKHR(VkPresentInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DEVICE_GROUP_PRESENT_INFO_KHR: {
                    int itemSize = vt_sizeof_VkDeviceGroupPresentInfoKHR((VkDeviceGroupPresentInfoKHR*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDeviceGroupPresentInfoKHR((VkDeviceGroupPresentInfoKHR*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->waitSemaphoreCount;
    bufferOffset += 4;

    if (val->pWaitSemaphores) {
        *(int*)(outputBuffer + bufferOffset) = val->waitSemaphoreCount;
        bufferOffset += 4;
        for (int i = 0; i < val->waitSemaphoreCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkSemaphore(val->pWaitSemaphores[i], outputBuffer + bufferOffset);
#else
            VkObject* waitSemaphoresObject = VkObject_fromHandle(val->pWaitSemaphores[i]);
            vt_serialize_VkSemaphore((VkSemaphore)&waitSemaphoresObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkSemaphore(val->pWaitSemaphores[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->swapchainCount;
    bufferOffset += 4;

    if (val->pSwapchains) {
        *(int*)(outputBuffer + bufferOffset) = val->swapchainCount;
        bufferOffset += 4;
        for (int i = 0; i < val->swapchainCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkSwapchainKHR(val->pSwapchains[i], outputBuffer + bufferOffset);
#else
            VkObject* swapchainsObject = VkObject_fromHandle(val->pSwapchains[i]);
            vt_serialize_VkSwapchainKHR((VkSwapchainKHR)&swapchainsObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkSwapchainKHR(val->pSwapchains[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pImageIndices) {
        int arrSize = val->swapchainCount ? val->swapchainCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pImageIndices, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pResults) {
        int arrSize = val->swapchainCount ? val->swapchainCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pResults, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkPresentInfoKHR(VkPresentInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_PRESENT_INFO_KHR: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDeviceGroupPresentInfoKHR* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DEVICE_GROUP_PRESENT_INFO_KHR) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDeviceGroupPresentInfoKHR));
                    vt_unserialize_VkDeviceGroupPresentInfoKHR(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->waitSemaphoreCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkSemaphore* arrValues = vt_alloc(memoryPool, dataSize4 * sizeof(VkSemaphore));
        for (int i = 0; i < dataSize4; i++) {
            uint64_t waitSemaphoresId;
            vt_unserialize_VkSemaphore((VkSemaphore)&waitSemaphoresId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(waitSemaphoresId);
#else
                VkObject* waitSemaphoresObject = VkObject_create(VK_OBJECT_TYPE_SEMAPHORE, waitSemaphoresId);
                arrValues[i] = VkObject_toHandle(waitSemaphoresObject);
#endif
                bufferOffset += vt_sizeof_VkSemaphore(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkSemaphore(VK_NULL_HANDLE);
        }
        val->pWaitSemaphores = arrValues;
    }
    else val->pWaitSemaphores = NULL;
    val->swapchainCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkSwapchainKHR* arrValues = vt_alloc(memoryPool, dataSize6 * sizeof(VkSwapchainKHR));
        for (int i = 0; i < dataSize6; i++) {
            uint64_t swapchainsId;
            vt_unserialize_VkSwapchainKHR((VkSwapchainKHR)&swapchainsId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(swapchainsId);
#else
                VkObject* swapchainsObject = VkObject_create(VK_OBJECT_TYPE_SWAPCHAIN_KHR, swapchainsId);
                arrValues[i] = VkObject_toHandle(swapchainsObject);
#endif
                bufferOffset += vt_sizeof_VkSwapchainKHR(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkSwapchainKHR(VK_NULL_HANDLE);
        }
        val->pSwapchains = arrValues;
    }
    else val->pSwapchains = NULL;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize7 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize7 * 4);
        bufferOffset += dataSize7 * 4;
        val->pImageIndices = arrValues;
    }
    else val->pImageIndices = NULL;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        VkResult* arrValues = vt_alloc(memoryPool, dataSize8 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize8 * 4);
        bufferOffset += dataSize8 * 4;
        val->pResults = arrValues;
    }
    else val->pResults = NULL;
}

static inline int vt_sizeof_VkImageSwapchainCreateInfoKHR(VkImageSwapchainCreateInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkSwapchainKHR(val->swapchain);
    return bufferSize;
}

static inline void vt_serialize_VkImageSwapchainCreateInfoKHR(VkImageSwapchainCreateInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkSwapchainKHR(val->swapchain, outputBuffer + bufferOffset);
#else
    VkObject* swapchainObject = VkObject_fromHandle(val->swapchain);
    vt_serialize_VkSwapchainKHR((VkSwapchainKHR)&swapchainObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkSwapchainKHR(val->swapchain);
}

static inline void vt_unserialize_VkImageSwapchainCreateInfoKHR(VkImageSwapchainCreateInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t swapchainId;
    vt_unserialize_VkSwapchainKHR((VkSwapchainKHR)&swapchainId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->swapchain = VkObject_fromId(swapchainId);
#else
    VkObject* swapchainObject = VkObject_create(VK_OBJECT_TYPE_SWAPCHAIN_KHR, swapchainId);
    val->swapchain = VkObject_toHandle(swapchainObject);
#endif
    bufferOffset += vt_sizeof_VkSwapchainKHR(val->swapchain);
}

static inline int vt_sizeof_VkBindImageMemorySwapchainInfoKHR(VkBindImageMemorySwapchainInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkSwapchainKHR(val->swapchain);
    bufferSize += 4; // imageIndex
    return bufferSize;
}

static inline void vt_serialize_VkBindImageMemorySwapchainInfoKHR(VkBindImageMemorySwapchainInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkSwapchainKHR(val->swapchain, outputBuffer + bufferOffset);
#else
    VkObject* swapchainObject = VkObject_fromHandle(val->swapchain);
    vt_serialize_VkSwapchainKHR((VkSwapchainKHR)&swapchainObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkSwapchainKHR(val->swapchain);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->imageIndex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkBindImageMemorySwapchainInfoKHR(VkBindImageMemorySwapchainInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t swapchainId;
    vt_unserialize_VkSwapchainKHR((VkSwapchainKHR)&swapchainId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->swapchain = VkObject_fromId(swapchainId);
#else
    VkObject* swapchainObject = VkObject_create(VK_OBJECT_TYPE_SWAPCHAIN_KHR, swapchainId);
    val->swapchain = VkObject_toHandle(swapchainObject);
#endif
    bufferOffset += vt_sizeof_VkSwapchainKHR(val->swapchain);
    val->imageIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkAcquireNextImageInfoKHR(VkAcquireNextImageInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkSwapchainKHR(val->swapchain);
    bufferSize += 8; // timeout
    bufferSize += vt_sizeof_VkSemaphore(val->semaphore);
    bufferSize += vt_sizeof_VkFence(val->fence);
    bufferSize += 4; // deviceMask
    return bufferSize;
}

static inline void vt_serialize_VkAcquireNextImageInfoKHR(VkAcquireNextImageInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkSwapchainKHR(val->swapchain, outputBuffer + bufferOffset);
#else
    VkObject* swapchainObject = VkObject_fromHandle(val->swapchain);
    vt_serialize_VkSwapchainKHR((VkSwapchainKHR)&swapchainObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkSwapchainKHR(val->swapchain);
    *(uint64_t*)(outputBuffer + bufferOffset) = val->timeout;
    bufferOffset += 8;

#ifdef VT_SERVER
    vt_serialize_VkSemaphore(val->semaphore, outputBuffer + bufferOffset);
#else
    VkObject* semaphoreObject = VkObject_fromHandle(val->semaphore);
    vt_serialize_VkSemaphore((VkSemaphore)&semaphoreObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkSemaphore(val->semaphore);

#ifdef VT_SERVER
    vt_serialize_VkFence(val->fence, outputBuffer + bufferOffset);
#else
    VkObject* fenceObject = VkObject_fromHandle(val->fence);
    vt_serialize_VkFence((VkFence)&fenceObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkFence(val->fence);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkAcquireNextImageInfoKHR(VkAcquireNextImageInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t swapchainId;
    vt_unserialize_VkSwapchainKHR((VkSwapchainKHR)&swapchainId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->swapchain = VkObject_fromId(swapchainId);
#else
    VkObject* swapchainObject = VkObject_create(VK_OBJECT_TYPE_SWAPCHAIN_KHR, swapchainId);
    val->swapchain = VkObject_toHandle(swapchainObject);
#endif
    bufferOffset += vt_sizeof_VkSwapchainKHR(val->swapchain);
    val->timeout = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;

    uint64_t semaphoreId;
    vt_unserialize_VkSemaphore((VkSemaphore)&semaphoreId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->semaphore = VkObject_fromId(semaphoreId);
#else
    VkObject* semaphoreObject = VkObject_create(VK_OBJECT_TYPE_SEMAPHORE, semaphoreId);
    val->semaphore = VkObject_toHandle(semaphoreObject);
#endif
    bufferOffset += vt_sizeof_VkSemaphore(val->semaphore);

    uint64_t fenceId;
    vt_unserialize_VkFence((VkFence)&fenceId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->fence = VkObject_fromId(fenceId);
#else
    VkObject* fenceObject = VkObject_create(VK_OBJECT_TYPE_FENCE, fenceId);
    val->fence = VkObject_toHandle(fenceObject);
#endif
    bufferOffset += vt_sizeof_VkFence(val->fence);
    val->deviceMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDeviceGroupPresentCapabilitiesKHR(VkDeviceGroupPresentCapabilitiesKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += VK_MAX_DEVICE_GROUP_SIZE;
    bufferSize += 4; // modes
    return bufferSize;
}

static inline void vt_serialize_VkDeviceGroupPresentCapabilitiesKHR(VkDeviceGroupPresentCapabilitiesKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->presentMask, VK_MAX_DEVICE_GROUP_SIZE);
    bufferOffset += VK_MAX_DEVICE_GROUP_SIZE;
    *(VkDeviceGroupPresentModeFlagsKHR*)(outputBuffer + bufferOffset) = val->modes;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDeviceGroupPresentCapabilitiesKHR(VkDeviceGroupPresentCapabilitiesKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    memcpy(val->presentMask, inputBuffer + bufferOffset, VK_MAX_DEVICE_GROUP_SIZE);
    bufferOffset += VK_MAX_DEVICE_GROUP_SIZE;
    val->modes = *(VkDeviceGroupPresentModeFlagsKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceTransformFeedbackFeaturesEXT(VkPhysicalDeviceTransformFeedbackFeaturesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // transformFeedback
    bufferSize += 4; // geometryStreams
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceTransformFeedbackFeaturesEXT(VkPhysicalDeviceTransformFeedbackFeaturesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->transformFeedback;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->geometryStreams;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceTransformFeedbackFeaturesEXT(VkPhysicalDeviceTransformFeedbackFeaturesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->transformFeedback = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->geometryStreams = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceTransformFeedbackPropertiesEXT(VkPhysicalDeviceTransformFeedbackPropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maxTransformFeedbackStreams
    bufferSize += 4; // maxTransformFeedbackBuffers
    bufferSize += 8; // maxTransformFeedbackBufferSize
    bufferSize += 4; // maxTransformFeedbackStreamDataSize
    bufferSize += 4; // maxTransformFeedbackBufferDataSize
    bufferSize += 4; // maxTransformFeedbackBufferDataStride
    bufferSize += 4; // transformFeedbackQueries
    bufferSize += 4; // transformFeedbackStreamsLinesTriangles
    bufferSize += 4; // transformFeedbackRasterizationStreamSelect
    bufferSize += 4; // transformFeedbackDraw
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceTransformFeedbackPropertiesEXT(VkPhysicalDeviceTransformFeedbackPropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTransformFeedbackStreams;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTransformFeedbackBuffers;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->maxTransformFeedbackBufferSize;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTransformFeedbackStreamDataSize;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTransformFeedbackBufferDataSize;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxTransformFeedbackBufferDataStride;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->transformFeedbackQueries;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->transformFeedbackStreamsLinesTriangles;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->transformFeedbackRasterizationStreamSelect;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->transformFeedbackDraw;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceTransformFeedbackPropertiesEXT(VkPhysicalDeviceTransformFeedbackPropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxTransformFeedbackStreams = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTransformFeedbackBuffers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTransformFeedbackBufferSize = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->maxTransformFeedbackStreamDataSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTransformFeedbackBufferDataSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxTransformFeedbackBufferDataStride = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->transformFeedbackQueries = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->transformFeedbackStreamsLinesTriangles = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->transformFeedbackRasterizationStreamSelect = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->transformFeedbackDraw = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkRenderingInfo(VkRenderingInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_RENDER_PASS_BEGIN_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDeviceGroupRenderPassBeginInfo((VkDeviceGroupRenderPassBeginInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += vt_sizeof_VkRect2D(&val->renderArea);
    bufferSize += 4; // layerCount
    bufferSize += 4; // viewMask
    bufferSize += 4; // colorAttachmentCount
    bufferSize += 4;
    if (val->pColorAttachments) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkRenderingAttachmentInfo(&val->pColorAttachments[i]);
        }
    }
    bufferSize += 4;
    bufferSize += val->pDepthAttachment ? vt_sizeof_VkRenderingAttachmentInfo(val->pDepthAttachment) : 0;
    bufferSize += 4;
    bufferSize += val->pStencilAttachment ? vt_sizeof_VkRenderingAttachmentInfo(val->pStencilAttachment) : 0;
    return bufferSize;
}

static inline void vt_serialize_VkRenderingInfo(VkRenderingInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DEVICE_GROUP_RENDER_PASS_BEGIN_INFO: {
                    int itemSize = vt_sizeof_VkDeviceGroupRenderPassBeginInfo((VkDeviceGroupRenderPassBeginInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDeviceGroupRenderPassBeginInfo((VkDeviceGroupRenderPassBeginInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkRenderingFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    vt_serialize_VkRect2D(&val->renderArea, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkRect2D(&val->renderArea);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->layerCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->viewMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->colorAttachmentCount;
    bufferOffset += 4;

    if (val->pColorAttachments) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkRenderingAttachmentInfo(&val->pColorAttachments[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkRenderingAttachmentInfo(&val->pColorAttachments[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pDepthAttachment) {
        int itemSize = vt_sizeof_VkRenderingAttachmentInfo(val->pDepthAttachment);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkRenderingAttachmentInfo(val->pDepthAttachment, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pStencilAttachment) {
        int itemSize = vt_sizeof_VkRenderingAttachmentInfo(val->pStencilAttachment);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkRenderingAttachmentInfo(val->pStencilAttachment, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkRenderingInfo(VkRenderingInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_RENDER_PASS_BEGIN_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDeviceGroupRenderPassBeginInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DEVICE_GROUP_RENDER_PASS_BEGIN_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDeviceGroupRenderPassBeginInfo));
                    vt_unserialize_VkDeviceGroupRenderPassBeginInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkRenderingFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkRect2D(&val->renderArea, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkRect2D(&val->renderArea);
    val->layerCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->viewMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->colorAttachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        VkRenderingAttachmentInfo* arrValues = vt_alloc(memoryPool, dataSize8 * sizeof(VkRenderingAttachmentInfo));
        for (int i = 0; i < dataSize8; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkRenderingAttachmentInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pColorAttachments = arrValues;
    }
    else val->pColorAttachments = NULL;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        val->pDepthAttachment = vt_alloc(memoryPool, sizeof(VkRenderingAttachmentInfo));
        vt_unserialize_VkRenderingAttachmentInfo(val->pDepthAttachment, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize9;
    }
    else val->pDepthAttachment = NULL;

    int dataSize10 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize10 > 0) {
        val->pStencilAttachment = vt_alloc(memoryPool, sizeof(VkRenderingAttachmentInfo));
        vt_unserialize_VkRenderingAttachmentInfo(val->pStencilAttachment, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize10;
    }
    else val->pStencilAttachment = NULL;
}

static inline int vt_sizeof_VkPipelineRenderingCreateInfo(VkPipelineRenderingCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // viewMask
    bufferSize += 4; // colorAttachmentCount
    bufferSize += 4;
    bufferSize += val->pColorAttachmentFormats ? val->colorAttachmentCount * 4 : 0;
    bufferSize += 4; // depthAttachmentFormat
    bufferSize += 4; // stencilAttachmentFormat
    return bufferSize;
}

static inline void vt_serialize_VkPipelineRenderingCreateInfo(VkPipelineRenderingCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->viewMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->colorAttachmentCount;
    bufferOffset += 4;

    if (val->pColorAttachmentFormats) {
        int arrSize = val->colorAttachmentCount ? val->colorAttachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pColorAttachmentFormats, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkFormat*)(outputBuffer + bufferOffset) = val->depthAttachmentFormat;
    bufferOffset += 4;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->stencilAttachmentFormat;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPipelineRenderingCreateInfo(VkPipelineRenderingCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->viewMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->colorAttachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkFormat* arrValues = vt_alloc(memoryPool, dataSize5 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize5 * 4);
        bufferOffset += dataSize5 * 4;
        val->pColorAttachmentFormats = arrValues;
    }
    else val->pColorAttachmentFormats = NULL;
    val->depthAttachmentFormat = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stencilAttachmentFormat = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceDynamicRenderingFeatures(VkPhysicalDeviceDynamicRenderingFeatures* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // dynamicRendering
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceDynamicRenderingFeatures(VkPhysicalDeviceDynamicRenderingFeatures* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->dynamicRendering;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceDynamicRenderingFeatures(VkPhysicalDeviceDynamicRenderingFeatures* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->dynamicRendering = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceFeatures2(VkPhysicalDeviceFeatures2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PRIVATE_DATA_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevicePrivateDataFeatures((VkPhysicalDevicePrivateDataFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VARIABLE_POINTERS_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVariablePointersFeatures((VkPhysicalDeviceVariablePointersFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMultiviewFeatures((VkPhysicalDeviceMultiviewFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_16BIT_STORAGE_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevice16BitStorageFeatures((VkPhysicalDevice16BitStorageFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_SUBGROUP_EXTENDED_TYPES_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures((VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_YCBCR_CONVERSION_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSamplerYcbcrConversionFeatures((VkPhysicalDeviceSamplerYcbcrConversionFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceProtectedMemoryFeatures((VkPhysicalDeviceProtectedMemoryFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceInlineUniformBlockFeatures((VkPhysicalDeviceInlineUniformBlockFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMaintenance4Features((VkPhysicalDeviceMaintenance4Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DRAW_PARAMETERS_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderDrawParametersFeatures((VkPhysicalDeviceShaderDrawParametersFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_FLOAT16_INT8_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderFloat16Int8Features((VkPhysicalDeviceShaderFloat16Int8Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_HOST_QUERY_RESET_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceHostQueryResetFeatures((VkPhysicalDeviceHostQueryResetFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceDescriptorIndexingFeatures((VkPhysicalDeviceDescriptorIndexingFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceTimelineSemaphoreFeatures((VkPhysicalDeviceTimelineSemaphoreFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_8BIT_STORAGE_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevice8BitStorageFeatures((VkPhysicalDevice8BitStorageFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONDITIONAL_RENDERING_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceConditionalRenderingFeaturesEXT((VkPhysicalDeviceConditionalRenderingFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_MEMORY_MODEL_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVulkanMemoryModelFeatures((VkPhysicalDeviceVulkanMemoryModelFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_ATOMIC_INT64_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderAtomicInt64Features((VkPhysicalDeviceShaderAtomicInt64Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_FEATURES_KHR: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR((VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceTransformFeedbackFeaturesEXT((VkPhysicalDeviceTransformFeedbackFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SCALAR_BLOCK_LAYOUT_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceScalarBlockLayoutFeatures((VkPhysicalDeviceScalarBlockLayoutFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_UNIFORM_BUFFER_STANDARD_LAYOUT_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceUniformBufferStandardLayoutFeatures((VkPhysicalDeviceUniformBufferStandardLayoutFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_ENABLE_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceDepthClipEnableFeaturesEXT((VkPhysicalDeviceDepthClipEnableFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BUFFER_DEVICE_ADDRESS_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceBufferDeviceAddressFeatures((VkPhysicalDeviceBufferDeviceAddressFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGELESS_FRAMEBUFFER_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceImagelessFramebufferFeatures((VkPhysicalDeviceImagelessFramebufferFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXTURE_COMPRESSION_ASTC_HDR_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceTextureCompressionASTCHDRFeatures((VkPhysicalDeviceTextureCompressionASTCHDRFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FRAGMENT_SHADER_INTERLOCK_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT((VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SEPARATE_DEPTH_STENCIL_LAYOUTS_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures((VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DEMOTE_TO_HELPER_INVOCATION_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures((VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT((VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSubgroupSizeControlFeatures((VkPhysicalDeviceSubgroupSizeControlFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PIPELINE_CREATION_CACHE_CONTROL_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevicePipelineCreationCacheControlFeatures((VkPhysicalDevicePipelineCreationCacheControlFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVulkan11Features((VkPhysicalDeviceVulkan11Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVulkan12Features((VkPhysicalDeviceVulkan12Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVulkan13Features((VkPhysicalDeviceVulkan13Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceCustomBorderColorFeaturesEXT((VkPhysicalDeviceCustomBorderColorFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BORDER_COLOR_SWIZZLE_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceBorderColorSwizzleFeaturesEXT((VkPhysicalDeviceBorderColorSwizzleFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceExtendedDynamicStateFeaturesEXT((VkPhysicalDeviceExtendedDynamicStateFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceExtendedDynamicState3FeaturesEXT((VkPhysicalDeviceExtendedDynamicState3FeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ZERO_INITIALIZE_WORKGROUP_MEMORY_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures((VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceRobustness2FeaturesEXT((VkPhysicalDeviceRobustness2FeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGE_ROBUSTNESS_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceImageRobustnessFeatures((VkPhysicalDeviceImageRobustnessFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_4444_FORMATS_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevice4444FormatsFeaturesEXT((VkPhysicalDevice4444FormatsFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_TERMINATE_INVOCATION_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderTerminateInvocationFeatures((VkPhysicalDeviceShaderTerminateInvocationFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MUTABLE_DESCRIPTOR_TYPE_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT((VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_CONTROL_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceDepthClipControlFeaturesEXT((VkPhysicalDeviceDepthClipControlFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_COLOR_WRITE_ENABLE_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceColorWriteEnableFeaturesEXT((VkPhysicalDeviceColorWriteEnableFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SYNCHRONIZATION_2_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSynchronization2Features((VkPhysicalDeviceSynchronization2Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceProvokingVertexFeaturesEXT((VkPhysicalDeviceProvokingVertexFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderIntegerDotProductFeatures((VkPhysicalDeviceShaderIntegerDotProductFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DYNAMIC_RENDERING_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceDynamicRenderingFeatures((VkPhysicalDeviceDynamicRenderingFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT((VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMapMemoryPlacedFeaturesEXT((VkPhysicalDeviceMapMemoryPlacedFeaturesEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkPhysicalDeviceFeatures(&val->features);
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceFeatures2(VkPhysicalDeviceFeatures2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PRIVATE_DATA_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDevicePrivateDataFeatures((VkPhysicalDevicePrivateDataFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevicePrivateDataFeatures((VkPhysicalDevicePrivateDataFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VARIABLE_POINTERS_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVariablePointersFeatures((VkPhysicalDeviceVariablePointersFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVariablePointersFeatures((VkPhysicalDeviceVariablePointersFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMultiviewFeatures((VkPhysicalDeviceMultiviewFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMultiviewFeatures((VkPhysicalDeviceMultiviewFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_16BIT_STORAGE_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDevice16BitStorageFeatures((VkPhysicalDevice16BitStorageFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevice16BitStorageFeatures((VkPhysicalDevice16BitStorageFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_SUBGROUP_EXTENDED_TYPES_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures((VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures((VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_YCBCR_CONVERSION_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSamplerYcbcrConversionFeatures((VkPhysicalDeviceSamplerYcbcrConversionFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSamplerYcbcrConversionFeatures((VkPhysicalDeviceSamplerYcbcrConversionFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceProtectedMemoryFeatures((VkPhysicalDeviceProtectedMemoryFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceProtectedMemoryFeatures((VkPhysicalDeviceProtectedMemoryFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceInlineUniformBlockFeatures((VkPhysicalDeviceInlineUniformBlockFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceInlineUniformBlockFeatures((VkPhysicalDeviceInlineUniformBlockFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMaintenance4Features((VkPhysicalDeviceMaintenance4Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMaintenance4Features((VkPhysicalDeviceMaintenance4Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DRAW_PARAMETERS_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderDrawParametersFeatures((VkPhysicalDeviceShaderDrawParametersFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderDrawParametersFeatures((VkPhysicalDeviceShaderDrawParametersFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_FLOAT16_INT8_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderFloat16Int8Features((VkPhysicalDeviceShaderFloat16Int8Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderFloat16Int8Features((VkPhysicalDeviceShaderFloat16Int8Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_HOST_QUERY_RESET_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceHostQueryResetFeatures((VkPhysicalDeviceHostQueryResetFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceHostQueryResetFeatures((VkPhysicalDeviceHostQueryResetFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceDescriptorIndexingFeatures((VkPhysicalDeviceDescriptorIndexingFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceDescriptorIndexingFeatures((VkPhysicalDeviceDescriptorIndexingFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceTimelineSemaphoreFeatures((VkPhysicalDeviceTimelineSemaphoreFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceTimelineSemaphoreFeatures((VkPhysicalDeviceTimelineSemaphoreFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_8BIT_STORAGE_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDevice8BitStorageFeatures((VkPhysicalDevice8BitStorageFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevice8BitStorageFeatures((VkPhysicalDevice8BitStorageFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONDITIONAL_RENDERING_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceConditionalRenderingFeaturesEXT((VkPhysicalDeviceConditionalRenderingFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceConditionalRenderingFeaturesEXT((VkPhysicalDeviceConditionalRenderingFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_MEMORY_MODEL_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVulkanMemoryModelFeatures((VkPhysicalDeviceVulkanMemoryModelFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVulkanMemoryModelFeatures((VkPhysicalDeviceVulkanMemoryModelFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_ATOMIC_INT64_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderAtomicInt64Features((VkPhysicalDeviceShaderAtomicInt64Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderAtomicInt64Features((VkPhysicalDeviceShaderAtomicInt64Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_FEATURES_KHR: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR((VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR((VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceTransformFeedbackFeaturesEXT((VkPhysicalDeviceTransformFeedbackFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceTransformFeedbackFeaturesEXT((VkPhysicalDeviceTransformFeedbackFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SCALAR_BLOCK_LAYOUT_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceScalarBlockLayoutFeatures((VkPhysicalDeviceScalarBlockLayoutFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceScalarBlockLayoutFeatures((VkPhysicalDeviceScalarBlockLayoutFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_UNIFORM_BUFFER_STANDARD_LAYOUT_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceUniformBufferStandardLayoutFeatures((VkPhysicalDeviceUniformBufferStandardLayoutFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceUniformBufferStandardLayoutFeatures((VkPhysicalDeviceUniformBufferStandardLayoutFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_ENABLE_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceDepthClipEnableFeaturesEXT((VkPhysicalDeviceDepthClipEnableFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceDepthClipEnableFeaturesEXT((VkPhysicalDeviceDepthClipEnableFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BUFFER_DEVICE_ADDRESS_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceBufferDeviceAddressFeatures((VkPhysicalDeviceBufferDeviceAddressFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceBufferDeviceAddressFeatures((VkPhysicalDeviceBufferDeviceAddressFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGELESS_FRAMEBUFFER_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceImagelessFramebufferFeatures((VkPhysicalDeviceImagelessFramebufferFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceImagelessFramebufferFeatures((VkPhysicalDeviceImagelessFramebufferFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXTURE_COMPRESSION_ASTC_HDR_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceTextureCompressionASTCHDRFeatures((VkPhysicalDeviceTextureCompressionASTCHDRFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceTextureCompressionASTCHDRFeatures((VkPhysicalDeviceTextureCompressionASTCHDRFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FRAGMENT_SHADER_INTERLOCK_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT((VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT((VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SEPARATE_DEPTH_STENCIL_LAYOUTS_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures((VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures((VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DEMOTE_TO_HELPER_INVOCATION_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures((VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures((VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT((VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT((VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSubgroupSizeControlFeatures((VkPhysicalDeviceSubgroupSizeControlFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSubgroupSizeControlFeatures((VkPhysicalDeviceSubgroupSizeControlFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PIPELINE_CREATION_CACHE_CONTROL_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDevicePipelineCreationCacheControlFeatures((VkPhysicalDevicePipelineCreationCacheControlFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevicePipelineCreationCacheControlFeatures((VkPhysicalDevicePipelineCreationCacheControlFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVulkan11Features((VkPhysicalDeviceVulkan11Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVulkan11Features((VkPhysicalDeviceVulkan11Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVulkan12Features((VkPhysicalDeviceVulkan12Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVulkan12Features((VkPhysicalDeviceVulkan12Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVulkan13Features((VkPhysicalDeviceVulkan13Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVulkan13Features((VkPhysicalDeviceVulkan13Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceCustomBorderColorFeaturesEXT((VkPhysicalDeviceCustomBorderColorFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceCustomBorderColorFeaturesEXT((VkPhysicalDeviceCustomBorderColorFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BORDER_COLOR_SWIZZLE_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceBorderColorSwizzleFeaturesEXT((VkPhysicalDeviceBorderColorSwizzleFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceBorderColorSwizzleFeaturesEXT((VkPhysicalDeviceBorderColorSwizzleFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceExtendedDynamicStateFeaturesEXT((VkPhysicalDeviceExtendedDynamicStateFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceExtendedDynamicStateFeaturesEXT((VkPhysicalDeviceExtendedDynamicStateFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceExtendedDynamicState3FeaturesEXT((VkPhysicalDeviceExtendedDynamicState3FeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceExtendedDynamicState3FeaturesEXT((VkPhysicalDeviceExtendedDynamicState3FeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ZERO_INITIALIZE_WORKGROUP_MEMORY_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures((VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures((VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceRobustness2FeaturesEXT((VkPhysicalDeviceRobustness2FeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceRobustness2FeaturesEXT((VkPhysicalDeviceRobustness2FeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGE_ROBUSTNESS_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceImageRobustnessFeatures((VkPhysicalDeviceImageRobustnessFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceImageRobustnessFeatures((VkPhysicalDeviceImageRobustnessFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_4444_FORMATS_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDevice4444FormatsFeaturesEXT((VkPhysicalDevice4444FormatsFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevice4444FormatsFeaturesEXT((VkPhysicalDevice4444FormatsFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_TERMINATE_INVOCATION_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderTerminateInvocationFeatures((VkPhysicalDeviceShaderTerminateInvocationFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderTerminateInvocationFeatures((VkPhysicalDeviceShaderTerminateInvocationFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MUTABLE_DESCRIPTOR_TYPE_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT((VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT((VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_CONTROL_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceDepthClipControlFeaturesEXT((VkPhysicalDeviceDepthClipControlFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceDepthClipControlFeaturesEXT((VkPhysicalDeviceDepthClipControlFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_COLOR_WRITE_ENABLE_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceColorWriteEnableFeaturesEXT((VkPhysicalDeviceColorWriteEnableFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceColorWriteEnableFeaturesEXT((VkPhysicalDeviceColorWriteEnableFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SYNCHRONIZATION_2_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSynchronization2Features((VkPhysicalDeviceSynchronization2Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSynchronization2Features((VkPhysicalDeviceSynchronization2Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceProvokingVertexFeaturesEXT((VkPhysicalDeviceProvokingVertexFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceProvokingVertexFeaturesEXT((VkPhysicalDeviceProvokingVertexFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderIntegerDotProductFeatures((VkPhysicalDeviceShaderIntegerDotProductFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderIntegerDotProductFeatures((VkPhysicalDeviceShaderIntegerDotProductFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DYNAMIC_RENDERING_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceDynamicRenderingFeatures((VkPhysicalDeviceDynamicRenderingFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceDynamicRenderingFeatures((VkPhysicalDeviceDynamicRenderingFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT((VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT((VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMapMemoryPlacedFeaturesEXT((VkPhysicalDeviceMapMemoryPlacedFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMapMemoryPlacedFeaturesEXT((VkPhysicalDeviceMapMemoryPlacedFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    vt_serialize_VkPhysicalDeviceFeatures(&val->features, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkPhysicalDeviceFeatures(&val->features);
}

static inline void vt_unserialize_VkPhysicalDeviceFeatures2(VkPhysicalDeviceFeatures2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PRIVATE_DATA_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevicePrivateDataFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PRIVATE_DATA_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevicePrivateDataFeatures));
                    vt_unserialize_VkPhysicalDevicePrivateDataFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VARIABLE_POINTERS_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVariablePointersFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VARIABLE_POINTERS_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVariablePointersFeatures));
                    vt_unserialize_VkPhysicalDeviceVariablePointersFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMultiviewFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMultiviewFeatures));
                    vt_unserialize_VkPhysicalDeviceMultiviewFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_16BIT_STORAGE_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevice16BitStorageFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_16BIT_STORAGE_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevice16BitStorageFeatures));
                    vt_unserialize_VkPhysicalDevice16BitStorageFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_SUBGROUP_EXTENDED_TYPES_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_SUBGROUP_EXTENDED_TYPES_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures));
                    vt_unserialize_VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_YCBCR_CONVERSION_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSamplerYcbcrConversionFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_YCBCR_CONVERSION_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSamplerYcbcrConversionFeatures));
                    vt_unserialize_VkPhysicalDeviceSamplerYcbcrConversionFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceProtectedMemoryFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceProtectedMemoryFeatures));
                    vt_unserialize_VkPhysicalDeviceProtectedMemoryFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceInlineUniformBlockFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceInlineUniformBlockFeatures));
                    vt_unserialize_VkPhysicalDeviceInlineUniformBlockFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMaintenance4Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMaintenance4Features));
                    vt_unserialize_VkPhysicalDeviceMaintenance4Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DRAW_PARAMETERS_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderDrawParametersFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DRAW_PARAMETERS_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderDrawParametersFeatures));
                    vt_unserialize_VkPhysicalDeviceShaderDrawParametersFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_FLOAT16_INT8_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderFloat16Int8Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_FLOAT16_INT8_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderFloat16Int8Features));
                    vt_unserialize_VkPhysicalDeviceShaderFloat16Int8Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_HOST_QUERY_RESET_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceHostQueryResetFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_HOST_QUERY_RESET_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceHostQueryResetFeatures));
                    vt_unserialize_VkPhysicalDeviceHostQueryResetFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceDescriptorIndexingFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceDescriptorIndexingFeatures));
                    vt_unserialize_VkPhysicalDeviceDescriptorIndexingFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceTimelineSemaphoreFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceTimelineSemaphoreFeatures));
                    vt_unserialize_VkPhysicalDeviceTimelineSemaphoreFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_8BIT_STORAGE_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevice8BitStorageFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_8BIT_STORAGE_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevice8BitStorageFeatures));
                    vt_unserialize_VkPhysicalDevice8BitStorageFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONDITIONAL_RENDERING_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceConditionalRenderingFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONDITIONAL_RENDERING_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceConditionalRenderingFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceConditionalRenderingFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_MEMORY_MODEL_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVulkanMemoryModelFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_MEMORY_MODEL_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVulkanMemoryModelFeatures));
                    vt_unserialize_VkPhysicalDeviceVulkanMemoryModelFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_ATOMIC_INT64_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderAtomicInt64Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_ATOMIC_INT64_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderAtomicInt64Features));
                    vt_unserialize_VkPhysicalDeviceShaderAtomicInt64Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_FEATURES_KHR: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_FEATURES_KHR) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR));
                    vt_unserialize_VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceTransformFeedbackFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceTransformFeedbackFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceTransformFeedbackFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SCALAR_BLOCK_LAYOUT_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceScalarBlockLayoutFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SCALAR_BLOCK_LAYOUT_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceScalarBlockLayoutFeatures));
                    vt_unserialize_VkPhysicalDeviceScalarBlockLayoutFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_UNIFORM_BUFFER_STANDARD_LAYOUT_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceUniformBufferStandardLayoutFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_UNIFORM_BUFFER_STANDARD_LAYOUT_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceUniformBufferStandardLayoutFeatures));
                    vt_unserialize_VkPhysicalDeviceUniformBufferStandardLayoutFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_ENABLE_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceDepthClipEnableFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_ENABLE_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceDepthClipEnableFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceDepthClipEnableFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BUFFER_DEVICE_ADDRESS_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceBufferDeviceAddressFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BUFFER_DEVICE_ADDRESS_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceBufferDeviceAddressFeatures));
                    vt_unserialize_VkPhysicalDeviceBufferDeviceAddressFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGELESS_FRAMEBUFFER_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceImagelessFramebufferFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGELESS_FRAMEBUFFER_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceImagelessFramebufferFeatures));
                    vt_unserialize_VkPhysicalDeviceImagelessFramebufferFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXTURE_COMPRESSION_ASTC_HDR_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceTextureCompressionASTCHDRFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXTURE_COMPRESSION_ASTC_HDR_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceTextureCompressionASTCHDRFeatures));
                    vt_unserialize_VkPhysicalDeviceTextureCompressionASTCHDRFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FRAGMENT_SHADER_INTERLOCK_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FRAGMENT_SHADER_INTERLOCK_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SEPARATE_DEPTH_STENCIL_LAYOUTS_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SEPARATE_DEPTH_STENCIL_LAYOUTS_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures));
                    vt_unserialize_VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DEMOTE_TO_HELPER_INVOCATION_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DEMOTE_TO_HELPER_INVOCATION_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures));
                    vt_unserialize_VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSubgroupSizeControlFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSubgroupSizeControlFeatures));
                    vt_unserialize_VkPhysicalDeviceSubgroupSizeControlFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PIPELINE_CREATION_CACHE_CONTROL_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevicePipelineCreationCacheControlFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PIPELINE_CREATION_CACHE_CONTROL_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevicePipelineCreationCacheControlFeatures));
                    vt_unserialize_VkPhysicalDevicePipelineCreationCacheControlFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVulkan11Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVulkan11Features));
                    vt_unserialize_VkPhysicalDeviceVulkan11Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVulkan12Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVulkan12Features));
                    vt_unserialize_VkPhysicalDeviceVulkan12Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVulkan13Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVulkan13Features));
                    vt_unserialize_VkPhysicalDeviceVulkan13Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceCustomBorderColorFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceCustomBorderColorFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceCustomBorderColorFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BORDER_COLOR_SWIZZLE_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceBorderColorSwizzleFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BORDER_COLOR_SWIZZLE_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceBorderColorSwizzleFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceBorderColorSwizzleFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceExtendedDynamicStateFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceExtendedDynamicStateFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceExtendedDynamicStateFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceExtendedDynamicState3FeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceExtendedDynamicState3FeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceExtendedDynamicState3FeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ZERO_INITIALIZE_WORKGROUP_MEMORY_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ZERO_INITIALIZE_WORKGROUP_MEMORY_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures));
                    vt_unserialize_VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceRobustness2FeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceRobustness2FeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceRobustness2FeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGE_ROBUSTNESS_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceImageRobustnessFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGE_ROBUSTNESS_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceImageRobustnessFeatures));
                    vt_unserialize_VkPhysicalDeviceImageRobustnessFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_4444_FORMATS_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevice4444FormatsFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_4444_FORMATS_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevice4444FormatsFeaturesEXT));
                    vt_unserialize_VkPhysicalDevice4444FormatsFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_TERMINATE_INVOCATION_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderTerminateInvocationFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_TERMINATE_INVOCATION_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderTerminateInvocationFeatures));
                    vt_unserialize_VkPhysicalDeviceShaderTerminateInvocationFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MUTABLE_DESCRIPTOR_TYPE_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MUTABLE_DESCRIPTOR_TYPE_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_CONTROL_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceDepthClipControlFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_CONTROL_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceDepthClipControlFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceDepthClipControlFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_COLOR_WRITE_ENABLE_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceColorWriteEnableFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_COLOR_WRITE_ENABLE_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceColorWriteEnableFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceColorWriteEnableFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SYNCHRONIZATION_2_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSynchronization2Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SYNCHRONIZATION_2_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSynchronization2Features));
                    vt_unserialize_VkPhysicalDeviceSynchronization2Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceProvokingVertexFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceProvokingVertexFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceProvokingVertexFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderIntegerDotProductFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderIntegerDotProductFeatures));
                    vt_unserialize_VkPhysicalDeviceShaderIntegerDotProductFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DYNAMIC_RENDERING_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceDynamicRenderingFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DYNAMIC_RENDERING_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceDynamicRenderingFeatures));
                    vt_unserialize_VkPhysicalDeviceDynamicRenderingFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMapMemoryPlacedFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMapMemoryPlacedFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceMapMemoryPlacedFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    vt_unserialize_VkPhysicalDeviceFeatures(&val->features, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkPhysicalDeviceFeatures(&val->features);
}

static inline int vt_sizeof_VkPhysicalDeviceProperties2(VkPhysicalDeviceProperties2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PUSH_DESCRIPTOR_PROPERTIES_KHR: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevicePushDescriptorPropertiesKHR((VkPhysicalDevicePushDescriptorPropertiesKHR*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DRIVER_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceDriverProperties((VkPhysicalDeviceDriverProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ID_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceIDProperties((VkPhysicalDeviceIDProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMultiviewProperties((VkPhysicalDeviceMultiviewProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSubgroupProperties((VkPhysicalDeviceSubgroupProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_POINT_CLIPPING_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevicePointClippingProperties((VkPhysicalDevicePointClippingProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceProtectedMemoryProperties((VkPhysicalDeviceProtectedMemoryProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_FILTER_MINMAX_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSamplerFilterMinmaxProperties((VkPhysicalDeviceSamplerFilterMinmaxProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLE_LOCATIONS_PROPERTIES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSampleLocationsPropertiesEXT((VkPhysicalDeviceSampleLocationsPropertiesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceInlineUniformBlockProperties((VkPhysicalDeviceInlineUniformBlockProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_3_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMaintenance3Properties((VkPhysicalDeviceMaintenance3Properties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMaintenance4Properties((VkPhysicalDeviceMaintenance4Properties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FLOAT_CONTROLS_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceFloatControlsProperties((VkPhysicalDeviceFloatControlsProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONSERVATIVE_RASTERIZATION_PROPERTIES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceConservativeRasterizationPropertiesEXT((VkPhysicalDeviceConservativeRasterizationPropertiesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceDescriptorIndexingProperties((VkPhysicalDeviceDescriptorIndexingProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceTimelineSemaphoreProperties((VkPhysicalDeviceTimelineSemaphoreProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_PROPERTIES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT((VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_PROPERTIES_KHR: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR((VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_STENCIL_RESOLVE_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceDepthStencilResolveProperties((VkPhysicalDeviceDepthStencilResolveProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_PROPERTIES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceTransformFeedbackPropertiesEXT((VkPhysicalDeviceTransformFeedbackPropertiesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceTexelBufferAlignmentProperties((VkPhysicalDeviceTexelBufferAlignmentProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSubgroupSizeControlProperties((VkPhysicalDeviceSubgroupSizeControlProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVulkan11Properties((VkPhysicalDeviceVulkan11Properties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVulkan12Properties((VkPhysicalDeviceVulkan12Properties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVulkan13Properties((VkPhysicalDeviceVulkan13Properties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_PROPERTIES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceCustomBorderColorPropertiesEXT((VkPhysicalDeviceCustomBorderColorPropertiesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_PROPERTIES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceExtendedDynamicState3PropertiesEXT((VkPhysicalDeviceExtendedDynamicState3PropertiesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_PROPERTIES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceRobustness2PropertiesEXT((VkPhysicalDeviceRobustness2PropertiesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_PROPERTIES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceProvokingVertexPropertiesEXT((VkPhysicalDeviceProvokingVertexPropertiesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderIntegerDotProductProperties((VkPhysicalDeviceShaderIntegerDotProductProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_PROPERTIES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT((VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_PROPERTIES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMapMemoryPlacedPropertiesEXT((VkPhysicalDeviceMapMemoryPlacedPropertiesEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkPhysicalDeviceProperties(&val->properties);
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceProperties2(VkPhysicalDeviceProperties2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PUSH_DESCRIPTOR_PROPERTIES_KHR: {
                    int itemSize = vt_sizeof_VkPhysicalDevicePushDescriptorPropertiesKHR((VkPhysicalDevicePushDescriptorPropertiesKHR*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevicePushDescriptorPropertiesKHR((VkPhysicalDevicePushDescriptorPropertiesKHR*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DRIVER_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceDriverProperties((VkPhysicalDeviceDriverProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceDriverProperties((VkPhysicalDeviceDriverProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ID_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceIDProperties((VkPhysicalDeviceIDProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceIDProperties((VkPhysicalDeviceIDProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMultiviewProperties((VkPhysicalDeviceMultiviewProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMultiviewProperties((VkPhysicalDeviceMultiviewProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSubgroupProperties((VkPhysicalDeviceSubgroupProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSubgroupProperties((VkPhysicalDeviceSubgroupProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_POINT_CLIPPING_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDevicePointClippingProperties((VkPhysicalDevicePointClippingProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevicePointClippingProperties((VkPhysicalDevicePointClippingProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceProtectedMemoryProperties((VkPhysicalDeviceProtectedMemoryProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceProtectedMemoryProperties((VkPhysicalDeviceProtectedMemoryProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_FILTER_MINMAX_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSamplerFilterMinmaxProperties((VkPhysicalDeviceSamplerFilterMinmaxProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSamplerFilterMinmaxProperties((VkPhysicalDeviceSamplerFilterMinmaxProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLE_LOCATIONS_PROPERTIES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSampleLocationsPropertiesEXT((VkPhysicalDeviceSampleLocationsPropertiesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSampleLocationsPropertiesEXT((VkPhysicalDeviceSampleLocationsPropertiesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceInlineUniformBlockProperties((VkPhysicalDeviceInlineUniformBlockProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceInlineUniformBlockProperties((VkPhysicalDeviceInlineUniformBlockProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_3_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMaintenance3Properties((VkPhysicalDeviceMaintenance3Properties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMaintenance3Properties((VkPhysicalDeviceMaintenance3Properties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMaintenance4Properties((VkPhysicalDeviceMaintenance4Properties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMaintenance4Properties((VkPhysicalDeviceMaintenance4Properties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FLOAT_CONTROLS_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceFloatControlsProperties((VkPhysicalDeviceFloatControlsProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceFloatControlsProperties((VkPhysicalDeviceFloatControlsProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONSERVATIVE_RASTERIZATION_PROPERTIES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceConservativeRasterizationPropertiesEXT((VkPhysicalDeviceConservativeRasterizationPropertiesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceConservativeRasterizationPropertiesEXT((VkPhysicalDeviceConservativeRasterizationPropertiesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceDescriptorIndexingProperties((VkPhysicalDeviceDescriptorIndexingProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceDescriptorIndexingProperties((VkPhysicalDeviceDescriptorIndexingProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceTimelineSemaphoreProperties((VkPhysicalDeviceTimelineSemaphoreProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceTimelineSemaphoreProperties((VkPhysicalDeviceTimelineSemaphoreProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_PROPERTIES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT((VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT((VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_PROPERTIES_KHR: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR((VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR((VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_STENCIL_RESOLVE_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceDepthStencilResolveProperties((VkPhysicalDeviceDepthStencilResolveProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceDepthStencilResolveProperties((VkPhysicalDeviceDepthStencilResolveProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_PROPERTIES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceTransformFeedbackPropertiesEXT((VkPhysicalDeviceTransformFeedbackPropertiesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceTransformFeedbackPropertiesEXT((VkPhysicalDeviceTransformFeedbackPropertiesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceTexelBufferAlignmentProperties((VkPhysicalDeviceTexelBufferAlignmentProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceTexelBufferAlignmentProperties((VkPhysicalDeviceTexelBufferAlignmentProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSubgroupSizeControlProperties((VkPhysicalDeviceSubgroupSizeControlProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSubgroupSizeControlProperties((VkPhysicalDeviceSubgroupSizeControlProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVulkan11Properties((VkPhysicalDeviceVulkan11Properties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVulkan11Properties((VkPhysicalDeviceVulkan11Properties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVulkan12Properties((VkPhysicalDeviceVulkan12Properties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVulkan12Properties((VkPhysicalDeviceVulkan12Properties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVulkan13Properties((VkPhysicalDeviceVulkan13Properties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVulkan13Properties((VkPhysicalDeviceVulkan13Properties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_PROPERTIES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceCustomBorderColorPropertiesEXT((VkPhysicalDeviceCustomBorderColorPropertiesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceCustomBorderColorPropertiesEXT((VkPhysicalDeviceCustomBorderColorPropertiesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_PROPERTIES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceExtendedDynamicState3PropertiesEXT((VkPhysicalDeviceExtendedDynamicState3PropertiesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceExtendedDynamicState3PropertiesEXT((VkPhysicalDeviceExtendedDynamicState3PropertiesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_PROPERTIES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceRobustness2PropertiesEXT((VkPhysicalDeviceRobustness2PropertiesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceRobustness2PropertiesEXT((VkPhysicalDeviceRobustness2PropertiesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_PROPERTIES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceProvokingVertexPropertiesEXT((VkPhysicalDeviceProvokingVertexPropertiesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceProvokingVertexPropertiesEXT((VkPhysicalDeviceProvokingVertexPropertiesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_PROPERTIES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderIntegerDotProductProperties((VkPhysicalDeviceShaderIntegerDotProductProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderIntegerDotProductProperties((VkPhysicalDeviceShaderIntegerDotProductProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_PROPERTIES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT((VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT((VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_PROPERTIES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMapMemoryPlacedPropertiesEXT((VkPhysicalDeviceMapMemoryPlacedPropertiesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMapMemoryPlacedPropertiesEXT((VkPhysicalDeviceMapMemoryPlacedPropertiesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    vt_serialize_VkPhysicalDeviceProperties(&val->properties, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkPhysicalDeviceProperties(&val->properties);
}

static inline void vt_unserialize_VkPhysicalDeviceProperties2(VkPhysicalDeviceProperties2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PUSH_DESCRIPTOR_PROPERTIES_KHR: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevicePushDescriptorPropertiesKHR* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PUSH_DESCRIPTOR_PROPERTIES_KHR) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevicePushDescriptorPropertiesKHR));
                    vt_unserialize_VkPhysicalDevicePushDescriptorPropertiesKHR(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DRIVER_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceDriverProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DRIVER_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceDriverProperties));
                    vt_unserialize_VkPhysicalDeviceDriverProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ID_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceIDProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ID_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceIDProperties));
                    vt_unserialize_VkPhysicalDeviceIDProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMultiviewProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMultiviewProperties));
                    vt_unserialize_VkPhysicalDeviceMultiviewProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSubgroupProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSubgroupProperties));
                    vt_unserialize_VkPhysicalDeviceSubgroupProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_POINT_CLIPPING_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevicePointClippingProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_POINT_CLIPPING_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevicePointClippingProperties));
                    vt_unserialize_VkPhysicalDevicePointClippingProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceProtectedMemoryProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceProtectedMemoryProperties));
                    vt_unserialize_VkPhysicalDeviceProtectedMemoryProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_FILTER_MINMAX_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSamplerFilterMinmaxProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_FILTER_MINMAX_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSamplerFilterMinmaxProperties));
                    vt_unserialize_VkPhysicalDeviceSamplerFilterMinmaxProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLE_LOCATIONS_PROPERTIES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSampleLocationsPropertiesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLE_LOCATIONS_PROPERTIES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSampleLocationsPropertiesEXT));
                    vt_unserialize_VkPhysicalDeviceSampleLocationsPropertiesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceInlineUniformBlockProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceInlineUniformBlockProperties));
                    vt_unserialize_VkPhysicalDeviceInlineUniformBlockProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_3_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMaintenance3Properties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_3_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMaintenance3Properties));
                    vt_unserialize_VkPhysicalDeviceMaintenance3Properties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMaintenance4Properties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMaintenance4Properties));
                    vt_unserialize_VkPhysicalDeviceMaintenance4Properties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FLOAT_CONTROLS_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceFloatControlsProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FLOAT_CONTROLS_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceFloatControlsProperties));
                    vt_unserialize_VkPhysicalDeviceFloatControlsProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONSERVATIVE_RASTERIZATION_PROPERTIES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceConservativeRasterizationPropertiesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONSERVATIVE_RASTERIZATION_PROPERTIES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceConservativeRasterizationPropertiesEXT));
                    vt_unserialize_VkPhysicalDeviceConservativeRasterizationPropertiesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceDescriptorIndexingProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceDescriptorIndexingProperties));
                    vt_unserialize_VkPhysicalDeviceDescriptorIndexingProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceTimelineSemaphoreProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceTimelineSemaphoreProperties));
                    vt_unserialize_VkPhysicalDeviceTimelineSemaphoreProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_PROPERTIES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_PROPERTIES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT));
                    vt_unserialize_VkPhysicalDeviceVertexAttributeDivisorPropertiesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_PROPERTIES_KHR: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_PROPERTIES_KHR) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR));
                    vt_unserialize_VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_STENCIL_RESOLVE_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceDepthStencilResolveProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_STENCIL_RESOLVE_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceDepthStencilResolveProperties));
                    vt_unserialize_VkPhysicalDeviceDepthStencilResolveProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_PROPERTIES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceTransformFeedbackPropertiesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_PROPERTIES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceTransformFeedbackPropertiesEXT));
                    vt_unserialize_VkPhysicalDeviceTransformFeedbackPropertiesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceTexelBufferAlignmentProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceTexelBufferAlignmentProperties));
                    vt_unserialize_VkPhysicalDeviceTexelBufferAlignmentProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSubgroupSizeControlProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSubgroupSizeControlProperties));
                    vt_unserialize_VkPhysicalDeviceSubgroupSizeControlProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVulkan11Properties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVulkan11Properties));
                    vt_unserialize_VkPhysicalDeviceVulkan11Properties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVulkan12Properties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVulkan12Properties));
                    vt_unserialize_VkPhysicalDeviceVulkan12Properties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVulkan13Properties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVulkan13Properties));
                    vt_unserialize_VkPhysicalDeviceVulkan13Properties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_PROPERTIES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceCustomBorderColorPropertiesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_PROPERTIES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceCustomBorderColorPropertiesEXT));
                    vt_unserialize_VkPhysicalDeviceCustomBorderColorPropertiesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_PROPERTIES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceExtendedDynamicState3PropertiesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_PROPERTIES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceExtendedDynamicState3PropertiesEXT));
                    vt_unserialize_VkPhysicalDeviceExtendedDynamicState3PropertiesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_PROPERTIES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceRobustness2PropertiesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_PROPERTIES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceRobustness2PropertiesEXT));
                    vt_unserialize_VkPhysicalDeviceRobustness2PropertiesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_PROPERTIES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceProvokingVertexPropertiesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_PROPERTIES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceProvokingVertexPropertiesEXT));
                    vt_unserialize_VkPhysicalDeviceProvokingVertexPropertiesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderIntegerDotProductProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderIntegerDotProductProperties));
                    vt_unserialize_VkPhysicalDeviceShaderIntegerDotProductProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_PROPERTIES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_PROPERTIES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT));
                    vt_unserialize_VkPhysicalDeviceShaderModuleIdentifierPropertiesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_PROPERTIES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMapMemoryPlacedPropertiesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_PROPERTIES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMapMemoryPlacedPropertiesEXT));
                    vt_unserialize_VkPhysicalDeviceMapMemoryPlacedPropertiesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    vt_unserialize_VkPhysicalDeviceProperties(&val->properties, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkPhysicalDeviceProperties(&val->properties);
}

static inline int vt_sizeof_VkFormatProperties2(VkFormatProperties2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_FORMAT_PROPERTIES_3: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkFormatProperties3((VkFormatProperties3*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkFormatProperties(&val->formatProperties);
    return bufferSize;
}

static inline void vt_serialize_VkFormatProperties2(VkFormatProperties2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_FORMAT_PROPERTIES_3: {
                    int itemSize = vt_sizeof_VkFormatProperties3((VkFormatProperties3*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkFormatProperties3((VkFormatProperties3*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    vt_serialize_VkFormatProperties(&val->formatProperties, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkFormatProperties(&val->formatProperties);
}

static inline void vt_unserialize_VkFormatProperties2(VkFormatProperties2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_FORMAT_PROPERTIES_3: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkFormatProperties3* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_FORMAT_PROPERTIES_3) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkFormatProperties3));
                    vt_unserialize_VkFormatProperties3(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    vt_unserialize_VkFormatProperties(&val->formatProperties, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkFormatProperties(&val->formatProperties);
}

static inline int vt_sizeof_VkImageFormatProperties2(VkImageFormatProperties2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_EXTERNAL_IMAGE_FORMAT_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkExternalImageFormatProperties((VkExternalImageFormatProperties*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_IMAGE_FORMAT_PROPERTIES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkSamplerYcbcrConversionImageFormatProperties((VkSamplerYcbcrConversionImageFormatProperties*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkImageFormatProperties(&val->imageFormatProperties);
    return bufferSize;
}

static inline void vt_serialize_VkImageFormatProperties2(VkImageFormatProperties2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_EXTERNAL_IMAGE_FORMAT_PROPERTIES: {
                    int itemSize = vt_sizeof_VkExternalImageFormatProperties((VkExternalImageFormatProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkExternalImageFormatProperties((VkExternalImageFormatProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_IMAGE_FORMAT_PROPERTIES: {
                    int itemSize = vt_sizeof_VkSamplerYcbcrConversionImageFormatProperties((VkSamplerYcbcrConversionImageFormatProperties*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkSamplerYcbcrConversionImageFormatProperties((VkSamplerYcbcrConversionImageFormatProperties*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    vt_serialize_VkImageFormatProperties(&val->imageFormatProperties, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageFormatProperties(&val->imageFormatProperties);
}

static inline void vt_unserialize_VkImageFormatProperties2(VkImageFormatProperties2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_EXTERNAL_IMAGE_FORMAT_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkExternalImageFormatProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_EXTERNAL_IMAGE_FORMAT_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkExternalImageFormatProperties));
                    vt_unserialize_VkExternalImageFormatProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_IMAGE_FORMAT_PROPERTIES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkSamplerYcbcrConversionImageFormatProperties* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_IMAGE_FORMAT_PROPERTIES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkSamplerYcbcrConversionImageFormatProperties));
                    vt_unserialize_VkSamplerYcbcrConversionImageFormatProperties(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    vt_unserialize_VkImageFormatProperties(&val->imageFormatProperties, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageFormatProperties(&val->imageFormatProperties);
}

static inline int vt_sizeof_VkPhysicalDeviceImageFormatInfo2(VkPhysicalDeviceImageFormatInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTERNAL_IMAGE_FORMAT_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceExternalImageFormatInfo((VkPhysicalDeviceExternalImageFormatInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkImageFormatListCreateInfo((VkImageFormatListCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_STENCIL_USAGE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkImageStencilUsageCreateInfo((VkImageStencilUsageCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // format
    bufferSize += 4; // type
    bufferSize += 4; // tiling
    bufferSize += 4; // usage
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceImageFormatInfo2(VkPhysicalDeviceImageFormatInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTERNAL_IMAGE_FORMAT_INFO: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceExternalImageFormatInfo((VkPhysicalDeviceExternalImageFormatInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceExternalImageFormatInfo((VkPhysicalDeviceExternalImageFormatInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkImageFormatListCreateInfo((VkImageFormatListCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkImageFormatListCreateInfo((VkImageFormatListCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_IMAGE_STENCIL_USAGE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkImageStencilUsageCreateInfo((VkImageStencilUsageCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkImageStencilUsageCreateInfo((VkImageStencilUsageCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->format;
    bufferOffset += 4;
    *(VkImageType*)(outputBuffer + bufferOffset) = val->type;
    bufferOffset += 4;
    *(VkImageTiling*)(outputBuffer + bufferOffset) = val->tiling;
    bufferOffset += 4;
    *(VkImageUsageFlags*)(outputBuffer + bufferOffset) = val->usage;
    bufferOffset += 4;
    *(VkImageCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceImageFormatInfo2(VkPhysicalDeviceImageFormatInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTERNAL_IMAGE_FORMAT_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceExternalImageFormatInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTERNAL_IMAGE_FORMAT_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceExternalImageFormatInfo));
                    vt_unserialize_VkPhysicalDeviceExternalImageFormatInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkImageFormatListCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkImageFormatListCreateInfo));
                    vt_unserialize_VkImageFormatListCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_STENCIL_USAGE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkImageStencilUsageCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_IMAGE_STENCIL_USAGE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkImageStencilUsageCreateInfo));
                    vt_unserialize_VkImageStencilUsageCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->type = *(VkImageType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->tiling = *(VkImageTiling*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->usage = *(VkImageUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->flags = *(VkImageCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkQueueFamilyProperties2(VkQueueFamilyProperties2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkQueueFamilyProperties(&val->queueFamilyProperties);
    return bufferSize;
}

static inline void vt_serialize_VkQueueFamilyProperties2(VkQueueFamilyProperties2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    vt_serialize_VkQueueFamilyProperties(&val->queueFamilyProperties, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkQueueFamilyProperties(&val->queueFamilyProperties);
}

static inline void vt_unserialize_VkQueueFamilyProperties2(VkQueueFamilyProperties2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    vt_unserialize_VkQueueFamilyProperties(&val->queueFamilyProperties, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkQueueFamilyProperties(&val->queueFamilyProperties);
}

static inline int vt_sizeof_VkPhysicalDeviceMemoryProperties2(VkPhysicalDeviceMemoryProperties2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MEMORY_BUDGET_PROPERTIES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMemoryBudgetPropertiesEXT((VkPhysicalDeviceMemoryBudgetPropertiesEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkPhysicalDeviceMemoryProperties(&val->memoryProperties);
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceMemoryProperties2(VkPhysicalDeviceMemoryProperties2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MEMORY_BUDGET_PROPERTIES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMemoryBudgetPropertiesEXT((VkPhysicalDeviceMemoryBudgetPropertiesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMemoryBudgetPropertiesEXT((VkPhysicalDeviceMemoryBudgetPropertiesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    vt_serialize_VkPhysicalDeviceMemoryProperties(&val->memoryProperties, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkPhysicalDeviceMemoryProperties(&val->memoryProperties);
}

static inline void vt_unserialize_VkPhysicalDeviceMemoryProperties2(VkPhysicalDeviceMemoryProperties2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MEMORY_BUDGET_PROPERTIES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMemoryBudgetPropertiesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MEMORY_BUDGET_PROPERTIES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMemoryBudgetPropertiesEXT));
                    vt_unserialize_VkPhysicalDeviceMemoryBudgetPropertiesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    vt_unserialize_VkPhysicalDeviceMemoryProperties(&val->memoryProperties, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkPhysicalDeviceMemoryProperties(&val->memoryProperties);
}

static inline int vt_sizeof_VkSparseImageFormatProperties2(VkSparseImageFormatProperties2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkSparseImageFormatProperties(&val->properties);
    return bufferSize;
}

static inline void vt_serialize_VkSparseImageFormatProperties2(VkSparseImageFormatProperties2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    vt_serialize_VkSparseImageFormatProperties(&val->properties, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkSparseImageFormatProperties(&val->properties);
}

static inline void vt_unserialize_VkSparseImageFormatProperties2(VkSparseImageFormatProperties2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    vt_unserialize_VkSparseImageFormatProperties(&val->properties, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkSparseImageFormatProperties(&val->properties);
}

static inline int vt_sizeof_VkPhysicalDeviceSparseImageFormatInfo2(VkPhysicalDeviceSparseImageFormatInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // format
    bufferSize += 4; // type
    bufferSize += 4; // samples
    bufferSize += 4; // usage
    bufferSize += 4; // tiling
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceSparseImageFormatInfo2(VkPhysicalDeviceSparseImageFormatInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->format;
    bufferOffset += 4;
    *(VkImageType*)(outputBuffer + bufferOffset) = val->type;
    bufferOffset += 4;
    *(VkSampleCountFlagBits*)(outputBuffer + bufferOffset) = val->samples;
    bufferOffset += 4;
    *(VkImageUsageFlags*)(outputBuffer + bufferOffset) = val->usage;
    bufferOffset += 4;
    *(VkImageTiling*)(outputBuffer + bufferOffset) = val->tiling;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceSparseImageFormatInfo2(VkPhysicalDeviceSparseImageFormatInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->type = *(VkImageType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->samples = *(VkSampleCountFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->usage = *(VkImageUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->tiling = *(VkImageTiling*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkExternalMemoryImageCreateInfo(VkExternalMemoryImageCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // handleTypes
    return bufferSize;
}

static inline void vt_serialize_VkExternalMemoryImageCreateInfo(VkExternalMemoryImageCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkExternalMemoryHandleTypeFlags*)(outputBuffer + bufferOffset) = val->handleTypes;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkExternalMemoryImageCreateInfo(VkExternalMemoryImageCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->handleTypes = *(VkExternalMemoryHandleTypeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkExternalMemoryBufferCreateInfo(VkExternalMemoryBufferCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // handleTypes
    return bufferSize;
}

static inline void vt_serialize_VkExternalMemoryBufferCreateInfo(VkExternalMemoryBufferCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkExternalMemoryHandleTypeFlags*)(outputBuffer + bufferOffset) = val->handleTypes;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkExternalMemoryBufferCreateInfo(VkExternalMemoryBufferCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->handleTypes = *(VkExternalMemoryHandleTypeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkExportMemoryAllocateInfo(VkExportMemoryAllocateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // handleTypes
    return bufferSize;
}

static inline void vt_serialize_VkExportMemoryAllocateInfo(VkExportMemoryAllocateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkExternalMemoryHandleTypeFlags*)(outputBuffer + bufferOffset) = val->handleTypes;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkExportMemoryAllocateInfo(VkExportMemoryAllocateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->handleTypes = *(VkExternalMemoryHandleTypeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkImportMemoryFdInfoKHR(VkImportMemoryFdInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // handleType
    bufferSize += 4; // fd
    return bufferSize;
}

static inline void vt_serialize_VkImportMemoryFdInfoKHR(VkImportMemoryFdInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkExternalMemoryHandleTypeFlagBits*)(outputBuffer + bufferOffset) = val->handleType;
    bufferOffset += 4;
    *(int*)(outputBuffer + bufferOffset) = val->fd;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkImportMemoryFdInfoKHR(VkImportMemoryFdInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->handleType = *(VkExternalMemoryHandleTypeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->fd = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkMemoryFdPropertiesKHR(VkMemoryFdPropertiesKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // memoryTypeBits
    return bufferSize;
}

static inline void vt_serialize_VkMemoryFdPropertiesKHR(VkMemoryFdPropertiesKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->memoryTypeBits;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkMemoryFdPropertiesKHR(VkMemoryFdPropertiesKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->memoryTypeBits = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkMemoryGetFdInfoKHR(VkMemoryGetFdInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkDeviceMemory(val->memory);
    bufferSize += 4; // handleType
    return bufferSize;
}

static inline void vt_serialize_VkMemoryGetFdInfoKHR(VkMemoryGetFdInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkDeviceMemory(val->memory, outputBuffer + bufferOffset);
#else
    VkObject* memoryObject = VkObject_fromHandle(val->memory);
    vt_serialize_VkDeviceMemory((VkDeviceMemory)&memoryObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    *(VkExternalMemoryHandleTypeFlagBits*)(outputBuffer + bufferOffset) = val->handleType;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkMemoryGetFdInfoKHR(VkMemoryGetFdInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t memoryId;
    vt_unserialize_VkDeviceMemory((VkDeviceMemory)&memoryId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->memory = vortekSerializerCastVkObject ? ((ResourceMemory*)VkObject_fromId(memoryId))->memory : VkObject_fromId(memoryId);
#else
    VkObject* memoryObject = VkObject_create(VK_OBJECT_TYPE_DEVICE_MEMORY, memoryId);
    val->memory = VkObject_toHandle(memoryObject);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    val->handleType = *(VkExternalMemoryHandleTypeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkExportSemaphoreCreateInfo(VkExportSemaphoreCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // handleTypes
    return bufferSize;
}

static inline void vt_serialize_VkExportSemaphoreCreateInfo(VkExportSemaphoreCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkExternalSemaphoreHandleTypeFlags*)(outputBuffer + bufferOffset) = val->handleTypes;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkExportSemaphoreCreateInfo(VkExportSemaphoreCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->handleTypes = *(VkExternalSemaphoreHandleTypeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkImportSemaphoreFdInfoKHR(VkImportSemaphoreFdInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkSemaphore(val->semaphore);
    bufferSize += 4; // flags
    bufferSize += 4; // handleType
    bufferSize += 4; // fd
    return bufferSize;
}

static inline void vt_serialize_VkImportSemaphoreFdInfoKHR(VkImportSemaphoreFdInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkSemaphore(val->semaphore, outputBuffer + bufferOffset);
#else
    VkObject* semaphoreObject = VkObject_fromHandle(val->semaphore);
    vt_serialize_VkSemaphore((VkSemaphore)&semaphoreObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkSemaphore(val->semaphore);
    *(VkSemaphoreImportFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkExternalSemaphoreHandleTypeFlagBits*)(outputBuffer + bufferOffset) = val->handleType;
    bufferOffset += 4;
    *(int*)(outputBuffer + bufferOffset) = val->fd;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkImportSemaphoreFdInfoKHR(VkImportSemaphoreFdInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t semaphoreId;
    vt_unserialize_VkSemaphore((VkSemaphore)&semaphoreId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->semaphore = VkObject_fromId(semaphoreId);
#else
    VkObject* semaphoreObject = VkObject_create(VK_OBJECT_TYPE_SEMAPHORE, semaphoreId);
    val->semaphore = VkObject_toHandle(semaphoreObject);
#endif
    bufferOffset += vt_sizeof_VkSemaphore(val->semaphore);
    val->flags = *(VkSemaphoreImportFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->handleType = *(VkExternalSemaphoreHandleTypeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->fd = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSemaphoreGetFdInfoKHR(VkSemaphoreGetFdInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkSemaphore(val->semaphore);
    bufferSize += 4; // handleType
    return bufferSize;
}

static inline void vt_serialize_VkSemaphoreGetFdInfoKHR(VkSemaphoreGetFdInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkSemaphore(val->semaphore, outputBuffer + bufferOffset);
#else
    VkObject* semaphoreObject = VkObject_fromHandle(val->semaphore);
    vt_serialize_VkSemaphore((VkSemaphore)&semaphoreObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkSemaphore(val->semaphore);
    *(VkExternalSemaphoreHandleTypeFlagBits*)(outputBuffer + bufferOffset) = val->handleType;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSemaphoreGetFdInfoKHR(VkSemaphoreGetFdInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t semaphoreId;
    vt_unserialize_VkSemaphore((VkSemaphore)&semaphoreId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->semaphore = VkObject_fromId(semaphoreId);
#else
    VkObject* semaphoreObject = VkObject_create(VK_OBJECT_TYPE_SEMAPHORE, semaphoreId);
    val->semaphore = VkObject_toHandle(semaphoreObject);
#endif
    bufferOffset += vt_sizeof_VkSemaphore(val->semaphore);
    val->handleType = *(VkExternalSemaphoreHandleTypeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkConditionalRenderingBeginInfoEXT(VkConditionalRenderingBeginInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkBuffer(val->buffer);
    bufferSize += 8; // offset
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_VkConditionalRenderingBeginInfoEXT(VkConditionalRenderingBeginInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->buffer, outputBuffer + bufferOffset);
#else
    VkObject* bufferObject = VkObject_fromHandle(val->buffer);
    vt_serialize_VkBuffer((VkBuffer)&bufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->offset;
    bufferOffset += 8;
    *(VkConditionalRenderingFlagsEXT*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkConditionalRenderingBeginInfoEXT(VkConditionalRenderingBeginInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t bufferId;
    vt_unserialize_VkBuffer((VkBuffer)&bufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->buffer = VkObject_fromId(bufferId);
#else
    VkObject* bufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, bufferId);
    val->buffer = VkObject_toHandle(bufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    val->offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->flags = *(VkConditionalRenderingFlagsEXT*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDescriptorUpdateTemplateEntry(VkDescriptorUpdateTemplateEntry* val) {
    int bufferSize = 0;
    bufferSize += 4; // dstBinding
    bufferSize += 4; // dstArrayElement
    bufferSize += 4; // descriptorCount
    bufferSize += 4; // descriptorType
    bufferSize += 8; // offset
    bufferSize += 8; // stride
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorUpdateTemplateEntry(VkDescriptorUpdateTemplateEntry* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstBinding;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstArrayElement;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->descriptorCount;
    bufferOffset += 4;
    *(VkDescriptorType*)(outputBuffer + bufferOffset) = val->descriptorType;
    bufferOffset += 4;
    *(size_t*)(outputBuffer + bufferOffset) = val->offset;
    bufferOffset += 8;
    *(size_t*)(outputBuffer + bufferOffset) = val->stride;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkDescriptorUpdateTemplateEntry(VkDescriptorUpdateTemplateEntry* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->dstBinding = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstArrayElement = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorType = *(VkDescriptorType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->offset = *(size_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->stride = *(size_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkDescriptorUpdateTemplateCreateInfo(VkDescriptorUpdateTemplateCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // descriptorUpdateEntryCount
    bufferSize += 4;
    if (val->pDescriptorUpdateEntries) {
        int arrSize = val->descriptorUpdateEntryCount ? val->descriptorUpdateEntryCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkDescriptorUpdateTemplateEntry(&val->pDescriptorUpdateEntries[i]);
        }
    }
    bufferSize += 4; // templateType
    bufferSize += vt_sizeof_VkDescriptorSetLayout(val->descriptorSetLayout);
    bufferSize += 4; // pipelineBindPoint
    bufferSize += vt_sizeof_VkPipelineLayout(val->pipelineLayout);
    bufferSize += 4; // set
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorUpdateTemplateCreateInfo(VkDescriptorUpdateTemplateCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDescriptorUpdateTemplateCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->descriptorUpdateEntryCount;
    bufferOffset += 4;

    if (val->pDescriptorUpdateEntries) {
        int arrSize = val->descriptorUpdateEntryCount ? val->descriptorUpdateEntryCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkDescriptorUpdateTemplateEntry(&val->pDescriptorUpdateEntries[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkDescriptorUpdateTemplateEntry(&val->pDescriptorUpdateEntries[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkDescriptorUpdateTemplateType*)(outputBuffer + bufferOffset) = val->templateType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkDescriptorSetLayout(val->descriptorSetLayout, outputBuffer + bufferOffset);
#else
    VkObject* descriptorSetLayoutObject = VkObject_fromHandle(val->descriptorSetLayout);
    vt_serialize_VkDescriptorSetLayout((VkDescriptorSetLayout)&descriptorSetLayoutObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDescriptorSetLayout(val->descriptorSetLayout);
    *(VkPipelineBindPoint*)(outputBuffer + bufferOffset) = val->pipelineBindPoint;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkPipelineLayout(val->pipelineLayout, outputBuffer + bufferOffset);
#else
    VkObject* pipelineLayoutObject = VkObject_fromHandle(val->pipelineLayout);
    vt_serialize_VkPipelineLayout((VkPipelineLayout)&pipelineLayoutObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkPipelineLayout(val->pipelineLayout);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->set;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDescriptorUpdateTemplateCreateInfo(VkDescriptorUpdateTemplateCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkDescriptorUpdateTemplateCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorUpdateEntryCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkDescriptorUpdateTemplateEntry* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkDescriptorUpdateTemplateEntry));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkDescriptorUpdateTemplateEntry(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pDescriptorUpdateEntries = arrValues;
    }
    else val->pDescriptorUpdateEntries = NULL;
    val->templateType = *(VkDescriptorUpdateTemplateType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t descriptorSetLayoutId;
    vt_unserialize_VkDescriptorSetLayout((VkDescriptorSetLayout)&descriptorSetLayoutId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->descriptorSetLayout = VkObject_fromId(descriptorSetLayoutId);
#else
    VkObject* descriptorSetLayoutObject = VkObject_create(VK_OBJECT_TYPE_DESCRIPTOR_SET_LAYOUT, descriptorSetLayoutId);
    val->descriptorSetLayout = VkObject_toHandle(descriptorSetLayoutObject);
#endif
    bufferOffset += vt_sizeof_VkDescriptorSetLayout(val->descriptorSetLayout);
    val->pipelineBindPoint = *(VkPipelineBindPoint*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t pipelineLayoutId;
    vt_unserialize_VkPipelineLayout((VkPipelineLayout)&pipelineLayoutId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->pipelineLayout = VkObject_fromId(pipelineLayoutId);
#else
    VkObject* pipelineLayoutObject = VkObject_create(VK_OBJECT_TYPE_PIPELINE_LAYOUT, pipelineLayoutId);
    val->pipelineLayout = VkObject_toHandle(pipelineLayoutObject);
#endif
    bufferOffset += vt_sizeof_VkPipelineLayout(val->pipelineLayout);
    val->set = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkFramebufferAttachmentsCreateInfo(VkFramebufferAttachmentsCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // attachmentImageInfoCount
    bufferSize += 4;
    if (val->pAttachmentImageInfos) {
        int arrSize = val->attachmentImageInfoCount ? val->attachmentImageInfoCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkFramebufferAttachmentImageInfo(&val->pAttachmentImageInfos[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkFramebufferAttachmentsCreateInfo(VkFramebufferAttachmentsCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->attachmentImageInfoCount;
    bufferOffset += 4;

    if (val->pAttachmentImageInfos) {
        int arrSize = val->attachmentImageInfoCount ? val->attachmentImageInfoCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkFramebufferAttachmentImageInfo(&val->pAttachmentImageInfos[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkFramebufferAttachmentImageInfo(&val->pAttachmentImageInfos[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkFramebufferAttachmentsCreateInfo(VkFramebufferAttachmentsCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->attachmentImageInfoCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkFramebufferAttachmentImageInfo* arrValues = vt_alloc(memoryPool, dataSize4 * sizeof(VkFramebufferAttachmentImageInfo));
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkFramebufferAttachmentImageInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pAttachmentImageInfos = arrValues;
    }
    else val->pAttachmentImageInfos = NULL;
}

static inline int vt_sizeof_VkRenderPassAttachmentBeginInfo(VkRenderPassAttachmentBeginInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    if (val->pAttachments) {
        for (int i = 0; i < val->attachmentCount; i++) {
            bufferSize += vt_sizeof_VkImageView(val->pAttachments[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkRenderPassAttachmentBeginInfo(VkRenderPassAttachmentBeginInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->attachmentCount;
    bufferOffset += 4;

    if (val->pAttachments) {
        *(int*)(outputBuffer + bufferOffset) = val->attachmentCount;
        bufferOffset += 4;
        for (int i = 0; i < val->attachmentCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkImageView(val->pAttachments[i], outputBuffer + bufferOffset);
#else
            VkObject* attachmentsObject = VkObject_fromHandle(val->pAttachments[i]);
            vt_serialize_VkImageView((VkImageView)&attachmentsObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkImageView(val->pAttachments[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkRenderPassAttachmentBeginInfo(VkRenderPassAttachmentBeginInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkImageView* arrValues = vt_alloc(memoryPool, dataSize4 * sizeof(VkImageView));
        for (int i = 0; i < dataSize4; i++) {
            uint64_t attachmentsId;
            vt_unserialize_VkImageView((VkImageView)&attachmentsId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(attachmentsId);
#else
                VkObject* attachmentsObject = VkObject_create(VK_OBJECT_TYPE_IMAGE_VIEW, attachmentsId);
                arrValues[i] = VkObject_toHandle(attachmentsObject);
#endif
                bufferOffset += vt_sizeof_VkImageView(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkImageView(VK_NULL_HANDLE);
        }
        val->pAttachments = arrValues;
    }
    else val->pAttachments = NULL;
}

static inline int vt_sizeof_VkRenderPassCreateInfo2(VkRenderPassCreateInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    if (val->pAttachments) {
        int arrSize = val->attachmentCount ? val->attachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkAttachmentDescription2(&val->pAttachments[i]);
        }
    }
    bufferSize += 4; // subpassCount
    bufferSize += 4;
    if (val->pSubpasses) {
        int arrSize = val->subpassCount ? val->subpassCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSubpassDescription2(&val->pSubpasses[i]);
        }
    }
    bufferSize += 4; // dependencyCount
    bufferSize += 4;
    if (val->pDependencies) {
        int arrSize = val->dependencyCount ? val->dependencyCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSubpassDependency2(&val->pDependencies[i]);
        }
    }
    bufferSize += 4; // correlatedViewMaskCount
    bufferSize += 4;
    bufferSize += val->pCorrelatedViewMasks ? val->correlatedViewMaskCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkRenderPassCreateInfo2(VkRenderPassCreateInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkRenderPassCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->attachmentCount;
    bufferOffset += 4;

    if (val->pAttachments) {
        int arrSize = val->attachmentCount ? val->attachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkAttachmentDescription2(&val->pAttachments[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkAttachmentDescription2(&val->pAttachments[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subpassCount;
    bufferOffset += 4;

    if (val->pSubpasses) {
        int arrSize = val->subpassCount ? val->subpassCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSubpassDescription2(&val->pSubpasses[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSubpassDescription2(&val->pSubpasses[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dependencyCount;
    bufferOffset += 4;

    if (val->pDependencies) {
        int arrSize = val->dependencyCount ? val->dependencyCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSubpassDependency2(&val->pDependencies[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSubpassDependency2(&val->pDependencies[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->correlatedViewMaskCount;
    bufferOffset += 4;

    if (val->pCorrelatedViewMasks) {
        int arrSize = val->correlatedViewMaskCount ? val->correlatedViewMaskCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pCorrelatedViewMasks, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkRenderPassCreateInfo2(VkRenderPassCreateInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkRenderPassCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkAttachmentDescription2* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkAttachmentDescription2));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkAttachmentDescription2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pAttachments = arrValues;
    }
    else val->pAttachments = NULL;
    val->subpassCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkSubpassDescription2* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkSubpassDescription2));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSubpassDescription2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pSubpasses = arrValues;
    }
    else val->pSubpasses = NULL;
    val->dependencyCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        VkSubpassDependency2* arrValues = vt_alloc(memoryPool, dataSize9 * sizeof(VkSubpassDependency2));
        for (int i = 0; i < dataSize9; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSubpassDependency2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pDependencies = arrValues;
    }
    else val->pDependencies = NULL;
    val->correlatedViewMaskCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize11 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize11 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize11 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize11 * 4);
        bufferOffset += dataSize11 * 4;
        val->pCorrelatedViewMasks = arrValues;
    }
    else val->pCorrelatedViewMasks = NULL;
}

static inline int vt_sizeof_VkSubpassBeginInfo(VkSubpassBeginInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // contents
    return bufferSize;
}

static inline void vt_serialize_VkSubpassBeginInfo(VkSubpassBeginInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkSubpassContents*)(outputBuffer + bufferOffset) = val->contents;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSubpassBeginInfo(VkSubpassBeginInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->contents = *(VkSubpassContents*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSubpassEndInfo(VkSubpassEndInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    return bufferSize;
}

static inline void vt_serialize_VkSubpassEndInfo(VkSubpassEndInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSubpassEndInfo(VkSubpassEndInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
}

static inline int vt_sizeof_VkExportFenceCreateInfo(VkExportFenceCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // handleTypes
    return bufferSize;
}

static inline void vt_serialize_VkExportFenceCreateInfo(VkExportFenceCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkExternalFenceHandleTypeFlags*)(outputBuffer + bufferOffset) = val->handleTypes;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkExportFenceCreateInfo(VkExportFenceCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->handleTypes = *(VkExternalFenceHandleTypeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkImportFenceFdInfoKHR(VkImportFenceFdInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkFence(val->fence);
    bufferSize += 4; // flags
    bufferSize += 4; // handleType
    bufferSize += 4; // fd
    return bufferSize;
}

static inline void vt_serialize_VkImportFenceFdInfoKHR(VkImportFenceFdInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkFence(val->fence, outputBuffer + bufferOffset);
#else
    VkObject* fenceObject = VkObject_fromHandle(val->fence);
    vt_serialize_VkFence((VkFence)&fenceObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkFence(val->fence);
    *(VkFenceImportFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkExternalFenceHandleTypeFlagBits*)(outputBuffer + bufferOffset) = val->handleType;
    bufferOffset += 4;
    *(int*)(outputBuffer + bufferOffset) = val->fd;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkImportFenceFdInfoKHR(VkImportFenceFdInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t fenceId;
    vt_unserialize_VkFence((VkFence)&fenceId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->fence = VkObject_fromId(fenceId);
#else
    VkObject* fenceObject = VkObject_create(VK_OBJECT_TYPE_FENCE, fenceId);
    val->fence = VkObject_toHandle(fenceObject);
#endif
    bufferOffset += vt_sizeof_VkFence(val->fence);
    val->flags = *(VkFenceImportFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->handleType = *(VkExternalFenceHandleTypeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->fd = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkFenceGetFdInfoKHR(VkFenceGetFdInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkFence(val->fence);
    bufferSize += 4; // handleType
    return bufferSize;
}

static inline void vt_serialize_VkFenceGetFdInfoKHR(VkFenceGetFdInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkFence(val->fence, outputBuffer + bufferOffset);
#else
    VkObject* fenceObject = VkObject_fromHandle(val->fence);
    vt_serialize_VkFence((VkFence)&fenceObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkFence(val->fence);
    *(VkExternalFenceHandleTypeFlagBits*)(outputBuffer + bufferOffset) = val->handleType;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkFenceGetFdInfoKHR(VkFenceGetFdInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t fenceId;
    vt_unserialize_VkFence((VkFence)&fenceId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->fence = VkObject_fromId(fenceId);
#else
    VkObject* fenceObject = VkObject_create(VK_OBJECT_TYPE_FENCE, fenceId);
    val->fence = VkObject_toHandle(fenceObject);
#endif
    bufferOffset += vt_sizeof_VkFence(val->fence);
    val->handleType = *(VkExternalFenceHandleTypeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkRenderPassInputAttachmentAspectCreateInfo(VkRenderPassInputAttachmentAspectCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // aspectReferenceCount
    bufferSize += 4;
    if (val->pAspectReferences) {
        int arrSize = val->aspectReferenceCount ? val->aspectReferenceCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkInputAttachmentAspectReference(&val->pAspectReferences[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkRenderPassInputAttachmentAspectCreateInfo(VkRenderPassInputAttachmentAspectCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->aspectReferenceCount;
    bufferOffset += 4;

    if (val->pAspectReferences) {
        int arrSize = val->aspectReferenceCount ? val->aspectReferenceCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkInputAttachmentAspectReference(&val->pAspectReferences[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkInputAttachmentAspectReference(&val->pAspectReferences[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkRenderPassInputAttachmentAspectCreateInfo(VkRenderPassInputAttachmentAspectCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->aspectReferenceCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkInputAttachmentAspectReference* arrValues = vt_alloc(memoryPool, dataSize4 * sizeof(VkInputAttachmentAspectReference));
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkInputAttachmentAspectReference(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pAspectReferences = arrValues;
    }
    else val->pAspectReferences = NULL;
}

static inline int vt_sizeof_VkImageViewUsageCreateInfo(VkImageViewUsageCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // usage
    return bufferSize;
}

static inline void vt_serialize_VkImageViewUsageCreateInfo(VkImageViewUsageCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkImageUsageFlags*)(outputBuffer + bufferOffset) = val->usage;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkImageViewUsageCreateInfo(VkImageViewUsageCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->usage = *(VkImageUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkMemoryDedicatedRequirements(VkMemoryDedicatedRequirements* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // prefersDedicatedAllocation
    bufferSize += 4; // requiresDedicatedAllocation
    return bufferSize;
}

static inline void vt_serialize_VkMemoryDedicatedRequirements(VkMemoryDedicatedRequirements* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->prefersDedicatedAllocation;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->requiresDedicatedAllocation;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkMemoryDedicatedRequirements(VkMemoryDedicatedRequirements* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->prefersDedicatedAllocation = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->requiresDedicatedAllocation = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkMemoryDedicatedAllocateInfo(VkMemoryDedicatedAllocateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkImage(val->image);
    bufferSize += vt_sizeof_VkBuffer(val->buffer);
    return bufferSize;
}

static inline void vt_serialize_VkMemoryDedicatedAllocateInfo(VkMemoryDedicatedAllocateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->image, outputBuffer + bufferOffset);
#else
    VkObject* imageObject = VkObject_fromHandle(val->image);
    vt_serialize_VkImage((VkImage)&imageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->buffer, outputBuffer + bufferOffset);
#else
    VkObject* bufferObject = VkObject_fromHandle(val->buffer);
    vt_serialize_VkBuffer((VkBuffer)&bufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
}

static inline void vt_unserialize_VkMemoryDedicatedAllocateInfo(VkMemoryDedicatedAllocateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t imageId;
    vt_unserialize_VkImage((VkImage)&imageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->image = VkObject_fromId(imageId);
#else
    VkObject* imageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, imageId);
    val->image = VkObject_toHandle(imageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);

    uint64_t bufferId;
    vt_unserialize_VkBuffer((VkBuffer)&bufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->buffer = VkObject_fromId(bufferId);
#else
    VkObject* bufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, bufferId);
    val->buffer = VkObject_toHandle(bufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
}

static inline int vt_sizeof_VkAttachmentSampleLocationsEXT(VkAttachmentSampleLocationsEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // attachmentIndex
    bufferSize += vt_sizeof_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo);
    return bufferSize;
}

static inline void vt_serialize_VkAttachmentSampleLocationsEXT(VkAttachmentSampleLocationsEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->attachmentIndex;
    bufferOffset += 4;
    vt_serialize_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo);
}

static inline void vt_unserialize_VkAttachmentSampleLocationsEXT(VkAttachmentSampleLocationsEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->attachmentIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo);
}

static inline int vt_sizeof_VkSubpassSampleLocationsEXT(VkSubpassSampleLocationsEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // subpassIndex
    bufferSize += vt_sizeof_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo);
    return bufferSize;
}

static inline void vt_serialize_VkSubpassSampleLocationsEXT(VkSubpassSampleLocationsEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subpassIndex;
    bufferOffset += 4;
    vt_serialize_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo);
}

static inline void vt_unserialize_VkSubpassSampleLocationsEXT(VkSubpassSampleLocationsEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->subpassIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkSampleLocationsInfoEXT(&val->sampleLocationsInfo);
}

static inline int vt_sizeof_VkRenderPassSampleLocationsBeginInfoEXT(VkRenderPassSampleLocationsBeginInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // attachmentInitialSampleLocationsCount
    bufferSize += 4;
    if (val->pAttachmentInitialSampleLocations) {
        int arrSize = val->attachmentInitialSampleLocationsCount ? val->attachmentInitialSampleLocationsCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkAttachmentSampleLocationsEXT(&val->pAttachmentInitialSampleLocations[i]);
        }
    }
    bufferSize += 4; // postSubpassSampleLocationsCount
    bufferSize += 4;
    if (val->pPostSubpassSampleLocations) {
        int arrSize = val->postSubpassSampleLocationsCount ? val->postSubpassSampleLocationsCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSubpassSampleLocationsEXT(&val->pPostSubpassSampleLocations[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkRenderPassSampleLocationsBeginInfoEXT(VkRenderPassSampleLocationsBeginInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->attachmentInitialSampleLocationsCount;
    bufferOffset += 4;

    if (val->pAttachmentInitialSampleLocations) {
        int arrSize = val->attachmentInitialSampleLocationsCount ? val->attachmentInitialSampleLocationsCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkAttachmentSampleLocationsEXT(&val->pAttachmentInitialSampleLocations[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkAttachmentSampleLocationsEXT(&val->pAttachmentInitialSampleLocations[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->postSubpassSampleLocationsCount;
    bufferOffset += 4;

    if (val->pPostSubpassSampleLocations) {
        int arrSize = val->postSubpassSampleLocationsCount ? val->postSubpassSampleLocationsCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSubpassSampleLocationsEXT(&val->pPostSubpassSampleLocations[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSubpassSampleLocationsEXT(&val->pPostSubpassSampleLocations[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkRenderPassSampleLocationsBeginInfoEXT(VkRenderPassSampleLocationsBeginInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->attachmentInitialSampleLocationsCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkAttachmentSampleLocationsEXT* arrValues = vt_alloc(memoryPool, dataSize4 * sizeof(VkAttachmentSampleLocationsEXT));
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkAttachmentSampleLocationsEXT(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pAttachmentInitialSampleLocations = arrValues;
    }
    else val->pAttachmentInitialSampleLocations = NULL;
    val->postSubpassSampleLocationsCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkSubpassSampleLocationsEXT* arrValues = vt_alloc(memoryPool, dataSize6 * sizeof(VkSubpassSampleLocationsEXT));
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSubpassSampleLocationsEXT(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pPostSubpassSampleLocations = arrValues;
    }
    else val->pPostSubpassSampleLocations = NULL;
}

static inline int vt_sizeof_VkMultisamplePropertiesEXT(VkMultisamplePropertiesEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkExtent2D(&val->maxSampleLocationGridSize);
    return bufferSize;
}

static inline void vt_serialize_VkMultisamplePropertiesEXT(VkMultisamplePropertiesEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    vt_serialize_VkExtent2D(&val->maxSampleLocationGridSize, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent2D(&val->maxSampleLocationGridSize);
}

static inline void vt_unserialize_VkMultisamplePropertiesEXT(VkMultisamplePropertiesEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    vt_unserialize_VkExtent2D(&val->maxSampleLocationGridSize, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent2D(&val->maxSampleLocationGridSize);
}

static inline int vt_sizeof_VkBufferMemoryRequirementsInfo2(VkBufferMemoryRequirementsInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkBuffer(val->buffer);
    return bufferSize;
}

static inline void vt_serialize_VkBufferMemoryRequirementsInfo2(VkBufferMemoryRequirementsInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->buffer, outputBuffer + bufferOffset);
#else
    VkObject* bufferObject = VkObject_fromHandle(val->buffer);
    vt_serialize_VkBuffer((VkBuffer)&bufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
}

static inline void vt_unserialize_VkBufferMemoryRequirementsInfo2(VkBufferMemoryRequirementsInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t bufferId;
    vt_unserialize_VkBuffer((VkBuffer)&bufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->buffer = VkObject_fromId(bufferId);
#else
    VkObject* bufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, bufferId);
    val->buffer = VkObject_toHandle(bufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
}

static inline int vt_sizeof_VkImageMemoryRequirementsInfo2(VkImageMemoryRequirementsInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_IMAGE_PLANE_MEMORY_REQUIREMENTS_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkImagePlaneMemoryRequirementsInfo((VkImagePlaneMemoryRequirementsInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkImage(val->image);
    return bufferSize;
}

static inline void vt_serialize_VkImageMemoryRequirementsInfo2(VkImageMemoryRequirementsInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_IMAGE_PLANE_MEMORY_REQUIREMENTS_INFO: {
                    int itemSize = vt_sizeof_VkImagePlaneMemoryRequirementsInfo((VkImagePlaneMemoryRequirementsInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkImagePlaneMemoryRequirementsInfo((VkImagePlaneMemoryRequirementsInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->image, outputBuffer + bufferOffset);
#else
    VkObject* imageObject = VkObject_fromHandle(val->image);
    vt_serialize_VkImage((VkImage)&imageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
}

static inline void vt_unserialize_VkImageMemoryRequirementsInfo2(VkImageMemoryRequirementsInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_IMAGE_PLANE_MEMORY_REQUIREMENTS_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkImagePlaneMemoryRequirementsInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_IMAGE_PLANE_MEMORY_REQUIREMENTS_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkImagePlaneMemoryRequirementsInfo));
                    vt_unserialize_VkImagePlaneMemoryRequirementsInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);

    uint64_t imageId;
    vt_unserialize_VkImage((VkImage)&imageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->image = VkObject_fromId(imageId);
#else
    VkObject* imageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, imageId);
    val->image = VkObject_toHandle(imageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
}

static inline int vt_sizeof_VkImageSparseMemoryRequirementsInfo2(VkImageSparseMemoryRequirementsInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkImage(val->image);
    return bufferSize;
}

static inline void vt_serialize_VkImageSparseMemoryRequirementsInfo2(VkImageSparseMemoryRequirementsInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->image, outputBuffer + bufferOffset);
#else
    VkObject* imageObject = VkObject_fromHandle(val->image);
    vt_serialize_VkImage((VkImage)&imageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
}

static inline void vt_unserialize_VkImageSparseMemoryRequirementsInfo2(VkImageSparseMemoryRequirementsInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t imageId;
    vt_unserialize_VkImage((VkImage)&imageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->image = VkObject_fromId(imageId);
#else
    VkObject* imageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, imageId);
    val->image = VkObject_toHandle(imageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
}

static inline int vt_sizeof_VkMemoryRequirements2(VkMemoryRequirements2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_MEMORY_DEDICATED_REQUIREMENTS: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkMemoryDedicatedRequirements((VkMemoryDedicatedRequirements*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkMemoryRequirements(&val->memoryRequirements);
    return bufferSize;
}

static inline void vt_serialize_VkMemoryRequirements2(VkMemoryRequirements2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_MEMORY_DEDICATED_REQUIREMENTS: {
                    int itemSize = vt_sizeof_VkMemoryDedicatedRequirements((VkMemoryDedicatedRequirements*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkMemoryDedicatedRequirements((VkMemoryDedicatedRequirements*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    vt_serialize_VkMemoryRequirements(&val->memoryRequirements, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkMemoryRequirements(&val->memoryRequirements);
}

static inline void vt_unserialize_VkMemoryRequirements2(VkMemoryRequirements2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_MEMORY_DEDICATED_REQUIREMENTS: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkMemoryDedicatedRequirements* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_MEMORY_DEDICATED_REQUIREMENTS) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkMemoryDedicatedRequirements));
                    vt_unserialize_VkMemoryDedicatedRequirements(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    vt_unserialize_VkMemoryRequirements(&val->memoryRequirements, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkMemoryRequirements(&val->memoryRequirements);
}

static inline int vt_sizeof_VkSparseImageMemoryRequirements2(VkSparseImageMemoryRequirements2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkSparseImageMemoryRequirements(&val->memoryRequirements);
    return bufferSize;
}

static inline void vt_serialize_VkSparseImageMemoryRequirements2(VkSparseImageMemoryRequirements2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    vt_serialize_VkSparseImageMemoryRequirements(&val->memoryRequirements, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkSparseImageMemoryRequirements(&val->memoryRequirements);
}

static inline void vt_unserialize_VkSparseImageMemoryRequirements2(VkSparseImageMemoryRequirements2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    vt_unserialize_VkSparseImageMemoryRequirements(&val->memoryRequirements, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkSparseImageMemoryRequirements(&val->memoryRequirements);
}

static inline int vt_sizeof_VkSamplerYcbcrConversionCreateInfo(VkSamplerYcbcrConversionCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // format
    bufferSize += 4; // ycbcrModel
    bufferSize += 4; // ycbcrRange
    bufferSize += vt_sizeof_VkComponentMapping(&val->components);
    bufferSize += 4; // xChromaOffset
    bufferSize += 4; // yChromaOffset
    bufferSize += 4; // chromaFilter
    bufferSize += 4; // forceExplicitReconstruction
    return bufferSize;
}

static inline void vt_serialize_VkSamplerYcbcrConversionCreateInfo(VkSamplerYcbcrConversionCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->format;
    bufferOffset += 4;
    *(VkSamplerYcbcrModelConversion*)(outputBuffer + bufferOffset) = val->ycbcrModel;
    bufferOffset += 4;
    *(VkSamplerYcbcrRange*)(outputBuffer + bufferOffset) = val->ycbcrRange;
    bufferOffset += 4;
    vt_serialize_VkComponentMapping(&val->components, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkComponentMapping(&val->components);
    *(VkChromaLocation*)(outputBuffer + bufferOffset) = val->xChromaOffset;
    bufferOffset += 4;
    *(VkChromaLocation*)(outputBuffer + bufferOffset) = val->yChromaOffset;
    bufferOffset += 4;
    *(VkFilter*)(outputBuffer + bufferOffset) = val->chromaFilter;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->forceExplicitReconstruction;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSamplerYcbcrConversionCreateInfo(VkSamplerYcbcrConversionCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->ycbcrModel = *(VkSamplerYcbcrModelConversion*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->ycbcrRange = *(VkSamplerYcbcrRange*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkComponentMapping(&val->components, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkComponentMapping(&val->components);
    val->xChromaOffset = *(VkChromaLocation*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->yChromaOffset = *(VkChromaLocation*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->chromaFilter = *(VkFilter*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->forceExplicitReconstruction = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSamplerYcbcrConversionInfo(VkSamplerYcbcrConversionInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkSamplerYcbcrConversion(val->conversion);
    return bufferSize;
}

static inline void vt_serialize_VkSamplerYcbcrConversionInfo(VkSamplerYcbcrConversionInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkSamplerYcbcrConversion(val->conversion, outputBuffer + bufferOffset);
#else
    VkObject* conversionObject = VkObject_fromHandle(val->conversion);
    vt_serialize_VkSamplerYcbcrConversion((VkSamplerYcbcrConversion)&conversionObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkSamplerYcbcrConversion(val->conversion);
}

static inline void vt_unserialize_VkSamplerYcbcrConversionInfo(VkSamplerYcbcrConversionInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t conversionId;
    vt_unserialize_VkSamplerYcbcrConversion((VkSamplerYcbcrConversion)&conversionId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->conversion = VkObject_fromId(conversionId);
#else
    VkObject* conversionObject = VkObject_create(VK_OBJECT_TYPE_SAMPLER_YCBCR_CONVERSION, conversionId);
    val->conversion = VkObject_toHandle(conversionObject);
#endif
    bufferOffset += vt_sizeof_VkSamplerYcbcrConversion(val->conversion);
}

static inline int vt_sizeof_VkBindImagePlaneMemoryInfo(VkBindImagePlaneMemoryInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // planeAspect
    return bufferSize;
}

static inline void vt_serialize_VkBindImagePlaneMemoryInfo(VkBindImagePlaneMemoryInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkImageAspectFlagBits*)(outputBuffer + bufferOffset) = val->planeAspect;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkBindImagePlaneMemoryInfo(VkBindImagePlaneMemoryInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->planeAspect = *(VkImageAspectFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDescriptorSetLayoutBindingFlagsCreateInfo(VkDescriptorSetLayoutBindingFlagsCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // bindingCount
    bufferSize += 4;
    bufferSize += val->pBindingFlags ? val->bindingCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorSetLayoutBindingFlagsCreateInfo(VkDescriptorSetLayoutBindingFlagsCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->bindingCount;
    bufferOffset += 4;

    if (val->pBindingFlags) {
        int arrSize = val->bindingCount ? val->bindingCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pBindingFlags, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDescriptorSetLayoutBindingFlagsCreateInfo(VkDescriptorSetLayoutBindingFlagsCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->bindingCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkDescriptorBindingFlags* arrValues = vt_alloc(memoryPool, dataSize4 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
        val->pBindingFlags = arrValues;
    }
    else val->pBindingFlags = NULL;
}

static inline int vt_sizeof_VkDescriptorSetVariableDescriptorCountAllocateInfo(VkDescriptorSetVariableDescriptorCountAllocateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // descriptorSetCount
    bufferSize += 4;
    bufferSize += val->pDescriptorCounts ? val->descriptorSetCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorSetVariableDescriptorCountAllocateInfo(VkDescriptorSetVariableDescriptorCountAllocateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->descriptorSetCount;
    bufferOffset += 4;

    if (val->pDescriptorCounts) {
        int arrSize = val->descriptorSetCount ? val->descriptorSetCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pDescriptorCounts, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDescriptorSetVariableDescriptorCountAllocateInfo(VkDescriptorSetVariableDescriptorCountAllocateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->descriptorSetCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize4 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
        val->pDescriptorCounts = arrValues;
    }
    else val->pDescriptorCounts = NULL;
}

static inline int vt_sizeof_VkDescriptorSetVariableDescriptorCountLayoutSupport(VkDescriptorSetVariableDescriptorCountLayoutSupport* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maxVariableDescriptorCount
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorSetVariableDescriptorCountLayoutSupport(VkDescriptorSetVariableDescriptorCountLayoutSupport* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxVariableDescriptorCount;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDescriptorSetVariableDescriptorCountLayoutSupport(VkDescriptorSetVariableDescriptorCountLayoutSupport* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxVariableDescriptorCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDescriptorSetLayoutSupport(VkDescriptorSetLayoutSupport* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DESCRIPTOR_SET_VARIABLE_DESCRIPTOR_COUNT_LAYOUT_SUPPORT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDescriptorSetVariableDescriptorCountLayoutSupport((VkDescriptorSetVariableDescriptorCountLayoutSupport*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // supported
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorSetLayoutSupport(VkDescriptorSetLayoutSupport* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DESCRIPTOR_SET_VARIABLE_DESCRIPTOR_COUNT_LAYOUT_SUPPORT: {
                    int itemSize = vt_sizeof_VkDescriptorSetVariableDescriptorCountLayoutSupport((VkDescriptorSetVariableDescriptorCountLayoutSupport*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDescriptorSetVariableDescriptorCountLayoutSupport((VkDescriptorSetVariableDescriptorCountLayoutSupport*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->supported;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDescriptorSetLayoutSupport(VkDescriptorSetLayoutSupport* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DESCRIPTOR_SET_VARIABLE_DESCRIPTOR_COUNT_LAYOUT_SUPPORT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDescriptorSetVariableDescriptorCountLayoutSupport* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DESCRIPTOR_SET_VARIABLE_DESCRIPTOR_COUNT_LAYOUT_SUPPORT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDescriptorSetVariableDescriptorCountLayoutSupport));
                    vt_unserialize_VkDescriptorSetVariableDescriptorCountLayoutSupport(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->supported = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSemaphoreTypeCreateInfo(VkSemaphoreTypeCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // semaphoreType
    bufferSize += 8; // initialValue
    return bufferSize;
}

static inline void vt_serialize_VkSemaphoreTypeCreateInfo(VkSemaphoreTypeCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkSemaphoreType*)(outputBuffer + bufferOffset) = val->semaphoreType;
    bufferOffset += 4;
    *(uint64_t*)(outputBuffer + bufferOffset) = val->initialValue;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkSemaphoreTypeCreateInfo(VkSemaphoreTypeCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->semaphoreType = *(VkSemaphoreType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->initialValue = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkTimelineSemaphoreSubmitInfo(VkTimelineSemaphoreSubmitInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // waitSemaphoreValueCount
    bufferSize += 4;
    bufferSize += val->pWaitSemaphoreValues ? val->waitSemaphoreValueCount * 8 : 0;
    bufferSize += 4; // signalSemaphoreValueCount
    bufferSize += 4;
    bufferSize += val->pSignalSemaphoreValues ? val->signalSemaphoreValueCount * 8 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkTimelineSemaphoreSubmitInfo(VkTimelineSemaphoreSubmitInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->waitSemaphoreValueCount;
    bufferOffset += 4;

    if (val->pWaitSemaphoreValues) {
        int arrSize = val->waitSemaphoreValueCount ? val->waitSemaphoreValueCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pWaitSemaphoreValues, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->signalSemaphoreValueCount;
    bufferOffset += 4;

    if (val->pSignalSemaphoreValues) {
        int arrSize = val->signalSemaphoreValueCount ? val->signalSemaphoreValueCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pSignalSemaphoreValues, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkTimelineSemaphoreSubmitInfo(VkTimelineSemaphoreSubmitInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->waitSemaphoreValueCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        uint64_t* arrValues = vt_alloc(memoryPool, dataSize4 * 8);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 8);
        bufferOffset += dataSize4 * 8;
        val->pWaitSemaphoreValues = arrValues;
    }
    else val->pWaitSemaphoreValues = NULL;
    val->signalSemaphoreValueCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        uint64_t* arrValues = vt_alloc(memoryPool, dataSize6 * 8);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize6 * 8);
        bufferOffset += dataSize6 * 8;
        val->pSignalSemaphoreValues = arrValues;
    }
    else val->pSignalSemaphoreValues = NULL;
}

static inline int vt_sizeof_VkSemaphoreWaitInfo(VkSemaphoreWaitInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // semaphoreCount
    bufferSize += 4;
    if (val->pSemaphores) {
        for (int i = 0; i < val->semaphoreCount; i++) {
            bufferSize += vt_sizeof_VkSemaphore(val->pSemaphores[i]);
        }
    }
    bufferSize += 4;
    bufferSize += val->pValues ? val->semaphoreCount * 8 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkSemaphoreWaitInfo(VkSemaphoreWaitInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkSemaphoreWaitFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->semaphoreCount;
    bufferOffset += 4;

    if (val->pSemaphores) {
        *(int*)(outputBuffer + bufferOffset) = val->semaphoreCount;
        bufferOffset += 4;
        for (int i = 0; i < val->semaphoreCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkSemaphore(val->pSemaphores[i], outputBuffer + bufferOffset);
#else
            VkObject* semaphoresObject = VkObject_fromHandle(val->pSemaphores[i]);
            vt_serialize_VkSemaphore((VkSemaphore)&semaphoresObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkSemaphore(val->pSemaphores[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pValues) {
        int arrSize = val->semaphoreCount ? val->semaphoreCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pValues, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkSemaphoreWaitInfo(VkSemaphoreWaitInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkSemaphoreWaitFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->semaphoreCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkSemaphore* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkSemaphore));
        for (int i = 0; i < dataSize5; i++) {
            uint64_t semaphoresId;
            vt_unserialize_VkSemaphore((VkSemaphore)&semaphoresId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(semaphoresId);
#else
                VkObject* semaphoresObject = VkObject_create(VK_OBJECT_TYPE_SEMAPHORE, semaphoresId);
                arrValues[i] = VkObject_toHandle(semaphoresObject);
#endif
                bufferOffset += vt_sizeof_VkSemaphore(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkSemaphore(VK_NULL_HANDLE);
        }
        val->pSemaphores = arrValues;
    }
    else val->pSemaphores = NULL;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        uint64_t* arrValues = vt_alloc(memoryPool, dataSize6 * 8);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize6 * 8);
        bufferOffset += dataSize6 * 8;
        val->pValues = arrValues;
    }
    else val->pValues = NULL;
}

static inline int vt_sizeof_VkSemaphoreSignalInfo(VkSemaphoreSignalInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkSemaphore(val->semaphore);
    bufferSize += 8; // value
    return bufferSize;
}

static inline void vt_serialize_VkSemaphoreSignalInfo(VkSemaphoreSignalInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkSemaphore(val->semaphore, outputBuffer + bufferOffset);
#else
    VkObject* semaphoreObject = VkObject_fromHandle(val->semaphore);
    vt_serialize_VkSemaphore((VkSemaphore)&semaphoreObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkSemaphore(val->semaphore);
    *(uint64_t*)(outputBuffer + bufferOffset) = val->value;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkSemaphoreSignalInfo(VkSemaphoreSignalInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t semaphoreId;
    vt_unserialize_VkSemaphore((VkSemaphore)&semaphoreId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->semaphore = VkObject_fromId(semaphoreId);
#else
    VkObject* semaphoreObject = VkObject_create(VK_OBJECT_TYPE_SEMAPHORE, semaphoreId);
    val->semaphore = VkObject_toHandle(semaphoreObject);
#endif
    bufferOffset += vt_sizeof_VkSemaphore(val->semaphore);
    val->value = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkSamplerCustomBorderColorCreateInfoEXT(VkSamplerCustomBorderColorCreateInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkClearColorValue(&val->customBorderColor);
    bufferSize += 4; // format
    return bufferSize;
}

static inline void vt_serialize_VkSamplerCustomBorderColorCreateInfoEXT(VkSamplerCustomBorderColorCreateInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    vt_serialize_VkClearColorValue(&val->customBorderColor, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkClearColorValue(&val->customBorderColor);
    *(VkFormat*)(outputBuffer + bufferOffset) = val->format;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSamplerCustomBorderColorCreateInfoEXT(VkSamplerCustomBorderColorCreateInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    vt_unserialize_VkClearColorValue(&val->customBorderColor, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkClearColorValue(&val->customBorderColor);
    val->format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkBufferMemoryBarrier2(VkBufferMemoryBarrier2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // srcStageMask
    bufferSize += 8; // srcAccessMask
    bufferSize += 8; // dstStageMask
    bufferSize += 8; // dstAccessMask
    bufferSize += 4; // srcQueueFamilyIndex
    bufferSize += 4; // dstQueueFamilyIndex
    bufferSize += vt_sizeof_VkBuffer(val->buffer);
    bufferSize += 8; // offset
    bufferSize += 8; // size
    return bufferSize;
}

static inline void vt_serialize_VkBufferMemoryBarrier2(VkBufferMemoryBarrier2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkPipelineStageFlags2*)(outputBuffer + bufferOffset) = val->srcStageMask;
    bufferOffset += 8;
    *(VkAccessFlags2*)(outputBuffer + bufferOffset) = val->srcAccessMask;
    bufferOffset += 8;
    *(VkPipelineStageFlags2*)(outputBuffer + bufferOffset) = val->dstStageMask;
    bufferOffset += 8;
    *(VkAccessFlags2*)(outputBuffer + bufferOffset) = val->dstAccessMask;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->srcQueueFamilyIndex;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstQueueFamilyIndex;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->buffer, outputBuffer + bufferOffset);
#else
    VkObject* bufferObject = VkObject_fromHandle(val->buffer);
    vt_serialize_VkBuffer((VkBuffer)&bufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->offset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkBufferMemoryBarrier2(VkBufferMemoryBarrier2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->srcStageMask = *(VkPipelineStageFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->srcAccessMask = *(VkAccessFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->dstStageMask = *(VkPipelineStageFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->dstAccessMask = *(VkAccessFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->srcQueueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstQueueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t bufferId;
    vt_unserialize_VkBuffer((VkBuffer)&bufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->buffer = VkObject_fromId(bufferId);
#else
    VkObject* bufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, bufferId);
    val->buffer = VkObject_toHandle(bufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    val->offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkImageMemoryBarrier2(VkImageMemoryBarrier2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_SAMPLE_LOCATIONS_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkSampleLocationsInfoEXT((VkSampleLocationsInfoEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 8; // srcStageMask
    bufferSize += 8; // srcAccessMask
    bufferSize += 8; // dstStageMask
    bufferSize += 8; // dstAccessMask
    bufferSize += 4; // oldLayout
    bufferSize += 4; // newLayout
    bufferSize += 4; // srcQueueFamilyIndex
    bufferSize += 4; // dstQueueFamilyIndex
    bufferSize += vt_sizeof_VkImage(val->image);
    bufferSize += vt_sizeof_VkImageSubresourceRange(&val->subresourceRange);
    return bufferSize;
}

static inline void vt_serialize_VkImageMemoryBarrier2(VkImageMemoryBarrier2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_SAMPLE_LOCATIONS_INFO_EXT: {
                    int itemSize = vt_sizeof_VkSampleLocationsInfoEXT((VkSampleLocationsInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkSampleLocationsInfoEXT((VkSampleLocationsInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkPipelineStageFlags2*)(outputBuffer + bufferOffset) = val->srcStageMask;
    bufferOffset += 8;
    *(VkAccessFlags2*)(outputBuffer + bufferOffset) = val->srcAccessMask;
    bufferOffset += 8;
    *(VkPipelineStageFlags2*)(outputBuffer + bufferOffset) = val->dstStageMask;
    bufferOffset += 8;
    *(VkAccessFlags2*)(outputBuffer + bufferOffset) = val->dstAccessMask;
    bufferOffset += 8;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->oldLayout;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->newLayout;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->srcQueueFamilyIndex;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstQueueFamilyIndex;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->image, outputBuffer + bufferOffset);
#else
    VkObject* imageObject = VkObject_fromHandle(val->image);
    vt_serialize_VkImage((VkImage)&imageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
    vt_serialize_VkImageSubresourceRange(&val->subresourceRange, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceRange(&val->subresourceRange);
}

static inline void vt_unserialize_VkImageMemoryBarrier2(VkImageMemoryBarrier2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_SAMPLE_LOCATIONS_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkSampleLocationsInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SAMPLE_LOCATIONS_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkSampleLocationsInfoEXT));
                    vt_unserialize_VkSampleLocationsInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->srcStageMask = *(VkPipelineStageFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->srcAccessMask = *(VkAccessFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->dstStageMask = *(VkPipelineStageFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->dstAccessMask = *(VkAccessFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->oldLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->newLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcQueueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstQueueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t imageId;
    vt_unserialize_VkImage((VkImage)&imageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->image = VkObject_fromId(imageId);
#else
    VkObject* imageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, imageId);
    val->image = VkObject_toHandle(imageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
    vt_unserialize_VkImageSubresourceRange(&val->subresourceRange, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceRange(&val->subresourceRange);
}

static inline int vt_sizeof_VkDependencyInfo(VkDependencyInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // dependencyFlags
    bufferSize += 4; // memoryBarrierCount
    bufferSize += 4;
    if (val->pMemoryBarriers) {
        int arrSize = val->memoryBarrierCount ? val->memoryBarrierCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkMemoryBarrier2(&val->pMemoryBarriers[i]);
        }
    }
    bufferSize += 4; // bufferMemoryBarrierCount
    bufferSize += 4;
    if (val->pBufferMemoryBarriers) {
        int arrSize = val->bufferMemoryBarrierCount ? val->bufferMemoryBarrierCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBufferMemoryBarrier2(&val->pBufferMemoryBarriers[i]);
        }
    }
    bufferSize += 4; // imageMemoryBarrierCount
    bufferSize += 4;
    if (val->pImageMemoryBarriers) {
        int arrSize = val->imageMemoryBarrierCount ? val->imageMemoryBarrierCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkImageMemoryBarrier2(&val->pImageMemoryBarriers[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkDependencyInfo(VkDependencyInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDependencyFlags*)(outputBuffer + bufferOffset) = val->dependencyFlags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->memoryBarrierCount;
    bufferOffset += 4;

    if (val->pMemoryBarriers) {
        int arrSize = val->memoryBarrierCount ? val->memoryBarrierCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkMemoryBarrier2(&val->pMemoryBarriers[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkMemoryBarrier2(&val->pMemoryBarriers[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->bufferMemoryBarrierCount;
    bufferOffset += 4;

    if (val->pBufferMemoryBarriers) {
        int arrSize = val->bufferMemoryBarrierCount ? val->bufferMemoryBarrierCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBufferMemoryBarrier2(&val->pBufferMemoryBarriers[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBufferMemoryBarrier2(&val->pBufferMemoryBarriers[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->imageMemoryBarrierCount;
    bufferOffset += 4;

    if (val->pImageMemoryBarriers) {
        int arrSize = val->imageMemoryBarrierCount ? val->imageMemoryBarrierCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkImageMemoryBarrier2(&val->pImageMemoryBarriers[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkImageMemoryBarrier2(&val->pImageMemoryBarriers[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDependencyInfo(VkDependencyInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->dependencyFlags = *(VkDependencyFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->memoryBarrierCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkMemoryBarrier2* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkMemoryBarrier2));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkMemoryBarrier2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pMemoryBarriers = arrValues;
    }
    else val->pMemoryBarriers = NULL;
    val->bufferMemoryBarrierCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkBufferMemoryBarrier2* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkBufferMemoryBarrier2));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBufferMemoryBarrier2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pBufferMemoryBarriers = arrValues;
    }
    else val->pBufferMemoryBarriers = NULL;
    val->imageMemoryBarrierCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        VkImageMemoryBarrier2* arrValues = vt_alloc(memoryPool, dataSize9 * sizeof(VkImageMemoryBarrier2));
        for (int i = 0; i < dataSize9; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkImageMemoryBarrier2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pImageMemoryBarriers = arrValues;
    }
    else val->pImageMemoryBarriers = NULL;
}

static inline int vt_sizeof_VkSubmitInfo2(VkSubmitInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // waitSemaphoreInfoCount
    bufferSize += 4;
    if (val->pWaitSemaphoreInfos) {
        int arrSize = val->waitSemaphoreInfoCount ? val->waitSemaphoreInfoCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSemaphoreSubmitInfo(&val->pWaitSemaphoreInfos[i]);
        }
    }
    bufferSize += 4; // commandBufferInfoCount
    bufferSize += 4;
    if (val->pCommandBufferInfos) {
        int arrSize = val->commandBufferInfoCount ? val->commandBufferInfoCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkCommandBufferSubmitInfo(&val->pCommandBufferInfos[i]);
        }
    }
    bufferSize += 4; // signalSemaphoreInfoCount
    bufferSize += 4;
    if (val->pSignalSemaphoreInfos) {
        int arrSize = val->signalSemaphoreInfoCount ? val->signalSemaphoreInfoCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSemaphoreSubmitInfo(&val->pSignalSemaphoreInfos[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkSubmitInfo2(VkSubmitInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkSubmitFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->waitSemaphoreInfoCount;
    bufferOffset += 4;

    if (val->pWaitSemaphoreInfos) {
        int arrSize = val->waitSemaphoreInfoCount ? val->waitSemaphoreInfoCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSemaphoreSubmitInfo(&val->pWaitSemaphoreInfos[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSemaphoreSubmitInfo(&val->pWaitSemaphoreInfos[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->commandBufferInfoCount;
    bufferOffset += 4;

    if (val->pCommandBufferInfos) {
        int arrSize = val->commandBufferInfoCount ? val->commandBufferInfoCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkCommandBufferSubmitInfo(&val->pCommandBufferInfos[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkCommandBufferSubmitInfo(&val->pCommandBufferInfos[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->signalSemaphoreInfoCount;
    bufferOffset += 4;

    if (val->pSignalSemaphoreInfos) {
        int arrSize = val->signalSemaphoreInfoCount ? val->signalSemaphoreInfoCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSemaphoreSubmitInfo(&val->pSignalSemaphoreInfos[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSemaphoreSubmitInfo(&val->pSignalSemaphoreInfos[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkSubmitInfo2(VkSubmitInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkSubmitFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->waitSemaphoreInfoCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkSemaphoreSubmitInfo* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkSemaphoreSubmitInfo));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSemaphoreSubmitInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pWaitSemaphoreInfos = arrValues;
    }
    else val->pWaitSemaphoreInfos = NULL;
    val->commandBufferInfoCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkCommandBufferSubmitInfo* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkCommandBufferSubmitInfo));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkCommandBufferSubmitInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pCommandBufferInfos = arrValues;
    }
    else val->pCommandBufferInfos = NULL;
    val->signalSemaphoreInfoCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        VkSemaphoreSubmitInfo* arrValues = vt_alloc(memoryPool, dataSize9 * sizeof(VkSemaphoreSubmitInfo));
        for (int i = 0; i < dataSize9; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSemaphoreSubmitInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pSignalSemaphoreInfos = arrValues;
    }
    else val->pSignalSemaphoreInfos = NULL;
}

static inline int vt_sizeof_VkSamplerBorderColorComponentMappingCreateInfoEXT(VkSamplerBorderColorComponentMappingCreateInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkComponentMapping(&val->components);
    bufferSize += 4; // srgb
    return bufferSize;
}

static inline void vt_serialize_VkSamplerBorderColorComponentMappingCreateInfoEXT(VkSamplerBorderColorComponentMappingCreateInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    vt_serialize_VkComponentMapping(&val->components, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkComponentMapping(&val->components);
    *(VkBool32*)(outputBuffer + bufferOffset) = val->srgb;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSamplerBorderColorComponentMappingCreateInfoEXT(VkSamplerBorderColorComponentMappingCreateInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    vt_unserialize_VkComponentMapping(&val->components, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkComponentMapping(&val->components);
    val->srgb = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkColorBlendEquationEXT(VkColorBlendEquationEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // srcColorBlendFactor
    bufferSize += 4; // dstColorBlendFactor
    bufferSize += 4; // colorBlendOp
    bufferSize += 4; // srcAlphaBlendFactor
    bufferSize += 4; // dstAlphaBlendFactor
    bufferSize += 4; // alphaBlendOp
    return bufferSize;
}

static inline void vt_serialize_VkColorBlendEquationEXT(VkColorBlendEquationEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkBlendFactor*)(outputBuffer + bufferOffset) = val->srcColorBlendFactor;
    bufferOffset += 4;
    *(VkBlendFactor*)(outputBuffer + bufferOffset) = val->dstColorBlendFactor;
    bufferOffset += 4;
    *(VkBlendOp*)(outputBuffer + bufferOffset) = val->colorBlendOp;
    bufferOffset += 4;
    *(VkBlendFactor*)(outputBuffer + bufferOffset) = val->srcAlphaBlendFactor;
    bufferOffset += 4;
    *(VkBlendFactor*)(outputBuffer + bufferOffset) = val->dstAlphaBlendFactor;
    bufferOffset += 4;
    *(VkBlendOp*)(outputBuffer + bufferOffset) = val->alphaBlendOp;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkColorBlendEquationEXT(VkColorBlendEquationEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->srcColorBlendFactor = *(VkBlendFactor*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstColorBlendFactor = *(VkBlendFactor*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->colorBlendOp = *(VkBlendOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcAlphaBlendFactor = *(VkBlendFactor*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstAlphaBlendFactor = *(VkBlendFactor*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->alphaBlendOp = *(VkBlendOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkColorBlendAdvancedEXT(VkColorBlendAdvancedEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // advancedBlendOp
    bufferSize += 4; // srcPremultiplied
    bufferSize += 4; // dstPremultiplied
    bufferSize += 4; // blendOverlap
    bufferSize += 4; // clampResults
    return bufferSize;
}

static inline void vt_serialize_VkColorBlendAdvancedEXT(VkColorBlendAdvancedEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkBlendOp*)(outputBuffer + bufferOffset) = val->advancedBlendOp;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->srcPremultiplied;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->dstPremultiplied;
    bufferOffset += 4;
    *(VkBlendOverlapEXT*)(outputBuffer + bufferOffset) = val->blendOverlap;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->clampResults;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkColorBlendAdvancedEXT(VkColorBlendAdvancedEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->advancedBlendOp = *(VkBlendOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcPremultiplied = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstPremultiplied = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->blendOverlap = *(VkBlendOverlapEXT*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->clampResults = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkShaderModuleIdentifierEXT(VkShaderModuleIdentifierEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // identifierSize
    bufferSize += val->identifierSize;
    return bufferSize;
}

static inline void vt_serialize_VkShaderModuleIdentifierEXT(VkShaderModuleIdentifierEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->identifierSize;
    bufferOffset += 4;
    if (val->identifierSize <= VK_MAX_SHADER_MODULE_IDENTIFIER_SIZE_EXT) memcpy(outputBuffer + bufferOffset, val->identifier, val->identifierSize);
    bufferOffset += val->identifierSize;
}

static inline void vt_unserialize_VkShaderModuleIdentifierEXT(VkShaderModuleIdentifierEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->identifierSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (val->identifierSize <= VK_MAX_SHADER_MODULE_IDENTIFIER_SIZE_EXT) memcpy(val->identifier, inputBuffer + bufferOffset, val->identifierSize);
    bufferOffset += val->identifierSize;
}

static inline int vt_sizeof_VkMutableDescriptorTypeListEXT(VkMutableDescriptorTypeListEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // descriptorTypeCount
    bufferSize += 4;
    bufferSize += val->pDescriptorTypes ? val->descriptorTypeCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkMutableDescriptorTypeListEXT(VkMutableDescriptorTypeListEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->descriptorTypeCount;
    bufferOffset += 4;

    if (val->pDescriptorTypes) {
        int arrSize = val->descriptorTypeCount ? val->descriptorTypeCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pDescriptorTypes, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkMutableDescriptorTypeListEXT(VkMutableDescriptorTypeListEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->descriptorTypeCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        VkDescriptorType* arrValues = vt_alloc(memoryPool, dataSize2 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize2 * 4);
        bufferOffset += dataSize2 * 4;
        val->pDescriptorTypes = arrValues;
    }
    else val->pDescriptorTypes = NULL;
}

static inline int vt_sizeof_VkMutableDescriptorTypeCreateInfoEXT(VkMutableDescriptorTypeCreateInfoEXT* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // mutableDescriptorTypeListCount
    bufferSize += 4;
    if (val->pMutableDescriptorTypeLists) {
        int arrSize = val->mutableDescriptorTypeListCount ? val->mutableDescriptorTypeListCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkMutableDescriptorTypeListEXT(&val->pMutableDescriptorTypeLists[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkMutableDescriptorTypeCreateInfoEXT(VkMutableDescriptorTypeCreateInfoEXT* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->mutableDescriptorTypeListCount;
    bufferOffset += 4;

    if (val->pMutableDescriptorTypeLists) {
        int arrSize = val->mutableDescriptorTypeListCount ? val->mutableDescriptorTypeListCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkMutableDescriptorTypeListEXT(&val->pMutableDescriptorTypeLists[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkMutableDescriptorTypeListEXT(&val->pMutableDescriptorTypeLists[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkMutableDescriptorTypeCreateInfoEXT(VkMutableDescriptorTypeCreateInfoEXT* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->mutableDescriptorTypeListCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkMutableDescriptorTypeListEXT* arrValues = vt_alloc(memoryPool, dataSize4 * sizeof(VkMutableDescriptorTypeListEXT));
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkMutableDescriptorTypeListEXT(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pMutableDescriptorTypeLists = arrValues;
    }
    else val->pMutableDescriptorTypeLists = NULL;
}

static inline int vt_sizeof_VkPipelineLayoutCreateInfo(VkPipelineLayoutCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // setLayoutCount
    bufferSize += 4;
    if (val->pSetLayouts) {
        for (int i = 0; i < val->setLayoutCount; i++) {
            bufferSize += vt_sizeof_VkDescriptorSetLayout(val->pSetLayouts[i]);
        }
    }
    bufferSize += 4; // pushConstantRangeCount
    bufferSize += 4;
    if (val->pPushConstantRanges) {
        int arrSize = val->pushConstantRangeCount ? val->pushConstantRangeCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkPushConstantRange(&val->pPushConstantRanges[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkPipelineLayoutCreateInfo(VkPipelineLayoutCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkPipelineLayoutCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->setLayoutCount;
    bufferOffset += 4;

    if (val->pSetLayouts) {
        *(int*)(outputBuffer + bufferOffset) = val->setLayoutCount;
        bufferOffset += 4;
        for (int i = 0; i < val->setLayoutCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkDescriptorSetLayout(val->pSetLayouts[i], outputBuffer + bufferOffset);
#else
            VkObject* setLayoutsObject = VkObject_fromHandle(val->pSetLayouts[i]);
            vt_serialize_VkDescriptorSetLayout((VkDescriptorSetLayout)&setLayoutsObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkDescriptorSetLayout(val->pSetLayouts[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->pushConstantRangeCount;
    bufferOffset += 4;

    if (val->pPushConstantRanges) {
        int arrSize = val->pushConstantRangeCount ? val->pushConstantRangeCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkPushConstantRange(&val->pPushConstantRanges[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkPushConstantRange(&val->pPushConstantRanges[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkPipelineLayoutCreateInfo(VkPipelineLayoutCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkPipelineLayoutCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->setLayoutCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkDescriptorSetLayout* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkDescriptorSetLayout));
        for (int i = 0; i < dataSize5; i++) {
            uint64_t setLayoutsId;
            vt_unserialize_VkDescriptorSetLayout((VkDescriptorSetLayout)&setLayoutsId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(setLayoutsId);
#else
                VkObject* setLayoutsObject = VkObject_create(VK_OBJECT_TYPE_DESCRIPTOR_SET_LAYOUT, setLayoutsId);
                arrValues[i] = VkObject_toHandle(setLayoutsObject);
#endif
                bufferOffset += vt_sizeof_VkDescriptorSetLayout(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkDescriptorSetLayout(VK_NULL_HANDLE);
        }
        val->pSetLayouts = arrValues;
    }
    else val->pSetLayouts = NULL;
    val->pushConstantRangeCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkPushConstantRange* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkPushConstantRange));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkPushConstantRange(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pPushConstantRanges = arrValues;
    }
    else val->pPushConstantRanges = NULL;
}

static inline int vt_sizeof_VkDevicePrivateDataCreateInfo(VkDevicePrivateDataCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // privateDataSlotRequestCount
    return bufferSize;
}

static inline void vt_serialize_VkDevicePrivateDataCreateInfo(VkDevicePrivateDataCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->privateDataSlotRequestCount;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDevicePrivateDataCreateInfo(VkDevicePrivateDataCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->privateDataSlotRequestCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkRenderPassMultiviewCreateInfo(VkRenderPassMultiviewCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // subpassCount
    bufferSize += 4;
    bufferSize += val->pViewMasks ? val->subpassCount * 4 : 0;
    bufferSize += 4; // dependencyCount
    bufferSize += 4;
    bufferSize += val->pViewOffsets ? val->dependencyCount * 4 : 0;
    bufferSize += 4; // correlationMaskCount
    bufferSize += 4;
    bufferSize += val->pCorrelationMasks ? val->correlationMaskCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkRenderPassMultiviewCreateInfo(VkRenderPassMultiviewCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subpassCount;
    bufferOffset += 4;

    if (val->pViewMasks) {
        int arrSize = val->subpassCount ? val->subpassCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pViewMasks, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dependencyCount;
    bufferOffset += 4;

    if (val->pViewOffsets) {
        int arrSize = val->dependencyCount ? val->dependencyCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pViewOffsets, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->correlationMaskCount;
    bufferOffset += 4;

    if (val->pCorrelationMasks) {
        int arrSize = val->correlationMaskCount ? val->correlationMaskCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pCorrelationMasks, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkRenderPassMultiviewCreateInfo(VkRenderPassMultiviewCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->subpassCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize4 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
        val->pViewMasks = arrValues;
    }
    else val->pViewMasks = NULL;
    val->dependencyCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        int32_t* arrValues = vt_alloc(memoryPool, dataSize6 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize6 * 4);
        bufferOffset += dataSize6 * 4;
        val->pViewOffsets = arrValues;
    }
    else val->pViewOffsets = NULL;
    val->correlationMaskCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize8 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize8 * 4);
        bufferOffset += dataSize8 * 4;
        val->pCorrelationMasks = arrValues;
    }
    else val->pCorrelationMasks = NULL;
}

static inline int vt_sizeof_VkMemoryAllocateFlagsInfo(VkMemoryAllocateFlagsInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // deviceMask
    return bufferSize;
}

static inline void vt_serialize_VkMemoryAllocateFlagsInfo(VkMemoryAllocateFlagsInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkMemoryAllocateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkMemoryAllocateFlagsInfo(VkMemoryAllocateFlagsInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkMemoryAllocateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->deviceMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkBindBufferMemoryDeviceGroupInfo(VkBindBufferMemoryDeviceGroupInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // deviceIndexCount
    bufferSize += 4;
    bufferSize += val->pDeviceIndices ? val->deviceIndexCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkBindBufferMemoryDeviceGroupInfo(VkBindBufferMemoryDeviceGroupInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceIndexCount;
    bufferOffset += 4;

    if (val->pDeviceIndices) {
        int arrSize = val->deviceIndexCount ? val->deviceIndexCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pDeviceIndices, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkBindBufferMemoryDeviceGroupInfo(VkBindBufferMemoryDeviceGroupInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->deviceIndexCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize4 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
        val->pDeviceIndices = arrValues;
    }
    else val->pDeviceIndices = NULL;
}

static inline int vt_sizeof_VkBindImageMemoryDeviceGroupInfo(VkBindImageMemoryDeviceGroupInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // deviceIndexCount
    bufferSize += 4;
    bufferSize += val->pDeviceIndices ? val->deviceIndexCount * 4 : 0;
    bufferSize += 4; // splitInstanceBindRegionCount
    bufferSize += 4;
    if (val->pSplitInstanceBindRegions) {
        int arrSize = val->splitInstanceBindRegionCount ? val->splitInstanceBindRegionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkRect2D(&val->pSplitInstanceBindRegions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkBindImageMemoryDeviceGroupInfo(VkBindImageMemoryDeviceGroupInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceIndexCount;
    bufferOffset += 4;

    if (val->pDeviceIndices) {
        int arrSize = val->deviceIndexCount ? val->deviceIndexCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pDeviceIndices, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->splitInstanceBindRegionCount;
    bufferOffset += 4;

    if (val->pSplitInstanceBindRegions) {
        int arrSize = val->splitInstanceBindRegionCount ? val->splitInstanceBindRegionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkRect2D(&val->pSplitInstanceBindRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkRect2D(&val->pSplitInstanceBindRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkBindImageMemoryDeviceGroupInfo(VkBindImageMemoryDeviceGroupInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->deviceIndexCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize4 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
        val->pDeviceIndices = arrValues;
    }
    else val->pDeviceIndices = NULL;
    val->splitInstanceBindRegionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkRect2D* arrValues = vt_alloc(memoryPool, dataSize6 * sizeof(VkRect2D));
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkRect2D(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pSplitInstanceBindRegions = arrValues;
    }
    else val->pSplitInstanceBindRegions = NULL;
}

static inline int vt_sizeof_VkDeviceGroupCommandBufferBeginInfo(VkDeviceGroupCommandBufferBeginInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // deviceMask
    return bufferSize;
}

static inline void vt_serialize_VkDeviceGroupCommandBufferBeginInfo(VkDeviceGroupCommandBufferBeginInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->deviceMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDeviceGroupCommandBufferBeginInfo(VkDeviceGroupCommandBufferBeginInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->deviceMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDeviceGroupSubmitInfo(VkDeviceGroupSubmitInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // waitSemaphoreCount
    bufferSize += 4;
    bufferSize += val->pWaitSemaphoreDeviceIndices ? val->waitSemaphoreCount * 4 : 0;
    bufferSize += 4; // commandBufferCount
    bufferSize += 4;
    bufferSize += val->pCommandBufferDeviceMasks ? val->commandBufferCount * 4 : 0;
    bufferSize += 4; // signalSemaphoreCount
    bufferSize += 4;
    bufferSize += val->pSignalSemaphoreDeviceIndices ? val->signalSemaphoreCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkDeviceGroupSubmitInfo(VkDeviceGroupSubmitInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->waitSemaphoreCount;
    bufferOffset += 4;

    if (val->pWaitSemaphoreDeviceIndices) {
        int arrSize = val->waitSemaphoreCount ? val->waitSemaphoreCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pWaitSemaphoreDeviceIndices, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->commandBufferCount;
    bufferOffset += 4;

    if (val->pCommandBufferDeviceMasks) {
        int arrSize = val->commandBufferCount ? val->commandBufferCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pCommandBufferDeviceMasks, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->signalSemaphoreCount;
    bufferOffset += 4;

    if (val->pSignalSemaphoreDeviceIndices) {
        int arrSize = val->signalSemaphoreCount ? val->signalSemaphoreCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pSignalSemaphoreDeviceIndices, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDeviceGroupSubmitInfo(VkDeviceGroupSubmitInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->waitSemaphoreCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize4 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
        val->pWaitSemaphoreDeviceIndices = arrValues;
    }
    else val->pWaitSemaphoreDeviceIndices = NULL;
    val->commandBufferCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize6 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize6 * 4);
        bufferOffset += dataSize6 * 4;
        val->pCommandBufferDeviceMasks = arrValues;
    }
    else val->pCommandBufferDeviceMasks = NULL;
    val->signalSemaphoreCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize8 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize8 * 4);
        bufferOffset += dataSize8 * 4;
        val->pSignalSemaphoreDeviceIndices = arrValues;
    }
    else val->pSignalSemaphoreDeviceIndices = NULL;
}

static inline int vt_sizeof_VkDeviceGroupBindSparseInfo(VkDeviceGroupBindSparseInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // resourceDeviceIndex
    bufferSize += 4; // memoryDeviceIndex
    return bufferSize;
}

static inline void vt_serialize_VkDeviceGroupBindSparseInfo(VkDeviceGroupBindSparseInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->resourceDeviceIndex;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->memoryDeviceIndex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDeviceGroupBindSparseInfo(VkDeviceGroupBindSparseInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->resourceDeviceIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->memoryDeviceIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDeviceGroupDeviceCreateInfo(VkDeviceGroupDeviceCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // physicalDeviceCount
    bufferSize += 4;
    if (val->pPhysicalDevices) {
        for (int i = 0; i < val->physicalDeviceCount; i++) {
            bufferSize += vt_sizeof_VkPhysicalDevice(val->pPhysicalDevices[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkDeviceGroupDeviceCreateInfo(VkDeviceGroupDeviceCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->physicalDeviceCount;
    bufferOffset += 4;

    if (val->pPhysicalDevices) {
        *(int*)(outputBuffer + bufferOffset) = val->physicalDeviceCount;
        bufferOffset += 4;
        for (int i = 0; i < val->physicalDeviceCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkPhysicalDevice(val->pPhysicalDevices[i], outputBuffer + bufferOffset);
#else
            VkObject* physicalDevicesObject = VkObject_fromHandle(val->pPhysicalDevices[i]);
            vt_serialize_VkPhysicalDevice((VkPhysicalDevice)&physicalDevicesObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkPhysicalDevice(val->pPhysicalDevices[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDeviceGroupDeviceCreateInfo(VkDeviceGroupDeviceCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->physicalDeviceCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkPhysicalDevice* arrValues = vt_alloc(memoryPool, dataSize4 * sizeof(VkPhysicalDevice));
        for (int i = 0; i < dataSize4; i++) {
            uint64_t physicalDevicesId;
            vt_unserialize_VkPhysicalDevice((VkPhysicalDevice)&physicalDevicesId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(physicalDevicesId);
#else
                VkObject* physicalDevicesObject = VkObject_create(VK_OBJECT_TYPE_PHYSICAL_DEVICE, physicalDevicesId);
                arrValues[i] = VkObject_toHandle(physicalDevicesObject);
#endif
                bufferOffset += vt_sizeof_VkPhysicalDevice(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
        }
        val->pPhysicalDevices = arrValues;
    }
    else val->pPhysicalDevices = NULL;
}

static inline int vt_sizeof_VkProtectedSubmitInfo(VkProtectedSubmitInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // protectedSubmit
    return bufferSize;
}

static inline void vt_serialize_VkProtectedSubmitInfo(VkProtectedSubmitInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->protectedSubmit;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkProtectedSubmitInfo(VkProtectedSubmitInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->protectedSubmit = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSamplerReductionModeCreateInfo(VkSamplerReductionModeCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // reductionMode
    return bufferSize;
}

static inline void vt_serialize_VkSamplerReductionModeCreateInfo(VkSamplerReductionModeCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkSamplerReductionMode*)(outputBuffer + bufferOffset) = val->reductionMode;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSamplerReductionModeCreateInfo(VkSamplerReductionModeCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->reductionMode = *(VkSamplerReductionMode*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkWriteDescriptorSetInlineUniformBlock(VkWriteDescriptorSetInlineUniformBlock* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // dataSize
    bufferSize += 4;
    bufferSize += val->pData ? val->dataSize : 0;
    return bufferSize;
}

static inline void vt_serialize_VkWriteDescriptorSetInlineUniformBlock(VkWriteDescriptorSetInlineUniformBlock* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dataSize;
    bufferOffset += 4;

    if (val->pData) {
        *(int*)(outputBuffer + bufferOffset) = val->dataSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pData, val->dataSize);
        bufferOffset += val->dataSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkWriteDescriptorSetInlineUniformBlock(VkWriteDescriptorSetInlineUniformBlock* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->dataSize = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        val->pData = vt_alloc(memoryPool, dataSize4);
        memcpy(val->pData, inputBuffer + bufferOffset, dataSize4);
        bufferOffset += dataSize4;
    }
    else val->pData = NULL;
}

static inline int vt_sizeof_VkDescriptorPoolInlineUniformBlockCreateInfo(VkDescriptorPoolInlineUniformBlockCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // maxInlineUniformBlockBindings
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorPoolInlineUniformBlockCreateInfo(VkDescriptorPoolInlineUniformBlockCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxInlineUniformBlockBindings;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDescriptorPoolInlineUniformBlockCreateInfo(VkDescriptorPoolInlineUniformBlockCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->maxInlineUniformBlockBindings = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkBufferOpaqueCaptureAddressCreateInfo(VkBufferOpaqueCaptureAddressCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // opaqueCaptureAddress
    return bufferSize;
}

static inline void vt_serialize_VkBufferOpaqueCaptureAddressCreateInfo(VkBufferOpaqueCaptureAddressCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint64_t*)(outputBuffer + bufferOffset) = val->opaqueCaptureAddress;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkBufferOpaqueCaptureAddressCreateInfo(VkBufferOpaqueCaptureAddressCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->opaqueCaptureAddress = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkPipelineCreationFeedbackCreateInfo(VkPipelineCreationFeedbackCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4;
    bufferSize += val->pPipelineCreationFeedback ? vt_sizeof_VkPipelineCreationFeedback(val->pPipelineCreationFeedback) : 0;
    bufferSize += 4; // pipelineStageCreationFeedbackCount
    bufferSize += 4;
    if (val->pPipelineStageCreationFeedbacks) {
        int arrSize = val->pipelineStageCreationFeedbackCount ? val->pipelineStageCreationFeedbackCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkPipelineCreationFeedback(&val->pPipelineStageCreationFeedbacks[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkPipelineCreationFeedbackCreateInfo(VkPipelineCreationFeedbackCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

    if (val->pPipelineCreationFeedback) {
        int itemSize = vt_sizeof_VkPipelineCreationFeedback(val->pPipelineCreationFeedback);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineCreationFeedback(val->pPipelineCreationFeedback, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->pipelineStageCreationFeedbackCount;
    bufferOffset += 4;

    if (val->pPipelineStageCreationFeedbacks) {
        int arrSize = val->pipelineStageCreationFeedbackCount ? val->pipelineStageCreationFeedbackCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkPipelineCreationFeedback(&val->pPipelineStageCreationFeedbacks[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkPipelineCreationFeedback(&val->pPipelineStageCreationFeedbacks[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkPipelineCreationFeedbackCreateInfo(VkPipelineCreationFeedbackCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        val->pPipelineCreationFeedback = vt_alloc(memoryPool, sizeof(VkPipelineCreationFeedback));
        vt_unserialize_VkPipelineCreationFeedback(val->pPipelineCreationFeedback, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else val->pPipelineCreationFeedback = NULL;
    val->pipelineStageCreationFeedbackCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkPipelineCreationFeedback* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkPipelineCreationFeedback));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkPipelineCreationFeedback(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pPipelineStageCreationFeedbacks = arrValues;
    }
    else val->pPipelineStageCreationFeedbacks = NULL;
}

static inline int vt_sizeof_VkMemoryOpaqueCaptureAddressAllocateInfo(VkMemoryOpaqueCaptureAddressAllocateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 8; // opaqueCaptureAddress
    return bufferSize;
}

static inline void vt_serialize_VkMemoryOpaqueCaptureAddressAllocateInfo(VkMemoryOpaqueCaptureAddressAllocateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint64_t*)(outputBuffer + bufferOffset) = val->opaqueCaptureAddress;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkMemoryOpaqueCaptureAddressAllocateInfo(VkMemoryOpaqueCaptureAddressAllocateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->opaqueCaptureAddress = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkInstanceCreateInfo(VkInstanceCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4;
    bufferSize += val->pApplicationInfo ? vt_sizeof_VkApplicationInfo(val->pApplicationInfo) : 0;
    bufferSize += 4; // enabledLayerCount
    bufferSize += 4;
    if (val->ppEnabledLayerNames) {
        int arrSize = val->enabledLayerCount ? val->enabledLayerCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += strlen(val->ppEnabledLayerNames[i]) + 1;
        }
    }
    bufferSize += 4; // enabledExtensionCount
    bufferSize += 4;
    if (val->ppEnabledExtensionNames) {
        int arrSize = val->enabledExtensionCount ? val->enabledExtensionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += strlen(val->ppEnabledExtensionNames[i]) + 1;
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkInstanceCreateInfo(VkInstanceCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkInstanceCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;

    if (val->pApplicationInfo) {
        int itemSize = vt_sizeof_VkApplicationInfo(val->pApplicationInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkApplicationInfo(val->pApplicationInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->enabledLayerCount;
    bufferOffset += 4;

    if (val->ppEnabledLayerNames) {
        int arrSize = val->enabledLayerCount ? val->enabledLayerCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int strSize = strlen(val->ppEnabledLayerNames[i]) + 1;
            *(int*)(outputBuffer + bufferOffset) = strSize;
            bufferOffset += 4;
            memcpy(outputBuffer + bufferOffset, val->ppEnabledLayerNames[i], strSize);
            bufferOffset += strSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->enabledExtensionCount;
    bufferOffset += 4;

    if (val->ppEnabledExtensionNames) {
        int arrSize = val->enabledExtensionCount ? val->enabledExtensionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int strSize = strlen(val->ppEnabledExtensionNames[i]) + 1;
            *(int*)(outputBuffer + bufferOffset) = strSize;
            bufferOffset += 4;
            memcpy(outputBuffer + bufferOffset, val->ppEnabledExtensionNames[i], strSize);
            bufferOffset += strSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkInstanceCreateInfo(VkInstanceCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkInstanceCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        val->pApplicationInfo = vt_alloc(memoryPool, sizeof(VkApplicationInfo));
        vt_unserialize_VkApplicationInfo(val->pApplicationInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize4;
    }
    else val->pApplicationInfo = NULL;
    val->enabledLayerCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        char** arrValues = vt_alloc(memoryPool, dataSize6 * sizeof(char*));
        for (int i = 0; i < dataSize6; i++) {
            int strSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            arrValues[i] = vt_alloc(memoryPool, strSize);
            memcpy(arrValues[i], inputBuffer + bufferOffset, strSize);
            bufferOffset += strSize;
        }
        val->ppEnabledLayerNames = (const char* const*)arrValues;
    }
    else val->ppEnabledLayerNames = NULL;
    val->enabledExtensionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        char** arrValues = vt_alloc(memoryPool, dataSize8 * sizeof(char*));
        for (int i = 0; i < dataSize8; i++) {
            int strSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            arrValues[i] = vt_alloc(memoryPool, strSize);
            memcpy(arrValues[i], inputBuffer + bufferOffset, strSize);
            bufferOffset += strSize;
        }
        val->ppEnabledExtensionNames = (const char* const*)arrValues;
    }
    else val->ppEnabledExtensionNames = NULL;
}

static inline int vt_sizeof_VkDeviceCreateInfo(VkDeviceCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DEVICE_PRIVATE_DATA_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDevicePrivateDataCreateInfo((VkDevicePrivateDataCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PRIVATE_DATA_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevicePrivateDataFeatures((VkPhysicalDevicePrivateDataFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FEATURES_2: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceFeatures2((VkPhysicalDeviceFeatures2*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VARIABLE_POINTERS_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVariablePointersFeatures((VkPhysicalDeviceVariablePointersFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMultiviewFeatures((VkPhysicalDeviceMultiviewFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_DEVICE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDeviceGroupDeviceCreateInfo((VkDeviceGroupDeviceCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_16BIT_STORAGE_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevice16BitStorageFeatures((VkPhysicalDevice16BitStorageFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_SUBGROUP_EXTENDED_TYPES_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures((VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_YCBCR_CONVERSION_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSamplerYcbcrConversionFeatures((VkPhysicalDeviceSamplerYcbcrConversionFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceProtectedMemoryFeatures((VkPhysicalDeviceProtectedMemoryFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceInlineUniformBlockFeatures((VkPhysicalDeviceInlineUniformBlockFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMaintenance4Features((VkPhysicalDeviceMaintenance4Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DRAW_PARAMETERS_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderDrawParametersFeatures((VkPhysicalDeviceShaderDrawParametersFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_FLOAT16_INT8_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderFloat16Int8Features((VkPhysicalDeviceShaderFloat16Int8Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_HOST_QUERY_RESET_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceHostQueryResetFeatures((VkPhysicalDeviceHostQueryResetFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceDescriptorIndexingFeatures((VkPhysicalDeviceDescriptorIndexingFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceTimelineSemaphoreFeatures((VkPhysicalDeviceTimelineSemaphoreFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_8BIT_STORAGE_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevice8BitStorageFeatures((VkPhysicalDevice8BitStorageFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONDITIONAL_RENDERING_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceConditionalRenderingFeaturesEXT((VkPhysicalDeviceConditionalRenderingFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_MEMORY_MODEL_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVulkanMemoryModelFeatures((VkPhysicalDeviceVulkanMemoryModelFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_ATOMIC_INT64_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderAtomicInt64Features((VkPhysicalDeviceShaderAtomicInt64Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_FEATURES_KHR: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR((VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceTransformFeedbackFeaturesEXT((VkPhysicalDeviceTransformFeedbackFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SCALAR_BLOCK_LAYOUT_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceScalarBlockLayoutFeatures((VkPhysicalDeviceScalarBlockLayoutFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_UNIFORM_BUFFER_STANDARD_LAYOUT_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceUniformBufferStandardLayoutFeatures((VkPhysicalDeviceUniformBufferStandardLayoutFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_ENABLE_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceDepthClipEnableFeaturesEXT((VkPhysicalDeviceDepthClipEnableFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BUFFER_DEVICE_ADDRESS_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceBufferDeviceAddressFeatures((VkPhysicalDeviceBufferDeviceAddressFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGELESS_FRAMEBUFFER_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceImagelessFramebufferFeatures((VkPhysicalDeviceImagelessFramebufferFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXTURE_COMPRESSION_ASTC_HDR_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceTextureCompressionASTCHDRFeatures((VkPhysicalDeviceTextureCompressionASTCHDRFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FRAGMENT_SHADER_INTERLOCK_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT((VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SEPARATE_DEPTH_STENCIL_LAYOUTS_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures((VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DEMOTE_TO_HELPER_INVOCATION_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures((VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT((VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSubgroupSizeControlFeatures((VkPhysicalDeviceSubgroupSizeControlFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PIPELINE_CREATION_CACHE_CONTROL_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevicePipelineCreationCacheControlFeatures((VkPhysicalDevicePipelineCreationCacheControlFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVulkan11Features((VkPhysicalDeviceVulkan11Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVulkan12Features((VkPhysicalDeviceVulkan12Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceVulkan13Features((VkPhysicalDeviceVulkan13Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceCustomBorderColorFeaturesEXT((VkPhysicalDeviceCustomBorderColorFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BORDER_COLOR_SWIZZLE_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceBorderColorSwizzleFeaturesEXT((VkPhysicalDeviceBorderColorSwizzleFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceExtendedDynamicStateFeaturesEXT((VkPhysicalDeviceExtendedDynamicStateFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceExtendedDynamicState3FeaturesEXT((VkPhysicalDeviceExtendedDynamicState3FeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ZERO_INITIALIZE_WORKGROUP_MEMORY_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures((VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceRobustness2FeaturesEXT((VkPhysicalDeviceRobustness2FeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGE_ROBUSTNESS_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceImageRobustnessFeatures((VkPhysicalDeviceImageRobustnessFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_4444_FORMATS_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDevice4444FormatsFeaturesEXT((VkPhysicalDevice4444FormatsFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_TERMINATE_INVOCATION_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderTerminateInvocationFeatures((VkPhysicalDeviceShaderTerminateInvocationFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MUTABLE_DESCRIPTOR_TYPE_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT((VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_CONTROL_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceDepthClipControlFeaturesEXT((VkPhysicalDeviceDepthClipControlFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_COLOR_WRITE_ENABLE_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceColorWriteEnableFeaturesEXT((VkPhysicalDeviceColorWriteEnableFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SYNCHRONIZATION_2_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceSynchronization2Features((VkPhysicalDeviceSynchronization2Features*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceProvokingVertexFeaturesEXT((VkPhysicalDeviceProvokingVertexFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderIntegerDotProductFeatures((VkPhysicalDeviceShaderIntegerDotProductFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DYNAMIC_RENDERING_FEATURES: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceDynamicRenderingFeatures((VkPhysicalDeviceDynamicRenderingFeatures*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT((VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_FEATURES_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPhysicalDeviceMapMemoryPlacedFeaturesEXT((VkPhysicalDeviceMapMemoryPlacedFeaturesEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // queueCreateInfoCount
    bufferSize += 4;
    if (val->pQueueCreateInfos) {
        int arrSize = val->queueCreateInfoCount ? val->queueCreateInfoCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkDeviceQueueCreateInfo(&val->pQueueCreateInfos[i]);
        }
    }
    bufferSize += 4; // enabledLayerCount
    bufferSize += 4;
    if (val->ppEnabledLayerNames) {
        int arrSize = val->enabledLayerCount ? val->enabledLayerCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += strlen(val->ppEnabledLayerNames[i]) + 1;
        }
    }
    bufferSize += 4; // enabledExtensionCount
    bufferSize += 4;
    if (val->ppEnabledExtensionNames) {
        int arrSize = val->enabledExtensionCount ? val->enabledExtensionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += strlen(val->ppEnabledExtensionNames[i]) + 1;
        }
    }
    bufferSize += 4;
    bufferSize += val->pEnabledFeatures ? vt_sizeof_VkPhysicalDeviceFeatures(val->pEnabledFeatures) : 0;
    return bufferSize;
}

static inline void vt_serialize_VkDeviceCreateInfo(VkDeviceCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DEVICE_PRIVATE_DATA_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkDevicePrivateDataCreateInfo((VkDevicePrivateDataCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDevicePrivateDataCreateInfo((VkDevicePrivateDataCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PRIVATE_DATA_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDevicePrivateDataFeatures((VkPhysicalDevicePrivateDataFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevicePrivateDataFeatures((VkPhysicalDevicePrivateDataFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FEATURES_2: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceFeatures2((VkPhysicalDeviceFeatures2*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceFeatures2((VkPhysicalDeviceFeatures2*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VARIABLE_POINTERS_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVariablePointersFeatures((VkPhysicalDeviceVariablePointersFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVariablePointersFeatures((VkPhysicalDeviceVariablePointersFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMultiviewFeatures((VkPhysicalDeviceMultiviewFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMultiviewFeatures((VkPhysicalDeviceMultiviewFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_DEVICE_GROUP_DEVICE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkDeviceGroupDeviceCreateInfo((VkDeviceGroupDeviceCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDeviceGroupDeviceCreateInfo((VkDeviceGroupDeviceCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_16BIT_STORAGE_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDevice16BitStorageFeatures((VkPhysicalDevice16BitStorageFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevice16BitStorageFeatures((VkPhysicalDevice16BitStorageFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_SUBGROUP_EXTENDED_TYPES_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures((VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures((VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_YCBCR_CONVERSION_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSamplerYcbcrConversionFeatures((VkPhysicalDeviceSamplerYcbcrConversionFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSamplerYcbcrConversionFeatures((VkPhysicalDeviceSamplerYcbcrConversionFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceProtectedMemoryFeatures((VkPhysicalDeviceProtectedMemoryFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceProtectedMemoryFeatures((VkPhysicalDeviceProtectedMemoryFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceInlineUniformBlockFeatures((VkPhysicalDeviceInlineUniformBlockFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceInlineUniformBlockFeatures((VkPhysicalDeviceInlineUniformBlockFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMaintenance4Features((VkPhysicalDeviceMaintenance4Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMaintenance4Features((VkPhysicalDeviceMaintenance4Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DRAW_PARAMETERS_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderDrawParametersFeatures((VkPhysicalDeviceShaderDrawParametersFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderDrawParametersFeatures((VkPhysicalDeviceShaderDrawParametersFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_FLOAT16_INT8_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderFloat16Int8Features((VkPhysicalDeviceShaderFloat16Int8Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderFloat16Int8Features((VkPhysicalDeviceShaderFloat16Int8Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_HOST_QUERY_RESET_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceHostQueryResetFeatures((VkPhysicalDeviceHostQueryResetFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceHostQueryResetFeatures((VkPhysicalDeviceHostQueryResetFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceDescriptorIndexingFeatures((VkPhysicalDeviceDescriptorIndexingFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceDescriptorIndexingFeatures((VkPhysicalDeviceDescriptorIndexingFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceTimelineSemaphoreFeatures((VkPhysicalDeviceTimelineSemaphoreFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceTimelineSemaphoreFeatures((VkPhysicalDeviceTimelineSemaphoreFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_8BIT_STORAGE_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDevice8BitStorageFeatures((VkPhysicalDevice8BitStorageFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevice8BitStorageFeatures((VkPhysicalDevice8BitStorageFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONDITIONAL_RENDERING_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceConditionalRenderingFeaturesEXT((VkPhysicalDeviceConditionalRenderingFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceConditionalRenderingFeaturesEXT((VkPhysicalDeviceConditionalRenderingFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_MEMORY_MODEL_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVulkanMemoryModelFeatures((VkPhysicalDeviceVulkanMemoryModelFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVulkanMemoryModelFeatures((VkPhysicalDeviceVulkanMemoryModelFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_ATOMIC_INT64_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderAtomicInt64Features((VkPhysicalDeviceShaderAtomicInt64Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderAtomicInt64Features((VkPhysicalDeviceShaderAtomicInt64Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_FEATURES_KHR: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR((VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR((VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceTransformFeedbackFeaturesEXT((VkPhysicalDeviceTransformFeedbackFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceTransformFeedbackFeaturesEXT((VkPhysicalDeviceTransformFeedbackFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SCALAR_BLOCK_LAYOUT_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceScalarBlockLayoutFeatures((VkPhysicalDeviceScalarBlockLayoutFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceScalarBlockLayoutFeatures((VkPhysicalDeviceScalarBlockLayoutFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_UNIFORM_BUFFER_STANDARD_LAYOUT_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceUniformBufferStandardLayoutFeatures((VkPhysicalDeviceUniformBufferStandardLayoutFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceUniformBufferStandardLayoutFeatures((VkPhysicalDeviceUniformBufferStandardLayoutFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_ENABLE_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceDepthClipEnableFeaturesEXT((VkPhysicalDeviceDepthClipEnableFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceDepthClipEnableFeaturesEXT((VkPhysicalDeviceDepthClipEnableFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BUFFER_DEVICE_ADDRESS_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceBufferDeviceAddressFeatures((VkPhysicalDeviceBufferDeviceAddressFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceBufferDeviceAddressFeatures((VkPhysicalDeviceBufferDeviceAddressFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGELESS_FRAMEBUFFER_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceImagelessFramebufferFeatures((VkPhysicalDeviceImagelessFramebufferFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceImagelessFramebufferFeatures((VkPhysicalDeviceImagelessFramebufferFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXTURE_COMPRESSION_ASTC_HDR_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceTextureCompressionASTCHDRFeatures((VkPhysicalDeviceTextureCompressionASTCHDRFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceTextureCompressionASTCHDRFeatures((VkPhysicalDeviceTextureCompressionASTCHDRFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FRAGMENT_SHADER_INTERLOCK_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT((VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT((VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SEPARATE_DEPTH_STENCIL_LAYOUTS_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures((VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures((VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DEMOTE_TO_HELPER_INVOCATION_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures((VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures((VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT((VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT((VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSubgroupSizeControlFeatures((VkPhysicalDeviceSubgroupSizeControlFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSubgroupSizeControlFeatures((VkPhysicalDeviceSubgroupSizeControlFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PIPELINE_CREATION_CACHE_CONTROL_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDevicePipelineCreationCacheControlFeatures((VkPhysicalDevicePipelineCreationCacheControlFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevicePipelineCreationCacheControlFeatures((VkPhysicalDevicePipelineCreationCacheControlFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVulkan11Features((VkPhysicalDeviceVulkan11Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVulkan11Features((VkPhysicalDeviceVulkan11Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVulkan12Features((VkPhysicalDeviceVulkan12Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVulkan12Features((VkPhysicalDeviceVulkan12Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceVulkan13Features((VkPhysicalDeviceVulkan13Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceVulkan13Features((VkPhysicalDeviceVulkan13Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceCustomBorderColorFeaturesEXT((VkPhysicalDeviceCustomBorderColorFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceCustomBorderColorFeaturesEXT((VkPhysicalDeviceCustomBorderColorFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BORDER_COLOR_SWIZZLE_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceBorderColorSwizzleFeaturesEXT((VkPhysicalDeviceBorderColorSwizzleFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceBorderColorSwizzleFeaturesEXT((VkPhysicalDeviceBorderColorSwizzleFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceExtendedDynamicStateFeaturesEXT((VkPhysicalDeviceExtendedDynamicStateFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceExtendedDynamicStateFeaturesEXT((VkPhysicalDeviceExtendedDynamicStateFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceExtendedDynamicState3FeaturesEXT((VkPhysicalDeviceExtendedDynamicState3FeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceExtendedDynamicState3FeaturesEXT((VkPhysicalDeviceExtendedDynamicState3FeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ZERO_INITIALIZE_WORKGROUP_MEMORY_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures((VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures((VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceRobustness2FeaturesEXT((VkPhysicalDeviceRobustness2FeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceRobustness2FeaturesEXT((VkPhysicalDeviceRobustness2FeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGE_ROBUSTNESS_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceImageRobustnessFeatures((VkPhysicalDeviceImageRobustnessFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceImageRobustnessFeatures((VkPhysicalDeviceImageRobustnessFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_4444_FORMATS_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDevice4444FormatsFeaturesEXT((VkPhysicalDevice4444FormatsFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDevice4444FormatsFeaturesEXT((VkPhysicalDevice4444FormatsFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_TERMINATE_INVOCATION_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderTerminateInvocationFeatures((VkPhysicalDeviceShaderTerminateInvocationFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderTerminateInvocationFeatures((VkPhysicalDeviceShaderTerminateInvocationFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MUTABLE_DESCRIPTOR_TYPE_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT((VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT((VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_CONTROL_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceDepthClipControlFeaturesEXT((VkPhysicalDeviceDepthClipControlFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceDepthClipControlFeaturesEXT((VkPhysicalDeviceDepthClipControlFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_COLOR_WRITE_ENABLE_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceColorWriteEnableFeaturesEXT((VkPhysicalDeviceColorWriteEnableFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceColorWriteEnableFeaturesEXT((VkPhysicalDeviceColorWriteEnableFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SYNCHRONIZATION_2_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceSynchronization2Features((VkPhysicalDeviceSynchronization2Features*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceSynchronization2Features((VkPhysicalDeviceSynchronization2Features*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceProvokingVertexFeaturesEXT((VkPhysicalDeviceProvokingVertexFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceProvokingVertexFeaturesEXT((VkPhysicalDeviceProvokingVertexFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderIntegerDotProductFeatures((VkPhysicalDeviceShaderIntegerDotProductFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderIntegerDotProductFeatures((VkPhysicalDeviceShaderIntegerDotProductFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DYNAMIC_RENDERING_FEATURES: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceDynamicRenderingFeatures((VkPhysicalDeviceDynamicRenderingFeatures*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceDynamicRenderingFeatures((VkPhysicalDeviceDynamicRenderingFeatures*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT((VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT((VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_FEATURES_EXT: {
                    int itemSize = vt_sizeof_VkPhysicalDeviceMapMemoryPlacedFeaturesEXT((VkPhysicalDeviceMapMemoryPlacedFeaturesEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPhysicalDeviceMapMemoryPlacedFeaturesEXT((VkPhysicalDeviceMapMemoryPlacedFeaturesEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkDeviceCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->queueCreateInfoCount;
    bufferOffset += 4;

    if (val->pQueueCreateInfos) {
        int arrSize = val->queueCreateInfoCount ? val->queueCreateInfoCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkDeviceQueueCreateInfo(&val->pQueueCreateInfos[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkDeviceQueueCreateInfo(&val->pQueueCreateInfos[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->enabledLayerCount;
    bufferOffset += 4;

    if (val->ppEnabledLayerNames) {
        int arrSize = val->enabledLayerCount ? val->enabledLayerCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int strSize = strlen(val->ppEnabledLayerNames[i]) + 1;
            *(int*)(outputBuffer + bufferOffset) = strSize;
            bufferOffset += 4;
            memcpy(outputBuffer + bufferOffset, val->ppEnabledLayerNames[i], strSize);
            bufferOffset += strSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->enabledExtensionCount;
    bufferOffset += 4;

    if (val->ppEnabledExtensionNames) {
        int arrSize = val->enabledExtensionCount ? val->enabledExtensionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int strSize = strlen(val->ppEnabledExtensionNames[i]) + 1;
            *(int*)(outputBuffer + bufferOffset) = strSize;
            bufferOffset += 4;
            memcpy(outputBuffer + bufferOffset, val->ppEnabledExtensionNames[i], strSize);
            bufferOffset += strSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pEnabledFeatures) {
        int itemSize = vt_sizeof_VkPhysicalDeviceFeatures(val->pEnabledFeatures);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceFeatures(val->pEnabledFeatures, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDeviceCreateInfo(VkDeviceCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DEVICE_PRIVATE_DATA_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDevicePrivateDataCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DEVICE_PRIVATE_DATA_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDevicePrivateDataCreateInfo));
                    vt_unserialize_VkDevicePrivateDataCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PRIVATE_DATA_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevicePrivateDataFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PRIVATE_DATA_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevicePrivateDataFeatures));
                    vt_unserialize_VkPhysicalDevicePrivateDataFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FEATURES_2: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceFeatures2* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FEATURES_2) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceFeatures2));
                    vt_unserialize_VkPhysicalDeviceFeatures2(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VARIABLE_POINTERS_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVariablePointersFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VARIABLE_POINTERS_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVariablePointersFeatures));
                    vt_unserialize_VkPhysicalDeviceVariablePointersFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMultiviewFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MULTIVIEW_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMultiviewFeatures));
                    vt_unserialize_VkPhysicalDeviceMultiviewFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_DEVICE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDeviceGroupDeviceCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DEVICE_GROUP_DEVICE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDeviceGroupDeviceCreateInfo));
                    vt_unserialize_VkDeviceGroupDeviceCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_16BIT_STORAGE_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevice16BitStorageFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_16BIT_STORAGE_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevice16BitStorageFeatures));
                    vt_unserialize_VkPhysicalDevice16BitStorageFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_SUBGROUP_EXTENDED_TYPES_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_SUBGROUP_EXTENDED_TYPES_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures));
                    vt_unserialize_VkPhysicalDeviceShaderSubgroupExtendedTypesFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_YCBCR_CONVERSION_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSamplerYcbcrConversionFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLER_YCBCR_CONVERSION_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSamplerYcbcrConversionFeatures));
                    vt_unserialize_VkPhysicalDeviceSamplerYcbcrConversionFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceProtectedMemoryFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROTECTED_MEMORY_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceProtectedMemoryFeatures));
                    vt_unserialize_VkPhysicalDeviceProtectedMemoryFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceInlineUniformBlockFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_INLINE_UNIFORM_BLOCK_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceInlineUniformBlockFeatures));
                    vt_unserialize_VkPhysicalDeviceInlineUniformBlockFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMaintenance4Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAINTENANCE_4_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMaintenance4Features));
                    vt_unserialize_VkPhysicalDeviceMaintenance4Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DRAW_PARAMETERS_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderDrawParametersFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DRAW_PARAMETERS_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderDrawParametersFeatures));
                    vt_unserialize_VkPhysicalDeviceShaderDrawParametersFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_FLOAT16_INT8_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderFloat16Int8Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_FLOAT16_INT8_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderFloat16Int8Features));
                    vt_unserialize_VkPhysicalDeviceShaderFloat16Int8Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_HOST_QUERY_RESET_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceHostQueryResetFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_HOST_QUERY_RESET_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceHostQueryResetFeatures));
                    vt_unserialize_VkPhysicalDeviceHostQueryResetFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceDescriptorIndexingFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DESCRIPTOR_INDEXING_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceDescriptorIndexingFeatures));
                    vt_unserialize_VkPhysicalDeviceDescriptorIndexingFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceTimelineSemaphoreFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TIMELINE_SEMAPHORE_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceTimelineSemaphoreFeatures));
                    vt_unserialize_VkPhysicalDeviceTimelineSemaphoreFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_8BIT_STORAGE_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevice8BitStorageFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_8BIT_STORAGE_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevice8BitStorageFeatures));
                    vt_unserialize_VkPhysicalDevice8BitStorageFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONDITIONAL_RENDERING_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceConditionalRenderingFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CONDITIONAL_RENDERING_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceConditionalRenderingFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceConditionalRenderingFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_MEMORY_MODEL_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVulkanMemoryModelFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_MEMORY_MODEL_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVulkanMemoryModelFeatures));
                    vt_unserialize_VkPhysicalDeviceVulkanMemoryModelFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_ATOMIC_INT64_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderAtomicInt64Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_ATOMIC_INT64_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderAtomicInt64Features));
                    vt_unserialize_VkPhysicalDeviceShaderAtomicInt64Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_FEATURES_KHR: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VERTEX_ATTRIBUTE_DIVISOR_FEATURES_KHR) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR));
                    vt_unserialize_VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceTransformFeedbackFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceTransformFeedbackFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceTransformFeedbackFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SCALAR_BLOCK_LAYOUT_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceScalarBlockLayoutFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SCALAR_BLOCK_LAYOUT_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceScalarBlockLayoutFeatures));
                    vt_unserialize_VkPhysicalDeviceScalarBlockLayoutFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_UNIFORM_BUFFER_STANDARD_LAYOUT_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceUniformBufferStandardLayoutFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_UNIFORM_BUFFER_STANDARD_LAYOUT_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceUniformBufferStandardLayoutFeatures));
                    vt_unserialize_VkPhysicalDeviceUniformBufferStandardLayoutFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_ENABLE_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceDepthClipEnableFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_ENABLE_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceDepthClipEnableFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceDepthClipEnableFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BUFFER_DEVICE_ADDRESS_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceBufferDeviceAddressFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BUFFER_DEVICE_ADDRESS_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceBufferDeviceAddressFeatures));
                    vt_unserialize_VkPhysicalDeviceBufferDeviceAddressFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGELESS_FRAMEBUFFER_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceImagelessFramebufferFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGELESS_FRAMEBUFFER_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceImagelessFramebufferFeatures));
                    vt_unserialize_VkPhysicalDeviceImagelessFramebufferFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXTURE_COMPRESSION_ASTC_HDR_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceTextureCompressionASTCHDRFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXTURE_COMPRESSION_ASTC_HDR_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceTextureCompressionASTCHDRFeatures));
                    vt_unserialize_VkPhysicalDeviceTextureCompressionASTCHDRFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FRAGMENT_SHADER_INTERLOCK_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_FRAGMENT_SHADER_INTERLOCK_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceFragmentShaderInterlockFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SEPARATE_DEPTH_STENCIL_LAYOUTS_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SEPARATE_DEPTH_STENCIL_LAYOUTS_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures));
                    vt_unserialize_VkPhysicalDeviceSeparateDepthStencilLayoutsFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DEMOTE_TO_HELPER_INVOCATION_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_DEMOTE_TO_HELPER_INVOCATION_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures));
                    vt_unserialize_VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TEXEL_BUFFER_ALIGNMENT_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSubgroupSizeControlFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SUBGROUP_SIZE_CONTROL_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSubgroupSizeControlFeatures));
                    vt_unserialize_VkPhysicalDeviceSubgroupSizeControlFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PIPELINE_CREATION_CACHE_CONTROL_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevicePipelineCreationCacheControlFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PIPELINE_CREATION_CACHE_CONTROL_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevicePipelineCreationCacheControlFeatures));
                    vt_unserialize_VkPhysicalDevicePipelineCreationCacheControlFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVulkan11Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_1_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVulkan11Features));
                    vt_unserialize_VkPhysicalDeviceVulkan11Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVulkan12Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_2_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVulkan12Features));
                    vt_unserialize_VkPhysicalDeviceVulkan12Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceVulkan13Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_VULKAN_1_3_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceVulkan13Features));
                    vt_unserialize_VkPhysicalDeviceVulkan13Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceCustomBorderColorFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_CUSTOM_BORDER_COLOR_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceCustomBorderColorFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceCustomBorderColorFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BORDER_COLOR_SWIZZLE_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceBorderColorSwizzleFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_BORDER_COLOR_SWIZZLE_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceBorderColorSwizzleFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceBorderColorSwizzleFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceExtendedDynamicStateFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceExtendedDynamicStateFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceExtendedDynamicStateFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceExtendedDynamicState3FeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_EXTENDED_DYNAMIC_STATE_3_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceExtendedDynamicState3FeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceExtendedDynamicState3FeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ZERO_INITIALIZE_WORKGROUP_MEMORY_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ZERO_INITIALIZE_WORKGROUP_MEMORY_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures));
                    vt_unserialize_VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceRobustness2FeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceRobustness2FeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceRobustness2FeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGE_ROBUSTNESS_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceImageRobustnessFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_IMAGE_ROBUSTNESS_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceImageRobustnessFeatures));
                    vt_unserialize_VkPhysicalDeviceImageRobustnessFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_4444_FORMATS_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDevice4444FormatsFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_4444_FORMATS_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDevice4444FormatsFeaturesEXT));
                    vt_unserialize_VkPhysicalDevice4444FormatsFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_TERMINATE_INVOCATION_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderTerminateInvocationFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_TERMINATE_INVOCATION_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderTerminateInvocationFeatures));
                    vt_unserialize_VkPhysicalDeviceShaderTerminateInvocationFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MUTABLE_DESCRIPTOR_TYPE_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MUTABLE_DESCRIPTOR_TYPE_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceMutableDescriptorTypeFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_CONTROL_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceDepthClipControlFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DEPTH_CLIP_CONTROL_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceDepthClipControlFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceDepthClipControlFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_COLOR_WRITE_ENABLE_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceColorWriteEnableFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_COLOR_WRITE_ENABLE_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceColorWriteEnableFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceColorWriteEnableFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SYNCHRONIZATION_2_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceSynchronization2Features* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SYNCHRONIZATION_2_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceSynchronization2Features));
                    vt_unserialize_VkPhysicalDeviceSynchronization2Features(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceProvokingVertexFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_PROVOKING_VERTEX_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceProvokingVertexFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceProvokingVertexFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderIntegerDotProductFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_DOT_PRODUCT_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderIntegerDotProductFeatures));
                    vt_unserialize_VkPhysicalDeviceShaderIntegerDotProductFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DYNAMIC_RENDERING_FEATURES: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceDynamicRenderingFeatures* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_DYNAMIC_RENDERING_FEATURES) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceDynamicRenderingFeatures));
                    vt_unserialize_VkPhysicalDeviceDynamicRenderingFeatures(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_MODULE_IDENTIFIER_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceShaderModuleIdentifierFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_FEATURES_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPhysicalDeviceMapMemoryPlacedFeaturesEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_MAP_MEMORY_PLACED_FEATURES_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceMapMemoryPlacedFeaturesEXT));
                    vt_unserialize_VkPhysicalDeviceMapMemoryPlacedFeaturesEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkDeviceCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queueCreateInfoCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkDeviceQueueCreateInfo* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkDeviceQueueCreateInfo));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkDeviceQueueCreateInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pQueueCreateInfos = arrValues;
    }
    else val->pQueueCreateInfos = NULL;
    val->enabledLayerCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        char** arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(char*));
        for (int i = 0; i < dataSize7; i++) {
            int strSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            arrValues[i] = vt_alloc(memoryPool, strSize);
            memcpy(arrValues[i], inputBuffer + bufferOffset, strSize);
            bufferOffset += strSize;
        }
        val->ppEnabledLayerNames = (const char* const*)arrValues;
    }
    else val->ppEnabledLayerNames = NULL;
    val->enabledExtensionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        char** arrValues = vt_alloc(memoryPool, dataSize9 * sizeof(char*));
        for (int i = 0; i < dataSize9; i++) {
            int strSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            arrValues[i] = vt_alloc(memoryPool, strSize);
            memcpy(arrValues[i], inputBuffer + bufferOffset, strSize);
            bufferOffset += strSize;
        }
        val->ppEnabledExtensionNames = (const char* const*)arrValues;
    }
    else val->ppEnabledExtensionNames = NULL;

    int dataSize10 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize10 > 0) {
        val->pEnabledFeatures = vt_alloc(memoryPool, sizeof(VkPhysicalDeviceFeatures));
        vt_unserialize_VkPhysicalDeviceFeatures(val->pEnabledFeatures, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize10;
    }
    else val->pEnabledFeatures = NULL;
}

static inline int vt_sizeof_VkExtensionProperties(VkExtensionProperties* val) {
    int bufferSize = 0;
    bufferSize += strlen(val->extensionName) + 1;
    bufferSize += 4; // specVersion
    return bufferSize;
}

static inline void vt_serialize_VkExtensionProperties(VkExtensionProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    int dataSize1 = strlen(val->extensionName) + 1;
    *(int*)(outputBuffer + bufferOffset) = dataSize1;
    bufferOffset += 4;
    memcpy(outputBuffer + bufferOffset, val->extensionName, dataSize1);
    bufferOffset += dataSize1;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->specVersion;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkExtensionProperties(VkExtensionProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    int dataSize1 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize1 > 0) {
        memcpy(val->extensionName, inputBuffer + bufferOffset, dataSize1);
        bufferOffset += dataSize1;
    }
    val->specVersion = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSubmitInfo(VkSubmitInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_SUBMIT_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDeviceGroupSubmitInfo((VkDeviceGroupSubmitInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PROTECTED_SUBMIT_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkProtectedSubmitInfo((VkProtectedSubmitInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_TIMELINE_SEMAPHORE_SUBMIT_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkTimelineSemaphoreSubmitInfo((VkTimelineSemaphoreSubmitInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // waitSemaphoreCount
    bufferSize += 4;
    if (val->pWaitSemaphores) {
        for (int i = 0; i < val->waitSemaphoreCount; i++) {
            bufferSize += vt_sizeof_VkSemaphore(val->pWaitSemaphores[i]);
        }
    }
    bufferSize += 4;
    bufferSize += val->pWaitDstStageMask ? val->waitSemaphoreCount * 4 : 0;
    bufferSize += 4; // commandBufferCount
    bufferSize += 4;
    if (val->pCommandBuffers) {
        for (int i = 0; i < val->commandBufferCount; i++) {
            bufferSize += vt_sizeof_VkCommandBuffer(val->pCommandBuffers[i]);
        }
    }
    bufferSize += 4; // signalSemaphoreCount
    bufferSize += 4;
    if (val->pSignalSemaphores) {
        for (int i = 0; i < val->signalSemaphoreCount; i++) {
            bufferSize += vt_sizeof_VkSemaphore(val->pSignalSemaphores[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkSubmitInfo(VkSubmitInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DEVICE_GROUP_SUBMIT_INFO: {
                    int itemSize = vt_sizeof_VkDeviceGroupSubmitInfo((VkDeviceGroupSubmitInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDeviceGroupSubmitInfo((VkDeviceGroupSubmitInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PROTECTED_SUBMIT_INFO: {
                    int itemSize = vt_sizeof_VkProtectedSubmitInfo((VkProtectedSubmitInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkProtectedSubmitInfo((VkProtectedSubmitInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_TIMELINE_SEMAPHORE_SUBMIT_INFO: {
                    int itemSize = vt_sizeof_VkTimelineSemaphoreSubmitInfo((VkTimelineSemaphoreSubmitInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkTimelineSemaphoreSubmitInfo((VkTimelineSemaphoreSubmitInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->waitSemaphoreCount;
    bufferOffset += 4;

    if (val->pWaitSemaphores) {
        *(int*)(outputBuffer + bufferOffset) = val->waitSemaphoreCount;
        bufferOffset += 4;
        for (int i = 0; i < val->waitSemaphoreCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkSemaphore(val->pWaitSemaphores[i], outputBuffer + bufferOffset);
#else
            VkObject* waitSemaphoresObject = VkObject_fromHandle(val->pWaitSemaphores[i]);
            vt_serialize_VkSemaphore((VkSemaphore)&waitSemaphoresObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkSemaphore(val->pWaitSemaphores[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pWaitDstStageMask) {
        int arrSize = val->waitSemaphoreCount ? val->waitSemaphoreCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pWaitDstStageMask, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->commandBufferCount;
    bufferOffset += 4;

    if (val->pCommandBuffers) {
        *(int*)(outputBuffer + bufferOffset) = val->commandBufferCount;
        bufferOffset += 4;
        for (int i = 0; i < val->commandBufferCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkCommandBuffer(val->pCommandBuffers[i], outputBuffer + bufferOffset);
#else
            VkObject* commandBuffersObject = VkObject_fromHandle(val->pCommandBuffers[i]);
            vt_serialize_VkCommandBuffer((VkCommandBuffer)&commandBuffersObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkCommandBuffer(val->pCommandBuffers[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->signalSemaphoreCount;
    bufferOffset += 4;

    if (val->pSignalSemaphores) {
        *(int*)(outputBuffer + bufferOffset) = val->signalSemaphoreCount;
        bufferOffset += 4;
        for (int i = 0; i < val->signalSemaphoreCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkSemaphore(val->pSignalSemaphores[i], outputBuffer + bufferOffset);
#else
            VkObject* signalSemaphoresObject = VkObject_fromHandle(val->pSignalSemaphores[i]);
            vt_serialize_VkSemaphore((VkSemaphore)&signalSemaphoresObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkSemaphore(val->pSignalSemaphores[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkSubmitInfo(VkSubmitInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_SUBMIT_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDeviceGroupSubmitInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DEVICE_GROUP_SUBMIT_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDeviceGroupSubmitInfo));
                    vt_unserialize_VkDeviceGroupSubmitInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PROTECTED_SUBMIT_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkProtectedSubmitInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PROTECTED_SUBMIT_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkProtectedSubmitInfo));
                    vt_unserialize_VkProtectedSubmitInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_TIMELINE_SEMAPHORE_SUBMIT_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkTimelineSemaphoreSubmitInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_TIMELINE_SEMAPHORE_SUBMIT_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkTimelineSemaphoreSubmitInfo));
                    vt_unserialize_VkTimelineSemaphoreSubmitInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->waitSemaphoreCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkSemaphore* arrValues = vt_alloc(memoryPool, dataSize4 * sizeof(VkSemaphore));
        for (int i = 0; i < dataSize4; i++) {
            uint64_t waitSemaphoresId;
            vt_unserialize_VkSemaphore((VkSemaphore)&waitSemaphoresId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(waitSemaphoresId);
#else
                VkObject* waitSemaphoresObject = VkObject_create(VK_OBJECT_TYPE_SEMAPHORE, waitSemaphoresId);
                arrValues[i] = VkObject_toHandle(waitSemaphoresObject);
#endif
                bufferOffset += vt_sizeof_VkSemaphore(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkSemaphore(VK_NULL_HANDLE);
        }
        val->pWaitSemaphores = arrValues;
    }
    else val->pWaitSemaphores = NULL;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkPipelineStageFlags* arrValues = vt_alloc(memoryPool, dataSize5 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize5 * 4);
        bufferOffset += dataSize5 * 4;
        val->pWaitDstStageMask = arrValues;
    }
    else val->pWaitDstStageMask = NULL;
    val->commandBufferCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkCommandBuffer* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkCommandBuffer));
        for (int i = 0; i < dataSize7; i++) {
            uint64_t commandBuffersId;
            vt_unserialize_VkCommandBuffer((VkCommandBuffer)&commandBuffersId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(commandBuffersId);
#else
                VkObject* commandBuffersObject = VkObject_create(VK_OBJECT_TYPE_COMMAND_BUFFER, commandBuffersId);
                arrValues[i] = VkObject_toHandle(commandBuffersObject);
#endif
                bufferOffset += vt_sizeof_VkCommandBuffer(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
        }
        val->pCommandBuffers = arrValues;
    }
    else val->pCommandBuffers = NULL;
    val->signalSemaphoreCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        VkSemaphore* arrValues = vt_alloc(memoryPool, dataSize9 * sizeof(VkSemaphore));
        for (int i = 0; i < dataSize9; i++) {
            uint64_t signalSemaphoresId;
            vt_unserialize_VkSemaphore((VkSemaphore)&signalSemaphoresId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(signalSemaphoresId);
#else
                VkObject* signalSemaphoresObject = VkObject_create(VK_OBJECT_TYPE_SEMAPHORE, signalSemaphoresId);
                arrValues[i] = VkObject_toHandle(signalSemaphoresObject);
#endif
                bufferOffset += vt_sizeof_VkSemaphore(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkSemaphore(VK_NULL_HANDLE);
        }
        val->pSignalSemaphores = arrValues;
    }
    else val->pSignalSemaphores = NULL;
}

static inline int vt_sizeof_VkMemoryAllocateInfo(VkMemoryAllocateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_EXPORT_MEMORY_ALLOCATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkExportMemoryAllocateInfo((VkExportMemoryAllocateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_IMPORT_MEMORY_FD_INFO_KHR: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkImportMemoryFdInfoKHR((VkImportMemoryFdInfoKHR*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_MEMORY_ALLOCATE_FLAGS_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkMemoryAllocateFlagsInfo((VkMemoryAllocateFlagsInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_MEMORY_DEDICATED_ALLOCATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkMemoryDedicatedAllocateInfo((VkMemoryDedicatedAllocateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_MEMORY_OPAQUE_CAPTURE_ADDRESS_ALLOCATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkMemoryOpaqueCaptureAddressAllocateInfo((VkMemoryOpaqueCaptureAddressAllocateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 8; // allocationSize
    bufferSize += 4; // memoryTypeIndex
    return bufferSize;
}

static inline void vt_serialize_VkMemoryAllocateInfo(VkMemoryAllocateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_EXPORT_MEMORY_ALLOCATE_INFO: {
                    int itemSize = vt_sizeof_VkExportMemoryAllocateInfo((VkExportMemoryAllocateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkExportMemoryAllocateInfo((VkExportMemoryAllocateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_IMPORT_MEMORY_FD_INFO_KHR: {
                    int itemSize = vt_sizeof_VkImportMemoryFdInfoKHR((VkImportMemoryFdInfoKHR*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkImportMemoryFdInfoKHR((VkImportMemoryFdInfoKHR*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_MEMORY_ALLOCATE_FLAGS_INFO: {
                    int itemSize = vt_sizeof_VkMemoryAllocateFlagsInfo((VkMemoryAllocateFlagsInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkMemoryAllocateFlagsInfo((VkMemoryAllocateFlagsInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_MEMORY_DEDICATED_ALLOCATE_INFO: {
                    int itemSize = vt_sizeof_VkMemoryDedicatedAllocateInfo((VkMemoryDedicatedAllocateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkMemoryDedicatedAllocateInfo((VkMemoryDedicatedAllocateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_MEMORY_OPAQUE_CAPTURE_ADDRESS_ALLOCATE_INFO: {
                    int itemSize = vt_sizeof_VkMemoryOpaqueCaptureAddressAllocateInfo((VkMemoryOpaqueCaptureAddressAllocateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkMemoryOpaqueCaptureAddressAllocateInfo((VkMemoryOpaqueCaptureAddressAllocateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->allocationSize;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->memoryTypeIndex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkMemoryAllocateInfo(VkMemoryAllocateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_EXPORT_MEMORY_ALLOCATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkExportMemoryAllocateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_EXPORT_MEMORY_ALLOCATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkExportMemoryAllocateInfo));
                    vt_unserialize_VkExportMemoryAllocateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_IMPORT_MEMORY_FD_INFO_KHR: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkImportMemoryFdInfoKHR* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_IMPORT_MEMORY_FD_INFO_KHR) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkImportMemoryFdInfoKHR));
                    vt_unserialize_VkImportMemoryFdInfoKHR(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_MEMORY_ALLOCATE_FLAGS_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkMemoryAllocateFlagsInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_MEMORY_ALLOCATE_FLAGS_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkMemoryAllocateFlagsInfo));
                    vt_unserialize_VkMemoryAllocateFlagsInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_MEMORY_DEDICATED_ALLOCATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkMemoryDedicatedAllocateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_MEMORY_DEDICATED_ALLOCATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkMemoryDedicatedAllocateInfo));
                    vt_unserialize_VkMemoryDedicatedAllocateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_MEMORY_OPAQUE_CAPTURE_ADDRESS_ALLOCATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkMemoryOpaqueCaptureAddressAllocateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_MEMORY_OPAQUE_CAPTURE_ADDRESS_ALLOCATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkMemoryOpaqueCaptureAddressAllocateInfo));
                    vt_unserialize_VkMemoryOpaqueCaptureAddressAllocateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->allocationSize = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->memoryTypeIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkMappedMemoryRange(VkMappedMemoryRange* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkDeviceMemory(val->memory);
    bufferSize += 8; // offset
    bufferSize += 8; // size
    return bufferSize;
}

static inline void vt_serialize_VkMappedMemoryRange(VkMappedMemoryRange* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkDeviceMemory(val->memory, outputBuffer + bufferOffset);
#else
    VkObject* memoryObject = VkObject_fromHandle(val->memory);
    vt_serialize_VkDeviceMemory((VkDeviceMemory)&memoryObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->offset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkMappedMemoryRange(VkMappedMemoryRange* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t memoryId;
    vt_unserialize_VkDeviceMemory((VkDeviceMemory)&memoryId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->memory = vortekSerializerCastVkObject ? ((ResourceMemory*)VkObject_fromId(memoryId))->memory : VkObject_fromId(memoryId);
#else
    VkObject* memoryObject = VkObject_create(VK_OBJECT_TYPE_DEVICE_MEMORY, memoryId);
    val->memory = VkObject_toHandle(memoryObject);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    val->offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkBindSparseInfo(VkBindSparseInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_BIND_SPARSE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDeviceGroupBindSparseInfo((VkDeviceGroupBindSparseInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_TIMELINE_SEMAPHORE_SUBMIT_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkTimelineSemaphoreSubmitInfo((VkTimelineSemaphoreSubmitInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // waitSemaphoreCount
    bufferSize += 4;
    if (val->pWaitSemaphores) {
        for (int i = 0; i < val->waitSemaphoreCount; i++) {
            bufferSize += vt_sizeof_VkSemaphore(val->pWaitSemaphores[i]);
        }
    }
    bufferSize += 4; // bufferBindCount
    bufferSize += 4;
    if (val->pBufferBinds) {
        int arrSize = val->bufferBindCount ? val->bufferBindCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSparseBufferMemoryBindInfo(&val->pBufferBinds[i]);
        }
    }
    bufferSize += 4; // imageOpaqueBindCount
    bufferSize += 4;
    if (val->pImageOpaqueBinds) {
        int arrSize = val->imageOpaqueBindCount ? val->imageOpaqueBindCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSparseImageOpaqueMemoryBindInfo(&val->pImageOpaqueBinds[i]);
        }
    }
    bufferSize += 4; // imageBindCount
    bufferSize += 4;
    if (val->pImageBinds) {
        int arrSize = val->imageBindCount ? val->imageBindCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSparseImageMemoryBindInfo(&val->pImageBinds[i]);
        }
    }
    bufferSize += 4; // signalSemaphoreCount
    bufferSize += 4;
    if (val->pSignalSemaphores) {
        for (int i = 0; i < val->signalSemaphoreCount; i++) {
            bufferSize += vt_sizeof_VkSemaphore(val->pSignalSemaphores[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkBindSparseInfo(VkBindSparseInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DEVICE_GROUP_BIND_SPARSE_INFO: {
                    int itemSize = vt_sizeof_VkDeviceGroupBindSparseInfo((VkDeviceGroupBindSparseInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDeviceGroupBindSparseInfo((VkDeviceGroupBindSparseInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_TIMELINE_SEMAPHORE_SUBMIT_INFO: {
                    int itemSize = vt_sizeof_VkTimelineSemaphoreSubmitInfo((VkTimelineSemaphoreSubmitInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkTimelineSemaphoreSubmitInfo((VkTimelineSemaphoreSubmitInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->waitSemaphoreCount;
    bufferOffset += 4;

    if (val->pWaitSemaphores) {
        *(int*)(outputBuffer + bufferOffset) = val->waitSemaphoreCount;
        bufferOffset += 4;
        for (int i = 0; i < val->waitSemaphoreCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkSemaphore(val->pWaitSemaphores[i], outputBuffer + bufferOffset);
#else
            VkObject* waitSemaphoresObject = VkObject_fromHandle(val->pWaitSemaphores[i]);
            vt_serialize_VkSemaphore((VkSemaphore)&waitSemaphoresObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkSemaphore(val->pWaitSemaphores[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->bufferBindCount;
    bufferOffset += 4;

    if (val->pBufferBinds) {
        int arrSize = val->bufferBindCount ? val->bufferBindCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSparseBufferMemoryBindInfo(&val->pBufferBinds[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSparseBufferMemoryBindInfo(&val->pBufferBinds[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->imageOpaqueBindCount;
    bufferOffset += 4;

    if (val->pImageOpaqueBinds) {
        int arrSize = val->imageOpaqueBindCount ? val->imageOpaqueBindCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSparseImageOpaqueMemoryBindInfo(&val->pImageOpaqueBinds[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSparseImageOpaqueMemoryBindInfo(&val->pImageOpaqueBinds[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->imageBindCount;
    bufferOffset += 4;

    if (val->pImageBinds) {
        int arrSize = val->imageBindCount ? val->imageBindCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSparseImageMemoryBindInfo(&val->pImageBinds[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSparseImageMemoryBindInfo(&val->pImageBinds[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->signalSemaphoreCount;
    bufferOffset += 4;

    if (val->pSignalSemaphores) {
        *(int*)(outputBuffer + bufferOffset) = val->signalSemaphoreCount;
        bufferOffset += 4;
        for (int i = 0; i < val->signalSemaphoreCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkSemaphore(val->pSignalSemaphores[i], outputBuffer + bufferOffset);
#else
            VkObject* signalSemaphoresObject = VkObject_fromHandle(val->pSignalSemaphores[i]);
            vt_serialize_VkSemaphore((VkSemaphore)&signalSemaphoresObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkSemaphore(val->pSignalSemaphores[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkBindSparseInfo(VkBindSparseInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_BIND_SPARSE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDeviceGroupBindSparseInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DEVICE_GROUP_BIND_SPARSE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDeviceGroupBindSparseInfo));
                    vt_unserialize_VkDeviceGroupBindSparseInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_TIMELINE_SEMAPHORE_SUBMIT_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkTimelineSemaphoreSubmitInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_TIMELINE_SEMAPHORE_SUBMIT_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkTimelineSemaphoreSubmitInfo));
                    vt_unserialize_VkTimelineSemaphoreSubmitInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->waitSemaphoreCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkSemaphore* arrValues = vt_alloc(memoryPool, dataSize4 * sizeof(VkSemaphore));
        for (int i = 0; i < dataSize4; i++) {
            uint64_t waitSemaphoresId;
            vt_unserialize_VkSemaphore((VkSemaphore)&waitSemaphoresId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(waitSemaphoresId);
#else
                VkObject* waitSemaphoresObject = VkObject_create(VK_OBJECT_TYPE_SEMAPHORE, waitSemaphoresId);
                arrValues[i] = VkObject_toHandle(waitSemaphoresObject);
#endif
                bufferOffset += vt_sizeof_VkSemaphore(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkSemaphore(VK_NULL_HANDLE);
        }
        val->pWaitSemaphores = arrValues;
    }
    else val->pWaitSemaphores = NULL;
    val->bufferBindCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkSparseBufferMemoryBindInfo* arrValues = vt_alloc(memoryPool, dataSize6 * sizeof(VkSparseBufferMemoryBindInfo));
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSparseBufferMemoryBindInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pBufferBinds = arrValues;
    }
    else val->pBufferBinds = NULL;
    val->imageOpaqueBindCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        VkSparseImageOpaqueMemoryBindInfo* arrValues = vt_alloc(memoryPool, dataSize8 * sizeof(VkSparseImageOpaqueMemoryBindInfo));
        for (int i = 0; i < dataSize8; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSparseImageOpaqueMemoryBindInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pImageOpaqueBinds = arrValues;
    }
    else val->pImageOpaqueBinds = NULL;
    val->imageBindCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize10 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize10 > 0) {
        VkSparseImageMemoryBindInfo* arrValues = vt_alloc(memoryPool, dataSize10 * sizeof(VkSparseImageMemoryBindInfo));
        for (int i = 0; i < dataSize10; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSparseImageMemoryBindInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pImageBinds = arrValues;
    }
    else val->pImageBinds = NULL;
    val->signalSemaphoreCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize12 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize12 > 0) {
        VkSemaphore* arrValues = vt_alloc(memoryPool, dataSize12 * sizeof(VkSemaphore));
        for (int i = 0; i < dataSize12; i++) {
            uint64_t signalSemaphoresId;
            vt_unserialize_VkSemaphore((VkSemaphore)&signalSemaphoresId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(signalSemaphoresId);
#else
                VkObject* signalSemaphoresObject = VkObject_create(VK_OBJECT_TYPE_SEMAPHORE, signalSemaphoresId);
                arrValues[i] = VkObject_toHandle(signalSemaphoresObject);
#endif
                bufferOffset += vt_sizeof_VkSemaphore(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkSemaphore(VK_NULL_HANDLE);
        }
        val->pSignalSemaphores = arrValues;
    }
    else val->pSignalSemaphores = NULL;
}

static inline int vt_sizeof_VkFenceCreateInfo(VkFenceCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_EXPORT_FENCE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkExportFenceCreateInfo((VkExportFenceCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_VkFenceCreateInfo(VkFenceCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_EXPORT_FENCE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkExportFenceCreateInfo((VkExportFenceCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkExportFenceCreateInfo((VkExportFenceCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkFenceCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkFenceCreateInfo(VkFenceCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_EXPORT_FENCE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkExportFenceCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_EXPORT_FENCE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkExportFenceCreateInfo));
                    vt_unserialize_VkExportFenceCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkFenceCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSemaphoreCreateInfo(VkSemaphoreCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_EXPORT_SEMAPHORE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkExportSemaphoreCreateInfo((VkExportSemaphoreCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_SEMAPHORE_TYPE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkSemaphoreTypeCreateInfo((VkSemaphoreTypeCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_VkSemaphoreCreateInfo(VkSemaphoreCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_EXPORT_SEMAPHORE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkExportSemaphoreCreateInfo((VkExportSemaphoreCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkExportSemaphoreCreateInfo((VkExportSemaphoreCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_SEMAPHORE_TYPE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkSemaphoreTypeCreateInfo((VkSemaphoreTypeCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkSemaphoreTypeCreateInfo((VkSemaphoreTypeCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkSemaphoreCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSemaphoreCreateInfo(VkSemaphoreCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_EXPORT_SEMAPHORE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkExportSemaphoreCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_EXPORT_SEMAPHORE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkExportSemaphoreCreateInfo));
                    vt_unserialize_VkExportSemaphoreCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_SEMAPHORE_TYPE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkSemaphoreTypeCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SEMAPHORE_TYPE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkSemaphoreTypeCreateInfo));
                    vt_unserialize_VkSemaphoreTypeCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkSemaphoreCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkEventCreateInfo(VkEventCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_VkEventCreateInfo(VkEventCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkEventCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkEventCreateInfo(VkEventCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkEventCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkQueryPoolCreateInfo(VkQueryPoolCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // queryType
    bufferSize += 4; // queryCount
    bufferSize += 4; // pipelineStatistics
    return bufferSize;
}

static inline void vt_serialize_VkQueryPoolCreateInfo(VkQueryPoolCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkQueryPoolCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkQueryType*)(outputBuffer + bufferOffset) = val->queryType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->queryCount;
    bufferOffset += 4;
    *(VkQueryPipelineStatisticFlags*)(outputBuffer + bufferOffset) = val->pipelineStatistics;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkQueryPoolCreateInfo(VkQueryPoolCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkQueryPoolCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queryType = *(VkQueryType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queryCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->pipelineStatistics = *(VkQueryPipelineStatisticFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkBufferCreateInfo(VkBufferCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_EXTERNAL_MEMORY_BUFFER_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkExternalMemoryBufferCreateInfo((VkExternalMemoryBufferCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_BUFFER_OPAQUE_CAPTURE_ADDRESS_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkBufferOpaqueCaptureAddressCreateInfo((VkBufferOpaqueCaptureAddressCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 8; // size
    bufferSize += 4; // usage
    bufferSize += 4; // sharingMode
    bufferSize += 4; // queueFamilyIndexCount
    bufferSize += 4;
    bufferSize += val->pQueueFamilyIndices ? val->queueFamilyIndexCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_VkBufferCreateInfo(VkBufferCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_EXTERNAL_MEMORY_BUFFER_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkExternalMemoryBufferCreateInfo((VkExternalMemoryBufferCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkExternalMemoryBufferCreateInfo((VkExternalMemoryBufferCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_BUFFER_OPAQUE_CAPTURE_ADDRESS_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkBufferOpaqueCaptureAddressCreateInfo((VkBufferOpaqueCaptureAddressCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkBufferOpaqueCaptureAddressCreateInfo((VkBufferOpaqueCaptureAddressCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkBufferCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 8;
    *(VkBufferUsageFlags*)(outputBuffer + bufferOffset) = val->usage;
    bufferOffset += 4;
    *(VkSharingMode*)(outputBuffer + bufferOffset) = val->sharingMode;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->queueFamilyIndexCount;
    bufferOffset += 4;

    if (val->pQueueFamilyIndices) {
        int arrSize = val->queueFamilyIndexCount ? val->queueFamilyIndexCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pQueueFamilyIndices, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkBufferCreateInfo(VkBufferCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_EXTERNAL_MEMORY_BUFFER_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkExternalMemoryBufferCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_EXTERNAL_MEMORY_BUFFER_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkExternalMemoryBufferCreateInfo));
                    vt_unserialize_VkExternalMemoryBufferCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_BUFFER_OPAQUE_CAPTURE_ADDRESS_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkBufferOpaqueCaptureAddressCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_BUFFER_OPAQUE_CAPTURE_ADDRESS_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkBufferOpaqueCaptureAddressCreateInfo));
                    vt_unserialize_VkBufferOpaqueCaptureAddressCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkBufferCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->usage = *(VkBufferUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sharingMode = *(VkSharingMode*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queueFamilyIndexCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize8 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize8 * 4);
        bufferOffset += dataSize8 * 4;
        val->pQueueFamilyIndices = arrValues;
    }
    else val->pQueueFamilyIndices = NULL;
}

static inline int vt_sizeof_VkBufferViewCreateInfo(VkBufferViewCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += vt_sizeof_VkBuffer(val->buffer);
    bufferSize += 4; // format
    bufferSize += 8; // offset
    bufferSize += 8; // range
    return bufferSize;
}

static inline void vt_serialize_VkBufferViewCreateInfo(VkBufferViewCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBufferViewCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->buffer, outputBuffer + bufferOffset);
#else
    VkObject* bufferObject = VkObject_fromHandle(val->buffer);
    vt_serialize_VkBuffer((VkBuffer)&bufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    *(VkFormat*)(outputBuffer + bufferOffset) = val->format;
    bufferOffset += 4;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->offset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->range;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkBufferViewCreateInfo(VkBufferViewCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkBufferViewCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t bufferId;
    vt_unserialize_VkBuffer((VkBuffer)&bufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->buffer = VkObject_fromId(bufferId);
#else
    VkObject* bufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, bufferId);
    val->buffer = VkObject_toHandle(bufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    val->format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->range = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkImageCreateInfo(VkImageCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_EXTERNAL_MEMORY_IMAGE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkExternalMemoryImageCreateInfo((VkExternalMemoryImageCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_SWAPCHAIN_CREATE_INFO_KHR: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkImageSwapchainCreateInfoKHR((VkImageSwapchainCreateInfoKHR*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkImageFormatListCreateInfo((VkImageFormatListCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_STENCIL_USAGE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkImageStencilUsageCreateInfo((VkImageStencilUsageCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // imageType
    bufferSize += 4; // format
    bufferSize += vt_sizeof_VkExtent3D(&val->extent);
    bufferSize += 4; // mipLevels
    bufferSize += 4; // arrayLayers
    bufferSize += 4; // samples
    bufferSize += 4; // tiling
    bufferSize += 4; // usage
    bufferSize += 4; // sharingMode
    bufferSize += 4; // queueFamilyIndexCount
    bufferSize += 4;
    bufferSize += val->pQueueFamilyIndices ? val->queueFamilyIndexCount * 4 : 0;
    bufferSize += 4; // initialLayout
    return bufferSize;
}

static inline void vt_serialize_VkImageCreateInfo(VkImageCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_EXTERNAL_MEMORY_IMAGE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkExternalMemoryImageCreateInfo((VkExternalMemoryImageCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkExternalMemoryImageCreateInfo((VkExternalMemoryImageCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_IMAGE_SWAPCHAIN_CREATE_INFO_KHR: {
                    int itemSize = vt_sizeof_VkImageSwapchainCreateInfoKHR((VkImageSwapchainCreateInfoKHR*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkImageSwapchainCreateInfoKHR((VkImageSwapchainCreateInfoKHR*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkImageFormatListCreateInfo((VkImageFormatListCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkImageFormatListCreateInfo((VkImageFormatListCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_IMAGE_STENCIL_USAGE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkImageStencilUsageCreateInfo((VkImageStencilUsageCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkImageStencilUsageCreateInfo((VkImageStencilUsageCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkImageCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkImageType*)(outputBuffer + bufferOffset) = val->imageType;
    bufferOffset += 4;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->format;
    bufferOffset += 4;
    vt_serialize_VkExtent3D(&val->extent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->mipLevels;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->arrayLayers;
    bufferOffset += 4;
    *(VkSampleCountFlagBits*)(outputBuffer + bufferOffset) = val->samples;
    bufferOffset += 4;
    *(VkImageTiling*)(outputBuffer + bufferOffset) = val->tiling;
    bufferOffset += 4;
    *(VkImageUsageFlags*)(outputBuffer + bufferOffset) = val->usage;
    bufferOffset += 4;
    *(VkSharingMode*)(outputBuffer + bufferOffset) = val->sharingMode;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->queueFamilyIndexCount;
    bufferOffset += 4;

    if (val->pQueueFamilyIndices) {
        int arrSize = val->queueFamilyIndexCount ? val->queueFamilyIndexCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pQueueFamilyIndices, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->initialLayout;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkImageCreateInfo(VkImageCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_EXTERNAL_MEMORY_IMAGE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkExternalMemoryImageCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_EXTERNAL_MEMORY_IMAGE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkExternalMemoryImageCreateInfo));
                    vt_unserialize_VkExternalMemoryImageCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_SWAPCHAIN_CREATE_INFO_KHR: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkImageSwapchainCreateInfoKHR* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_IMAGE_SWAPCHAIN_CREATE_INFO_KHR) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkImageSwapchainCreateInfoKHR));
                    vt_unserialize_VkImageSwapchainCreateInfoKHR(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkImageFormatListCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkImageFormatListCreateInfo));
                    vt_unserialize_VkImageFormatListCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_IMAGE_STENCIL_USAGE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkImageStencilUsageCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_IMAGE_STENCIL_USAGE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkImageStencilUsageCreateInfo));
                    vt_unserialize_VkImageStencilUsageCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkImageCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->imageType = *(VkImageType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkExtent3D(&val->extent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);
    val->mipLevels = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->arrayLayers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->samples = *(VkSampleCountFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->tiling = *(VkImageTiling*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->usage = *(VkImageUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->sharingMode = *(VkSharingMode*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queueFamilyIndexCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize14 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize14 > 0) {
        uint32_t* arrValues = vt_alloc(memoryPool, dataSize14 * 4);
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize14 * 4);
        bufferOffset += dataSize14 * 4;
        val->pQueueFamilyIndices = arrValues;
    }
    else val->pQueueFamilyIndices = NULL;
    val->initialLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSubresourceLayout(VkSubresourceLayout* val) {
    int bufferSize = 0;
    bufferSize += 8; // offset
    bufferSize += 8; // size
    bufferSize += 8; // rowPitch
    bufferSize += 8; // arrayPitch
    bufferSize += 8; // depthPitch
    return bufferSize;
}

static inline void vt_serialize_VkSubresourceLayout(VkSubresourceLayout* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->offset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->rowPitch;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->arrayPitch;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->depthPitch;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkSubresourceLayout(VkSubresourceLayout* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->rowPitch = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->arrayPitch = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->depthPitch = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkImageViewCreateInfo(VkImageViewCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_IMAGE_VIEW_USAGE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkImageViewUsageCreateInfo((VkImageViewUsageCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkSamplerYcbcrConversionInfo((VkSamplerYcbcrConversionInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += vt_sizeof_VkImage(val->image);
    bufferSize += 4; // viewType
    bufferSize += 4; // format
    bufferSize += vt_sizeof_VkComponentMapping(&val->components);
    bufferSize += vt_sizeof_VkImageSubresourceRange(&val->subresourceRange);
    return bufferSize;
}

static inline void vt_serialize_VkImageViewCreateInfo(VkImageViewCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_IMAGE_VIEW_USAGE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkImageViewUsageCreateInfo((VkImageViewUsageCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkImageViewUsageCreateInfo((VkImageViewUsageCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_INFO: {
                    int itemSize = vt_sizeof_VkSamplerYcbcrConversionInfo((VkSamplerYcbcrConversionInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkSamplerYcbcrConversionInfo((VkSamplerYcbcrConversionInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkImageViewCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->image, outputBuffer + bufferOffset);
#else
    VkObject* imageObject = VkObject_fromHandle(val->image);
    vt_serialize_VkImage((VkImage)&imageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
    *(VkImageViewType*)(outputBuffer + bufferOffset) = val->viewType;
    bufferOffset += 4;
    *(VkFormat*)(outputBuffer + bufferOffset) = val->format;
    bufferOffset += 4;
    vt_serialize_VkComponentMapping(&val->components, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkComponentMapping(&val->components);
    vt_serialize_VkImageSubresourceRange(&val->subresourceRange, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceRange(&val->subresourceRange);
}

static inline void vt_unserialize_VkImageViewCreateInfo(VkImageViewCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_IMAGE_VIEW_USAGE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkImageViewUsageCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_IMAGE_VIEW_USAGE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkImageViewUsageCreateInfo));
                    vt_unserialize_VkImageViewUsageCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkSamplerYcbcrConversionInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkSamplerYcbcrConversionInfo));
                    vt_unserialize_VkSamplerYcbcrConversionInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkImageViewCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t imageId;
    vt_unserialize_VkImage((VkImage)&imageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->image = VkObject_fromId(imageId);
#else
    VkObject* imageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, imageId);
    val->image = VkObject_toHandle(imageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
    val->viewType = *(VkImageViewType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkComponentMapping(&val->components, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkComponentMapping(&val->components);
    vt_unserialize_VkImageSubresourceRange(&val->subresourceRange, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceRange(&val->subresourceRange);
}

static inline int vt_sizeof_VkPipelineCacheCreateInfo(VkPipelineCacheCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 8; // initialDataSize
    bufferSize += 4;
    bufferSize += val->pInitialData ? val->initialDataSize : 0;
    return bufferSize;
}

static inline void vt_serialize_VkPipelineCacheCreateInfo(VkPipelineCacheCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkPipelineCacheCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(size_t*)(outputBuffer + bufferOffset) = val->initialDataSize;
    bufferOffset += 8;

    if (val->pInitialData) {
        *(int*)(outputBuffer + bufferOffset) = val->initialDataSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, val->pInitialData, val->initialDataSize);
        bufferOffset += val->initialDataSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkPipelineCacheCreateInfo(VkPipelineCacheCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkPipelineCacheCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->initialDataSize = *(size_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        val->pInitialData = vt_alloc(memoryPool, dataSize5);
        memcpy(val->pInitialData, inputBuffer + bufferOffset, dataSize5);
        bufferOffset += dataSize5;
    }
    else val->pInitialData = NULL;
}

static inline int vt_sizeof_VkGraphicsPipelineCreateInfo(VkGraphicsPipelineCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PIPELINE_CREATION_FEEDBACK_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineCreationFeedbackCreateInfo((VkPipelineCreationFeedbackCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_RENDERING_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineRenderingCreateInfo((VkPipelineRenderingCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // stageCount
    bufferSize += 4;
    if (val->pStages) {
        int arrSize = val->stageCount ? val->stageCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkPipelineShaderStageCreateInfo(&val->pStages[i]);
        }
    }
    bufferSize += 4;
    bufferSize += val->pVertexInputState ? vt_sizeof_VkPipelineVertexInputStateCreateInfo(val->pVertexInputState) : 0;
    bufferSize += 4;
    bufferSize += val->pInputAssemblyState ? vt_sizeof_VkPipelineInputAssemblyStateCreateInfo(val->pInputAssemblyState) : 0;
    bufferSize += 4;
    bufferSize += val->pTessellationState ? vt_sizeof_VkPipelineTessellationStateCreateInfo(val->pTessellationState) : 0;
    bufferSize += 4;
    bufferSize += val->pViewportState ? vt_sizeof_VkPipelineViewportStateCreateInfo(val->pViewportState) : 0;
    bufferSize += 4;
    bufferSize += val->pRasterizationState ? vt_sizeof_VkPipelineRasterizationStateCreateInfo(val->pRasterizationState) : 0;
    bufferSize += 4;
    bufferSize += val->pMultisampleState ? vt_sizeof_VkPipelineMultisampleStateCreateInfo(val->pMultisampleState) : 0;
    bufferSize += 4;
    bufferSize += val->pDepthStencilState ? vt_sizeof_VkPipelineDepthStencilStateCreateInfo(val->pDepthStencilState) : 0;
    bufferSize += 4;
    bufferSize += val->pColorBlendState ? vt_sizeof_VkPipelineColorBlendStateCreateInfo(val->pColorBlendState) : 0;
    bufferSize += 4;
    bufferSize += val->pDynamicState ? vt_sizeof_VkPipelineDynamicStateCreateInfo(val->pDynamicState) : 0;
    bufferSize += vt_sizeof_VkPipelineLayout(val->layout);
    bufferSize += vt_sizeof_VkRenderPass(val->renderPass);
    bufferSize += 4; // subpass
    bufferSize += vt_sizeof_VkPipeline(val->basePipelineHandle);
    bufferSize += 4; // basePipelineIndex
    return bufferSize;
}

static inline void vt_serialize_VkGraphicsPipelineCreateInfo(VkGraphicsPipelineCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PIPELINE_CREATION_FEEDBACK_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkPipelineCreationFeedbackCreateInfo((VkPipelineCreationFeedbackCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineCreationFeedbackCreateInfo((VkPipelineCreationFeedbackCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_PIPELINE_RENDERING_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkPipelineRenderingCreateInfo((VkPipelineRenderingCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineRenderingCreateInfo((VkPipelineRenderingCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkPipelineCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->stageCount;
    bufferOffset += 4;

    if (val->pStages) {
        int arrSize = val->stageCount ? val->stageCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkPipelineShaderStageCreateInfo(&val->pStages[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkPipelineShaderStageCreateInfo(&val->pStages[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pVertexInputState) {
        int itemSize = vt_sizeof_VkPipelineVertexInputStateCreateInfo(val->pVertexInputState);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineVertexInputStateCreateInfo(val->pVertexInputState, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pInputAssemblyState) {
        int itemSize = vt_sizeof_VkPipelineInputAssemblyStateCreateInfo(val->pInputAssemblyState);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineInputAssemblyStateCreateInfo(val->pInputAssemblyState, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pTessellationState) {
        int itemSize = vt_sizeof_VkPipelineTessellationStateCreateInfo(val->pTessellationState);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineTessellationStateCreateInfo(val->pTessellationState, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pViewportState) {
        int itemSize = vt_sizeof_VkPipelineViewportStateCreateInfo(val->pViewportState);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineViewportStateCreateInfo(val->pViewportState, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pRasterizationState) {
        int itemSize = vt_sizeof_VkPipelineRasterizationStateCreateInfo(val->pRasterizationState);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineRasterizationStateCreateInfo(val->pRasterizationState, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pMultisampleState) {
        int itemSize = vt_sizeof_VkPipelineMultisampleStateCreateInfo(val->pMultisampleState);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineMultisampleStateCreateInfo(val->pMultisampleState, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pDepthStencilState) {
        int itemSize = vt_sizeof_VkPipelineDepthStencilStateCreateInfo(val->pDepthStencilState);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineDepthStencilStateCreateInfo(val->pDepthStencilState, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pColorBlendState) {
        int itemSize = vt_sizeof_VkPipelineColorBlendStateCreateInfo(val->pColorBlendState);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineColorBlendStateCreateInfo(val->pColorBlendState, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pDynamicState) {
        int itemSize = vt_sizeof_VkPipelineDynamicStateCreateInfo(val->pDynamicState);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineDynamicStateCreateInfo(val->pDynamicState, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

#ifdef VT_SERVER
    vt_serialize_VkPipelineLayout(val->layout, outputBuffer + bufferOffset);
#else
    VkObject* layoutObject = VkObject_fromHandle(val->layout);
    vt_serialize_VkPipelineLayout((VkPipelineLayout)&layoutObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkPipelineLayout(val->layout);

#ifdef VT_SERVER
    vt_serialize_VkRenderPass(val->renderPass, outputBuffer + bufferOffset);
#else
    VkObject* renderPassObject = VkObject_fromHandle(val->renderPass);
    vt_serialize_VkRenderPass((VkRenderPass)&renderPassObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkRenderPass(val->renderPass);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subpass;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkPipeline(val->basePipelineHandle, outputBuffer + bufferOffset);
#else
    VkObject* basePipelineHandleObject = VkObject_fromHandle(val->basePipelineHandle);
    vt_serialize_VkPipeline((VkPipeline)&basePipelineHandleObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkPipeline(val->basePipelineHandle);
    *(int32_t*)(outputBuffer + bufferOffset) = val->basePipelineIndex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkGraphicsPipelineCreateInfo(VkGraphicsPipelineCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PIPELINE_CREATION_FEEDBACK_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineCreationFeedbackCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_CREATION_FEEDBACK_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineCreationFeedbackCreateInfo));
                    vt_unserialize_VkPipelineCreationFeedbackCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_PIPELINE_RENDERING_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineRenderingCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_RENDERING_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineRenderingCreateInfo));
                    vt_unserialize_VkPipelineRenderingCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkPipelineCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->stageCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkPipelineShaderStageCreateInfo* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkPipelineShaderStageCreateInfo));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkPipelineShaderStageCreateInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pStages = arrValues;
    }
    else val->pStages = NULL;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        val->pVertexInputState = vt_alloc(memoryPool, sizeof(VkPipelineVertexInputStateCreateInfo));
        vt_unserialize_VkPipelineVertexInputStateCreateInfo(val->pVertexInputState, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize6;
    }
    else val->pVertexInputState = NULL;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        val->pInputAssemblyState = vt_alloc(memoryPool, sizeof(VkPipelineInputAssemblyStateCreateInfo));
        vt_unserialize_VkPipelineInputAssemblyStateCreateInfo(val->pInputAssemblyState, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize7;
    }
    else val->pInputAssemblyState = NULL;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        val->pTessellationState = vt_alloc(memoryPool, sizeof(VkPipelineTessellationStateCreateInfo));
        vt_unserialize_VkPipelineTessellationStateCreateInfo(val->pTessellationState, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize8;
    }
    else val->pTessellationState = NULL;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        val->pViewportState = vt_alloc(memoryPool, sizeof(VkPipelineViewportStateCreateInfo));
        vt_unserialize_VkPipelineViewportStateCreateInfo(val->pViewportState, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize9;
    }
    else val->pViewportState = NULL;

    int dataSize10 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize10 > 0) {
        val->pRasterizationState = vt_alloc(memoryPool, sizeof(VkPipelineRasterizationStateCreateInfo));
        vt_unserialize_VkPipelineRasterizationStateCreateInfo(val->pRasterizationState, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize10;
    }
    else val->pRasterizationState = NULL;

    int dataSize11 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize11 > 0) {
        val->pMultisampleState = vt_alloc(memoryPool, sizeof(VkPipelineMultisampleStateCreateInfo));
        vt_unserialize_VkPipelineMultisampleStateCreateInfo(val->pMultisampleState, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize11;
    }
    else val->pMultisampleState = NULL;

    int dataSize12 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize12 > 0) {
        val->pDepthStencilState = vt_alloc(memoryPool, sizeof(VkPipelineDepthStencilStateCreateInfo));
        vt_unserialize_VkPipelineDepthStencilStateCreateInfo(val->pDepthStencilState, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize12;
    }
    else val->pDepthStencilState = NULL;

    int dataSize13 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize13 > 0) {
        val->pColorBlendState = vt_alloc(memoryPool, sizeof(VkPipelineColorBlendStateCreateInfo));
        vt_unserialize_VkPipelineColorBlendStateCreateInfo(val->pColorBlendState, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize13;
    }
    else val->pColorBlendState = NULL;

    int dataSize14 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize14 > 0) {
        val->pDynamicState = vt_alloc(memoryPool, sizeof(VkPipelineDynamicStateCreateInfo));
        vt_unserialize_VkPipelineDynamicStateCreateInfo(val->pDynamicState, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize14;
    }
    else val->pDynamicState = NULL;

    uint64_t layoutId;
    vt_unserialize_VkPipelineLayout((VkPipelineLayout)&layoutId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->layout = VkObject_fromId(layoutId);
#else
    VkObject* layoutObject = VkObject_create(VK_OBJECT_TYPE_PIPELINE_LAYOUT, layoutId);
    val->layout = VkObject_toHandle(layoutObject);
#endif
    bufferOffset += vt_sizeof_VkPipelineLayout(val->layout);

    uint64_t renderPassId;
    vt_unserialize_VkRenderPass((VkRenderPass)&renderPassId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->renderPass = VkObject_fromId(renderPassId);
#else
    VkObject* renderPassObject = VkObject_create(VK_OBJECT_TYPE_RENDER_PASS, renderPassId);
    val->renderPass = VkObject_toHandle(renderPassObject);
#endif
    bufferOffset += vt_sizeof_VkRenderPass(val->renderPass);
    val->subpass = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t basePipelineHandleId;
    vt_unserialize_VkPipeline((VkPipeline)&basePipelineHandleId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->basePipelineHandle = VkObject_fromId(basePipelineHandleId);
#else
    VkObject* basePipelineHandleObject = VkObject_create(VK_OBJECT_TYPE_PIPELINE, basePipelineHandleId);
    val->basePipelineHandle = VkObject_toHandle(basePipelineHandleObject);
#endif
    bufferOffset += vt_sizeof_VkPipeline(val->basePipelineHandle);
    val->basePipelineIndex = *(int32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkComputePipelineCreateInfo(VkComputePipelineCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_PIPELINE_CREATION_FEEDBACK_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkPipelineCreationFeedbackCreateInfo((VkPipelineCreationFeedbackCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += vt_sizeof_VkPipelineShaderStageCreateInfo(&val->stage);
    bufferSize += vt_sizeof_VkPipelineLayout(val->layout);
    bufferSize += vt_sizeof_VkPipeline(val->basePipelineHandle);
    bufferSize += 4; // basePipelineIndex
    return bufferSize;
}

static inline void vt_serialize_VkComputePipelineCreateInfo(VkComputePipelineCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_PIPELINE_CREATION_FEEDBACK_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkPipelineCreationFeedbackCreateInfo((VkPipelineCreationFeedbackCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkPipelineCreationFeedbackCreateInfo((VkPipelineCreationFeedbackCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkPipelineCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    vt_serialize_VkPipelineShaderStageCreateInfo(&val->stage, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkPipelineShaderStageCreateInfo(&val->stage);

#ifdef VT_SERVER
    vt_serialize_VkPipelineLayout(val->layout, outputBuffer + bufferOffset);
#else
    VkObject* layoutObject = VkObject_fromHandle(val->layout);
    vt_serialize_VkPipelineLayout((VkPipelineLayout)&layoutObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkPipelineLayout(val->layout);

#ifdef VT_SERVER
    vt_serialize_VkPipeline(val->basePipelineHandle, outputBuffer + bufferOffset);
#else
    VkObject* basePipelineHandleObject = VkObject_fromHandle(val->basePipelineHandle);
    vt_serialize_VkPipeline((VkPipeline)&basePipelineHandleObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkPipeline(val->basePipelineHandle);
    *(int32_t*)(outputBuffer + bufferOffset) = val->basePipelineIndex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkComputePipelineCreateInfo(VkComputePipelineCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_PIPELINE_CREATION_FEEDBACK_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkPipelineCreationFeedbackCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_PIPELINE_CREATION_FEEDBACK_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkPipelineCreationFeedbackCreateInfo));
                    vt_unserialize_VkPipelineCreationFeedbackCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkPipelineCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkPipelineShaderStageCreateInfo(&val->stage, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkPipelineShaderStageCreateInfo(&val->stage);

    uint64_t layoutId;
    vt_unserialize_VkPipelineLayout((VkPipelineLayout)&layoutId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->layout = VkObject_fromId(layoutId);
#else
    VkObject* layoutObject = VkObject_create(VK_OBJECT_TYPE_PIPELINE_LAYOUT, layoutId);
    val->layout = VkObject_toHandle(layoutObject);
#endif
    bufferOffset += vt_sizeof_VkPipelineLayout(val->layout);

    uint64_t basePipelineHandleId;
    vt_unserialize_VkPipeline((VkPipeline)&basePipelineHandleId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->basePipelineHandle = VkObject_fromId(basePipelineHandleId);
#else
    VkObject* basePipelineHandleObject = VkObject_create(VK_OBJECT_TYPE_PIPELINE, basePipelineHandleId);
    val->basePipelineHandle = VkObject_toHandle(basePipelineHandleObject);
#endif
    bufferOffset += vt_sizeof_VkPipeline(val->basePipelineHandle);
    val->basePipelineIndex = *(int32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkSamplerCreateInfo(VkSamplerCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkSamplerYcbcrConversionInfo((VkSamplerYcbcrConversionInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_SAMPLER_REDUCTION_MODE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkSamplerReductionModeCreateInfo((VkSamplerReductionModeCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_SAMPLER_CUSTOM_BORDER_COLOR_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkSamplerCustomBorderColorCreateInfoEXT((VkSamplerCustomBorderColorCreateInfoEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_SAMPLER_BORDER_COLOR_COMPONENT_MAPPING_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkSamplerBorderColorComponentMappingCreateInfoEXT((VkSamplerBorderColorComponentMappingCreateInfoEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // magFilter
    bufferSize += 4; // minFilter
    bufferSize += 4; // mipmapMode
    bufferSize += 4; // addressModeU
    bufferSize += 4; // addressModeV
    bufferSize += 4; // addressModeW
    bufferSize += 4; // mipLodBias
    bufferSize += 4; // anisotropyEnable
    bufferSize += 4; // maxAnisotropy
    bufferSize += 4; // compareEnable
    bufferSize += 4; // compareOp
    bufferSize += 4; // minLod
    bufferSize += 4; // maxLod
    bufferSize += 4; // borderColor
    bufferSize += 4; // unnormalizedCoordinates
    return bufferSize;
}

static inline void vt_serialize_VkSamplerCreateInfo(VkSamplerCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_INFO: {
                    int itemSize = vt_sizeof_VkSamplerYcbcrConversionInfo((VkSamplerYcbcrConversionInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkSamplerYcbcrConversionInfo((VkSamplerYcbcrConversionInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_SAMPLER_REDUCTION_MODE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkSamplerReductionModeCreateInfo((VkSamplerReductionModeCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkSamplerReductionModeCreateInfo((VkSamplerReductionModeCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_SAMPLER_CUSTOM_BORDER_COLOR_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkSamplerCustomBorderColorCreateInfoEXT((VkSamplerCustomBorderColorCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkSamplerCustomBorderColorCreateInfoEXT((VkSamplerCustomBorderColorCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_SAMPLER_BORDER_COLOR_COMPONENT_MAPPING_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkSamplerBorderColorComponentMappingCreateInfoEXT((VkSamplerBorderColorComponentMappingCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkSamplerBorderColorComponentMappingCreateInfoEXT((VkSamplerBorderColorComponentMappingCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkSamplerCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkFilter*)(outputBuffer + bufferOffset) = val->magFilter;
    bufferOffset += 4;
    *(VkFilter*)(outputBuffer + bufferOffset) = val->minFilter;
    bufferOffset += 4;
    *(VkSamplerMipmapMode*)(outputBuffer + bufferOffset) = val->mipmapMode;
    bufferOffset += 4;
    *(VkSamplerAddressMode*)(outputBuffer + bufferOffset) = val->addressModeU;
    bufferOffset += 4;
    *(VkSamplerAddressMode*)(outputBuffer + bufferOffset) = val->addressModeV;
    bufferOffset += 4;
    *(VkSamplerAddressMode*)(outputBuffer + bufferOffset) = val->addressModeW;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->mipLodBias;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->anisotropyEnable;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->maxAnisotropy;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->compareEnable;
    bufferOffset += 4;
    *(VkCompareOp*)(outputBuffer + bufferOffset) = val->compareOp;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->minLod;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = val->maxLod;
    bufferOffset += 4;
    *(VkBorderColor*)(outputBuffer + bufferOffset) = val->borderColor;
    bufferOffset += 4;
    *(VkBool32*)(outputBuffer + bufferOffset) = val->unnormalizedCoordinates;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkSamplerCreateInfo(VkSamplerCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkSamplerYcbcrConversionInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SAMPLER_YCBCR_CONVERSION_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkSamplerYcbcrConversionInfo));
                    vt_unserialize_VkSamplerYcbcrConversionInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_SAMPLER_REDUCTION_MODE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkSamplerReductionModeCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SAMPLER_REDUCTION_MODE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkSamplerReductionModeCreateInfo));
                    vt_unserialize_VkSamplerReductionModeCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_SAMPLER_CUSTOM_BORDER_COLOR_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkSamplerCustomBorderColorCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SAMPLER_CUSTOM_BORDER_COLOR_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkSamplerCustomBorderColorCreateInfoEXT));
                    vt_unserialize_VkSamplerCustomBorderColorCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_SAMPLER_BORDER_COLOR_COMPONENT_MAPPING_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkSamplerBorderColorComponentMappingCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SAMPLER_BORDER_COLOR_COMPONENT_MAPPING_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkSamplerBorderColorComponentMappingCreateInfoEXT));
                    vt_unserialize_VkSamplerBorderColorComponentMappingCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkSamplerCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->magFilter = *(VkFilter*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->minFilter = *(VkFilter*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->mipmapMode = *(VkSamplerMipmapMode*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->addressModeU = *(VkSamplerAddressMode*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->addressModeV = *(VkSamplerAddressMode*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->addressModeW = *(VkSamplerAddressMode*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->mipLodBias = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->anisotropyEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxAnisotropy = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->compareEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->compareOp = *(VkCompareOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->minLod = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxLod = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->borderColor = *(VkBorderColor*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->unnormalizedCoordinates = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDescriptorSetLayoutCreateInfo(VkDescriptorSetLayoutCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DESCRIPTOR_SET_LAYOUT_BINDING_FLAGS_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDescriptorSetLayoutBindingFlagsCreateInfo((VkDescriptorSetLayoutBindingFlagsCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_MUTABLE_DESCRIPTOR_TYPE_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkMutableDescriptorTypeCreateInfoEXT((VkMutableDescriptorTypeCreateInfoEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // bindingCount
    bufferSize += 4;
    if (val->pBindings) {
        int arrSize = val->bindingCount ? val->bindingCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkDescriptorSetLayoutBinding(&val->pBindings[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorSetLayoutCreateInfo(VkDescriptorSetLayoutCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DESCRIPTOR_SET_LAYOUT_BINDING_FLAGS_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkDescriptorSetLayoutBindingFlagsCreateInfo((VkDescriptorSetLayoutBindingFlagsCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDescriptorSetLayoutBindingFlagsCreateInfo((VkDescriptorSetLayoutBindingFlagsCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_MUTABLE_DESCRIPTOR_TYPE_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkMutableDescriptorTypeCreateInfoEXT((VkMutableDescriptorTypeCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkMutableDescriptorTypeCreateInfoEXT((VkMutableDescriptorTypeCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkDescriptorSetLayoutCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->bindingCount;
    bufferOffset += 4;

    if (val->pBindings) {
        int arrSize = val->bindingCount ? val->bindingCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkDescriptorSetLayoutBinding(&val->pBindings[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkDescriptorSetLayoutBinding(&val->pBindings[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDescriptorSetLayoutCreateInfo(VkDescriptorSetLayoutCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DESCRIPTOR_SET_LAYOUT_BINDING_FLAGS_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDescriptorSetLayoutBindingFlagsCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DESCRIPTOR_SET_LAYOUT_BINDING_FLAGS_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDescriptorSetLayoutBindingFlagsCreateInfo));
                    vt_unserialize_VkDescriptorSetLayoutBindingFlagsCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_MUTABLE_DESCRIPTOR_TYPE_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkMutableDescriptorTypeCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_MUTABLE_DESCRIPTOR_TYPE_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkMutableDescriptorTypeCreateInfoEXT));
                    vt_unserialize_VkMutableDescriptorTypeCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkDescriptorSetLayoutCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->bindingCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkDescriptorSetLayoutBinding* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkDescriptorSetLayoutBinding));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkDescriptorSetLayoutBinding(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pBindings = arrValues;
    }
    else val->pBindings = NULL;
}

static inline int vt_sizeof_VkDescriptorPoolCreateInfo(VkDescriptorPoolCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DESCRIPTOR_POOL_INLINE_UNIFORM_BLOCK_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDescriptorPoolInlineUniformBlockCreateInfo((VkDescriptorPoolInlineUniformBlockCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_MUTABLE_DESCRIPTOR_TYPE_CREATE_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkMutableDescriptorTypeCreateInfoEXT((VkMutableDescriptorTypeCreateInfoEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // maxSets
    bufferSize += 4; // poolSizeCount
    bufferSize += 4;
    if (val->pPoolSizes) {
        int arrSize = val->poolSizeCount ? val->poolSizeCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkDescriptorPoolSize(&val->pPoolSizes[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorPoolCreateInfo(VkDescriptorPoolCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DESCRIPTOR_POOL_INLINE_UNIFORM_BLOCK_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkDescriptorPoolInlineUniformBlockCreateInfo((VkDescriptorPoolInlineUniformBlockCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDescriptorPoolInlineUniformBlockCreateInfo((VkDescriptorPoolInlineUniformBlockCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_MUTABLE_DESCRIPTOR_TYPE_CREATE_INFO_EXT: {
                    int itemSize = vt_sizeof_VkMutableDescriptorTypeCreateInfoEXT((VkMutableDescriptorTypeCreateInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkMutableDescriptorTypeCreateInfoEXT((VkMutableDescriptorTypeCreateInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkDescriptorPoolCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->maxSets;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->poolSizeCount;
    bufferOffset += 4;

    if (val->pPoolSizes) {
        int arrSize = val->poolSizeCount ? val->poolSizeCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkDescriptorPoolSize(&val->pPoolSizes[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkDescriptorPoolSize(&val->pPoolSizes[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDescriptorPoolCreateInfo(VkDescriptorPoolCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DESCRIPTOR_POOL_INLINE_UNIFORM_BLOCK_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDescriptorPoolInlineUniformBlockCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DESCRIPTOR_POOL_INLINE_UNIFORM_BLOCK_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDescriptorPoolInlineUniformBlockCreateInfo));
                    vt_unserialize_VkDescriptorPoolInlineUniformBlockCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_MUTABLE_DESCRIPTOR_TYPE_CREATE_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkMutableDescriptorTypeCreateInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_MUTABLE_DESCRIPTOR_TYPE_CREATE_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkMutableDescriptorTypeCreateInfoEXT));
                    vt_unserialize_VkMutableDescriptorTypeCreateInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkDescriptorPoolCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->maxSets = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->poolSizeCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkDescriptorPoolSize* arrValues = vt_alloc(memoryPool, dataSize6 * sizeof(VkDescriptorPoolSize));
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkDescriptorPoolSize(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pPoolSizes = arrValues;
    }
    else val->pPoolSizes = NULL;
}

static inline int vt_sizeof_VkDescriptorSetAllocateInfo(VkDescriptorSetAllocateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DESCRIPTOR_SET_VARIABLE_DESCRIPTOR_COUNT_ALLOCATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDescriptorSetVariableDescriptorCountAllocateInfo((VkDescriptorSetVariableDescriptorCountAllocateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkDescriptorPool(val->descriptorPool);
    bufferSize += 4; // descriptorSetCount
    bufferSize += 4;
    if (val->pSetLayouts) {
        for (int i = 0; i < val->descriptorSetCount; i++) {
            bufferSize += vt_sizeof_VkDescriptorSetLayout(val->pSetLayouts[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkDescriptorSetAllocateInfo(VkDescriptorSetAllocateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DESCRIPTOR_SET_VARIABLE_DESCRIPTOR_COUNT_ALLOCATE_INFO: {
                    int itemSize = vt_sizeof_VkDescriptorSetVariableDescriptorCountAllocateInfo((VkDescriptorSetVariableDescriptorCountAllocateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDescriptorSetVariableDescriptorCountAllocateInfo((VkDescriptorSetVariableDescriptorCountAllocateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkDescriptorPool(val->descriptorPool, outputBuffer + bufferOffset);
#else
    VkObject* descriptorPoolObject = VkObject_fromHandle(val->descriptorPool);
    vt_serialize_VkDescriptorPool((VkDescriptorPool)&descriptorPoolObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDescriptorPool(val->descriptorPool);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->descriptorSetCount;
    bufferOffset += 4;

    if (val->pSetLayouts) {
        *(int*)(outputBuffer + bufferOffset) = val->descriptorSetCount;
        bufferOffset += 4;
        for (int i = 0; i < val->descriptorSetCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkDescriptorSetLayout(val->pSetLayouts[i], outputBuffer + bufferOffset);
#else
            VkObject* setLayoutsObject = VkObject_fromHandle(val->pSetLayouts[i]);
            vt_serialize_VkDescriptorSetLayout((VkDescriptorSetLayout)&setLayoutsObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkDescriptorSetLayout(val->pSetLayouts[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDescriptorSetAllocateInfo(VkDescriptorSetAllocateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DESCRIPTOR_SET_VARIABLE_DESCRIPTOR_COUNT_ALLOCATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDescriptorSetVariableDescriptorCountAllocateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DESCRIPTOR_SET_VARIABLE_DESCRIPTOR_COUNT_ALLOCATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDescriptorSetVariableDescriptorCountAllocateInfo));
                    vt_unserialize_VkDescriptorSetVariableDescriptorCountAllocateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);

    uint64_t descriptorPoolId;
    vt_unserialize_VkDescriptorPool((VkDescriptorPool)&descriptorPoolId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->descriptorPool = VkObject_fromId(descriptorPoolId);
#else
    VkObject* descriptorPoolObject = VkObject_create(VK_OBJECT_TYPE_DESCRIPTOR_POOL, descriptorPoolId);
    val->descriptorPool = VkObject_toHandle(descriptorPoolObject);
#endif
    bufferOffset += vt_sizeof_VkDescriptorPool(val->descriptorPool);
    val->descriptorSetCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkDescriptorSetLayout* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkDescriptorSetLayout));
        for (int i = 0; i < dataSize5; i++) {
            uint64_t setLayoutsId;
            vt_unserialize_VkDescriptorSetLayout((VkDescriptorSetLayout)&setLayoutsId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(setLayoutsId);
#else
                VkObject* setLayoutsObject = VkObject_create(VK_OBJECT_TYPE_DESCRIPTOR_SET_LAYOUT, setLayoutsId);
                arrValues[i] = VkObject_toHandle(setLayoutsObject);
#endif
                bufferOffset += vt_sizeof_VkDescriptorSetLayout(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkDescriptorSetLayout(VK_NULL_HANDLE);
        }
        val->pSetLayouts = arrValues;
    }
    else val->pSetLayouts = NULL;
}

static inline int vt_sizeof_VkWriteDescriptorSet(VkWriteDescriptorSet* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_WRITE_DESCRIPTOR_SET_INLINE_UNIFORM_BLOCK: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkWriteDescriptorSetInlineUniformBlock((VkWriteDescriptorSetInlineUniformBlock*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkDescriptorSet(val->dstSet);
    bufferSize += 4; // dstBinding
    bufferSize += 4; // dstArrayElement
    bufferSize += 4; // descriptorCount
    bufferSize += 4; // descriptorType
    bufferSize += 4;
    if (val->pImageInfo) {
        int arrSize = val->descriptorCount ? val->descriptorCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkDescriptorImageInfo(&val->pImageInfo[i]);
        }
    }
    bufferSize += 4;
    if (val->pBufferInfo) {
        int arrSize = val->descriptorCount ? val->descriptorCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkDescriptorBufferInfo(&val->pBufferInfo[i]);
        }
    }
    bufferSize += 4;
    if (val->pTexelBufferView) {
        for (int i = 0; i < val->descriptorCount; i++) {
            bufferSize += vt_sizeof_VkBufferView(val->pTexelBufferView[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkWriteDescriptorSet(VkWriteDescriptorSet* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_WRITE_DESCRIPTOR_SET_INLINE_UNIFORM_BLOCK: {
                    int itemSize = vt_sizeof_VkWriteDescriptorSetInlineUniformBlock((VkWriteDescriptorSetInlineUniformBlock*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkWriteDescriptorSetInlineUniformBlock((VkWriteDescriptorSetInlineUniformBlock*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkDescriptorSet(val->dstSet, outputBuffer + bufferOffset);
#else
    VkObject* dstSetObject = VkObject_fromHandle(val->dstSet);
    vt_serialize_VkDescriptorSet((VkDescriptorSet)&dstSetObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDescriptorSet(val->dstSet);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstBinding;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstArrayElement;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->descriptorCount;
    bufferOffset += 4;
    *(VkDescriptorType*)(outputBuffer + bufferOffset) = val->descriptorType;
    bufferOffset += 4;

    if (val->pImageInfo && IS_DESCRIPTOR_IMAGE_INFO(val->descriptorType)) {
        int arrSize = val->descriptorCount ? val->descriptorCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkDescriptorImageInfo(&val->pImageInfo[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkDescriptorImageInfo(&val->pImageInfo[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pBufferInfo && IS_DESCRIPTOR_BUFFER_INFO(val->descriptorType)) {
        int arrSize = val->descriptorCount ? val->descriptorCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkDescriptorBufferInfo(&val->pBufferInfo[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkDescriptorBufferInfo(&val->pBufferInfo[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (val->pTexelBufferView && IS_DESCRIPTOR_TEXEL_BUFFER_VIEW(val->descriptorType)) {
        *(int*)(outputBuffer + bufferOffset) = val->descriptorCount;
        bufferOffset += 4;
        for (int i = 0; i < val->descriptorCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkBufferView(val->pTexelBufferView[i], outputBuffer + bufferOffset);
#else
            VkObject* texelBufferViewObject = VkObject_fromHandle(val->pTexelBufferView[i]);
            vt_serialize_VkBufferView((VkBufferView)&texelBufferViewObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkBufferView(val->pTexelBufferView[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkWriteDescriptorSet(VkWriteDescriptorSet* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_WRITE_DESCRIPTOR_SET_INLINE_UNIFORM_BLOCK: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkWriteDescriptorSetInlineUniformBlock* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_WRITE_DESCRIPTOR_SET_INLINE_UNIFORM_BLOCK) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkWriteDescriptorSetInlineUniformBlock));
                    vt_unserialize_VkWriteDescriptorSetInlineUniformBlock(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);

    uint64_t dstSetId;
    vt_unserialize_VkDescriptorSet((VkDescriptorSet)&dstSetId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->dstSet = VkObject_fromId(dstSetId);
#else
    VkObject* dstSetObject = VkObject_create(VK_OBJECT_TYPE_DESCRIPTOR_SET, dstSetId);
    val->dstSet = VkObject_toHandle(dstSetObject);
#endif
    bufferOffset += vt_sizeof_VkDescriptorSet(val->dstSet);
    val->dstBinding = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstArrayElement = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorType = *(VkDescriptorType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        VkDescriptorImageInfo* arrValues = vt_alloc(memoryPool, dataSize8 * sizeof(VkDescriptorImageInfo));
        for (int i = 0; i < dataSize8; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkDescriptorImageInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pImageInfo = arrValues;
    }
    else val->pImageInfo = NULL;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        VkDescriptorBufferInfo* arrValues = vt_alloc(memoryPool, dataSize9 * sizeof(VkDescriptorBufferInfo));
        for (int i = 0; i < dataSize9; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkDescriptorBufferInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pBufferInfo = arrValues;
    }
    else val->pBufferInfo = NULL;

    int dataSize10 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize10 > 0) {
        VkBufferView* arrValues = vt_alloc(memoryPool, dataSize10 * sizeof(VkBufferView));
        for (int i = 0; i < dataSize10; i++) {
            uint64_t texelBufferViewId;
            vt_unserialize_VkBufferView((VkBufferView)&texelBufferViewId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(texelBufferViewId);
#else
                VkObject* texelBufferViewObject = VkObject_create(VK_OBJECT_TYPE_BUFFER_VIEW, texelBufferViewId);
                arrValues[i] = VkObject_toHandle(texelBufferViewObject);
#endif
                bufferOffset += vt_sizeof_VkBufferView(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkBufferView(VK_NULL_HANDLE);
        }
        val->pTexelBufferView = arrValues;
    }
    else val->pTexelBufferView = NULL;
}

static inline int vt_sizeof_VkCopyDescriptorSet(VkCopyDescriptorSet* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkDescriptorSet(val->srcSet);
    bufferSize += 4; // srcBinding
    bufferSize += 4; // srcArrayElement
    bufferSize += vt_sizeof_VkDescriptorSet(val->dstSet);
    bufferSize += 4; // dstBinding
    bufferSize += 4; // dstArrayElement
    bufferSize += 4; // descriptorCount
    return bufferSize;
}

static inline void vt_serialize_VkCopyDescriptorSet(VkCopyDescriptorSet* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkDescriptorSet(val->srcSet, outputBuffer + bufferOffset);
#else
    VkObject* srcSetObject = VkObject_fromHandle(val->srcSet);
    vt_serialize_VkDescriptorSet((VkDescriptorSet)&srcSetObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDescriptorSet(val->srcSet);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->srcBinding;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->srcArrayElement;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkDescriptorSet(val->dstSet, outputBuffer + bufferOffset);
#else
    VkObject* dstSetObject = VkObject_fromHandle(val->dstSet);
    vt_serialize_VkDescriptorSet((VkDescriptorSet)&dstSetObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDescriptorSet(val->dstSet);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstBinding;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstArrayElement;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->descriptorCount;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkCopyDescriptorSet(VkCopyDescriptorSet* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t srcSetId;
    vt_unserialize_VkDescriptorSet((VkDescriptorSet)&srcSetId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->srcSet = VkObject_fromId(srcSetId);
#else
    VkObject* srcSetObject = VkObject_create(VK_OBJECT_TYPE_DESCRIPTOR_SET, srcSetId);
    val->srcSet = VkObject_toHandle(srcSetObject);
#endif
    bufferOffset += vt_sizeof_VkDescriptorSet(val->srcSet);
    val->srcBinding = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcArrayElement = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t dstSetId;
    vt_unserialize_VkDescriptorSet((VkDescriptorSet)&dstSetId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->dstSet = VkObject_fromId(dstSetId);
#else
    VkObject* dstSetObject = VkObject_create(VK_OBJECT_TYPE_DESCRIPTOR_SET, dstSetId);
    val->dstSet = VkObject_toHandle(dstSetObject);
#endif
    bufferOffset += vt_sizeof_VkDescriptorSet(val->dstSet);
    val->dstBinding = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstArrayElement = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->descriptorCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkFramebufferCreateInfo(VkFramebufferCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_FRAMEBUFFER_ATTACHMENTS_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkFramebufferAttachmentsCreateInfo((VkFramebufferAttachmentsCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += vt_sizeof_VkRenderPass(val->renderPass);
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    if (val->pAttachments) {
        for (int i = 0; i < val->attachmentCount; i++) {
            bufferSize += vt_sizeof_VkImageView(val->pAttachments[i]);
        }
    }
    bufferSize += 4; // width
    bufferSize += 4; // height
    bufferSize += 4; // layers
    return bufferSize;
}

static inline void vt_serialize_VkFramebufferCreateInfo(VkFramebufferCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_FRAMEBUFFER_ATTACHMENTS_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkFramebufferAttachmentsCreateInfo((VkFramebufferAttachmentsCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkFramebufferAttachmentsCreateInfo((VkFramebufferAttachmentsCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkFramebufferCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkRenderPass(val->renderPass, outputBuffer + bufferOffset);
#else
    VkObject* renderPassObject = VkObject_fromHandle(val->renderPass);
    vt_serialize_VkRenderPass((VkRenderPass)&renderPassObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkRenderPass(val->renderPass);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->attachmentCount;
    bufferOffset += 4;

    if (val->pAttachments) {
        *(int*)(outputBuffer + bufferOffset) = val->attachmentCount;
        bufferOffset += 4;
        for (int i = 0; i < val->attachmentCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkImageView(val->pAttachments[i], outputBuffer + bufferOffset);
#else
            VkObject* attachmentsObject = VkObject_fromHandle(val->pAttachments[i]);
            vt_serialize_VkImageView((VkImageView)&attachmentsObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkImageView(val->pAttachments[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->width;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->height;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->layers;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkFramebufferCreateInfo(VkFramebufferCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_FRAMEBUFFER_ATTACHMENTS_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkFramebufferAttachmentsCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_FRAMEBUFFER_ATTACHMENTS_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkFramebufferAttachmentsCreateInfo));
                    vt_unserialize_VkFramebufferAttachmentsCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkFramebufferCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t renderPassId;
    vt_unserialize_VkRenderPass((VkRenderPass)&renderPassId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->renderPass = VkObject_fromId(renderPassId);
#else
    VkObject* renderPassObject = VkObject_create(VK_OBJECT_TYPE_RENDER_PASS, renderPassId);
    val->renderPass = VkObject_toHandle(renderPassObject);
#endif
    bufferOffset += vt_sizeof_VkRenderPass(val->renderPass);
    val->attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkImageView* arrValues = vt_alloc(memoryPool, dataSize6 * sizeof(VkImageView));
        for (int i = 0; i < dataSize6; i++) {
            uint64_t attachmentsId;
            vt_unserialize_VkImageView((VkImageView)&attachmentsId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(attachmentsId);
#else
                VkObject* attachmentsObject = VkObject_create(VK_OBJECT_TYPE_IMAGE_VIEW, attachmentsId);
                arrValues[i] = VkObject_toHandle(attachmentsObject);
#endif
                bufferOffset += vt_sizeof_VkImageView(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkImageView(VK_NULL_HANDLE);
        }
        val->pAttachments = arrValues;
    }
    else val->pAttachments = NULL;
    val->width = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->height = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->layers = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkRenderPassCreateInfo(VkRenderPassCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_RENDER_PASS_MULTIVIEW_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkRenderPassMultiviewCreateInfo((VkRenderPassMultiviewCreateInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_RENDER_PASS_INPUT_ATTACHMENT_ASPECT_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkRenderPassInputAttachmentAspectCreateInfo((VkRenderPassInputAttachmentAspectCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    if (val->pAttachments) {
        int arrSize = val->attachmentCount ? val->attachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkAttachmentDescription(&val->pAttachments[i]);
        }
    }
    bufferSize += 4; // subpassCount
    bufferSize += 4;
    if (val->pSubpasses) {
        int arrSize = val->subpassCount ? val->subpassCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSubpassDescription(&val->pSubpasses[i]);
        }
    }
    bufferSize += 4; // dependencyCount
    bufferSize += 4;
    if (val->pDependencies) {
        int arrSize = val->dependencyCount ? val->dependencyCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSubpassDependency(&val->pDependencies[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkRenderPassCreateInfo(VkRenderPassCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_RENDER_PASS_MULTIVIEW_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkRenderPassMultiviewCreateInfo((VkRenderPassMultiviewCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkRenderPassMultiviewCreateInfo((VkRenderPassMultiviewCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_RENDER_PASS_INPUT_ATTACHMENT_ASPECT_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkRenderPassInputAttachmentAspectCreateInfo((VkRenderPassInputAttachmentAspectCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkRenderPassInputAttachmentAspectCreateInfo((VkRenderPassInputAttachmentAspectCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkRenderPassCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->attachmentCount;
    bufferOffset += 4;

    if (val->pAttachments) {
        int arrSize = val->attachmentCount ? val->attachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkAttachmentDescription(&val->pAttachments[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkAttachmentDescription(&val->pAttachments[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->subpassCount;
    bufferOffset += 4;

    if (val->pSubpasses) {
        int arrSize = val->subpassCount ? val->subpassCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSubpassDescription(&val->pSubpasses[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSubpassDescription(&val->pSubpasses[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dependencyCount;
    bufferOffset += 4;

    if (val->pDependencies) {
        int arrSize = val->dependencyCount ? val->dependencyCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSubpassDependency(&val->pDependencies[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSubpassDependency(&val->pDependencies[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkRenderPassCreateInfo(VkRenderPassCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_RENDER_PASS_MULTIVIEW_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkRenderPassMultiviewCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_RENDER_PASS_MULTIVIEW_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkRenderPassMultiviewCreateInfo));
                    vt_unserialize_VkRenderPassMultiviewCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_RENDER_PASS_INPUT_ATTACHMENT_ASPECT_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkRenderPassInputAttachmentAspectCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_RENDER_PASS_INPUT_ATTACHMENT_ASPECT_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkRenderPassInputAttachmentAspectCreateInfo));
                    vt_unserialize_VkRenderPassInputAttachmentAspectCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkRenderPassCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkAttachmentDescription* arrValues = vt_alloc(memoryPool, dataSize5 * sizeof(VkAttachmentDescription));
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkAttachmentDescription(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pAttachments = arrValues;
    }
    else val->pAttachments = NULL;
    val->subpassCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkSubpassDescription* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkSubpassDescription));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSubpassDescription(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pSubpasses = arrValues;
    }
    else val->pSubpasses = NULL;
    val->dependencyCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        VkSubpassDependency* arrValues = vt_alloc(memoryPool, dataSize9 * sizeof(VkSubpassDependency));
        for (int i = 0; i < dataSize9; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSubpassDependency(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pDependencies = arrValues;
    }
    else val->pDependencies = NULL;
}

static inline int vt_sizeof_VkCommandPoolCreateInfo(VkCommandPoolCreateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // queueFamilyIndex
    return bufferSize;
}

static inline void vt_serialize_VkCommandPoolCreateInfo(VkCommandPoolCreateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkCommandPoolCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->queueFamilyIndex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkCommandPoolCreateInfo(VkCommandPoolCreateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkCommandPoolCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkCommandBufferAllocateInfo(VkCommandBufferAllocateInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkCommandPool(val->commandPool);
    bufferSize += 4; // level
    bufferSize += 4; // commandBufferCount
    return bufferSize;
}

static inline void vt_serialize_VkCommandBufferAllocateInfo(VkCommandBufferAllocateInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkCommandPool(val->commandPool, outputBuffer + bufferOffset);
#else
    VkObject* commandPoolObject = VkObject_fromHandle(val->commandPool);
    vt_serialize_VkCommandPool((VkCommandPool)&commandPoolObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkCommandPool(val->commandPool);
    *(VkCommandBufferLevel*)(outputBuffer + bufferOffset) = val->level;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->commandBufferCount;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkCommandBufferAllocateInfo(VkCommandBufferAllocateInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t commandPoolId;
    vt_unserialize_VkCommandPool((VkCommandPool)&commandPoolId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->commandPool = VkObject_fromId(commandPoolId);
#else
    VkObject* commandPoolObject = VkObject_create(VK_OBJECT_TYPE_COMMAND_POOL, commandPoolId);
    val->commandPool = VkObject_toHandle(commandPoolObject);
#endif
    bufferOffset += vt_sizeof_VkCommandPool(val->commandPool);
    val->level = *(VkCommandBufferLevel*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->commandBufferCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkCommandBufferBeginInfo(VkCommandBufferBeginInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_COMMAND_BUFFER_BEGIN_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDeviceGroupCommandBufferBeginInfo((VkDeviceGroupCommandBufferBeginInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // flags
    bufferSize += 4;
    bufferSize += val->pInheritanceInfo ? vt_sizeof_VkCommandBufferInheritanceInfo(val->pInheritanceInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_VkCommandBufferBeginInfo(VkCommandBufferBeginInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DEVICE_GROUP_COMMAND_BUFFER_BEGIN_INFO: {
                    int itemSize = vt_sizeof_VkDeviceGroupCommandBufferBeginInfo((VkDeviceGroupCommandBufferBeginInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDeviceGroupCommandBufferBeginInfo((VkDeviceGroupCommandBufferBeginInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkCommandBufferUsageFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;

    if (val->pInheritanceInfo) {
        int itemSize = vt_sizeof_VkCommandBufferInheritanceInfo(val->pInheritanceInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkCommandBufferInheritanceInfo(val->pInheritanceInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkCommandBufferBeginInfo(VkCommandBufferBeginInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_COMMAND_BUFFER_BEGIN_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDeviceGroupCommandBufferBeginInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DEVICE_GROUP_COMMAND_BUFFER_BEGIN_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDeviceGroupCommandBufferBeginInfo));
                    vt_unserialize_VkDeviceGroupCommandBufferBeginInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->flags = *(VkCommandBufferUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        val->pInheritanceInfo = vt_alloc(memoryPool, sizeof(VkCommandBufferInheritanceInfo));
        vt_unserialize_VkCommandBufferInheritanceInfo(val->pInheritanceInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize4;
    }
    else val->pInheritanceInfo = NULL;
}

static inline int vt_sizeof_VkBufferCopy(VkBufferCopy* val) {
    int bufferSize = 0;
    bufferSize += 8; // srcOffset
    bufferSize += 8; // dstOffset
    bufferSize += 8; // size
    return bufferSize;
}

static inline void vt_serialize_VkBufferCopy(VkBufferCopy* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->srcOffset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->dstOffset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkBufferCopy(VkBufferCopy* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->srcOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->dstOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkImageCopy(VkImageCopy* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    bufferSize += vt_sizeof_VkOffset3D(&val->srcOffset);
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    bufferSize += vt_sizeof_VkOffset3D(&val->dstOffset);
    bufferSize += vt_sizeof_VkExtent3D(&val->extent);
    return bufferSize;
}

static inline void vt_serialize_VkImageCopy(VkImageCopy* val, char* outputBuffer) {
    int bufferOffset = 0;
    vt_serialize_VkImageSubresourceLayers(&val->srcSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    vt_serialize_VkOffset3D(&val->srcOffset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset3D(&val->srcOffset);
    vt_serialize_VkImageSubresourceLayers(&val->dstSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    vt_serialize_VkOffset3D(&val->dstOffset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset3D(&val->dstOffset);
    vt_serialize_VkExtent3D(&val->extent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);
}

static inline void vt_unserialize_VkImageCopy(VkImageCopy* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    vt_unserialize_VkImageSubresourceLayers(&val->srcSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    vt_unserialize_VkOffset3D(&val->srcOffset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset3D(&val->srcOffset);
    vt_unserialize_VkImageSubresourceLayers(&val->dstSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    vt_unserialize_VkOffset3D(&val->dstOffset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset3D(&val->dstOffset);
    vt_unserialize_VkExtent3D(&val->extent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);
}

static inline int vt_sizeof_VkImageBlit(VkImageBlit* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    bufferSize += 2 * vt_sizeof_VkOffset3D(&val->srcOffsets[0]);
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    bufferSize += 2 * vt_sizeof_VkOffset3D(&val->dstOffsets[0]);
    return bufferSize;
}

static inline void vt_serialize_VkImageBlit(VkImageBlit* val, char* outputBuffer) {
    int bufferOffset = 0;
    vt_serialize_VkImageSubresourceLayers(&val->srcSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    memcpy(outputBuffer + bufferOffset, val->srcOffsets, 2 * vt_sizeof_VkOffset3D(&val->srcOffsets[0]));
    bufferOffset += 2 * vt_sizeof_VkOffset3D(&val->srcOffsets[0]);
    vt_serialize_VkImageSubresourceLayers(&val->dstSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    memcpy(outputBuffer + bufferOffset, val->dstOffsets, 2 * vt_sizeof_VkOffset3D(&val->dstOffsets[0]));
    bufferOffset += 2 * vt_sizeof_VkOffset3D(&val->dstOffsets[0]);
}

static inline void vt_unserialize_VkImageBlit(VkImageBlit* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    vt_unserialize_VkImageSubresourceLayers(&val->srcSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    memcpy(val->srcOffsets, inputBuffer + bufferOffset, 2 * vt_sizeof_VkOffset3D(&val->srcOffsets[0]));
    bufferOffset += 2 * vt_sizeof_VkOffset3D(&val->srcOffsets[0]);
    vt_unserialize_VkImageSubresourceLayers(&val->dstSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    memcpy(val->dstOffsets, inputBuffer + bufferOffset, 2 * vt_sizeof_VkOffset3D(&val->dstOffsets[0]));
    bufferOffset += 2 * vt_sizeof_VkOffset3D(&val->dstOffsets[0]);
}

static inline int vt_sizeof_VkBufferImageCopy(VkBufferImageCopy* val) {
    int bufferSize = 0;
    bufferSize += 8; // bufferOffset
    bufferSize += 4; // bufferRowLength
    bufferSize += 4; // bufferImageHeight
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->imageSubresource);
    bufferSize += vt_sizeof_VkOffset3D(&val->imageOffset);
    bufferSize += vt_sizeof_VkExtent3D(&val->imageExtent);
    return bufferSize;
}

static inline void vt_serialize_VkBufferImageCopy(VkBufferImageCopy* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->bufferOffset;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->bufferRowLength;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->bufferImageHeight;
    bufferOffset += 4;
    vt_serialize_VkImageSubresourceLayers(&val->imageSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->imageSubresource);
    vt_serialize_VkOffset3D(&val->imageOffset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset3D(&val->imageOffset);
    vt_serialize_VkExtent3D(&val->imageExtent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent3D(&val->imageExtent);
}

static inline void vt_unserialize_VkBufferImageCopy(VkBufferImageCopy* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->bufferOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->bufferRowLength = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->bufferImageHeight = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkImageSubresourceLayers(&val->imageSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->imageSubresource);
    vt_unserialize_VkOffset3D(&val->imageOffset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset3D(&val->imageOffset);
    vt_unserialize_VkExtent3D(&val->imageExtent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent3D(&val->imageExtent);
}

static inline int vt_sizeof_VkClearAttachment(VkClearAttachment* val) {
    int bufferSize = 0;
    bufferSize += 4; // aspectMask
    bufferSize += 4; // colorAttachment
    bufferSize += vt_sizeof_VkClearValue(&val->clearValue);
    return bufferSize;
}

static inline void vt_serialize_VkClearAttachment(VkClearAttachment* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkImageAspectFlags*)(outputBuffer + bufferOffset) = val->aspectMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->colorAttachment;
    bufferOffset += 4;
    vt_serialize_VkClearValue(&val->clearValue, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkClearValue(&val->clearValue);
}

static inline void vt_unserialize_VkClearAttachment(VkClearAttachment* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->aspectMask = *(VkImageAspectFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->colorAttachment = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    vt_unserialize_VkClearValue(&val->clearValue, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkClearValue(&val->clearValue);
}

static inline int vt_sizeof_VkClearRect(VkClearRect* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkRect2D(&val->rect);
    bufferSize += 4; // baseArrayLayer
    bufferSize += 4; // layerCount
    return bufferSize;
}

static inline void vt_serialize_VkClearRect(VkClearRect* val, char* outputBuffer) {
    int bufferOffset = 0;
    vt_serialize_VkRect2D(&val->rect, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkRect2D(&val->rect);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->baseArrayLayer;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->layerCount;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkClearRect(VkClearRect* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    vt_unserialize_VkRect2D(&val->rect, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkRect2D(&val->rect);
    val->baseArrayLayer = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->layerCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkImageResolve(VkImageResolve* val) {
    int bufferSize = 0;
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    bufferSize += vt_sizeof_VkOffset3D(&val->srcOffset);
    bufferSize += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    bufferSize += vt_sizeof_VkOffset3D(&val->dstOffset);
    bufferSize += vt_sizeof_VkExtent3D(&val->extent);
    return bufferSize;
}

static inline void vt_serialize_VkImageResolve(VkImageResolve* val, char* outputBuffer) {
    int bufferOffset = 0;
    vt_serialize_VkImageSubresourceLayers(&val->srcSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    vt_serialize_VkOffset3D(&val->srcOffset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset3D(&val->srcOffset);
    vt_serialize_VkImageSubresourceLayers(&val->dstSubresource, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    vt_serialize_VkOffset3D(&val->dstOffset, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkOffset3D(&val->dstOffset);
    vt_serialize_VkExtent3D(&val->extent, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);
}

static inline void vt_unserialize_VkImageResolve(VkImageResolve* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    vt_unserialize_VkImageSubresourceLayers(&val->srcSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->srcSubresource);
    vt_unserialize_VkOffset3D(&val->srcOffset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset3D(&val->srcOffset);
    vt_unserialize_VkImageSubresourceLayers(&val->dstSubresource, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceLayers(&val->dstSubresource);
    vt_unserialize_VkOffset3D(&val->dstOffset, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkOffset3D(&val->dstOffset);
    vt_unserialize_VkExtent3D(&val->extent, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExtent3D(&val->extent);
}

static inline int vt_sizeof_VkMemoryBarrier(VkMemoryBarrier* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // srcAccessMask
    bufferSize += 4; // dstAccessMask
    return bufferSize;
}

static inline void vt_serialize_VkMemoryBarrier(VkMemoryBarrier* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkAccessFlags*)(outputBuffer + bufferOffset) = val->srcAccessMask;
    bufferOffset += 4;
    *(VkAccessFlags*)(outputBuffer + bufferOffset) = val->dstAccessMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkMemoryBarrier(VkMemoryBarrier* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->srcAccessMask = *(VkAccessFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstAccessMask = *(VkAccessFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkBufferMemoryBarrier(VkBufferMemoryBarrier* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // srcAccessMask
    bufferSize += 4; // dstAccessMask
    bufferSize += 4; // srcQueueFamilyIndex
    bufferSize += 4; // dstQueueFamilyIndex
    bufferSize += vt_sizeof_VkBuffer(val->buffer);
    bufferSize += 8; // offset
    bufferSize += 8; // size
    return bufferSize;
}

static inline void vt_serialize_VkBufferMemoryBarrier(VkBufferMemoryBarrier* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkAccessFlags*)(outputBuffer + bufferOffset) = val->srcAccessMask;
    bufferOffset += 4;
    *(VkAccessFlags*)(outputBuffer + bufferOffset) = val->dstAccessMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->srcQueueFamilyIndex;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstQueueFamilyIndex;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->buffer, outputBuffer + bufferOffset);
#else
    VkObject* bufferObject = VkObject_fromHandle(val->buffer);
    vt_serialize_VkBuffer((VkBuffer)&bufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->offset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->size;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkBufferMemoryBarrier(VkBufferMemoryBarrier* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->srcAccessMask = *(VkAccessFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstAccessMask = *(VkAccessFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcQueueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstQueueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t bufferId;
    vt_unserialize_VkBuffer((VkBuffer)&bufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->buffer = VkObject_fromId(bufferId);
#else
    VkObject* bufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, bufferId);
    val->buffer = VkObject_toHandle(bufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
    val->offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    val->size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkImageMemoryBarrier(VkImageMemoryBarrier* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_SAMPLE_LOCATIONS_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkSampleLocationsInfoEXT((VkSampleLocationsInfoEXT*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // srcAccessMask
    bufferSize += 4; // dstAccessMask
    bufferSize += 4; // oldLayout
    bufferSize += 4; // newLayout
    bufferSize += 4; // srcQueueFamilyIndex
    bufferSize += 4; // dstQueueFamilyIndex
    bufferSize += vt_sizeof_VkImage(val->image);
    bufferSize += vt_sizeof_VkImageSubresourceRange(&val->subresourceRange);
    return bufferSize;
}

static inline void vt_serialize_VkImageMemoryBarrier(VkImageMemoryBarrier* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_SAMPLE_LOCATIONS_INFO_EXT: {
                    int itemSize = vt_sizeof_VkSampleLocationsInfoEXT((VkSampleLocationsInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkSampleLocationsInfoEXT((VkSampleLocationsInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkAccessFlags*)(outputBuffer + bufferOffset) = val->srcAccessMask;
    bufferOffset += 4;
    *(VkAccessFlags*)(outputBuffer + bufferOffset) = val->dstAccessMask;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->oldLayout;
    bufferOffset += 4;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->newLayout;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->srcQueueFamilyIndex;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->dstQueueFamilyIndex;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->image, outputBuffer + bufferOffset);
#else
    VkObject* imageObject = VkObject_fromHandle(val->image);
    vt_serialize_VkImage((VkImage)&imageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
    vt_serialize_VkImageSubresourceRange(&val->subresourceRange, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkImageSubresourceRange(&val->subresourceRange);
}

static inline void vt_unserialize_VkImageMemoryBarrier(VkImageMemoryBarrier* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_SAMPLE_LOCATIONS_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkSampleLocationsInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SAMPLE_LOCATIONS_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkSampleLocationsInfoEXT));
                    vt_unserialize_VkSampleLocationsInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->srcAccessMask = *(VkAccessFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstAccessMask = *(VkAccessFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->oldLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->newLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->srcQueueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->dstQueueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t imageId;
    vt_unserialize_VkImage((VkImage)&imageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->image = VkObject_fromId(imageId);
#else
    VkObject* imageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, imageId);
    val->image = VkObject_toHandle(imageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);
    vt_unserialize_VkImageSubresourceRange(&val->subresourceRange, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkImageSubresourceRange(&val->subresourceRange);
}

static inline int vt_sizeof_VkRenderPassBeginInfo(VkRenderPassBeginInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_RENDER_PASS_BEGIN_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkDeviceGroupRenderPassBeginInfo((VkDeviceGroupRenderPassBeginInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_RENDER_PASS_SAMPLE_LOCATIONS_BEGIN_INFO_EXT: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkRenderPassSampleLocationsBeginInfoEXT((VkRenderPassSampleLocationsBeginInfoEXT*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_RENDER_PASS_ATTACHMENT_BEGIN_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkRenderPassAttachmentBeginInfo((VkRenderPassAttachmentBeginInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkRenderPass(val->renderPass);
    bufferSize += vt_sizeof_VkFramebuffer(val->framebuffer);
    bufferSize += vt_sizeof_VkRect2D(&val->renderArea);
    bufferSize += 4; // clearValueCount
    bufferSize += 4;
    if (val->pClearValues) {
        int arrSize = val->clearValueCount ? val->clearValueCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkClearValue(&val->pClearValues[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkRenderPassBeginInfo(VkRenderPassBeginInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_DEVICE_GROUP_RENDER_PASS_BEGIN_INFO: {
                    int itemSize = vt_sizeof_VkDeviceGroupRenderPassBeginInfo((VkDeviceGroupRenderPassBeginInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkDeviceGroupRenderPassBeginInfo((VkDeviceGroupRenderPassBeginInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_RENDER_PASS_SAMPLE_LOCATIONS_BEGIN_INFO_EXT: {
                    int itemSize = vt_sizeof_VkRenderPassSampleLocationsBeginInfoEXT((VkRenderPassSampleLocationsBeginInfoEXT*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkRenderPassSampleLocationsBeginInfoEXT((VkRenderPassSampleLocationsBeginInfoEXT*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_RENDER_PASS_ATTACHMENT_BEGIN_INFO: {
                    int itemSize = vt_sizeof_VkRenderPassAttachmentBeginInfo((VkRenderPassAttachmentBeginInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkRenderPassAttachmentBeginInfo((VkRenderPassAttachmentBeginInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkRenderPass(val->renderPass, outputBuffer + bufferOffset);
#else
    VkObject* renderPassObject = VkObject_fromHandle(val->renderPass);
    vt_serialize_VkRenderPass((VkRenderPass)&renderPassObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkRenderPass(val->renderPass);

#ifdef VT_SERVER
    vt_serialize_VkFramebuffer(val->framebuffer, outputBuffer + bufferOffset);
#else
    VkObject* framebufferObject = VkObject_fromHandle(val->framebuffer);
    vt_serialize_VkFramebuffer((VkFramebuffer)&framebufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkFramebuffer(val->framebuffer);
    vt_serialize_VkRect2D(&val->renderArea, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkRect2D(&val->renderArea);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->clearValueCount;
    bufferOffset += 4;

    if (val->pClearValues) {
        int arrSize = val->clearValueCount ? val->clearValueCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkClearValue(&val->pClearValues[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkClearValue(&val->pClearValues[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkRenderPassBeginInfo(VkRenderPassBeginInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_DEVICE_GROUP_RENDER_PASS_BEGIN_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkDeviceGroupRenderPassBeginInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_DEVICE_GROUP_RENDER_PASS_BEGIN_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkDeviceGroupRenderPassBeginInfo));
                    vt_unserialize_VkDeviceGroupRenderPassBeginInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_RENDER_PASS_SAMPLE_LOCATIONS_BEGIN_INFO_EXT: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkRenderPassSampleLocationsBeginInfoEXT* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_RENDER_PASS_SAMPLE_LOCATIONS_BEGIN_INFO_EXT) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkRenderPassSampleLocationsBeginInfoEXT));
                    vt_unserialize_VkRenderPassSampleLocationsBeginInfoEXT(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_RENDER_PASS_ATTACHMENT_BEGIN_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkRenderPassAttachmentBeginInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_RENDER_PASS_ATTACHMENT_BEGIN_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkRenderPassAttachmentBeginInfo));
                    vt_unserialize_VkRenderPassAttachmentBeginInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);

    uint64_t renderPassId;
    vt_unserialize_VkRenderPass((VkRenderPass)&renderPassId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->renderPass = VkObject_fromId(renderPassId);
#else
    VkObject* renderPassObject = VkObject_create(VK_OBJECT_TYPE_RENDER_PASS, renderPassId);
    val->renderPass = VkObject_toHandle(renderPassObject);
#endif
    bufferOffset += vt_sizeof_VkRenderPass(val->renderPass);

    uint64_t framebufferId;
    vt_unserialize_VkFramebuffer((VkFramebuffer)&framebufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->framebuffer = VkObject_fromId(framebufferId);
#else
    VkObject* framebufferObject = VkObject_create(VK_OBJECT_TYPE_FRAMEBUFFER, framebufferId);
    val->framebuffer = VkObject_toHandle(framebufferObject);
#endif
    bufferOffset += vt_sizeof_VkFramebuffer(val->framebuffer);
    vt_unserialize_VkRect2D(&val->renderArea, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkRect2D(&val->renderArea);
    val->clearValueCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkClearValue* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkClearValue));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkClearValue(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pClearValues = arrValues;
    }
    else val->pClearValues = NULL;
}

static inline int vt_sizeof_VkPhysicalDeviceExternalBufferInfo(VkPhysicalDeviceExternalBufferInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // usage
    bufferSize += 4; // handleType
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceExternalBufferInfo(VkPhysicalDeviceExternalBufferInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkBufferCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(VkBufferUsageFlags*)(outputBuffer + bufferOffset) = val->usage;
    bufferOffset += 4;
    *(VkExternalMemoryHandleTypeFlagBits*)(outputBuffer + bufferOffset) = val->handleType;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceExternalBufferInfo(VkPhysicalDeviceExternalBufferInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkBufferCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->usage = *(VkBufferUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->handleType = *(VkExternalMemoryHandleTypeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkExternalBufferProperties(VkExternalBufferProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkExternalMemoryProperties(&val->externalMemoryProperties);
    return bufferSize;
}

static inline void vt_serialize_VkExternalBufferProperties(VkExternalBufferProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    vt_serialize_VkExternalMemoryProperties(&val->externalMemoryProperties, outputBuffer + bufferOffset);
    bufferOffset += vt_sizeof_VkExternalMemoryProperties(&val->externalMemoryProperties);
}

static inline void vt_unserialize_VkExternalBufferProperties(VkExternalBufferProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    vt_unserialize_VkExternalMemoryProperties(&val->externalMemoryProperties, inputBuffer + bufferOffset, memoryPool);
    bufferOffset += vt_sizeof_VkExternalMemoryProperties(&val->externalMemoryProperties);
}

static inline int vt_sizeof_VkPhysicalDeviceExternalSemaphoreInfo(VkPhysicalDeviceExternalSemaphoreInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_SEMAPHORE_TYPE_CREATE_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkSemaphoreTypeCreateInfo((VkSemaphoreTypeCreateInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += 4; // handleType
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceExternalSemaphoreInfo(VkPhysicalDeviceExternalSemaphoreInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_SEMAPHORE_TYPE_CREATE_INFO: {
                    int itemSize = vt_sizeof_VkSemaphoreTypeCreateInfo((VkSemaphoreTypeCreateInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkSemaphoreTypeCreateInfo((VkSemaphoreTypeCreateInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;
    *(VkExternalSemaphoreHandleTypeFlagBits*)(outputBuffer + bufferOffset) = val->handleType;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceExternalSemaphoreInfo(VkPhysicalDeviceExternalSemaphoreInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_SEMAPHORE_TYPE_CREATE_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkSemaphoreTypeCreateInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_SEMAPHORE_TYPE_CREATE_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkSemaphoreTypeCreateInfo));
                    vt_unserialize_VkSemaphoreTypeCreateInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);
    val->handleType = *(VkExternalSemaphoreHandleTypeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkExternalSemaphoreProperties(VkExternalSemaphoreProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // exportFromImportedHandleTypes
    bufferSize += 4; // compatibleHandleTypes
    bufferSize += 4; // externalSemaphoreFeatures
    return bufferSize;
}

static inline void vt_serialize_VkExternalSemaphoreProperties(VkExternalSemaphoreProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkExternalSemaphoreHandleTypeFlags*)(outputBuffer + bufferOffset) = val->exportFromImportedHandleTypes;
    bufferOffset += 4;
    *(VkExternalSemaphoreHandleTypeFlags*)(outputBuffer + bufferOffset) = val->compatibleHandleTypes;
    bufferOffset += 4;
    *(VkExternalSemaphoreFeatureFlags*)(outputBuffer + bufferOffset) = val->externalSemaphoreFeatures;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkExternalSemaphoreProperties(VkExternalSemaphoreProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->exportFromImportedHandleTypes = *(VkExternalSemaphoreHandleTypeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->compatibleHandleTypes = *(VkExternalSemaphoreHandleTypeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->externalSemaphoreFeatures = *(VkExternalSemaphoreFeatureFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceExternalFenceInfo(VkPhysicalDeviceExternalFenceInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // handleType
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceExternalFenceInfo(VkPhysicalDeviceExternalFenceInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkExternalFenceHandleTypeFlagBits*)(outputBuffer + bufferOffset) = val->handleType;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceExternalFenceInfo(VkPhysicalDeviceExternalFenceInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->handleType = *(VkExternalFenceHandleTypeFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkExternalFenceProperties(VkExternalFenceProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // exportFromImportedHandleTypes
    bufferSize += 4; // compatibleHandleTypes
    bufferSize += 4; // externalFenceFeatures
    return bufferSize;
}

static inline void vt_serialize_VkExternalFenceProperties(VkExternalFenceProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkExternalFenceHandleTypeFlags*)(outputBuffer + bufferOffset) = val->exportFromImportedHandleTypes;
    bufferOffset += 4;
    *(VkExternalFenceHandleTypeFlags*)(outputBuffer + bufferOffset) = val->compatibleHandleTypes;
    bufferOffset += 4;
    *(VkExternalFenceFeatureFlags*)(outputBuffer + bufferOffset) = val->externalFenceFeatures;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkExternalFenceProperties(VkExternalFenceProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->exportFromImportedHandleTypes = *(VkExternalFenceHandleTypeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->compatibleHandleTypes = *(VkExternalFenceHandleTypeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->externalFenceFeatures = *(VkExternalFenceFeatureFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkPhysicalDeviceGroupProperties(VkPhysicalDeviceGroupProperties* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // physicalDeviceCount
    bufferSize += val->physicalDeviceCount * vt_sizeof_VkPhysicalDevice(val->physicalDevices[0]);
    bufferSize += 4; // subsetAllocation
    return bufferSize;
}

static inline void vt_serialize_VkPhysicalDeviceGroupProperties(VkPhysicalDeviceGroupProperties* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->physicalDeviceCount;
    bufferOffset += 4;

    for (int i = 0; i < val->physicalDeviceCount; i++) {
#ifdef VT_SERVER
        vt_serialize_VkPhysicalDevice(val->physicalDevices[i], outputBuffer + bufferOffset);
#else
        VkObject* physicalDevicesObject = VkObject_fromHandle(val->physicalDevices[i]);
        vt_serialize_VkPhysicalDevice((VkPhysicalDevice)&physicalDevicesObject->id, outputBuffer + bufferOffset);
#endif
        bufferOffset += vt_sizeof_VkPhysicalDevice(val->physicalDevices[i]);
    }
    *(VkBool32*)(outputBuffer + bufferOffset) = val->subsetAllocation;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkPhysicalDeviceGroupProperties(VkPhysicalDeviceGroupProperties* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->physicalDeviceCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    for (int i = 0; i < val->physicalDeviceCount; i++) {
        uint64_t physicalDevicesId;
        vt_unserialize_VkPhysicalDevice((VkPhysicalDevice)&physicalDevicesId, inputBuffer + bufferOffset, memoryPool);
        if (physicalDevicesId != VKOBJECT_NULL_ID) {
#ifdef VT_SERVER
            val->physicalDevices[i] = VkObject_fromId(physicalDevicesId);
#else
            VkObject* physicalDevicesObject = VkObject_create(VK_OBJECT_TYPE_PHYSICAL_DEVICE, physicalDevicesId);
            val->physicalDevices[i] = VkObject_toHandle(physicalDevicesObject);
#endif
        }
        else val->physicalDevices[i] = VK_NULL_HANDLE;
        bufferOffset += vt_sizeof_VkPhysicalDevice(val->physicalDevices[i]);
    }
    val->subsetAllocation = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkBindBufferMemoryInfo(VkBindBufferMemoryInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_BIND_BUFFER_MEMORY_DEVICE_GROUP_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkBindBufferMemoryDeviceGroupInfo((VkBindBufferMemoryDeviceGroupInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkBuffer(val->buffer);
    bufferSize += vt_sizeof_VkDeviceMemory(val->memory);
    bufferSize += 8; // memoryOffset
    return bufferSize;
}

static inline void vt_serialize_VkBindBufferMemoryInfo(VkBindBufferMemoryInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_BIND_BUFFER_MEMORY_DEVICE_GROUP_INFO: {
                    int itemSize = vt_sizeof_VkBindBufferMemoryDeviceGroupInfo((VkBindBufferMemoryDeviceGroupInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkBindBufferMemoryDeviceGroupInfo((VkBindBufferMemoryDeviceGroupInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->buffer, outputBuffer + bufferOffset);
#else
    VkObject* bufferObject = VkObject_fromHandle(val->buffer);
    vt_serialize_VkBuffer((VkBuffer)&bufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);

#ifdef VT_SERVER
    vt_serialize_VkDeviceMemory(val->memory, outputBuffer + bufferOffset);
#else
    VkObject* memoryObject = VkObject_fromHandle(val->memory);
    vt_serialize_VkDeviceMemory((VkDeviceMemory)&memoryObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->memoryOffset;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkBindBufferMemoryInfo(VkBindBufferMemoryInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_BIND_BUFFER_MEMORY_DEVICE_GROUP_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkBindBufferMemoryDeviceGroupInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_BIND_BUFFER_MEMORY_DEVICE_GROUP_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkBindBufferMemoryDeviceGroupInfo));
                    vt_unserialize_VkBindBufferMemoryDeviceGroupInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);

    uint64_t bufferId;
    vt_unserialize_VkBuffer((VkBuffer)&bufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->buffer = VkObject_fromId(bufferId);
#else
    VkObject* bufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, bufferId);
    val->buffer = VkObject_toHandle(bufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);

    uint64_t memoryId;
    vt_unserialize_VkDeviceMemory((VkDeviceMemory)&memoryId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->memory = vortekSerializerCastVkObject ? ((ResourceMemory*)VkObject_fromId(memoryId))->memory : VkObject_fromId(memoryId);
#else
    VkObject* memoryObject = VkObject_create(VK_OBJECT_TYPE_DEVICE_MEMORY, memoryId);
    val->memory = VkObject_toHandle(memoryObject);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    val->memoryOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkBindImageMemoryInfo(VkBindImageMemoryInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    void* pNext = val->pNext;
    while (pNext) {
        bufferSize += 4;
        VkBaseOutStructure* sBase = pNext;
        void* oldPNext = sBase->pNext;
        sBase->pNext = NULL;

        switch (sBase->sType) {
            case VK_STRUCTURE_TYPE_BIND_IMAGE_MEMORY_DEVICE_GROUP_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkBindImageMemoryDeviceGroupInfo((VkBindImageMemoryDeviceGroupInfo*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_BIND_IMAGE_MEMORY_SWAPCHAIN_INFO_KHR: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkBindImageMemorySwapchainInfoKHR((VkBindImageMemorySwapchainInfoKHR*)pNext);
                break;
            }
            case VK_STRUCTURE_TYPE_BIND_IMAGE_PLANE_MEMORY_INFO: {
                bufferSize += 4;
                bufferSize += vt_sizeof_VkBindImagePlaneMemoryInfo((VkBindImagePlaneMemoryInfo*)pNext);
                break;
            }
            default: {
                bufferSize += 4;
                break;
            }
        }

        sBase->pNext = oldPNext;
        pNext = sBase->pNext;
    }
    bufferSize += 4;
    bufferSize += vt_sizeof_VkImage(val->image);
    bufferSize += vt_sizeof_VkDeviceMemory(val->memory);
    bufferSize += 8; // memoryOffset
    return bufferSize;
}

static inline void vt_serialize_VkBindImageMemoryInfo(VkBindImageMemoryInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    if (val->pNext) {
        void* pNext = val->pNext;
        while (pNext) {
            VkBaseOutStructure* sBase = pNext;
            *(int*)(outputBuffer + bufferOffset) = sBase->sType;
            bufferOffset += 4;
            void* oldPNext = sBase->pNext;
            sBase->pNext = NULL;

            switch (sBase->sType) {
                case VK_STRUCTURE_TYPE_BIND_IMAGE_MEMORY_DEVICE_GROUP_INFO: {
                    int itemSize = vt_sizeof_VkBindImageMemoryDeviceGroupInfo((VkBindImageMemoryDeviceGroupInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkBindImageMemoryDeviceGroupInfo((VkBindImageMemoryDeviceGroupInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_BIND_IMAGE_MEMORY_SWAPCHAIN_INFO_KHR: {
                    int itemSize = vt_sizeof_VkBindImageMemorySwapchainInfoKHR((VkBindImageMemorySwapchainInfoKHR*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkBindImageMemorySwapchainInfoKHR((VkBindImageMemorySwapchainInfoKHR*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                case VK_STRUCTURE_TYPE_BIND_IMAGE_PLANE_MEMORY_INFO: {
                    int itemSize = vt_sizeof_VkBindImagePlaneMemoryInfo((VkBindImagePlaneMemoryInfo*)pNext);
                    *(int*)(outputBuffer + bufferOffset) = itemSize;
                    bufferOffset += 4;
                    vt_serialize_VkBindImagePlaneMemoryInfo((VkBindImagePlaneMemoryInfo*)pNext, outputBuffer + bufferOffset);
                    bufferOffset += itemSize;
                    break;
                }
                default: {
                    *(int*)(outputBuffer + bufferOffset) = 0;
                    bufferOffset += 4;
                    break;
                }
            }

            sBase->pNext = oldPNext;
            pNext = sBase->pNext;
        }
    }

    *(int*)(outputBuffer + bufferOffset) = -1;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->image, outputBuffer + bufferOffset);
#else
    VkObject* imageObject = VkObject_fromHandle(val->image);
    vt_serialize_VkImage((VkImage)&imageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);

#ifdef VT_SERVER
    vt_serialize_VkDeviceMemory(val->memory, outputBuffer + bufferOffset);
#else
    VkObject* memoryObject = VkObject_fromHandle(val->memory);
    vt_serialize_VkDeviceMemory((VkDeviceMemory)&memoryObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = val->memoryOffset;
    bufferOffset += 8;
}

static inline void vt_unserialize_VkBindImageMemoryInfo(VkBindImageMemoryInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int pNextType = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    bool pNextIsNULL = true;
#else
    bool pNextIsNULL = val->pNext == NULL;
#endif
    void* pNext = NULL;

    while (pNextType != -1) {
        switch (pNextType) {
            case VK_STRUCTURE_TYPE_BIND_IMAGE_MEMORY_DEVICE_GROUP_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkBindImageMemoryDeviceGroupInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_BIND_IMAGE_MEMORY_DEVICE_GROUP_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkBindImageMemoryDeviceGroupInfo));
                    vt_unserialize_VkBindImageMemoryDeviceGroupInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_BIND_IMAGE_MEMORY_SWAPCHAIN_INFO_KHR: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkBindImageMemorySwapchainInfoKHR* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_BIND_IMAGE_MEMORY_SWAPCHAIN_INFO_KHR) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkBindImageMemorySwapchainInfoKHR));
                    vt_unserialize_VkBindImageMemorySwapchainInfoKHR(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            case VK_STRUCTURE_TYPE_BIND_IMAGE_PLANE_MEMORY_INFO: {
                int itemSize = *(int*)(inputBuffer + bufferOffset);
                bufferOffset += 4;
                if (itemSize > 0) {
                    VkBindImagePlaneMemoryInfo* item = !pNextIsNULL ? findNextVkStructure(val->pNext, VK_STRUCTURE_TYPE_BIND_IMAGE_PLANE_MEMORY_INFO) : NULL;
                    if (!item) item = vt_alloc(memoryPool, sizeof(VkBindImagePlaneMemoryInfo));
                    vt_unserialize_VkBindImagePlaneMemoryInfo(item, inputBuffer + bufferOffset, memoryPool);
                    bufferOffset += itemSize;

                    if (pNextIsNULL) {
                        item->pNext = pNext;
                        pNext = item;
                    }
                }
                break;
            }
            default: {
                bufferOffset += 4;
                break;
            }
        }
        pNextType = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += 4;
    }

    if (pNextIsNULL) val->pNext = invertVkStructuresChain(pNext);

    uint64_t imageId;
    vt_unserialize_VkImage((VkImage)&imageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->image = VkObject_fromId(imageId);
#else
    VkObject* imageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, imageId);
    val->image = VkObject_toHandle(imageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->image);

    uint64_t memoryId;
    vt_unserialize_VkDeviceMemory((VkDeviceMemory)&memoryId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->memory = vortekSerializerCastVkObject ? ((ResourceMemory*)VkObject_fromId(memoryId))->memory : VkObject_fromId(memoryId);
#else
    VkObject* memoryObject = VkObject_create(VK_OBJECT_TYPE_DEVICE_MEMORY, memoryId);
    val->memory = VkObject_toHandle(memoryObject);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
    val->memoryOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_VkDeviceBufferMemoryRequirements(VkDeviceBufferMemoryRequirements* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4;
    bufferSize += val->pCreateInfo ? vt_sizeof_VkBufferCreateInfo(val->pCreateInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_VkDeviceBufferMemoryRequirements(VkDeviceBufferMemoryRequirements* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

    if (val->pCreateInfo) {
        int itemSize = vt_sizeof_VkBufferCreateInfo(val->pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkBufferCreateInfo(val->pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkDeviceBufferMemoryRequirements(VkDeviceBufferMemoryRequirements* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        val->pCreateInfo = vt_alloc(memoryPool, sizeof(VkBufferCreateInfo));
        vt_unserialize_VkBufferCreateInfo(val->pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else val->pCreateInfo = NULL;
}

static inline int vt_sizeof_VkDeviceImageMemoryRequirements(VkDeviceImageMemoryRequirements* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4;
    bufferSize += val->pCreateInfo ? vt_sizeof_VkImageCreateInfo(val->pCreateInfo) : 0;
    bufferSize += 4; // planeAspect
    return bufferSize;
}

static inline void vt_serialize_VkDeviceImageMemoryRequirements(VkDeviceImageMemoryRequirements* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

    if (val->pCreateInfo) {
        int itemSize = vt_sizeof_VkImageCreateInfo(val->pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkImageCreateInfo(val->pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkImageAspectFlagBits*)(outputBuffer + bufferOffset) = val->planeAspect;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDeviceImageMemoryRequirements(VkDeviceImageMemoryRequirements* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        val->pCreateInfo = vt_alloc(memoryPool, sizeof(VkImageCreateInfo));
        vt_unserialize_VkImageCreateInfo(val->pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else val->pCreateInfo = NULL;
    val->planeAspect = *(VkImageAspectFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkDeviceQueueInfo2(VkDeviceQueueInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // flags
    bufferSize += 4; // queueFamilyIndex
    bufferSize += 4; // queueIndex
    return bufferSize;
}

static inline void vt_serialize_VkDeviceQueueInfo2(VkDeviceQueueInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkDeviceQueueCreateFlags*)(outputBuffer + bufferOffset) = val->flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->queueFamilyIndex;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->queueIndex;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkDeviceQueueInfo2(VkDeviceQueueInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->flags = *(VkDeviceQueueCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->queueIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkCalibratedTimestampInfoKHR(VkCalibratedTimestampInfoKHR* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += 4; // timeDomain
    return bufferSize;
}

static inline void vt_serialize_VkCalibratedTimestampInfoKHR(VkCalibratedTimestampInfoKHR* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;
    *(VkTimeDomainKHR*)(outputBuffer + bufferOffset) = val->timeDomain;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkCalibratedTimestampInfoKHR(VkCalibratedTimestampInfoKHR* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif
    val->timeDomain = *(VkTimeDomainKHR*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkBufferDeviceAddressInfo(VkBufferDeviceAddressInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkBuffer(val->buffer);
    return bufferSize;
}

static inline void vt_serialize_VkBufferDeviceAddressInfo(VkBufferDeviceAddressInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->buffer, outputBuffer + bufferOffset);
#else
    VkObject* bufferObject = VkObject_fromHandle(val->buffer);
    vt_serialize_VkBuffer((VkBuffer)&bufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
}

static inline void vt_unserialize_VkBufferDeviceAddressInfo(VkBufferDeviceAddressInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t bufferId;
    vt_unserialize_VkBuffer((VkBuffer)&bufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->buffer = VkObject_fromId(bufferId);
#else
    VkObject* bufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, bufferId);
    val->buffer = VkObject_toHandle(bufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->buffer);
}

static inline int vt_sizeof_VkDeviceMemoryOpaqueCaptureAddressInfo(VkDeviceMemoryOpaqueCaptureAddressInfo* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkDeviceMemory(val->memory);
    return bufferSize;
}

static inline void vt_serialize_VkDeviceMemoryOpaqueCaptureAddressInfo(VkDeviceMemoryOpaqueCaptureAddressInfo* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkDeviceMemory(val->memory, outputBuffer + bufferOffset);
#else
    VkObject* memoryObject = VkObject_fromHandle(val->memory);
    vt_serialize_VkDeviceMemory((VkDeviceMemory)&memoryObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
}

static inline void vt_unserialize_VkDeviceMemoryOpaqueCaptureAddressInfo(VkDeviceMemoryOpaqueCaptureAddressInfo* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t memoryId;
    vt_unserialize_VkDeviceMemory((VkDeviceMemory)&memoryId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->memory = vortekSerializerCastVkObject ? ((ResourceMemory*)VkObject_fromId(memoryId))->memory : VkObject_fromId(memoryId);
#else
    VkObject* memoryObject = VkObject_create(VK_OBJECT_TYPE_DEVICE_MEMORY, memoryId);
    val->memory = VkObject_toHandle(memoryObject);
#endif
    bufferOffset += vt_sizeof_VkDeviceMemory(val->memory);
}

static inline int vt_sizeof_VkCopyBufferInfo2(VkCopyBufferInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkBuffer(val->srcBuffer);
    bufferSize += vt_sizeof_VkBuffer(val->dstBuffer);
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBufferCopy2(&val->pRegions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkCopyBufferInfo2(VkCopyBufferInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->srcBuffer, outputBuffer + bufferOffset);
#else
    VkObject* srcBufferObject = VkObject_fromHandle(val->srcBuffer);
    vt_serialize_VkBuffer((VkBuffer)&srcBufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->srcBuffer);

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->dstBuffer, outputBuffer + bufferOffset);
#else
    VkObject* dstBufferObject = VkObject_fromHandle(val->dstBuffer);
    vt_serialize_VkBuffer((VkBuffer)&dstBufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->dstBuffer);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->regionCount;
    bufferOffset += 4;

    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBufferCopy2(&val->pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBufferCopy2(&val->pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkCopyBufferInfo2(VkCopyBufferInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t srcBufferId;
    vt_unserialize_VkBuffer((VkBuffer)&srcBufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->srcBuffer = VkObject_fromId(srcBufferId);
#else
    VkObject* srcBufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, srcBufferId);
    val->srcBuffer = VkObject_toHandle(srcBufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->srcBuffer);

    uint64_t dstBufferId;
    vt_unserialize_VkBuffer((VkBuffer)&dstBufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->dstBuffer = VkObject_fromId(dstBufferId);
#else
    VkObject* dstBufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, dstBufferId);
    val->dstBuffer = VkObject_toHandle(dstBufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->dstBuffer);
    val->regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkBufferCopy2* arrValues = vt_alloc(memoryPool, dataSize6 * sizeof(VkBufferCopy2));
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBufferCopy2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pRegions = arrValues;
    }
    else val->pRegions = NULL;
}

static inline int vt_sizeof_VkCopyImageInfo2(VkCopyImageInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkImage(val->srcImage);
    bufferSize += 4; // srcImageLayout
    bufferSize += vt_sizeof_VkImage(val->dstImage);
    bufferSize += 4; // dstImageLayout
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkImageCopy2(&val->pRegions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkCopyImageInfo2(VkCopyImageInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->srcImage, outputBuffer + bufferOffset);
#else
    VkObject* srcImageObject = VkObject_fromHandle(val->srcImage);
    vt_serialize_VkImage((VkImage)&srcImageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->srcImage);
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->srcImageLayout;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->dstImage, outputBuffer + bufferOffset);
#else
    VkObject* dstImageObject = VkObject_fromHandle(val->dstImage);
    vt_serialize_VkImage((VkImage)&dstImageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->dstImage);
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->dstImageLayout;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->regionCount;
    bufferOffset += 4;

    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkImageCopy2(&val->pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkImageCopy2(&val->pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkCopyImageInfo2(VkCopyImageInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t srcImageId;
    vt_unserialize_VkImage((VkImage)&srcImageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->srcImage = VkObject_fromId(srcImageId);
#else
    VkObject* srcImageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, srcImageId);
    val->srcImage = VkObject_toHandle(srcImageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->srcImage);
    val->srcImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t dstImageId;
    vt_unserialize_VkImage((VkImage)&dstImageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->dstImage = VkObject_fromId(dstImageId);
#else
    VkObject* dstImageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, dstImageId);
    val->dstImage = VkObject_toHandle(dstImageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->dstImage);
    val->dstImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        VkImageCopy2* arrValues = vt_alloc(memoryPool, dataSize8 * sizeof(VkImageCopy2));
        for (int i = 0; i < dataSize8; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkImageCopy2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pRegions = arrValues;
    }
    else val->pRegions = NULL;
}

static inline int vt_sizeof_VkBlitImageInfo2(VkBlitImageInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkImage(val->srcImage);
    bufferSize += 4; // srcImageLayout
    bufferSize += vt_sizeof_VkImage(val->dstImage);
    bufferSize += 4; // dstImageLayout
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkImageBlit2(&val->pRegions[i]);
        }
    }
    bufferSize += 4; // filter
    return bufferSize;
}

static inline void vt_serialize_VkBlitImageInfo2(VkBlitImageInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->srcImage, outputBuffer + bufferOffset);
#else
    VkObject* srcImageObject = VkObject_fromHandle(val->srcImage);
    vt_serialize_VkImage((VkImage)&srcImageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->srcImage);
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->srcImageLayout;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->dstImage, outputBuffer + bufferOffset);
#else
    VkObject* dstImageObject = VkObject_fromHandle(val->dstImage);
    vt_serialize_VkImage((VkImage)&dstImageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->dstImage);
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->dstImageLayout;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->regionCount;
    bufferOffset += 4;

    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkImageBlit2(&val->pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkImageBlit2(&val->pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkFilter*)(outputBuffer + bufferOffset) = val->filter;
    bufferOffset += 4;
}

static inline void vt_unserialize_VkBlitImageInfo2(VkBlitImageInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t srcImageId;
    vt_unserialize_VkImage((VkImage)&srcImageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->srcImage = VkObject_fromId(srcImageId);
#else
    VkObject* srcImageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, srcImageId);
    val->srcImage = VkObject_toHandle(srcImageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->srcImage);
    val->srcImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t dstImageId;
    vt_unserialize_VkImage((VkImage)&dstImageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->dstImage = VkObject_fromId(dstImageId);
#else
    VkObject* dstImageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, dstImageId);
    val->dstImage = VkObject_toHandle(dstImageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->dstImage);
    val->dstImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        VkImageBlit2* arrValues = vt_alloc(memoryPool, dataSize8 * sizeof(VkImageBlit2));
        for (int i = 0; i < dataSize8; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkImageBlit2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pRegions = arrValues;
    }
    else val->pRegions = NULL;
    val->filter = *(VkFilter*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_VkCopyBufferToImageInfo2(VkCopyBufferToImageInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkBuffer(val->srcBuffer);
    bufferSize += vt_sizeof_VkImage(val->dstImage);
    bufferSize += 4; // dstImageLayout
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBufferImageCopy2(&val->pRegions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkCopyBufferToImageInfo2(VkCopyBufferToImageInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->srcBuffer, outputBuffer + bufferOffset);
#else
    VkObject* srcBufferObject = VkObject_fromHandle(val->srcBuffer);
    vt_serialize_VkBuffer((VkBuffer)&srcBufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->srcBuffer);

#ifdef VT_SERVER
    vt_serialize_VkImage(val->dstImage, outputBuffer + bufferOffset);
#else
    VkObject* dstImageObject = VkObject_fromHandle(val->dstImage);
    vt_serialize_VkImage((VkImage)&dstImageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->dstImage);
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->dstImageLayout;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->regionCount;
    bufferOffset += 4;

    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBufferImageCopy2(&val->pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBufferImageCopy2(&val->pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkCopyBufferToImageInfo2(VkCopyBufferToImageInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t srcBufferId;
    vt_unserialize_VkBuffer((VkBuffer)&srcBufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->srcBuffer = VkObject_fromId(srcBufferId);
#else
    VkObject* srcBufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, srcBufferId);
    val->srcBuffer = VkObject_toHandle(srcBufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->srcBuffer);

    uint64_t dstImageId;
    vt_unserialize_VkImage((VkImage)&dstImageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->dstImage = VkObject_fromId(dstImageId);
#else
    VkObject* dstImageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, dstImageId);
    val->dstImage = VkObject_toHandle(dstImageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->dstImage);
    val->dstImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkBufferImageCopy2* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkBufferImageCopy2));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBufferImageCopy2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pRegions = arrValues;
    }
    else val->pRegions = NULL;
}

static inline int vt_sizeof_VkCopyImageToBufferInfo2(VkCopyImageToBufferInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkImage(val->srcImage);
    bufferSize += 4; // srcImageLayout
    bufferSize += vt_sizeof_VkBuffer(val->dstBuffer);
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBufferImageCopy2(&val->pRegions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkCopyImageToBufferInfo2(VkCopyImageToBufferInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->srcImage, outputBuffer + bufferOffset);
#else
    VkObject* srcImageObject = VkObject_fromHandle(val->srcImage);
    vt_serialize_VkImage((VkImage)&srcImageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->srcImage);
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->srcImageLayout;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkBuffer(val->dstBuffer, outputBuffer + bufferOffset);
#else
    VkObject* dstBufferObject = VkObject_fromHandle(val->dstBuffer);
    vt_serialize_VkBuffer((VkBuffer)&dstBufferObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->dstBuffer);
    *(uint32_t*)(outputBuffer + bufferOffset) = val->regionCount;
    bufferOffset += 4;

    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBufferImageCopy2(&val->pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBufferImageCopy2(&val->pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkCopyImageToBufferInfo2(VkCopyImageToBufferInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t srcImageId;
    vt_unserialize_VkImage((VkImage)&srcImageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->srcImage = VkObject_fromId(srcImageId);
#else
    VkObject* srcImageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, srcImageId);
    val->srcImage = VkObject_toHandle(srcImageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->srcImage);
    val->srcImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t dstBufferId;
    vt_unserialize_VkBuffer((VkBuffer)&dstBufferId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->dstBuffer = VkObject_fromId(dstBufferId);
#else
    VkObject* dstBufferObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, dstBufferId);
    val->dstBuffer = VkObject_toHandle(dstBufferObject);
#endif
    bufferOffset += vt_sizeof_VkBuffer(val->dstBuffer);
    val->regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkBufferImageCopy2* arrValues = vt_alloc(memoryPool, dataSize7 * sizeof(VkBufferImageCopy2));
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBufferImageCopy2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pRegions = arrValues;
    }
    else val->pRegions = NULL;
}

static inline int vt_sizeof_VkResolveImageInfo2(VkResolveImageInfo2* val) {
    int bufferSize = 0;
    bufferSize += 4; // sType
    bufferSize += vt_sizeof_VkImage(val->srcImage);
    bufferSize += 4; // srcImageLayout
    bufferSize += vt_sizeof_VkImage(val->dstImage);
    bufferSize += 4; // dstImageLayout
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkImageResolve2(&val->pRegions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_VkResolveImageInfo2(VkResolveImageInfo2* val, char* outputBuffer) {
    int bufferOffset = 0;
    *(VkStructureType*)(outputBuffer + bufferOffset) = val->sType;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->srcImage, outputBuffer + bufferOffset);
#else
    VkObject* srcImageObject = VkObject_fromHandle(val->srcImage);
    vt_serialize_VkImage((VkImage)&srcImageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->srcImage);
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->srcImageLayout;
    bufferOffset += 4;

#ifdef VT_SERVER
    vt_serialize_VkImage(val->dstImage, outputBuffer + bufferOffset);
#else
    VkObject* dstImageObject = VkObject_fromHandle(val->dstImage);
    vt_serialize_VkImage((VkImage)&dstImageObject->id, outputBuffer + bufferOffset);
#endif
    bufferOffset += vt_sizeof_VkImage(val->dstImage);
    *(VkImageLayout*)(outputBuffer + bufferOffset) = val->dstImageLayout;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = val->regionCount;
    bufferOffset += 4;

    if (val->pRegions) {
        int arrSize = val->regionCount ? val->regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkImageResolve2(&val->pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkImageResolve2(&val->pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_VkResolveImageInfo2(VkResolveImageInfo2* val, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;
    val->sType = *(VkStructureType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
#ifdef VT_SERVER
    val->pNext = NULL;
#endif

    uint64_t srcImageId;
    vt_unserialize_VkImage((VkImage)&srcImageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->srcImage = VkObject_fromId(srcImageId);
#else
    VkObject* srcImageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, srcImageId);
    val->srcImage = VkObject_toHandle(srcImageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->srcImage);
    val->srcImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    uint64_t dstImageId;
    vt_unserialize_VkImage((VkImage)&dstImageId, inputBuffer + bufferOffset, memoryPool);
#ifdef VT_SERVER
    val->dstImage = VkObject_fromId(dstImageId);
#else
    VkObject* dstImageObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, dstImageId);
    val->dstImage = VkObject_toHandle(dstImageObject);
#endif
    bufferOffset += vt_sizeof_VkImage(val->dstImage);
    val->dstImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    val->regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        VkImageResolve2* arrValues = vt_alloc(memoryPool, dataSize8 * sizeof(VkImageResolve2));
        for (int i = 0; i < dataSize8; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkImageResolve2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
        val->pRegions = arrValues;
    }
    else val->pRegions = NULL;
}

static inline int vt_sizeof_vkCreateInstance(const VkInstanceCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkInstance* pInstance) {
    int bufferSize = 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkInstanceCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pInstance ? vt_sizeof_VkInstance(*pInstance) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateInstance(const VkInstanceCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkInstance* pInstance, char* outputBuffer) {
    int bufferOffset = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkInstanceCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkInstanceCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pInstance) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkInstance(*pInstance, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkInstance(*pInstance);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateInstance(const VkInstanceCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkInstance* pInstance, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    int dataSize1 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize1 > 0) {
        if (pCreateInfo) vt_unserialize_VkInstanceCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize1;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pInstance) {
            vt_unserialize_VkInstance(*pInstance, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkInstance(*pInstance);
        }
        else bufferOffset += vt_sizeof_VkInstance(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyInstance(VkInstance instance, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += instance ? vt_sizeof_VkInstance(instance) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyInstance(VkInstance instance, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (instance) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkInstance(instance, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkInstance(instance);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyInstance(VkInstance instance, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (instance) {
            vt_unserialize_VkInstance(instance, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkInstance(instance);
        }
        else bufferOffset += vt_sizeof_VkInstance(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkEnumeratePhysicalDevices(VkInstance instance, uint32_t* pPhysicalDeviceCount, VkPhysicalDevice* pPhysicalDevices) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += instance ? vt_sizeof_VkInstance(instance) : 0;
    bufferSize += 4;
    bufferSize += pPhysicalDeviceCount ? 4 : 0;
    bufferSize += 4;
    if (pPhysicalDevices) {
        for (int i = 0; i < *pPhysicalDeviceCount; i++) {
            bufferSize += vt_sizeof_VkPhysicalDevice(pPhysicalDevices[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkEnumeratePhysicalDevices(VkInstance instance, uint32_t* pPhysicalDeviceCount, VkPhysicalDevice* pPhysicalDevices, char* outputBuffer) {
    int bufferOffset = 0;

    if (instance) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkInstance(instance, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkInstance(instance);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pPhysicalDeviceCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pPhysicalDeviceCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pPhysicalDevices) {
        *(int*)(outputBuffer + bufferOffset) = *pPhysicalDeviceCount;
        bufferOffset += 4;
        for (int i = 0; i < *pPhysicalDeviceCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkPhysicalDevice(pPhysicalDevices[i], outputBuffer + bufferOffset);
#else
            VkObject* physicalDevicesObject = VkObject_fromHandle(pPhysicalDevices[i]);
            vt_serialize_VkPhysicalDevice((VkPhysicalDevice)&physicalDevicesObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkPhysicalDevice(pPhysicalDevices[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkEnumeratePhysicalDevices(VkInstance instance, uint32_t* pPhysicalDeviceCount, VkPhysicalDevice* pPhysicalDevices, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (instance) {
            vt_unserialize_VkInstance(instance, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkInstance(instance);
        }
        else bufferOffset += vt_sizeof_VkInstance(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pPhysicalDeviceCount) *pPhysicalDeviceCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize2;
    }
    else if (pPhysicalDeviceCount) pPhysicalDeviceCount = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkPhysicalDevice* arrValues = pPhysicalDevices;
        for (int i = 0; i < dataSize3; i++) {
            uint64_t physicalDevicesId;
            vt_unserialize_VkPhysicalDevice((VkPhysicalDevice)&physicalDevicesId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(physicalDevicesId);
#else
                VkObject* physicalDevicesObject = VkObject_create(VK_OBJECT_TYPE_PHYSICAL_DEVICE, physicalDevicesId);
                arrValues[i] = VkObject_toHandle(physicalDevicesObject);
#endif
                bufferOffset += vt_sizeof_VkPhysicalDevice(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
        }
    }
    else pPhysicalDevices = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceProperties(VkPhysicalDevice physicalDevice, VkPhysicalDeviceProperties* pProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pProperties ? vt_sizeof_VkPhysicalDeviceProperties(pProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceProperties(VkPhysicalDevice physicalDevice, VkPhysicalDeviceProperties* pProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pProperties) {
        int itemSize = vt_sizeof_VkPhysicalDeviceProperties(pProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceProperties(pProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceProperties(VkPhysicalDevice physicalDevice, VkPhysicalDeviceProperties* pProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pProperties) vt_unserialize_VkPhysicalDeviceProperties(pProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pProperties) pProperties = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceQueueFamilyProperties(VkPhysicalDevice physicalDevice, uint32_t* pQueueFamilyPropertyCount, VkQueueFamilyProperties* pQueueFamilyProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pQueueFamilyPropertyCount ? 4 : 0;
    bufferSize += 4;
    if (pQueueFamilyProperties) {
        int arrSize = pQueueFamilyPropertyCount ? *pQueueFamilyPropertyCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkQueueFamilyProperties(&pQueueFamilyProperties[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceQueueFamilyProperties(VkPhysicalDevice physicalDevice, uint32_t* pQueueFamilyPropertyCount, VkQueueFamilyProperties* pQueueFamilyProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pQueueFamilyPropertyCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pQueueFamilyPropertyCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pQueueFamilyProperties) {
        int arrSize = pQueueFamilyPropertyCount ? *pQueueFamilyPropertyCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkQueueFamilyProperties(&pQueueFamilyProperties[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkQueueFamilyProperties(&pQueueFamilyProperties[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceQueueFamilyProperties(VkPhysicalDevice physicalDevice, uint32_t* pQueueFamilyPropertyCount, VkQueueFamilyProperties* pQueueFamilyProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pQueueFamilyPropertyCount) *pQueueFamilyPropertyCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize2;
    }
    else if (pQueueFamilyPropertyCount) pQueueFamilyPropertyCount = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkQueueFamilyProperties* arrValues = pQueueFamilyProperties;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkQueueFamilyProperties(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pQueueFamilyProperties = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceMemoryProperties(VkPhysicalDevice physicalDevice, VkPhysicalDeviceMemoryProperties* pMemoryProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pMemoryProperties ? vt_sizeof_VkPhysicalDeviceMemoryProperties(pMemoryProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceMemoryProperties(VkPhysicalDevice physicalDevice, VkPhysicalDeviceMemoryProperties* pMemoryProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pMemoryProperties) {
        int itemSize = vt_sizeof_VkPhysicalDeviceMemoryProperties(pMemoryProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceMemoryProperties(pMemoryProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceMemoryProperties(VkPhysicalDevice physicalDevice, VkPhysicalDeviceMemoryProperties* pMemoryProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pMemoryProperties) vt_unserialize_VkPhysicalDeviceMemoryProperties(pMemoryProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pMemoryProperties) pMemoryProperties = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceFeatures(VkPhysicalDevice physicalDevice, VkPhysicalDeviceFeatures* pFeatures) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pFeatures ? vt_sizeof_VkPhysicalDeviceFeatures(pFeatures) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceFeatures(VkPhysicalDevice physicalDevice, VkPhysicalDeviceFeatures* pFeatures, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pFeatures) {
        int itemSize = vt_sizeof_VkPhysicalDeviceFeatures(pFeatures);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceFeatures(pFeatures, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceFeatures(VkPhysicalDevice physicalDevice, VkPhysicalDeviceFeatures* pFeatures, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pFeatures) vt_unserialize_VkPhysicalDeviceFeatures(pFeatures, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pFeatures) pFeatures = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceFormatProperties(VkPhysicalDevice physicalDevice, VkFormat format, VkFormatProperties* pFormatProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4; // format
    bufferSize += 4;
    bufferSize += pFormatProperties ? vt_sizeof_VkFormatProperties(pFormatProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceFormatProperties(VkPhysicalDevice physicalDevice, VkFormat format, VkFormatProperties* pFormatProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkFormat*)(outputBuffer + bufferOffset) = format;
    bufferOffset += 4;

    if (pFormatProperties) {
        int itemSize = vt_sizeof_VkFormatProperties(pFormatProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkFormatProperties(pFormatProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceFormatProperties(VkPhysicalDevice physicalDevice, VkFormat* format, VkFormatProperties* pFormatProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }
    if (format) *format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pFormatProperties) vt_unserialize_VkFormatProperties(pFormatProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pFormatProperties) pFormatProperties = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceImageFormatProperties(VkPhysicalDevice physicalDevice, VkFormat format, VkImageType type, VkImageTiling tiling, VkImageUsageFlags usage, VkImageCreateFlags flags, VkImageFormatProperties* pImageFormatProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4; // format
    bufferSize += 4; // type
    bufferSize += 4; // tiling
    bufferSize += 4; // usage
    bufferSize += 4; // flags
    bufferSize += 4;
    bufferSize += pImageFormatProperties ? vt_sizeof_VkImageFormatProperties(pImageFormatProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceImageFormatProperties(VkPhysicalDevice physicalDevice, VkFormat format, VkImageType type, VkImageTiling tiling, VkImageUsageFlags usage, VkImageCreateFlags flags, VkImageFormatProperties* pImageFormatProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkFormat*)(outputBuffer + bufferOffset) = format;
    bufferOffset += 4;
    *(VkImageType*)(outputBuffer + bufferOffset) = type;
    bufferOffset += 4;
    *(VkImageTiling*)(outputBuffer + bufferOffset) = tiling;
    bufferOffset += 4;
    *(VkImageUsageFlags*)(outputBuffer + bufferOffset) = usage;
    bufferOffset += 4;
    *(VkImageCreateFlags*)(outputBuffer + bufferOffset) = flags;
    bufferOffset += 4;

    if (pImageFormatProperties) {
        int itemSize = vt_sizeof_VkImageFormatProperties(pImageFormatProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkImageFormatProperties(pImageFormatProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceImageFormatProperties(VkPhysicalDevice physicalDevice, VkFormat* format, VkImageType* type, VkImageTiling* tiling, VkImageUsageFlags* usage, VkImageCreateFlags* flags, VkImageFormatProperties* pImageFormatProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }
    if (format) *format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (type) *type = *(VkImageType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (tiling) *tiling = *(VkImageTiling*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (usage) *usage = *(VkImageUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (flags) *flags = *(VkImageCreateFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        if (pImageFormatProperties) vt_unserialize_VkImageFormatProperties(pImageFormatProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize7;
    }
    else if (pImageFormatProperties) pImageFormatProperties = NULL;
}

static inline int vt_sizeof_vkCreateDevice(VkPhysicalDevice physicalDevice, const VkDeviceCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkDevice* pDevice) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkDeviceCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pDevice ? vt_sizeof_VkDevice(*pDevice) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateDevice(VkPhysicalDevice physicalDevice, const VkDeviceCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkDevice* pDevice, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkDeviceCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDeviceCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(*pDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(*pDevice);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateDevice(VkPhysicalDevice physicalDevice, const VkDeviceCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkDevice* pDevice, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkDeviceCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pDevice) {
            vt_unserialize_VkDevice(*pDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(*pDevice);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyDevice(VkDevice device, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyDevice(VkDevice device, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyDevice(VkDevice device, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkEnumerateInstanceVersion(uint32_t* pApiVersion) {
    int bufferSize = 0;
    bufferSize += 4;
    bufferSize += pApiVersion ? 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkEnumerateInstanceVersion(uint32_t* pApiVersion, char* outputBuffer) {
    int bufferOffset = 0;

    if (pApiVersion) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pApiVersion;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkEnumerateInstanceVersion(uint32_t* pApiVersion, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    int dataSize1 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize1 > 0) {
        if (pApiVersion) *pApiVersion = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize1;
    }
    else if (pApiVersion) pApiVersion = NULL;
}

static inline int vt_sizeof_vkEnumerateInstanceExtensionProperties(const char* pLayerName, uint32_t* pPropertyCount, VkExtensionProperties* pProperties) {
    int bufferSize = 0;
    bufferSize += 4;
    bufferSize += pLayerName ? strlen(pLayerName) + 1 : 0;
    bufferSize += 4;
    bufferSize += pPropertyCount ? 4 : 0;
    bufferSize += 4;
    if (pProperties) {
        int arrSize = pPropertyCount ? *pPropertyCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkExtensionProperties(&pProperties[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkEnumerateInstanceExtensionProperties(const char* pLayerName, uint32_t* pPropertyCount, VkExtensionProperties* pProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (pLayerName) {
        int strSize = strlen(pLayerName) + 1;
        *(int*)(outputBuffer + bufferOffset) = strSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pLayerName, strSize);
        bufferOffset += strSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pPropertyCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pPropertyCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pProperties) {
        int arrSize = pPropertyCount ? *pPropertyCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkExtensionProperties(&pProperties[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkExtensionProperties(&pProperties[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkEnumerateInstanceExtensionProperties(char* pLayerName, uint32_t* pPropertyCount, VkExtensionProperties* pProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    int dataSize1 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize1 > 0) {
        char* arrValues = vt_alloc(memoryPool, dataSize1);
        memcpy(arrValues, inputBuffer + bufferOffset, dataSize1);
        pLayerName = arrValues;
        bufferOffset += dataSize1;
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pPropertyCount) *pPropertyCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize2;
    }
    else if (pPropertyCount) pPropertyCount = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkExtensionProperties* arrValues = pProperties;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkExtensionProperties(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pProperties = NULL;
}

static inline int vt_sizeof_vkEnumerateDeviceExtensionProperties(VkPhysicalDevice physicalDevice, const char* pLayerName, uint32_t* pPropertyCount, VkExtensionProperties* pProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pLayerName ? strlen(pLayerName) + 1 : 0;
    bufferSize += 4;
    bufferSize += pPropertyCount ? 4 : 0;
    bufferSize += 4;
    if (pProperties) {
        int arrSize = pPropertyCount ? *pPropertyCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkExtensionProperties(&pProperties[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkEnumerateDeviceExtensionProperties(VkPhysicalDevice physicalDevice, const char* pLayerName, uint32_t* pPropertyCount, VkExtensionProperties* pProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pLayerName) {
        int strSize = strlen(pLayerName) + 1;
        *(int*)(outputBuffer + bufferOffset) = strSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pLayerName, strSize);
        bufferOffset += strSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pPropertyCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pPropertyCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pProperties) {
        int arrSize = pPropertyCount ? *pPropertyCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkExtensionProperties(&pProperties[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkExtensionProperties(&pProperties[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkEnumerateDeviceExtensionProperties(VkPhysicalDevice physicalDevice, char* pLayerName, uint32_t* pPropertyCount, VkExtensionProperties* pProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        char* arrValues = vt_alloc(memoryPool, dataSize2);
        memcpy(arrValues, inputBuffer + bufferOffset, dataSize2);
        pLayerName = arrValues;
        bufferOffset += dataSize2;
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pPropertyCount) *pPropertyCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pPropertyCount) pPropertyCount = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkExtensionProperties* arrValues = pProperties;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkExtensionProperties(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pProperties = NULL;
}

static inline int vt_sizeof_vkGetDeviceQueue(VkDevice device, uint32_t queueFamilyIndex, uint32_t queueIndex, VkQueue* pQueue) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4; // queueFamilyIndex
    bufferSize += 4; // queueIndex
    bufferSize++;
    bufferSize += pQueue ? vt_sizeof_VkQueue(*pQueue) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetDeviceQueue(VkDevice device, uint32_t queueFamilyIndex, uint32_t queueIndex, VkQueue* pQueue, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = queueFamilyIndex;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = queueIndex;
    bufferOffset += 4;

    if (pQueue) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueue(*pQueue, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueue(*pQueue);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkGetDeviceQueue(VkDevice device, uint32_t* queueFamilyIndex, uint32_t* queueIndex, VkQueue* pQueue, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
    if (queueFamilyIndex) *queueFamilyIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (queueIndex) *queueIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    if (inputBuffer[bufferOffset++]) {
        if (pQueue) {
            vt_unserialize_VkQueue(*pQueue, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueue(*pQueue);
        }
        else bufferOffset += vt_sizeof_VkQueue(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkQueueSubmit(VkQueue queue, uint32_t submitCount, const VkSubmitInfo* pSubmits, VkFence fence) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += queue ? vt_sizeof_VkQueue(queue) : 0;
    bufferSize += 4; // submitCount
    bufferSize += 4;
    if (pSubmits) {
        int arrSize = submitCount ? submitCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSubmitInfo(&pSubmits[i]);
        }
    }
    bufferSize++;
    bufferSize += fence ? vt_sizeof_VkFence(fence) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkQueueSubmit(VkQueue queue, uint32_t submitCount, const VkSubmitInfo* pSubmits, VkFence fence, char* outputBuffer) {
    int bufferOffset = 0;

    if (queue) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueue(queue, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueue(queue);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = submitCount;
    bufferOffset += 4;

    if (pSubmits) {
        int arrSize = submitCount ? submitCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSubmitInfo(&pSubmits[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSubmitInfo(&pSubmits[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (fence) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkFence(fence, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkFence(fence);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkQueueSubmit(VkQueue queue, uint32_t* submitCount, const VkSubmitInfo* pSubmits, VkFence fence, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (queue) {
            vt_unserialize_VkQueue(queue, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueue(queue);
        }
        else bufferOffset += vt_sizeof_VkQueue(VK_NULL_HANDLE);
    }
    if (submitCount) *submitCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkSubmitInfo* arrValues = pSubmits;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSubmitInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pSubmits = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (fence) {
            vt_unserialize_VkFence(fence, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkFence(fence);
        }
        else bufferOffset += vt_sizeof_VkFence(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkQueueWaitIdle(VkQueue queue) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += queue ? vt_sizeof_VkQueue(queue) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkQueueWaitIdle(VkQueue queue, char* outputBuffer) {
    int bufferOffset = 0;

    if (queue) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueue(queue, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueue(queue);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkQueueWaitIdle(VkQueue queue, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (queue) {
            vt_unserialize_VkQueue(queue, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueue(queue);
        }
        else bufferOffset += vt_sizeof_VkQueue(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDeviceWaitIdle(VkDevice device) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDeviceWaitIdle(VkDevice device, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDeviceWaitIdle(VkDevice device, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkAllocateMemory(VkDevice device, const VkMemoryAllocateInfo* pAllocateInfo, const VkAllocationCallbacks* pAllocator, VkDeviceMemory* pMemory) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pAllocateInfo ? vt_sizeof_VkMemoryAllocateInfo(pAllocateInfo) : 0;
    bufferSize++;
    bufferSize += pMemory ? vt_sizeof_VkDeviceMemory(*pMemory) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkAllocateMemory(VkDevice device, const VkMemoryAllocateInfo* pAllocateInfo, const VkAllocationCallbacks* pAllocator, VkDeviceMemory* pMemory, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pAllocateInfo) {
        int itemSize = vt_sizeof_VkMemoryAllocateInfo(pAllocateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkMemoryAllocateInfo(pAllocateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pMemory) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDeviceMemory(*pMemory, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDeviceMemory(*pMemory);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkAllocateMemory(VkDevice device, const VkMemoryAllocateInfo* pAllocateInfo, const VkAllocationCallbacks* pAllocator, VkDeviceMemory* pMemory, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pAllocateInfo) vt_unserialize_VkMemoryAllocateInfo(pAllocateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pAllocateInfo) pAllocateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pMemory) {
            vt_unserialize_VkDeviceMemory(*pMemory, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDeviceMemory(*pMemory);
        }
        else bufferOffset += vt_sizeof_VkDeviceMemory(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkFreeMemory(VkDevice device, VkDeviceMemory memory, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += memory ? vt_sizeof_VkDeviceMemory(memory) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkFreeMemory(VkDevice device, VkDeviceMemory memory, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (memory) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDeviceMemory(memory, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDeviceMemory(memory);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkFreeMemory(VkDevice device, VkDeviceMemory memory, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (memory) {
            vt_unserialize_VkDeviceMemory(memory, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDeviceMemory(memory);
        }
        else bufferOffset += vt_sizeof_VkDeviceMemory(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkMapMemory(VkDevice device, VkDeviceMemory memory, VkDeviceSize offset, VkDeviceSize size, VkMemoryMapFlags flags, void** ppData) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += memory ? vt_sizeof_VkDeviceMemory(memory) : 0;
    bufferSize += 8; // offset
    bufferSize += 8; // size
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_vkMapMemory(VkDevice device, VkDeviceMemory memory, VkDeviceSize offset, VkDeviceSize size, VkMemoryMapFlags flags, void** ppData, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (memory) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDeviceMemory(memory, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDeviceMemory(memory);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = offset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = size;
    bufferOffset += 8;
    *(VkMemoryMapFlags*)(outputBuffer + bufferOffset) = flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkMapMemory(VkDevice device, VkDeviceMemory memory, VkDeviceSize* offset, VkDeviceSize* size, VkMemoryMapFlags* flags, void** ppData, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (memory) {
            vt_unserialize_VkDeviceMemory(memory, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDeviceMemory(memory);
        }
        else bufferOffset += vt_sizeof_VkDeviceMemory(VK_NULL_HANDLE);
    }
    if (offset) *offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (size) *size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (flags) *flags = *(VkMemoryMapFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkUnmapMemory(VkDevice device, VkDeviceMemory memory) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += memory ? vt_sizeof_VkDeviceMemory(memory) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkUnmapMemory(VkDevice device, VkDeviceMemory memory, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (memory) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDeviceMemory(memory, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDeviceMemory(memory);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkUnmapMemory(VkDevice device, VkDeviceMemory memory, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (memory) {
            vt_unserialize_VkDeviceMemory(memory, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDeviceMemory(memory);
        }
        else bufferOffset += vt_sizeof_VkDeviceMemory(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkFlushMappedMemoryRanges(VkDevice device, uint32_t memoryRangeCount, const VkMappedMemoryRange* pMemoryRanges) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4; // memoryRangeCount
    bufferSize += 4;
    if (pMemoryRanges) {
        int arrSize = memoryRangeCount ? memoryRangeCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkMappedMemoryRange(&pMemoryRanges[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkFlushMappedMemoryRanges(VkDevice device, uint32_t memoryRangeCount, const VkMappedMemoryRange* pMemoryRanges, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = memoryRangeCount;
    bufferOffset += 4;

    if (pMemoryRanges) {
        int arrSize = memoryRangeCount ? memoryRangeCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkMappedMemoryRange(&pMemoryRanges[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkMappedMemoryRange(&pMemoryRanges[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkFlushMappedMemoryRanges(VkDevice device, uint32_t* memoryRangeCount, const VkMappedMemoryRange* pMemoryRanges, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
    if (memoryRangeCount) *memoryRangeCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkMappedMemoryRange* arrValues = pMemoryRanges;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkMappedMemoryRange(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pMemoryRanges = NULL;
}

static inline int vt_sizeof_vkInvalidateMappedMemoryRanges(VkDevice device, uint32_t memoryRangeCount, const VkMappedMemoryRange* pMemoryRanges) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4; // memoryRangeCount
    bufferSize += 4;
    if (pMemoryRanges) {
        int arrSize = memoryRangeCount ? memoryRangeCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkMappedMemoryRange(&pMemoryRanges[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkInvalidateMappedMemoryRanges(VkDevice device, uint32_t memoryRangeCount, const VkMappedMemoryRange* pMemoryRanges, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = memoryRangeCount;
    bufferOffset += 4;

    if (pMemoryRanges) {
        int arrSize = memoryRangeCount ? memoryRangeCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkMappedMemoryRange(&pMemoryRanges[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkMappedMemoryRange(&pMemoryRanges[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkInvalidateMappedMemoryRanges(VkDevice device, uint32_t* memoryRangeCount, const VkMappedMemoryRange* pMemoryRanges, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
    if (memoryRangeCount) *memoryRangeCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkMappedMemoryRange* arrValues = pMemoryRanges;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkMappedMemoryRange(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pMemoryRanges = NULL;
}

static inline int vt_sizeof_vkGetDeviceMemoryCommitment(VkDevice device, VkDeviceMemory memory, VkDeviceSize* pCommittedMemoryInBytes) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += memory ? vt_sizeof_VkDeviceMemory(memory) : 0;
    bufferSize += 4;
    bufferSize += pCommittedMemoryInBytes ? 8 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetDeviceMemoryCommitment(VkDevice device, VkDeviceMemory memory, VkDeviceSize* pCommittedMemoryInBytes, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (memory) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDeviceMemory(memory, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDeviceMemory(memory);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCommittedMemoryInBytes) {
        int itemSize = 8;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(VkDeviceSize*)(outputBuffer + bufferOffset) = *pCommittedMemoryInBytes;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetDeviceMemoryCommitment(VkDevice device, VkDeviceMemory memory, VkDeviceSize* pCommittedMemoryInBytes, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (memory) {
            vt_unserialize_VkDeviceMemory(memory, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDeviceMemory(memory);
        }
        else bufferOffset += vt_sizeof_VkDeviceMemory(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pCommittedMemoryInBytes) *pCommittedMemoryInBytes = *(VkDeviceSize*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pCommittedMemoryInBytes) pCommittedMemoryInBytes = NULL;
}

static inline int vt_sizeof_vkGetBufferMemoryRequirements(VkDevice device, VkBuffer buffer, VkMemoryRequirements* pMemoryRequirements) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += buffer ? vt_sizeof_VkBuffer(buffer) : 0;
    bufferSize += 4;
    bufferSize += pMemoryRequirements ? vt_sizeof_VkMemoryRequirements(pMemoryRequirements) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetBufferMemoryRequirements(VkDevice device, VkBuffer buffer, VkMemoryRequirements* pMemoryRequirements, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (buffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(buffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(buffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pMemoryRequirements) {
        int itemSize = vt_sizeof_VkMemoryRequirements(pMemoryRequirements);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkMemoryRequirements(pMemoryRequirements, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetBufferMemoryRequirements(VkDevice device, VkBuffer buffer, VkMemoryRequirements* pMemoryRequirements, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (buffer) {
            vt_unserialize_VkBuffer(buffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(buffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pMemoryRequirements) vt_unserialize_VkMemoryRequirements(pMemoryRequirements, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pMemoryRequirements) pMemoryRequirements = NULL;
}

static inline int vt_sizeof_vkBindBufferMemory(VkDevice device, VkBuffer buffer, VkDeviceMemory memory, VkDeviceSize memoryOffset) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += buffer ? vt_sizeof_VkBuffer(buffer) : 0;
    bufferSize++;
    bufferSize += memory ? vt_sizeof_VkDeviceMemory(memory) : 0;
    bufferSize += 8; // memoryOffset
    return bufferSize;
}

static inline void vt_serialize_vkBindBufferMemory(VkDevice device, VkBuffer buffer, VkDeviceMemory memory, VkDeviceSize memoryOffset, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (buffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(buffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(buffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (memory) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDeviceMemory(memory, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDeviceMemory(memory);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = memoryOffset;
    bufferOffset += 8;
}

static inline void vt_unserialize_vkBindBufferMemory(VkDevice device, VkBuffer buffer, VkDeviceMemory memory, VkDeviceSize* memoryOffset, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (buffer) {
            vt_unserialize_VkBuffer(buffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(buffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (memory) {
            vt_unserialize_VkDeviceMemory(memory, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDeviceMemory(memory);
        }
        else bufferOffset += vt_sizeof_VkDeviceMemory(VK_NULL_HANDLE);
    }
    if (memoryOffset) *memoryOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_vkGetImageMemoryRequirements(VkDevice device, VkImage image, VkMemoryRequirements* pMemoryRequirements) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += image ? vt_sizeof_VkImage(image) : 0;
    bufferSize += 4;
    bufferSize += pMemoryRequirements ? vt_sizeof_VkMemoryRequirements(pMemoryRequirements) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetImageMemoryRequirements(VkDevice device, VkImage image, VkMemoryRequirements* pMemoryRequirements, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (image) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(image, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(image);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pMemoryRequirements) {
        int itemSize = vt_sizeof_VkMemoryRequirements(pMemoryRequirements);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkMemoryRequirements(pMemoryRequirements, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetImageMemoryRequirements(VkDevice device, VkImage image, VkMemoryRequirements* pMemoryRequirements, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (image) {
            vt_unserialize_VkImage(image, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(image);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pMemoryRequirements) vt_unserialize_VkMemoryRequirements(pMemoryRequirements, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pMemoryRequirements) pMemoryRequirements = NULL;
}

static inline int vt_sizeof_vkBindImageMemory(VkDevice device, VkImage image, VkDeviceMemory memory, VkDeviceSize memoryOffset) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += image ? vt_sizeof_VkImage(image) : 0;
    bufferSize++;
    bufferSize += memory ? vt_sizeof_VkDeviceMemory(memory) : 0;
    bufferSize += 8; // memoryOffset
    return bufferSize;
}

static inline void vt_serialize_vkBindImageMemory(VkDevice device, VkImage image, VkDeviceMemory memory, VkDeviceSize memoryOffset, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (image) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(image, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(image);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (memory) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDeviceMemory(memory, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDeviceMemory(memory);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = memoryOffset;
    bufferOffset += 8;
}

static inline void vt_unserialize_vkBindImageMemory(VkDevice device, VkImage image, VkDeviceMemory memory, VkDeviceSize* memoryOffset, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (image) {
            vt_unserialize_VkImage(image, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(image);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (memory) {
            vt_unserialize_VkDeviceMemory(memory, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDeviceMemory(memory);
        }
        else bufferOffset += vt_sizeof_VkDeviceMemory(VK_NULL_HANDLE);
    }
    if (memoryOffset) *memoryOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_vkGetImageSparseMemoryRequirements(VkDevice device, VkImage image, uint32_t* pSparseMemoryRequirementCount, VkSparseImageMemoryRequirements* pSparseMemoryRequirements) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += image ? vt_sizeof_VkImage(image) : 0;
    bufferSize += 4;
    bufferSize += pSparseMemoryRequirementCount ? 4 : 0;
    bufferSize += 4;
    if (pSparseMemoryRequirements) {
        int arrSize = pSparseMemoryRequirementCount ? *pSparseMemoryRequirementCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSparseImageMemoryRequirements(&pSparseMemoryRequirements[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkGetImageSparseMemoryRequirements(VkDevice device, VkImage image, uint32_t* pSparseMemoryRequirementCount, VkSparseImageMemoryRequirements* pSparseMemoryRequirements, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (image) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(image, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(image);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pSparseMemoryRequirementCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pSparseMemoryRequirementCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSparseMemoryRequirements) {
        int arrSize = pSparseMemoryRequirementCount ? *pSparseMemoryRequirementCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSparseImageMemoryRequirements(&pSparseMemoryRequirements[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSparseImageMemoryRequirements(&pSparseMemoryRequirements[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetImageSparseMemoryRequirements(VkDevice device, VkImage image, uint32_t* pSparseMemoryRequirementCount, VkSparseImageMemoryRequirements* pSparseMemoryRequirements, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (image) {
            vt_unserialize_VkImage(image, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(image);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pSparseMemoryRequirementCount) *pSparseMemoryRequirementCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pSparseMemoryRequirementCount) pSparseMemoryRequirementCount = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkSparseImageMemoryRequirements* arrValues = pSparseMemoryRequirements;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSparseImageMemoryRequirements(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pSparseMemoryRequirements = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceSparseImageFormatProperties(VkPhysicalDevice physicalDevice, VkFormat format, VkImageType type, VkSampleCountFlagBits samples, VkImageUsageFlags usage, VkImageTiling tiling, uint32_t* pPropertyCount, VkSparseImageFormatProperties* pProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4; // format
    bufferSize += 4; // type
    bufferSize += 4; // samples
    bufferSize += 4; // usage
    bufferSize += 4; // tiling
    bufferSize += 4;
    bufferSize += pPropertyCount ? 4 : 0;
    bufferSize += 4;
    if (pProperties) {
        int arrSize = pPropertyCount ? *pPropertyCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSparseImageFormatProperties(&pProperties[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceSparseImageFormatProperties(VkPhysicalDevice physicalDevice, VkFormat format, VkImageType type, VkSampleCountFlagBits samples, VkImageUsageFlags usage, VkImageTiling tiling, uint32_t* pPropertyCount, VkSparseImageFormatProperties* pProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkFormat*)(outputBuffer + bufferOffset) = format;
    bufferOffset += 4;
    *(VkImageType*)(outputBuffer + bufferOffset) = type;
    bufferOffset += 4;
    *(VkSampleCountFlagBits*)(outputBuffer + bufferOffset) = samples;
    bufferOffset += 4;
    *(VkImageUsageFlags*)(outputBuffer + bufferOffset) = usage;
    bufferOffset += 4;
    *(VkImageTiling*)(outputBuffer + bufferOffset) = tiling;
    bufferOffset += 4;

    if (pPropertyCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pPropertyCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pProperties) {
        int arrSize = pPropertyCount ? *pPropertyCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSparseImageFormatProperties(&pProperties[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSparseImageFormatProperties(&pProperties[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceSparseImageFormatProperties(VkPhysicalDevice physicalDevice, VkFormat* format, VkImageType* type, VkSampleCountFlagBits* samples, VkImageUsageFlags* usage, VkImageTiling* tiling, uint32_t* pPropertyCount, VkSparseImageFormatProperties* pProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }
    if (format) *format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (type) *type = *(VkImageType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (samples) *samples = *(VkSampleCountFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (usage) *usage = *(VkImageUsageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (tiling) *tiling = *(VkImageTiling*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        if (pPropertyCount) *pPropertyCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize7;
    }
    else if (pPropertyCount) pPropertyCount = NULL;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        VkSparseImageFormatProperties* arrValues = pProperties;
        for (int i = 0; i < dataSize8; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSparseImageFormatProperties(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pProperties = NULL;
}

static inline int vt_sizeof_vkQueueBindSparse(VkQueue queue, uint32_t bindInfoCount, const VkBindSparseInfo* pBindInfo, VkFence fence) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += queue ? vt_sizeof_VkQueue(queue) : 0;
    bufferSize += 4; // bindInfoCount
    bufferSize += 4;
    if (pBindInfo) {
        int arrSize = bindInfoCount ? bindInfoCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBindSparseInfo(&pBindInfo[i]);
        }
    }
    bufferSize++;
    bufferSize += fence ? vt_sizeof_VkFence(fence) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkQueueBindSparse(VkQueue queue, uint32_t bindInfoCount, const VkBindSparseInfo* pBindInfo, VkFence fence, char* outputBuffer) {
    int bufferOffset = 0;

    if (queue) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueue(queue, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueue(queue);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = bindInfoCount;
    bufferOffset += 4;

    if (pBindInfo) {
        int arrSize = bindInfoCount ? bindInfoCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBindSparseInfo(&pBindInfo[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBindSparseInfo(&pBindInfo[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (fence) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkFence(fence, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkFence(fence);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkQueueBindSparse(VkQueue queue, uint32_t* bindInfoCount, const VkBindSparseInfo* pBindInfo, VkFence fence, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (queue) {
            vt_unserialize_VkQueue(queue, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueue(queue);
        }
        else bufferOffset += vt_sizeof_VkQueue(VK_NULL_HANDLE);
    }
    if (bindInfoCount) *bindInfoCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkBindSparseInfo* arrValues = pBindInfo;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBindSparseInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pBindInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (fence) {
            vt_unserialize_VkFence(fence, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkFence(fence);
        }
        else bufferOffset += vt_sizeof_VkFence(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreateFence(VkDevice device, const VkFenceCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkFence* pFence) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkFenceCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pFence ? vt_sizeof_VkFence(*pFence) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateFence(VkDevice device, const VkFenceCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkFence* pFence, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkFenceCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkFenceCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pFence) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkFence(*pFence, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkFence(*pFence);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateFence(VkDevice device, const VkFenceCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkFence* pFence, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkFenceCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pFence) {
            vt_unserialize_VkFence(*pFence, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkFence(*pFence);
        }
        else bufferOffset += vt_sizeof_VkFence(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyFence(VkDevice device, VkFence fence, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += fence ? vt_sizeof_VkFence(fence) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyFence(VkDevice device, VkFence fence, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (fence) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkFence(fence, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkFence(fence);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyFence(VkDevice device, VkFence fence, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (fence) {
            vt_unserialize_VkFence(fence, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkFence(fence);
        }
        else bufferOffset += vt_sizeof_VkFence(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkResetFences(VkDevice device, uint32_t fenceCount, const VkFence* pFences) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4; // fenceCount
    bufferSize += 4;
    if (pFences) {
        for (int i = 0; i < fenceCount; i++) {
            bufferSize += vt_sizeof_VkFence(pFences[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkResetFences(VkDevice device, uint32_t fenceCount, const VkFence* pFences, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = fenceCount;
    bufferOffset += 4;

    if (pFences) {
        *(int*)(outputBuffer + bufferOffset) = fenceCount;
        bufferOffset += 4;
        for (int i = 0; i < fenceCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkFence(pFences[i], outputBuffer + bufferOffset);
#else
            VkObject* fencesObject = VkObject_fromHandle(pFences[i]);
            vt_serialize_VkFence((VkFence)&fencesObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkFence(pFences[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkResetFences(VkDevice device, uint32_t* fenceCount, const VkFence* pFences, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
    if (fenceCount) *fenceCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkFence* arrValues = pFences;
        for (int i = 0; i < dataSize3; i++) {
            uint64_t fencesId;
            vt_unserialize_VkFence((VkFence)&fencesId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(fencesId);
#else
                VkObject* fencesObject = VkObject_create(VK_OBJECT_TYPE_FENCE, fencesId);
                arrValues[i] = VkObject_toHandle(fencesObject);
#endif
                bufferOffset += vt_sizeof_VkFence(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkFence(VK_NULL_HANDLE);
        }
    }
    else pFences = NULL;
}

static inline int vt_sizeof_vkGetFenceStatus(VkDevice device, VkFence fence) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += fence ? vt_sizeof_VkFence(fence) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetFenceStatus(VkDevice device, VkFence fence, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (fence) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkFence(fence, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkFence(fence);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkGetFenceStatus(VkDevice device, VkFence fence, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (fence) {
            vt_unserialize_VkFence(fence, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkFence(fence);
        }
        else bufferOffset += vt_sizeof_VkFence(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkWaitForFences(VkDevice device, uint32_t fenceCount, const VkFence* pFences, VkBool32 waitAll, uint64_t timeout) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4; // fenceCount
    bufferSize += 4;
    if (pFences) {
        for (int i = 0; i < fenceCount; i++) {
            bufferSize += vt_sizeof_VkFence(pFences[i]);
        }
    }
    bufferSize += 4; // waitAll
    bufferSize += 8; // timeout
    return bufferSize;
}

static inline void vt_serialize_vkWaitForFences(VkDevice device, uint32_t fenceCount, const VkFence* pFences, VkBool32 waitAll, uint64_t timeout, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = fenceCount;
    bufferOffset += 4;

    if (pFences) {
        *(int*)(outputBuffer + bufferOffset) = fenceCount;
        bufferOffset += 4;
        for (int i = 0; i < fenceCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkFence(pFences[i], outputBuffer + bufferOffset);
#else
            VkObject* fencesObject = VkObject_fromHandle(pFences[i]);
            vt_serialize_VkFence((VkFence)&fencesObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkFence(pFences[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkBool32*)(outputBuffer + bufferOffset) = waitAll;
    bufferOffset += 4;
    *(uint64_t*)(outputBuffer + bufferOffset) = timeout;
    bufferOffset += 8;
}

static inline void vt_unserialize_vkWaitForFences(VkDevice device, uint32_t* fenceCount, const VkFence* pFences, VkBool32* waitAll, uint64_t* timeout, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
    if (fenceCount) *fenceCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkFence* arrValues = pFences;
        for (int i = 0; i < dataSize3; i++) {
            uint64_t fencesId;
            vt_unserialize_VkFence((VkFence)&fencesId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(fencesId);
#else
                VkObject* fencesObject = VkObject_create(VK_OBJECT_TYPE_FENCE, fencesId);
                arrValues[i] = VkObject_toHandle(fencesObject);
#endif
                bufferOffset += vt_sizeof_VkFence(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkFence(VK_NULL_HANDLE);
        }
    }
    else pFences = NULL;
    if (waitAll) *waitAll = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (timeout) *timeout = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_vkCreateSemaphore(VkDevice device, const VkSemaphoreCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSemaphore* pSemaphore) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkSemaphoreCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pSemaphore ? vt_sizeof_VkSemaphore(*pSemaphore) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateSemaphore(VkDevice device, const VkSemaphoreCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSemaphore* pSemaphore, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkSemaphoreCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSemaphoreCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSemaphore) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSemaphore(*pSemaphore, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSemaphore(*pSemaphore);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateSemaphore(VkDevice device, const VkSemaphoreCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSemaphore* pSemaphore, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkSemaphoreCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pSemaphore) {
            vt_unserialize_VkSemaphore(*pSemaphore, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSemaphore(*pSemaphore);
        }
        else bufferOffset += vt_sizeof_VkSemaphore(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroySemaphore(VkDevice device, VkSemaphore semaphore, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += semaphore ? vt_sizeof_VkSemaphore(semaphore) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroySemaphore(VkDevice device, VkSemaphore semaphore, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (semaphore) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSemaphore(semaphore, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSemaphore(semaphore);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroySemaphore(VkDevice device, VkSemaphore semaphore, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (semaphore) {
            vt_unserialize_VkSemaphore(semaphore, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSemaphore(semaphore);
        }
        else bufferOffset += vt_sizeof_VkSemaphore(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreateEvent(VkDevice device, const VkEventCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkEvent* pEvent) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkEventCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pEvent ? vt_sizeof_VkEvent(*pEvent) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateEvent(VkDevice device, const VkEventCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkEvent* pEvent, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkEventCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkEventCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pEvent) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkEvent(*pEvent, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkEvent(*pEvent);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateEvent(VkDevice device, const VkEventCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkEvent* pEvent, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkEventCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pEvent) {
            vt_unserialize_VkEvent(*pEvent, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkEvent(*pEvent);
        }
        else bufferOffset += vt_sizeof_VkEvent(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyEvent(VkDevice device, VkEvent event, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += event ? vt_sizeof_VkEvent(event) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyEvent(VkDevice device, VkEvent event, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (event) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkEvent(event, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkEvent(event);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyEvent(VkDevice device, VkEvent event, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (event) {
            vt_unserialize_VkEvent(event, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkEvent(event);
        }
        else bufferOffset += vt_sizeof_VkEvent(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkGetEventStatus(VkDevice device, VkEvent event) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += event ? vt_sizeof_VkEvent(event) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetEventStatus(VkDevice device, VkEvent event, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (event) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkEvent(event, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkEvent(event);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkGetEventStatus(VkDevice device, VkEvent event, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (event) {
            vt_unserialize_VkEvent(event, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkEvent(event);
        }
        else bufferOffset += vt_sizeof_VkEvent(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkSetEvent(VkDevice device, VkEvent event) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += event ? vt_sizeof_VkEvent(event) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkSetEvent(VkDevice device, VkEvent event, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (event) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkEvent(event, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkEvent(event);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkSetEvent(VkDevice device, VkEvent event, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (event) {
            vt_unserialize_VkEvent(event, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkEvent(event);
        }
        else bufferOffset += vt_sizeof_VkEvent(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkResetEvent(VkDevice device, VkEvent event) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += event ? vt_sizeof_VkEvent(event) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkResetEvent(VkDevice device, VkEvent event, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (event) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkEvent(event, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkEvent(event);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkResetEvent(VkDevice device, VkEvent event, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (event) {
            vt_unserialize_VkEvent(event, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkEvent(event);
        }
        else bufferOffset += vt_sizeof_VkEvent(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreateQueryPool(VkDevice device, const VkQueryPoolCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkQueryPool* pQueryPool) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkQueryPoolCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pQueryPool ? vt_sizeof_VkQueryPool(*pQueryPool) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateQueryPool(VkDevice device, const VkQueryPoolCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkQueryPool* pQueryPool, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkQueryPoolCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkQueryPoolCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pQueryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(*pQueryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(*pQueryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateQueryPool(VkDevice device, const VkQueryPoolCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkQueryPool* pQueryPool, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkQueryPoolCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pQueryPool) {
            vt_unserialize_VkQueryPool(*pQueryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(*pQueryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyQueryPool(VkDevice device, VkQueryPool queryPool, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += queryPool ? vt_sizeof_VkQueryPool(queryPool) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyQueryPool(VkDevice device, VkQueryPool queryPool, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (queryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(queryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(queryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyQueryPool(VkDevice device, VkQueryPool queryPool, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (queryPool) {
            vt_unserialize_VkQueryPool(queryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(queryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkGetQueryPoolResults(VkDevice device, VkQueryPool queryPool, uint32_t firstQuery, uint32_t queryCount, size_t dataSize, void* pData, VkDeviceSize stride, VkQueryResultFlags flags) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += queryPool ? vt_sizeof_VkQueryPool(queryPool) : 0;
    bufferSize += 4; // firstQuery
    bufferSize += 4; // queryCount
    bufferSize += 8; // dataSize
    bufferSize += 4;
    bufferSize += pData ? dataSize : 0;
    bufferSize += 8; // stride
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_vkGetQueryPoolResults(VkDevice device, VkQueryPool queryPool, uint32_t firstQuery, uint32_t queryCount, size_t dataSize, void* pData, VkDeviceSize stride, VkQueryResultFlags flags, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (queryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(queryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(queryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstQuery;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = queryCount;
    bufferOffset += 4;
    *(size_t*)(outputBuffer + bufferOffset) = dataSize;
    bufferOffset += 8;

    if (pData) {
        *(int*)(outputBuffer + bufferOffset) = dataSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pData, dataSize);
        bufferOffset += dataSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = stride;
    bufferOffset += 8;
    *(VkQueryResultFlags*)(outputBuffer + bufferOffset) = flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkGetQueryPoolResults(VkDevice device, VkQueryPool queryPool, uint32_t* firstQuery, uint32_t* queryCount, size_t* dataSize, void* pData, VkDeviceSize* stride, VkQueryResultFlags* flags, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (queryPool) {
            vt_unserialize_VkQueryPool(queryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(queryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
    if (firstQuery) *firstQuery = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (queryCount) *queryCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize) *dataSize = *(size_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        if (pData) memcpy(pData, inputBuffer + bufferOffset, dataSize6);
        bufferOffset += dataSize6;
    }
    else pData = NULL;
    if (stride) *stride = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (flags) *flags = *(VkQueryResultFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkResetQueryPool(VkDevice device, VkQueryPool queryPool, uint32_t firstQuery, uint32_t queryCount) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += queryPool ? vt_sizeof_VkQueryPool(queryPool) : 0;
    bufferSize += 4; // firstQuery
    bufferSize += 4; // queryCount
    return bufferSize;
}

static inline void vt_serialize_vkResetQueryPool(VkDevice device, VkQueryPool queryPool, uint32_t firstQuery, uint32_t queryCount, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (queryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(queryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(queryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstQuery;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = queryCount;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkResetQueryPool(VkDevice device, VkQueryPool queryPool, uint32_t* firstQuery, uint32_t* queryCount, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (queryPool) {
            vt_unserialize_VkQueryPool(queryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(queryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
    if (firstQuery) *firstQuery = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (queryCount) *queryCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCreateBuffer(VkDevice device, const VkBufferCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkBuffer* pBuffer) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkBufferCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pBuffer ? vt_sizeof_VkBuffer(*pBuffer) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateBuffer(VkDevice device, const VkBufferCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkBuffer* pBuffer, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkBufferCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkBufferCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(*pBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(*pBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateBuffer(VkDevice device, const VkBufferCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkBuffer* pBuffer, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkBufferCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pBuffer) {
            vt_unserialize_VkBuffer(*pBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(*pBuffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyBuffer(VkDevice device, VkBuffer buffer, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += buffer ? vt_sizeof_VkBuffer(buffer) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyBuffer(VkDevice device, VkBuffer buffer, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (buffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(buffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(buffer);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyBuffer(VkDevice device, VkBuffer buffer, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (buffer) {
            vt_unserialize_VkBuffer(buffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(buffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreateBufferView(VkDevice device, const VkBufferViewCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkBufferView* pView) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkBufferViewCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pView ? vt_sizeof_VkBufferView(*pView) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateBufferView(VkDevice device, const VkBufferViewCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkBufferView* pView, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkBufferViewCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkBufferViewCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pView) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBufferView(*pView, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBufferView(*pView);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateBufferView(VkDevice device, const VkBufferViewCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkBufferView* pView, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkBufferViewCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pView) {
            vt_unserialize_VkBufferView(*pView, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBufferView(*pView);
        }
        else bufferOffset += vt_sizeof_VkBufferView(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyBufferView(VkDevice device, VkBufferView bufferView, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += bufferView ? vt_sizeof_VkBufferView(bufferView) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyBufferView(VkDevice device, VkBufferView bufferView, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (bufferView) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBufferView(bufferView, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBufferView(bufferView);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyBufferView(VkDevice device, VkBufferView bufferView, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (bufferView) {
            vt_unserialize_VkBufferView(bufferView, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBufferView(bufferView);
        }
        else bufferOffset += vt_sizeof_VkBufferView(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreateImage(VkDevice device, const VkImageCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkImage* pImage) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkImageCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pImage ? vt_sizeof_VkImage(*pImage) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateImage(VkDevice device, const VkImageCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkImage* pImage, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkImageCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkImageCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pImage) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(*pImage, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(*pImage);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateImage(VkDevice device, const VkImageCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkImage* pImage, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkImageCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pImage) {
            vt_unserialize_VkImage(*pImage, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(*pImage);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyImage(VkDevice device, VkImage image, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += image ? vt_sizeof_VkImage(image) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyImage(VkDevice device, VkImage image, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (image) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(image, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(image);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyImage(VkDevice device, VkImage image, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (image) {
            vt_unserialize_VkImage(image, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(image);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkGetImageSubresourceLayout(VkDevice device, VkImage image, const VkImageSubresource* pSubresource, VkSubresourceLayout* pLayout) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += image ? vt_sizeof_VkImage(image) : 0;
    bufferSize += 4;
    bufferSize += pSubresource ? vt_sizeof_VkImageSubresource(pSubresource) : 0;
    bufferSize += 4;
    bufferSize += pLayout ? vt_sizeof_VkSubresourceLayout(pLayout) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetImageSubresourceLayout(VkDevice device, VkImage image, const VkImageSubresource* pSubresource, VkSubresourceLayout* pLayout, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (image) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(image, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(image);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pSubresource) {
        int itemSize = vt_sizeof_VkImageSubresource(pSubresource);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkImageSubresource(pSubresource, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pLayout) {
        int itemSize = vt_sizeof_VkSubresourceLayout(pLayout);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSubresourceLayout(pLayout, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetImageSubresourceLayout(VkDevice device, VkImage image, const VkImageSubresource* pSubresource, VkSubresourceLayout* pLayout, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (image) {
            vt_unserialize_VkImage(image, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(image);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pSubresource) vt_unserialize_VkImageSubresource(pSubresource, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pSubresource) pSubresource = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        if (pLayout) vt_unserialize_VkSubresourceLayout(pLayout, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize4;
    }
    else if (pLayout) pLayout = NULL;
}

static inline int vt_sizeof_vkCreateImageView(VkDevice device, const VkImageViewCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkImageView* pView) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkImageViewCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pView ? vt_sizeof_VkImageView(*pView) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateImageView(VkDevice device, const VkImageViewCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkImageView* pView, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkImageViewCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkImageViewCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pView) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImageView(*pView, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImageView(*pView);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateImageView(VkDevice device, const VkImageViewCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkImageView* pView, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkImageViewCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pView) {
            vt_unserialize_VkImageView(*pView, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImageView(*pView);
        }
        else bufferOffset += vt_sizeof_VkImageView(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyImageView(VkDevice device, VkImageView imageView, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += imageView ? vt_sizeof_VkImageView(imageView) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyImageView(VkDevice device, VkImageView imageView, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (imageView) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImageView(imageView, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImageView(imageView);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyImageView(VkDevice device, VkImageView imageView, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (imageView) {
            vt_unserialize_VkImageView(imageView, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImageView(imageView);
        }
        else bufferOffset += vt_sizeof_VkImageView(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreateShaderModule(VkDevice device, const VkShaderModuleCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkShaderModule* pShaderModule) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkShaderModuleCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pShaderModule ? vt_sizeof_VkShaderModule(*pShaderModule) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateShaderModule(VkDevice device, const VkShaderModuleCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkShaderModule* pShaderModule, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkShaderModuleCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkShaderModuleCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pShaderModule) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkShaderModule(*pShaderModule, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkShaderModule(*pShaderModule);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateShaderModule(VkDevice device, const VkShaderModuleCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkShaderModule* pShaderModule, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkShaderModuleCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pShaderModule) {
            vt_unserialize_VkShaderModule(*pShaderModule, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkShaderModule(*pShaderModule);
        }
        else bufferOffset += vt_sizeof_VkShaderModule(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyShaderModule(VkDevice device, VkShaderModule shaderModule, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += shaderModule ? vt_sizeof_VkShaderModule(shaderModule) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyShaderModule(VkDevice device, VkShaderModule shaderModule, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (shaderModule) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkShaderModule(shaderModule, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkShaderModule(shaderModule);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyShaderModule(VkDevice device, VkShaderModule shaderModule, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (shaderModule) {
            vt_unserialize_VkShaderModule(shaderModule, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkShaderModule(shaderModule);
        }
        else bufferOffset += vt_sizeof_VkShaderModule(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreatePipelineCache(VkDevice device, const VkPipelineCacheCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkPipelineCache* pPipelineCache) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkPipelineCacheCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pPipelineCache ? vt_sizeof_VkPipelineCache(*pPipelineCache) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreatePipelineCache(VkDevice device, const VkPipelineCacheCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkPipelineCache* pPipelineCache, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkPipelineCacheCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineCacheCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pPipelineCache) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipelineCache(*pPipelineCache, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipelineCache(*pPipelineCache);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreatePipelineCache(VkDevice device, const VkPipelineCacheCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkPipelineCache* pPipelineCache, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkPipelineCacheCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pPipelineCache) {
            vt_unserialize_VkPipelineCache(*pPipelineCache, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipelineCache(*pPipelineCache);
        }
        else bufferOffset += vt_sizeof_VkPipelineCache(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyPipelineCache(VkDevice device, VkPipelineCache pipelineCache, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += pipelineCache ? vt_sizeof_VkPipelineCache(pipelineCache) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyPipelineCache(VkDevice device, VkPipelineCache pipelineCache, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pipelineCache) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipelineCache(pipelineCache, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipelineCache(pipelineCache);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyPipelineCache(VkDevice device, VkPipelineCache pipelineCache, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (pipelineCache) {
            vt_unserialize_VkPipelineCache(pipelineCache, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipelineCache(pipelineCache);
        }
        else bufferOffset += vt_sizeof_VkPipelineCache(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkGetPipelineCacheData(VkDevice device, VkPipelineCache pipelineCache, size_t* pDataSize, void* pData) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += pipelineCache ? vt_sizeof_VkPipelineCache(pipelineCache) : 0;
    bufferSize += 4;
    bufferSize += pDataSize ? 8 : 0;
    bufferSize += 4;
    bufferSize += pData ? *pDataSize : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPipelineCacheData(VkDevice device, VkPipelineCache pipelineCache, size_t* pDataSize, void* pData, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pipelineCache) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipelineCache(pipelineCache, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipelineCache(pipelineCache);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pDataSize) {
        int itemSize = 8;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(size_t*)(outputBuffer + bufferOffset) = *pDataSize;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pData) {
        *(int*)(outputBuffer + bufferOffset) = *pDataSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pData, *pDataSize);
        bufferOffset += *pDataSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPipelineCacheData(VkDevice device, VkPipelineCache pipelineCache, size_t* pDataSize, void* pData, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (pipelineCache) {
            vt_unserialize_VkPipelineCache(pipelineCache, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipelineCache(pipelineCache);
        }
        else bufferOffset += vt_sizeof_VkPipelineCache(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pDataSize) *pDataSize = *(size_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pDataSize) pDataSize = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        if (pData) memcpy(pData, inputBuffer + bufferOffset, dataSize4);
        bufferOffset += dataSize4;
    }
    else pData = NULL;
}

static inline int vt_sizeof_vkMergePipelineCaches(VkDevice device, VkPipelineCache dstCache, uint32_t srcCacheCount, const VkPipelineCache* pSrcCaches) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += dstCache ? vt_sizeof_VkPipelineCache(dstCache) : 0;
    bufferSize += 4; // srcCacheCount
    bufferSize += 4;
    if (pSrcCaches) {
        for (int i = 0; i < srcCacheCount; i++) {
            bufferSize += vt_sizeof_VkPipelineCache(pSrcCaches[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkMergePipelineCaches(VkDevice device, VkPipelineCache dstCache, uint32_t srcCacheCount, const VkPipelineCache* pSrcCaches, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (dstCache) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipelineCache(dstCache, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipelineCache(dstCache);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = srcCacheCount;
    bufferOffset += 4;

    if (pSrcCaches) {
        *(int*)(outputBuffer + bufferOffset) = srcCacheCount;
        bufferOffset += 4;
        for (int i = 0; i < srcCacheCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkPipelineCache(pSrcCaches[i], outputBuffer + bufferOffset);
#else
            VkObject* srcCachesObject = VkObject_fromHandle(pSrcCaches[i]);
            vt_serialize_VkPipelineCache((VkPipelineCache)&srcCachesObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkPipelineCache(pSrcCaches[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkMergePipelineCaches(VkDevice device, VkPipelineCache dstCache, uint32_t* srcCacheCount, const VkPipelineCache* pSrcCaches, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (dstCache) {
            vt_unserialize_VkPipelineCache(dstCache, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipelineCache(dstCache);
        }
        else bufferOffset += vt_sizeof_VkPipelineCache(VK_NULL_HANDLE);
    }
    if (srcCacheCount) *srcCacheCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkPipelineCache* arrValues = pSrcCaches;
        for (int i = 0; i < dataSize4; i++) {
            uint64_t srcCachesId;
            vt_unserialize_VkPipelineCache((VkPipelineCache)&srcCachesId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(srcCachesId);
#else
                VkObject* srcCachesObject = VkObject_create(VK_OBJECT_TYPE_PIPELINE_CACHE, srcCachesId);
                arrValues[i] = VkObject_toHandle(srcCachesObject);
#endif
                bufferOffset += vt_sizeof_VkPipelineCache(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkPipelineCache(VK_NULL_HANDLE);
        }
    }
    else pSrcCaches = NULL;
}

static inline int vt_sizeof_vkCreateGraphicsPipelines(VkDevice device, VkPipelineCache pipelineCache, uint32_t createInfoCount, const VkGraphicsPipelineCreateInfo* pCreateInfos, const VkAllocationCallbacks* pAllocator, VkPipeline* pPipelines) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += pipelineCache ? vt_sizeof_VkPipelineCache(pipelineCache) : 0;
    bufferSize += 4; // createInfoCount
    bufferSize += 4;
    if (pCreateInfos) {
        int arrSize = createInfoCount ? createInfoCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkGraphicsPipelineCreateInfo(&pCreateInfos[i]);
        }
    }
    bufferSize += 4;
    if (pPipelines) {
        for (int i = 0; i < createInfoCount; i++) {
            bufferSize += vt_sizeof_VkPipeline(pPipelines[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCreateGraphicsPipelines(VkDevice device, VkPipelineCache pipelineCache, uint32_t createInfoCount, const VkGraphicsPipelineCreateInfo* pCreateInfos, const VkAllocationCallbacks* pAllocator, VkPipeline* pPipelines, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pipelineCache) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipelineCache(pipelineCache, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipelineCache(pipelineCache);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = createInfoCount;
    bufferOffset += 4;

    if (pCreateInfos) {
        int arrSize = createInfoCount ? createInfoCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkGraphicsPipelineCreateInfo(&pCreateInfos[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkGraphicsPipelineCreateInfo(&pCreateInfos[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pPipelines) {
        *(int*)(outputBuffer + bufferOffset) = createInfoCount;
        bufferOffset += 4;
        for (int i = 0; i < createInfoCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkPipeline(pPipelines[i], outputBuffer + bufferOffset);
#else
            VkObject* pipelinesObject = VkObject_fromHandle(pPipelines[i]);
            vt_serialize_VkPipeline((VkPipeline)&pipelinesObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkPipeline(pPipelines[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCreateGraphicsPipelines(VkDevice device, VkPipelineCache pipelineCache, uint32_t* createInfoCount, const VkGraphicsPipelineCreateInfo* pCreateInfos, const VkAllocationCallbacks* pAllocator, VkPipeline* pPipelines, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (pipelineCache) {
            vt_unserialize_VkPipelineCache(pipelineCache, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipelineCache(pipelineCache);
        }
        else bufferOffset += vt_sizeof_VkPipelineCache(VK_NULL_HANDLE);
    }
    if (createInfoCount) *createInfoCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkGraphicsPipelineCreateInfo* arrValues = pCreateInfos;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkGraphicsPipelineCreateInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pCreateInfos = NULL;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkPipeline* arrValues = pPipelines;
        for (int i = 0; i < dataSize6; i++) {
            uint64_t pipelinesId;
            vt_unserialize_VkPipeline((VkPipeline)&pipelinesId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(pipelinesId);
#else
                VkObject* pipelinesObject = VkObject_create(VK_OBJECT_TYPE_PIPELINE, pipelinesId);
                arrValues[i] = VkObject_toHandle(pipelinesObject);
#endif
                bufferOffset += vt_sizeof_VkPipeline(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkPipeline(VK_NULL_HANDLE);
        }
    }
    else pPipelines = NULL;
}

static inline int vt_sizeof_vkCreateComputePipelines(VkDevice device, VkPipelineCache pipelineCache, uint32_t createInfoCount, const VkComputePipelineCreateInfo* pCreateInfos, const VkAllocationCallbacks* pAllocator, VkPipeline* pPipelines) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += pipelineCache ? vt_sizeof_VkPipelineCache(pipelineCache) : 0;
    bufferSize += 4; // createInfoCount
    bufferSize += 4;
    if (pCreateInfos) {
        int arrSize = createInfoCount ? createInfoCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkComputePipelineCreateInfo(&pCreateInfos[i]);
        }
    }
    bufferSize += 4;
    if (pPipelines) {
        for (int i = 0; i < createInfoCount; i++) {
            bufferSize += vt_sizeof_VkPipeline(pPipelines[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCreateComputePipelines(VkDevice device, VkPipelineCache pipelineCache, uint32_t createInfoCount, const VkComputePipelineCreateInfo* pCreateInfos, const VkAllocationCallbacks* pAllocator, VkPipeline* pPipelines, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pipelineCache) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipelineCache(pipelineCache, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipelineCache(pipelineCache);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = createInfoCount;
    bufferOffset += 4;

    if (pCreateInfos) {
        int arrSize = createInfoCount ? createInfoCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkComputePipelineCreateInfo(&pCreateInfos[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkComputePipelineCreateInfo(&pCreateInfos[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pPipelines) {
        *(int*)(outputBuffer + bufferOffset) = createInfoCount;
        bufferOffset += 4;
        for (int i = 0; i < createInfoCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkPipeline(pPipelines[i], outputBuffer + bufferOffset);
#else
            VkObject* pipelinesObject = VkObject_fromHandle(pPipelines[i]);
            vt_serialize_VkPipeline((VkPipeline)&pipelinesObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkPipeline(pPipelines[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCreateComputePipelines(VkDevice device, VkPipelineCache pipelineCache, uint32_t* createInfoCount, const VkComputePipelineCreateInfo* pCreateInfos, const VkAllocationCallbacks* pAllocator, VkPipeline* pPipelines, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (pipelineCache) {
            vt_unserialize_VkPipelineCache(pipelineCache, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipelineCache(pipelineCache);
        }
        else bufferOffset += vt_sizeof_VkPipelineCache(VK_NULL_HANDLE);
    }
    if (createInfoCount) *createInfoCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkComputePipelineCreateInfo* arrValues = pCreateInfos;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkComputePipelineCreateInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pCreateInfos = NULL;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkPipeline* arrValues = pPipelines;
        for (int i = 0; i < dataSize6; i++) {
            uint64_t pipelinesId;
            vt_unserialize_VkPipeline((VkPipeline)&pipelinesId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(pipelinesId);
#else
                VkObject* pipelinesObject = VkObject_create(VK_OBJECT_TYPE_PIPELINE, pipelinesId);
                arrValues[i] = VkObject_toHandle(pipelinesObject);
#endif
                bufferOffset += vt_sizeof_VkPipeline(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkPipeline(VK_NULL_HANDLE);
        }
    }
    else pPipelines = NULL;
}

static inline int vt_sizeof_vkDestroyPipeline(VkDevice device, VkPipeline pipeline, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += pipeline ? vt_sizeof_VkPipeline(pipeline) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyPipeline(VkDevice device, VkPipeline pipeline, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pipeline) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipeline(pipeline, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipeline(pipeline);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyPipeline(VkDevice device, VkPipeline pipeline, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (pipeline) {
            vt_unserialize_VkPipeline(pipeline, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipeline(pipeline);
        }
        else bufferOffset += vt_sizeof_VkPipeline(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreatePipelineLayout(VkDevice device, const VkPipelineLayoutCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkPipelineLayout* pPipelineLayout) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkPipelineLayoutCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pPipelineLayout ? vt_sizeof_VkPipelineLayout(*pPipelineLayout) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreatePipelineLayout(VkDevice device, const VkPipelineLayoutCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkPipelineLayout* pPipelineLayout, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkPipelineLayoutCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPipelineLayoutCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pPipelineLayout) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipelineLayout(*pPipelineLayout, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipelineLayout(*pPipelineLayout);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreatePipelineLayout(VkDevice device, const VkPipelineLayoutCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkPipelineLayout* pPipelineLayout, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkPipelineLayoutCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pPipelineLayout) {
            vt_unserialize_VkPipelineLayout(*pPipelineLayout, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipelineLayout(*pPipelineLayout);
        }
        else bufferOffset += vt_sizeof_VkPipelineLayout(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyPipelineLayout(VkDevice device, VkPipelineLayout pipelineLayout, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += pipelineLayout ? vt_sizeof_VkPipelineLayout(pipelineLayout) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyPipelineLayout(VkDevice device, VkPipelineLayout pipelineLayout, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pipelineLayout) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipelineLayout(pipelineLayout, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipelineLayout(pipelineLayout);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyPipelineLayout(VkDevice device, VkPipelineLayout pipelineLayout, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (pipelineLayout) {
            vt_unserialize_VkPipelineLayout(pipelineLayout, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipelineLayout(pipelineLayout);
        }
        else bufferOffset += vt_sizeof_VkPipelineLayout(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreateSampler(VkDevice device, const VkSamplerCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSampler* pSampler) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkSamplerCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pSampler ? vt_sizeof_VkSampler(*pSampler) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateSampler(VkDevice device, const VkSamplerCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSampler* pSampler, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkSamplerCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSamplerCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSampler) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSampler(*pSampler, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSampler(*pSampler);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateSampler(VkDevice device, const VkSamplerCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSampler* pSampler, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkSamplerCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pSampler) {
            vt_unserialize_VkSampler(*pSampler, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSampler(*pSampler);
        }
        else bufferOffset += vt_sizeof_VkSampler(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroySampler(VkDevice device, VkSampler sampler, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += sampler ? vt_sizeof_VkSampler(sampler) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroySampler(VkDevice device, VkSampler sampler, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (sampler) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSampler(sampler, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSampler(sampler);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroySampler(VkDevice device, VkSampler sampler, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (sampler) {
            vt_unserialize_VkSampler(sampler, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSampler(sampler);
        }
        else bufferOffset += vt_sizeof_VkSampler(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreateDescriptorSetLayout(VkDevice device, const VkDescriptorSetLayoutCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkDescriptorSetLayout* pSetLayout) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkDescriptorSetLayoutCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pSetLayout ? vt_sizeof_VkDescriptorSetLayout(*pSetLayout) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateDescriptorSetLayout(VkDevice device, const VkDescriptorSetLayoutCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkDescriptorSetLayout* pSetLayout, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkDescriptorSetLayoutCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDescriptorSetLayoutCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSetLayout) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDescriptorSetLayout(*pSetLayout, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDescriptorSetLayout(*pSetLayout);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateDescriptorSetLayout(VkDevice device, const VkDescriptorSetLayoutCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkDescriptorSetLayout* pSetLayout, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkDescriptorSetLayoutCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pSetLayout) {
            vt_unserialize_VkDescriptorSetLayout(*pSetLayout, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDescriptorSetLayout(*pSetLayout);
        }
        else bufferOffset += vt_sizeof_VkDescriptorSetLayout(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyDescriptorSetLayout(VkDevice device, VkDescriptorSetLayout descriptorSetLayout, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += descriptorSetLayout ? vt_sizeof_VkDescriptorSetLayout(descriptorSetLayout) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyDescriptorSetLayout(VkDevice device, VkDescriptorSetLayout descriptorSetLayout, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (descriptorSetLayout) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDescriptorSetLayout(descriptorSetLayout, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDescriptorSetLayout(descriptorSetLayout);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyDescriptorSetLayout(VkDevice device, VkDescriptorSetLayout descriptorSetLayout, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (descriptorSetLayout) {
            vt_unserialize_VkDescriptorSetLayout(descriptorSetLayout, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDescriptorSetLayout(descriptorSetLayout);
        }
        else bufferOffset += vt_sizeof_VkDescriptorSetLayout(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreateDescriptorPool(VkDevice device, const VkDescriptorPoolCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkDescriptorPool* pDescriptorPool) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkDescriptorPoolCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pDescriptorPool ? vt_sizeof_VkDescriptorPool(*pDescriptorPool) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateDescriptorPool(VkDevice device, const VkDescriptorPoolCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkDescriptorPool* pDescriptorPool, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkDescriptorPoolCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDescriptorPoolCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pDescriptorPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDescriptorPool(*pDescriptorPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDescriptorPool(*pDescriptorPool);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateDescriptorPool(VkDevice device, const VkDescriptorPoolCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkDescriptorPool* pDescriptorPool, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkDescriptorPoolCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pDescriptorPool) {
            vt_unserialize_VkDescriptorPool(*pDescriptorPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDescriptorPool(*pDescriptorPool);
        }
        else bufferOffset += vt_sizeof_VkDescriptorPool(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyDescriptorPool(VkDevice device, VkDescriptorPool descriptorPool, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += descriptorPool ? vt_sizeof_VkDescriptorPool(descriptorPool) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyDescriptorPool(VkDevice device, VkDescriptorPool descriptorPool, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (descriptorPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDescriptorPool(descriptorPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDescriptorPool(descriptorPool);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyDescriptorPool(VkDevice device, VkDescriptorPool descriptorPool, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (descriptorPool) {
            vt_unserialize_VkDescriptorPool(descriptorPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDescriptorPool(descriptorPool);
        }
        else bufferOffset += vt_sizeof_VkDescriptorPool(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkResetDescriptorPool(VkDevice device, VkDescriptorPool descriptorPool, VkDescriptorPoolResetFlags flags) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += descriptorPool ? vt_sizeof_VkDescriptorPool(descriptorPool) : 0;
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_vkResetDescriptorPool(VkDevice device, VkDescriptorPool descriptorPool, VkDescriptorPoolResetFlags flags, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (descriptorPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDescriptorPool(descriptorPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDescriptorPool(descriptorPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDescriptorPoolResetFlags*)(outputBuffer + bufferOffset) = flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkResetDescriptorPool(VkDevice device, VkDescriptorPool descriptorPool, VkDescriptorPoolResetFlags* flags, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (descriptorPool) {
            vt_unserialize_VkDescriptorPool(descriptorPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDescriptorPool(descriptorPool);
        }
        else bufferOffset += vt_sizeof_VkDescriptorPool(VK_NULL_HANDLE);
    }
    if (flags) *flags = *(VkDescriptorPoolResetFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkAllocateDescriptorSets(VkDevice device, const VkDescriptorSetAllocateInfo* pAllocateInfo, VkDescriptorSet* pDescriptorSets) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pAllocateInfo ? vt_sizeof_VkDescriptorSetAllocateInfo(pAllocateInfo) : 0;
    bufferSize += 4;
    if (pDescriptorSets) {
        for (int i = 0; i < pAllocateInfo->descriptorSetCount; i++) {
            bufferSize += vt_sizeof_VkDescriptorSet(pDescriptorSets[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkAllocateDescriptorSets(VkDevice device, const VkDescriptorSetAllocateInfo* pAllocateInfo, VkDescriptorSet* pDescriptorSets, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pAllocateInfo) {
        int itemSize = vt_sizeof_VkDescriptorSetAllocateInfo(pAllocateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDescriptorSetAllocateInfo(pAllocateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pDescriptorSets) {
        *(int*)(outputBuffer + bufferOffset) = pAllocateInfo->descriptorSetCount;
        bufferOffset += 4;
        for (int i = 0; i < pAllocateInfo->descriptorSetCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkDescriptorSet(pDescriptorSets[i], outputBuffer + bufferOffset);
#else
            VkObject* descriptorSetsObject = VkObject_fromHandle(pDescriptorSets[i]);
            vt_serialize_VkDescriptorSet((VkDescriptorSet)&descriptorSetsObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkDescriptorSet(pDescriptorSets[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkAllocateDescriptorSets(VkDevice device, const VkDescriptorSetAllocateInfo* pAllocateInfo, VkDescriptorSet* pDescriptorSets, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pAllocateInfo) vt_unserialize_VkDescriptorSetAllocateInfo(pAllocateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pAllocateInfo) pAllocateInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkDescriptorSet* arrValues = pDescriptorSets;
        for (int i = 0; i < dataSize3; i++) {
            uint64_t descriptorSetsId;
            vt_unserialize_VkDescriptorSet((VkDescriptorSet)&descriptorSetsId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(descriptorSetsId);
#else
                VkObject* descriptorSetsObject = VkObject_create(VK_OBJECT_TYPE_DESCRIPTOR_SET, descriptorSetsId);
                arrValues[i] = VkObject_toHandle(descriptorSetsObject);
#endif
                bufferOffset += vt_sizeof_VkDescriptorSet(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkDescriptorSet(VK_NULL_HANDLE);
        }
    }
    else pDescriptorSets = NULL;
}

static inline int vt_sizeof_vkFreeDescriptorSets(VkDevice device, VkDescriptorPool descriptorPool, uint32_t descriptorSetCount, const VkDescriptorSet* pDescriptorSets) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += descriptorPool ? vt_sizeof_VkDescriptorPool(descriptorPool) : 0;
    bufferSize += 4; // descriptorSetCount
    bufferSize += 4;
    if (pDescriptorSets) {
        for (int i = 0; i < descriptorSetCount; i++) {
            bufferSize += vt_sizeof_VkDescriptorSet(pDescriptorSets[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkFreeDescriptorSets(VkDevice device, VkDescriptorPool descriptorPool, uint32_t descriptorSetCount, const VkDescriptorSet* pDescriptorSets, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (descriptorPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDescriptorPool(descriptorPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDescriptorPool(descriptorPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = descriptorSetCount;
    bufferOffset += 4;

    if (pDescriptorSets) {
        *(int*)(outputBuffer + bufferOffset) = descriptorSetCount;
        bufferOffset += 4;
        for (int i = 0; i < descriptorSetCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkDescriptorSet(pDescriptorSets[i], outputBuffer + bufferOffset);
#else
            VkObject* descriptorSetsObject = VkObject_fromHandle(pDescriptorSets[i]);
            vt_serialize_VkDescriptorSet((VkDescriptorSet)&descriptorSetsObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkDescriptorSet(pDescriptorSets[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkFreeDescriptorSets(VkDevice device, VkDescriptorPool descriptorPool, uint32_t* descriptorSetCount, const VkDescriptorSet* pDescriptorSets, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (descriptorPool) {
            vt_unserialize_VkDescriptorPool(descriptorPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDescriptorPool(descriptorPool);
        }
        else bufferOffset += vt_sizeof_VkDescriptorPool(VK_NULL_HANDLE);
    }
    if (descriptorSetCount) *descriptorSetCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkDescriptorSet* arrValues = pDescriptorSets;
        for (int i = 0; i < dataSize4; i++) {
            uint64_t descriptorSetsId;
            vt_unserialize_VkDescriptorSet((VkDescriptorSet)&descriptorSetsId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(descriptorSetsId);
#else
                VkObject* descriptorSetsObject = VkObject_create(VK_OBJECT_TYPE_DESCRIPTOR_SET, descriptorSetsId);
                arrValues[i] = VkObject_toHandle(descriptorSetsObject);
#endif
                bufferOffset += vt_sizeof_VkDescriptorSet(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkDescriptorSet(VK_NULL_HANDLE);
        }
    }
    else pDescriptorSets = NULL;
}

static inline int vt_sizeof_vkUpdateDescriptorSets(VkDevice device, uint32_t descriptorWriteCount, const VkWriteDescriptorSet* pDescriptorWrites, uint32_t descriptorCopyCount, const VkCopyDescriptorSet* pDescriptorCopies) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4; // descriptorWriteCount
    bufferSize += 4;
    if (pDescriptorWrites) {
        int arrSize = descriptorWriteCount ? descriptorWriteCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkWriteDescriptorSet(&pDescriptorWrites[i]);
        }
    }
    bufferSize += 4; // descriptorCopyCount
    bufferSize += 4;
    if (pDescriptorCopies) {
        int arrSize = descriptorCopyCount ? descriptorCopyCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkCopyDescriptorSet(&pDescriptorCopies[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkUpdateDescriptorSets(VkDevice device, uint32_t descriptorWriteCount, const VkWriteDescriptorSet* pDescriptorWrites, uint32_t descriptorCopyCount, const VkCopyDescriptorSet* pDescriptorCopies, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = descriptorWriteCount;
    bufferOffset += 4;

    if (pDescriptorWrites) {
        int arrSize = descriptorWriteCount ? descriptorWriteCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkWriteDescriptorSet(&pDescriptorWrites[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkWriteDescriptorSet(&pDescriptorWrites[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = descriptorCopyCount;
    bufferOffset += 4;

    if (pDescriptorCopies) {
        int arrSize = descriptorCopyCount ? descriptorCopyCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkCopyDescriptorSet(&pDescriptorCopies[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkCopyDescriptorSet(&pDescriptorCopies[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkUpdateDescriptorSets(VkDevice device, uint32_t* descriptorWriteCount, const VkWriteDescriptorSet* pDescriptorWrites, uint32_t* descriptorCopyCount, const VkCopyDescriptorSet* pDescriptorCopies, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
    if (descriptorWriteCount) *descriptorWriteCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkWriteDescriptorSet* arrValues = pDescriptorWrites;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkWriteDescriptorSet(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pDescriptorWrites = NULL;
    if (descriptorCopyCount) *descriptorCopyCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkCopyDescriptorSet* arrValues = pDescriptorCopies;
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkCopyDescriptorSet(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pDescriptorCopies = NULL;
}

static inline int vt_sizeof_vkCreateFramebuffer(VkDevice device, const VkFramebufferCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkFramebuffer* pFramebuffer) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkFramebufferCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pFramebuffer ? vt_sizeof_VkFramebuffer(*pFramebuffer) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateFramebuffer(VkDevice device, const VkFramebufferCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkFramebuffer* pFramebuffer, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkFramebufferCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkFramebufferCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pFramebuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkFramebuffer(*pFramebuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkFramebuffer(*pFramebuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateFramebuffer(VkDevice device, const VkFramebufferCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkFramebuffer* pFramebuffer, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkFramebufferCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pFramebuffer) {
            vt_unserialize_VkFramebuffer(*pFramebuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkFramebuffer(*pFramebuffer);
        }
        else bufferOffset += vt_sizeof_VkFramebuffer(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyFramebuffer(VkDevice device, VkFramebuffer framebuffer, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += framebuffer ? vt_sizeof_VkFramebuffer(framebuffer) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyFramebuffer(VkDevice device, VkFramebuffer framebuffer, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (framebuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkFramebuffer(framebuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkFramebuffer(framebuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyFramebuffer(VkDevice device, VkFramebuffer framebuffer, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (framebuffer) {
            vt_unserialize_VkFramebuffer(framebuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkFramebuffer(framebuffer);
        }
        else bufferOffset += vt_sizeof_VkFramebuffer(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCreateRenderPass(VkDevice device, const VkRenderPassCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkRenderPass* pRenderPass) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkRenderPassCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pRenderPass ? vt_sizeof_VkRenderPass(*pRenderPass) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateRenderPass(VkDevice device, const VkRenderPassCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkRenderPass* pRenderPass, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkRenderPassCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkRenderPassCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pRenderPass) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkRenderPass(*pRenderPass, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkRenderPass(*pRenderPass);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateRenderPass(VkDevice device, const VkRenderPassCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkRenderPass* pRenderPass, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkRenderPassCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pRenderPass) {
            vt_unserialize_VkRenderPass(*pRenderPass, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkRenderPass(*pRenderPass);
        }
        else bufferOffset += vt_sizeof_VkRenderPass(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyRenderPass(VkDevice device, VkRenderPass renderPass, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += renderPass ? vt_sizeof_VkRenderPass(renderPass) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyRenderPass(VkDevice device, VkRenderPass renderPass, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (renderPass) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkRenderPass(renderPass, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkRenderPass(renderPass);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyRenderPass(VkDevice device, VkRenderPass renderPass, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (renderPass) {
            vt_unserialize_VkRenderPass(renderPass, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkRenderPass(renderPass);
        }
        else bufferOffset += vt_sizeof_VkRenderPass(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkGetRenderAreaGranularity(VkDevice device, VkRenderPass renderPass, VkExtent2D* pGranularity) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += renderPass ? vt_sizeof_VkRenderPass(renderPass) : 0;
    bufferSize += 4;
    bufferSize += pGranularity ? vt_sizeof_VkExtent2D(pGranularity) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetRenderAreaGranularity(VkDevice device, VkRenderPass renderPass, VkExtent2D* pGranularity, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (renderPass) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkRenderPass(renderPass, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkRenderPass(renderPass);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pGranularity) {
        int itemSize = vt_sizeof_VkExtent2D(pGranularity);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkExtent2D(pGranularity, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetRenderAreaGranularity(VkDevice device, VkRenderPass renderPass, VkExtent2D* pGranularity, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (renderPass) {
            vt_unserialize_VkRenderPass(renderPass, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkRenderPass(renderPass);
        }
        else bufferOffset += vt_sizeof_VkRenderPass(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pGranularity) vt_unserialize_VkExtent2D(pGranularity, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pGranularity) pGranularity = NULL;
}

static inline int vt_sizeof_vkCreateCommandPool(VkDevice device, const VkCommandPoolCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkCommandPool* pCommandPool) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkCommandPoolCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pCommandPool ? vt_sizeof_VkCommandPool(*pCommandPool) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateCommandPool(VkDevice device, const VkCommandPoolCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkCommandPool* pCommandPool, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkCommandPoolCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkCommandPoolCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pCommandPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandPool(*pCommandPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandPool(*pCommandPool);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateCommandPool(VkDevice device, const VkCommandPoolCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkCommandPool* pCommandPool, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkCommandPoolCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pCommandPool) {
            vt_unserialize_VkCommandPool(*pCommandPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandPool(*pCommandPool);
        }
        else bufferOffset += vt_sizeof_VkCommandPool(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroyCommandPool(VkDevice device, VkCommandPool commandPool, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += commandPool ? vt_sizeof_VkCommandPool(commandPool) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroyCommandPool(VkDevice device, VkCommandPool commandPool, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (commandPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandPool(commandPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandPool(commandPool);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroyCommandPool(VkDevice device, VkCommandPool commandPool, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (commandPool) {
            vt_unserialize_VkCommandPool(commandPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandPool(commandPool);
        }
        else bufferOffset += vt_sizeof_VkCommandPool(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkResetCommandPool(VkDevice device, VkCommandPool commandPool, VkCommandPoolResetFlags flags) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += commandPool ? vt_sizeof_VkCommandPool(commandPool) : 0;
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_vkResetCommandPool(VkDevice device, VkCommandPool commandPool, VkCommandPoolResetFlags flags, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (commandPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandPool(commandPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandPool(commandPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkCommandPoolResetFlags*)(outputBuffer + bufferOffset) = flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkResetCommandPool(VkDevice device, VkCommandPool commandPool, VkCommandPoolResetFlags* flags, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (commandPool) {
            vt_unserialize_VkCommandPool(commandPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandPool(commandPool);
        }
        else bufferOffset += vt_sizeof_VkCommandPool(VK_NULL_HANDLE);
    }
    if (flags) *flags = *(VkCommandPoolResetFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkAllocateCommandBuffers(VkDevice device, const VkCommandBufferAllocateInfo* pAllocateInfo, VkCommandBuffer* pCommandBuffers) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pAllocateInfo ? vt_sizeof_VkCommandBufferAllocateInfo(pAllocateInfo) : 0;
    bufferSize += 4;
    if (pCommandBuffers) {
        for (int i = 0; i < pAllocateInfo->commandBufferCount; i++) {
            bufferSize += vt_sizeof_VkCommandBuffer(pCommandBuffers[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkAllocateCommandBuffers(VkDevice device, const VkCommandBufferAllocateInfo* pAllocateInfo, VkCommandBuffer* pCommandBuffers, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pAllocateInfo) {
        int itemSize = vt_sizeof_VkCommandBufferAllocateInfo(pAllocateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkCommandBufferAllocateInfo(pAllocateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pCommandBuffers) {
        *(int*)(outputBuffer + bufferOffset) = pAllocateInfo->commandBufferCount;
        bufferOffset += 4;
        for (int i = 0; i < pAllocateInfo->commandBufferCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkCommandBuffer(pCommandBuffers[i], outputBuffer + bufferOffset);
#else
            VkObject* commandBuffersObject = VkObject_fromHandle(pCommandBuffers[i]);
            vt_serialize_VkCommandBuffer((VkCommandBuffer)&commandBuffersObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkCommandBuffer(pCommandBuffers[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkAllocateCommandBuffers(VkDevice device, const VkCommandBufferAllocateInfo* pAllocateInfo, VkCommandBuffer* pCommandBuffers, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pAllocateInfo) vt_unserialize_VkCommandBufferAllocateInfo(pAllocateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pAllocateInfo) pAllocateInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkCommandBuffer* arrValues = pCommandBuffers;
        for (int i = 0; i < dataSize3; i++) {
            uint64_t commandBuffersId;
            vt_unserialize_VkCommandBuffer((VkCommandBuffer)&commandBuffersId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(commandBuffersId);
#else
                VkObject* commandBuffersObject = VkObject_create(VK_OBJECT_TYPE_COMMAND_BUFFER, commandBuffersId);
                arrValues[i] = VkObject_toHandle(commandBuffersObject);
#endif
                bufferOffset += vt_sizeof_VkCommandBuffer(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
        }
    }
    else pCommandBuffers = NULL;
}

static inline int vt_sizeof_vkFreeCommandBuffers(VkDevice device, VkCommandPool commandPool, uint32_t commandBufferCount, const VkCommandBuffer* pCommandBuffers) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += commandPool ? vt_sizeof_VkCommandPool(commandPool) : 0;
    bufferSize += 4; // commandBufferCount
    bufferSize += 4;
    if (pCommandBuffers) {
        for (int i = 0; i < commandBufferCount; i++) {
            bufferSize += vt_sizeof_VkCommandBuffer(pCommandBuffers[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkFreeCommandBuffers(VkDevice device, VkCommandPool commandPool, uint32_t commandBufferCount, const VkCommandBuffer* pCommandBuffers, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (commandPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandPool(commandPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandPool(commandPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = commandBufferCount;
    bufferOffset += 4;

    if (pCommandBuffers) {
        *(int*)(outputBuffer + bufferOffset) = commandBufferCount;
        bufferOffset += 4;
        for (int i = 0; i < commandBufferCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkCommandBuffer(pCommandBuffers[i], outputBuffer + bufferOffset);
#else
            VkObject* commandBuffersObject = VkObject_fromHandle(pCommandBuffers[i]);
            vt_serialize_VkCommandBuffer((VkCommandBuffer)&commandBuffersObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkCommandBuffer(pCommandBuffers[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkFreeCommandBuffers(VkDevice device, VkCommandPool commandPool, uint32_t* commandBufferCount, const VkCommandBuffer* pCommandBuffers, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (commandPool) {
            vt_unserialize_VkCommandPool(commandPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandPool(commandPool);
        }
        else bufferOffset += vt_sizeof_VkCommandPool(VK_NULL_HANDLE);
    }
    if (commandBufferCount) *commandBufferCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkCommandBuffer* arrValues = pCommandBuffers;
        for (int i = 0; i < dataSize4; i++) {
            uint64_t commandBuffersId;
            vt_unserialize_VkCommandBuffer((VkCommandBuffer)&commandBuffersId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(commandBuffersId);
#else
                VkObject* commandBuffersObject = VkObject_create(VK_OBJECT_TYPE_COMMAND_BUFFER, commandBuffersId);
                arrValues[i] = VkObject_toHandle(commandBuffersObject);
#endif
                bufferOffset += vt_sizeof_VkCommandBuffer(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
        }
    }
    else pCommandBuffers = NULL;
}

static inline int vt_sizeof_vkBeginCommandBuffer(VkCommandBuffer commandBuffer, const VkCommandBufferBeginInfo* pBeginInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pBeginInfo ? vt_sizeof_VkCommandBufferBeginInfo(pBeginInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkBeginCommandBuffer(VkCommandBuffer commandBuffer, const VkCommandBufferBeginInfo* pBeginInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pBeginInfo) {
        int itemSize = vt_sizeof_VkCommandBufferBeginInfo(pBeginInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkCommandBufferBeginInfo(pBeginInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkBeginCommandBuffer(VkCommandBuffer commandBuffer, const VkCommandBufferBeginInfo* pBeginInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pBeginInfo) vt_unserialize_VkCommandBufferBeginInfo(pBeginInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pBeginInfo) pBeginInfo = NULL;
}

static inline int vt_sizeof_vkEndCommandBuffer(VkCommandBuffer commandBuffer) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkEndCommandBuffer(VkCommandBuffer commandBuffer, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkEndCommandBuffer(VkCommandBuffer commandBuffer, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkResetCommandBuffer(VkCommandBuffer commandBuffer, VkCommandBufferResetFlags flags) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_vkResetCommandBuffer(VkCommandBuffer commandBuffer, VkCommandBufferResetFlags flags, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkCommandBufferResetFlags*)(outputBuffer + bufferOffset) = flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkResetCommandBuffer(VkCommandBuffer commandBuffer, VkCommandBufferResetFlags* flags, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (flags) *flags = *(VkCommandBufferResetFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdBindPipeline(VkCommandBuffer commandBuffer, VkPipelineBindPoint pipelineBindPoint, VkPipeline pipeline) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // pipelineBindPoint
    bufferSize++;
    bufferSize += pipeline ? vt_sizeof_VkPipeline(pipeline) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdBindPipeline(VkCommandBuffer commandBuffer, VkPipelineBindPoint pipelineBindPoint, VkPipeline pipeline, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkPipelineBindPoint*)(outputBuffer + bufferOffset) = pipelineBindPoint;
    bufferOffset += 4;

    if (pipeline) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipeline(pipeline, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipeline(pipeline);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCmdBindPipeline(VkCommandBuffer commandBuffer, VkPipelineBindPoint* pipelineBindPoint, VkPipeline pipeline, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (pipelineBindPoint) *pipelineBindPoint = *(VkPipelineBindPoint*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    if (inputBuffer[bufferOffset++]) {
        if (pipeline) {
            vt_unserialize_VkPipeline(pipeline, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipeline(pipeline);
        }
        else bufferOffset += vt_sizeof_VkPipeline(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCmdSetViewport(VkCommandBuffer commandBuffer, uint32_t firstViewport, uint32_t viewportCount, const VkViewport* pViewports) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // firstViewport
    bufferSize += 4; // viewportCount
    bufferSize += 4;
    if (pViewports) {
        int arrSize = viewportCount ? viewportCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkViewport(&pViewports[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetViewport(VkCommandBuffer commandBuffer, uint32_t firstViewport, uint32_t viewportCount, const VkViewport* pViewports, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstViewport;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = viewportCount;
    bufferOffset += 4;

    if (pViewports) {
        int arrSize = viewportCount ? viewportCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkViewport(&pViewports[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkViewport(&pViewports[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdSetViewport(VkCommandBuffer commandBuffer, uint32_t* firstViewport, uint32_t* viewportCount, const VkViewport* pViewports, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (firstViewport) *firstViewport = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (viewportCount) *viewportCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkViewport* arrValues = pViewports;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkViewport(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pViewports = NULL;
}

static inline int vt_sizeof_vkCmdSetScissor(VkCommandBuffer commandBuffer, uint32_t firstScissor, uint32_t scissorCount, const VkRect2D* pScissors) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // firstScissor
    bufferSize += 4; // scissorCount
    bufferSize += 4;
    if (pScissors) {
        int arrSize = scissorCount ? scissorCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkRect2D(&pScissors[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetScissor(VkCommandBuffer commandBuffer, uint32_t firstScissor, uint32_t scissorCount, const VkRect2D* pScissors, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstScissor;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = scissorCount;
    bufferOffset += 4;

    if (pScissors) {
        int arrSize = scissorCount ? scissorCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkRect2D(&pScissors[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkRect2D(&pScissors[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdSetScissor(VkCommandBuffer commandBuffer, uint32_t* firstScissor, uint32_t* scissorCount, const VkRect2D* pScissors, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (firstScissor) *firstScissor = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (scissorCount) *scissorCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkRect2D* arrValues = pScissors;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkRect2D(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pScissors = NULL;
}

static inline int vt_sizeof_vkCmdSetLineWidth(VkCommandBuffer commandBuffer, float lineWidth) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // lineWidth
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetLineWidth(VkCommandBuffer commandBuffer, float lineWidth, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(float*)(outputBuffer + bufferOffset) = lineWidth;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetLineWidth(VkCommandBuffer commandBuffer, float* lineWidth, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (lineWidth) *lineWidth = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetDepthBias(VkCommandBuffer commandBuffer, float depthBiasConstantFactor, float depthBiasClamp, float depthBiasSlopeFactor) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // depthBiasConstantFactor
    bufferSize += 4; // depthBiasClamp
    bufferSize += 4; // depthBiasSlopeFactor
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetDepthBias(VkCommandBuffer commandBuffer, float depthBiasConstantFactor, float depthBiasClamp, float depthBiasSlopeFactor, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(float*)(outputBuffer + bufferOffset) = depthBiasConstantFactor;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = depthBiasClamp;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = depthBiasSlopeFactor;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetDepthBias(VkCommandBuffer commandBuffer, float* depthBiasConstantFactor, float* depthBiasClamp, float* depthBiasSlopeFactor, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (depthBiasConstantFactor) *depthBiasConstantFactor = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (depthBiasClamp) *depthBiasClamp = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (depthBiasSlopeFactor) *depthBiasSlopeFactor = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetBlendConstants(VkCommandBuffer commandBuffer, const float blendConstants[4]) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4 * 4;
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetBlendConstants(VkCommandBuffer commandBuffer, const float blendConstants[4], char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    memcpy(outputBuffer + bufferOffset, blendConstants, 4 * 4);
    bufferOffset += 4 * 4;
}

static inline void vt_unserialize_vkCmdSetBlendConstants(VkCommandBuffer commandBuffer, const float blendConstants[4], char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (blendConstants) memcpy(blendConstants, inputBuffer + bufferOffset, 4 * 4);
    bufferOffset += 4 * 4;
}

static inline int vt_sizeof_vkCmdSetDepthBounds(VkCommandBuffer commandBuffer, float minDepthBounds, float maxDepthBounds) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // minDepthBounds
    bufferSize += 4; // maxDepthBounds
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetDepthBounds(VkCommandBuffer commandBuffer, float minDepthBounds, float maxDepthBounds, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(float*)(outputBuffer + bufferOffset) = minDepthBounds;
    bufferOffset += 4;
    *(float*)(outputBuffer + bufferOffset) = maxDepthBounds;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetDepthBounds(VkCommandBuffer commandBuffer, float* minDepthBounds, float* maxDepthBounds, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (minDepthBounds) *minDepthBounds = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (maxDepthBounds) *maxDepthBounds = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetStencilCompareMask(VkCommandBuffer commandBuffer, VkStencilFaceFlags faceMask, uint32_t compareMask) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // faceMask
    bufferSize += 4; // compareMask
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetStencilCompareMask(VkCommandBuffer commandBuffer, VkStencilFaceFlags faceMask, uint32_t compareMask, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkStencilFaceFlags*)(outputBuffer + bufferOffset) = faceMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = compareMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetStencilCompareMask(VkCommandBuffer commandBuffer, VkStencilFaceFlags* faceMask, uint32_t* compareMask, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (faceMask) *faceMask = *(VkStencilFaceFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (compareMask) *compareMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetStencilWriteMask(VkCommandBuffer commandBuffer, VkStencilFaceFlags faceMask, uint32_t writeMask) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // faceMask
    bufferSize += 4; // writeMask
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetStencilWriteMask(VkCommandBuffer commandBuffer, VkStencilFaceFlags faceMask, uint32_t writeMask, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkStencilFaceFlags*)(outputBuffer + bufferOffset) = faceMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = writeMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetStencilWriteMask(VkCommandBuffer commandBuffer, VkStencilFaceFlags* faceMask, uint32_t* writeMask, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (faceMask) *faceMask = *(VkStencilFaceFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (writeMask) *writeMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetStencilReference(VkCommandBuffer commandBuffer, VkStencilFaceFlags faceMask, uint32_t reference) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // faceMask
    bufferSize += 4; // reference
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetStencilReference(VkCommandBuffer commandBuffer, VkStencilFaceFlags faceMask, uint32_t reference, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkStencilFaceFlags*)(outputBuffer + bufferOffset) = faceMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = reference;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetStencilReference(VkCommandBuffer commandBuffer, VkStencilFaceFlags* faceMask, uint32_t* reference, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (faceMask) *faceMask = *(VkStencilFaceFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (reference) *reference = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdBindDescriptorSets(VkCommandBuffer commandBuffer, VkPipelineBindPoint pipelineBindPoint, VkPipelineLayout layout, uint32_t firstSet, uint32_t descriptorSetCount, const VkDescriptorSet* pDescriptorSets, uint32_t dynamicOffsetCount, const uint32_t* pDynamicOffsets) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // pipelineBindPoint
    bufferSize++;
    bufferSize += layout ? vt_sizeof_VkPipelineLayout(layout) : 0;
    bufferSize += 4; // firstSet
    bufferSize += 4; // descriptorSetCount
    bufferSize += 4;
    if (pDescriptorSets) {
        for (int i = 0; i < descriptorSetCount; i++) {
            bufferSize += vt_sizeof_VkDescriptorSet(pDescriptorSets[i]);
        }
    }
    bufferSize += 4; // dynamicOffsetCount
    bufferSize += 4;
    bufferSize += pDynamicOffsets ? dynamicOffsetCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdBindDescriptorSets(VkCommandBuffer commandBuffer, VkPipelineBindPoint pipelineBindPoint, VkPipelineLayout layout, uint32_t firstSet, uint32_t descriptorSetCount, const VkDescriptorSet* pDescriptorSets, uint32_t dynamicOffsetCount, const uint32_t* pDynamicOffsets, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkPipelineBindPoint*)(outputBuffer + bufferOffset) = pipelineBindPoint;
    bufferOffset += 4;

    if (layout) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipelineLayout(layout, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipelineLayout(layout);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstSet;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = descriptorSetCount;
    bufferOffset += 4;

    if (pDescriptorSets) {
        *(int*)(outputBuffer + bufferOffset) = descriptorSetCount;
        bufferOffset += 4;
        for (int i = 0; i < descriptorSetCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkDescriptorSet(pDescriptorSets[i], outputBuffer + bufferOffset);
#else
            VkObject* descriptorSetsObject = VkObject_fromHandle(pDescriptorSets[i]);
            vt_serialize_VkDescriptorSet((VkDescriptorSet)&descriptorSetsObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkDescriptorSet(pDescriptorSets[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = dynamicOffsetCount;
    bufferOffset += 4;

    if (pDynamicOffsets) {
        int arrSize = dynamicOffsetCount ? dynamicOffsetCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pDynamicOffsets, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdBindDescriptorSets(VkCommandBuffer commandBuffer, VkPipelineBindPoint* pipelineBindPoint, VkPipelineLayout layout, uint32_t* firstSet, uint32_t* descriptorSetCount, const VkDescriptorSet* pDescriptorSets, uint32_t* dynamicOffsetCount, uint32_t* pDynamicOffsets, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (pipelineBindPoint) *pipelineBindPoint = *(VkPipelineBindPoint*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    if (inputBuffer[bufferOffset++]) {
        if (layout) {
            vt_unserialize_VkPipelineLayout(layout, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipelineLayout(layout);
        }
        else bufferOffset += vt_sizeof_VkPipelineLayout(VK_NULL_HANDLE);
    }
    if (firstSet) *firstSet = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (descriptorSetCount) *descriptorSetCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkDescriptorSet* arrValues = pDescriptorSets;
        for (int i = 0; i < dataSize6; i++) {
            uint64_t descriptorSetsId;
            vt_unserialize_VkDescriptorSet((VkDescriptorSet)&descriptorSetsId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(descriptorSetsId);
#else
                VkObject* descriptorSetsObject = VkObject_create(VK_OBJECT_TYPE_DESCRIPTOR_SET, descriptorSetsId);
                arrValues[i] = VkObject_toHandle(descriptorSetsObject);
#endif
                bufferOffset += vt_sizeof_VkDescriptorSet(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkDescriptorSet(VK_NULL_HANDLE);
        }
    }
    else pDescriptorSets = NULL;
    if (dynamicOffsetCount) *dynamicOffsetCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        uint32_t* arrValues = pDynamicOffsets;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize8 * 4);
        bufferOffset += dataSize8 * 4;
    }
    else pDynamicOffsets = NULL;
}

static inline int vt_sizeof_vkCmdBindIndexBuffer(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset, VkIndexType indexType) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += buffer ? vt_sizeof_VkBuffer(buffer) : 0;
    bufferSize += 8; // offset
    bufferSize += 4; // indexType
    return bufferSize;
}

static inline void vt_serialize_vkCmdBindIndexBuffer(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset, VkIndexType indexType, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (buffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(buffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(buffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = offset;
    bufferOffset += 8;
    *(VkIndexType*)(outputBuffer + bufferOffset) = indexType;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdBindIndexBuffer(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize* offset, VkIndexType* indexType, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (buffer) {
            vt_unserialize_VkBuffer(buffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(buffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (offset) *offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (indexType) *indexType = *(VkIndexType*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdBindVertexBuffers(VkCommandBuffer commandBuffer, uint32_t firstBinding, uint32_t bindingCount, const VkBuffer* pBuffers, const VkDeviceSize* pOffsets) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // firstBinding
    bufferSize += 4; // bindingCount
    bufferSize += 4;
    if (pBuffers) {
        for (int i = 0; i < bindingCount; i++) {
            bufferSize += vt_sizeof_VkBuffer(pBuffers[i]);
        }
    }
    bufferSize += 4;
    bufferSize += pOffsets ? bindingCount * 8 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdBindVertexBuffers(VkCommandBuffer commandBuffer, uint32_t firstBinding, uint32_t bindingCount, const VkBuffer* pBuffers, const VkDeviceSize* pOffsets, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstBinding;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = bindingCount;
    bufferOffset += 4;

    if (pBuffers) {
        *(int*)(outputBuffer + bufferOffset) = bindingCount;
        bufferOffset += 4;
        for (int i = 0; i < bindingCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkBuffer(pBuffers[i], outputBuffer + bufferOffset);
#else
            VkObject* buffersObject = VkObject_fromHandle(pBuffers[i]);
            vt_serialize_VkBuffer((VkBuffer)&buffersObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkBuffer(pBuffers[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pOffsets) {
        int arrSize = bindingCount ? bindingCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pOffsets, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdBindVertexBuffers(VkCommandBuffer commandBuffer, uint32_t* firstBinding, uint32_t* bindingCount, const VkBuffer* pBuffers, VkDeviceSize* pOffsets, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (firstBinding) *firstBinding = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (bindingCount) *bindingCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkBuffer* arrValues = pBuffers;
        for (int i = 0; i < dataSize4; i++) {
            uint64_t buffersId;
            vt_unserialize_VkBuffer((VkBuffer)&buffersId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(buffersId);
#else
                VkObject* buffersObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, buffersId);
                arrValues[i] = VkObject_toHandle(buffersObject);
#endif
                bufferOffset += vt_sizeof_VkBuffer(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
        }
    }
    else pBuffers = NULL;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkDeviceSize* arrValues = pOffsets;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize5 * 8);
        bufferOffset += dataSize5 * 8;
    }
    else pOffsets = NULL;
}

static inline int vt_sizeof_vkCmdDraw(VkCommandBuffer commandBuffer, uint32_t vertexCount, uint32_t instanceCount, uint32_t firstVertex, uint32_t firstInstance) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // vertexCount
    bufferSize += 4; // instanceCount
    bufferSize += 4; // firstVertex
    bufferSize += 4; // firstInstance
    return bufferSize;
}

static inline void vt_serialize_vkCmdDraw(VkCommandBuffer commandBuffer, uint32_t vertexCount, uint32_t instanceCount, uint32_t firstVertex, uint32_t firstInstance, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = vertexCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = instanceCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstVertex;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstInstance;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdDraw(VkCommandBuffer commandBuffer, uint32_t* vertexCount, uint32_t* instanceCount, uint32_t* firstVertex, uint32_t* firstInstance, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (vertexCount) *vertexCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (instanceCount) *instanceCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (firstVertex) *firstVertex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (firstInstance) *firstInstance = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdDrawIndexed(VkCommandBuffer commandBuffer, uint32_t indexCount, uint32_t instanceCount, uint32_t firstIndex, int32_t vertexOffset, uint32_t firstInstance) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // indexCount
    bufferSize += 4; // instanceCount
    bufferSize += 4; // firstIndex
    bufferSize += 4; // vertexOffset
    bufferSize += 4; // firstInstance
    return bufferSize;
}

static inline void vt_serialize_vkCmdDrawIndexed(VkCommandBuffer commandBuffer, uint32_t indexCount, uint32_t instanceCount, uint32_t firstIndex, int32_t vertexOffset, uint32_t firstInstance, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = indexCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = instanceCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstIndex;
    bufferOffset += 4;
    *(int32_t*)(outputBuffer + bufferOffset) = vertexOffset;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstInstance;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdDrawIndexed(VkCommandBuffer commandBuffer, uint32_t* indexCount, uint32_t* instanceCount, uint32_t* firstIndex, int32_t* vertexOffset, uint32_t* firstInstance, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (indexCount) *indexCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (instanceCount) *instanceCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (firstIndex) *firstIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (vertexOffset) *vertexOffset = *(int32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (firstInstance) *firstInstance = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdDrawIndirect(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset, uint32_t drawCount, uint32_t stride) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += buffer ? vt_sizeof_VkBuffer(buffer) : 0;
    bufferSize += 8; // offset
    bufferSize += 4; // drawCount
    bufferSize += 4; // stride
    return bufferSize;
}

static inline void vt_serialize_vkCmdDrawIndirect(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset, uint32_t drawCount, uint32_t stride, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (buffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(buffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(buffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = offset;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = drawCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = stride;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdDrawIndirect(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize* offset, uint32_t* drawCount, uint32_t* stride, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (buffer) {
            vt_unserialize_VkBuffer(buffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(buffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (offset) *offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (drawCount) *drawCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (stride) *stride = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdDrawIndexedIndirect(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset, uint32_t drawCount, uint32_t stride) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += buffer ? vt_sizeof_VkBuffer(buffer) : 0;
    bufferSize += 8; // offset
    bufferSize += 4; // drawCount
    bufferSize += 4; // stride
    return bufferSize;
}

static inline void vt_serialize_vkCmdDrawIndexedIndirect(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset, uint32_t drawCount, uint32_t stride, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (buffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(buffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(buffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = offset;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = drawCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = stride;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdDrawIndexedIndirect(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize* offset, uint32_t* drawCount, uint32_t* stride, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (buffer) {
            vt_unserialize_VkBuffer(buffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(buffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (offset) *offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (drawCount) *drawCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (stride) *stride = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdDispatch(VkCommandBuffer commandBuffer, uint32_t groupCountX, uint32_t groupCountY, uint32_t groupCountZ) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // groupCountX
    bufferSize += 4; // groupCountY
    bufferSize += 4; // groupCountZ
    return bufferSize;
}

static inline void vt_serialize_vkCmdDispatch(VkCommandBuffer commandBuffer, uint32_t groupCountX, uint32_t groupCountY, uint32_t groupCountZ, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = groupCountX;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = groupCountY;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = groupCountZ;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdDispatch(VkCommandBuffer commandBuffer, uint32_t* groupCountX, uint32_t* groupCountY, uint32_t* groupCountZ, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (groupCountX) *groupCountX = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (groupCountY) *groupCountY = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (groupCountZ) *groupCountZ = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdDispatchIndirect(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += buffer ? vt_sizeof_VkBuffer(buffer) : 0;
    bufferSize += 8; // offset
    return bufferSize;
}

static inline void vt_serialize_vkCmdDispatchIndirect(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (buffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(buffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(buffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = offset;
    bufferOffset += 8;
}

static inline void vt_unserialize_vkCmdDispatchIndirect(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize* offset, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (buffer) {
            vt_unserialize_VkBuffer(buffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(buffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (offset) *offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_vkCmdCopyBuffer(VkCommandBuffer commandBuffer, VkBuffer srcBuffer, VkBuffer dstBuffer, uint32_t regionCount, const VkBufferCopy* pRegions) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += srcBuffer ? vt_sizeof_VkBuffer(srcBuffer) : 0;
    bufferSize++;
    bufferSize += dstBuffer ? vt_sizeof_VkBuffer(dstBuffer) : 0;
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBufferCopy(&pRegions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdCopyBuffer(VkCommandBuffer commandBuffer, VkBuffer srcBuffer, VkBuffer dstBuffer, uint32_t regionCount, const VkBufferCopy* pRegions, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (srcBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(srcBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(srcBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (dstBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(dstBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(dstBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = regionCount;
    bufferOffset += 4;

    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBufferCopy(&pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBufferCopy(&pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdCopyBuffer(VkCommandBuffer commandBuffer, VkBuffer srcBuffer, VkBuffer dstBuffer, uint32_t* regionCount, const VkBufferCopy* pRegions, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (srcBuffer) {
            vt_unserialize_VkBuffer(srcBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(srcBuffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (dstBuffer) {
            vt_unserialize_VkBuffer(dstBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(dstBuffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (regionCount) *regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkBufferCopy* arrValues = pRegions;
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBufferCopy(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pRegions = NULL;
}

static inline int vt_sizeof_vkCmdCopyImage(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout srcImageLayout, VkImage dstImage, VkImageLayout dstImageLayout, uint32_t regionCount, const VkImageCopy* pRegions) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += srcImage ? vt_sizeof_VkImage(srcImage) : 0;
    bufferSize += 4; // srcImageLayout
    bufferSize++;
    bufferSize += dstImage ? vt_sizeof_VkImage(dstImage) : 0;
    bufferSize += 4; // dstImageLayout
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkImageCopy(&pRegions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdCopyImage(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout srcImageLayout, VkImage dstImage, VkImageLayout dstImageLayout, uint32_t regionCount, const VkImageCopy* pRegions, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (srcImage) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(srcImage, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(srcImage);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = srcImageLayout;
    bufferOffset += 4;

    if (dstImage) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(dstImage, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(dstImage);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = dstImageLayout;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = regionCount;
    bufferOffset += 4;

    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkImageCopy(&pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkImageCopy(&pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdCopyImage(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout* srcImageLayout, VkImage dstImage, VkImageLayout* dstImageLayout, uint32_t* regionCount, const VkImageCopy* pRegions, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (srcImage) {
            vt_unserialize_VkImage(srcImage, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(srcImage);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
    if (srcImageLayout) *srcImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    if (inputBuffer[bufferOffset++]) {
        if (dstImage) {
            vt_unserialize_VkImage(dstImage, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(dstImage);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
    if (dstImageLayout) *dstImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (regionCount) *regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkImageCopy* arrValues = pRegions;
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkImageCopy(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pRegions = NULL;
}

static inline int vt_sizeof_vkCmdBlitImage(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout srcImageLayout, VkImage dstImage, VkImageLayout dstImageLayout, uint32_t regionCount, const VkImageBlit* pRegions, VkFilter filter) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += srcImage ? vt_sizeof_VkImage(srcImage) : 0;
    bufferSize += 4; // srcImageLayout
    bufferSize++;
    bufferSize += dstImage ? vt_sizeof_VkImage(dstImage) : 0;
    bufferSize += 4; // dstImageLayout
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkImageBlit(&pRegions[i]);
        }
    }
    bufferSize += 4; // filter
    return bufferSize;
}

static inline void vt_serialize_vkCmdBlitImage(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout srcImageLayout, VkImage dstImage, VkImageLayout dstImageLayout, uint32_t regionCount, const VkImageBlit* pRegions, VkFilter filter, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (srcImage) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(srcImage, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(srcImage);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = srcImageLayout;
    bufferOffset += 4;

    if (dstImage) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(dstImage, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(dstImage);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = dstImageLayout;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = regionCount;
    bufferOffset += 4;

    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkImageBlit(&pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkImageBlit(&pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkFilter*)(outputBuffer + bufferOffset) = filter;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdBlitImage(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout* srcImageLayout, VkImage dstImage, VkImageLayout* dstImageLayout, uint32_t* regionCount, const VkImageBlit* pRegions, VkFilter* filter, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (srcImage) {
            vt_unserialize_VkImage(srcImage, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(srcImage);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
    if (srcImageLayout) *srcImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    if (inputBuffer[bufferOffset++]) {
        if (dstImage) {
            vt_unserialize_VkImage(dstImage, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(dstImage);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
    if (dstImageLayout) *dstImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (regionCount) *regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkImageBlit* arrValues = pRegions;
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkImageBlit(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pRegions = NULL;
    if (filter) *filter = *(VkFilter*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdCopyBufferToImage(VkCommandBuffer commandBuffer, VkBuffer srcBuffer, VkImage dstImage, VkImageLayout dstImageLayout, uint32_t regionCount, const VkBufferImageCopy* pRegions) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += srcBuffer ? vt_sizeof_VkBuffer(srcBuffer) : 0;
    bufferSize++;
    bufferSize += dstImage ? vt_sizeof_VkImage(dstImage) : 0;
    bufferSize += 4; // dstImageLayout
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBufferImageCopy(&pRegions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdCopyBufferToImage(VkCommandBuffer commandBuffer, VkBuffer srcBuffer, VkImage dstImage, VkImageLayout dstImageLayout, uint32_t regionCount, const VkBufferImageCopy* pRegions, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (srcBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(srcBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(srcBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (dstImage) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(dstImage, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(dstImage);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = dstImageLayout;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = regionCount;
    bufferOffset += 4;

    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBufferImageCopy(&pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBufferImageCopy(&pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdCopyBufferToImage(VkCommandBuffer commandBuffer, VkBuffer srcBuffer, VkImage dstImage, VkImageLayout* dstImageLayout, uint32_t* regionCount, const VkBufferImageCopy* pRegions, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (srcBuffer) {
            vt_unserialize_VkBuffer(srcBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(srcBuffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (dstImage) {
            vt_unserialize_VkImage(dstImage, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(dstImage);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
    if (dstImageLayout) *dstImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (regionCount) *regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkBufferImageCopy* arrValues = pRegions;
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBufferImageCopy(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pRegions = NULL;
}

static inline int vt_sizeof_vkCmdCopyImageToBuffer(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout srcImageLayout, VkBuffer dstBuffer, uint32_t regionCount, const VkBufferImageCopy* pRegions) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += srcImage ? vt_sizeof_VkImage(srcImage) : 0;
    bufferSize += 4; // srcImageLayout
    bufferSize++;
    bufferSize += dstBuffer ? vt_sizeof_VkBuffer(dstBuffer) : 0;
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBufferImageCopy(&pRegions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdCopyImageToBuffer(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout srcImageLayout, VkBuffer dstBuffer, uint32_t regionCount, const VkBufferImageCopy* pRegions, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (srcImage) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(srcImage, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(srcImage);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = srcImageLayout;
    bufferOffset += 4;

    if (dstBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(dstBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(dstBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = regionCount;
    bufferOffset += 4;

    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBufferImageCopy(&pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBufferImageCopy(&pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdCopyImageToBuffer(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout* srcImageLayout, VkBuffer dstBuffer, uint32_t* regionCount, const VkBufferImageCopy* pRegions, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (srcImage) {
            vt_unserialize_VkImage(srcImage, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(srcImage);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
    if (srcImageLayout) *srcImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    if (inputBuffer[bufferOffset++]) {
        if (dstBuffer) {
            vt_unserialize_VkBuffer(dstBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(dstBuffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (regionCount) *regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkBufferImageCopy* arrValues = pRegions;
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBufferImageCopy(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pRegions = NULL;
}

static inline int vt_sizeof_vkCmdUpdateBuffer(VkCommandBuffer commandBuffer, VkBuffer dstBuffer, VkDeviceSize dstOffset, VkDeviceSize dataSize, const void* pData) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += dstBuffer ? vt_sizeof_VkBuffer(dstBuffer) : 0;
    bufferSize += 8; // dstOffset
    bufferSize += 8; // dataSize
    bufferSize += 4;
    bufferSize += pData ? dataSize : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdUpdateBuffer(VkCommandBuffer commandBuffer, VkBuffer dstBuffer, VkDeviceSize dstOffset, VkDeviceSize dataSize, const void* pData, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (dstBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(dstBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(dstBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = dstOffset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = dataSize;
    bufferOffset += 8;

    if (pData) {
        *(int*)(outputBuffer + bufferOffset) = dataSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pData, dataSize);
        bufferOffset += dataSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdUpdateBuffer(VkCommandBuffer commandBuffer, VkBuffer dstBuffer, VkDeviceSize* dstOffset, VkDeviceSize* dataSize, const void* pData, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (dstBuffer) {
            vt_unserialize_VkBuffer(dstBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(dstBuffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (dstOffset) *dstOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (dataSize) *dataSize = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        if (pData) memcpy(pData, inputBuffer + bufferOffset, dataSize5);
        bufferOffset += dataSize5;
    }
    else pData = NULL;
}

static inline int vt_sizeof_vkCmdFillBuffer(VkCommandBuffer commandBuffer, VkBuffer dstBuffer, VkDeviceSize dstOffset, VkDeviceSize size, uint32_t data) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += dstBuffer ? vt_sizeof_VkBuffer(dstBuffer) : 0;
    bufferSize += 8; // dstOffset
    bufferSize += 8; // size
    bufferSize += 4; // data
    return bufferSize;
}

static inline void vt_serialize_vkCmdFillBuffer(VkCommandBuffer commandBuffer, VkBuffer dstBuffer, VkDeviceSize dstOffset, VkDeviceSize size, uint32_t data, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (dstBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(dstBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(dstBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = dstOffset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = size;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = data;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdFillBuffer(VkCommandBuffer commandBuffer, VkBuffer dstBuffer, VkDeviceSize* dstOffset, VkDeviceSize* size, uint32_t* data, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (dstBuffer) {
            vt_unserialize_VkBuffer(dstBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(dstBuffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (dstOffset) *dstOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (size) *size = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (data) *data = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdClearColorImage(VkCommandBuffer commandBuffer, VkImage image, VkImageLayout imageLayout, const VkClearColorValue* pColor, uint32_t rangeCount, const VkImageSubresourceRange* pRanges) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += image ? vt_sizeof_VkImage(image) : 0;
    bufferSize += 4; // imageLayout
    bufferSize += 4;
    bufferSize += pColor ? vt_sizeof_VkClearColorValue(pColor) : 0;
    bufferSize += 4; // rangeCount
    bufferSize += 4;
    if (pRanges) {
        int arrSize = rangeCount ? rangeCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkImageSubresourceRange(&pRanges[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdClearColorImage(VkCommandBuffer commandBuffer, VkImage image, VkImageLayout imageLayout, const VkClearColorValue* pColor, uint32_t rangeCount, const VkImageSubresourceRange* pRanges, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (image) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(image, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(image);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = imageLayout;
    bufferOffset += 4;

    if (pColor) {
        int itemSize = vt_sizeof_VkClearColorValue(pColor);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkClearColorValue(pColor, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = rangeCount;
    bufferOffset += 4;

    if (pRanges) {
        int arrSize = rangeCount ? rangeCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkImageSubresourceRange(&pRanges[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkImageSubresourceRange(&pRanges[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdClearColorImage(VkCommandBuffer commandBuffer, VkImage image, VkImageLayout* imageLayout, const VkClearColorValue* pColor, uint32_t* rangeCount, const VkImageSubresourceRange* pRanges, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (image) {
            vt_unserialize_VkImage(image, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(image);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
    if (imageLayout) *imageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        if (pColor) vt_unserialize_VkClearColorValue(pColor, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize4;
    }
    else if (pColor) pColor = NULL;
    if (rangeCount) *rangeCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkImageSubresourceRange* arrValues = pRanges;
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkImageSubresourceRange(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pRanges = NULL;
}

static inline int vt_sizeof_vkCmdClearDepthStencilImage(VkCommandBuffer commandBuffer, VkImage image, VkImageLayout imageLayout, const VkClearDepthStencilValue* pDepthStencil, uint32_t rangeCount, const VkImageSubresourceRange* pRanges) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += image ? vt_sizeof_VkImage(image) : 0;
    bufferSize += 4; // imageLayout
    bufferSize += 4;
    bufferSize += pDepthStencil ? vt_sizeof_VkClearDepthStencilValue(pDepthStencil) : 0;
    bufferSize += 4; // rangeCount
    bufferSize += 4;
    if (pRanges) {
        int arrSize = rangeCount ? rangeCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkImageSubresourceRange(&pRanges[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdClearDepthStencilImage(VkCommandBuffer commandBuffer, VkImage image, VkImageLayout imageLayout, const VkClearDepthStencilValue* pDepthStencil, uint32_t rangeCount, const VkImageSubresourceRange* pRanges, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (image) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(image, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(image);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = imageLayout;
    bufferOffset += 4;

    if (pDepthStencil) {
        int itemSize = vt_sizeof_VkClearDepthStencilValue(pDepthStencil);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkClearDepthStencilValue(pDepthStencil, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = rangeCount;
    bufferOffset += 4;

    if (pRanges) {
        int arrSize = rangeCount ? rangeCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkImageSubresourceRange(&pRanges[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkImageSubresourceRange(&pRanges[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdClearDepthStencilImage(VkCommandBuffer commandBuffer, VkImage image, VkImageLayout* imageLayout, const VkClearDepthStencilValue* pDepthStencil, uint32_t* rangeCount, const VkImageSubresourceRange* pRanges, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (image) {
            vt_unserialize_VkImage(image, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(image);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
    if (imageLayout) *imageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        if (pDepthStencil) vt_unserialize_VkClearDepthStencilValue(pDepthStencil, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize4;
    }
    else if (pDepthStencil) pDepthStencil = NULL;
    if (rangeCount) *rangeCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkImageSubresourceRange* arrValues = pRanges;
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkImageSubresourceRange(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pRanges = NULL;
}

static inline int vt_sizeof_vkCmdClearAttachments(VkCommandBuffer commandBuffer, uint32_t attachmentCount, const VkClearAttachment* pAttachments, uint32_t rectCount, const VkClearRect* pRects) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    if (pAttachments) {
        int arrSize = attachmentCount ? attachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkClearAttachment(&pAttachments[i]);
        }
    }
    bufferSize += 4; // rectCount
    bufferSize += 4;
    if (pRects) {
        int arrSize = rectCount ? rectCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkClearRect(&pRects[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdClearAttachments(VkCommandBuffer commandBuffer, uint32_t attachmentCount, const VkClearAttachment* pAttachments, uint32_t rectCount, const VkClearRect* pRects, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = attachmentCount;
    bufferOffset += 4;

    if (pAttachments) {
        int arrSize = attachmentCount ? attachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkClearAttachment(&pAttachments[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkClearAttachment(&pAttachments[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = rectCount;
    bufferOffset += 4;

    if (pRects) {
        int arrSize = rectCount ? rectCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkClearRect(&pRects[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkClearRect(&pRects[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdClearAttachments(VkCommandBuffer commandBuffer, uint32_t* attachmentCount, const VkClearAttachment* pAttachments, uint32_t* rectCount, const VkClearRect* pRects, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (attachmentCount) *attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkClearAttachment* arrValues = pAttachments;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkClearAttachment(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pAttachments = NULL;
    if (rectCount) *rectCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkClearRect* arrValues = pRects;
        for (int i = 0; i < dataSize5; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkClearRect(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pRects = NULL;
}

static inline int vt_sizeof_vkCmdResolveImage(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout srcImageLayout, VkImage dstImage, VkImageLayout dstImageLayout, uint32_t regionCount, const VkImageResolve* pRegions) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += srcImage ? vt_sizeof_VkImage(srcImage) : 0;
    bufferSize += 4; // srcImageLayout
    bufferSize++;
    bufferSize += dstImage ? vt_sizeof_VkImage(dstImage) : 0;
    bufferSize += 4; // dstImageLayout
    bufferSize += 4; // regionCount
    bufferSize += 4;
    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkImageResolve(&pRegions[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdResolveImage(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout srcImageLayout, VkImage dstImage, VkImageLayout dstImageLayout, uint32_t regionCount, const VkImageResolve* pRegions, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (srcImage) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(srcImage, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(srcImage);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = srcImageLayout;
    bufferOffset += 4;

    if (dstImage) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkImage(dstImage, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkImage(dstImage);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkImageLayout*)(outputBuffer + bufferOffset) = dstImageLayout;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = regionCount;
    bufferOffset += 4;

    if (pRegions) {
        int arrSize = regionCount ? regionCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkImageResolve(&pRegions[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkImageResolve(&pRegions[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdResolveImage(VkCommandBuffer commandBuffer, VkImage srcImage, VkImageLayout* srcImageLayout, VkImage dstImage, VkImageLayout* dstImageLayout, uint32_t* regionCount, const VkImageResolve* pRegions, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (srcImage) {
            vt_unserialize_VkImage(srcImage, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(srcImage);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
    if (srcImageLayout) *srcImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    if (inputBuffer[bufferOffset++]) {
        if (dstImage) {
            vt_unserialize_VkImage(dstImage, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkImage(dstImage);
        }
        else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
    }
    if (dstImageLayout) *dstImageLayout = *(VkImageLayout*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (regionCount) *regionCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkImageResolve* arrValues = pRegions;
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkImageResolve(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pRegions = NULL;
}

static inline int vt_sizeof_vkCmdSetEvent(VkCommandBuffer commandBuffer, VkEvent event, VkPipelineStageFlags stageMask) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += event ? vt_sizeof_VkEvent(event) : 0;
    bufferSize += 4; // stageMask
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetEvent(VkCommandBuffer commandBuffer, VkEvent event, VkPipelineStageFlags stageMask, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (event) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkEvent(event, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkEvent(event);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkPipelineStageFlags*)(outputBuffer + bufferOffset) = stageMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetEvent(VkCommandBuffer commandBuffer, VkEvent event, VkPipelineStageFlags* stageMask, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (event) {
            vt_unserialize_VkEvent(event, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkEvent(event);
        }
        else bufferOffset += vt_sizeof_VkEvent(VK_NULL_HANDLE);
    }
    if (stageMask) *stageMask = *(VkPipelineStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdResetEvent(VkCommandBuffer commandBuffer, VkEvent event, VkPipelineStageFlags stageMask) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += event ? vt_sizeof_VkEvent(event) : 0;
    bufferSize += 4; // stageMask
    return bufferSize;
}

static inline void vt_serialize_vkCmdResetEvent(VkCommandBuffer commandBuffer, VkEvent event, VkPipelineStageFlags stageMask, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (event) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkEvent(event, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkEvent(event);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkPipelineStageFlags*)(outputBuffer + bufferOffset) = stageMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdResetEvent(VkCommandBuffer commandBuffer, VkEvent event, VkPipelineStageFlags* stageMask, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (event) {
            vt_unserialize_VkEvent(event, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkEvent(event);
        }
        else bufferOffset += vt_sizeof_VkEvent(VK_NULL_HANDLE);
    }
    if (stageMask) *stageMask = *(VkPipelineStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdWaitEvents(VkCommandBuffer commandBuffer, uint32_t eventCount, const VkEvent* pEvents, VkPipelineStageFlags srcStageMask, VkPipelineStageFlags dstStageMask, uint32_t memoryBarrierCount, const VkMemoryBarrier* pMemoryBarriers, uint32_t bufferMemoryBarrierCount, const VkBufferMemoryBarrier* pBufferMemoryBarriers, uint32_t imageMemoryBarrierCount, const VkImageMemoryBarrier* pImageMemoryBarriers) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // eventCount
    bufferSize += 4;
    if (pEvents) {
        for (int i = 0; i < eventCount; i++) {
            bufferSize += vt_sizeof_VkEvent(pEvents[i]);
        }
    }
    bufferSize += 4; // srcStageMask
    bufferSize += 4; // dstStageMask
    bufferSize += 4; // memoryBarrierCount
    bufferSize += 4;
    if (pMemoryBarriers) {
        int arrSize = memoryBarrierCount ? memoryBarrierCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkMemoryBarrier(&pMemoryBarriers[i]);
        }
    }
    bufferSize += 4; // bufferMemoryBarrierCount
    bufferSize += 4;
    if (pBufferMemoryBarriers) {
        int arrSize = bufferMemoryBarrierCount ? bufferMemoryBarrierCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBufferMemoryBarrier(&pBufferMemoryBarriers[i]);
        }
    }
    bufferSize += 4; // imageMemoryBarrierCount
    bufferSize += 4;
    if (pImageMemoryBarriers) {
        int arrSize = imageMemoryBarrierCount ? imageMemoryBarrierCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkImageMemoryBarrier(&pImageMemoryBarriers[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdWaitEvents(VkCommandBuffer commandBuffer, uint32_t eventCount, const VkEvent* pEvents, VkPipelineStageFlags srcStageMask, VkPipelineStageFlags dstStageMask, uint32_t memoryBarrierCount, const VkMemoryBarrier* pMemoryBarriers, uint32_t bufferMemoryBarrierCount, const VkBufferMemoryBarrier* pBufferMemoryBarriers, uint32_t imageMemoryBarrierCount, const VkImageMemoryBarrier* pImageMemoryBarriers, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = eventCount;
    bufferOffset += 4;

    if (pEvents) {
        *(int*)(outputBuffer + bufferOffset) = eventCount;
        bufferOffset += 4;
        for (int i = 0; i < eventCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkEvent(pEvents[i], outputBuffer + bufferOffset);
#else
            VkObject* eventsObject = VkObject_fromHandle(pEvents[i]);
            vt_serialize_VkEvent((VkEvent)&eventsObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkEvent(pEvents[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkPipelineStageFlags*)(outputBuffer + bufferOffset) = srcStageMask;
    bufferOffset += 4;
    *(VkPipelineStageFlags*)(outputBuffer + bufferOffset) = dstStageMask;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = memoryBarrierCount;
    bufferOffset += 4;

    if (pMemoryBarriers) {
        int arrSize = memoryBarrierCount ? memoryBarrierCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkMemoryBarrier(&pMemoryBarriers[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkMemoryBarrier(&pMemoryBarriers[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = bufferMemoryBarrierCount;
    bufferOffset += 4;

    if (pBufferMemoryBarriers) {
        int arrSize = bufferMemoryBarrierCount ? bufferMemoryBarrierCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBufferMemoryBarrier(&pBufferMemoryBarriers[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBufferMemoryBarrier(&pBufferMemoryBarriers[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = imageMemoryBarrierCount;
    bufferOffset += 4;

    if (pImageMemoryBarriers) {
        int arrSize = imageMemoryBarrierCount ? imageMemoryBarrierCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkImageMemoryBarrier(&pImageMemoryBarriers[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkImageMemoryBarrier(&pImageMemoryBarriers[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdWaitEvents(VkCommandBuffer commandBuffer, uint32_t* eventCount, const VkEvent* pEvents, VkPipelineStageFlags* srcStageMask, VkPipelineStageFlags* dstStageMask, uint32_t* memoryBarrierCount, const VkMemoryBarrier* pMemoryBarriers, uint32_t* bufferMemoryBarrierCount, const VkBufferMemoryBarrier* pBufferMemoryBarriers, uint32_t* imageMemoryBarrierCount, const VkImageMemoryBarrier* pImageMemoryBarriers, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (eventCount) *eventCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkEvent* arrValues = pEvents;
        for (int i = 0; i < dataSize3; i++) {
            uint64_t eventsId;
            vt_unserialize_VkEvent((VkEvent)&eventsId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(eventsId);
#else
                VkObject* eventsObject = VkObject_create(VK_OBJECT_TYPE_EVENT, eventsId);
                arrValues[i] = VkObject_toHandle(eventsObject);
#endif
                bufferOffset += vt_sizeof_VkEvent(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkEvent(VK_NULL_HANDLE);
        }
    }
    else pEvents = NULL;
    if (srcStageMask) *srcStageMask = *(VkPipelineStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dstStageMask) *dstStageMask = *(VkPipelineStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (memoryBarrierCount) *memoryBarrierCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkMemoryBarrier* arrValues = pMemoryBarriers;
        for (int i = 0; i < dataSize7; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkMemoryBarrier(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pMemoryBarriers = NULL;
    if (bufferMemoryBarrierCount) *bufferMemoryBarrierCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize9 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize9 > 0) {
        VkBufferMemoryBarrier* arrValues = pBufferMemoryBarriers;
        for (int i = 0; i < dataSize9; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBufferMemoryBarrier(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pBufferMemoryBarriers = NULL;
    if (imageMemoryBarrierCount) *imageMemoryBarrierCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize11 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize11 > 0) {
        VkImageMemoryBarrier* arrValues = pImageMemoryBarriers;
        for (int i = 0; i < dataSize11; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkImageMemoryBarrier(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pImageMemoryBarriers = NULL;
}

static inline int vt_sizeof_vkCmdPipelineBarrier(VkCommandBuffer commandBuffer, VkPipelineStageFlags srcStageMask, VkPipelineStageFlags dstStageMask, VkDependencyFlags dependencyFlags, uint32_t memoryBarrierCount, const VkMemoryBarrier* pMemoryBarriers, uint32_t bufferMemoryBarrierCount, const VkBufferMemoryBarrier* pBufferMemoryBarriers, uint32_t imageMemoryBarrierCount, const VkImageMemoryBarrier* pImageMemoryBarriers) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // srcStageMask
    bufferSize += 4; // dstStageMask
    bufferSize += 4; // dependencyFlags
    bufferSize += 4; // memoryBarrierCount
    bufferSize += 4;
    if (pMemoryBarriers) {
        int arrSize = memoryBarrierCount ? memoryBarrierCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkMemoryBarrier(&pMemoryBarriers[i]);
        }
    }
    bufferSize += 4; // bufferMemoryBarrierCount
    bufferSize += 4;
    if (pBufferMemoryBarriers) {
        int arrSize = bufferMemoryBarrierCount ? bufferMemoryBarrierCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBufferMemoryBarrier(&pBufferMemoryBarriers[i]);
        }
    }
    bufferSize += 4; // imageMemoryBarrierCount
    bufferSize += 4;
    if (pImageMemoryBarriers) {
        int arrSize = imageMemoryBarrierCount ? imageMemoryBarrierCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkImageMemoryBarrier(&pImageMemoryBarriers[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdPipelineBarrier(VkCommandBuffer commandBuffer, VkPipelineStageFlags srcStageMask, VkPipelineStageFlags dstStageMask, VkDependencyFlags dependencyFlags, uint32_t memoryBarrierCount, const VkMemoryBarrier* pMemoryBarriers, uint32_t bufferMemoryBarrierCount, const VkBufferMemoryBarrier* pBufferMemoryBarriers, uint32_t imageMemoryBarrierCount, const VkImageMemoryBarrier* pImageMemoryBarriers, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkPipelineStageFlags*)(outputBuffer + bufferOffset) = srcStageMask;
    bufferOffset += 4;
    *(VkPipelineStageFlags*)(outputBuffer + bufferOffset) = dstStageMask;
    bufferOffset += 4;
    *(VkDependencyFlags*)(outputBuffer + bufferOffset) = dependencyFlags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = memoryBarrierCount;
    bufferOffset += 4;

    if (pMemoryBarriers) {
        int arrSize = memoryBarrierCount ? memoryBarrierCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkMemoryBarrier(&pMemoryBarriers[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkMemoryBarrier(&pMemoryBarriers[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = bufferMemoryBarrierCount;
    bufferOffset += 4;

    if (pBufferMemoryBarriers) {
        int arrSize = bufferMemoryBarrierCount ? bufferMemoryBarrierCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBufferMemoryBarrier(&pBufferMemoryBarriers[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBufferMemoryBarrier(&pBufferMemoryBarriers[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint32_t*)(outputBuffer + bufferOffset) = imageMemoryBarrierCount;
    bufferOffset += 4;

    if (pImageMemoryBarriers) {
        int arrSize = imageMemoryBarrierCount ? imageMemoryBarrierCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkImageMemoryBarrier(&pImageMemoryBarriers[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkImageMemoryBarrier(&pImageMemoryBarriers[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdPipelineBarrier(VkCommandBuffer commandBuffer, VkPipelineStageFlags* srcStageMask, VkPipelineStageFlags* dstStageMask, VkDependencyFlags* dependencyFlags, uint32_t* memoryBarrierCount, const VkMemoryBarrier* pMemoryBarriers, uint32_t* bufferMemoryBarrierCount, const VkBufferMemoryBarrier* pBufferMemoryBarriers, uint32_t* imageMemoryBarrierCount, const VkImageMemoryBarrier* pImageMemoryBarriers, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (srcStageMask) *srcStageMask = *(VkPipelineStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dstStageMask) *dstStageMask = *(VkPipelineStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dependencyFlags) *dependencyFlags = *(VkDependencyFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (memoryBarrierCount) *memoryBarrierCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkMemoryBarrier* arrValues = pMemoryBarriers;
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkMemoryBarrier(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pMemoryBarriers = NULL;
    if (bufferMemoryBarrierCount) *bufferMemoryBarrierCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize8 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize8 > 0) {
        VkBufferMemoryBarrier* arrValues = pBufferMemoryBarriers;
        for (int i = 0; i < dataSize8; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBufferMemoryBarrier(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pBufferMemoryBarriers = NULL;
    if (imageMemoryBarrierCount) *imageMemoryBarrierCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize10 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize10 > 0) {
        VkImageMemoryBarrier* arrValues = pImageMemoryBarriers;
        for (int i = 0; i < dataSize10; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkImageMemoryBarrier(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pImageMemoryBarriers = NULL;
}

static inline int vt_sizeof_vkCmdBeginQuery(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t query, VkQueryControlFlags flags) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += queryPool ? vt_sizeof_VkQueryPool(queryPool) : 0;
    bufferSize += 4; // query
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_vkCmdBeginQuery(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t query, VkQueryControlFlags flags, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (queryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(queryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(queryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = query;
    bufferOffset += 4;
    *(VkQueryControlFlags*)(outputBuffer + bufferOffset) = flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdBeginQuery(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t* query, VkQueryControlFlags* flags, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (queryPool) {
            vt_unserialize_VkQueryPool(queryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(queryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
    if (query) *query = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (flags) *flags = *(VkQueryControlFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdEndQuery(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t query) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += queryPool ? vt_sizeof_VkQueryPool(queryPool) : 0;
    bufferSize += 4; // query
    return bufferSize;
}

static inline void vt_serialize_vkCmdEndQuery(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t query, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (queryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(queryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(queryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = query;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdEndQuery(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t* query, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (queryPool) {
            vt_unserialize_VkQueryPool(queryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(queryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
    if (query) *query = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdBeginConditionalRenderingEXT(VkCommandBuffer commandBuffer, const VkConditionalRenderingBeginInfoEXT* pConditionalRenderingBegin) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pConditionalRenderingBegin ? vt_sizeof_VkConditionalRenderingBeginInfoEXT(pConditionalRenderingBegin) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdBeginConditionalRenderingEXT(VkCommandBuffer commandBuffer, const VkConditionalRenderingBeginInfoEXT* pConditionalRenderingBegin, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pConditionalRenderingBegin) {
        int itemSize = vt_sizeof_VkConditionalRenderingBeginInfoEXT(pConditionalRenderingBegin);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkConditionalRenderingBeginInfoEXT(pConditionalRenderingBegin, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdBeginConditionalRenderingEXT(VkCommandBuffer commandBuffer, const VkConditionalRenderingBeginInfoEXT* pConditionalRenderingBegin, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pConditionalRenderingBegin) vt_unserialize_VkConditionalRenderingBeginInfoEXT(pConditionalRenderingBegin, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pConditionalRenderingBegin) pConditionalRenderingBegin = NULL;
}

static inline int vt_sizeof_vkCmdEndConditionalRenderingEXT(VkCommandBuffer commandBuffer) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdEndConditionalRenderingEXT(VkCommandBuffer commandBuffer, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCmdEndConditionalRenderingEXT(VkCommandBuffer commandBuffer, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCmdResetQueryPool(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t firstQuery, uint32_t queryCount) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += queryPool ? vt_sizeof_VkQueryPool(queryPool) : 0;
    bufferSize += 4; // firstQuery
    bufferSize += 4; // queryCount
    return bufferSize;
}

static inline void vt_serialize_vkCmdResetQueryPool(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t firstQuery, uint32_t queryCount, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (queryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(queryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(queryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstQuery;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = queryCount;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdResetQueryPool(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t* firstQuery, uint32_t* queryCount, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (queryPool) {
            vt_unserialize_VkQueryPool(queryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(queryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
    if (firstQuery) *firstQuery = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (queryCount) *queryCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdWriteTimestamp(VkCommandBuffer commandBuffer, VkPipelineStageFlagBits pipelineStage, VkQueryPool queryPool, uint32_t query) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // pipelineStage
    bufferSize++;
    bufferSize += queryPool ? vt_sizeof_VkQueryPool(queryPool) : 0;
    bufferSize += 4; // query
    return bufferSize;
}

static inline void vt_serialize_vkCmdWriteTimestamp(VkCommandBuffer commandBuffer, VkPipelineStageFlagBits pipelineStage, VkQueryPool queryPool, uint32_t query, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkPipelineStageFlagBits*)(outputBuffer + bufferOffset) = pipelineStage;
    bufferOffset += 4;

    if (queryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(queryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(queryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = query;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdWriteTimestamp(VkCommandBuffer commandBuffer, VkPipelineStageFlagBits* pipelineStage, VkQueryPool queryPool, uint32_t* query, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (pipelineStage) *pipelineStage = *(VkPipelineStageFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    if (inputBuffer[bufferOffset++]) {
        if (queryPool) {
            vt_unserialize_VkQueryPool(queryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(queryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
    if (query) *query = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdCopyQueryPoolResults(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t firstQuery, uint32_t queryCount, VkBuffer dstBuffer, VkDeviceSize dstOffset, VkDeviceSize stride, VkQueryResultFlags flags) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += queryPool ? vt_sizeof_VkQueryPool(queryPool) : 0;
    bufferSize += 4; // firstQuery
    bufferSize += 4; // queryCount
    bufferSize++;
    bufferSize += dstBuffer ? vt_sizeof_VkBuffer(dstBuffer) : 0;
    bufferSize += 8; // dstOffset
    bufferSize += 8; // stride
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_vkCmdCopyQueryPoolResults(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t firstQuery, uint32_t queryCount, VkBuffer dstBuffer, VkDeviceSize dstOffset, VkDeviceSize stride, VkQueryResultFlags flags, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (queryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(queryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(queryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstQuery;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = queryCount;
    bufferOffset += 4;

    if (dstBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(dstBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(dstBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = dstOffset;
    bufferOffset += 8;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = stride;
    bufferOffset += 8;
    *(VkQueryResultFlags*)(outputBuffer + bufferOffset) = flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdCopyQueryPoolResults(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t* firstQuery, uint32_t* queryCount, VkBuffer dstBuffer, VkDeviceSize* dstOffset, VkDeviceSize* stride, VkQueryResultFlags* flags, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (queryPool) {
            vt_unserialize_VkQueryPool(queryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(queryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
    if (firstQuery) *firstQuery = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (queryCount) *queryCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    if (inputBuffer[bufferOffset++]) {
        if (dstBuffer) {
            vt_unserialize_VkBuffer(dstBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(dstBuffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (dstOffset) *dstOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (stride) *stride = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (flags) *flags = *(VkQueryResultFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdPushConstants(VkCommandBuffer commandBuffer, VkPipelineLayout layout, VkShaderStageFlags stageFlags, uint32_t offset, uint32_t size, const void* pValues) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += layout ? vt_sizeof_VkPipelineLayout(layout) : 0;
    bufferSize += 4; // stageFlags
    bufferSize += 4; // offset
    bufferSize += 4; // size
    bufferSize += 4;
    bufferSize += pValues ? size : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdPushConstants(VkCommandBuffer commandBuffer, VkPipelineLayout layout, VkShaderStageFlags stageFlags, uint32_t offset, uint32_t size, const void* pValues, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (layout) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipelineLayout(layout, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipelineLayout(layout);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkShaderStageFlags*)(outputBuffer + bufferOffset) = stageFlags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = offset;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = size;
    bufferOffset += 4;

    if (pValues) {
        *(int*)(outputBuffer + bufferOffset) = size;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pValues, size);
        bufferOffset += size;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdPushConstants(VkCommandBuffer commandBuffer, VkPipelineLayout layout, VkShaderStageFlags* stageFlags, uint32_t* offset, uint32_t* size, const void* pValues, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (layout) {
            vt_unserialize_VkPipelineLayout(layout, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipelineLayout(layout);
        }
        else bufferOffset += vt_sizeof_VkPipelineLayout(VK_NULL_HANDLE);
    }
    if (stageFlags) *stageFlags = *(VkShaderStageFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (offset) *offset = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (size) *size = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        if (pValues) memcpy(pValues, inputBuffer + bufferOffset, dataSize6);
        bufferOffset += dataSize6;
    }
    else pValues = NULL;
}

static inline int vt_sizeof_vkCmdBeginRenderPass(VkCommandBuffer commandBuffer, const VkRenderPassBeginInfo* pRenderPassBegin, VkSubpassContents contents) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pRenderPassBegin ? vt_sizeof_VkRenderPassBeginInfo(pRenderPassBegin) : 0;
    bufferSize += 4; // contents
    return bufferSize;
}

static inline void vt_serialize_vkCmdBeginRenderPass(VkCommandBuffer commandBuffer, const VkRenderPassBeginInfo* pRenderPassBegin, VkSubpassContents contents, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pRenderPassBegin) {
        int itemSize = vt_sizeof_VkRenderPassBeginInfo(pRenderPassBegin);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkRenderPassBeginInfo(pRenderPassBegin, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(VkSubpassContents*)(outputBuffer + bufferOffset) = contents;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdBeginRenderPass(VkCommandBuffer commandBuffer, const VkRenderPassBeginInfo* pRenderPassBegin, VkSubpassContents* contents, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pRenderPassBegin) vt_unserialize_VkRenderPassBeginInfo(pRenderPassBegin, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pRenderPassBegin) pRenderPassBegin = NULL;
    if (contents) *contents = *(VkSubpassContents*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdNextSubpass(VkCommandBuffer commandBuffer, VkSubpassContents contents) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // contents
    return bufferSize;
}

static inline void vt_serialize_vkCmdNextSubpass(VkCommandBuffer commandBuffer, VkSubpassContents contents, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkSubpassContents*)(outputBuffer + bufferOffset) = contents;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdNextSubpass(VkCommandBuffer commandBuffer, VkSubpassContents* contents, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (contents) *contents = *(VkSubpassContents*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdEndRenderPass(VkCommandBuffer commandBuffer) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdEndRenderPass(VkCommandBuffer commandBuffer, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCmdEndRenderPass(VkCommandBuffer commandBuffer, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCmdExecuteCommands(VkCommandBuffer commandBuffer, uint32_t commandBufferCount, const VkCommandBuffer* pCommandBuffers) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // commandBufferCount
    bufferSize += 4;
    if (pCommandBuffers) {
        for (int i = 0; i < commandBufferCount; i++) {
            bufferSize += vt_sizeof_VkCommandBuffer(pCommandBuffers[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdExecuteCommands(VkCommandBuffer commandBuffer, uint32_t commandBufferCount, const VkCommandBuffer* pCommandBuffers, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = commandBufferCount;
    bufferOffset += 4;

    if (pCommandBuffers) {
        *(int*)(outputBuffer + bufferOffset) = commandBufferCount;
        bufferOffset += 4;
        for (int i = 0; i < commandBufferCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkCommandBuffer(pCommandBuffers[i], outputBuffer + bufferOffset);
#else
            VkObject* commandBuffersObject = VkObject_fromHandle(pCommandBuffers[i]);
            vt_serialize_VkCommandBuffer((VkCommandBuffer)&commandBuffersObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkCommandBuffer(pCommandBuffers[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdExecuteCommands(VkCommandBuffer commandBuffer, uint32_t* commandBufferCount, const VkCommandBuffer* pCommandBuffers, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (commandBufferCount) *commandBufferCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkCommandBuffer* arrValues = pCommandBuffers;
        for (int i = 0; i < dataSize3; i++) {
            uint64_t commandBuffersId;
            vt_unserialize_VkCommandBuffer((VkCommandBuffer)&commandBuffersId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(commandBuffersId);
#else
                VkObject* commandBuffersObject = VkObject_create(VK_OBJECT_TYPE_COMMAND_BUFFER, commandBuffersId);
                arrValues[i] = VkObject_toHandle(commandBuffersObject);
#endif
                bufferOffset += vt_sizeof_VkCommandBuffer(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
        }
    }
    else pCommandBuffers = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, VkSurfaceCapabilitiesKHR* pSurfaceCapabilities) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize++;
    bufferSize += surface ? vt_sizeof_VkSurfaceKHR(surface) : 0;
    bufferSize += 4;
    bufferSize += pSurfaceCapabilities ? vt_sizeof_VkSurfaceCapabilitiesKHR(pSurfaceCapabilities) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, VkSurfaceCapabilitiesKHR* pSurfaceCapabilities, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (surface) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSurfaceKHR(surface, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSurfaceKHR(surface);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pSurfaceCapabilities) {
        int itemSize = vt_sizeof_VkSurfaceCapabilitiesKHR(pSurfaceCapabilities);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSurfaceCapabilitiesKHR(pSurfaceCapabilities, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceSurfaceCapabilitiesKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, VkSurfaceCapabilitiesKHR* pSurfaceCapabilities, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (surface) {
            vt_unserialize_VkSurfaceKHR(surface, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSurfaceKHR(surface);
        }
        else bufferOffset += vt_sizeof_VkSurfaceKHR(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pSurfaceCapabilities) vt_unserialize_VkSurfaceCapabilitiesKHR(pSurfaceCapabilities, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pSurfaceCapabilities) pSurfaceCapabilities = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceSurfaceFormatsKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, uint32_t* pSurfaceFormatCount, VkSurfaceFormatKHR* pSurfaceFormats) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize++;
    bufferSize += surface ? vt_sizeof_VkSurfaceKHR(surface) : 0;
    bufferSize += 4;
    bufferSize += pSurfaceFormatCount ? 4 : 0;
    bufferSize += 4;
    if (pSurfaceFormats) {
        int arrSize = pSurfaceFormatCount ? *pSurfaceFormatCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSurfaceFormatKHR(&pSurfaceFormats[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceSurfaceFormatsKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, uint32_t* pSurfaceFormatCount, VkSurfaceFormatKHR* pSurfaceFormats, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (surface) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSurfaceKHR(surface, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSurfaceKHR(surface);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pSurfaceFormatCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pSurfaceFormatCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSurfaceFormats) {
        int arrSize = pSurfaceFormatCount ? *pSurfaceFormatCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSurfaceFormatKHR(&pSurfaceFormats[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSurfaceFormatKHR(&pSurfaceFormats[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceSurfaceFormatsKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, uint32_t* pSurfaceFormatCount, VkSurfaceFormatKHR* pSurfaceFormats, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (surface) {
            vt_unserialize_VkSurfaceKHR(surface, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSurfaceKHR(surface);
        }
        else bufferOffset += vt_sizeof_VkSurfaceKHR(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pSurfaceFormatCount) *pSurfaceFormatCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pSurfaceFormatCount) pSurfaceFormatCount = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkSurfaceFormatKHR* arrValues = pSurfaceFormats;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSurfaceFormatKHR(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pSurfaceFormats = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceSurfacePresentModesKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, uint32_t* pPresentModeCount, VkPresentModeKHR* pPresentModes) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize++;
    bufferSize += surface ? vt_sizeof_VkSurfaceKHR(surface) : 0;
    bufferSize += 4;
    bufferSize += pPresentModeCount ? 4 : 0;
    bufferSize += 4;
    bufferSize += pPresentModes ? *pPresentModeCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceSurfacePresentModesKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, uint32_t* pPresentModeCount, VkPresentModeKHR* pPresentModes, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (surface) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSurfaceKHR(surface, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSurfaceKHR(surface);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pPresentModeCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pPresentModeCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pPresentModes) {
        int arrSize = pPresentModeCount ? *pPresentModeCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pPresentModes, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceSurfacePresentModesKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, uint32_t* pPresentModeCount, VkPresentModeKHR* pPresentModes, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (surface) {
            vt_unserialize_VkSurfaceKHR(surface, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSurfaceKHR(surface);
        }
        else bufferOffset += vt_sizeof_VkSurfaceKHR(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pPresentModeCount) *pPresentModeCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pPresentModeCount) pPresentModeCount = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkPresentModeKHR* arrValues = pPresentModes;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
    }
    else pPresentModes = NULL;
}

static inline int vt_sizeof_vkCreateSwapchainKHR(VkDevice device, const VkSwapchainCreateInfoKHR* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSwapchainKHR* pSwapchain) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkSwapchainCreateInfoKHR(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pSwapchain ? vt_sizeof_VkSwapchainKHR(*pSwapchain) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateSwapchainKHR(VkDevice device, const VkSwapchainCreateInfoKHR* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSwapchainKHR* pSwapchain, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkSwapchainCreateInfoKHR(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSwapchainCreateInfoKHR(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSwapchain) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSwapchainKHR(*pSwapchain, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSwapchainKHR(*pSwapchain);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateSwapchainKHR(VkDevice device, const VkSwapchainCreateInfoKHR* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSwapchainKHR* pSwapchain, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkSwapchainCreateInfoKHR(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pSwapchain) {
            vt_unserialize_VkSwapchainKHR(*pSwapchain, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSwapchainKHR(*pSwapchain);
        }
        else bufferOffset += vt_sizeof_VkSwapchainKHR(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroySwapchainKHR(VkDevice device, VkSwapchainKHR swapchain, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += swapchain ? vt_sizeof_VkSwapchainKHR(swapchain) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroySwapchainKHR(VkDevice device, VkSwapchainKHR swapchain, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (swapchain) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSwapchainKHR(swapchain, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSwapchainKHR(swapchain);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroySwapchainKHR(VkDevice device, VkSwapchainKHR swapchain, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (swapchain) {
            vt_unserialize_VkSwapchainKHR(swapchain, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSwapchainKHR(swapchain);
        }
        else bufferOffset += vt_sizeof_VkSwapchainKHR(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkGetSwapchainImagesKHR(VkDevice device, VkSwapchainKHR swapchain, uint32_t* pSwapchainImageCount, VkImage* pSwapchainImages) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += swapchain ? vt_sizeof_VkSwapchainKHR(swapchain) : 0;
    bufferSize += 4;
    bufferSize += pSwapchainImageCount ? 4 : 0;
    bufferSize += 4;
    if (pSwapchainImages) {
        for (int i = 0; i < *pSwapchainImageCount; i++) {
            bufferSize += vt_sizeof_VkImage(pSwapchainImages[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkGetSwapchainImagesKHR(VkDevice device, VkSwapchainKHR swapchain, uint32_t* pSwapchainImageCount, VkImage* pSwapchainImages, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (swapchain) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSwapchainKHR(swapchain, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSwapchainKHR(swapchain);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pSwapchainImageCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pSwapchainImageCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSwapchainImages) {
        *(int*)(outputBuffer + bufferOffset) = *pSwapchainImageCount;
        bufferOffset += 4;
        for (int i = 0; i < *pSwapchainImageCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkImage(pSwapchainImages[i], outputBuffer + bufferOffset);
#else
            VkObject* swapchainImagesObject = VkObject_fromHandle(pSwapchainImages[i]);
            vt_serialize_VkImage((VkImage)&swapchainImagesObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkImage(pSwapchainImages[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetSwapchainImagesKHR(VkDevice device, VkSwapchainKHR swapchain, uint32_t* pSwapchainImageCount, VkImage* pSwapchainImages, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (swapchain) {
            vt_unserialize_VkSwapchainKHR(swapchain, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSwapchainKHR(swapchain);
        }
        else bufferOffset += vt_sizeof_VkSwapchainKHR(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pSwapchainImageCount) *pSwapchainImageCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pSwapchainImageCount) pSwapchainImageCount = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkImage* arrValues = pSwapchainImages;
        for (int i = 0; i < dataSize4; i++) {
            uint64_t swapchainImagesId;
            vt_unserialize_VkImage((VkImage)&swapchainImagesId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(swapchainImagesId);
#else
                VkObject* swapchainImagesObject = VkObject_create(VK_OBJECT_TYPE_IMAGE, swapchainImagesId);
                arrValues[i] = VkObject_toHandle(swapchainImagesObject);
#endif
                bufferOffset += vt_sizeof_VkImage(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkImage(VK_NULL_HANDLE);
        }
    }
    else pSwapchainImages = NULL;
}

static inline int vt_sizeof_vkAcquireNextImageKHR(VkDevice device, VkSwapchainKHR swapchain, uint64_t timeout, VkSemaphore semaphore, VkFence fence, uint32_t* pImageIndex) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += swapchain ? vt_sizeof_VkSwapchainKHR(swapchain) : 0;
    bufferSize += 8; // timeout
    bufferSize++;
    bufferSize += semaphore ? vt_sizeof_VkSemaphore(semaphore) : 0;
    bufferSize++;
    bufferSize += fence ? vt_sizeof_VkFence(fence) : 0;
    bufferSize += 4;
    bufferSize += pImageIndex ? 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkAcquireNextImageKHR(VkDevice device, VkSwapchainKHR swapchain, uint64_t timeout, VkSemaphore semaphore, VkFence fence, uint32_t* pImageIndex, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (swapchain) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSwapchainKHR(swapchain, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSwapchainKHR(swapchain);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint64_t*)(outputBuffer + bufferOffset) = timeout;
    bufferOffset += 8;

    if (semaphore) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSemaphore(semaphore, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSemaphore(semaphore);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (fence) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkFence(fence, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkFence(fence);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pImageIndex) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pImageIndex;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkAcquireNextImageKHR(VkDevice device, VkSwapchainKHR swapchain, uint64_t* timeout, VkSemaphore semaphore, VkFence fence, uint32_t* pImageIndex, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (swapchain) {
            vt_unserialize_VkSwapchainKHR(swapchain, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSwapchainKHR(swapchain);
        }
        else bufferOffset += vt_sizeof_VkSwapchainKHR(VK_NULL_HANDLE);
    }
    if (timeout) *timeout = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;

    if (inputBuffer[bufferOffset++]) {
        if (semaphore) {
            vt_unserialize_VkSemaphore(semaphore, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSemaphore(semaphore);
        }
        else bufferOffset += vt_sizeof_VkSemaphore(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (fence) {
            vt_unserialize_VkFence(fence, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkFence(fence);
        }
        else bufferOffset += vt_sizeof_VkFence(VK_NULL_HANDLE);
    }

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        if (pImageIndex) *pImageIndex = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize6;
    }
    else if (pImageIndex) pImageIndex = NULL;
}

static inline int vt_sizeof_vkQueuePresentKHR(VkQueue queue, const VkPresentInfoKHR* pPresentInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += queue ? vt_sizeof_VkQueue(queue) : 0;
    bufferSize += 4;
    bufferSize += pPresentInfo ? vt_sizeof_VkPresentInfoKHR(pPresentInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkQueuePresentKHR(VkQueue queue, const VkPresentInfoKHR* pPresentInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (queue) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueue(queue, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueue(queue);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pPresentInfo) {
        int itemSize = vt_sizeof_VkPresentInfoKHR(pPresentInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPresentInfoKHR(pPresentInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkQueuePresentKHR(VkQueue queue, const VkPresentInfoKHR* pPresentInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (queue) {
            vt_unserialize_VkQueue(queue, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueue(queue);
        }
        else bufferOffset += vt_sizeof_VkQueue(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pPresentInfo) vt_unserialize_VkPresentInfoKHR(pPresentInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pPresentInfo) pPresentInfo = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceFeatures2(VkPhysicalDevice physicalDevice, VkPhysicalDeviceFeatures2* pFeatures) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pFeatures ? vt_sizeof_VkPhysicalDeviceFeatures2(pFeatures) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceFeatures2(VkPhysicalDevice physicalDevice, VkPhysicalDeviceFeatures2* pFeatures, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pFeatures) {
        int itemSize = vt_sizeof_VkPhysicalDeviceFeatures2(pFeatures);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceFeatures2(pFeatures, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceFeatures2(VkPhysicalDevice physicalDevice, VkPhysicalDeviceFeatures2* pFeatures, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pFeatures) vt_unserialize_VkPhysicalDeviceFeatures2(pFeatures, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pFeatures) pFeatures = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceProperties2(VkPhysicalDevice physicalDevice, VkPhysicalDeviceProperties2* pProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pProperties ? vt_sizeof_VkPhysicalDeviceProperties2(pProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceProperties2(VkPhysicalDevice physicalDevice, VkPhysicalDeviceProperties2* pProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pProperties) {
        int itemSize = vt_sizeof_VkPhysicalDeviceProperties2(pProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceProperties2(pProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceProperties2(VkPhysicalDevice physicalDevice, VkPhysicalDeviceProperties2* pProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pProperties) vt_unserialize_VkPhysicalDeviceProperties2(pProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pProperties) pProperties = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceFormatProperties2(VkPhysicalDevice physicalDevice, VkFormat format, VkFormatProperties2* pFormatProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4; // format
    bufferSize += 4;
    bufferSize += pFormatProperties ? vt_sizeof_VkFormatProperties2(pFormatProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceFormatProperties2(VkPhysicalDevice physicalDevice, VkFormat format, VkFormatProperties2* pFormatProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkFormat*)(outputBuffer + bufferOffset) = format;
    bufferOffset += 4;

    if (pFormatProperties) {
        int itemSize = vt_sizeof_VkFormatProperties2(pFormatProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkFormatProperties2(pFormatProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceFormatProperties2(VkPhysicalDevice physicalDevice, VkFormat* format, VkFormatProperties2* pFormatProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }
    if (format) *format = *(VkFormat*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pFormatProperties) vt_unserialize_VkFormatProperties2(pFormatProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pFormatProperties) pFormatProperties = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceImageFormatProperties2(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo, VkImageFormatProperties2* pImageFormatProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pImageFormatInfo ? vt_sizeof_VkPhysicalDeviceImageFormatInfo2(pImageFormatInfo) : 0;
    bufferSize += 4;
    bufferSize += pImageFormatProperties ? vt_sizeof_VkImageFormatProperties2(pImageFormatProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceImageFormatProperties2(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo, VkImageFormatProperties2* pImageFormatProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pImageFormatInfo) {
        int itemSize = vt_sizeof_VkPhysicalDeviceImageFormatInfo2(pImageFormatInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceImageFormatInfo2(pImageFormatInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pImageFormatProperties) {
        int itemSize = vt_sizeof_VkImageFormatProperties2(pImageFormatProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkImageFormatProperties2(pImageFormatProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceImageFormatProperties2(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceImageFormatInfo2* pImageFormatInfo, VkImageFormatProperties2* pImageFormatProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pImageFormatInfo) vt_unserialize_VkPhysicalDeviceImageFormatInfo2(pImageFormatInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pImageFormatInfo) pImageFormatInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pImageFormatProperties) vt_unserialize_VkImageFormatProperties2(pImageFormatProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pImageFormatProperties) pImageFormatProperties = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceQueueFamilyProperties2(VkPhysicalDevice physicalDevice, uint32_t* pQueueFamilyPropertyCount, VkQueueFamilyProperties2* pQueueFamilyProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pQueueFamilyPropertyCount ? 4 : 0;
    bufferSize += 4;
    if (pQueueFamilyProperties) {
        int arrSize = pQueueFamilyPropertyCount ? *pQueueFamilyPropertyCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkQueueFamilyProperties2(&pQueueFamilyProperties[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceQueueFamilyProperties2(VkPhysicalDevice physicalDevice, uint32_t* pQueueFamilyPropertyCount, VkQueueFamilyProperties2* pQueueFamilyProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pQueueFamilyPropertyCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pQueueFamilyPropertyCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pQueueFamilyProperties) {
        int arrSize = pQueueFamilyPropertyCount ? *pQueueFamilyPropertyCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkQueueFamilyProperties2(&pQueueFamilyProperties[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkQueueFamilyProperties2(&pQueueFamilyProperties[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceQueueFamilyProperties2(VkPhysicalDevice physicalDevice, uint32_t* pQueueFamilyPropertyCount, VkQueueFamilyProperties2* pQueueFamilyProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pQueueFamilyPropertyCount) *pQueueFamilyPropertyCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize2;
    }
    else if (pQueueFamilyPropertyCount) pQueueFamilyPropertyCount = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkQueueFamilyProperties2* arrValues = pQueueFamilyProperties;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkQueueFamilyProperties2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pQueueFamilyProperties = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceMemoryProperties2(VkPhysicalDevice physicalDevice, VkPhysicalDeviceMemoryProperties2* pMemoryProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pMemoryProperties ? vt_sizeof_VkPhysicalDeviceMemoryProperties2(pMemoryProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceMemoryProperties2(VkPhysicalDevice physicalDevice, VkPhysicalDeviceMemoryProperties2* pMemoryProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pMemoryProperties) {
        int itemSize = vt_sizeof_VkPhysicalDeviceMemoryProperties2(pMemoryProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceMemoryProperties2(pMemoryProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceMemoryProperties2(VkPhysicalDevice physicalDevice, VkPhysicalDeviceMemoryProperties2* pMemoryProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pMemoryProperties) vt_unserialize_VkPhysicalDeviceMemoryProperties2(pMemoryProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pMemoryProperties) pMemoryProperties = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceSparseImageFormatProperties2(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo, uint32_t* pPropertyCount, VkSparseImageFormatProperties2* pProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pFormatInfo ? vt_sizeof_VkPhysicalDeviceSparseImageFormatInfo2(pFormatInfo) : 0;
    bufferSize += 4;
    bufferSize += pPropertyCount ? 4 : 0;
    bufferSize += 4;
    if (pProperties) {
        int arrSize = pPropertyCount ? *pPropertyCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSparseImageFormatProperties2(&pProperties[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceSparseImageFormatProperties2(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo, uint32_t* pPropertyCount, VkSparseImageFormatProperties2* pProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pFormatInfo) {
        int itemSize = vt_sizeof_VkPhysicalDeviceSparseImageFormatInfo2(pFormatInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceSparseImageFormatInfo2(pFormatInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pPropertyCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pPropertyCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pProperties) {
        int arrSize = pPropertyCount ? *pPropertyCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSparseImageFormatProperties2(&pProperties[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSparseImageFormatProperties2(&pProperties[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceSparseImageFormatProperties2(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceSparseImageFormatInfo2* pFormatInfo, uint32_t* pPropertyCount, VkSparseImageFormatProperties2* pProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pFormatInfo) vt_unserialize_VkPhysicalDeviceSparseImageFormatInfo2(pFormatInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pFormatInfo) pFormatInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pPropertyCount) *pPropertyCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pPropertyCount) pPropertyCount = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkSparseImageFormatProperties2* arrValues = pProperties;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSparseImageFormatProperties2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pProperties = NULL;
}

static inline int vt_sizeof_vkCmdPushDescriptorSetKHR(VkCommandBuffer commandBuffer, VkPipelineBindPoint pipelineBindPoint, VkPipelineLayout layout, uint32_t set, uint32_t descriptorWriteCount, const VkWriteDescriptorSet* pDescriptorWrites) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // pipelineBindPoint
    bufferSize++;
    bufferSize += layout ? vt_sizeof_VkPipelineLayout(layout) : 0;
    bufferSize += 4; // set
    bufferSize += 4; // descriptorWriteCount
    bufferSize += 4;
    if (pDescriptorWrites) {
        int arrSize = descriptorWriteCount ? descriptorWriteCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkWriteDescriptorSet(&pDescriptorWrites[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdPushDescriptorSetKHR(VkCommandBuffer commandBuffer, VkPipelineBindPoint pipelineBindPoint, VkPipelineLayout layout, uint32_t set, uint32_t descriptorWriteCount, const VkWriteDescriptorSet* pDescriptorWrites, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkPipelineBindPoint*)(outputBuffer + bufferOffset) = pipelineBindPoint;
    bufferOffset += 4;

    if (layout) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPipelineLayout(layout, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPipelineLayout(layout);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = set;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = descriptorWriteCount;
    bufferOffset += 4;

    if (pDescriptorWrites) {
        int arrSize = descriptorWriteCount ? descriptorWriteCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkWriteDescriptorSet(&pDescriptorWrites[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkWriteDescriptorSet(&pDescriptorWrites[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdPushDescriptorSetKHR(VkCommandBuffer commandBuffer, VkPipelineBindPoint* pipelineBindPoint, VkPipelineLayout layout, uint32_t* set, uint32_t* descriptorWriteCount, const VkWriteDescriptorSet* pDescriptorWrites, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (pipelineBindPoint) *pipelineBindPoint = *(VkPipelineBindPoint*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    if (inputBuffer[bufferOffset++]) {
        if (layout) {
            vt_unserialize_VkPipelineLayout(layout, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPipelineLayout(layout);
        }
        else bufferOffset += vt_sizeof_VkPipelineLayout(VK_NULL_HANDLE);
    }
    if (set) *set = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (descriptorWriteCount) *descriptorWriteCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkWriteDescriptorSet* arrValues = pDescriptorWrites;
        for (int i = 0; i < dataSize6; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkWriteDescriptorSet(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pDescriptorWrites = NULL;
}

static inline int vt_sizeof_vkTrimCommandPool(VkDevice device, VkCommandPool commandPool, VkCommandPoolTrimFlags flags) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += commandPool ? vt_sizeof_VkCommandPool(commandPool) : 0;
    bufferSize += 4; // flags
    return bufferSize;
}

static inline void vt_serialize_vkTrimCommandPool(VkDevice device, VkCommandPool commandPool, VkCommandPoolTrimFlags flags, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (commandPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandPool(commandPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandPool(commandPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkCommandPoolTrimFlags*)(outputBuffer + bufferOffset) = flags;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkTrimCommandPool(VkDevice device, VkCommandPool commandPool, VkCommandPoolTrimFlags* flags, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (commandPool) {
            vt_unserialize_VkCommandPool(commandPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandPool(commandPool);
        }
        else bufferOffset += vt_sizeof_VkCommandPool(VK_NULL_HANDLE);
    }
    if (flags) *flags = *(VkCommandPoolTrimFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkGetPhysicalDeviceExternalBufferProperties(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo, VkExternalBufferProperties* pExternalBufferProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pExternalBufferInfo ? vt_sizeof_VkPhysicalDeviceExternalBufferInfo(pExternalBufferInfo) : 0;
    bufferSize += 4;
    bufferSize += pExternalBufferProperties ? vt_sizeof_VkExternalBufferProperties(pExternalBufferProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceExternalBufferProperties(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo, VkExternalBufferProperties* pExternalBufferProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pExternalBufferInfo) {
        int itemSize = vt_sizeof_VkPhysicalDeviceExternalBufferInfo(pExternalBufferInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceExternalBufferInfo(pExternalBufferInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pExternalBufferProperties) {
        int itemSize = vt_sizeof_VkExternalBufferProperties(pExternalBufferProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkExternalBufferProperties(pExternalBufferProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceExternalBufferProperties(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceExternalBufferInfo* pExternalBufferInfo, VkExternalBufferProperties* pExternalBufferProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pExternalBufferInfo) vt_unserialize_VkPhysicalDeviceExternalBufferInfo(pExternalBufferInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pExternalBufferInfo) pExternalBufferInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pExternalBufferProperties) vt_unserialize_VkExternalBufferProperties(pExternalBufferProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pExternalBufferProperties) pExternalBufferProperties = NULL;
}

static inline int vt_sizeof_vkGetMemoryFdKHR(VkDevice device, const VkMemoryGetFdInfoKHR* pGetFdInfo, int* pFd) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pGetFdInfo ? vt_sizeof_VkMemoryGetFdInfoKHR(pGetFdInfo) : 0;
    bufferSize += 4;
    bufferSize += pFd ? 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetMemoryFdKHR(VkDevice device, const VkMemoryGetFdInfoKHR* pGetFdInfo, int* pFd, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pGetFdInfo) {
        int itemSize = vt_sizeof_VkMemoryGetFdInfoKHR(pGetFdInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkMemoryGetFdInfoKHR(pGetFdInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pFd) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(int*)(outputBuffer + bufferOffset) = *pFd;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetMemoryFdKHR(VkDevice device, const VkMemoryGetFdInfoKHR* pGetFdInfo, int* pFd, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pGetFdInfo) vt_unserialize_VkMemoryGetFdInfoKHR(pGetFdInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pGetFdInfo) pGetFdInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pFd) *pFd = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pFd) pFd = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceExternalSemaphoreProperties(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo, VkExternalSemaphoreProperties* pExternalSemaphoreProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pExternalSemaphoreInfo ? vt_sizeof_VkPhysicalDeviceExternalSemaphoreInfo(pExternalSemaphoreInfo) : 0;
    bufferSize += 4;
    bufferSize += pExternalSemaphoreProperties ? vt_sizeof_VkExternalSemaphoreProperties(pExternalSemaphoreProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceExternalSemaphoreProperties(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo, VkExternalSemaphoreProperties* pExternalSemaphoreProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pExternalSemaphoreInfo) {
        int itemSize = vt_sizeof_VkPhysicalDeviceExternalSemaphoreInfo(pExternalSemaphoreInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceExternalSemaphoreInfo(pExternalSemaphoreInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pExternalSemaphoreProperties) {
        int itemSize = vt_sizeof_VkExternalSemaphoreProperties(pExternalSemaphoreProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkExternalSemaphoreProperties(pExternalSemaphoreProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceExternalSemaphoreProperties(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceExternalSemaphoreInfo* pExternalSemaphoreInfo, VkExternalSemaphoreProperties* pExternalSemaphoreProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pExternalSemaphoreInfo) vt_unserialize_VkPhysicalDeviceExternalSemaphoreInfo(pExternalSemaphoreInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pExternalSemaphoreInfo) pExternalSemaphoreInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pExternalSemaphoreProperties) vt_unserialize_VkExternalSemaphoreProperties(pExternalSemaphoreProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pExternalSemaphoreProperties) pExternalSemaphoreProperties = NULL;
}

static inline int vt_sizeof_vkGetSemaphoreFdKHR(VkDevice device, const VkSemaphoreGetFdInfoKHR* pGetFdInfo, int* pFd) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pGetFdInfo ? vt_sizeof_VkSemaphoreGetFdInfoKHR(pGetFdInfo) : 0;
    bufferSize += 4;
    bufferSize += pFd ? 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetSemaphoreFdKHR(VkDevice device, const VkSemaphoreGetFdInfoKHR* pGetFdInfo, int* pFd, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pGetFdInfo) {
        int itemSize = vt_sizeof_VkSemaphoreGetFdInfoKHR(pGetFdInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSemaphoreGetFdInfoKHR(pGetFdInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pFd) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(int*)(outputBuffer + bufferOffset) = *pFd;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetSemaphoreFdKHR(VkDevice device, const VkSemaphoreGetFdInfoKHR* pGetFdInfo, int* pFd, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pGetFdInfo) vt_unserialize_VkSemaphoreGetFdInfoKHR(pGetFdInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pGetFdInfo) pGetFdInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pFd) *pFd = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pFd) pFd = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceExternalFenceProperties(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo, VkExternalFenceProperties* pExternalFenceProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pExternalFenceInfo ? vt_sizeof_VkPhysicalDeviceExternalFenceInfo(pExternalFenceInfo) : 0;
    bufferSize += 4;
    bufferSize += pExternalFenceProperties ? vt_sizeof_VkExternalFenceProperties(pExternalFenceProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceExternalFenceProperties(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo, VkExternalFenceProperties* pExternalFenceProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pExternalFenceInfo) {
        int itemSize = vt_sizeof_VkPhysicalDeviceExternalFenceInfo(pExternalFenceInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkPhysicalDeviceExternalFenceInfo(pExternalFenceInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pExternalFenceProperties) {
        int itemSize = vt_sizeof_VkExternalFenceProperties(pExternalFenceProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkExternalFenceProperties(pExternalFenceProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceExternalFenceProperties(VkPhysicalDevice physicalDevice, const VkPhysicalDeviceExternalFenceInfo* pExternalFenceInfo, VkExternalFenceProperties* pExternalFenceProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pExternalFenceInfo) vt_unserialize_VkPhysicalDeviceExternalFenceInfo(pExternalFenceInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pExternalFenceInfo) pExternalFenceInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pExternalFenceProperties) vt_unserialize_VkExternalFenceProperties(pExternalFenceProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pExternalFenceProperties) pExternalFenceProperties = NULL;
}

static inline int vt_sizeof_vkGetFenceFdKHR(VkDevice device, const VkFenceGetFdInfoKHR* pGetFdInfo, int* pFd) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pGetFdInfo ? vt_sizeof_VkFenceGetFdInfoKHR(pGetFdInfo) : 0;
    bufferSize += 4;
    bufferSize += pFd ? 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetFenceFdKHR(VkDevice device, const VkFenceGetFdInfoKHR* pGetFdInfo, int* pFd, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pGetFdInfo) {
        int itemSize = vt_sizeof_VkFenceGetFdInfoKHR(pGetFdInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkFenceGetFdInfoKHR(pGetFdInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pFd) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(int*)(outputBuffer + bufferOffset) = *pFd;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetFenceFdKHR(VkDevice device, const VkFenceGetFdInfoKHR* pGetFdInfo, int* pFd, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pGetFdInfo) vt_unserialize_VkFenceGetFdInfoKHR(pGetFdInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pGetFdInfo) pGetFdInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pFd) *pFd = *(int*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pFd) pFd = NULL;
}

static inline int vt_sizeof_vkEnumeratePhysicalDeviceGroups(VkInstance instance, uint32_t* pPhysicalDeviceGroupCount, VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += instance ? vt_sizeof_VkInstance(instance) : 0;
    bufferSize += 4;
    bufferSize += pPhysicalDeviceGroupCount ? 4 : 0;
    bufferSize += 4;
    if (pPhysicalDeviceGroupProperties) {
        int arrSize = pPhysicalDeviceGroupCount ? *pPhysicalDeviceGroupCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkPhysicalDeviceGroupProperties(&pPhysicalDeviceGroupProperties[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkEnumeratePhysicalDeviceGroups(VkInstance instance, uint32_t* pPhysicalDeviceGroupCount, VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (instance) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkInstance(instance, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkInstance(instance);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pPhysicalDeviceGroupCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pPhysicalDeviceGroupCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pPhysicalDeviceGroupProperties) {
        int arrSize = pPhysicalDeviceGroupCount ? *pPhysicalDeviceGroupCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkPhysicalDeviceGroupProperties(&pPhysicalDeviceGroupProperties[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkPhysicalDeviceGroupProperties(&pPhysicalDeviceGroupProperties[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkEnumeratePhysicalDeviceGroups(VkInstance instance, uint32_t* pPhysicalDeviceGroupCount, VkPhysicalDeviceGroupProperties* pPhysicalDeviceGroupProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (instance) {
            vt_unserialize_VkInstance(instance, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkInstance(instance);
        }
        else bufferOffset += vt_sizeof_VkInstance(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pPhysicalDeviceGroupCount) *pPhysicalDeviceGroupCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize2;
    }
    else if (pPhysicalDeviceGroupCount) pPhysicalDeviceGroupCount = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkPhysicalDeviceGroupProperties* arrValues = pPhysicalDeviceGroupProperties;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkPhysicalDeviceGroupProperties(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pPhysicalDeviceGroupProperties = NULL;
}

static inline int vt_sizeof_vkGetDeviceGroupPeerMemoryFeatures(VkDevice device, uint32_t heapIndex, uint32_t localDeviceIndex, uint32_t remoteDeviceIndex, VkPeerMemoryFeatureFlags* pPeerMemoryFeatures) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4; // heapIndex
    bufferSize += 4; // localDeviceIndex
    bufferSize += 4; // remoteDeviceIndex
    bufferSize += 4;
    bufferSize += pPeerMemoryFeatures ? 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetDeviceGroupPeerMemoryFeatures(VkDevice device, uint32_t heapIndex, uint32_t localDeviceIndex, uint32_t remoteDeviceIndex, VkPeerMemoryFeatureFlags* pPeerMemoryFeatures, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = heapIndex;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = localDeviceIndex;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = remoteDeviceIndex;
    bufferOffset += 4;

    if (pPeerMemoryFeatures) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(VkPeerMemoryFeatureFlags*)(outputBuffer + bufferOffset) = *pPeerMemoryFeatures;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetDeviceGroupPeerMemoryFeatures(VkDevice device, uint32_t* heapIndex, uint32_t* localDeviceIndex, uint32_t* remoteDeviceIndex, VkPeerMemoryFeatureFlags* pPeerMemoryFeatures, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
    if (heapIndex) *heapIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (localDeviceIndex) *localDeviceIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (remoteDeviceIndex) *remoteDeviceIndex = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        if (pPeerMemoryFeatures) *pPeerMemoryFeatures = *(VkPeerMemoryFeatureFlags*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize5;
    }
    else if (pPeerMemoryFeatures) pPeerMemoryFeatures = NULL;
}

static inline int vt_sizeof_vkBindBufferMemory2(VkDevice device, uint32_t bindInfoCount, const VkBindBufferMemoryInfo* pBindInfos) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4; // bindInfoCount
    bufferSize += 4;
    if (pBindInfos) {
        int arrSize = bindInfoCount ? bindInfoCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBindBufferMemoryInfo(&pBindInfos[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkBindBufferMemory2(VkDevice device, uint32_t bindInfoCount, const VkBindBufferMemoryInfo* pBindInfos, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = bindInfoCount;
    bufferOffset += 4;

    if (pBindInfos) {
        int arrSize = bindInfoCount ? bindInfoCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBindBufferMemoryInfo(&pBindInfos[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBindBufferMemoryInfo(&pBindInfos[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkBindBufferMemory2(VkDevice device, uint32_t* bindInfoCount, const VkBindBufferMemoryInfo* pBindInfos, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
    if (bindInfoCount) *bindInfoCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkBindBufferMemoryInfo* arrValues = pBindInfos;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBindBufferMemoryInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pBindInfos = NULL;
}

static inline int vt_sizeof_vkBindImageMemory2(VkDevice device, uint32_t bindInfoCount, const VkBindImageMemoryInfo* pBindInfos) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4; // bindInfoCount
    bufferSize += 4;
    if (pBindInfos) {
        int arrSize = bindInfoCount ? bindInfoCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkBindImageMemoryInfo(&pBindInfos[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkBindImageMemory2(VkDevice device, uint32_t bindInfoCount, const VkBindImageMemoryInfo* pBindInfos, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = bindInfoCount;
    bufferOffset += 4;

    if (pBindInfos) {
        int arrSize = bindInfoCount ? bindInfoCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkBindImageMemoryInfo(&pBindInfos[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkBindImageMemoryInfo(&pBindInfos[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkBindImageMemory2(VkDevice device, uint32_t* bindInfoCount, const VkBindImageMemoryInfo* pBindInfos, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
    if (bindInfoCount) *bindInfoCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkBindImageMemoryInfo* arrValues = pBindInfos;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkBindImageMemoryInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pBindInfos = NULL;
}

static inline int vt_sizeof_vkCmdSetDeviceMask(VkCommandBuffer commandBuffer, uint32_t deviceMask) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // deviceMask
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetDeviceMask(VkCommandBuffer commandBuffer, uint32_t deviceMask, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = deviceMask;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetDeviceMask(VkCommandBuffer commandBuffer, uint32_t* deviceMask, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (deviceMask) *deviceMask = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkAcquireNextImage2KHR(VkDevice device, const VkAcquireNextImageInfoKHR* pAcquireInfo, uint32_t* pImageIndex) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pAcquireInfo ? vt_sizeof_VkAcquireNextImageInfoKHR(pAcquireInfo) : 0;
    bufferSize += 4;
    bufferSize += pImageIndex ? 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkAcquireNextImage2KHR(VkDevice device, const VkAcquireNextImageInfoKHR* pAcquireInfo, uint32_t* pImageIndex, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pAcquireInfo) {
        int itemSize = vt_sizeof_VkAcquireNextImageInfoKHR(pAcquireInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkAcquireNextImageInfoKHR(pAcquireInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pImageIndex) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pImageIndex;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkAcquireNextImage2KHR(VkDevice device, const VkAcquireNextImageInfoKHR* pAcquireInfo, uint32_t* pImageIndex, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pAcquireInfo) vt_unserialize_VkAcquireNextImageInfoKHR(pAcquireInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pAcquireInfo) pAcquireInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pImageIndex) *pImageIndex = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pImageIndex) pImageIndex = NULL;
}

static inline int vt_sizeof_vkCmdDispatchBase(VkCommandBuffer commandBuffer, uint32_t baseGroupX, uint32_t baseGroupY, uint32_t baseGroupZ, uint32_t groupCountX, uint32_t groupCountY, uint32_t groupCountZ) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // baseGroupX
    bufferSize += 4; // baseGroupY
    bufferSize += 4; // baseGroupZ
    bufferSize += 4; // groupCountX
    bufferSize += 4; // groupCountY
    bufferSize += 4; // groupCountZ
    return bufferSize;
}

static inline void vt_serialize_vkCmdDispatchBase(VkCommandBuffer commandBuffer, uint32_t baseGroupX, uint32_t baseGroupY, uint32_t baseGroupZ, uint32_t groupCountX, uint32_t groupCountY, uint32_t groupCountZ, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = baseGroupX;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = baseGroupY;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = baseGroupZ;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = groupCountX;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = groupCountY;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = groupCountZ;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdDispatchBase(VkCommandBuffer commandBuffer, uint32_t* baseGroupX, uint32_t* baseGroupY, uint32_t* baseGroupZ, uint32_t* groupCountX, uint32_t* groupCountY, uint32_t* groupCountZ, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (baseGroupX) *baseGroupX = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (baseGroupY) *baseGroupY = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (baseGroupZ) *baseGroupZ = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (groupCountX) *groupCountX = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (groupCountY) *groupCountY = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (groupCountZ) *groupCountZ = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkGetPhysicalDevicePresentRectanglesKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, uint32_t* pRectCount, VkRect2D* pRects) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize++;
    bufferSize += surface ? vt_sizeof_VkSurfaceKHR(surface) : 0;
    bufferSize += 4;
    bufferSize += pRectCount ? 4 : 0;
    bufferSize += 4;
    if (pRects) {
        int arrSize = pRectCount ? *pRectCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkRect2D(&pRects[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDevicePresentRectanglesKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, uint32_t* pRectCount, VkRect2D* pRects, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (surface) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSurfaceKHR(surface, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSurfaceKHR(surface);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pRectCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pRectCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pRects) {
        int arrSize = pRectCount ? *pRectCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkRect2D(&pRects[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkRect2D(&pRects[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDevicePresentRectanglesKHR(VkPhysicalDevice physicalDevice, VkSurfaceKHR surface, uint32_t* pRectCount, VkRect2D* pRects, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (surface) {
            vt_unserialize_VkSurfaceKHR(surface, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSurfaceKHR(surface);
        }
        else bufferOffset += vt_sizeof_VkSurfaceKHR(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pRectCount) *pRectCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pRectCount) pRectCount = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkRect2D* arrValues = pRects;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkRect2D(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pRects = NULL;
}

static inline int vt_sizeof_vkCmdSetSampleLocationsEXT(VkCommandBuffer commandBuffer, const VkSampleLocationsInfoEXT* pSampleLocationsInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pSampleLocationsInfo ? vt_sizeof_VkSampleLocationsInfoEXT(pSampleLocationsInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetSampleLocationsEXT(VkCommandBuffer commandBuffer, const VkSampleLocationsInfoEXT* pSampleLocationsInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pSampleLocationsInfo) {
        int itemSize = vt_sizeof_VkSampleLocationsInfoEXT(pSampleLocationsInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSampleLocationsInfoEXT(pSampleLocationsInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdSetSampleLocationsEXT(VkCommandBuffer commandBuffer, const VkSampleLocationsInfoEXT* pSampleLocationsInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pSampleLocationsInfo) vt_unserialize_VkSampleLocationsInfoEXT(pSampleLocationsInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pSampleLocationsInfo) pSampleLocationsInfo = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceMultisamplePropertiesEXT(VkPhysicalDevice physicalDevice, VkSampleCountFlagBits samples, VkMultisamplePropertiesEXT* pMultisampleProperties) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4; // samples
    bufferSize += 4;
    bufferSize += pMultisampleProperties ? vt_sizeof_VkMultisamplePropertiesEXT(pMultisampleProperties) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceMultisamplePropertiesEXT(VkPhysicalDevice physicalDevice, VkSampleCountFlagBits samples, VkMultisamplePropertiesEXT* pMultisampleProperties, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkSampleCountFlagBits*)(outputBuffer + bufferOffset) = samples;
    bufferOffset += 4;

    if (pMultisampleProperties) {
        int itemSize = vt_sizeof_VkMultisamplePropertiesEXT(pMultisampleProperties);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkMultisamplePropertiesEXT(pMultisampleProperties, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceMultisamplePropertiesEXT(VkPhysicalDevice physicalDevice, VkSampleCountFlagBits* samples, VkMultisamplePropertiesEXT* pMultisampleProperties, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }
    if (samples) *samples = *(VkSampleCountFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pMultisampleProperties) vt_unserialize_VkMultisamplePropertiesEXT(pMultisampleProperties, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pMultisampleProperties) pMultisampleProperties = NULL;
}

static inline int vt_sizeof_vkGetBufferMemoryRequirements2(VkDevice device, const VkBufferMemoryRequirementsInfo2* pInfo, VkMemoryRequirements2* pMemoryRequirements) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pInfo ? vt_sizeof_VkBufferMemoryRequirementsInfo2(pInfo) : 0;
    bufferSize += 4;
    bufferSize += pMemoryRequirements ? vt_sizeof_VkMemoryRequirements2(pMemoryRequirements) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetBufferMemoryRequirements2(VkDevice device, const VkBufferMemoryRequirementsInfo2* pInfo, VkMemoryRequirements2* pMemoryRequirements, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pInfo) {
        int itemSize = vt_sizeof_VkBufferMemoryRequirementsInfo2(pInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkBufferMemoryRequirementsInfo2(pInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pMemoryRequirements) {
        int itemSize = vt_sizeof_VkMemoryRequirements2(pMemoryRequirements);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkMemoryRequirements2(pMemoryRequirements, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetBufferMemoryRequirements2(VkDevice device, const VkBufferMemoryRequirementsInfo2* pInfo, VkMemoryRequirements2* pMemoryRequirements, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pInfo) vt_unserialize_VkBufferMemoryRequirementsInfo2(pInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pInfo) pInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pMemoryRequirements) vt_unserialize_VkMemoryRequirements2(pMemoryRequirements, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pMemoryRequirements) pMemoryRequirements = NULL;
}

static inline int vt_sizeof_vkGetImageMemoryRequirements2(VkDevice device, const VkImageMemoryRequirementsInfo2* pInfo, VkMemoryRequirements2* pMemoryRequirements) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pInfo ? vt_sizeof_VkImageMemoryRequirementsInfo2(pInfo) : 0;
    bufferSize += 4;
    bufferSize += pMemoryRequirements ? vt_sizeof_VkMemoryRequirements2(pMemoryRequirements) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetImageMemoryRequirements2(VkDevice device, const VkImageMemoryRequirementsInfo2* pInfo, VkMemoryRequirements2* pMemoryRequirements, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pInfo) {
        int itemSize = vt_sizeof_VkImageMemoryRequirementsInfo2(pInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkImageMemoryRequirementsInfo2(pInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pMemoryRequirements) {
        int itemSize = vt_sizeof_VkMemoryRequirements2(pMemoryRequirements);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkMemoryRequirements2(pMemoryRequirements, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetImageMemoryRequirements2(VkDevice device, const VkImageMemoryRequirementsInfo2* pInfo, VkMemoryRequirements2* pMemoryRequirements, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pInfo) vt_unserialize_VkImageMemoryRequirementsInfo2(pInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pInfo) pInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pMemoryRequirements) vt_unserialize_VkMemoryRequirements2(pMemoryRequirements, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pMemoryRequirements) pMemoryRequirements = NULL;
}

static inline int vt_sizeof_vkGetImageSparseMemoryRequirements2(VkDevice device, const VkImageSparseMemoryRequirementsInfo2* pInfo, uint32_t* pSparseMemoryRequirementCount, VkSparseImageMemoryRequirements2* pSparseMemoryRequirements) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pInfo ? vt_sizeof_VkImageSparseMemoryRequirementsInfo2(pInfo) : 0;
    bufferSize += 4;
    bufferSize += pSparseMemoryRequirementCount ? 4 : 0;
    bufferSize += 4;
    if (pSparseMemoryRequirements) {
        int arrSize = pSparseMemoryRequirementCount ? *pSparseMemoryRequirementCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSparseImageMemoryRequirements2(&pSparseMemoryRequirements[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkGetImageSparseMemoryRequirements2(VkDevice device, const VkImageSparseMemoryRequirementsInfo2* pInfo, uint32_t* pSparseMemoryRequirementCount, VkSparseImageMemoryRequirements2* pSparseMemoryRequirements, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pInfo) {
        int itemSize = vt_sizeof_VkImageSparseMemoryRequirementsInfo2(pInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkImageSparseMemoryRequirementsInfo2(pInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSparseMemoryRequirementCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pSparseMemoryRequirementCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSparseMemoryRequirements) {
        int arrSize = pSparseMemoryRequirementCount ? *pSparseMemoryRequirementCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSparseImageMemoryRequirements2(&pSparseMemoryRequirements[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSparseImageMemoryRequirements2(&pSparseMemoryRequirements[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetImageSparseMemoryRequirements2(VkDevice device, const VkImageSparseMemoryRequirementsInfo2* pInfo, uint32_t* pSparseMemoryRequirementCount, VkSparseImageMemoryRequirements2* pSparseMemoryRequirements, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pInfo) vt_unserialize_VkImageSparseMemoryRequirementsInfo2(pInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pInfo) pInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pSparseMemoryRequirementCount) *pSparseMemoryRequirementCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pSparseMemoryRequirementCount) pSparseMemoryRequirementCount = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkSparseImageMemoryRequirements2* arrValues = pSparseMemoryRequirements;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSparseImageMemoryRequirements2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pSparseMemoryRequirements = NULL;
}

static inline int vt_sizeof_vkGetDeviceBufferMemoryRequirements(VkDevice device, const VkDeviceBufferMemoryRequirements* pInfo, VkMemoryRequirements2* pMemoryRequirements) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pInfo ? vt_sizeof_VkDeviceBufferMemoryRequirements(pInfo) : 0;
    bufferSize += 4;
    bufferSize += pMemoryRequirements ? vt_sizeof_VkMemoryRequirements2(pMemoryRequirements) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetDeviceBufferMemoryRequirements(VkDevice device, const VkDeviceBufferMemoryRequirements* pInfo, VkMemoryRequirements2* pMemoryRequirements, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pInfo) {
        int itemSize = vt_sizeof_VkDeviceBufferMemoryRequirements(pInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDeviceBufferMemoryRequirements(pInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pMemoryRequirements) {
        int itemSize = vt_sizeof_VkMemoryRequirements2(pMemoryRequirements);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkMemoryRequirements2(pMemoryRequirements, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetDeviceBufferMemoryRequirements(VkDevice device, const VkDeviceBufferMemoryRequirements* pInfo, VkMemoryRequirements2* pMemoryRequirements, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pInfo) vt_unserialize_VkDeviceBufferMemoryRequirements(pInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pInfo) pInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pMemoryRequirements) vt_unserialize_VkMemoryRequirements2(pMemoryRequirements, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pMemoryRequirements) pMemoryRequirements = NULL;
}

static inline int vt_sizeof_vkGetDeviceImageMemoryRequirements(VkDevice device, const VkDeviceImageMemoryRequirements* pInfo, VkMemoryRequirements2* pMemoryRequirements) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pInfo ? vt_sizeof_VkDeviceImageMemoryRequirements(pInfo) : 0;
    bufferSize += 4;
    bufferSize += pMemoryRequirements ? vt_sizeof_VkMemoryRequirements2(pMemoryRequirements) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetDeviceImageMemoryRequirements(VkDevice device, const VkDeviceImageMemoryRequirements* pInfo, VkMemoryRequirements2* pMemoryRequirements, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pInfo) {
        int itemSize = vt_sizeof_VkDeviceImageMemoryRequirements(pInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDeviceImageMemoryRequirements(pInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pMemoryRequirements) {
        int itemSize = vt_sizeof_VkMemoryRequirements2(pMemoryRequirements);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkMemoryRequirements2(pMemoryRequirements, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetDeviceImageMemoryRequirements(VkDevice device, const VkDeviceImageMemoryRequirements* pInfo, VkMemoryRequirements2* pMemoryRequirements, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pInfo) vt_unserialize_VkDeviceImageMemoryRequirements(pInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pInfo) pInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pMemoryRequirements) vt_unserialize_VkMemoryRequirements2(pMemoryRequirements, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pMemoryRequirements) pMemoryRequirements = NULL;
}

static inline int vt_sizeof_vkGetDeviceImageSparseMemoryRequirements(VkDevice device, const VkDeviceImageMemoryRequirements* pInfo, uint32_t* pSparseMemoryRequirementCount, VkSparseImageMemoryRequirements2* pSparseMemoryRequirements) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pInfo ? vt_sizeof_VkDeviceImageMemoryRequirements(pInfo) : 0;
    bufferSize += 4;
    bufferSize += pSparseMemoryRequirementCount ? 4 : 0;
    bufferSize += 4;
    if (pSparseMemoryRequirements) {
        int arrSize = pSparseMemoryRequirementCount ? *pSparseMemoryRequirementCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSparseImageMemoryRequirements2(&pSparseMemoryRequirements[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkGetDeviceImageSparseMemoryRequirements(VkDevice device, const VkDeviceImageMemoryRequirements* pInfo, uint32_t* pSparseMemoryRequirementCount, VkSparseImageMemoryRequirements2* pSparseMemoryRequirements, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pInfo) {
        int itemSize = vt_sizeof_VkDeviceImageMemoryRequirements(pInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDeviceImageMemoryRequirements(pInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSparseMemoryRequirementCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pSparseMemoryRequirementCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSparseMemoryRequirements) {
        int arrSize = pSparseMemoryRequirementCount ? *pSparseMemoryRequirementCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSparseImageMemoryRequirements2(&pSparseMemoryRequirements[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSparseImageMemoryRequirements2(&pSparseMemoryRequirements[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetDeviceImageSparseMemoryRequirements(VkDevice device, const VkDeviceImageMemoryRequirements* pInfo, uint32_t* pSparseMemoryRequirementCount, VkSparseImageMemoryRequirements2* pSparseMemoryRequirements, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pInfo) vt_unserialize_VkDeviceImageMemoryRequirements(pInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pInfo) pInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pSparseMemoryRequirementCount) *pSparseMemoryRequirementCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pSparseMemoryRequirementCount) pSparseMemoryRequirementCount = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkSparseImageMemoryRequirements2* arrValues = pSparseMemoryRequirements;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSparseImageMemoryRequirements2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pSparseMemoryRequirements = NULL;
}

static inline int vt_sizeof_vkCreateSamplerYcbcrConversion(VkDevice device, const VkSamplerYcbcrConversionCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSamplerYcbcrConversion* pYcbcrConversion) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkSamplerYcbcrConversionCreateInfo(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pYcbcrConversion ? vt_sizeof_VkSamplerYcbcrConversion(*pYcbcrConversion) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateSamplerYcbcrConversion(VkDevice device, const VkSamplerYcbcrConversionCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSamplerYcbcrConversion* pYcbcrConversion, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkSamplerYcbcrConversionCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSamplerYcbcrConversionCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pYcbcrConversion) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSamplerYcbcrConversion(*pYcbcrConversion, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSamplerYcbcrConversion(*pYcbcrConversion);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateSamplerYcbcrConversion(VkDevice device, const VkSamplerYcbcrConversionCreateInfo* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkSamplerYcbcrConversion* pYcbcrConversion, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkSamplerYcbcrConversionCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pYcbcrConversion) {
            vt_unserialize_VkSamplerYcbcrConversion(*pYcbcrConversion, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSamplerYcbcrConversion(*pYcbcrConversion);
        }
        else bufferOffset += vt_sizeof_VkSamplerYcbcrConversion(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkDestroySamplerYcbcrConversion(VkDevice device, VkSamplerYcbcrConversion ycbcrConversion, const VkAllocationCallbacks* pAllocator) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += ycbcrConversion ? vt_sizeof_VkSamplerYcbcrConversion(ycbcrConversion) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkDestroySamplerYcbcrConversion(VkDevice device, VkSamplerYcbcrConversion ycbcrConversion, const VkAllocationCallbacks* pAllocator, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (ycbcrConversion) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSamplerYcbcrConversion(ycbcrConversion, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSamplerYcbcrConversion(ycbcrConversion);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkDestroySamplerYcbcrConversion(VkDevice device, VkSamplerYcbcrConversion ycbcrConversion, const VkAllocationCallbacks* pAllocator, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (ycbcrConversion) {
            vt_unserialize_VkSamplerYcbcrConversion(ycbcrConversion, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSamplerYcbcrConversion(ycbcrConversion);
        }
        else bufferOffset += vt_sizeof_VkSamplerYcbcrConversion(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkGetDeviceQueue2(VkDevice device, const VkDeviceQueueInfo2* pQueueInfo, VkQueue* pQueue) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pQueueInfo ? vt_sizeof_VkDeviceQueueInfo2(pQueueInfo) : 0;
    bufferSize++;
    bufferSize += pQueue ? vt_sizeof_VkQueue(*pQueue) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetDeviceQueue2(VkDevice device, const VkDeviceQueueInfo2* pQueueInfo, VkQueue* pQueue, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pQueueInfo) {
        int itemSize = vt_sizeof_VkDeviceQueueInfo2(pQueueInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDeviceQueueInfo2(pQueueInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pQueue) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueue(*pQueue, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueue(*pQueue);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkGetDeviceQueue2(VkDevice device, const VkDeviceQueueInfo2* pQueueInfo, VkQueue* pQueue, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pQueueInfo) vt_unserialize_VkDeviceQueueInfo2(pQueueInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pQueueInfo) pQueueInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pQueue) {
            vt_unserialize_VkQueue(*pQueue, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueue(*pQueue);
        }
        else bufferOffset += vt_sizeof_VkQueue(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkGetDescriptorSetLayoutSupport(VkDevice device, const VkDescriptorSetLayoutCreateInfo* pCreateInfo, VkDescriptorSetLayoutSupport* pSupport) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkDescriptorSetLayoutCreateInfo(pCreateInfo) : 0;
    bufferSize += 4;
    bufferSize += pSupport ? vt_sizeof_VkDescriptorSetLayoutSupport(pSupport) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetDescriptorSetLayoutSupport(VkDevice device, const VkDescriptorSetLayoutCreateInfo* pCreateInfo, VkDescriptorSetLayoutSupport* pSupport, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkDescriptorSetLayoutCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDescriptorSetLayoutCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSupport) {
        int itemSize = vt_sizeof_VkDescriptorSetLayoutSupport(pSupport);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDescriptorSetLayoutSupport(pSupport, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetDescriptorSetLayoutSupport(VkDevice device, const VkDescriptorSetLayoutCreateInfo* pCreateInfo, VkDescriptorSetLayoutSupport* pSupport, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkDescriptorSetLayoutCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pSupport) vt_unserialize_VkDescriptorSetLayoutSupport(pSupport, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pSupport) pSupport = NULL;
}

static inline int vt_sizeof_vkGetPhysicalDeviceCalibrateableTimeDomainsKHR(VkPhysicalDevice physicalDevice, uint32_t* pTimeDomainCount, VkTimeDomainKHR* pTimeDomains) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += physicalDevice ? vt_sizeof_VkPhysicalDevice(physicalDevice) : 0;
    bufferSize += 4;
    bufferSize += pTimeDomainCount ? 4 : 0;
    bufferSize += 4;
    bufferSize += pTimeDomains ? *pTimeDomainCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetPhysicalDeviceCalibrateableTimeDomainsKHR(VkPhysicalDevice physicalDevice, uint32_t* pTimeDomainCount, VkTimeDomainKHR* pTimeDomains, char* outputBuffer) {
    int bufferOffset = 0;

    if (physicalDevice) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkPhysicalDevice(physicalDevice, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pTimeDomainCount) {
        int itemSize = 4;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint32_t*)(outputBuffer + bufferOffset) = *pTimeDomainCount;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pTimeDomains) {
        int arrSize = pTimeDomainCount ? *pTimeDomainCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pTimeDomains, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetPhysicalDeviceCalibrateableTimeDomainsKHR(VkPhysicalDevice physicalDevice, uint32_t* pTimeDomainCount, VkTimeDomainKHR* pTimeDomains, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (physicalDevice) {
            vt_unserialize_VkPhysicalDevice(physicalDevice, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkPhysicalDevice(physicalDevice);
        }
        else bufferOffset += vt_sizeof_VkPhysicalDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pTimeDomainCount) *pTimeDomainCount = *(uint32_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize2;
    }
    else if (pTimeDomainCount) pTimeDomainCount = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkTimeDomainKHR* arrValues = pTimeDomains;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize3 * 4);
        bufferOffset += dataSize3 * 4;
    }
    else pTimeDomains = NULL;
}

static inline int vt_sizeof_vkGetCalibratedTimestampsKHR(VkDevice device, uint32_t timestampCount, const VkCalibratedTimestampInfoKHR* pTimestampInfos, uint64_t* pTimestamps, uint64_t* pMaxDeviation) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4; // timestampCount
    bufferSize += 4;
    if (pTimestampInfos) {
        int arrSize = timestampCount ? timestampCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkCalibratedTimestampInfoKHR(&pTimestampInfos[i]);
        }
    }
    bufferSize += 4;
    bufferSize += pTimestamps ? timestampCount * 8 : 0;
    bufferSize += 4;
    bufferSize += pMaxDeviation ? 8 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetCalibratedTimestampsKHR(VkDevice device, uint32_t timestampCount, const VkCalibratedTimestampInfoKHR* pTimestampInfos, uint64_t* pTimestamps, uint64_t* pMaxDeviation, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = timestampCount;
    bufferOffset += 4;

    if (pTimestampInfos) {
        int arrSize = timestampCount ? timestampCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkCalibratedTimestampInfoKHR(&pTimestampInfos[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkCalibratedTimestampInfoKHR(&pTimestampInfos[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pTimestamps) {
        int arrSize = timestampCount ? timestampCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pTimestamps, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pMaxDeviation) {
        int itemSize = 8;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint64_t*)(outputBuffer + bufferOffset) = *pMaxDeviation;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetCalibratedTimestampsKHR(VkDevice device, uint32_t* timestampCount, const VkCalibratedTimestampInfoKHR* pTimestampInfos, uint64_t* pTimestamps, uint64_t* pMaxDeviation, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }
    if (timestampCount) *timestampCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkCalibratedTimestampInfoKHR* arrValues = pTimestampInfos;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkCalibratedTimestampInfoKHR(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pTimestampInfos = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        uint64_t* arrValues = pTimestamps;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 8);
        bufferOffset += dataSize4 * 8;
    }
    else pTimestamps = NULL;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        if (pMaxDeviation) *pMaxDeviation = *(uint64_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize5;
    }
    else if (pMaxDeviation) pMaxDeviation = NULL;
}

static inline int vt_sizeof_vkCreateRenderPass2(VkDevice device, const VkRenderPassCreateInfo2* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkRenderPass* pRenderPass) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkRenderPassCreateInfo2(pCreateInfo) : 0;
    bufferSize++;
    bufferSize += pRenderPass ? vt_sizeof_VkRenderPass(*pRenderPass) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCreateRenderPass2(VkDevice device, const VkRenderPassCreateInfo2* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkRenderPass* pRenderPass, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkRenderPassCreateInfo2(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkRenderPassCreateInfo2(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pRenderPass) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkRenderPass(*pRenderPass, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkRenderPass(*pRenderPass);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCreateRenderPass2(VkDevice device, const VkRenderPassCreateInfo2* pCreateInfo, const VkAllocationCallbacks* pAllocator, VkRenderPass* pRenderPass, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkRenderPassCreateInfo2(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (pRenderPass) {
            vt_unserialize_VkRenderPass(*pRenderPass, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkRenderPass(*pRenderPass);
        }
        else bufferOffset += vt_sizeof_VkRenderPass(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCmdBeginRenderPass2(VkCommandBuffer commandBuffer, const VkRenderPassBeginInfo* pRenderPassBegin, const VkSubpassBeginInfo* pSubpassBeginInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pRenderPassBegin ? vt_sizeof_VkRenderPassBeginInfo(pRenderPassBegin) : 0;
    bufferSize += 4;
    bufferSize += pSubpassBeginInfo ? vt_sizeof_VkSubpassBeginInfo(pSubpassBeginInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdBeginRenderPass2(VkCommandBuffer commandBuffer, const VkRenderPassBeginInfo* pRenderPassBegin, const VkSubpassBeginInfo* pSubpassBeginInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pRenderPassBegin) {
        int itemSize = vt_sizeof_VkRenderPassBeginInfo(pRenderPassBegin);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkRenderPassBeginInfo(pRenderPassBegin, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSubpassBeginInfo) {
        int itemSize = vt_sizeof_VkSubpassBeginInfo(pSubpassBeginInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSubpassBeginInfo(pSubpassBeginInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdBeginRenderPass2(VkCommandBuffer commandBuffer, const VkRenderPassBeginInfo* pRenderPassBegin, const VkSubpassBeginInfo* pSubpassBeginInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pRenderPassBegin) vt_unserialize_VkRenderPassBeginInfo(pRenderPassBegin, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pRenderPassBegin) pRenderPassBegin = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pSubpassBeginInfo) vt_unserialize_VkSubpassBeginInfo(pSubpassBeginInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pSubpassBeginInfo) pSubpassBeginInfo = NULL;
}

static inline int vt_sizeof_vkCmdNextSubpass2(VkCommandBuffer commandBuffer, const VkSubpassBeginInfo* pSubpassBeginInfo, const VkSubpassEndInfo* pSubpassEndInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pSubpassBeginInfo ? vt_sizeof_VkSubpassBeginInfo(pSubpassBeginInfo) : 0;
    bufferSize += 4;
    bufferSize += pSubpassEndInfo ? vt_sizeof_VkSubpassEndInfo(pSubpassEndInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdNextSubpass2(VkCommandBuffer commandBuffer, const VkSubpassBeginInfo* pSubpassBeginInfo, const VkSubpassEndInfo* pSubpassEndInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pSubpassBeginInfo) {
        int itemSize = vt_sizeof_VkSubpassBeginInfo(pSubpassBeginInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSubpassBeginInfo(pSubpassBeginInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSubpassEndInfo) {
        int itemSize = vt_sizeof_VkSubpassEndInfo(pSubpassEndInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSubpassEndInfo(pSubpassEndInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdNextSubpass2(VkCommandBuffer commandBuffer, const VkSubpassBeginInfo* pSubpassBeginInfo, const VkSubpassEndInfo* pSubpassEndInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pSubpassBeginInfo) vt_unserialize_VkSubpassBeginInfo(pSubpassBeginInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pSubpassBeginInfo) pSubpassBeginInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pSubpassEndInfo) vt_unserialize_VkSubpassEndInfo(pSubpassEndInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pSubpassEndInfo) pSubpassEndInfo = NULL;
}

static inline int vt_sizeof_vkCmdEndRenderPass2(VkCommandBuffer commandBuffer, const VkSubpassEndInfo* pSubpassEndInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pSubpassEndInfo ? vt_sizeof_VkSubpassEndInfo(pSubpassEndInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdEndRenderPass2(VkCommandBuffer commandBuffer, const VkSubpassEndInfo* pSubpassEndInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pSubpassEndInfo) {
        int itemSize = vt_sizeof_VkSubpassEndInfo(pSubpassEndInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSubpassEndInfo(pSubpassEndInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdEndRenderPass2(VkCommandBuffer commandBuffer, const VkSubpassEndInfo* pSubpassEndInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pSubpassEndInfo) vt_unserialize_VkSubpassEndInfo(pSubpassEndInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pSubpassEndInfo) pSubpassEndInfo = NULL;
}

static inline int vt_sizeof_vkGetSemaphoreCounterValue(VkDevice device, VkSemaphore semaphore, uint64_t* pValue) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += semaphore ? vt_sizeof_VkSemaphore(semaphore) : 0;
    bufferSize += 4;
    bufferSize += pValue ? 8 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetSemaphoreCounterValue(VkDevice device, VkSemaphore semaphore, uint64_t* pValue, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (semaphore) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkSemaphore(semaphore, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkSemaphore(semaphore);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pValue) {
        int itemSize = 8;
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        *(uint64_t*)(outputBuffer + bufferOffset) = *pValue;
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetSemaphoreCounterValue(VkDevice device, VkSemaphore semaphore, uint64_t* pValue, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (semaphore) {
            vt_unserialize_VkSemaphore(semaphore, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkSemaphore(semaphore);
        }
        else bufferOffset += vt_sizeof_VkSemaphore(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pValue) *pValue = *(uint64_t*)(inputBuffer + bufferOffset);
        bufferOffset += dataSize3;
    }
    else if (pValue) pValue = NULL;
}

static inline int vt_sizeof_vkWaitSemaphores(VkDevice device, const VkSemaphoreWaitInfo* pWaitInfo, uint64_t timeout) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pWaitInfo ? vt_sizeof_VkSemaphoreWaitInfo(pWaitInfo) : 0;
    bufferSize += 8; // timeout
    return bufferSize;
}

static inline void vt_serialize_vkWaitSemaphores(VkDevice device, const VkSemaphoreWaitInfo* pWaitInfo, uint64_t timeout, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pWaitInfo) {
        int itemSize = vt_sizeof_VkSemaphoreWaitInfo(pWaitInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSemaphoreWaitInfo(pWaitInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
    *(uint64_t*)(outputBuffer + bufferOffset) = timeout;
    bufferOffset += 8;
}

static inline void vt_unserialize_vkWaitSemaphores(VkDevice device, const VkSemaphoreWaitInfo* pWaitInfo, uint64_t* timeout, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pWaitInfo) vt_unserialize_VkSemaphoreWaitInfo(pWaitInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pWaitInfo) pWaitInfo = NULL;
    if (timeout) *timeout = *(uint64_t*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_vkSignalSemaphore(VkDevice device, const VkSemaphoreSignalInfo* pSignalInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pSignalInfo ? vt_sizeof_VkSemaphoreSignalInfo(pSignalInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkSignalSemaphore(VkDevice device, const VkSemaphoreSignalInfo* pSignalInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pSignalInfo) {
        int itemSize = vt_sizeof_VkSemaphoreSignalInfo(pSignalInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkSemaphoreSignalInfo(pSignalInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkSignalSemaphore(VkDevice device, const VkSemaphoreSignalInfo* pSignalInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pSignalInfo) vt_unserialize_VkSemaphoreSignalInfo(pSignalInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pSignalInfo) pSignalInfo = NULL;
}

static inline int vt_sizeof_vkCmdDrawIndirectCount(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset, VkBuffer countBuffer, VkDeviceSize countBufferOffset, uint32_t maxDrawCount, uint32_t stride) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += buffer ? vt_sizeof_VkBuffer(buffer) : 0;
    bufferSize += 8; // offset
    bufferSize++;
    bufferSize += countBuffer ? vt_sizeof_VkBuffer(countBuffer) : 0;
    bufferSize += 8; // countBufferOffset
    bufferSize += 4; // maxDrawCount
    bufferSize += 4; // stride
    return bufferSize;
}

static inline void vt_serialize_vkCmdDrawIndirectCount(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset, VkBuffer countBuffer, VkDeviceSize countBufferOffset, uint32_t maxDrawCount, uint32_t stride, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (buffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(buffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(buffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = offset;
    bufferOffset += 8;

    if (countBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(countBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(countBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = countBufferOffset;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = maxDrawCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = stride;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdDrawIndirectCount(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize* offset, VkBuffer countBuffer, VkDeviceSize* countBufferOffset, uint32_t* maxDrawCount, uint32_t* stride, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (buffer) {
            vt_unserialize_VkBuffer(buffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(buffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (offset) *offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;

    if (inputBuffer[bufferOffset++]) {
        if (countBuffer) {
            vt_unserialize_VkBuffer(countBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(countBuffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (countBufferOffset) *countBufferOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (maxDrawCount) *maxDrawCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (stride) *stride = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdDrawIndexedIndirectCount(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset, VkBuffer countBuffer, VkDeviceSize countBufferOffset, uint32_t maxDrawCount, uint32_t stride) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += buffer ? vt_sizeof_VkBuffer(buffer) : 0;
    bufferSize += 8; // offset
    bufferSize++;
    bufferSize += countBuffer ? vt_sizeof_VkBuffer(countBuffer) : 0;
    bufferSize += 8; // countBufferOffset
    bufferSize += 4; // maxDrawCount
    bufferSize += 4; // stride
    return bufferSize;
}

static inline void vt_serialize_vkCmdDrawIndexedIndirectCount(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize offset, VkBuffer countBuffer, VkDeviceSize countBufferOffset, uint32_t maxDrawCount, uint32_t stride, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (buffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(buffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(buffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = offset;
    bufferOffset += 8;

    if (countBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(countBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(countBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = countBufferOffset;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = maxDrawCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = stride;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdDrawIndexedIndirectCount(VkCommandBuffer commandBuffer, VkBuffer buffer, VkDeviceSize* offset, VkBuffer countBuffer, VkDeviceSize* countBufferOffset, uint32_t* maxDrawCount, uint32_t* stride, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (buffer) {
            vt_unserialize_VkBuffer(buffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(buffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (offset) *offset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;

    if (inputBuffer[bufferOffset++]) {
        if (countBuffer) {
            vt_unserialize_VkBuffer(countBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(countBuffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (countBufferOffset) *countBufferOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (maxDrawCount) *maxDrawCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (stride) *stride = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdBindTransformFeedbackBuffersEXT(VkCommandBuffer commandBuffer, uint32_t firstBinding, uint32_t bindingCount, const VkBuffer* pBuffers, const VkDeviceSize* pOffsets, const VkDeviceSize* pSizes) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // firstBinding
    bufferSize += 4; // bindingCount
    bufferSize += 4;
    if (pBuffers) {
        for (int i = 0; i < bindingCount; i++) {
            bufferSize += vt_sizeof_VkBuffer(pBuffers[i]);
        }
    }
    bufferSize += 4;
    bufferSize += pOffsets ? bindingCount * 8 : 0;
    bufferSize += 4;
    bufferSize += pSizes ? bindingCount * 8 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdBindTransformFeedbackBuffersEXT(VkCommandBuffer commandBuffer, uint32_t firstBinding, uint32_t bindingCount, const VkBuffer* pBuffers, const VkDeviceSize* pOffsets, const VkDeviceSize* pSizes, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstBinding;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = bindingCount;
    bufferOffset += 4;

    if (pBuffers) {
        *(int*)(outputBuffer + bufferOffset) = bindingCount;
        bufferOffset += 4;
        for (int i = 0; i < bindingCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkBuffer(pBuffers[i], outputBuffer + bufferOffset);
#else
            VkObject* buffersObject = VkObject_fromHandle(pBuffers[i]);
            vt_serialize_VkBuffer((VkBuffer)&buffersObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkBuffer(pBuffers[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pOffsets) {
        int arrSize = bindingCount ? bindingCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pOffsets, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSizes) {
        int arrSize = bindingCount ? bindingCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pSizes, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdBindTransformFeedbackBuffersEXT(VkCommandBuffer commandBuffer, uint32_t* firstBinding, uint32_t* bindingCount, const VkBuffer* pBuffers, VkDeviceSize* pOffsets, VkDeviceSize* pSizes, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (firstBinding) *firstBinding = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (bindingCount) *bindingCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkBuffer* arrValues = pBuffers;
        for (int i = 0; i < dataSize4; i++) {
            uint64_t buffersId;
            vt_unserialize_VkBuffer((VkBuffer)&buffersId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(buffersId);
#else
                VkObject* buffersObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, buffersId);
                arrValues[i] = VkObject_toHandle(buffersObject);
#endif
                bufferOffset += vt_sizeof_VkBuffer(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
        }
    }
    else pBuffers = NULL;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkDeviceSize* arrValues = pOffsets;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize5 * 8);
        bufferOffset += dataSize5 * 8;
    }
    else pOffsets = NULL;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkDeviceSize* arrValues = pSizes;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize6 * 8);
        bufferOffset += dataSize6 * 8;
    }
    else pSizes = NULL;
}

static inline int vt_sizeof_vkCmdBeginTransformFeedbackEXT(VkCommandBuffer commandBuffer, uint32_t firstCounterBuffer, uint32_t counterBufferCount, const VkBuffer* pCounterBuffers, const VkDeviceSize* pCounterBufferOffsets) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // firstCounterBuffer
    bufferSize += 4; // counterBufferCount
    bufferSize += 4;
    if (pCounterBuffers) {
        for (int i = 0; i < counterBufferCount; i++) {
            bufferSize += vt_sizeof_VkBuffer(pCounterBuffers[i]);
        }
    }
    bufferSize += 4;
    bufferSize += pCounterBufferOffsets ? counterBufferCount * 8 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdBeginTransformFeedbackEXT(VkCommandBuffer commandBuffer, uint32_t firstCounterBuffer, uint32_t counterBufferCount, const VkBuffer* pCounterBuffers, const VkDeviceSize* pCounterBufferOffsets, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstCounterBuffer;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = counterBufferCount;
    bufferOffset += 4;

    if (pCounterBuffers) {
        *(int*)(outputBuffer + bufferOffset) = counterBufferCount;
        bufferOffset += 4;
        for (int i = 0; i < counterBufferCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkBuffer(pCounterBuffers[i], outputBuffer + bufferOffset);
#else
            VkObject* counterBuffersObject = VkObject_fromHandle(pCounterBuffers[i]);
            vt_serialize_VkBuffer((VkBuffer)&counterBuffersObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkBuffer(pCounterBuffers[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pCounterBufferOffsets) {
        int arrSize = counterBufferCount ? counterBufferCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pCounterBufferOffsets, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdBeginTransformFeedbackEXT(VkCommandBuffer commandBuffer, uint32_t* firstCounterBuffer, uint32_t* counterBufferCount, const VkBuffer* pCounterBuffers, VkDeviceSize* pCounterBufferOffsets, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (firstCounterBuffer) *firstCounterBuffer = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (counterBufferCount) *counterBufferCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkBuffer* arrValues = pCounterBuffers;
        for (int i = 0; i < dataSize4; i++) {
            uint64_t counterBuffersId;
            vt_unserialize_VkBuffer((VkBuffer)&counterBuffersId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(counterBuffersId);
#else
                VkObject* counterBuffersObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, counterBuffersId);
                arrValues[i] = VkObject_toHandle(counterBuffersObject);
#endif
                bufferOffset += vt_sizeof_VkBuffer(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
        }
    }
    else pCounterBuffers = NULL;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkDeviceSize* arrValues = pCounterBufferOffsets;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize5 * 8);
        bufferOffset += dataSize5 * 8;
    }
    else pCounterBufferOffsets = NULL;
}

static inline int vt_sizeof_vkCmdEndTransformFeedbackEXT(VkCommandBuffer commandBuffer, uint32_t firstCounterBuffer, uint32_t counterBufferCount, const VkBuffer* pCounterBuffers, const VkDeviceSize* pCounterBufferOffsets) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // firstCounterBuffer
    bufferSize += 4; // counterBufferCount
    bufferSize += 4;
    if (pCounterBuffers) {
        for (int i = 0; i < counterBufferCount; i++) {
            bufferSize += vt_sizeof_VkBuffer(pCounterBuffers[i]);
        }
    }
    bufferSize += 4;
    bufferSize += pCounterBufferOffsets ? counterBufferCount * 8 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdEndTransformFeedbackEXT(VkCommandBuffer commandBuffer, uint32_t firstCounterBuffer, uint32_t counterBufferCount, const VkBuffer* pCounterBuffers, const VkDeviceSize* pCounterBufferOffsets, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstCounterBuffer;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = counterBufferCount;
    bufferOffset += 4;

    if (pCounterBuffers) {
        *(int*)(outputBuffer + bufferOffset) = counterBufferCount;
        bufferOffset += 4;
        for (int i = 0; i < counterBufferCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkBuffer(pCounterBuffers[i], outputBuffer + bufferOffset);
#else
            VkObject* counterBuffersObject = VkObject_fromHandle(pCounterBuffers[i]);
            vt_serialize_VkBuffer((VkBuffer)&counterBuffersObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkBuffer(pCounterBuffers[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pCounterBufferOffsets) {
        int arrSize = counterBufferCount ? counterBufferCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pCounterBufferOffsets, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdEndTransformFeedbackEXT(VkCommandBuffer commandBuffer, uint32_t* firstCounterBuffer, uint32_t* counterBufferCount, const VkBuffer* pCounterBuffers, VkDeviceSize* pCounterBufferOffsets, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (firstCounterBuffer) *firstCounterBuffer = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (counterBufferCount) *counterBufferCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkBuffer* arrValues = pCounterBuffers;
        for (int i = 0; i < dataSize4; i++) {
            uint64_t counterBuffersId;
            vt_unserialize_VkBuffer((VkBuffer)&counterBuffersId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(counterBuffersId);
#else
                VkObject* counterBuffersObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, counterBuffersId);
                arrValues[i] = VkObject_toHandle(counterBuffersObject);
#endif
                bufferOffset += vt_sizeof_VkBuffer(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
        }
    }
    else pCounterBuffers = NULL;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkDeviceSize* arrValues = pCounterBufferOffsets;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize5 * 8);
        bufferOffset += dataSize5 * 8;
    }
    else pCounterBufferOffsets = NULL;
}

static inline int vt_sizeof_vkCmdBeginQueryIndexedEXT(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t query, VkQueryControlFlags flags, uint32_t index) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += queryPool ? vt_sizeof_VkQueryPool(queryPool) : 0;
    bufferSize += 4; // query
    bufferSize += 4; // flags
    bufferSize += 4; // index
    return bufferSize;
}

static inline void vt_serialize_vkCmdBeginQueryIndexedEXT(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t query, VkQueryControlFlags flags, uint32_t index, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (queryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(queryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(queryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = query;
    bufferOffset += 4;
    *(VkQueryControlFlags*)(outputBuffer + bufferOffset) = flags;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = index;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdBeginQueryIndexedEXT(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t* query, VkQueryControlFlags* flags, uint32_t* index, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (queryPool) {
            vt_unserialize_VkQueryPool(queryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(queryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
    if (query) *query = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (flags) *flags = *(VkQueryControlFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (index) *index = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdEndQueryIndexedEXT(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t query, uint32_t index) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += queryPool ? vt_sizeof_VkQueryPool(queryPool) : 0;
    bufferSize += 4; // query
    bufferSize += 4; // index
    return bufferSize;
}

static inline void vt_serialize_vkCmdEndQueryIndexedEXT(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t query, uint32_t index, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (queryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(queryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(queryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = query;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = index;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdEndQueryIndexedEXT(VkCommandBuffer commandBuffer, VkQueryPool queryPool, uint32_t* query, uint32_t* index, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (queryPool) {
            vt_unserialize_VkQueryPool(queryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(queryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
    if (query) *query = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (index) *index = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdDrawIndirectByteCountEXT(VkCommandBuffer commandBuffer, uint32_t instanceCount, uint32_t firstInstance, VkBuffer counterBuffer, VkDeviceSize counterBufferOffset, uint32_t counterOffset, uint32_t vertexStride) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // instanceCount
    bufferSize += 4; // firstInstance
    bufferSize++;
    bufferSize += counterBuffer ? vt_sizeof_VkBuffer(counterBuffer) : 0;
    bufferSize += 8; // counterBufferOffset
    bufferSize += 4; // counterOffset
    bufferSize += 4; // vertexStride
    return bufferSize;
}

static inline void vt_serialize_vkCmdDrawIndirectByteCountEXT(VkCommandBuffer commandBuffer, uint32_t instanceCount, uint32_t firstInstance, VkBuffer counterBuffer, VkDeviceSize counterBufferOffset, uint32_t counterOffset, uint32_t vertexStride, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = instanceCount;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstInstance;
    bufferOffset += 4;

    if (counterBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkBuffer(counterBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkBuffer(counterBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkDeviceSize*)(outputBuffer + bufferOffset) = counterBufferOffset;
    bufferOffset += 8;
    *(uint32_t*)(outputBuffer + bufferOffset) = counterOffset;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = vertexStride;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdDrawIndirectByteCountEXT(VkCommandBuffer commandBuffer, uint32_t* instanceCount, uint32_t* firstInstance, VkBuffer counterBuffer, VkDeviceSize* counterBufferOffset, uint32_t* counterOffset, uint32_t* vertexStride, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (instanceCount) *instanceCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (firstInstance) *firstInstance = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    if (inputBuffer[bufferOffset++]) {
        if (counterBuffer) {
            vt_unserialize_VkBuffer(counterBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkBuffer(counterBuffer);
        }
        else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
    }
    if (counterBufferOffset) *counterBufferOffset = *(VkDeviceSize*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
    if (counterOffset) *counterOffset = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (vertexStride) *vertexStride = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkGetBufferOpaqueCaptureAddress(VkDevice device, const VkBufferDeviceAddressInfo* pInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pInfo ? vt_sizeof_VkBufferDeviceAddressInfo(pInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetBufferOpaqueCaptureAddress(VkDevice device, const VkBufferDeviceAddressInfo* pInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pInfo) {
        int itemSize = vt_sizeof_VkBufferDeviceAddressInfo(pInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkBufferDeviceAddressInfo(pInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetBufferOpaqueCaptureAddress(VkDevice device, const VkBufferDeviceAddressInfo* pInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pInfo) vt_unserialize_VkBufferDeviceAddressInfo(pInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pInfo) pInfo = NULL;
}

static inline int vt_sizeof_vkGetBufferDeviceAddress(VkDevice device, const VkBufferDeviceAddressInfo* pInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pInfo ? vt_sizeof_VkBufferDeviceAddressInfo(pInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetBufferDeviceAddress(VkDevice device, const VkBufferDeviceAddressInfo* pInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pInfo) {
        int itemSize = vt_sizeof_VkBufferDeviceAddressInfo(pInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkBufferDeviceAddressInfo(pInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetBufferDeviceAddress(VkDevice device, const VkBufferDeviceAddressInfo* pInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pInfo) vt_unserialize_VkBufferDeviceAddressInfo(pInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pInfo) pInfo = NULL;
}

static inline int vt_sizeof_vkGetDeviceMemoryOpaqueCaptureAddress(VkDevice device, const VkDeviceMemoryOpaqueCaptureAddressInfo* pInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pInfo ? vt_sizeof_VkDeviceMemoryOpaqueCaptureAddressInfo(pInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetDeviceMemoryOpaqueCaptureAddress(VkDevice device, const VkDeviceMemoryOpaqueCaptureAddressInfo* pInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pInfo) {
        int itemSize = vt_sizeof_VkDeviceMemoryOpaqueCaptureAddressInfo(pInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDeviceMemoryOpaqueCaptureAddressInfo(pInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetDeviceMemoryOpaqueCaptureAddress(VkDevice device, const VkDeviceMemoryOpaqueCaptureAddressInfo* pInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pInfo) vt_unserialize_VkDeviceMemoryOpaqueCaptureAddressInfo(pInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pInfo) pInfo = NULL;
}

static inline int vt_sizeof_vkCmdSetLineStippleKHR(VkCommandBuffer commandBuffer, uint32_t lineStippleFactor, uint16_t lineStipplePattern) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // lineStippleFactor
    bufferSize += 2; // lineStipplePattern
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetLineStippleKHR(VkCommandBuffer commandBuffer, uint32_t lineStippleFactor, uint16_t lineStipplePattern, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = lineStippleFactor;
    bufferOffset += 4;
    *(uint16_t*)(outputBuffer + bufferOffset) = lineStipplePattern;
    bufferOffset += 2;
}

static inline void vt_unserialize_vkCmdSetLineStippleKHR(VkCommandBuffer commandBuffer, uint32_t* lineStippleFactor, uint16_t* lineStipplePattern, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (lineStippleFactor) *lineStippleFactor = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (lineStipplePattern) *lineStipplePattern = *(uint16_t*)(inputBuffer + bufferOffset);
    bufferOffset += 2;
}

static inline int vt_sizeof_vkCmdSetCullMode(VkCommandBuffer commandBuffer, VkCullModeFlags cullMode) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // cullMode
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetCullMode(VkCommandBuffer commandBuffer, VkCullModeFlags cullMode, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkCullModeFlags*)(outputBuffer + bufferOffset) = cullMode;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetCullMode(VkCommandBuffer commandBuffer, VkCullModeFlags* cullMode, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (cullMode) *cullMode = *(VkCullModeFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetFrontFace(VkCommandBuffer commandBuffer, VkFrontFace frontFace) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // frontFace
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetFrontFace(VkCommandBuffer commandBuffer, VkFrontFace frontFace, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkFrontFace*)(outputBuffer + bufferOffset) = frontFace;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetFrontFace(VkCommandBuffer commandBuffer, VkFrontFace* frontFace, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (frontFace) *frontFace = *(VkFrontFace*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetPrimitiveTopology(VkCommandBuffer commandBuffer, VkPrimitiveTopology primitiveTopology) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // primitiveTopology
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetPrimitiveTopology(VkCommandBuffer commandBuffer, VkPrimitiveTopology primitiveTopology, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkPrimitiveTopology*)(outputBuffer + bufferOffset) = primitiveTopology;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetPrimitiveTopology(VkCommandBuffer commandBuffer, VkPrimitiveTopology* primitiveTopology, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (primitiveTopology) *primitiveTopology = *(VkPrimitiveTopology*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetViewportWithCount(VkCommandBuffer commandBuffer, uint32_t viewportCount, const VkViewport* pViewports) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // viewportCount
    bufferSize += 4;
    if (pViewports) {
        int arrSize = viewportCount ? viewportCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkViewport(&pViewports[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetViewportWithCount(VkCommandBuffer commandBuffer, uint32_t viewportCount, const VkViewport* pViewports, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = viewportCount;
    bufferOffset += 4;

    if (pViewports) {
        int arrSize = viewportCount ? viewportCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkViewport(&pViewports[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkViewport(&pViewports[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdSetViewportWithCount(VkCommandBuffer commandBuffer, uint32_t* viewportCount, const VkViewport* pViewports, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (viewportCount) *viewportCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkViewport* arrValues = pViewports;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkViewport(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pViewports = NULL;
}

static inline int vt_sizeof_vkCmdSetScissorWithCount(VkCommandBuffer commandBuffer, uint32_t scissorCount, const VkRect2D* pScissors) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // scissorCount
    bufferSize += 4;
    if (pScissors) {
        int arrSize = scissorCount ? scissorCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkRect2D(&pScissors[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetScissorWithCount(VkCommandBuffer commandBuffer, uint32_t scissorCount, const VkRect2D* pScissors, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = scissorCount;
    bufferOffset += 4;

    if (pScissors) {
        int arrSize = scissorCount ? scissorCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkRect2D(&pScissors[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkRect2D(&pScissors[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdSetScissorWithCount(VkCommandBuffer commandBuffer, uint32_t* scissorCount, const VkRect2D* pScissors, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (scissorCount) *scissorCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkRect2D* arrValues = pScissors;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkRect2D(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pScissors = NULL;
}

static inline int vt_sizeof_vkCmdBindVertexBuffers2(VkCommandBuffer commandBuffer, uint32_t firstBinding, uint32_t bindingCount, const VkBuffer* pBuffers, const VkDeviceSize* pOffsets, const VkDeviceSize* pSizes, const VkDeviceSize* pStrides) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // firstBinding
    bufferSize += 4; // bindingCount
    bufferSize += 4;
    if (pBuffers) {
        for (int i = 0; i < bindingCount; i++) {
            bufferSize += vt_sizeof_VkBuffer(pBuffers[i]);
        }
    }
    bufferSize += 4;
    bufferSize += pOffsets ? bindingCount * 8 : 0;
    bufferSize += 4;
    bufferSize += pSizes ? bindingCount * 8 : 0;
    bufferSize += 4;
    bufferSize += pStrides ? bindingCount * 8 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdBindVertexBuffers2(VkCommandBuffer commandBuffer, uint32_t firstBinding, uint32_t bindingCount, const VkBuffer* pBuffers, const VkDeviceSize* pOffsets, const VkDeviceSize* pSizes, const VkDeviceSize* pStrides, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstBinding;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = bindingCount;
    bufferOffset += 4;

    if (pBuffers) {
        *(int*)(outputBuffer + bufferOffset) = bindingCount;
        bufferOffset += 4;
        for (int i = 0; i < bindingCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkBuffer(pBuffers[i], outputBuffer + bufferOffset);
#else
            VkObject* buffersObject = VkObject_fromHandle(pBuffers[i]);
            vt_serialize_VkBuffer((VkBuffer)&buffersObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkBuffer(pBuffers[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pOffsets) {
        int arrSize = bindingCount ? bindingCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pOffsets, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pSizes) {
        int arrSize = bindingCount ? bindingCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pSizes, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pStrides) {
        int arrSize = bindingCount ? bindingCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pStrides, arrSize * 8);
        bufferOffset += arrSize * 8;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdBindVertexBuffers2(VkCommandBuffer commandBuffer, uint32_t* firstBinding, uint32_t* bindingCount, const VkBuffer* pBuffers, VkDeviceSize* pOffsets, VkDeviceSize* pSizes, VkDeviceSize* pStrides, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (firstBinding) *firstBinding = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (bindingCount) *bindingCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkBuffer* arrValues = pBuffers;
        for (int i = 0; i < dataSize4; i++) {
            uint64_t buffersId;
            vt_unserialize_VkBuffer((VkBuffer)&buffersId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(buffersId);
#else
                VkObject* buffersObject = VkObject_create(VK_OBJECT_TYPE_BUFFER, buffersId);
                arrValues[i] = VkObject_toHandle(buffersObject);
#endif
                bufferOffset += vt_sizeof_VkBuffer(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkBuffer(VK_NULL_HANDLE);
        }
    }
    else pBuffers = NULL;

    int dataSize5 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize5 > 0) {
        VkDeviceSize* arrValues = pOffsets;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize5 * 8);
        bufferOffset += dataSize5 * 8;
    }
    else pOffsets = NULL;

    int dataSize6 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize6 > 0) {
        VkDeviceSize* arrValues = pSizes;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize6 * 8);
        bufferOffset += dataSize6 * 8;
    }
    else pSizes = NULL;

    int dataSize7 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize7 > 0) {
        VkDeviceSize* arrValues = pStrides;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize7 * 8);
        bufferOffset += dataSize7 * 8;
    }
    else pStrides = NULL;
}

static inline int vt_sizeof_vkCmdSetDepthTestEnable(VkCommandBuffer commandBuffer, VkBool32 depthTestEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // depthTestEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetDepthTestEnable(VkCommandBuffer commandBuffer, VkBool32 depthTestEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = depthTestEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetDepthTestEnable(VkCommandBuffer commandBuffer, VkBool32* depthTestEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (depthTestEnable) *depthTestEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetDepthWriteEnable(VkCommandBuffer commandBuffer, VkBool32 depthWriteEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // depthWriteEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetDepthWriteEnable(VkCommandBuffer commandBuffer, VkBool32 depthWriteEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = depthWriteEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetDepthWriteEnable(VkCommandBuffer commandBuffer, VkBool32* depthWriteEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (depthWriteEnable) *depthWriteEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetDepthCompareOp(VkCommandBuffer commandBuffer, VkCompareOp depthCompareOp) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // depthCompareOp
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetDepthCompareOp(VkCommandBuffer commandBuffer, VkCompareOp depthCompareOp, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkCompareOp*)(outputBuffer + bufferOffset) = depthCompareOp;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetDepthCompareOp(VkCommandBuffer commandBuffer, VkCompareOp* depthCompareOp, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (depthCompareOp) *depthCompareOp = *(VkCompareOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetDepthBoundsTestEnable(VkCommandBuffer commandBuffer, VkBool32 depthBoundsTestEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // depthBoundsTestEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetDepthBoundsTestEnable(VkCommandBuffer commandBuffer, VkBool32 depthBoundsTestEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = depthBoundsTestEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetDepthBoundsTestEnable(VkCommandBuffer commandBuffer, VkBool32* depthBoundsTestEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (depthBoundsTestEnable) *depthBoundsTestEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetStencilTestEnable(VkCommandBuffer commandBuffer, VkBool32 stencilTestEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // stencilTestEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetStencilTestEnable(VkCommandBuffer commandBuffer, VkBool32 stencilTestEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = stencilTestEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetStencilTestEnable(VkCommandBuffer commandBuffer, VkBool32* stencilTestEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (stencilTestEnable) *stencilTestEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetStencilOp(VkCommandBuffer commandBuffer, VkStencilFaceFlags faceMask, VkStencilOp failOp, VkStencilOp passOp, VkStencilOp depthFailOp, VkCompareOp compareOp) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // faceMask
    bufferSize += 4; // failOp
    bufferSize += 4; // passOp
    bufferSize += 4; // depthFailOp
    bufferSize += 4; // compareOp
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetStencilOp(VkCommandBuffer commandBuffer, VkStencilFaceFlags faceMask, VkStencilOp failOp, VkStencilOp passOp, VkStencilOp depthFailOp, VkCompareOp compareOp, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkStencilFaceFlags*)(outputBuffer + bufferOffset) = faceMask;
    bufferOffset += 4;
    *(VkStencilOp*)(outputBuffer + bufferOffset) = failOp;
    bufferOffset += 4;
    *(VkStencilOp*)(outputBuffer + bufferOffset) = passOp;
    bufferOffset += 4;
    *(VkStencilOp*)(outputBuffer + bufferOffset) = depthFailOp;
    bufferOffset += 4;
    *(VkCompareOp*)(outputBuffer + bufferOffset) = compareOp;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetStencilOp(VkCommandBuffer commandBuffer, VkStencilFaceFlags* faceMask, VkStencilOp* failOp, VkStencilOp* passOp, VkStencilOp* depthFailOp, VkCompareOp* compareOp, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (faceMask) *faceMask = *(VkStencilFaceFlags*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (failOp) *failOp = *(VkStencilOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (passOp) *passOp = *(VkStencilOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (depthFailOp) *depthFailOp = *(VkStencilOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (compareOp) *compareOp = *(VkCompareOp*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetRasterizerDiscardEnable(VkCommandBuffer commandBuffer, VkBool32 rasterizerDiscardEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // rasterizerDiscardEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetRasterizerDiscardEnable(VkCommandBuffer commandBuffer, VkBool32 rasterizerDiscardEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = rasterizerDiscardEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetRasterizerDiscardEnable(VkCommandBuffer commandBuffer, VkBool32* rasterizerDiscardEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (rasterizerDiscardEnable) *rasterizerDiscardEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetDepthBiasEnable(VkCommandBuffer commandBuffer, VkBool32 depthBiasEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // depthBiasEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetDepthBiasEnable(VkCommandBuffer commandBuffer, VkBool32 depthBiasEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = depthBiasEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetDepthBiasEnable(VkCommandBuffer commandBuffer, VkBool32* depthBiasEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (depthBiasEnable) *depthBiasEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetPrimitiveRestartEnable(VkCommandBuffer commandBuffer, VkBool32 primitiveRestartEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // primitiveRestartEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetPrimitiveRestartEnable(VkCommandBuffer commandBuffer, VkBool32 primitiveRestartEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = primitiveRestartEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetPrimitiveRestartEnable(VkCommandBuffer commandBuffer, VkBool32* primitiveRestartEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (primitiveRestartEnable) *primitiveRestartEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetTessellationDomainOriginEXT(VkCommandBuffer commandBuffer, VkTessellationDomainOrigin domainOrigin) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // domainOrigin
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetTessellationDomainOriginEXT(VkCommandBuffer commandBuffer, VkTessellationDomainOrigin domainOrigin, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkTessellationDomainOrigin*)(outputBuffer + bufferOffset) = domainOrigin;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetTessellationDomainOriginEXT(VkCommandBuffer commandBuffer, VkTessellationDomainOrigin* domainOrigin, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (domainOrigin) *domainOrigin = *(VkTessellationDomainOrigin*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetDepthClampEnableEXT(VkCommandBuffer commandBuffer, VkBool32 depthClampEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // depthClampEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetDepthClampEnableEXT(VkCommandBuffer commandBuffer, VkBool32 depthClampEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = depthClampEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetDepthClampEnableEXT(VkCommandBuffer commandBuffer, VkBool32* depthClampEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (depthClampEnable) *depthClampEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetPolygonModeEXT(VkCommandBuffer commandBuffer, VkPolygonMode polygonMode) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // polygonMode
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetPolygonModeEXT(VkCommandBuffer commandBuffer, VkPolygonMode polygonMode, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkPolygonMode*)(outputBuffer + bufferOffset) = polygonMode;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetPolygonModeEXT(VkCommandBuffer commandBuffer, VkPolygonMode* polygonMode, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (polygonMode) *polygonMode = *(VkPolygonMode*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetRasterizationSamplesEXT(VkCommandBuffer commandBuffer, VkSampleCountFlagBits rasterizationSamples) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // rasterizationSamples
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetRasterizationSamplesEXT(VkCommandBuffer commandBuffer, VkSampleCountFlagBits rasterizationSamples, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkSampleCountFlagBits*)(outputBuffer + bufferOffset) = rasterizationSamples;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetRasterizationSamplesEXT(VkCommandBuffer commandBuffer, VkSampleCountFlagBits* rasterizationSamples, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (rasterizationSamples) *rasterizationSamples = *(VkSampleCountFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetSampleMaskEXT(VkCommandBuffer commandBuffer, VkSampleCountFlagBits samples, const VkSampleMask* pSampleMask) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // samples
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetSampleMaskEXT(VkCommandBuffer commandBuffer, VkSampleCountFlagBits samples, const VkSampleMask* pSampleMask, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkSampleCountFlagBits*)(outputBuffer + bufferOffset) = samples;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetSampleMaskEXT(VkCommandBuffer commandBuffer, VkSampleCountFlagBits* samples, VkSampleMask* pSampleMask, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (samples) *samples = *(VkSampleCountFlagBits*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    pSampleMask = NULL;
}

static inline int vt_sizeof_vkCmdSetAlphaToCoverageEnableEXT(VkCommandBuffer commandBuffer, VkBool32 alphaToCoverageEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // alphaToCoverageEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetAlphaToCoverageEnableEXT(VkCommandBuffer commandBuffer, VkBool32 alphaToCoverageEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = alphaToCoverageEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetAlphaToCoverageEnableEXT(VkCommandBuffer commandBuffer, VkBool32* alphaToCoverageEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (alphaToCoverageEnable) *alphaToCoverageEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetAlphaToOneEnableEXT(VkCommandBuffer commandBuffer, VkBool32 alphaToOneEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // alphaToOneEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetAlphaToOneEnableEXT(VkCommandBuffer commandBuffer, VkBool32 alphaToOneEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = alphaToOneEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetAlphaToOneEnableEXT(VkCommandBuffer commandBuffer, VkBool32* alphaToOneEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (alphaToOneEnable) *alphaToOneEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetLogicOpEnableEXT(VkCommandBuffer commandBuffer, VkBool32 logicOpEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // logicOpEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetLogicOpEnableEXT(VkCommandBuffer commandBuffer, VkBool32 logicOpEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = logicOpEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetLogicOpEnableEXT(VkCommandBuffer commandBuffer, VkBool32* logicOpEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (logicOpEnable) *logicOpEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetColorBlendEnableEXT(VkCommandBuffer commandBuffer, uint32_t firstAttachment, uint32_t attachmentCount, const VkBool32* pColorBlendEnables) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // firstAttachment
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    bufferSize += pColorBlendEnables ? attachmentCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetColorBlendEnableEXT(VkCommandBuffer commandBuffer, uint32_t firstAttachment, uint32_t attachmentCount, const VkBool32* pColorBlendEnables, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstAttachment;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = attachmentCount;
    bufferOffset += 4;

    if (pColorBlendEnables) {
        int arrSize = attachmentCount ? attachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pColorBlendEnables, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdSetColorBlendEnableEXT(VkCommandBuffer commandBuffer, uint32_t* firstAttachment, uint32_t* attachmentCount, VkBool32* pColorBlendEnables, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (firstAttachment) *firstAttachment = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (attachmentCount) *attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkBool32* arrValues = pColorBlendEnables;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
    }
    else pColorBlendEnables = NULL;
}

static inline int vt_sizeof_vkCmdSetColorBlendEquationEXT(VkCommandBuffer commandBuffer, uint32_t firstAttachment, uint32_t attachmentCount, const VkColorBlendEquationEXT* pColorBlendEquations) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // firstAttachment
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    if (pColorBlendEquations) {
        int arrSize = attachmentCount ? attachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkColorBlendEquationEXT(&pColorBlendEquations[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetColorBlendEquationEXT(VkCommandBuffer commandBuffer, uint32_t firstAttachment, uint32_t attachmentCount, const VkColorBlendEquationEXT* pColorBlendEquations, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstAttachment;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = attachmentCount;
    bufferOffset += 4;

    if (pColorBlendEquations) {
        int arrSize = attachmentCount ? attachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkColorBlendEquationEXT(&pColorBlendEquations[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkColorBlendEquationEXT(&pColorBlendEquations[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdSetColorBlendEquationEXT(VkCommandBuffer commandBuffer, uint32_t* firstAttachment, uint32_t* attachmentCount, const VkColorBlendEquationEXT* pColorBlendEquations, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (firstAttachment) *firstAttachment = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (attachmentCount) *attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkColorBlendEquationEXT* arrValues = pColorBlendEquations;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkColorBlendEquationEXT(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pColorBlendEquations = NULL;
}

static inline int vt_sizeof_vkCmdSetColorWriteMaskEXT(VkCommandBuffer commandBuffer, uint32_t firstAttachment, uint32_t attachmentCount, const VkColorComponentFlags* pColorWriteMasks) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // firstAttachment
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    bufferSize += pColorWriteMasks ? attachmentCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetColorWriteMaskEXT(VkCommandBuffer commandBuffer, uint32_t firstAttachment, uint32_t attachmentCount, const VkColorComponentFlags* pColorWriteMasks, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstAttachment;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = attachmentCount;
    bufferOffset += 4;

    if (pColorWriteMasks) {
        int arrSize = attachmentCount ? attachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pColorWriteMasks, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdSetColorWriteMaskEXT(VkCommandBuffer commandBuffer, uint32_t* firstAttachment, uint32_t* attachmentCount, VkColorComponentFlags* pColorWriteMasks, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (firstAttachment) *firstAttachment = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (attachmentCount) *attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkColorComponentFlags* arrValues = pColorWriteMasks;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize4 * 4);
        bufferOffset += dataSize4 * 4;
    }
    else pColorWriteMasks = NULL;
}

static inline int vt_sizeof_vkCmdSetRasterizationStreamEXT(VkCommandBuffer commandBuffer, uint32_t rasterizationStream) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // rasterizationStream
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetRasterizationStreamEXT(VkCommandBuffer commandBuffer, uint32_t rasterizationStream, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = rasterizationStream;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetRasterizationStreamEXT(VkCommandBuffer commandBuffer, uint32_t* rasterizationStream, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (rasterizationStream) *rasterizationStream = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetConservativeRasterizationModeEXT(VkCommandBuffer commandBuffer, VkConservativeRasterizationModeEXT conservativeRasterizationMode) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // conservativeRasterizationMode
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetConservativeRasterizationModeEXT(VkCommandBuffer commandBuffer, VkConservativeRasterizationModeEXT conservativeRasterizationMode, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkConservativeRasterizationModeEXT*)(outputBuffer + bufferOffset) = conservativeRasterizationMode;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetConservativeRasterizationModeEXT(VkCommandBuffer commandBuffer, VkConservativeRasterizationModeEXT* conservativeRasterizationMode, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (conservativeRasterizationMode) *conservativeRasterizationMode = *(VkConservativeRasterizationModeEXT*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetExtraPrimitiveOverestimationSizeEXT(VkCommandBuffer commandBuffer, float extraPrimitiveOverestimationSize) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // extraPrimitiveOverestimationSize
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetExtraPrimitiveOverestimationSizeEXT(VkCommandBuffer commandBuffer, float extraPrimitiveOverestimationSize, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(float*)(outputBuffer + bufferOffset) = extraPrimitiveOverestimationSize;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetExtraPrimitiveOverestimationSizeEXT(VkCommandBuffer commandBuffer, float* extraPrimitiveOverestimationSize, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (extraPrimitiveOverestimationSize) *extraPrimitiveOverestimationSize = *(float*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetDepthClipEnableEXT(VkCommandBuffer commandBuffer, VkBool32 depthClipEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // depthClipEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetDepthClipEnableEXT(VkCommandBuffer commandBuffer, VkBool32 depthClipEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = depthClipEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetDepthClipEnableEXT(VkCommandBuffer commandBuffer, VkBool32* depthClipEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (depthClipEnable) *depthClipEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetSampleLocationsEnableEXT(VkCommandBuffer commandBuffer, VkBool32 sampleLocationsEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // sampleLocationsEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetSampleLocationsEnableEXT(VkCommandBuffer commandBuffer, VkBool32 sampleLocationsEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = sampleLocationsEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetSampleLocationsEnableEXT(VkCommandBuffer commandBuffer, VkBool32* sampleLocationsEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (sampleLocationsEnable) *sampleLocationsEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetColorBlendAdvancedEXT(VkCommandBuffer commandBuffer, uint32_t firstAttachment, uint32_t attachmentCount, const VkColorBlendAdvancedEXT* pColorBlendAdvanced) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // firstAttachment
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    if (pColorBlendAdvanced) {
        int arrSize = attachmentCount ? attachmentCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkColorBlendAdvancedEXT(&pColorBlendAdvanced[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetColorBlendAdvancedEXT(VkCommandBuffer commandBuffer, uint32_t firstAttachment, uint32_t attachmentCount, const VkColorBlendAdvancedEXT* pColorBlendAdvanced, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = firstAttachment;
    bufferOffset += 4;
    *(uint32_t*)(outputBuffer + bufferOffset) = attachmentCount;
    bufferOffset += 4;

    if (pColorBlendAdvanced) {
        int arrSize = attachmentCount ? attachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkColorBlendAdvancedEXT(&pColorBlendAdvanced[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkColorBlendAdvancedEXT(&pColorBlendAdvanced[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdSetColorBlendAdvancedEXT(VkCommandBuffer commandBuffer, uint32_t* firstAttachment, uint32_t* attachmentCount, const VkColorBlendAdvancedEXT* pColorBlendAdvanced, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (firstAttachment) *firstAttachment = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (attachmentCount) *attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkColorBlendAdvancedEXT* arrValues = pColorBlendAdvanced;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkColorBlendAdvancedEXT(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pColorBlendAdvanced = NULL;
}

static inline int vt_sizeof_vkCmdSetProvokingVertexModeEXT(VkCommandBuffer commandBuffer, VkProvokingVertexModeEXT provokingVertexMode) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // provokingVertexMode
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetProvokingVertexModeEXT(VkCommandBuffer commandBuffer, VkProvokingVertexModeEXT provokingVertexMode, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkProvokingVertexModeEXT*)(outputBuffer + bufferOffset) = provokingVertexMode;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetProvokingVertexModeEXT(VkCommandBuffer commandBuffer, VkProvokingVertexModeEXT* provokingVertexMode, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (provokingVertexMode) *provokingVertexMode = *(VkProvokingVertexModeEXT*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetLineRasterizationModeEXT(VkCommandBuffer commandBuffer, VkLineRasterizationModeEXT lineRasterizationMode) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // lineRasterizationMode
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetLineRasterizationModeEXT(VkCommandBuffer commandBuffer, VkLineRasterizationModeEXT lineRasterizationMode, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkLineRasterizationModeEXT*)(outputBuffer + bufferOffset) = lineRasterizationMode;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetLineRasterizationModeEXT(VkCommandBuffer commandBuffer, VkLineRasterizationModeEXT* lineRasterizationMode, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (lineRasterizationMode) *lineRasterizationMode = *(VkLineRasterizationModeEXT*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetLineStippleEnableEXT(VkCommandBuffer commandBuffer, VkBool32 stippledLineEnable) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // stippledLineEnable
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetLineStippleEnableEXT(VkCommandBuffer commandBuffer, VkBool32 stippledLineEnable, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = stippledLineEnable;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetLineStippleEnableEXT(VkCommandBuffer commandBuffer, VkBool32* stippledLineEnable, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (stippledLineEnable) *stippledLineEnable = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdSetDepthClipNegativeOneToOneEXT(VkCommandBuffer commandBuffer, VkBool32 negativeOneToOne) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // negativeOneToOne
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetDepthClipNegativeOneToOneEXT(VkCommandBuffer commandBuffer, VkBool32 negativeOneToOne, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkBool32*)(outputBuffer + bufferOffset) = negativeOneToOne;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdSetDepthClipNegativeOneToOneEXT(VkCommandBuffer commandBuffer, VkBool32* negativeOneToOne, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (negativeOneToOne) *negativeOneToOne = *(VkBool32*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdCopyBuffer2(VkCommandBuffer commandBuffer, const VkCopyBufferInfo2* pCopyBufferInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pCopyBufferInfo ? vt_sizeof_VkCopyBufferInfo2(pCopyBufferInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdCopyBuffer2(VkCommandBuffer commandBuffer, const VkCopyBufferInfo2* pCopyBufferInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCopyBufferInfo) {
        int itemSize = vt_sizeof_VkCopyBufferInfo2(pCopyBufferInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkCopyBufferInfo2(pCopyBufferInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdCopyBuffer2(VkCommandBuffer commandBuffer, const VkCopyBufferInfo2* pCopyBufferInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCopyBufferInfo) vt_unserialize_VkCopyBufferInfo2(pCopyBufferInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCopyBufferInfo) pCopyBufferInfo = NULL;
}

static inline int vt_sizeof_vkCmdCopyImage2(VkCommandBuffer commandBuffer, const VkCopyImageInfo2* pCopyImageInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pCopyImageInfo ? vt_sizeof_VkCopyImageInfo2(pCopyImageInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdCopyImage2(VkCommandBuffer commandBuffer, const VkCopyImageInfo2* pCopyImageInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCopyImageInfo) {
        int itemSize = vt_sizeof_VkCopyImageInfo2(pCopyImageInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkCopyImageInfo2(pCopyImageInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdCopyImage2(VkCommandBuffer commandBuffer, const VkCopyImageInfo2* pCopyImageInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCopyImageInfo) vt_unserialize_VkCopyImageInfo2(pCopyImageInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCopyImageInfo) pCopyImageInfo = NULL;
}

static inline int vt_sizeof_vkCmdBlitImage2(VkCommandBuffer commandBuffer, const VkBlitImageInfo2* pBlitImageInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pBlitImageInfo ? vt_sizeof_VkBlitImageInfo2(pBlitImageInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdBlitImage2(VkCommandBuffer commandBuffer, const VkBlitImageInfo2* pBlitImageInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pBlitImageInfo) {
        int itemSize = vt_sizeof_VkBlitImageInfo2(pBlitImageInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkBlitImageInfo2(pBlitImageInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdBlitImage2(VkCommandBuffer commandBuffer, const VkBlitImageInfo2* pBlitImageInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pBlitImageInfo) vt_unserialize_VkBlitImageInfo2(pBlitImageInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pBlitImageInfo) pBlitImageInfo = NULL;
}

static inline int vt_sizeof_vkCmdCopyBufferToImage2(VkCommandBuffer commandBuffer, const VkCopyBufferToImageInfo2* pCopyBufferToImageInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pCopyBufferToImageInfo ? vt_sizeof_VkCopyBufferToImageInfo2(pCopyBufferToImageInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdCopyBufferToImage2(VkCommandBuffer commandBuffer, const VkCopyBufferToImageInfo2* pCopyBufferToImageInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCopyBufferToImageInfo) {
        int itemSize = vt_sizeof_VkCopyBufferToImageInfo2(pCopyBufferToImageInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkCopyBufferToImageInfo2(pCopyBufferToImageInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdCopyBufferToImage2(VkCommandBuffer commandBuffer, const VkCopyBufferToImageInfo2* pCopyBufferToImageInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCopyBufferToImageInfo) vt_unserialize_VkCopyBufferToImageInfo2(pCopyBufferToImageInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCopyBufferToImageInfo) pCopyBufferToImageInfo = NULL;
}

static inline int vt_sizeof_vkCmdCopyImageToBuffer2(VkCommandBuffer commandBuffer, const VkCopyImageToBufferInfo2* pCopyImageToBufferInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pCopyImageToBufferInfo ? vt_sizeof_VkCopyImageToBufferInfo2(pCopyImageToBufferInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdCopyImageToBuffer2(VkCommandBuffer commandBuffer, const VkCopyImageToBufferInfo2* pCopyImageToBufferInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCopyImageToBufferInfo) {
        int itemSize = vt_sizeof_VkCopyImageToBufferInfo2(pCopyImageToBufferInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkCopyImageToBufferInfo2(pCopyImageToBufferInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdCopyImageToBuffer2(VkCommandBuffer commandBuffer, const VkCopyImageToBufferInfo2* pCopyImageToBufferInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCopyImageToBufferInfo) vt_unserialize_VkCopyImageToBufferInfo2(pCopyImageToBufferInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCopyImageToBufferInfo) pCopyImageToBufferInfo = NULL;
}

static inline int vt_sizeof_vkCmdResolveImage2(VkCommandBuffer commandBuffer, const VkResolveImageInfo2* pResolveImageInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pResolveImageInfo ? vt_sizeof_VkResolveImageInfo2(pResolveImageInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdResolveImage2(VkCommandBuffer commandBuffer, const VkResolveImageInfo2* pResolveImageInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pResolveImageInfo) {
        int itemSize = vt_sizeof_VkResolveImageInfo2(pResolveImageInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkResolveImageInfo2(pResolveImageInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdResolveImage2(VkCommandBuffer commandBuffer, const VkResolveImageInfo2* pResolveImageInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pResolveImageInfo) vt_unserialize_VkResolveImageInfo2(pResolveImageInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pResolveImageInfo) pResolveImageInfo = NULL;
}

static inline int vt_sizeof_vkCmdSetColorWriteEnableEXT(VkCommandBuffer commandBuffer, uint32_t attachmentCount, const VkBool32* pColorWriteEnables) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // attachmentCount
    bufferSize += 4;
    bufferSize += pColorWriteEnables ? attachmentCount * 4 : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetColorWriteEnableEXT(VkCommandBuffer commandBuffer, uint32_t attachmentCount, const VkBool32* pColorWriteEnables, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = attachmentCount;
    bufferOffset += 4;

    if (pColorWriteEnables) {
        int arrSize = attachmentCount ? attachmentCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        memcpy(outputBuffer + bufferOffset, pColorWriteEnables, arrSize * 4);
        bufferOffset += arrSize * 4;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdSetColorWriteEnableEXT(VkCommandBuffer commandBuffer, uint32_t* attachmentCount, VkBool32* pColorWriteEnables, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (attachmentCount) *attachmentCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkBool32* arrValues = pColorWriteEnables;
        if (arrValues) memcpy(arrValues, inputBuffer + bufferOffset, dataSize3 * 4);
        bufferOffset += dataSize3 * 4;
    }
    else pColorWriteEnables = NULL;
}

static inline int vt_sizeof_vkCmdSetEvent2(VkCommandBuffer commandBuffer, VkEvent event, const VkDependencyInfo* pDependencyInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += event ? vt_sizeof_VkEvent(event) : 0;
    bufferSize += 4;
    bufferSize += pDependencyInfo ? vt_sizeof_VkDependencyInfo(pDependencyInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdSetEvent2(VkCommandBuffer commandBuffer, VkEvent event, const VkDependencyInfo* pDependencyInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (event) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkEvent(event, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkEvent(event);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pDependencyInfo) {
        int itemSize = vt_sizeof_VkDependencyInfo(pDependencyInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDependencyInfo(pDependencyInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdSetEvent2(VkCommandBuffer commandBuffer, VkEvent event, const VkDependencyInfo* pDependencyInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (event) {
            vt_unserialize_VkEvent(event, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkEvent(event);
        }
        else bufferOffset += vt_sizeof_VkEvent(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pDependencyInfo) vt_unserialize_VkDependencyInfo(pDependencyInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pDependencyInfo) pDependencyInfo = NULL;
}

static inline int vt_sizeof_vkCmdResetEvent2(VkCommandBuffer commandBuffer, VkEvent event, VkPipelineStageFlags2 stageMask) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize++;
    bufferSize += event ? vt_sizeof_VkEvent(event) : 0;
    bufferSize += 8; // stageMask
    return bufferSize;
}

static inline void vt_serialize_vkCmdResetEvent2(VkCommandBuffer commandBuffer, VkEvent event, VkPipelineStageFlags2 stageMask, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (event) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkEvent(event, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkEvent(event);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkPipelineStageFlags2*)(outputBuffer + bufferOffset) = stageMask;
    bufferOffset += 8;
}

static inline void vt_unserialize_vkCmdResetEvent2(VkCommandBuffer commandBuffer, VkEvent event, VkPipelineStageFlags2* stageMask, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (event) {
            vt_unserialize_VkEvent(event, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkEvent(event);
        }
        else bufferOffset += vt_sizeof_VkEvent(VK_NULL_HANDLE);
    }
    if (stageMask) *stageMask = *(VkPipelineStageFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;
}

static inline int vt_sizeof_vkCmdWaitEvents2(VkCommandBuffer commandBuffer, uint32_t eventCount, const VkEvent* pEvents, const VkDependencyInfo* pDependencyInfos) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4; // eventCount
    bufferSize += 4;
    if (pEvents) {
        for (int i = 0; i < eventCount; i++) {
            bufferSize += vt_sizeof_VkEvent(pEvents[i]);
        }
    }
    bufferSize += 4;
    if (pDependencyInfos) {
        int arrSize = eventCount ? eventCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkDependencyInfo(&pDependencyInfos[i]);
        }
    }
    return bufferSize;
}

static inline void vt_serialize_vkCmdWaitEvents2(VkCommandBuffer commandBuffer, uint32_t eventCount, const VkEvent* pEvents, const VkDependencyInfo* pDependencyInfos, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = eventCount;
    bufferOffset += 4;

    if (pEvents) {
        *(int*)(outputBuffer + bufferOffset) = eventCount;
        bufferOffset += 4;
        for (int i = 0; i < eventCount; i++) {
#ifdef VT_SERVER
            vt_serialize_VkEvent(pEvents[i], outputBuffer + bufferOffset);
#else
            VkObject* eventsObject = VkObject_fromHandle(pEvents[i]);
            vt_serialize_VkEvent((VkEvent)&eventsObject->id, outputBuffer + bufferOffset);
#endif
            bufferOffset += vt_sizeof_VkEvent(pEvents[i]);
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pDependencyInfos) {
        int arrSize = eventCount ? eventCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkDependencyInfo(&pDependencyInfos[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkDependencyInfo(&pDependencyInfos[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdWaitEvents2(VkCommandBuffer commandBuffer, uint32_t* eventCount, const VkEvent* pEvents, const VkDependencyInfo* pDependencyInfos, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (eventCount) *eventCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkEvent* arrValues = pEvents;
        for (int i = 0; i < dataSize3; i++) {
            uint64_t eventsId;
            vt_unserialize_VkEvent((VkEvent)&eventsId, inputBuffer + bufferOffset, memoryPool);
            if (arrValues) {
#ifdef VT_SERVER
                arrValues[i] = VkObject_fromId(eventsId);
#else
                VkObject* eventsObject = VkObject_create(VK_OBJECT_TYPE_EVENT, eventsId);
                arrValues[i] = VkObject_toHandle(eventsObject);
#endif
                bufferOffset += vt_sizeof_VkEvent(arrValues[i]);
            }
            else bufferOffset += vt_sizeof_VkEvent(VK_NULL_HANDLE);
        }
    }
    else pEvents = NULL;

    int dataSize4 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize4 > 0) {
        VkDependencyInfo* arrValues = pDependencyInfos;
        for (int i = 0; i < dataSize4; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkDependencyInfo(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pDependencyInfos = NULL;
}

static inline int vt_sizeof_vkCmdPipelineBarrier2(VkCommandBuffer commandBuffer, const VkDependencyInfo* pDependencyInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pDependencyInfo ? vt_sizeof_VkDependencyInfo(pDependencyInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdPipelineBarrier2(VkCommandBuffer commandBuffer, const VkDependencyInfo* pDependencyInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pDependencyInfo) {
        int itemSize = vt_sizeof_VkDependencyInfo(pDependencyInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkDependencyInfo(pDependencyInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdPipelineBarrier2(VkCommandBuffer commandBuffer, const VkDependencyInfo* pDependencyInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pDependencyInfo) vt_unserialize_VkDependencyInfo(pDependencyInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pDependencyInfo) pDependencyInfo = NULL;
}

static inline int vt_sizeof_vkQueueSubmit2(VkQueue queue, uint32_t submitCount, const VkSubmitInfo2* pSubmits, VkFence fence) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += queue ? vt_sizeof_VkQueue(queue) : 0;
    bufferSize += 4; // submitCount
    bufferSize += 4;
    if (pSubmits) {
        int arrSize = submitCount ? submitCount : 0;
        for (int i = 0; i < arrSize; i++) {
            bufferSize += 4;
            bufferSize += vt_sizeof_VkSubmitInfo2(&pSubmits[i]);
        }
    }
    bufferSize++;
    bufferSize += fence ? vt_sizeof_VkFence(fence) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkQueueSubmit2(VkQueue queue, uint32_t submitCount, const VkSubmitInfo2* pSubmits, VkFence fence, char* outputBuffer) {
    int bufferOffset = 0;

    if (queue) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueue(queue, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueue(queue);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = submitCount;
    bufferOffset += 4;

    if (pSubmits) {
        int arrSize = submitCount ? submitCount : 0;
        *(int*)(outputBuffer + bufferOffset) = arrSize;
        bufferOffset += 4;
        for (int i = 0; i < arrSize; i++) {
            int itemSize = vt_sizeof_VkSubmitInfo2(&pSubmits[i]);
            *(int*)(outputBuffer + bufferOffset) = itemSize;
            bufferOffset += 4;
            vt_serialize_VkSubmitInfo2(&pSubmits[i], outputBuffer + bufferOffset);
            bufferOffset += itemSize;
        }
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (fence) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkFence(fence, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkFence(fence);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkQueueSubmit2(VkQueue queue, uint32_t* submitCount, const VkSubmitInfo2* pSubmits, VkFence fence, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (queue) {
            vt_unserialize_VkQueue(queue, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueue(queue);
        }
        else bufferOffset += vt_sizeof_VkQueue(VK_NULL_HANDLE);
    }
    if (submitCount) *submitCount = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        VkSubmitInfo2* arrValues = pSubmits;
        for (int i = 0; i < dataSize3; i++) {
            int itemSize = *(int*)(inputBuffer + bufferOffset);
            bufferOffset += 4;
            if (arrValues) {
                vt_unserialize_VkSubmitInfo2(&arrValues[i], inputBuffer + bufferOffset, memoryPool);
            }
            bufferOffset += itemSize;
        }
    }
    else pSubmits = NULL;

    if (inputBuffer[bufferOffset++]) {
        if (fence) {
            vt_unserialize_VkFence(fence, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkFence(fence);
        }
        else bufferOffset += vt_sizeof_VkFence(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkCmdWriteTimestamp2(VkCommandBuffer commandBuffer, VkPipelineStageFlags2 stage, VkQueryPool queryPool, uint32_t query) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 8; // stage
    bufferSize++;
    bufferSize += queryPool ? vt_sizeof_VkQueryPool(queryPool) : 0;
    bufferSize += 4; // query
    return bufferSize;
}

static inline void vt_serialize_vkCmdWriteTimestamp2(VkCommandBuffer commandBuffer, VkPipelineStageFlags2 stage, VkQueryPool queryPool, uint32_t query, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(VkPipelineStageFlags2*)(outputBuffer + bufferOffset) = stage;
    bufferOffset += 8;

    if (queryPool) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkQueryPool(queryPool, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkQueryPool(queryPool);
    }
    else outputBuffer[bufferOffset++] = 0;
    *(uint32_t*)(outputBuffer + bufferOffset) = query;
    bufferOffset += 4;
}

static inline void vt_unserialize_vkCmdWriteTimestamp2(VkCommandBuffer commandBuffer, VkPipelineStageFlags2* stage, VkQueryPool queryPool, uint32_t* query, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
    if (stage) *stage = *(VkPipelineStageFlags2*)(inputBuffer + bufferOffset);
    bufferOffset += 8;

    if (inputBuffer[bufferOffset++]) {
        if (queryPool) {
            vt_unserialize_VkQueryPool(queryPool, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkQueryPool(queryPool);
        }
        else bufferOffset += vt_sizeof_VkQueryPool(VK_NULL_HANDLE);
    }
    if (query) *query = *(uint32_t*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
}

static inline int vt_sizeof_vkCmdBeginRendering(VkCommandBuffer commandBuffer, const VkRenderingInfo* pRenderingInfo) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    bufferSize += 4;
    bufferSize += pRenderingInfo ? vt_sizeof_VkRenderingInfo(pRenderingInfo) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdBeginRendering(VkCommandBuffer commandBuffer, const VkRenderingInfo* pRenderingInfo, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pRenderingInfo) {
        int itemSize = vt_sizeof_VkRenderingInfo(pRenderingInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkRenderingInfo(pRenderingInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkCmdBeginRendering(VkCommandBuffer commandBuffer, const VkRenderingInfo* pRenderingInfo, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pRenderingInfo) vt_unserialize_VkRenderingInfo(pRenderingInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pRenderingInfo) pRenderingInfo = NULL;
}

static inline int vt_sizeof_vkCmdEndRendering(VkCommandBuffer commandBuffer) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += commandBuffer ? vt_sizeof_VkCommandBuffer(commandBuffer) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkCmdEndRendering(VkCommandBuffer commandBuffer, char* outputBuffer) {
    int bufferOffset = 0;

    if (commandBuffer) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkCommandBuffer(commandBuffer, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
    }
    else outputBuffer[bufferOffset++] = 0;
}

static inline void vt_unserialize_vkCmdEndRendering(VkCommandBuffer commandBuffer, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (commandBuffer) {
            vt_unserialize_VkCommandBuffer(commandBuffer, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkCommandBuffer(commandBuffer);
        }
        else bufferOffset += vt_sizeof_VkCommandBuffer(VK_NULL_HANDLE);
    }
}

static inline int vt_sizeof_vkGetShaderModuleIdentifierEXT(VkDevice device, VkShaderModule shaderModule, VkShaderModuleIdentifierEXT* pIdentifier) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize++;
    bufferSize += shaderModule ? vt_sizeof_VkShaderModule(shaderModule) : 0;
    bufferSize += 4;
    bufferSize += pIdentifier ? vt_sizeof_VkShaderModuleIdentifierEXT(pIdentifier) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetShaderModuleIdentifierEXT(VkDevice device, VkShaderModule shaderModule, VkShaderModuleIdentifierEXT* pIdentifier, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (shaderModule) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkShaderModule(shaderModule, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkShaderModule(shaderModule);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pIdentifier) {
        int itemSize = vt_sizeof_VkShaderModuleIdentifierEXT(pIdentifier);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkShaderModuleIdentifierEXT(pIdentifier, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetShaderModuleIdentifierEXT(VkDevice device, VkShaderModule shaderModule, VkShaderModuleIdentifierEXT* pIdentifier, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    if (inputBuffer[bufferOffset++]) {
        if (shaderModule) {
            vt_unserialize_VkShaderModule(shaderModule, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkShaderModule(shaderModule);
        }
        else bufferOffset += vt_sizeof_VkShaderModule(VK_NULL_HANDLE);
    }

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pIdentifier) vt_unserialize_VkShaderModuleIdentifierEXT(pIdentifier, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pIdentifier) pIdentifier = NULL;
}

static inline int vt_sizeof_vkGetShaderModuleCreateInfoIdentifierEXT(VkDevice device, const VkShaderModuleCreateInfo* pCreateInfo, VkShaderModuleIdentifierEXT* pIdentifier) {
    int bufferSize = 0;
    bufferSize++;
    bufferSize += device ? vt_sizeof_VkDevice(device) : 0;
    bufferSize += 4;
    bufferSize += pCreateInfo ? vt_sizeof_VkShaderModuleCreateInfo(pCreateInfo) : 0;
    bufferSize += 4;
    bufferSize += pIdentifier ? vt_sizeof_VkShaderModuleIdentifierEXT(pIdentifier) : 0;
    return bufferSize;
}

static inline void vt_serialize_vkGetShaderModuleCreateInfoIdentifierEXT(VkDevice device, const VkShaderModuleCreateInfo* pCreateInfo, VkShaderModuleIdentifierEXT* pIdentifier, char* outputBuffer) {
    int bufferOffset = 0;

    if (device) {
        outputBuffer[bufferOffset++] = 1;
        vt_serialize_VkDevice(device, outputBuffer + bufferOffset);
        bufferOffset += vt_sizeof_VkDevice(device);
    }
    else outputBuffer[bufferOffset++] = 0;

    if (pCreateInfo) {
        int itemSize = vt_sizeof_VkShaderModuleCreateInfo(pCreateInfo);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkShaderModuleCreateInfo(pCreateInfo, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }

    if (pIdentifier) {
        int itemSize = vt_sizeof_VkShaderModuleIdentifierEXT(pIdentifier);
        *(int*)(outputBuffer + bufferOffset) = itemSize;
        bufferOffset += 4;
        vt_serialize_VkShaderModuleIdentifierEXT(pIdentifier, outputBuffer + bufferOffset);
        bufferOffset += itemSize;
    }
    else {
        *(int*)(outputBuffer + bufferOffset) = 0;
        bufferOffset += 4;
    }
}

static inline void vt_unserialize_vkGetShaderModuleCreateInfoIdentifierEXT(VkDevice device, const VkShaderModuleCreateInfo* pCreateInfo, VkShaderModuleIdentifierEXT* pIdentifier, char* inputBuffer, MemoryPool* memoryPool) {
    int bufferOffset = 0;

    if (inputBuffer[bufferOffset++]) {
        if (device) {
            vt_unserialize_VkDevice(device, inputBuffer + bufferOffset, memoryPool);
            bufferOffset += vt_sizeof_VkDevice(device);
        }
        else bufferOffset += vt_sizeof_VkDevice(VK_NULL_HANDLE);
    }

    int dataSize2 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize2 > 0) {
        if (pCreateInfo) vt_unserialize_VkShaderModuleCreateInfo(pCreateInfo, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize2;
    }
    else if (pCreateInfo) pCreateInfo = NULL;

    int dataSize3 = *(int*)(inputBuffer + bufferOffset);
    bufferOffset += 4;
    if (dataSize3 > 0) {
        if (pIdentifier) vt_unserialize_VkShaderModuleIdentifierEXT(pIdentifier, inputBuffer + bufferOffset, memoryPool);
        bufferOffset += dataSize3;
    }
    else if (pIdentifier) pIdentifier = NULL;
}

#endif